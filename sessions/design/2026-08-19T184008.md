---
short-id: 6bec1f7a
description: Reacquire and redesign the Nix and operating-system skills
last-edited: 2026-08-19T18:52:44+02:00
---

## Objective

Reacquire the psyche context and repository history around the Nix and operating-system skills, including material from commit `01a01998`, then show the living psyche the observed deployed shapes, tensions, proposals, and anatomy questions before any edit.

## Governing direction

- No skill edit without the living psyche approving the exact proposal.
- Authored skill sources live under `Curriculum skills`; generated consumer trees are evidence only.
- Keep observations, hypotheses, and unknowns separate.

## State

Decision bead `primary-mm6` is claimed. Three read-only Luna exploration subflows are acquiring: (1) exact psyche expressions and prior design forks, (2) authored/history evidence around `01a01998` plus reference-skill prior art, and (3) generated/deployed variants and manifest selection. No skill edit is authorized.

## Findings so far

- No Nix-specific Intent entry was found. The governing written psyche is Spirit plus topic-scoped Vision.
- Direct Vision establishes a minimal `flake.nix` as entry point, centralized package customization, Lojix ownership in the operating system, typed Lojix/meta-Lojix deployment interfaces, and a distinction between structured-data reconciliation and lifecycle management.
- Direct Vision does not yet settle remote-builder doctrine, live-switch versus boot-only deployment, rollback/data-migration semantics, or the authority wording currently attached to reboot and emergency runtime mutation.
- Older situational root-SSH recovery language sits in tension with later typed-CLI-only deployment language. The later same-host SSH correction says the route should be improved, not characterized as rejected.

## Recovered incident

`01a01998` is the short ID of the 2026-08-19 realization session, not a Curriculum commit. That session produced Curriculum revision `68fd64d2`. The session summarized a two-skill proposal but silently made and deployed additional material decisions: it selected a three-skill dependency anatomy, activated the previously inactive `nix-input-upgrade`, changed `operating-system`, and decided what belonged in generic training versus repository policy. When challenged, the agent acknowledged that the exact wording and those choices had not been reviewed. Curriculum `70fd140d` later restored the pre-redesign sources and deactivated `nix-input-upgrade`.

## Version state

- The active primary workspace still pins `68fd64d2`; its `.agents` and `.claude` generated Nix skills match that revision.
- Published primary `main` pins current Curriculum `70fd140d`, but external runtime activation was not witnessed.
- `68fd64d2` deploys three disjoint triggers: `nix-workflow`, `nix-input-upgrade`, and `operating-system`; both specialists depend on the workflow foundation.
- `70fd140d` deploys a broad overlapping `nix-workflow` and standalone `operating-system`; `nix-input-upgrade` remains authored but inactive and contains incident-specific prose.

## Proposed design direction, not ruled

Prefer three capabilities: a shared/direct `nix-workflow` foundation, a minimal `nix-input-upgrade` specialization, and an `operating-system` specialization depending on the foundation. Keep generic declarative and evidence doctrine in the foundation; keep input/provider/lock-graph rules in input upgrade; keep operating-system authority and activation boundaries in operating-system. Move setup-specific builder and Lojix mechanics to workspace-owned instruction or explicit skill variables rather than universal training.

## Open forks for the living psyche

1. Whether the capability anatomy is three skills, two skills, or a different phase-based split.
2. Whether `operating-system` owns both declarative source changes and deployment/activation, or only source changes.
3. Whether reboot and emergency runtime mutation always require explicit psyche approval, and whether that is Vision or broader Intent.
4. Whether remote-builder-only execution is universal doctrine or setup-specific policy.
5. Whether the generic skills name only evidence boundaries while the CriomOS workspace owns Lojix/meta-Lojix protocol.
6. Whether the later typed-CLI-only interface supersedes the older root-SSH/manual-reload recovery language or leaves an emergency fallback.
