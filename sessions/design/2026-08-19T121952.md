---
short-id: 7c3f0c1d
description: Resume skills / context strata conversation; psyche critique of last context-strata skill
last-edited: 2026-08-19T14:30:00+02:00
---

# Objective

Recover the prior design conversation on skills and context strata and resume it.
Psyche opening ruling (verbatim):

> the description is bad. and LLM should appear in the skill, possibly in the description, which should also indicate that this skill is rarely ever needed

# State recovered

- No context-strata skill file exists anywhere (Curriculum skills, generated
  trees, manifests, git history). The draft the psyche critiques is the last
  one shown in session `358f143a` (transcript line 672, 2026-08-18T15:49Z);
  its description was "The authority of text in a flow's context must be
  known, or text must be placed where it binds." Never answered, session ended.
- Settled in 358f143a: strata not rungs; top/middle/bottom stratum; promotion;
  harness seizure; "anchored" dropped; middle stratum is the typed prompt,
  source unknown to the model; Codex left out of the draft (unverified).
- Prior art (Reference skill collections): anthropics/skills `claude-api`
  states render order `tools → system → messages` and a role:system message in
  `messages[]` as an "operator-authority" channel — a claim from a reference
  skill, not verified in code. No reference description phrases rarity.

# Rulings logged this session (all pushed)

- gradientsOfAuthority.md: the strata description is bad; LLM should appear in
  the skill, possibly the description; description should say rarely needed.
- roleDescriptions.md (new topic): "A missed detail changes the conclusion."
  is really bad (read-critical line in Curriculum manifests/role-descriptions.dotos).
- session-log.md: remove last-edited and the time in the session filename.

# Landed (all pushed)

- Role descriptions approved and applied: read demanding "…Assemble it from
  reading the parts."; read critical "Reading cannot settle it. A probe must be
  designed and run."; write critical "Several parts must change together, and
  a choice in one constrains the others." Curriculum commit 71705c7ab on branch
  `role-descriptions-by-shape` (not main — needs landing); primary regenerated
  (bedb318da, by a concurrent session; adafbfbfe empty).
- `verified/` ledger created (b08850b2b): README, claude-code-context.md (8
  facts, 2.1.231), codex-context.md (5 facts, rust-v0.145.0). No prior flow
  verified the API role/block of typed prompt, skills, system-reminders, tool
  results — reading (b) of the strata skill needs one fresh verification.
- Rulings logged: roleDescriptions.md ×3, verifiedInformation.md ×2,
  gradientsOfAuthority.md ×4, session-log.md ×3, psycheLogStructure.md ×2.
- Curriculum `nix flake check` has a pre-existing failure
  `role-cross-product-manifests` (expects trivial = claude-haiku-4-5 /
  gpt-5.4-mini; role-depths.dotos now has gpt-5.6-luna) — not from this work.

# Awaiting the psyche

1. Strata skill: cut-down description "Designing or implementing something
   that depends on where text enters an LLM's context. Almost never arises in
   ordinary task work." + body with "Universal invariants go here." and last
   line "…is verified information, not read from docs."
2. Session-log: unified distilled statement (one subject) → skill body; index
   without date, append-only, tail = latest.
3. `session-log` user-only? Needs: how /design+/subflows+/spirit reached this
   session (generator dependencies are reference-only; not expansion).
4. psyche-distillation skill proposal (text in transcript).
5. behavior line: "Verified information is kept in the verified ledger, one
   fact with every verification appended under it; it is read before anything
   is verified again."
6. First distillation run: archive the 8 session-log records to
   psyche-archive/2026-08-19.md with ids; sessionLogging.md goes.

# Notes

- Repo was on a detached HEAD at origin/main; fast-forwarded `main` and
  checked it out before pushing.
- Mis-tiered one subflow (read-critical for a one-file extraction); the psyche
  ruled the tier line itself bad → role lines redone by shape of work.
