---
short-id: 7c3f0c1d
description: Resume skills / context strata conversation; psyche critique of last context-strata skill
last-edited: 2026-08-19T12:58:00+02:00
---

# Objective

Recover the prior design conversation on skills and context strata and resume it.
Psyche opening ruling (verbatim):

> the description is bad. and LLM should appear in the skill, possibly in the description, which should also indicate that this skill is rarely ever needed

# State recovered

- No context-strata skill file exists anywhere (Curriculum skills, generated
  trees, manifests, git history). The draft the psyche critiques is the last
  one shown in session `358f143a` (transcript line 672, 2026-08-18T15:49Z);
  its description was "The authority of text in a flow's context must be
  known, or text must be placed where it binds." Never answered, session ended.
- Settled in 358f143a: strata not rungs; top/middle/bottom stratum; promotion;
  harness seizure; "anchored" dropped; middle stratum is the typed prompt,
  source unknown to the model; Codex left out of the draft (unverified).
- Prior art (Reference skill collections): anthropics/skills `claude-api`
  states render order `tools → system → messages` and a role:system message in
  `messages[]` as an "operator-authority" channel — a claim from a reference
  skill, not verified in code. No reference description phrases rarity.

# Rulings logged this session (all pushed)

- gradientsOfAuthority.md: the strata description is bad; LLM should appear in
  the skill, possibly the description; description should say rarely needed.
- roleDescriptions.md (new topic): "A missed detail changes the conclusion."
  is really bad (read-critical line in Curriculum manifests/role-descriptions.dotos).
- session-log.md: remove last-edited and the time in the session filename.

# Open forks awaiting the psyche

1. Strata description: shape (i) two-incident trigger + "almost never in
   ordinary task work" vs (ii) "Rarely: only when text is being placed into an
   LLM's context on purpose…". "LLM in the skill": (a) naming only vs (b) ground
   strata in the LLM's message roles/blocks — (b) needs code verification first.
   Question asked: is the reader the design aspect only, or also workers?
2. Role descriptions: (a) rewrite read/write critical lines as task shapes vs
   (b) cut the critical read tier. Write critical's "too late to be cheap" half
   has the same flaw.
3. Session-log skill: proposed filename `<short-id>.md`, drop `short-id:` and
   `last-edited:` from frontmatter; existing files not renamed. Alternative
   `<date>-<short-id>.md`. Also: duplicate Vision topics sessionLogging.md and
   session-log.md — proposed fold into session-log.md.

# Notes

- Repo was on a detached HEAD at origin/main; fast-forwarded `main` to HEAD and
  checked it out before pushing.
- Mis-tiered one subflow (read-critical for a one-file verbatim extraction);
  psyche ruled the tier line itself bad — see roleDescriptions.md.
