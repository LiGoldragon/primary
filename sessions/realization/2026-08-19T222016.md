---
short-id: 01a01bac
description: Move the root deployment guidance proposal to operating-system
last-edited: 2026-08-22T12:56:32+02:00
---

## Objective

Resume realization session `01a01a93`, recover the problems its deployment work exposed, and propose the missing operating-system guidance. Do not create or edit a Lojix skill.

## Governing direction

The living psyche ruled that a Lojix skill is nonsensical and that the guidance belongs in operating-system. Skill editing still requires explicit psyche approval after the proposal.

## Open work

- The psyche superseded the operating-system-only placement: create a Lojix skill that properly documents Lojix, then reference it from operating-system.
- The Lojix skill must explain the exact strict Dotos/Datom syntax of its interface. Recover that grammar and the actual Lojix forms before drafting.
- The psyche ruled the any-brace source validator and brace-free generated-output checker quackery. Remove that checker contract rather than replace it with a narrower brace checker.
- Propose the exact skill-designing line that teaches Curriculum target-template syntax, preserve conditional rendering itself, and continue the approved Lojix realization after exact approval of this revised prerequisite.
- Do not reboot or perform an emergency runtime mutation. Bead `primary-68d` tracks realization and proof.

## Recovered failure classes

- CompleteHost deployments 14 and 15 reused target-user transport. They built but could not mutate the system or boot profiles. Deployment 16 succeeded through the witnessed root store URI and SSH destination.
- The immutable deployment required the pushed consumer revision and its pushed producer pins.
- Admission and build were not completion. Terminal status 4 followed a real system switch to generation 163.
- Lojix controller records stayed at generation 7 while the live system and profile links were generation 163. Controller state was not live-target evidence.
- The psyche's direct deploy request authorized deployment without another transport confirmation, but did not authorize reboot or emergency runtime mutation.

## Superseded operating-system-only proposal

Superseded by the 2026-08-20 ruling to create a proper Lojix skill and reference it from operating-system. Retained as history; do not implement it.

Replace the authored `operating-system.md` with:

```md
---
description: The declarative operating system or its deployment is being changed.
dependencies: [nix-workflow]
---

This is a declarative operating system. Change it through the owning declarative source named in the workspace's `AGENTS.md`, then rebuild.

Never use a runtime mutation as a deployment path.

Submit deployments through `meta-lojix` and observe them through `lojix` until terminal. Admission is not completion.

`Deploy.Host` with `CompleteHost` uses `CompleteHostDeploymentStoreUri` and `CompleteHostDeploymentSshDestination`; `Deploy.UserEnvironment` uses `UserEnvironmentDeploymentStoreUri` and `UserEnvironmentDeploymentSshDestination`.

A direct deployment request authorizes the configured transport; do not ask for transport confirmation.

Set source revision policy to `RequireImmutable`. Deploy only a pushed consumer revision whose pinned producers are pushed.

After any terminal failure, inspect the target profile, runtime links, and activation evidence before stating what is live. Controller state is not live target state.

Require explicit psyche approval before a reboot or emergency runtime mutation.
```

Rename the two generic target-user variables and add the witnessed CompleteHost pair:

```text
UserEnvironmentDeploymentStoreUri: ssh-ng://li@ouranos
UserEnvironmentDeploymentSshDestination: li@ouranos
CompleteHostDeploymentStoreUri: ssh-ng://root@ouranos.goldragon.criome
CompleteHostDeploymentSshDestination: root@ouranos.goldragon.criome
```

The generic variables have no authored-source or manifest consumers. `operating-system` is already active. The no-Lojix-skill scope below is superseded.

## Strict syntax recovery

Current `lojix` and `meta-lojix` parse their sole CLI argument directly through `dotos::DotosSource::parse`; the separate Datom crate documentation is not their executable grammar. The Lojix skill must therefore teach the current Dotos contract inline rather than use JSON-like or angle-bracket placeholders.

Required syntax teaching recovered from source and tests:

- exactly one inline Dotos object; no flags, subcommands, files, or extra arguments;
- glued variant application `Head.Payload`;
- positional, parenthesized product fields rather than named fields;
- vectors `[a b]`, bare `None`, unary `Some.X`, and the bare-versus-curly string rule;
- exact field order for every documented request form;
- exact executable request and reply examples, including the distinction between `DeployAccepted` admission and terminal deployment state.

Session `01a01a93` contains exact production witnesses for `Deploy.UserEnvironment` and `Query.ByNode`, but no exact production `Deploy.Host` command. A fixture supplies the Host field order, not production transport values. No production Host example may be invented from placeholders or incomplete transport evidence.

The complete proposal preserves the existing Lojix authority split, one-object input rule, sockets, startup boundary, schema refusal/reset policy, explicit transport, proposal validation, bootstrap boundary, OS-only placement, CLI-only interface, and admission semantics. It corrects the stale pseudo-Dotos examples and explains the strict current syntax and every public ordinary/owner request family. Auxiliary configuration, inspection, reset, and bootstrap syntax remain documented as separate ingress contracts rather than being silently removed.

The proposal must not claim that the current thin `lojix` CLI streams subscription events: `WatchDeployments` and `WatchCacheRetention` return a subscription token and commit marker, but the executable reads one reply and exits. Terminal observation therefore re-queries by deployment or event log. The proposed operating-system change adds only a `lojix` dependency and one reference sentence; its two existing rules remain verbatim.
