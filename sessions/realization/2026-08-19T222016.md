---
short-id: 01a01bac
description: Move the root deployment guidance proposal to operating-system
last-edited: 2026-08-19T22:29:11+02:00
---

## Objective

Resume realization session `01a01a93`, recover the problems its deployment work exposed, and propose the missing operating-system guidance. Do not create or edit a Lojix skill.

## Governing direction

The living psyche ruled that a Lojix skill is nonsensical and that the guidance belongs in operating-system. Skill editing still requires explicit psyche approval after the proposal.

## Open work

- Await explicit psyche approval, amendment, or rejection of the exact operating-system and variable proposal below. Bead `primary-68d` records the approval boundary.

## Recovered failure classes

- CompleteHost deployments 14 and 15 reused target-user transport. They built but could not mutate the system or boot profiles. Deployment 16 succeeded through the witnessed root store URI and SSH destination.
- The immutable deployment required the pushed consumer revision and its pushed producer pins.
- Admission and build were not completion. Terminal status 4 followed a real system switch to generation 163.
- Lojix controller records stayed at generation 7 while the live system and profile links were generation 163. Controller state was not live-target evidence.
- The psyche's direct deploy request authorized deployment without another transport confirmation, but did not authorize reboot or emergency runtime mutation.

## Approval-pending proposal

Replace the authored `operating-system.md` with:

```md
---
description: The declarative operating system or its deployment is being changed.
dependencies: [nix-workflow]
---

This is a declarative operating system. Change it through the owning declarative source named in the workspace's `AGENTS.md`, then rebuild.

Never use a runtime mutation as a deployment path.

Submit deployments through `meta-lojix` and observe them through `lojix` until terminal. Admission is not completion.

`Deploy.Host` with `CompleteHost` uses `CompleteHostDeploymentStoreUri` and `CompleteHostDeploymentSshDestination`; `Deploy.UserEnvironment` uses `UserEnvironmentDeploymentStoreUri` and `UserEnvironmentDeploymentSshDestination`.

A direct deployment request authorizes the configured transport; do not ask for transport confirmation.

Set source revision policy to `RequireImmutable`. Deploy only a pushed consumer revision whose pinned producers are pushed.

After any terminal failure, inspect the target profile, runtime links, and activation evidence before stating what is live. Controller state is not live target state.

Require explicit psyche approval before a reboot or emergency runtime mutation.
```

Rename the two generic target-user variables and add the witnessed CompleteHost pair:

```text
UserEnvironmentDeploymentStoreUri: ssh-ng://li@ouranos
UserEnvironmentDeploymentSshDestination: li@ouranos
CompleteHostDeploymentStoreUri: ssh-ng://root@ouranos.goldragon.criome
CompleteHostDeploymentSshDestination: root@ouranos.goldragon.criome
```

The generic variables have no authored-source or manifest consumers. `operating-system` is already active; no manifest change is proposed. No Lojix skill or Lojix skill activation is proposed.
