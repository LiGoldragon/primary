---
short-id: 759fc98a
description: Recover and repair the recently upgraded user environment
last-edited: 2026-08-18T11:57:48+02:00
---

The psyche asked realization to mine the recent Claude flow and its subagents concerning the user-environment upgrade, then use that recovered context to address the now-broken environment. Reported symptoms: no status bar, no application launcher, the shell/session layer basically does not work, and further breakage is likely. Management will dispatch reconstruction, psyche acquisition, and live diagnosis in parallel. No ruling has landed yet; the problem report is a request for repair, not a psyche ruling.

Named skills active for the full session: management, session-log, spirit, psyche, psyche-interraction, and psyche-acquisition. Forks will use guaranteed non-Sol worker models and must read `NON_MANAGEMENT_AGENTS.md`.

At 17:15, the Claude-flow miner identified the main upgrade transcript as `/home/li/.claude/projects/-home-li-primary/d076fe3a-76a5-430d-8926-76b7f9a3566b.jsonl`, spanning August 14–17. Early evidence says the psyche asked for a CriomOS-pinned user-environment generation to be built and deployed locally. Generation 962 was reportedly built and activated around August 16 19:58 as a standalone Home Manager activation, not via `nixos-rebuild`. This evidence was forwarded to the live repair worker with a request to verify the active profile and running user-session state.

At 17:16, environment/config acquisition established that the intended desktop stack is Niri with Noctalia/QuickShell providing the single status bar and application launcher, and Ghostty as the terminal. The generated configuration still declares `noctalia-shell` and `mako`, with Mod+D/Space bound to Noctalia IPC, but the live session contains only Niri: Noctalia, QuickShell, Waybar, and Mako are absent. Niri's process environment lacks `WAYLAND_DISPLAY`, `DISPLAY`, `NIRI_SOCKET`, and `XDG_CURRENT_DESKTOP`; the journal records `wl-gammarelay` starting before the Niri socket and failing because `$XDG_RUNTIME_DIR` and `$WAYLAND_DISPLAY` were unavailable. Current hypothesis, explicitly not yet a conclusion: the regression is in session startup ordering/environment propagation rather than missing feature configuration. The Claude-flow miner was asked to trace relevant launch-wrapper and graphical-session changes.

At 17:17, raw Claude command evidence corrected the earlier activation account. The August 16 flow demonstrably ran `nix build .#independentHomeConfigurations.li.activationPackage` with input overrides, inspected the result, and removed the `result` link; the miner found no activation invocation. Therefore deployment of that result is unknown until live profile evidence establishes it. Recent authored-source changes included the Noctalia Home Manager module/API rename and forcing Niri 26.04, but did not change the older `syncSessionEnvironment` startup sequence. That sequence has a documented past Noctalia startup regression and imports several graphical variables but not `NIRI_SOCKET` or `XDG_RUNTIME_DIR`. The correction and historical leads were sent to the repair worker.

At 17:18, acquisition found a direct and definitive explanation for the missing status bar and launcher: the installed profile provides the executable `noctalia`, but the authored Niri startup at `CriomOS-home/modules/home/profiles/min/niri.nix` still invokes `noctalia-shell`. Niri's PATH contains the profile bin, so this is an executable-name mismatch following the Noctalia option migration, not a PATH failure. Noctalia owns both the bar and launcher, and its absence also makes the launcher IPC keybindings inert. This is distinct from the session-environment ordering defect. The repair worker was directed to update the authored source and all consumers, rebuild/deploy, and verify the live result.

At 17:18, deeper mining of the main transcript corrected the activation uncertainty: lines 805–806 show `/nix/store/nr653fdzg7i748mmmwi4md6xbfyw2847-home-manager-generation/activate` running at 2026-08-16T19:58:05.972Z and completing seconds later. The user systemd session was already degraded before activation (`agent-intercom-fleet-cleanup.service` and `app-pulseaudio@autostart.service` failed); activation restarted Wayland-dependent units including `wl-gammarelay-rs.service`, and `message-daemon.service` subsequently hit its start limit. These were added to the live audit with the requirement to distinguish pre-existing failures from upgrade-caused regressions.

Psyche acquisition found no Intent or Vision ruling choosing a desktop or window manager. Relevant direct Vision requires the user environment and OS pins to remain synchronized, warns that the host environment was untrustworthy during recovery, requires the fix to be universal in CriomOS-home without bird/zeus hardwiring, and requires setup-independent interfaces. Accordingly, the established Niri plus Noctalia architecture is being restored without changing that product choice. Zsh itself is structurally and operationally healthy; evidence indicates the psyche's forgotten “shell” is Noctalia Shell, the graphical shell whose executable was renamed.

At 17:21, complete chronology localized the regression window. The August 14 generation 159 and 160 system switches upgraded Niri 25.05.1 to 26.04 and Noctalia to 5.0.0 while explicitly preserving the old Niri processes. The psyche rebooted at 2026-08-14T15:36:30.944Z, necessarily creating the first fresh graphical session under the new stack. Thus the visible regression fits that reboot/new-session boundary, not the later August 16 Home Manager activation and not the older generation 962. The active August 16 generation used CriomOS commit `b0f0c2c9817a`, pinning CriomOS-home `6f89e88`, whose ancestry contains the August 14 Noctalia option rename without the corresponding Niri startup executable rename.

The live repair worker is implementing the universal authored-source correction, validating a possible Ghostty regression, auditing the failed user units, and must deploy through the established Lojix/meta-Lojix interface rather than an ad hoc activation.

At 17:22, the psyche supplied decisive new chronology: “I just rebooted. youre the first flow after my reboot. my shell was working before I rebooted”. This is a process observation, not a psyche ruling. The current state is therefore the first fresh post-reboot session, and Noctalia Shell worked immediately before the reboot. The evidence was sent to the repair worker with a request to preserve and re-audit current boot/session state before manual startup or activation. This is consistent with the stale Niri spawn command being exposed only at fresh startup while a previously running Noctalia process masked it; the independent session lifecycle defect remains under investigation.

The repair worker pushed CriomOS-home commit `b86c56146d12`, deriving the absolute current Noctalia executable and adding a desktop-shell launch check; it also disabled Ghostty automatic shell integration because it found the injection loses `ZDOTDIR` while managed Zsh already loads integration. CriomOS commit `b75caa399113` pins and locks that home commit. Syntax, formatting, and a direct static witness passed. The published Home check is currently blocked before the new check by a pre-existing flake contract-pin assertion mismatch. Nothing has been deployed yet because deployment must use the typed Lojix/meta-Lojix interface.

Fresh audit also found other failed user services: agent-intercom fleet cleanup lacks `@earendil-works/pi-ai`; a duplicate/obsolete PulseAudio X11 autostart coexists with PipeWire; message-daemon sends an obsolete Dotos request shape; and Orchestrate retains a Sema schema mismatch. A dedicated worker is investigating these without touching the desktop-shell repair, while a read-only worker resolves the exact Lojix/meta-Lojix deployment request from local cluster data.

At 17:25, fresh-session evidence became conclusive. The current system booted at 17:09 and the Wayland session began at 17:09:23. The active Home generation remains `/nix/store/nr653…` from August 16 and contains the obsolete startup command. Niri 26.04 started at 17:09:24 and Mako started successfully, but no Noctalia/QuickShell startup scope or process exists; later launcher keypresses created only failed Noctalia IPC scopes. This proves the stale executable invocation causes the absent bar and launcher in the fresh session.

The worker also disproved its tentative Ghostty hypothesis: the actual Zsh child has `GHOSTTY_ZSH_ZDOTDIR=/home/li/.config/zsh`, and a controlled contract test restores `ZDOTDIR`, loads the managed Zsh config, and provides the expected aliases. It removed the Ghostty change. The final narrow repair is CriomOS-home `f00bac90b657`, pinned by CriomOS `1c0172eb2f07`; it changes only Noctalia startup. Deployment remains pending discovery of the authoritative cluster proposal/typed request record required by the Lojix `UserEnvironment ActivateNow` interface.

At 17:28, the exact grammar-validated deployment request was reconstructed except for three intentionally request-owned routing fields: the Nix SSH store URI, SSH activation destination, and optional builder. Identity and source are established as goldragon/ouranos/li, the goldragon Dotos proposal, immutable CriomOS `1c0172eb2f0761ddc7bfdfa8ca02eb2530a6c09f`, Horizon, selector `(homeConfigurations.li.activationPackage)`, `HomeManagerNixProfileV1`, `ActivateNow`, and `RequireImmutable`. The Lojix query API exposes only an old rejected host record and architecture forbids deriving routing from node identity. Workers continue searching local transcripts/history for an authoritative prior user-environment request; no deployment request has been sent.

The broader service audit proved three universal source defects: message-daemon emits the obsolete Dotos configuration-write request shape; Agent Intercom packaging omits the Pi peers imported by fleet cleanup; and CriomOS exposes `pulseaudioFull` in `environment.systemPackages`, causing an obsolete XDG PulseAudio autostart while PipeWire Pulse is active. The worker is implementing and testing these. Orchestrate's persistent Sema failure instead follows removal of its July schema migration path; persistent-store mutation is held pending an exact migration proposal and explicit psyche approval.

At 17:34, the psyche rejected the historically reconstructed deployment route as needless circular transport: “youre already user li, thats just adding ssh overhead to go in a complete circle right back to where you started. not that its a grave mistake, but its useless churn.” The proposed root-mediated SSH deployment was not submitted. This ruling was recorded verbatim in `psyche/Vision/setupIndependentInterfaces.md`. A follow-up audit is locating why agents learned the wrong route, the minimal authored skill/training correction, and the Lojix type/runtime invariant that should reject or make unrepresentable a local user deployment which leaves the machine through SSH only to return and switch back to the caller.

Final handoff: the visible desktop shell was restored live by having Niri spawn the installed `noctalia` executable. This transient process supplies the bar and launcher only until logout/reboot. The complete durable environment repair is published as CriomOS `6274a9436536209bb4442d1baf0ed1141f7fb2c0`, pinning CriomOS-home `f939dcb66cd34af130d281d023fb2725b22b64a6`. Its ancestry repairs the Noctalia v5 executable and native IPC migration, makes Nix executable dependencies package-derived, records the psyche's selected auto/wallpaper/m3-rainbow theme, sets the bar end margin to zero, restores GTK primary-selection paste, supplies Agent Intercom's Pi peer packages, updates message-daemon's Dotos request shape, and removes obsolete PulseAudio X11 autostart while retaining PipeWire Pulse. Focused checks passed; full checks remain blocked by pre-existing contract-pin/materialized-input conditions. The active machine generation is still the old broken generation because Lojix cannot currently represent a local user deployment without mandatory remote transport; the circular root-SSH request was rejected. The proposed product repair is a typed `Local | Remote` deployment transport with configured daemon user/host admission checks.

The session-log skill was redesigned with stable start-time filenames, `last-edited` frontmatter, and a material handoff-state update test. Authored Curriculum `c7103090fc1cd1caaf5c5cd4906fd5fbb3927973` and primary deployment `2d87696a2c520230c7924bbb4be37c8541615118` are published and verified. Generated artifacts were regenerated, never hand-edited. Official Codex research confirmed selected skills load the full `SKILL.md`; Codex user-only invocation is enforced by companion `agents/openai.yaml`, while Claude/Pi use `disable-model-invocation`.

Open design decisions: approve the proposed five-line `nix-workflow` executable-boundary rule; decide whether Hexis should trigger immediate Noctalia reload after continuous surgical reconciliation; approve implementation of Hexis watcher/syntax-preserving/race-safe product work; approve the typed local Lojix route so the published environment can be deployed; and approve the separately prepared Orchestrate schema-evolution restoration before touching its fail-closed store. No agent forks remain open.

Later handoff correction and activation: the psyche deferred all Lojix redesign to a dedicated future session and authorized a one-time local hot activation. The first immutable build exposed a real Stylix/Noctalia option-priority conflict; the repair was published as CriomOS-home `da4d5a785042961d9091fc7d4a9c2de5729c98d5` and CriomOS `e39bfc838d8ee65ead5e48e4b82a0b9710dbc207`, narrowly making the declared theme leaves override Stylix and adding a pinned-module composition check. Prometheus built the activation package and `/nix/store/blqbfshp24iwypsz44i252w1h885jgsy-home-manager-generation/activate` completed locally as `li`. No OS activation occurred.

The current Niri session accepted `action spawn` requests but executed neither the selected Noctalia binary nor an absolute marker command and logged no error. With explicit live-recovery authority, Noctalia was restored in temporary collecting user scope `noctalia-live-recovery-e39.scope`. The bar is live and full width; native Mod+D/Mod+Space launcher IPC works; theme is auto/light with wallpaper m3-rainbow; Dconf primary paste is enabled. The scope lasts only for the current session and can be stopped with `systemctl --user stop noctalia-live-recovery-e39.scope`. A fresh Niri session is expected to exercise the corrected startup command but has not been attempted. Remaining OS-only PulseAudio cleanup awaits normal OS deployment. Orchestrate remains deliberately fail-closed pending a dedicated migration ruling.

The psyche corrected the Hexis design boundary: Hexis is a structured-data editor, not a service manager. The proposed watcher/reload ownership was rejected and recorded in `psyche/Vision/surgicalDataEditor.md`; inspection confirms current Hexis contains one-shot activation/pre-launch integration but no watcher, daemon, supervised service, or application reload hook. The executable rule proposal was rewritten in concrete Nix terms after the psyche rejected the earlier abstract “execution boundary” wording; no Nix skill edit has been authorized or made. No agent forks remain open.

Lockout incident diagnosis: the framebuffer recovery was actually TTY2 access in Codex session `01a01127-ddc6-72f3-99a8-9f938e94c6fe`. Niri PID 2009 and graphical session 3 never crashed or restarted. The prior generation's stale QuickShell wrapper failed at 20:37:15, but the then-running Noctalia successfully established the lock surface at 20:37:29. During the authorized hot-activation flow, our worker deliberately terminated that Noctalia process cleanly at 21:05:41 while Niri/logind still held the session lock. The transient replacement started at 21:08:12 with `locked:false` and did not recreate the already-existing lock surface, leaving Niri securely locked with only its tinted background and cursor. At 21:27:07 the framebuffer recovery issued direct `noctalia msg session lock`; Niri logged `locking session (replacing existing dead lock)`, the authentication surface returned, and the original Niri session later unlocked. No reboot, logout, compositor restart, or process termination restored access.

Current read-only snapshot: Niri remains healthy and unlocked, but Noctalia is no longer running. Niri reports no layer-shell surfaces, Noctalia IPC sockets are stale filesystem entries without kernel sockets, and the two Noctalia-named transient scopes contain unrelated descendant applications. Therefore there is currently no proven bar, launcher, or Noctalia lock surface. Separate current source defect: `criomos-sync-session-environment` omits `NIRI_SOCKET` even though the post-lock service calls `niri msg action power-off-monitors`; its failure is hidden with `|| true`. The current user manager happens to contain `NIRI_SOCKET`, most plausibly from Niri `--session`, but that setter is unlogged. Niri's live `action spawn` also previously returned success while executing nothing; its cause remains unknown without a fresh session/debugging. No diagnostic subflows remain open and no change was made during this diagnosis.

Resource incident: the framebuffer-recovery subflow launched an unbounded PCRE ripgrep tool call at 21:45:46 CEST over all 2026 Codex and Claude JSONL transcripts. Its first search PID 111182 ran for 699 seconds at roughly 1,184% CPU with 13 threads and read about 198 MB before explicit SIGTERM; it exited within 2.5 seconds. The wrapper then advanced to a second similarly expensive search PID 126881 at roughly 1,157% CPU; it and the wrapper shell exited before the follow-up termination authorization arrived. Repeated verification found no remaining `rg`, wrapper, process-group member, or respawn. Origin: transcript `01a01141-0f95-72d0-9110-7697c7ef40ab`, tool call `call_cwLakIvsEYZPNeoDBWdgtGOQ`. The owning Codex/terminal processes were not terminated.

Niri design correction: the earlier conclusion that live `niri msg action spawn` was broken is withdrawn. Exact pinned Niri 26.04 source shows IPC always returns `Handled`, but `do_action` silently suppresses `Spawn`/`SpawnSh` while the compositor is Locking or Locked; all failed spawn probes occurred during the 20:37–21:28 lock. `spawn-at-startup` is a distinct compositor-start path that bypasses that guard. Niri therefore behaved as designed. Current immediate recovery can use the selected Noctalia executable through Niri IPC now that the session is unlocked, without restarting the compositor.

Permanent repair design: current CriomOS-home disables Noctalia's systemd service and uses unsupervised Niri `spawn-at-startup`, while Home Manager activation can stop obsolete services and Niri does not replay startup on reload. The lock helper has no readiness/confirmed-lock contract, suppresses errors, omits `NIRI_SOCKET` from its local importer, and sleep locking hardcodes a Wayland display. A supervised Noctalia user service is the recommended owner, but safe locking additionally requires either an upstream Noctalia lifecycle contract (readiness, requested/confirmed lock phases, relock intent across restart) or an independently owned locker/fallback. Activation must not restart/remove the only authentication surface while the session is locked. No source or live change was made during this design audit.

Late live recovery: at the psyche's request before sleep, the exact Noctalia 5.0.0 executable selected by the active generated Niri configuration was started through Niri's supported IPC while the session was unlocked. Scope `app-niri-noctalia-168986.scope` is active; fresh Noctalia IPC sockets and the `noctalia-bar-widgets` layer prove the shell is live. The active idle policy fades for five seconds, powers the display off after 300 seconds, locks after 3600 seconds, and does not suspend. No mutable idle override exists. Niri, the session, configuration, and lock state were not restarted or changed; keyboard, touchpad, or mouse input wakes the display.

Idle-policy correction: the preceding claim that the policy was active was false. The generated configuration used five obsolete v4 flat `idle.*` fields; pinned Noctalia v5 warned that all were unknown, seeded only disabled default behaviors, and therefore never registered a screen-off timer. The live process remained healthy overnight and no systemd idle inhibitor existed, isolating the failure to ignored configuration. The v5 migration was published as CriomOS-home `b8d1a664bb62d09998ed6454971f2aec15081b69` and CriomOS `096eb748d67c16c5d5f945636de2237b60dc2fca`. A focused remote-built check asserts exact v5 behavior tables, removal of legacy idle keys, and fails on unknown `idle.*` validator warnings; two unrelated legacy bar warnings remain visible and out of scope.

The immutable Home generation was built remotely from transcript-recovered materialized ouranos user-environment inputs and activated locally as `li`; Niri and Noctalia were not restarted or locked. At 01:33:43 Noctalia reloaded the config and logged registration of `screen-off` at 300 seconds and `lock` at 3600 seconds with a five-second pre-action fade. Effective TOML contains the exact v5 actions, mutable state has no idle override, Niri IPC works, and there are zero idle validation warnings. No physical blanking was forced; the remaining proof is an observed real five-minute idle transition. Input wakes the display.

Daytime red-screen recovery: the session was unlocked and Noctalia healthy, ruling out the dead-lock-surface incident. The active gamma owner `wl-gammarelay-rs` retained prior output-removal/gamma-control failure state; probing an invalid D-Bus property path reproduced an upstream unwrap panic, after which systemd restarted it and it reacquired eDP-1's 1024-entry ramp. Supported IPC then explicitly set temperature 6500 K, gamma 1.0, and brightness 1.0. The relay and Chroma remained active without post-reset errors; Chroma's current daylight policy was Cold. Niri, session processes, and persistent configuration were untouched. Physical pixel neutrality still requires the psyche's visual confirmation.

Addendum — 01a0145c — 2026-08-18. Read-only recheck and handoff persistence; the prior body and frontmatter are unchanged.

Observation: the earlier desktop repair, native Noctalia IPC, Noctalia v5 idle-schema migration, Message request-shape repair, Agent Intercom Pi-peer packaging, and Hexis single-shot/structured-data boundary are already addressed in the preceding authored/deployed evidence. No new implementation or deployment was performed here. Psyche acquisition found no relevant Intent entry. The governing Vision still says local user deployment must not leave the machine through root SSH to return to local `li`; Hexis is an editor, not a service manager.

Correction and boundary: the former “no local Lojix route” conclusion is superseded in meaning by the current `lojix` `schema_runtime` local user-activation fast path and its tested witness. This corrects the old handoff’s semantic claim, but does not prove a fresh live deployment or authorize redesign. The appropriate existing owners remain `primary-mjl.2` and `primary-mjl.6`; persistent-store migration or deletion remains destructive and unauthorized.

Current observations: Orchestrate remains partially failed at startup on the persistent Sema claim-schema registration mismatch; its existing coordination bead remains the owner, and no store mutation is authorized. The running OS predates the authored PulseAudio cleanup, so the obsolete PulseAudio autostart still fails alongside active PipeWire; this needs normal OS deployment, not a new design. The session-environment importer omits `NIRI_SOCKET`, while the lock helper calls `niri msg` and hides failures; the current manager happens to contain the variable, so this is a future-failure risk rather than a proven present defect. Noctalia remains unsupervised `spawn-at-startup` with no established readiness, lock-recovery, restart, or ownership anatomy.

Proof gaps: a real five-minute physical screen-off and post-gamma physical visual confirmation remain unproved; two unrelated non-idle Noctalia widget warnings remain. The caller’s proposed new Agent Intercom hardcoded-FHS/repeated-failure finding is not present in the durable evidence rechecked here and is therefore unknown, not recorded as fact. Product/design choices for Noctalia lifecycle, physical proof, and any new Intercom repair still need authority. No implementation, service, deployment, persistent-state, or psyche-log change occurred.

Correction addendum — 01a0145c — 2026-08-18T12:26:45+02:00. The preceding Intercom classification is superseded by a bounded live recheck. Deployed `/home/li/.pi/agent/packages/agent-intercom-orchestrator/src/file-lock.ts` resolves to `/nix/store/ncmbqjyccyjji9ghvmlpjgv1a5clqw78-agent-intercom-0.10.0/share/agent-intercom/orchestrator/src/file-lock.ts`; lines 3–5 still hardcode `/usr/bin/flock`, `/bin/sh`, and `/usr/bin/cat`. `/usr/bin/flock` and `/usr/bin/cat` are absent; `/bin/sh` currently exists as a symlink to Nix `mksh`. The enabled `agent-intercom-fleet-cleanup.timer` triggers its static `agent-intercom-fleet-cleanup.service` every 15 minutes, and the service fails with `Could not start /usr/bin/flock` at each observed run from 08:14:47 through 12:14:50 CEST, including the requested 11:59:49 window. This is confirmed-current service failure and confirmed-current deployed-code FHS risk, not merely historical or unconfirmed. No service action or implementation occurred.

Tracking correction — 01a0145c — 2026-08-18: child bead `primary-fuf.1` now owns the narrowly scoped NixOS FHS-path repair/proof, with successful cleanup-unit and repository Nix-check acceptance; no implementation is claimed.
