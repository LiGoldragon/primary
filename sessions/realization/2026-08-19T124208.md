---
short-id: 01a01998
description: Follow current yt-dlp throughout the Nix user environment
last-edited: 2026-08-19T15:06:42+02:00
---

## Objective

Add an independently updating yt-dlp input to the user-environment flake, make the nixpkgs package set resolve yt-dlp to it so consumers such as mpv use the same executable, and validate the resulting evaluation and deployment.

## Governing direction

- Keep `flake.nix` minimal as an entry point.
- Centralize package customization so future agents can update one dependency safely.
- Research current public Nix agent instructions and flake-structure guidance before settling the shape.
- Preserve existing peer work, use the repository workflow, and commit and push the result.

## Handoff state

Bead `home-3kf` is closed. CriomOS-home commits `062561b4e29f2b08729f86bf33ca3f2c2e1f7417` and `20b53335be4243555a9568062487454a96e202ee` are pushed and ancestors of current remote `main` `0225e261ce074583a7ef1dfa161afc95ea2e73bc`. They add the independently locked upstream yt-dlp source, central overlay registry, production-wiring check, and minimal agent convention without changing the lunar nixpkgs pin.

The lock selects upstream master `0d1e029d14058ddfb986fdafb07c00869c89105a`, 35 commits ahead of nixpkgs' packaged source while still truthfully reporting `2026.07.04`. Independent positive and negative evaluations witness that the durable check uses the exact Home package set. The focused derivation built on the configured remote builder, mpv discovered yt-dlp through runtime `PATH`, and current Lojix generation 9 selects Home `0225e261`; the active profile contains yt-dlp.

The repository-wide check attribute remains independently blocked by a stale mentci-egui assertion, tracked as `home-2wc`. A read-only `Query.ByDeployment.(9)` exposed a separate Lojix worker panic, tracked and pushed as `lojix-7f0`; the service remained active and was not restarted. Generation 9's selector and builder provenance are not exposed by ordinary Lojix records, so no claim is made about which builder produced that already-current activation.

## Continued objective

The psyche has ruled Mentci-egui deprecated and requested its removal from Home, then explicitly cleared deployment. The same round must mine recent Nix sessions, assess the existing authored `nix-workflow` skill, design and implement the terminal minimal Nix skill shape in Curriculum, regenerate the configured consumer evidence from authored sources/manifests, remove Mentci-egui from CriomOS-home, validate both changes, push them, and deploy the resulting Home environment.
