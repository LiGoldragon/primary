---
short-id: 01a0193f
description: Restore the Listener status-bar widget for Noctalia v5
last-edited: 2026-08-19T11:15:00+02:00
---

The psyche directly authorized restoring the STT Listener widget promptly. The
current v4 QML plugin is declared and its listener socket is healthy, but the
pinned Noctalia v5 runtime filters it because it requires plugin.toml and Luau.

Work is delegated to a bounded implementation lane. The recovery will retain
the existing visual/state/socket behavior, update its durable source contract,
then pin and deploy only through the declared Lojix user-environment route.
The documented Orchestrate request grammar was rejected before it could record
the session lane; this is treated as the unavailable-tool fallback.

The port is now pushed as CriomOS-home `23aaa932481c595b7e50f0b8cb0972177a98eaf5`;
its direct check was built successfully only on the configured Prometheus
remote builder. CriomOS `b0409d58a3b657be5f7057937fef495b8755765a` pins that
Home revision without changing the independently pre-existing broad
schema-rust gate.

Deployment is intentionally held. The current Lojix source confirms the
dotted 13-field UserEnvironment grammar, but the routing/identity values for
the new revision are only a historical candidate and are absent from the
declared setup-variable source (`SKILL_VARIABLES`; no `AGENT_VARIABLES` file
exists). The source also does not select independent versus embedded Home
activation. No SSH, Lojix request, activation, Noctalia reload, or recording
occurred.
