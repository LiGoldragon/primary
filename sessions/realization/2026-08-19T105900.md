---
short-id: 01a0193f
description: Restore the Listener status-bar widget for Noctalia v5
last-edited: 2026-08-19T11:26:00+02:00
---

The psyche directly authorized restoring the STT Listener widget promptly. The
current v4 QML plugin is declared and its listener socket is healthy, but the
pinned Noctalia v5 runtime filters it because it requires plugin.toml and Luau.

Work is delegated to a bounded implementation lane. The recovery will retain
the existing visual/state/socket behavior, update its durable source contract,
then pin and deploy only through the declared Lojix user-environment route.
The documented Orchestrate request grammar was rejected before it could record
the session lane; this is treated as the unavailable-tool fallback.

The port is now pushed as CriomOS-home `23aaa932481c595b7e50f0b8cb0972177a98eaf5`;
its direct check was built successfully only on the configured Prometheus
remote builder. CriomOS `b0409d58a3b657be5f7057937fef495b8755765a` pins that
Home revision without changing the independently pre-existing broad
schema-rust gate.

Deployment is intentionally held. The current Lojix source confirms the
dotted 13-field UserEnvironment grammar, but the routing/identity values for
the new revision are only a historical candidate and are absent from the
declared setup-variable source (`SKILL_VARIABLES`; no `AGENT_VARIABLES` file
exists). The source also does not select independent versus embedded Home
activation. No SSH, Lojix request, activation, Noctalia reload, or recording
occurred.

The authorized correction supplied the immutable source form with `?rev=`.
The second exact dotted request was admitted as Lojix deployment 3. The Lojix
daemon journal then witnessed its terminal failure at `CopyClosure`; no Home
activation, new generation, generated widget files, Noctalia reload, or
recording followed. A malformed ordinary status query also logged a
WireShapeError panic in one daemon worker; the daemon itself remained active.
No further query grammar was guessed.

The approved one-time SSH-agent bootstrap was applied narrowly to
`lojix-daemon.service` through systemd's runtime edit interface, then removed
with `systemctl revert` after the permanent service wrapper was active. Host
deployment 6 used an accidentally fabricated full `e518` revision and reached
terminal `FlakeAuth`; it made no system switch. The corrected Host deployment
7 used the repository-witnessed
`e518e91d64337b9b18bb93e2e390d9488ea4f0dd` with the root activation
destination. Journal/system-profile evidence shows a switch at 12:42:49–
12:42:50 to
`/nix/store/57razdn3ykyimpqnn9nkh92p67df5j1k-nixos-system-ouranos-26.11.20260813.0e251e2`,
without reboot. After reverting the temporary drop-in and restarting only the
daemon, its active process had
`SSH_AUTH_SOCK=/run/user/1001/gnupg/S.gpg-agent.ssh`; this is supplied by the
new permanent wrapper, not manager state or a remaining runtime override.

The same-revision UserEnvironment deployment 8 reached the daemon's terminal
state at 12:46:51 and advanced Home Manager to
`/nix/store/8qskcs3wwj4p8qigy0qr5sn8yc736cfr-home-manager-generation`. Its
generated Noctalia files install canonical `criomos/listener-level` plugin TOML
and rendered Luau with an absolute store `socat`; the config enables that
plugin, uses `criomos/listener-level:level`, and inserts `listener-level` in
the bar. With the local Wayland session active and unlocked and the li-owned
GPG SSH-agent socket live, the source-schema-supported `noctalia msg
config-reload` returned `ok`; no shell restart or GUI help probe occurred.
The runtime registry lists `criomos/listener-level [local] 1.0.0 enabled`,
Niri lists the `noctalia-bar-widgets` top layer on eDP-1, and a passive listener
status-socket read returned idle level 0.0 with zero in-flight work. No audio
was recorded. Compositor introspection does not expose individual bar children,
so visible user confirmation remains the final proof gap.

The first deployed v5 plugin still did not render because the pinned Noctalia
source accepts `[bar.main]` `start`, `center`, and `end` string lanes, not the
obsolete `[bar.widgets]` `left`/`right` object lanes. The existing runtime log
proved those fields were ignored. CriomOS-home
`0225e261ce074583a7ef1dfa161afc95ea2e73bc` replaces them with the exact
working order: start `launcher`, `clock`, `media`; center `workspaces`; end
`listener-level`, `tray`, `battery`, `volume`, `brightness`, `control-center`.
It preserves the canonical Listener alias/type and maps the two supported v4
widget-option equivalents (`tray.drawer`, `battery.display_mode`). The already
unregistered v4 `solar-time` and `active-network` lane entries are excluded;
their old source/assets are retained and they were neither removed nor ported.
The listener and generated-config composition derivations evaluated and built
only with the configured remote builder, yielding
`/nix/store/krf8mwnyf8xy9pq7dmgsfn4cmy6dfrj8-listener-level-widget` and
`/nix/store/lbynlf9anlh5l29g6pzppf30rlirvq2g-noctalia-settings-composition`.
The unrelated broad flake gate remains blocked at its existing
`2af0aa5a... != f8b86ffe...` witness.

CriomOS `b96c776ec67201cbaf00231198096ff21e55a62b` pins that Home commit.
Its exact immutable UserEnvironment deployment 9 was admitted and reached the
daemon's terminal state at 13:31:26, advancing Home to
`/nix/store/nqx54gw1w98v2vv2akagjjkgfzbzh4gj-home-manager-generation`.
The generated config validates successfully and contains the v5 lanes with no
left/right fields. After a fresh unlocked-session and agent-socket witness,
the supported `noctalia msg config-reload` returned `ok`. Its fresh log block
now records `widget.listener-level` actions and watches the installed Luau;
there are no new unknown-setting or unrecognized-widget warnings. The runtime
registry lists the plugin enabled and Niri reports the `noctalia-bar-main` top
layer. At 13:32:20 a passive status-socket read reported an already-recording
Listener capture (nonzero level); no agent invoked Mod+C, recorded audio, or
should take any further Listener action. User-visible confirmation remains
pending.
