---
short-id: 01a02356
description: Restore VSCodium and diagnose inconsistent managed-extension state
last-edited: 2026-08-21T10:31:33+02:00
---

The initial objective was read-only causal diagnosis of `criomos-codium: Managed extension state is inconsistent...`. The user then explicitly prioritized immediate restoration followed by postmortem. The tracker is `primary-g0s`.

Emergency VSCodium is live from the direct Nix-owned binary using `/tmp/criomos-codium-recovery.6UDf73Xe/{user-data,extensions}` and `--disable-extensions`; managed state is unchanged.

The exact pre-repair split was: Home/stable Claude 2.1.235; absent desired versioned link; manifest 2.1.223 pointing to an old store output named 2.1.220 whose package identifies 2.1.223; existing versioned 2.1.223 link pointing to a different output; and a GC root retaining the manifest target.

Durable owner-authenticated Nix reconciliation is being implemented, tested, and deployed. No new psyche ruling landed.

The active session-log instruction conflicts with older `psyche/Vision/session-log.md` filename/frontmatter guidance; the active skill was followed.

Unrelated dirty work was preserved and not conflated.

Final handoff: the focused Nix lifecycle check exited 0, including the three-version fixture, repeat/idempotence, and unmanaged registry/settings preservation. The Home closure build succeeded via a bounded local fallback after Prometheus cache and remote-builder SSH timeouts. The backup is at `/home/li/.local/state/criomos/vscodium-claude/recovery-20260821T101900Z`.

Nix-built activation-refresh succeeded, with live convergence to Claude 2.1.235. `codium --version` exited 0 with `1.126.04524`; a normal real-profile GUI launch exited 0 with PID 451570 under user-manager PID 2305; and `codium --status` exited 0. The registry now reports Claude 2.1.235 and ChatGPT 26.5814.41407.

CriomOS-home commit `b09ac277` is contained in main commit `9d2896c7`; the CriomOS pin `41b58b04` was pushed. Tracker `primary-g0s` is closed. The exact historical event that created the contradiction remains unknown.

By choice, the temporary isolated no-extension recovery window at `/tmp/criomos-codium-recovery.6UDf73Xe` remains live alongside the normal GUI, avoiding closure of user-visible work.
