---
short-id: 01a0145c
description: Audit and persist the recovered user-environment handoff
last-edited: 2026-08-18T12:27:56+02:00
---

Objective: re-acquire and persist the audited continuation of `sessions/realization/2026-08-17T173445.md`, while keeping observations, authority, hypotheses, and unknowns distinct. The target session was rechecked stable before this edit; its existing body was preserved and this record is a separate session pointer.

Governing psyche: no relevant Intent entry was found. Existing Vision requires setup-independent deployment and rejects a local `li` deployment that loops through root SSH; Hexis edits structured data and does not own application lifecycle. No new psyche ruling landed.

Audited state: prior desktop/native-IPC, Noctalia v5 idle schema, Message shape, Agent Intercom peer packaging, and Hexis single-shot findings are already represented as addressed in the prior evidence. The old semantic “no local Lojix route” statement is superseded by the current tested local user-activation fast path, without proving a fresh deployment. Orchestrate’s persistent Sema claim-schema registration mismatch remains partial and destructive migration/delete is unauthorized. The running OS still predates authored PulseAudio cleanup. `NIRI_SOCKET` is omitted from systemd import while the current manager happens to contain it; this is a future risk, not a proven current failure. Noctalia remains unsupervised startup with unresolved readiness/lock/restart ownership.

Unaddressed proof and authority: physical five-minute screen-off and post-gamma visual confirmation are unproved; two unrelated widget warnings remain. New Agent Intercom hardcoded-FHS/repeated-failure evidence was not found in the durable recheck and remains unknown. No relevant Intent exists for the remaining product choices. No implementation, deployment, runtime, persistent-state, or psyche-log change occurred.

Coordination and landing: Beads were read sequentially; existing owners are `primary-mjl.2`, `primary-mjl.6`, and `primary-ahk.1`. The pre-existing dirty working copy was first committed and pushed as `2d375a1ec007`. Orchestrate lane registration/observation was unavailable, so no claim was made. This session and the target addendum are the documentation/tracking deliverables for the next handoff.

Final handoff: updated `primary-mjl.2`, `primary-ahk.1`, and `primary-fuf`; created evidence-backed follow-ups `primary-mjl.7` (NIRI_SOCKET), `primary-mjl.8` (Noctalia lifecycle authority), and `primary-mjl.9` (physical idle/gamma proof). No Intercom FHS task was created because the proposed live failure evidence was absent. Documentation landed and pushed as `3d34a64e63fb`; the remote `main` bookmark matches. No implementation or runtime action was claimed.

Correction addendum — 01a0145c — 2026-08-18T12:26:45+02:00: the Intercom FHS finding is confirmed-current. The deployed `file-lock.ts` still hardcodes `/usr/bin/flock`, `/bin/sh`, and `/usr/bin/cat`; `/usr/bin/flock` and `/usr/bin/cat` are absent, while `/bin/sh` is presently available. The enabled 15-minute cleanup timer repeatedly starts the static cleanup service, which fails at `/usr/bin/flock` in the journal window 08:00–12:20 CEST, including 11:59:49 and 12:14:49. The prior “evidence absent” statement is superseded. No implementation or service action occurred.

Tracking correction — 01a0145c — 2026-08-18: child bead `primary-fuf.1` now records the repair/proof gate. The corrected handoff is confirmed-current; no implementation or service action occurred.
