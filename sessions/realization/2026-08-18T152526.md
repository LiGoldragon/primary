---
short-id: b5b1f5fe
description: Make Ghostty shortcut inherit terminal working directory.
last-edited: 2026-08-18T15:34:36+02:00
---

Direct request: make Mod+Shift+Enter open Ghostty in the last working directory, matching launcher behavior.

Initial governing evidence: no Ghostty/Niri-specific psyche expression was found. The source owner is CriomOS-home. The existing primary user-environment bead is primary-mjl.2.

Coordination: attempted to register and claim lane GhosttyInheritCwd, but the installed Orchestrate tools rejected the documented parenthesized DOTOS form, requiring an unknown brace-block wire shape. Continued under the documented unavailable-tool fallback.

Observation: Ghostty 1.3.1's `+new-window` command explicitly passes its launcher's working directory, while GTK singleton activation creates an unadorned window and the default `window-inherit-working-directory` copies the focused terminal's reported PWD. The source change therefore replaces `+new-window` with `--gtk-single-instance=true`; the check evaluates the Niri module and requires the exact generated command.

Landing: CriomOS-home commit `56a475105c80a5bd82820f44782d621ff4917b76` is pushed to `main`. Direct headless evaluation and the configured-remote-builder derivation build succeeded. Full flake check evaluation remains blocked by an unrelated existing input-pin mismatch (`2af0aa5a86fd` vs `f8b86ffeab51`). No deployment was submitted: the current CriomOS revision `096eb748` pins the earlier Home revision `b8d1a664`, so its normal typed UserEnvironment deployment cannot include this source change without an explicitly approved pin update or a verified direct-Home request shape.

Pin propagation was then explicitly authorized and landed as CriomOS `234877917963a65f0f91386adc39cc013da0a29b`, pinning Home `56a47510`. Its smallest ownership/pin witness cannot evaluate because of a separate existing Lojix revision assertion (`0d968da44bc0be8ed875b8546bebf52c3de53a81` locked versus `7693ad3e8814cbea68ea6491d6a10f04d5cb2979` expected). No remote build of that failed evaluation and no typed Lojix deployment request were submitted. Activation therefore remains blocked, and the live binding remains the old generated `+new-window` form.

Conduct correction: a `ghostty +new-window --help` probe was capable of opening the visible application and likely caused a Ghostty configuration-error dialog. No persistent Ghostty configuration or generated configuration changed. All further Ghostty runtime invocation stopped; this is an observation about conduct, not a psyche ruling.
