---
short-id: 91254d53
description: Restore the declarative Lojix SSH-agent boundary
last-edited: 2026-08-19T11:38:00+02:00
---

The authorized repair is narrowly scoped to declaratively project the selected
local user's GPG-agent SSH socket into the OS-owned Lojix system service. It
will use existing typed local-user/deployment options rather than setup values,
and will be witnessed with a focused rendered-unit check built only through
configured remote builders. No keys will be read; no activation, deployment,
restart, or reboot is authorized.

Coordination registration and claim requests were rejected by the installed
Orchestrate grammar, so this work proceeds under the documented unavailable-tool
fallback. The existing primary-mjl.2 bead is being tracked.

Read-only acquisition found the relevant Vision: Lojix "should only be in OS";
same-host SSH "should be improved" rather than rejected; and typed inputs are
required at interface boundaries. CriomOS already owns the optional
`services.lojix.sshAuthSocket` projection and has no current assignment. The
implementation lane is now establishing that assignment at the local-user
configuration layer and adding a focused rendered-unit witness.
