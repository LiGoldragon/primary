## Task
Make the browser-agent setup durable in CriomOS/CriomOS-home so Bird on Zeus can have an agent drive her browser, then push the latest fullos through the daemon to Zeus and reactivate Bird's profile.

## Done When
The setup is committed and pushed in the appropriate CriomOS repos, deployed to Zeus through the daemon, Bird's Home profile is reactivated, and browser automation availability is verified on Zeus.

## Established
- The human assigned this lane cloud-maintainer and asked for orchestrate compatibility plus a first browser-use situation report. — evidence: user@initial: 'you role is cloud-maintainer - make sure your role works with the orchestrate tool and you have your first report in on the current situation with browser-use.'; basis: user; reopen: if the human reassigns the lane or retires the cloud-maintainer setup
- The cloud-maintainer lane was added to the primary workspace registry and docs, with reports in reports/cloud-maintainer. — evidence: cmd:rg -n "cloud-maintainer" AGENTS.md orchestrate/AGENTS.md orchestrate/roles.list skills/role-lanes.md#output; basis: observed; reopen: if those files change to remove or rename cloud-maintainer
- Primary commit 7b60f43d registered the cloud-maintainer lane. — evidence: cmd:jj commit -m 'register cloud-maintainer lane'#output; basis: output; reopen: if primary history is rewritten or the commit is reverted
- The current secret policy allows agents to inspect tokens when plaintext inspection is needed for an authorized workspace task, while keeping secret values out of durable or broadly visible surfaces. — evidence: /home/li/primary/skills/secrets.md:1; basis: observed; reopen: if skills/secrets.md changes around the top secret-handling policy
- The real-profile Chrome CDP bridge is experimental and failed in practice with bad-token, port-collision, and manual attach problems. — evidence: cmd:browser-use-attach DigitalOcean task#output; basis: output; reopen: if the bridge package is patched and passes a real-profile example.com smoke test
- CriomOS-home already contains packaged browser-use and chrome-cdp-bridge sources under packages/browser-use and packages/chrome-cdp-bridge. — evidence: cmd:find /git/github.com/LiGoldragon/CriomOS-home -maxdepth 4 -type f | rg 'packages|modules/home/profiles|min|med'#output; basis: output; reopen: if those package paths are removed or renamed
- A dedicated cloud-maintainer Chrome automation profile was started locally at /home/li/.local/state/cloud-maintainer/chrome-profile with native CDP on http://127.0.0.1:9223. — evidence: cmd:curl -fsS http://127.0.0.1:9223/json/version | python3 -m json.tool#output; basis: output; reopen: if the profile is stopped, moved, or port 9223 is no longer serving Chrome CDP
- Port 9222 collided with an existing Chrome listener, so the local durable default was changed to 9223. — evidence: cmd:ss -ltnp 'sport = :9222'#output; basis: output; reopen: if 9222 becomes intentionally reserved for this profile or the documented default changes
- The dedicated Chrome profile documentation exists at protocols/cloud-maintainer-browser-profile.md and says agents attach browser-use or Playwright-style tooling directly to http://127.0.0.1:9223. — evidence: /home/li/primary/protocols/cloud-maintainer-browser-profile.md:1; basis: observed; reopen: if protocols/cloud-maintainer-browser-profile.md changes or is removed
- OpenAI gpt-4.1 with browser-use against the dedicated Chrome CDP endpoint passed the neutral example.com smoke test and returned 'heading: Example Domain'. — evidence: cmd:browser-use-python /nix/store/048jiinw3hrv0qfi8kj5cp021lhv4lda-browser-use-local-driver.py#example.com-output; basis: output; reopen: if the OpenAI key, model, browser-use driver, or Chrome profile changes and the smoke test no longer passes
- The local Gemma browser-use path was unreliable for this task, timing out or returning empty content, while direct OpenAI gpt-4.1 responded OK in about 1.2 seconds. — evidence: cmd:OpenAI gpt-4.1 chat completion probe#output; basis: output; reopen: if local Gemma serving is tuned and passes browser-use smoke tests reliably
- The latest human request is to move the setup into CriomOS so Bird can use it on Zeus, then deploy latest fullos via the daemon and reactivate Bird's profile. — evidence: user@latest: 'make sure all our setup is in criomos, so bird can also have an agent drive her browser on zeus. then push the latest fullos (through the daemon) to zeus and reactivate bird's profile'; basis: user; reopen: if the human changes the requested target host, user, or deployment path
- CriomOS-home max profile browser-use module now packages browser-agent-chrome, which uses XDG state, a per-user deterministic CDP port, and no ordinary logged-in Chrome profile. — evidence: /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/max/browser-use.nix:71; basis: observed; reopen: if modules/home/profiles/max/browser-use.nix changes around agentChrome or the max profile no longer includes it
- CriomOS-home commit f4dcf6a8 adds the dedicated browser agent Chrome profile launcher and browser-use-agent-local wrapper. — evidence: cmd:jj commit -m 'home: add dedicated browser agent chrome profile'#output; basis: output; reopen: if CriomOS-home history is rewritten or the commit is reverted
- A local Nix symlinkJoin build of the browser-use max-profile package set produced browser-agent-chrome, browser-use-agent-local, and browser-use-local; browser-agent-chrome --cdp-url returned http://127.0.0.1:9224 for uid 1001. — evidence: cmd:WITNESS=$(nix build --impure --no-link --print-out-paths --expr ...); "$WITNESS/bin/browser-agent-chrome" --cdp-url; "$WITNESS/bin/browser-agent-chrome" --status; ls "$WITNESS/bin" | rg ...#output; basis: output; reopen: if browser-use.nix or the Chromium wrapper changes
- CriomOS commit fdd07e2d pins CriomOS-home to the browser-agent-profile commit. — evidence: cmd:jj commit -m 'CriomOS: pin browser agent home profile'#output; basis: output; reopen: if CriomOS history is rewritten, the commit is reverted, or the criomos-home flake input changes away from f4dcf6a8 without an equivalent browser-agent profile
- CriomOS commit 2fd1631a makes test-vm-guest tolerate old horizons without the testVm facet by using behavesAs.testVm or false. — evidence: cmd:jj commit -m 'CriomOS: tolerate old horizons without test VM facet'#output; basis: output; reopen: if modules/nixos/test-vm-guest.nix changes around the behavesAs.testVm guard
- CriomOS commit fcd9f793 adds gitMinimal to the lojix-daemon service path so daemon flake evaluation can invoke git after deployment of that generation. — evidence: cmd:jj commit -m 'CriomOS: give lojix daemon git for flake evaluation'#output; basis: output; reopen: if modules/nixos/lojix.nix changes around systemd.services.lojix-daemon.path or the commit is reverted
- Cloud commit 7f190c36 changes the DigitalOcean gopass path consumed by the cloud wrapper from digitalocean/api-token to digitalocean.com/api-token and was pushed to main. — evidence: cmd:jj commit -m 'cloud: read DigitalOcean token from domain gopass path'; jj git push --bookmark main#output; basis: output; reopen: if /git/github.com/LiGoldragon/cloud/flake.nix changes around DIGITALOCEAN_ACCESS_TOKEN or cloud history is rewritten
- The DigitalOcean token was created through the existing CDP browser session by browser-use with OpenAI gpt-4.1, stored in gopass at digitalocean.com/api-token, and verified as 71 bytes without printing the secret. — evidence: cmd:node /tmp/cdp-store-visible-do-token.cjs && gopass show -o digitalocean.com/api-token | wc -c#output; basis: output; reopen: if digitalocean.com/api-token is removed, rotated, or fails DigitalOcean API checks
- The stored DigitalOcean token authorizes the automation-critical droplets, account/keys, and projects endpoints with HTTP 200, while /v2/account returns HTTP 403. — evidence: cmd:for endpoint in droplets account/keys projects; do curl ... https://api.digitalocean.com/v2/$endpoint; done#output; basis: output; reopen: if the token is rotated or any of those endpoint checks no longer returns the same authorization pattern
- The running dedicated Chrome CDP profile on port 9223 is signed into DigitalOcean and currently has a DigitalOcean dashboard/API tab rather than the earlier login redirect. — evidence: cmd:python3 urllib.request http://127.0.0.1:9223/json/list#output; basis: output; reopen: if the tab navigates, the profile signs out, or port 9223 stops serving Chrome CDP
- Root SSH to ouranos.goldragon.criome works non-interactively with BatchMode, enabling runtime repair of the system lojix-daemon service. — evidence: cmd:ssh -vv -o BatchMode=yes -o ConnectTimeout=10 root@ouranos.goldragon.criome 'echo root-ssh-ok'#output; basis: output; reopen: if root SSH to ouranos starts failing or requires interactive auth
- The live lojix-daemon service on ouranos has a runtime drop-in adding HOME=/home/li, a PATH containing /run/current-system/sw/bin, and an EnvironmentFile /run/lojix-env carrying NIX_CONFIG for GitHub access; /run/lojix-env is root-owned mode 0400. — evidence: cmd:ssh root@ouranos.goldragon.criome '... systemctl show lojix-daemon.service -p EnvironmentFiles ... stat -c ... /run/lojix-env'#output; basis: output; reopen: if lojix-daemon restarts without the drop-in, /run/lojix-env is removed, or the committed service generation replaces the runtime override
- Direct eval of CriomOS revision 649fa8e8 for Zeus FullOs with lojix-generated horizon/system/deployment overrides succeeds after the clavifaber lock bump. — evidence: cmd:nix eval --refresh --raw --override-input horizon path:/var/lib/lojix/generated-inputs/goldragon/zeus/full-os/horizon --override-input system path:/var/lib/lojix/generated-inputs/goldragon/zeus/full-os/system --override-input deployment path:/var/lib/lojix/generated-inputs/goldragon/zeus/full-os/deployment github:LiGoldragon/CriomOS/649fa8e8844aafbb41e74414ee22fe3c9a51abf5#nixosConfigurations.target.config.system.build.toplevel.drvPath#direct_eval_ok; basis: output; reopen: if CriomOS, clavifaber, or the generated lojix inputs change
- CriomOS commit 649fa8e8 updates the clavifaber flake input to the current nota-next stack, removing the dead/private nota-derive fetch path that blocked Zeus eval. — evidence: cmd:jj commit -m 'CriomOS: update clavifaber for current nota-next'#output; basis: output; reopen: if CriomOS flake.lock changes around clavifaber or the commit is reverted
- The latest daemon FullOs deploy request for Zeus used meta-lojix Deploy System goldragon zeus FullOs with CriomOS revision 649fa8e8, source /git/github.com/LiGoldragon/goldragon/datom.nota, action Switch, and builder prometheus; the daemon accepted it as (Deployed (2 (77 77))). — evidence: cmd:meta-lojix "(Deploy (System (goldragon zeus FullOs /git/github.com/LiGoldragon/goldragon/datom.nota github:LiGoldragon/CriomOS/649fa8e8844aafbb41e74414ee22fe3c9a51abf5 Switch (Some prometheus) [] None)))"#output; basis: output; reopen: if a newer deploy request supersedes it or the daemon reports DeployRejected or effect failed for this revision
- After the accepted 649fa8e8 deploy, the daemon progressed from nix eval to nix build of /nix/store/vycpjzlqvdca2nddf60z2kfglrz6vf38-nixos-system-zeus-26.05.20260422.0726a0e.drv with max-jobs 0 and builders @/etc/nix/machines. — evidence: cmd:poll after meta-lojix Deploy revision 649fa8e8#poll-5-output; basis: output; reopen: if subsequent polling shows deploy failure, build completion, copy, or activation

## Learned
- Use a dedicated Chrome automation profile with native remote debugging for browser agents; avoid the real-profile extension bridge for account setup until separately audited and fixed. — source: session experience: real-profile bridge failed with bad token, EADDRINUSE, and attach friction; dedicated profile on 9223 passed CDP and example.com smoke tests
- For browser-use in this workflow, gpt-4.1 is currently the reliable model; local Gemma is not ready for live provider signup/token/payment flows. — source: session experience: Gemma timed out or returned empty content, OpenAI gpt-4.1 passed chat and browser-use example.com probes
- The human wants one existing browser tab/window reused for automation, not a new window or tab per launcher invocation. — source: user@browser-profile: 'you also have to stop using a new window every time' and 'now you just started a new tab instaed'
- Browser automation setup must be packaged in CriomOS/CriomOS-home rather than left as ad-hoc files under /home/li/.local so other users such as Bird on Zeus can receive it through deploys. — source: user@latest and current file state showing /home/li/.local/bin/cloud-maintainer-chrome is local-only
- Paid OpenAI model use is allowed for this DigitalOcean/browser-use path because the human explicitly asked to try a GPT model and noted an OpenAI token in gopass. — source: user@model: 'lets try using a gpt model to do it. theres an openai token in gopass'
- For DigitalOcean in this workspace, the active gopass convention is domain-based digitalocean.com/api-token, matching cloudflare.com/token and dolthub.com/api-token; do not assume digitalocean/api-token. — source: user@do-token-path and cmd:gopass ls -f | rg -i 'github|gh|token'#output plus cloud flake path fix
- When a one-time provider token is visible in the browser, start the browser-driving/capture path immediately; delaying for repo packaging frustrates the human and risks losing the token. — source: user@browser-agent-urgency: 'how come you dont have an agent getting the token from the browseer already' and 'where the fuck is the browser-drivin agent?'
- The current lojix-daemon Deploy response shape can acknowledge request acceptance while the actual eval/build/copy/activation continues asynchronously; always poll lojix Query and daemon journal before claiming deployment success. — source: session experience: meta-lojix returned (Deployed (2 ...)) while live Query still showed generation 1 and journal later showed eval failures
- The live lojix-daemon on ouranos needed runtime git, HOME, and GitHub-token NIX_CONFIG environment to evaluate private GitHub flake dependencies; the committed gitMinimal fix alone cannot help until that generation is deployed. — source: session experience: daemon eval failed first without git, then with private nota-derive fetch errors until runtime drop-ins were applied
- A stale CriomOS clavifaber flake input can surface as a private/dead nota-derive fetch during Zeus FullOs eval; updating clavifaber to the current nota-next stack fixed direct eval. — source: cmd:nix flake update clavifaber#output and cmd:nix eval ...#direct_eval_ok

## Open
- Does Bird's packaged browser automation workflow need an OpenAI gpt-4.1 wrapper in addition to browser-use-agent-local's local-Gemma path, given prior Gemma unreliability for live provider tasks? — verifies: Inspect the packaged wrappers in CriomOS-home after deployment; either add a gpt-4.1/OpenAI wrapper that targets browser-agent-chrome CDP or prove local Gemma now passes a browser-use smoke test on Zeus.
- Did the daemon-accepted FullOs Switch deploy of CriomOS 649fa8e8 to Zeus finish build, copy, and activation successfully? — verifies: Continue polling lojix '(Query (ByNode (goldragon zeus None)))', pgrep children of lojix-daemon, and journalctl -u lojix-daemon.service from the deploy start until a new FullOs Switch Current generation appears or a DeployRejected/effect failed line appears.
- After the FullOs deploy, is Bird's Home profile reactivated on Zeus and does it include browser-agent-chrome and browser-use-agent-local? — verifies: Once Zeus reports the new FullOs generation current, check the Home Manager activation state or user profile on Zeus for Bird and verify the two browser-agent commands exist in Bird's PATH.
- Can Bird on Zeus start the packaged dedicated Chrome automation profile and expose CDP without port collisions? — verifies: On or for Zeus as Bird, run browser-agent-chrome --cdp-url and --status or start it, then curl the reported /json/version endpoint without exposing private browser state.
- Should the temporary ouranos lojix-daemon runtime EnvironmentFile /run/lojix-env and drop-in be removed after the deployed generation provides a durable service environment? — verifies: After successful deploy or an intentional durable fix, inspect systemctl cat lojix-daemon.service and remove /run/systemd/system/lojix-daemon.service.d/10-runtime-git-path.conf plus /run/lojix-env only when future daemon evals no longer need them.
- Is DigitalOcean account introspection scope required for the cloud daemon or planned automation, given the new token returns 200 for droplets, account/keys, and projects but 403 for /v2/account? — verifies: Read cloud DigitalOcean adapter requirements or run the actual cloud DigitalOcean operation; rotate the token only if an operation requires /v2/account or another unauthorized scope.

## Next
- Resume by checking the current lojix-daemon deploy progress for Zeus: query lojix ByNode, inspect children of the running lojix-daemon, and scan journalctl since the 649fa8e8 deploy for build/copy/activate or failure. → Know whether the accepted FullOs deploy completed, is still building, or failed with a concrete error.
- If the 649fa8e8 deploy failed, triage the exact daemon journal error and fix only that blocker; if it is still building, keep polling without starting duplicate deploys. → Avoid duplicate asynchronous deploy submissions and move the current daemon deployment to a terminal state.
- When the FullOs deploy is current on Zeus, verify or reactivate Bird's Home profile through the appropriate lojix/Home Manager path. → Bird's profile is active on Zeus with the latest CriomOS-home input.
- Verify Bird's profile has browser-agent-chrome and browser-use-agent-local and that the dedicated profile can serve CDP without colliding with existing ports. → The browser-agent setup is proven available for Bird on Zeus.
- Decide whether to add or use an OpenAI gpt-4.1 packaged wrapper for Bird, or prove the local-Gemma wrapper is reliable enough with a neutral smoke test. → Browser automation availability is not dependent on the previously unreliable local Gemma path unless it has been revalidated.
- Clean up or replace the temporary ouranos lojix-daemon runtime drop-in and /run/lojix-env after the durable deployed service environment is confirmed. → No unnecessary tmpfs secret/runtime override remains beyond the deployment need.
- Report succinctly to the human with DigitalOcean token status, cloud path fix commit, deploy status, and any remaining blocker or verification result. → The human gets the urgent token fix plus a clear state of the Zeus/Bird deployment work.
