## Task
Run a cargo-sweep cleanup and migrate every unclaimed/simple Rust flake possible to the shared rust-build source-cleaning helper, while preserving active claims and recent build artifacts.

## Done When
Done when the sweep has run, every safely migratable unclaimed Rust flake is tested, committed, pushed, skipped repos are justified, and the workspace is clean with claims released.

## Forbid
- Keep secrets out of chat/logs/reports; redact Nix store paths in human-facing output. — source: previous-brief:Constraints & Preferences
- Use jj; commit/push meaningful changes to main. — source: previous-brief:Constraints & Preferences
- Avoid disrupting live desktop/session state; check service idleness before restarts when relevant. — source: previous-brief:Constraints & Preferences
- Avoid disrupting active media/movie playback; do not STOP, kill, pause, or restart mpv unless explicitly authorized. — source: previous-brief:Constraints & Preferences
- Do not touch/restart Niri unless specifically needed/authorized. — source: previous-brief:Constraints & Preferences
- Do not reintroduce the old DJI hot-loop virtual sink/source. — source: previous-brief:Constraints & Preferences
- Prefer system default-source policy over Whisrs-specific source selection. — source: previous-brief:Constraints & Preferences
- Do not diagnose Chrome/browser; the issue was Chroma. — source: previous-brief:Constraints & Preferences
- Use chroma-daemon.service, not inactive/dead chroma.service. — source: previous-brief:Constraints & Preferences
- Do not rely on attaching strace to the live chroma-daemon; ptrace attach returned Operation not permitted. — source: previous-brief:Constraints & Preferences
- Do not use nix run nixpkgs#gsettings; this registry did not provide gsettings. — source: previous-brief:Constraints & Preferences
- Do not emit quotation marks in generated Chroma NOTA config; Chroma treats quotes as literal path/color characters. — source: previous-brief:Constraints & Preferences
- BTD 700 appears as a USB ALSA/PipeWire device, not a normal BlueZ bluetoothctl device. — source: previous-brief:Constraints & Preferences
- If audio stack restart is needed, restart PipeWire, PipeWire-Pulse, and WirePlumber together. — source: previous-brief:Constraints & Preferences
- Do not delete whole target/ directories as the normal cleanup path; preserve the newest useful local build artifacts with cargo-sweep size caps. — source: user@history:asked whether cleanup can avoid removing most recent builds and approved sweep
- Skip repos currently claimed by another lane unless you coordinate first. — source: cmd:/home/li/primary/tools/orchestrate status#current-turn-output

## Established
- DJI keepalive is sidecar-only: it consumes the real DJI mic to keep it awake but must not become the default or Whisrs source. — evidence: previous-brief:Key Decisions#DJI keepalive is sidecar-only; basis: user; reopen: if the human changes the DJI/Whisrs input policy or CriomOS-home dictation wiring changes
- CriomOS-home implemented DJI keepalive as dji_mic_keepalive_capture consuming bluez_input.04:A8:5A:0B:EB:B0 and dji_mic_keepalive_playback discarding to dji_mic_keepalive_sink. — evidence: previous-brief:Progress Done#CriomOS-home implements DJI keepalive; basis: observed; reopen: if CriomOS-home modules/home/profiles/min/dictation.nix or related PipeWire graph configuration changes
- Focused Whisrs/default-input checks passed for .#checks.x86_64-linux.whisrs-default-input and .#checks.x86_64-linux.whisrs-dictation-bindings. — evidence: previous-brief:Progress Done#Focused Whisrs/default-input checks passed; basis: test; reopen: if CriomOS-home dictation, Whisrs bindings, or check definitions change
- HomeOnly activation on Ouranos succeeded for CriomOS-home main containing the DJI keepalive sidecar. — evidence: previous-brief:Progress Done#HomeOnly activation on Ouranos succeeded; basis: output; reopen: if CriomOS-home main changes or Ouranos home activation is rerun and fails
- CriomOS pinned the DJI keepalive sidecar profile at CriomOS@dd391881f6c1. — evidence: previous-brief:Progress Done#CriomOS pinned the DJI keepalive sidecar profile; basis: observed; reopen: if CriomOS flake.lock or the criomos-home input changes
- Chroma root cause was quoted NOTA atoms like "/nix/store/..." in ~/.config/chroma/config.nota; Chroma treated quotes literally and hit ENOENT. — evidence: previous-brief:Progress Done#Chroma root cause found; basis: observed; reopen: if Chroma NOTA parsing semantics or generated config quoting changes
- CriomOS-home modules/home/profiles/min/chroma.nix was edited to emit bare NOTA atoms for palette colors, dconf, emacsclient, and Ghostty template paths. — evidence: /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/chroma.nix; basis: observed; reopen: if /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/chroma.nix changes around Chroma config generation
- The focused Chroma NOTA config check exists at /git/github.com/LiGoldragon/CriomOS-home/checks/chroma-nota-config/default.nix and is wired in the CriomOS-home flake. — evidence: /git/github.com/LiGoldragon/CriomOS-home/checks/chroma-nota-config/default.nix; basis: observed; reopen: if the check file or /git/github.com/LiGoldragon/CriomOS-home/flake.nix changes
- Focused Chroma check passed with nix build --no-link ".#checks.${system}.chroma-nota-config". — evidence: previous-brief:Progress Done#Focused check passed; basis: test; reopen: if CriomOS-home Chroma config generation or the check changes
- CriomOS-home pushed Chroma bare-NOTA fix commit e9b602426f12 with description emit Chroma config as bare NOTA atoms. — evidence: previous-brief:Critical Context#Final Chroma commits; basis: output; reopen: if CriomOS-home main advances past e9b602426f12 and changes Chroma generation
- CriomOS pinned the Chroma bare-NOTA config fix at commit b4747f6f369d with description pin Chroma bare NOTA config fix. — evidence: previous-brief:Critical Context#Final Chroma commits; basis: output; reopen: if /git/github.com/LiGoldragon/CriomOS/flake.lock changes its criomos-home input
- After deployment, chroma-daemon.service was active/running and chroma '(GetState)' returned (State Dark 2700 85) with GTK, Ghostty, dconf, and portal dark outputs verified. — evidence: previous-brief:Critical Context#Live fixed Chroma state after deployment; basis: output; reopen: if Chroma service state, theme state, desktop settings, or Chroma config changes
- BTD 700 was exposed as a PipeWire ALSA USB device, not a normal BlueZ bluetoothctl device. — evidence: previous-brief:Progress Done#Initial BTD/PipeWire inspection; basis: observed; reopen: if hardware path changes or BTD 700 appears as a BlueZ audio endpoint in a later inspection
- The last observed audio defaults were BTD 700 Digital Stereo IEC958 output and laptop mic input. — evidence: previous-brief:Critical Context#Current/last observed audio state; basis: observed; reopen: if PipeWire defaults change or the human reports audio is still wrong
- PipeWire, PipeWire-Pulse, and WirePlumber were restarted together and all returned ActiveState=active SubState=running. — evidence: previous-brief:Progress Done#Restarted pipewire.service pipewire-pulse.service and wireplumber.service together; basis: output; reopen: if any of those user services are restarted, fail, or become inactive
- The previous agent mistakenly stopped/paused mpv during audio stabilization, disrupting the movie; it then resumed mpv with pkill -CONT and BTD output was preserved. — evidence: previous-brief:Movie disruption context; basis: observed; reopen: if the human authorizes media disruption or mpv session state changes materially
- The original orchestrate helper failure was caused by orchestrate-cli trying to fetch deleted/stale nota-codec revision f761421c..., and the replacement dependency is nota-next at 7426a6a7d60d6e37ac40df4ecc98717af58129f8. — evidence: previous-brief:Turn Context#Original Request and Early Progress; basis: observed; reopen: if orchestrate-cli Cargo.toml or Cargo.lock changes its NOTA dependency again
- orchestrate-cli was migrated from nota-codec to nota-next, versioned 0.2.1, Rust version 1.89, and cargo build/test passed before the transport follow-up. — evidence: previous-brief:Turn Context#Early Progress; basis: test; reopen: if /home/li/primary/orchestrate-cli/Cargo.toml, Cargo.lock, or NOTA encode/decode code changes
- orchestrate helper now writes a binary rkyv StartupConfiguration to orchestrate/orchestrate-daemon.signal and starts orchestrate-daemon with that file path, matching current daemon startup requirements. — evidence: previous-brief:Turn Context#Patched orchestrate-cli daemon startup; basis: observed; reopen: if /home/li/primary/orchestrate-cli/src/daemon_client.rs or /git/github.com/LiGoldragon/orchestrate startup semantics change
- orchestrate helper transport was changed to call the current generated thin CLI subprocess at /git/github.com/LiGoldragon/orchestrate/target/release/orchestrate with PERSONA_ORCHESTRATE_SOCKET and one NOTA argument, parsing stdout as the reply. — evidence: previous-brief:Turn Context#Fixed by changing compatibility helper transport; basis: observed; reopen: if /home/li/primary/orchestrate-cli/src/daemon_client.rs or generated orchestrate CLI argument semantics change
- Direct schema CLI probe showed the current daemon accepts release as (Release system-maintainer) and rejects parenthesized/bracketed role-name variants. — evidence: cmd:PERSONA_ORCHESTRATE_SOCKET=/home/li/primary/orchestrate/orchestrate.sock /git/github.com/LiGoldragon/orchestrate/target/release/orchestrate '(Release system-maintainer)'#output; basis: output; reopen: if signal-orchestrate schema release input syntax changes
- orchestrate-cli release_request was changed to emit the schema-generated Input::Release shape, and the unit test asserts request.to_nota() == (Release operator). — evidence: /home/li/primary/orchestrate-cli/tests/argv_decode.rs; basis: observed; reopen: if /home/li/primary/orchestrate-cli/src/request.rs or tests/argv_decode.rs changes around release_request
- orchestrate-cli tests and release smoke passed after the schema release fix: cargo test passed and tools/orchestrate claim/release worked with system-maintainer.lock idle afterward. — evidence: cmd:cd /home/li/primary/orchestrate-cli && cargo test --quiet && cargo build --release --bin orchestrate --quiet && ./tools/orchestrate claim ... && ./tools/orchestrate release system-maintainer#output; basis: test; reopen: if orchestrate-cli source, signal-orchestrate schema, or orchestrate daemon changes
- Primary remote main advanced to commit 0ae84222bf4d for orchestrate helper emit schema release, and the working copy was clean afterward. — evidence: cmd:git ls-remote origin refs/heads/main && jj log -r main#output-0ae84222; basis: output; reopen: if /home/li/primary main advances and changes orchestrate-cli or tools/orchestrate
- ~/.claude.json was a zero-byte file causing JSON Unexpected EOF, and it was backed up to ~/.claude.json.empty-20260618111724.bak and replaced with a valid empty JSON object {}. — evidence: cmd:python json.load ~/.claude.json && cp -p ~/.claude.json ~/.claude.json.empty-20260618111724.bak && printf '{}\n' > ~/.claude.json#output; basis: output; reopen: if /home/li/.claude.json changes or Claude reports JSON parse errors again
- Report 15 established the durable Nix source-filter repair shape: put a prunedSourceDirectory guard before existing Crane/repo-specific filters so Nix does not descend into target, .git, .jj, .direnv, or node_modules. — evidence: /home/li/primary/reports/system-maintainer/15-nix-source-filter-target-prune-sandbox-2026-06-17.md; basis: doc; reopen: if report 15 is superseded or rust-build source-cleaning semantics change
- CriomOS-lib currently scopes itself to shared helpers/data for CriomOS and CriomOS-home and is dependency-free, so using it for all 63 Rust repos would broaden or violate its current scope. — evidence: /git/github.com/LiGoldragon/CriomOS-lib/AGENTS.md; basis: doc; reopen: if /git/github.com/LiGoldragon/CriomOS-lib/INTENT.md, AGENTS.md, or ARCHITECTURE.md changes to broaden scope
- A new rust-build repository was created and pushed as github:LiGoldragon/rust-build; its flake exports lib.${system}.fromPkgs, fromToolchainFile, craneLib, toolchain, cleanSource, and cleanCargoSource with source pruning for target, .git, .jj, .direnv, and node_modules. — evidence: cmd:git ls-remote origin refs/heads/main in /git/github.com/LiGoldragon/rust-build#output-d97af86e37c8; basis: output; reopen: if /git/github.com/LiGoldragon/rust-build/flake.nix or lib/default.nix changes
- rust-build has a source-prune policy check, and nix flake check --no-build plus nix build --no-link .#checks.x86_64-linux.source-prune-policy passed before push. — evidence: cmd:cd /git/github.com/LiGoldragon/rust-build && nix flake check --no-build && nix build --no-link .#checks.x86_64-linux.source-prune-policy#output; basis: test; reopen: if rust-build flake, lib, or check definitions change
- rust-build was extended to support custom rust-toolchain.toml pins through fromToolchainFile and pushed at commit d97af86e37c8f53d5d1c71b6002d426e90814f8b. — evidence: cmd:jj log -r main in /git/github.com/LiGoldragon/rust-build#output-d97af86e37c8; basis: output; reopen: if rust-build toolchain helper code changes
- The first migrated core repos use rust-build and have clean local/remote main matches: nota-next 7105c2be3606, schema-next b3be7d0f9161, schema-rust-next bb4dfe293e03, and spirit fe04c12cb10a. — evidence: cmd:final clean/remote verification across migrated repos#output; basis: output; reopen: if any listed repo flake.nix or flake.lock changes
- The Spirit/Agent signal repos use rust-build and were pushed: signal-spirit ee5a98edd163, meta-signal-spirit 0a10c806bb8f, signal-agent 688e8cc7, and meta-signal-agent pushed after migration. — evidence: cmd:commit/push signal-spirit meta-signal-spirit signal-agent meta-signal-agent#output; basis: output; reopen: if any listed repo flake.nix or flake.lock changes
- The orchestrate transport repos use rust-build and were pushed: signal-frame b0c2c65c3da5, signal-orchestrate 80911d1a, meta-signal-orchestrate, and orchestrate migrated in the same wave. — evidence: cmd:commit/push signal-frame signal-orchestrate meta-signal-orchestrate orchestrate#output; basis: output; reopen: if any listed repo flake.nix or flake.lock changes
- skills/rust-discipline.md now documents target directory hygiene: prefer cargo-sweep with a per-repo size cap, use dry-run for new caps/scopes, and use github:NixOS/nixpkgs/nixpkgs-unstable#cargo-sweep command forms. — evidence: /home/li/primary/skills/rust-discipline.md; basis: doc; reopen: if /home/li/primary/skills/rust-discipline.md changes around Target directory hygiene
- Primary commit e07f69d5d326b3b17a44f8da448a97f5cd0066d0 pushed the Rust discipline update prefer cargo-sweep target cleanup. — evidence: cmd:git ls-remote origin refs/heads/main && jj log -r main#output-e07f69d5; basis: output; reopen: if primary main advances and changes skills/rust-discipline.md target cleanup guidance
- System-maintainer report 16 audited rust repo hygiene after rust-build and was pushed at primary commit 1a90fa9ff52f06a176905f89a24cd74d4d58fe25. — evidence: /home/li/primary/reports/system-maintainer/16-rust-repo-hygiene-after-rust-build-2026-06-19.md; basis: doc; reopen: if a later report supersedes report 16 or new audit data changes its conclusions
- Report 16 found the migrated 12 repos improved, but workspace habit had not converged: 26 of 42 recently-worked Rust repos still exposed raw Crane source cleaning, and five flake-touched misses were cloud, mentci-egui, meta-signal-introspect, meta-signal-message, and signal-standard. — evidence: /home/li/primary/reports/system-maintainer/16-rust-repo-hygiene-after-rust-build-2026-06-19.md; basis: doc; reopen: if additional repos are migrated or a new audit scan supersedes these counts
- Current turn ran cargo-sweep recursively with --maxsize 4GB over /git/github.com/LiGoldragon and logged output to /tmp/cargo-sweep-20260619-rust-build-migration.log. — evidence: cmd:nix run github:NixOS/nixpkgs/nixpkgs-unstable#cargo-sweep -- sweep --recursive --maxsize 4GB /git/github.com/LiGoldragon#current-turn-output; basis: output; reopen: if new Rust builds run or target directory sizes are rescanned
- At the start of the current migration turn, active locks showed operator claimed meta-signal-mirror, meta-signal-spirit, mirror, signal-mirror, signal-spirit, and spirit; cloud-operator claimed CriomOS and horizon-rs; system-designer claimed lojix, meta-signal-lojix, and signal-lojix. — evidence: cmd:./tools/orchestrate status#current-turn-output; basis: output; reopen: if orchestrate locks change
- The current candidate scan excluded claimed repos and already rust-build repos, then found unclaimed dirty=false examples still using raw Crane/cleanCargoSource including WebPublish, chroma, chronos, clavifaber, cloud, criome, domain-criome, harness, introspect, and kameo-testing before output truncation. — evidence: cmd:python3 candidate scan under /git/github.com/LiGoldragon#current-turn-output; basis: output; reopen: if the candidate scan is rerun or any candidate flake changes

## Learned
- The right durable landing for the target-prune fix is a shared Rust build flake, not per-repo hand patches and not CriomOS-lib unless CriomOS-lib is intentionally broadened. — source: session reasoning anchored by /git/github.com/LiGoldragon/CriomOS-lib/AGENTS.md and human challenge about modifying the Rust flake
- rust-build consumers should call rust-build.lib.${system}.fromPkgs pkgs for standard stable toolchains, or fromToolchainFile pkgs { file = ./rust-toolchain.toml; sha256 = ...; } for pinned toolchain repos. — source: /git/github.com/LiGoldragon/rust-build/lib/default.nix
- When migrating a flake to rust-build, preserve repo-specific data inclusions as extraFilters passed to rust.cleanSource rather than deleting schema/example/script/golden filters. — source: successful migrations in nota-next, schema-next, signal-frame, signal-orchestrate, and orchestrate flakes
- cargo-sweep --maxsize preserves newest build artifacts while trimming older Cargo outputs, which satisfies the human’s requirement to avoid removing the most recent builds better than deleting target/ or cargo clean. — source: cmd:nix run nixpkgs#cargo-sweep -- sweep --help and cmd:dry-run --maxsize 8GB in /git/github.com/LiGoldragon/spirit#output
- One-time cargo-sweep is not a durable control loop; active Rust work can rebuild target usage quickly, so use a timer or closeout practice if disk pressure matters. — source: /home/li/primary/reports/system-maintainer/16-rust-repo-hygiene-after-rust-build-2026-06-19.md
- Agents obey explicit migration tasks but do not yet automatically adopt rust-build when touching flakes; add enforceable scanners/gates rather than relying only on prose guidance. — source: /home/li/primary/reports/system-maintainer/16-rust-repo-hygiene-after-rust-build-2026-06-19.md
- For orchestrate release compatibility, the generated schema CLI accepts bare role syntax (Release system-maintainer); bracketed or nested role forms are non-canonical or invalid. — source: cmd:PERSONA_ORCHESTRATE_SOCKET=/home/li/primary/orchestrate/orchestrate.sock /git/github.com/LiGoldragon/orchestrate/target/release/orchestrate release form probes#output
- When Nix refuses a new flake with Path 'flake.nix' is not tracked by Git, track new files with jj file track before evaluating. — source: cmd:cd /git/github.com/LiGoldragon/rust-build && nix flake check --no-build#error-output
- nix run github:NixOS/nixpkgs/nixpkgs-unstable#cargo-sweep is preferred in docs over nixpkgs#cargo-sweep to avoid relying on local registry state. — source: /home/li/primary/skills/rust-discipline.md
- Current Rust-flake migration should skip currently claimed repos first, then revisit after locks clear or coordination is explicit. — source: cmd:./tools/orchestrate status#current-turn-output

## Open
- Does BTD 700 audio now work for the human, and does UI freezing persist after the movie was resumed? — verifies: Ask the human without disrupting playback; if needed use non-disruptive pactl/wpctl checks only.
- Should Chroma’s own theme concern errors be improved to include failing paths/operations? — verifies: If prioritized, patch /git/github.com/LiGoldragon/chroma/src/theme.rs and add a focused test or reproduction showing path/operation context in errors.
- Which remaining active Rust flakes should be migrated after the current batch, including claimed repos once released? — verifies: Rerun a full non-truncated scan of /git/github.com/LiGoldragon for Rust flakes without rust-build, compare against current orchestrate locks, and produce the skipped/migrated list.
- What exact total did the current cargo-sweep reclaim? — verifies: Parse /tmp/cargo-sweep-20260619-rust-build-migration.log for cargo-sweep Would clean/Cleaned lines and compare a fresh du -sm target total against the prior report 16 baseline.
- Can WebPublish be migrated now despite missing INTENT.md, or should the missing INTENT.md be filled first? — verifies: Inspect /git/github.com/LiGoldragon/WebPublish AGENTS/README/flake shape and workspace repo-intent rules; if editing code repo without INTENT.md is inappropriate, add or request INTENT before migration.
- Which unclaimed candidate repos from the current scan are simple enough for mechanical rust-build migration? — verifies: Rerun the candidate scan to a file without output truncation; inspect each candidate flake for standard fenix/crane pattern, repo-specific filters, custom toolchain, devShell toolchain references, and clean jj status.
- Should a workspace-enforced Rust flake hygiene scanner/gate be added after migrations? — verifies: Design and implement a scanner that fails on raw craneLib.filterCargoSources or cleanCargoSource in Rust flakes without rust-build, then run it against changed repos and report exceptions.
- Should cargo-sweep become an automatic timer rather than a manual closeout step? — verifies: If prioritized, design a CriomOS-home user systemd timer or documented operational procedure, test dry-run behavior, and confirm acceptable caps per heavy repo.
- Is a visible faster-whisper-python import/CLI smoke test still desired? — verifies: Ask or receive prioritization; then run a visible import/CLI smoke test and record the result.

## Next
- Rerun the unclaimed Rust-flake candidate scan to an untruncated file, including repo name, dirty state, INTENT presence, toolchain mode, source-filter shape, and current lock owner. → A complete prioritized batch list and skip list for safe rust-build migration.
- Claim the first migration batch of unclaimed clean candidate repos, excluding current operator/cloud-operator/system-designer locks. → Coordination lock protects the flake edits from cross-lane conflict.
- For each claimed simple repo, read its INTENT.md and AGENTS.md if present, patch flake.nix to replace raw Fenix/Crane source cleaning with rust-build while preserving extra filters and toolchain pins, then run nix flake lock. → Each candidate consumes rust-build without losing repo-specific source inclusions.
- Run nix flake check --no-build for each patched repo and build one cheap source-realizing check such as fmt or a source-only check. → Migration is validated enough to catch evaluation/source-filter mistakes before push.
- Commit and push each migrated code repo on main with jj commit -m, jj bookmark set main -r @-, and jj git push --bookmark main; handle main advancement by fetching/importing/rebasing before push. → Remote main reflects the rust-build migrations and local working copies are clean.
- Parse /tmp/cargo-sweep-20260619-rust-build-migration.log and run a fresh target-size scan. → Final response can report exact cleanup impact from the current sweep.
- Release the system-maintainer claim after all current-batch migrations are pushed or explicitly skipped. → The coordination lock no longer blocks other agents.
- Reply with a concise summary: sweep reclaim amount, migrated repos and commit IDs, skipped repos with reasons, tests run, and remaining recommended batch. → The human sees exactly what was improved and what remains.
