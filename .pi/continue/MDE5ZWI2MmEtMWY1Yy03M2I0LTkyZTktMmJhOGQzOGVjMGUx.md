## Task
Fix the user's default editor so the deployed Home profile uses emacsclient via Horizon/user-preference data consumed by CriomOS-home, rather than a hardcoded VSCodium override.

## Done When
Pushed code/data and a successful Home profile deploy on ouranos make EDITOR/VISUAL and the intended default-editor surface resolve to emacsclient, with post-deploy checks proving VSCodium no longer forces the default.

## Forbid
- Use jj; commit/push meaningful changes to main. — source: previous-brief:Constraints & Preferences
- Primary requires whole-working-copy commits. — source: previous-brief:Constraints & Preferences
- Keep secrets out of chat/logs/reports; redact Nix store paths in human-facing output. — source: previous-brief:Constraints & Preferences
- Skip claimed repos unless coordinated. — source: previous-brief:Constraints & Preferences
- Do not delete whole target/ directories; prefer cargo-sweep --maxsize. — source: previous-brief:Constraints & Preferences
- Avoid disrupting live desktop/media state; do not stop/kill/pause/restart mpv, Niri, QEMU, PipeWire, or live Nix builds unless explicitly authorized. — source: previous-brief:Constraints & Preferences
- Do not kill, STOP, or terminate live agent processes as a media-stutter mitigation without explicit authorization; this session proved it destroyed agent work and did not fix the kernel/display issue. — source: session: user challenged killing agents after STOP/TERM/KILL of Claude/Codex workers
- For privileged local CriomOS maintenance, do not rely on sudo/pkexec as the first path when root SSH is available; use documented ssh -o BatchMode=yes root@localhost. — source: user@history: so killing all my agents was your answer? you can use ssh root instead of sudo. isnt that documented?
- If audio restart is needed, restart PipeWire, PipeWire-Pulse, and WirePlumber together. — source: previous-brief:Constraints & Preferences
- Use chroma-daemon.service, not inactive/dead chroma.service. — source: previous-brief:Constraints & Preferences
- Do not emit quotation marks in generated Chroma NOTA config. — source: previous-brief:Constraints & Preferences
- BTD 700 is a USB ALSA/PipeWire device, not normal BlueZ. — source: previous-brief:Constraints & Preferences
- For Rust-flake migration, preserve custom rust-toolchain.toml pins and repo-specific filters. — source: previous-brief:Constraints & Preferences
- Skip repos without INTENT.md unless explicitly doing repo-intent repair. — source: previous-brief:Constraints & Preferences
- For BEADS, do not report bare bead ids to psyche; close only with direct evidence; leave ambiguous/partially complete items open. — source: previous-brief:Constraints & Preferences
- Coordination release is whole-role only via tools/orchestrate release <role>; manual/scoped lock edits are unsafe. — source: previous-brief:Constraints & Preferences
- Do not put the concrete per-user default-editor value into Spirit intent-record descriptions; that value goes in code or cluster data. — source: user@history: that goes in the code, not the intent record

## Established
- Recursive cargo-sweep with --maxsize 4GB has already been run over /git/github.com/LiGoldragon and /home/li/wt/github.com/LiGoldragon, reclaiming a reported 21.84 GiB total. — evidence: /tmp/cargo-sweep-all-targets-20260619-180012.log and /tmp/cargo-sweep-worktrees-20260619-180049.log; basis: output; reopen: if new target cleanup is requested or build outputs grow again
- Post-sweep target allocated total was 177.08 GiB across 105 top-level target directories; largest reported were spirit 15.55 GiB, mind 8.61 GiB, router 7.38 GiB, kameo 6.23 GiB, and mirror 4.36 GiB. — evidence: previous-brief:Critical Context/Post-sweep target allocated total; basis: output; reopen: if target directories are cleaned or rebuilt
- Shared rust-build was created and pushed at commit d97af86e37c8f53d5d1c71b6002d426e90814f8b. — evidence: previous-brief:Progress/Created and pushed rust-build; basis: output; reopen: if rust-build main moves or helper API changes
- rust-build exports rust.cleanSource { root; extraFilters; name; pruneDirectories; } and cleanCargoSource = root: cleanSource { inherit root; }. — evidence: /git/github.com/LiGoldragon/rust-build/lib/default.nix; basis: observed; reopen: if /git/github.com/LiGoldragon/rust-build/lib/default.nix changes around the helper definitions
- rust-build validation previously passed nix flake check --no-build and nix build --no-link .#checks.x86_64-linux.source-prune-policy. — evidence: previous-brief:Progress/rust-build checks passed; basis: test; reopen: if rust-build source or tests change
- The first Rust-flake migration batch and later listed repos were migrated and pushed to shared rust-build, including chroma, chronos, clavifaber, lojix-cli, mentci, nexus, nota-config, sema, signal family, nota-next, schema-next, spirit family, signal-agent, signal-frame, signal-orchestrate, and orchestrate. — evidence: previous-brief:Progress/Migrated/pushed validated repos; basis: output; reopen: if any listed repo's flake.nix changes or is rolled back
- cloud was migrated with preserved schemaFilter and pushed at commit 83916871f2e57be2de7f7569932973d11f9c4889. — evidence: /tmp/rust-build-cloud-commit.txt; basis: output; reopen: if /git/github.com/LiGoldragon/cloud/flake.nix changes or cloud main moves away from that migration
- cloud passed local and GitHub nix flake check --no-build --refresh after migration. — evidence: previous-brief:Progress/cloud passed local and GitHub check; basis: test; reopen: if /git/github.com/LiGoldragon/cloud changes or its CI reruns and fails
- Wave-2 migration loop completed with no migrations because candidates were dirty or claim-conflicted. — evidence: /tmp/rust-build-migration-wave2-summary-20260619.tsv; basis: output; reopen: if dirty state is triaged or claims are released
- BEADS audit closed 16 stale/resolved beads, reducing open count 132 to 116, with report 17 pushed at primary commit 80267d06. — evidence: /home/li/primary/reports/system-maintainer/17-bead-staleness-audit-2026-06-19.md; basis: observed; reopen: if BEADS backend is restored to a pre-audit state or report 17 changes
- Second BEADS age-refresh wave completed through 2026-05-17 and left open count 110; report 18 and age-sorted export were produced and primary commit c33f79a49cca was pushed. — evidence: /home/li/primary/reports/system-maintainer/18-aged-bead-refresh-2026-06-19.md; basis: observed; reopen: if BEADS open counts or report 18 are revised
- bd dolt push completed after BEADS work; bd dolt commit returned Nothing to commit in embedded mode context. — evidence: previous-brief:Critical Context/BEADS backend pushed with bd dolt push; basis: output; reopen: if BEADS backend reports unpushed local changes
- Manual/scoped lock editing did not clear the effective system-maintainer lock; tools/orchestrate release system-maintainer cleared it to idle. — evidence: cmd:tools/orchestrate release system-maintainer#previous-brief-lock-malfunction; basis: output; reopen: if system-maintainer.lock becomes non-idle again or manual edits are attempted
- Movie-stutter diagnosis found CPU pressure 0 but very high I/O pressure and iowait before intervention. — evidence: cmd:vmstat and cat /proc/pressure/io#previous-brief-movie-stutter-snapshot; basis: output; reopen: if fresh pressure metrics show a different bottleneck
- Before intervention, swap pressure was high: zram about 7.7G/7.9G used, swapfile about 4.4G used, total swapped about 12G; Ghostty had about 3.7GiB swap and mpv about 100MiB swapped. — evidence: previous-brief:Critical Context/Swap snapshot; basis: output; reopen: if swap is recycled, processes exit, or a fresh swap snapshot differs
- The mpv process under diagnosis was PID 2145459 playing /home/li/Downloads/Fallout.2024.S02.1080p.x265-ELiTE/Fallout.2024.S02E01.1080p.x265-ELiTE.mkv using software HEVC threads av:hevc:* before later checks found no mpv output. — evidence: previous-brief:Critical Context/mpv process; basis: output; reopen: if mpv restarts or plays a different file
- Initial cgroup checks found mpv was not directly throttled: cpu.max=max, cpu.weight=100, io.weight=default 100, no io.max, memory.high/max/swap.max=max. — evidence: previous-brief:Turn Context/Early Progress; basis: output; reopen: if mpv moves cgroup or user/session resource units change
- Root SSH to localhost works for privileged local maintenance on ouranos: ssh -o BatchMode=yes root@localhost returned hostname ouranos and uid=0(root). — evidence: cmd:ssh -o BatchMode=yes -o ConnectTimeout=5 root@localhost 'hostname; id; date -Is'#root-ssh-check; basis: output; reopen: if root SSH auth/config changes or the target host changes
- Using root SSH, swapoff -a and swapon -a completed; after swapon, swap dropped from 8.4GiB used to about 14MiB used locally, with /swapfile active. — evidence: cmd:ssh root@localhost 'swapoff -a; swapon -a' plus local free/swapon postcheck#after-swapon; basis: output; reopen: if swap usage rises again or swap devices are reconfigured
- zram swap was restored after swap recycle: systemd-zram-setup@zram0.service was active and /dev/zram0 swap was active again. — evidence: cmd:ssh root@localhost 'systemctl list-units --type=swap --all; systemctl start dev-zram0.swap; systemctl start systemd-zram-setup@zram0.service; swapon --show'#zram-units; basis: output; reopen: if zram units are stopped or swap layout changes
- After swap recycle, kernel/display path still had a stuck D-state worker kworker/u60:14+i915_flip while /proc/stat reported 26 blocked processes. — evidence: cmd:ssh root@localhost 'journalctl -k ...' and ps D-state check#current-blocked-D; basis: output; reopen: if a fresh ps/proc stat shows no stuck i915_flip worker and blocked count normalizes
- CriomOS-home Pi/Claude/Codex update was pushed in commit f34fb148f414 with Pi 0.79.8, Claude VSIX 2.1.183, Codex CLI update path, pi-utils-ui 0.5.0, and pi-subagents 0.29.0 changes. — evidence: cmd:jj log -r main in /git/github.com/LiGoldragon/CriomOS-home#f34fb148f414; basis: output; reopen: if CriomOS-home package pins move again or the commit is reverted
- lojix-cli Rust stable toolchain hash was corrected to sha256-mvUGEOHYJpn3ikC5hckneuGixaC+yGrkMM/liDIDgoU= and pushed at commit 95d9f4e12344. — evidence: cmd:jj log -r main in /git/github.com/LiGoldragon/lojix-cli#95d9f4e12344; basis: output; reopen: if /git/github.com/LiGoldragon/lojix-cli/flake.nix changes around the toolchain hash or main advances with a different fix
- hexis Rust stable toolchain hash refresh was pushed at commit c0ab42c8cc93 after nix flake check --no-build and package build succeeded. — evidence: cmd:jj log -r main in /git/github.com/LiGoldragon/hexis#c0ab42c8cc93; basis: output; reopen: if hexis main advances or its flake/toolchain files change
- agent Rust stable toolchain hash refresh was pushed at commit abfa64bdcf00 after nix flake check --no-build rerun passed. — evidence: cmd:jj log -r main in /git/github.com/LiGoldragon/agent#abfa64bdcf00; basis: output; reopen: if agent main advances or its flake/toolchain files change
- chroma Rust stable toolchain hash refresh was pushed at commit 2ce9d55fa7ca; chroma main later advanced to a3ff23e3ef60 but still contains the refreshed hash. — evidence: cmd:jj log and rg in /git/github.com/LiGoldragon/chroma#main-a3ff23e3-hash-check; basis: output; reopen: if chroma flake.nix changes around fromToolchainFile or the hash
- CriomOS-home Home profile deployment succeeded via lojix-run HomeOnly Activate using run directory /home/li/.local/state/lojix-runs/20260619212121-homeonly-ouranos. — evidence: cmd:lojix-run '(HomeOnly goldragon ouranos li /tmp/goldragon-datom-compatible-main-parent.nota github:LiGoldragon/CriomOS-home/main Activate None None)'#lojix_run=success; basis: output; reopen: if Home profile is deployed again, rolled back, or activation failures occur
- Post-deploy checks showed pi 0.79.8, claude 2.1.183, codex-cli 0.141.0, Pi transport websocket, and active chroma-daemon, spirit-daemon, agent-daemon, whisrs, PipeWire, PipeWire-Pulse, and WirePlumber services. — evidence: cmd:post-deploy versions/settings/services check#versions-and-services; basis: output; reopen: if Home profile is redeployed, packages are repinned, services restart/fail, or Pi settings change
- Spirit accepted record q87a capturing the abstract intent that Horizon user preference data carries a typed preferred-editor setting consumed by Home/profile config, while concrete editor choices live in code or data rather than intent-record descriptions. — evidence: cmd:spirit /tmp/editor-preference-shape-spirit-record.nota#RecordAccepted-q87a; basis: output; reopen: if Spirit record q87a is superseded, clarified, or retired
- No erroneous concrete emacsclient Spirit record landed before q87a; PublicTextSearch emacsclient returned no matching record after the aborted first attempt. — evidence: cmd:spirit '(PublicTextSearch emacsclient)'#Error-no-matching-record; basis: output; reopen: if a later Spirit search finds an emacsclient intent record or q87a is changed
- The current live bug report is that the user's default editor is still VSCodium despite the package/profile update. — evidence: user@turn-prefix: doesnt work, my default editor is still vscodium; basis: user; reopen: if post-deploy checks show the default editor surface no longer points at VSCodium
- CriomOS-home currently has no working-copy changes and main at a473ea68, described as home: repin wave-2 nightly aggregators, after later work advanced it beyond the earlier c05b7b25 deploy commit. — evidence: cmd:jj st --no-pager in /git/github.com/LiGoldragon/CriomOS-home#turn-prefix-status; basis: output; reopen: if CriomOS-home main or working copy changes
- horizon-rs currently has no working-copy changes and main at 8d6cbc66, described as flake: newest nightly via fenix complete, drop frozen toolchain-file sha256. — evidence: cmd:jj st --no-pager in /git/github.com/LiGoldragon/horizon-rs#turn-prefix-status; basis: output; reopen: if horizon-rs main or working copy changes
- goldragon currently has no working-copy changes and main at 84c7f9e6, described as enabling VM hosting on prometheus and migrating datom to current horizon schema. — evidence: cmd:jj st --no-pager in /git/github.com/LiGoldragon/goldragon#turn-prefix-status; basis: output; reopen: if goldragon main or working copy changes
- system-designer currently claims CriomOS, clavifaber, goldragon, lojix, meta-signal-lojix, and signal-lojix; goldragon is therefore not safe to edit without coordination. — evidence: cmd:cat /home/li/primary/orchestrate/system-designer.lock#turn-prefix-claims; basis: output; reopen: if system-designer releases or changes the claim
- CriomOS-home's VSCodium module currently forces EDITOR and VISUAL to codium --wait and forces text/source MIME defaults to codium.desktop. — evidence: /git/github.com/LiGoldragon/CriomOS-home/modules/home/vscodium/vscodium/default.nix:175-188; basis: observed; reopen: if modules/home/vscodium/vscodium/default.nix changes around the editor/MIME defaults

## Learned
- Use allocated disk size for target accounting: du -sB1, not du -sb apparent size. — source: previous-brief:Key Decisions/Use allocated disk size
- cargo sweep was not installed as a Cargo subcommand in this environment; invoke it through nix shell nixpkgs#cargo-sweep -c cargo-sweep. — source: previous-brief:Progress/cargo sweep not installed as subcommand
- jj diff --quiet was unreliable/portable-bad for dirty detection in migration loops; use jj st --no-pager. — source: previous-brief:Critical Context/jj diff --quiet unreliable
- For Rust-flake migrations, rust-build is the durable landing, and custom rust-toolchain.toml pins should migrate via rust-build.lib.${system}.fromToolchainFile pkgs { file = ./rust-toolchain.toml; sha256 = ...; }. — source: previous-brief:Key Decisions/Use rust-build and preserve toolchain pins
- For repos with schema/example/script/golden source filters, preserve those filters as extraFilters to rust.cleanSource; do not extract filterCargoSources itself as an extra filter because rust.cleanSource already provides it internally. — source: previous-brief:Key Decisions/Preserve repo-specific filters and exclude filterCargoSources
- Close BEADS conservatively: close only content-free placeholders, duplicates, or directly shipped/superseded items, and leave ambiguous or partially complete items open. — source: previous-brief:Key Decisions/Close BEADS conservatively
- QEMU diagnostics should be non-disruptive by default; the observed live QEMU load was CriomOS-test-cluster Nix VM checks, not rust-build migration. — source: previous-brief:Key Decisions/QEMU source is CriomOS-test-cluster Nix VM checks
- Movie stutter in this session was I/O/swap/display pressure rather than CPU saturation or direct mpv cgroup throttling. — source: cmd:vmstat+/proc/pressure and cgroup checks#movie-stutter-diagnosis
- For privileged local host maintenance from this harness, documented root SSH is the right escalation path; sudo and pkexec failed due missing password/controlling terminal. — source: cmd:sudo swapoff failed; pkexec failed; ssh root@localhost succeeded
- Stopping and killing background Claude/Codex processes as a media relief attempt was a bad escalation: it destroyed agent processes and did not fix the underlying kernel/display stall. — source: session:user correction after STOP/TERM/KILL and subsequent root-swap/i915 checks
- Current goldragon datom at 84c7f9e6 uses a newer Horizon schema than older lojix-cli parser versions; earlier Home deploy used a compatible main-parent datom snapshot as a temporary workaround. — source: cmd:lojix-run current datom error expected ClusterProposal roots; later HomeOnly Activate used /tmp/goldragon-datom-compatible-main-parent.nota
- Spirit Guardian rejected the editor-preference intent record when certainty was High; Medium was accepted as q87a, matching the human's should-level wording. — source: cmd:spirit /tmp/editor-preference-shape-spirit-record.nota#GuardianRejected-then-RecordAccepted-q87a
- The concrete editor value emacsclient belongs in code or cluster data, not in the Spirit intent record; q87a deliberately records only the abstract Horizon/Home preference shape. — source: user@history: that goes in the code, not the intent record; cmd:RecordAccepted q87a
- Updating Pi/Claude/Codex packages and redeploying the Home profile does not change the default editor because CriomOS-home's VSCodium module still mkForce's EDITOR/VISUAL and MIME defaults to Codium. — source: /git/github.com/LiGoldragon/CriomOS-home/modules/home/vscodium/vscodium/default.nix:175-188 and user@turn-prefix

## Open
- Does the live ouranos system still suffer video/desktop stutter after the swap recycle and later profile redeploy, or did the remaining i915_flip D-state require reboot/session restart? — verifies: Ask the human or collect fresh vmstat, /proc/pressure, /proc/stat, ps D-state, and playback/compositor symptoms without disrupting the desktop.
- Determine whether wave-2 dirty Rust-flake skips are real repo changes, untracked/generated files, or dirty-detection artifacts. — verifies: Run jj st --no-pager in representative skipped repos such as message, meta-signal-terminal, sema-engine, and criome; classify each dirty state and record safe remediation.
- Rerun a full untruncated Rust-flake candidate scan after cloud and later profile-blocker repins. — verifies: Produce a scan including repo name, dirty state, INTENT.md presence, lock owner, toolchain mode, and source-filter shape; compare against migrated/skipped lists.
- Produce the final migrated/skipped justification report for the rust-build resumed phase. — verifies: Write and push a system-maintainer report covering cloud, wave-2 skip reasons, cargo-sweep actual totals, and later Rust-hash blocker fixes if relevant.
- Continue BEADS age-refresh from 2026-05-18 onward while respecting active claims. — verifies: Use the age-sorted export, inspect each bead with firsthand evidence, update/close only directly evidenced items, and push BEADS/backend/report updates.
- Resolve the current default-editor bug: the user's default editor remains VSCodium, and CriomOS-home still forces EDITOR/VISUAL and MIME defaults to Codium. — verifies: Change the relevant Home/Horizon/user-preference code and/or data, deploy on ouranos, and prove EDITOR/VISUAL and intended MIME/file-opening defaults no longer point at VSCodium.
- Implement the Horizon-backed typed per-user preferred-editor setting captured in Spirit record q87a, with concrete emacsclient value in code/data rather than Spirit. — verifies: Add/adjust horizon-rs schema/projection tests, update CriomOS-home to consume the projected user preference, place the concrete value in the appropriate data source, and pass builds/tests/deploy.
- Coordinate or wait before editing goldragon data for the user's editor preference because system-designer currently claims goldragon. — verifies: system-designer.lock releases goldragon, the owner authorizes coordination, or another safe data path is identified that does not conflict with the active claim.
- Decide whether existing Spirit record 1924 about VSCodium as default file-opening/MIME editor needs clarification or supersession after the emacsclient default-editor implementation. — verifies: Compare the implemented surface against record 1924; if the human's new intent contradicts it, use Spirit Clarify/Supersede/Retire tooling rather than adding an unlinked sibling record.
- Determine whether current lojix-cli/Horizon now parses the current five-root goldragon datom, or whether Home deploys still need a temporary compatible datom snapshot. — verifies: Run a non-activating HomeOnly Build or parser check against current goldragon/datom.nota with current lojix-run/lojix-cli; success closes the workaround, failure anchors the parser/schema blocker.
- Repos without INTENT.md remain skipped unless doing repo-intent repair, including WebPublish, kameo-testing, signal-derive, and substack-cli. — verifies: Either create/confirm INTENT.md under an explicit repo-intent repair task, or continue excluding these repos from code migration.
- Active claim-conflict repos remain skipped for migration until owners release or coordination occurs. — verifies: Check orchestrate locks; proceed only after claims release or an explicit coordinated handoff.
- BEADS items intentionally left open/uncertain still need direct evidence before closure: SEMA VC-hardening integration, Chroma config plus sema adoption, NOTA bracket-string epic, router/criome networking plus BLS, Spirit runner extraction, and legacy orchestrator retirement. — verifies: For each item, inspect source/reports/tests and close only if direct evidence proves shipped, duplicate, or superseded.

## Next
- Claim the unclaimed implementation surface for the editor fix, at minimum CriomOS-home and horizon-rs; do not claim or edit goldragon unless system-designer releases or coordination is explicit. → A safe coordination state for code changes without trampling the active system-designer goldragon/CriomOS work.
- Inspect horizon-rs lib/src/user.rs, lib/src/proposal.rs, lib/src/horizon.rs, and tests to add a typed preferred-editor user preference with projection output. → A tested Horizon schema/projection change that can carry user preferred editor without hardcoding the value in Home modules.
- Inspect CriomOS-home modules/home/profiles/* and modules/home/vscodium/vscodium/default.nix, then remove or narrow the mkForce VSCodium EDITOR/VISUAL and MIME defaults so the projected preferred editor can win. → Home configuration consumes Horizon preferred editor and no longer forces Codium as the default editor when emacsclient is preferred.
- If goldragon remains claimed, either ask/coordinate for the concrete user-data edit or implement a temporary CriomOS-home-side data bridge only if it does not violate q87a's placement intent. → The concrete emacsclient value lands in code/data safely, not in Spirit, and without claim conflict.
- Run focused checks for horizon-rs and CriomOS-home, including tests that prove the preferred-editor field projects and Home evaluates with emacsclient defaults. → Build/test evidence that the schema and Home consumer work before deploy.
- Commit and push the editor fix repos with jj, preserving whole-working-copy discipline for primary and main/bookmark discipline for code repos as appropriate. → Reproducible pushed revisions for deployment.
- Deploy the ouranos li Home profile with lojix-run HomeOnly Activate, using current goldragon datom only if the parser accepts it; otherwise document/coordinate the datom-parser mismatch rather than silently using stale data. → The running profile is updated or a precise deploy blocker is surfaced.
- Post-check command -v/editor environment/MIME behavior under the activated profile and report concise results to the human. → Proof that default editor no longer remains VSCodium, or exact remaining failure details.
- After the editor task is complete, resume the rust-build and BEADS backlog: dirty-skip triage, full candidate scan, final migration report, and BEADS age pass from 2026-05-18. → Previously open maintenance work advances without losing the new editor fix.
