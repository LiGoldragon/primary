## Task
Implement the subtractive CriomOS-home dictation mic fix so Whisrs uses ordinary/default input when the DJI mic is absent, and add videographer-requested video test tools to the medium Home profile.

## Done When
Done when the CriomOS-home changes are committed, pushed, activated on Ouranos through lojix-run, Whisrs/default-source behavior is verified, medium video tools are verified, and the human receives a closeout.

## Forbid
- Keep secrets out of chat/logs/reports; redact Nix store paths in human-facing output. — source: prior-brief:Constraints & Preferences
- Do not use target/debug for final lojix-daemon validation; install locally via production deploy path first. — source: prior-brief:Constraints & Preferences
- Avoid disrupting live desktop/session state; check whisrs status before switching/restarting. — source: prior-brief:Constraints & Preferences
- Direct bird@zeus.goldragon.criome SSH is unavailable; use root-mediated commands. — source: prior-brief:Constraints & Preferences
- Use jj; commit/push meaningful changes to main. — source: prior-brief:Constraints & Preferences
- Do not trust current meta-lojix Deployed output as terminal success; verify daemon journal, target state, and ordinary query. — source: prior-brief:Constraints & Preferences
- Do not let the CriomOS-home DJI/Whisrs mic fix grow into a large pile of special-case keepalive code; prefer reducing or deleting brittle PipeWire/WirePlumber policy. — source: user@history-to-summarize:mic-simplification-prompt

## Established
- The earlier primary-htrk Zeús live Switch and ordinary lojix query parity task is closed, with the closeout report landed. — evidence: /home/li/primary/reports/system-maintainer/11-lojix-daemon-zeus-live-switch-query-parity-2026-06-15.md:1; basis: doc; reopen: if the human reopens primary-htrk or new lojix/CriomOS changes invalidate the verified Zeús Switch/query behavior
- The human clarified that the video tools should be added to a medium-sized node, i.e. the medium Home profile boundary. — evidence: user@turn-prefix:I mean add them to a medium sized node; basis: user; reopen: if the human redirects the target profile or asks for a different node size
- Spirit accepted record 1vj5 capturing the constraint to simplify the CriomOS-home DJI/Whisrs mic path rather than adding more keepalive code. — evidence: cmd:spirit /tmp/dji-mic-simplification-record.nota#RecordAccepted-1vj5; basis: output; reopen: if Spirit record 1vj5 is superseded, retired, or corrected
- Spirit accepted record tve6 capturing the decision to prioritize reliable ordinary laptop-microphone fallback over the DJI Bluetooth wake-latency optimization for this implementation. — evidence: cmd:spirit /tmp/dji-fallback-decision-record.nota#RecordAccepted-tve6; basis: output; reopen: if Spirit record tve6 is superseded, retired, or corrected
- CriomOS-home INTENT.md now states under Dictation that DJI/Whisrs microphone support should stay small and subtractive, with the human's quoted wording. — evidence: /git/github.com/LiGoldragon/CriomOS-home/INTENT.md:31; basis: observed; reopen: if /git/github.com/LiGoldragon/CriomOS-home/INTENT.md changes around the Dictation section
- The DJI/Whisrs simplification survey report was written and committed in primary as commit 6cd889ec. — evidence: cmd:jj commit -m 'survey DJI Whisrs hot-loop simplification'#commit-6cd889ec; basis: output; reopen: if the report is superseded or amended
- The final CriomOS-home implementation commit on main is 865c47e1, titled avoid video Python profile collisions, after earlier fixes 38b7b268, 6817b9a2, and 38052fe4. — evidence: cmd:jj commit -m 'avoid video Python profile collisions'#commit-865c47e1; basis: output; reopen: if CriomOS-home main advances with further relevant dictation or medium-profile changes
- The old dji-keepalive check was removed and replaced by checks/whisrs-default-input/default.nix, which asserts Whisrs is not hard-bound to PIPEWIRE_NODE and that ordinary dictation does not create the old DJI hot source or WirePlumber policy. — evidence: /git/github.com/LiGoldragon/CriomOS-home/checks/whisrs-default-input/default.nix:28; basis: observed; reopen: if checks/whisrs-default-input/default.nix changes
- CriomOS-home flake checks now expose whisrs-default-input instead of dji-keepalive. — evidence: /git/github.com/LiGoldragon/CriomOS-home/flake.nix:317; basis: observed; reopen: if /git/github.com/LiGoldragon/CriomOS-home/flake.nix changes around the projectChecks list
- The subtractive dictation edit removed the whisrsServe PIPEWIRE_NODE serial-resolution block and removed the declared PipeWire hot sink/loopback plus DJI WirePlumber policy from modules/home/profiles/min/dictation.nix. — evidence: cmd:rg -n 'dji-keepalive|dji_mic_hot|PIPEWIRE_NODE|bluez_card.04_A8|60-dji|whisrs-default-input' modules checks flake.nix INTENT.md#no-dictation-hot-loop-matches; basis: output; reopen: if /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/dictation.nix changes
- The medium Home profile includes Node.js and pnpm for Remotion/Codex video workflow testing. — evidence: /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/med/default.nix:119; basis: observed; reopen: if /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/med/default.nix changes around the package list
- The medium Home profile includes scenedetect, openai-whisper, and a dedicated faster-whisper-python wrapper instead of a colliding whole Python environment. — evidence: /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/med/default.nix:40; basis: observed; reopen: if /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/med/default.nix changes around fasterWhisperPython or the package list
- The minimum Home profile already provides several videographer-requested tools inherited by medium nodes: ffmpeg-full, wf-recorder, mpv, Inter, Montserrat, and Noto color emoji fonts. — evidence: /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/default.nix:50; basis: observed; reopen: if /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/default.nix changes around fontPackages or modernGraphicalPackages
- Focused local Nix checks for whisrs-default-input and whisrs-dictation-bindings passed before the final commit. — evidence: cmd:nix build --no-link .#checks.${system}.whisrs-default-input .#checks.${system}.whisrs-dictation-bindings#exit-status-0-before-commit-865c47e1; basis: test; reopen: if dictation.nix, whisrs-default-input, whisrs-dictation-bindings, flake.nix, or relevant Home profile inputs change
- HomeOnly activation on Ouranos succeeded from the pushed CriomOS-home main ref after commit 865c47e1. — evidence: cmd:lojix-run '(HomeOnly goldragon ouranos li /git/github.com/LiGoldragon/goldragon/datom.nota github:LiGoldragon/CriomOS-home/main Activate None None)'#run_directory-20260617112525-homeonly-ouranos-success; basis: output; reopen: if CriomOS-home main advances, the Home profile is rolled back, or a later activation fails
- After activation, whisrs.service was active/running and its process environment did not include PIPEWIRE_NODE. — evidence: cmd:systemctl --user show whisrs.service ... /proc/$pid/environ#whisrs-env-no-PIPEWIRE_NODE; basis: output; reopen: if whisrs.service is restarted from a different generation or its service wrapper changes
- After activation, the managed DJI hot-loop files were absent from ~/.config/pipewire and ~/.config/wireplumber. — evidence: cmd:for f in ~/.config/pipewire/...60-dji... ~/.config/wireplumber/...60-dji...; do ...#managed-files-removed; basis: output; reopen: if Home activation is rolled back or those files reappear
- The live default source was switched to the laptop Mic1 source alsa_input.pci-0000_00_1f.3-platform-skl_hda_dsp_generic.HiFi__Mic1__source, and a two-second pw-record level check showed real signal with max_volume -16.9 dB. — evidence: cmd:pactl set-default-source ... && pw-record --target=$laptop_source ... ffmpeg volumedetect#max_volume--16.9dB; basis: output; reopen: if the default source changes or the laptop mic source disappears
- A Whisrs start/cancel smoke test opened capture from source 863509, the laptop Mic1 source, logged using audio input device: default, and returned to idle without sending a transcription. — evidence: cmd:whisrs toggle-copy; pactl list source-outputs; whisrs cancel; journalctl --user -u whisrs.service#source-863509-default-cancelled; basis: test; reopen: if Whisrs capture behavior changes, default source changes, or whisrs.service restarts from a different generation
- After activation, video tool commands were present in the user profile for ffmpeg, ffprobe, wf-recorder, yt-dlp, mpv, magick, mediainfo, mkvmerge, piper, espeak-ng, scenedetect, whisper, node, pnpm, and faster-whisper-python. — evidence: cmd:for c in ffmpeg ffprobe wf-recorder yt-dlp mpv magick mediainfo mkvmerge piper espeak-ng scenedetect whisper node pnpm faster-whisper-python; do command -v $c; done#video-tools-present; basis: output; reopen: if the Home profile changes or any command disappears from PATH
- The live PipeWire graph still showed a stale dji_mic_hot_sink.monitor node and old DJI loopback source-output after the declarative files were removed, because PipeWire was not restarted. — evidence: cmd:pactl list short sources && pactl list source-outputs#dji_mic_hot_sink-monitor-still-present; basis: output; reopen: if PipeWire is restarted, the stale module is manually unloaded, or a later graph check shows the node absent

## Learned
- For this Whisrs version, capture code uses cpal's default input device; exporting PIPEWIRE_NODE in the service wrapper was the mechanism forcing capture away from the ordinary default-source path. — source: /git/github.com/LiGoldragon/whisrs/src/audio/capture.rs:1 and cmd:whisrs-smoke#using-audio-input-device-default
- The current DJI hot-loop design made a removable Bluetooth accessory mandatory for dictation because dji_mic_hot_sink.monitor stayed present even when the real DJI source was absent or stale. — source: /home/li/primary/reports/system-maintainer/12-dji-whisrs-hot-loop-simplification-survey-2026-06-17.md:1
- python3Packages.whisperx is not deployable at the current nixpkgs pin; Home activation failed because a fixed-output dependency hash no longer matched, so do not add WhisperX until the pin or package is fixed. — source: cmd:lojix-run HomeOnly Activate#run_directory-20260617111901-homeonly-ouranos-failed-whisperx
- Adding a full pkgs.python3.withPackages environment to home.packages can collide with the profile's existing python binary; expose specialized Python modules through a named wrapper such as faster-whisper-python instead. — source: cmd:lojix-run HomeOnly Activate#run_directory-20260617112409-homeonly-ouranos-failed-home-manager-path
- Adding raw python3Packages.faster-whisper to home.packages did not make faster_whisper importable from the ambient profile python; a dedicated wrapper/interpreter is needed for module-level use. — source: cmd:python -c import faster_whisper#failed-No-module-named-faster_whisper
- tools/orchestrate claim was unusable this cycle because orchestrate-cli tried to fetch unavailable pinned nota-codec revision f761421c; visible lock projections were checked before the narrow INTENT edit. — source: cmd:tools/orchestrate claim#failed-nota-codec-f761421c-not-found and cmd:ls orchestrate/*.lock#visible-locks
- CriomOS-home has pre-existing unrelated nixfmt drift; format only touched/new files unless explicitly doing a formatting sweep. — source: cmd:nix fmt -- --check .#unrelated-files-not-formatted
- The lojix-run HomeOnly request fields are cluster, node, user, proposal source path, Home flake ref, mode, builder, and substituters; github:LiGoldragon/CriomOS-home/main is rewritten to an exact pushed revision. — source: /git/github.com/LiGoldragon/lojix-cli/src/request.rs:31 and /git/github.com/LiGoldragon/CriomOS-home/packages/lojix-run/lojix-run.py:1
- For prior lojix deploy work, meta-lojix Deployed should be treated as admission unless terminal daemon output, target state, and ordinary query prove success. — source: prior-brief:Key Decisions
- For prior lojix deploy work, canonical NOTA atoms were required because Horizon rejected bracket-delimited bare strings such as rock64. — source: prior-brief:Key Decisions
- For prior lojix deploy work, dispatcher-to-target nix copy with --substitute-on-destination avoided unsupported direct root@prometheus source SSH. — source: prior-brief:Key Decisions
- For prior lojix deploy work, lojix-daemon.service needed operator SSH-agent access via /run/user/1001/gnupg/S.gpg-agent.ssh for daemon-owned target copy. — source: prior-brief:Key Decisions
- Zeús Home Manager generation parity remains separate from FullOs system activation parity; the prior Zeús system switched but Home symlinks did not move. — source: prior-brief:Key Decisions

## Open
- Does faster-whisper-python successfully import faster_whisper after the final activation? — verifies: Run `faster-whisper-python - <<'PY'` with `import faster_whisper; print('ok')` and record visible output; the previous validation output was truncated before the import result.
- Should the stale live dji_mic_hot_sink.monitor node and old DJI loopback be manually unloaded now, or left until the next PipeWire restart since Whisrs no longer uses it? — verifies: Check `pactl list modules`, `pw-link -l`, and `pactl list short sources`; if safe, unload only the stale DJI loopback/hot node or restart PipeWire with the human's approval, then confirm the node is gone and Whisrs still records from laptop default.
- Should DJI wake-latency optimization return later as an explicit optional mode instead of an always-on hot loop? — verifies: Ask only if the human reports unacceptable first-seconds loss after the subtractive fallback fix; otherwise keep the code deleted.
- Should report 12 be amended with the implementation closeout, or is the commit history plus chat closeout sufficient? — verifies: If durable analysis beyond the existing survey is needed, append or create a system-maintainer report section naming commits 865c47e1 and activation run 20260617112525.
- If continuing lojix architecture cleanup, should meta-signal-lojix distinguish admission from terminal deploy success in names/contracts? — verifies: Read current meta-signal-lojix and lojix daemon contract, propose the rename/split, and validate with tests or a design report.
- If continuing lojix operations polish, should lojix expose queryable in-progress and terminal job status so operators do not rely on journals during detached deploys? — verifies: Inspect signal-lojix/meta-signal-lojix query surfaces and add or design a status API with tests.
- If continuing lojix credential cleanup, should ambient operator GPG SSH-agent dependence be replaced with first-class daemon deploy credentials? — verifies: Design and implement daemon credential configuration, then verify daemon-owned nix copy to Zeús works without relying on the operator session agent.
- If FullOs is meant to move Home generations too, how should Zeús Home Manager activation parity be tracked? — verifies: Compare FullOs activation semantics against Zeús Home profile symlinks and create/close a separate bead or report if parity is required.
- Should Prometheus remote-builder FullOS eval for Ouranos be retested? — verifies: Only if that path becomes relevant again, run the remote-builder FullOS eval with redacted logs and capture the result.

## Next
- Run one quick final check in CriomOS-home: `jj status --no-pager`, `jj git push --bookmark main`, and the visible `faster-whisper-python` import test if time allows. → Confirm the working copy and origin are clean and close the remaining verification gap for faster-whisper-python.
- Send the human a concise closeout. → State that the subtractive mic fix is implemented and activated, Whisrs now records from the laptop default source with no PIPEWIRE_NODE, medium video tools were added, WhisperX was replaced because it is broken at the current pin, and mention the stale DJI hot node caveat.
- If the human wants the stale DJI runtime node gone immediately, unload the stale loopback/hot node or restart PipeWire only after confirming Whisrs is idle and the disruption is acceptable. → Remove confusing leftover runtime graph state without reintroducing code or disrupting active dictation.
