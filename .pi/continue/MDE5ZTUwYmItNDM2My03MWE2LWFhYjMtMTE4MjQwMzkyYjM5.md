## Task
Finish the Pi/CriomOS operator run by fixing and shipping the Nix-packaged lojix-run deploy wrapper, then keep working through queued maintenance while preserving the older cloud and Prometheus context.

## Done When
The current CriomOS-home lojix-run changes are fixed, checked, committed, pushed, propagated or explicitly deferred, locks are released, primary-yt58 is closed or left with a precise blocker, and remaining cloud/Prometheus/Pi work has clear next actions.

## Forbid
- Do not overwrite active workspace locks. — source: previous brief Constraints & Preferences
- No secret bytes in logs, source, reports, chat, or command output; verify secrets only by path, status, exit code, ciphertext marker, or byte count. — source: previous brief Constraints & Preferences
- Cloud credentials cross policy boundaries only by handle. — source: previous brief Constraints & Preferences
- Signal repos carry only Signal wire vocabulary; daemon runtime schemas live in runtime repos such as schema/nexus.schema and schema/sema.schema. — source: previous brief Constraints & Preferences
- Cloud build must use Cargo schema metadata, not hard-coded workspace paths. — source: previous brief Constraints & Preferences
- Generated schema files are generator-owned; avoid cargo fmt or rustfmt drift on generated artifacts. — source: previous brief Constraints & Preferences
- Do not install persistent Pi packages or extensions statefully through ~/.pi/agent npm/settings mutation; package them through Nix/Home Manager profile symlinks. — source: user@previous-cycle Pi install correction
- Review third-party Pi package source before installing because Pi packages run with full local permissions. — source: previous brief Constraints & Preferences
- Do not download copyrighted bestseller content; only legal/open downloads are allowed. — source: previous brief Progress Done
- Do not use browser-use to control the live default Chrome profile; Playwright Extension remains the working browser automation path. — source: previous brief Blocked
- Use jj and commit/push completed work; on primary work directly on main without feature branches. — source: previous brief Constraints & Preferences and AGENTS.md
- Do not put raw Nix store paths in chat summaries; capture them in shell variables or logs and redact store hashes from surfaced failure tails. — source: CriomOS/reports/0034-self-review-prom-deploy-session.md:84

## Established
- Prometheus freeze/memory-pressure diagnosis found high RAM and I/O pressure, no swap, and no kernel OOM or lockup evidence. — evidence: previous-brief:Progress Done#prometheus-freeze-diagnosis; basis: output; reopen: if new Prometheus freeze evidence, kernel logs, memory metrics, or swap configuration changes are collected
- persona-spirit-daemon-v0.1.1.service previously restarted with InvalidDaemonConfiguration because it expected a string literal or bare identifier and got LBracket. — evidence: previous-brief:Progress Done#persona-spirit-daemon-v0.1.1.service; basis: output; reopen: if the service unit, deployed spirit version, or daemon configuration changes
- Local Gemma subagent metadata was provider criomos-local with model gemma-4-26b-a4b-ud-q4-k-xl. — evidence: previous-brief:Progress Done#local-gemma-subagent-metadata; basis: output; reopen: if local AI model metadata or provider configuration changes
- Open PDFs downloaded earlier were SICP, Little Book of Semaphores, and Mathematics for Machine Learning under /tmp/relevant-open-books-2026-06-04. — evidence: previous-brief:Critical Context#open-pdfs-downloaded-earlier; basis: output; reopen: if /tmp is cleaned or the files are moved
- Cloud and domain-criome runtime schemas were promoted from concept schemas to real schema/nexus.schema and schema/sema.schema files. — evidence: commit:683631e84901 and commit:8ecfa3e634bd; basis: output; reopen: if cloud or domain-criome schema files are renamed, removed, or regenerated
- cloud main d85873b34a82 contains the first fresh generated schema runtime slice. — evidence: commit:d85873b34a82; basis: output; reopen: if cloud main moves or src/schema_runtime.rs, src/schema/mod.rs, generated schema modules, or related tests change
- domain-criome main was 3e21c8022a1e after integrating meta-signal schema runtime artifacts. — evidence: previous-brief:Critical Context#current-domain-criome-main; basis: output; reopen: if domain-criome main moves
- signal-cloud main was fc1a3a32a819 after ObservationResult carried result types. — evidence: previous-brief:Critical Context#current-signal-cloud-main; basis: output; reopen: if signal-cloud main moves
- meta-signal-cloud main was c5bd5a1fc7db after refreshing the signal schema dependency. — evidence: previous-brief:Critical Context#current-meta-signal-cloud-main; basis: output; reopen: if meta-signal-cloud main moves
- meta-signal-cloud is the active policy signal repository replacing the old policy-signal naming. — evidence: previous-brief:Progress Done#policy-signal-cloud-replaced; basis: observed; reopen: if the old policy-signal repository is revived as an active canonical repo
- Cargo check and quiet tests previously passed for signal-cloud, meta-signal-cloud, cloud, signal-domain-criome, meta-signal-domain-criome, and domain-criome after schema metadata work. — evidence: previous-brief:Progress Done#cargo-check-passes; basis: test; reopen: if any source, schema, Cargo, or test file in those repos changes
- cloud/src/lib.rs exposes extern crate self as cloud, pub mod schema, and pub mod schema_runtime. — evidence: previous-brief:Critical Context#cloud-src-lib-rs-exposes; basis: observed; reopen: if cloud/src/lib.rs changes
- cloud/build.rs uses schema_rust_next::build::{DependencySchema, GenerationDriver, GenerationPlan} and CLOUD_UPDATE_SCHEMA_ARTIFACTS. — evidence: previous-brief:Critical Context#cloud-build-rs; basis: observed; reopen: if cloud/build.rs changes
- Pi 0.78.0 source treats overflow compaction as compact-and-auto-retry and threshold compaction as compact with no auto-retry. — evidence: /home/li/.local/share/criomos/pi/package/src/core/agent-session.ts:1783-1785; basis: observed; reopen: if the deployed Pi source or version changes
- Pi RPC docs say willRetry is true only for successful overflow compaction. — evidence: /home/li/.local/share/criomos/pi/package/docs/rpc.md:934; basis: doc; reopen: if Pi docs or deployed Pi version changes
- The previously wrong stateful pi-continue npm install was removed: npmCommand is null, packages include packages/pi-continue through the profile, ~/.pi/agent/npm is absent, and a fresh Pi runtime registers /continue on ouranos. — evidence: cmd:settings/packages/symlink/pi list/Pi RPC verification on ouranos#output packages/pi-continue npmCommand null no ~/.pi/agent/npm continue command present; basis: output; reopen: if ~/.pi/agent/settings.json, ~/.pi/agent/packages, Home Manager profile, or Pi package loading changes
- CriomOS-home main a2ac795f0e21 packages pi-continue through the Nix/Home Manager profile. — evidence: cmd:jj -R /git/github.com/LiGoldragon/CriomOS-home log -r 'main | @ | @-'#output a2ac795f main home: package pi-continue through nix profile; basis: output; reopen: if CriomOS-home main moves or pi-continue packaging files change
- CriomOS main 339143a6df30 pins CriomOS-home a2ac795f0e21. — evidence: cmd:python flake.lock check and jj log in /git/github.com/LiGoldragon/CriomOS#output CriomOS lock equals a2ac795f0e21ffae226e1ee0f99c0807f0f19898; basis: output; reopen: if CriomOS flake.lock changes or CriomOS main moves
- The human reloaded Pi after pi-continue was installed through the profile. — evidence: user@history-to-summarize:Okay, I've reloaded Pi, we'll see if it works.; basis: user; reopen: if the human later says the reload did not happen or Pi was restarted into a different profile
- Unauthenticated GitHub API rate limiting made github:LiGoldragon/CriomOS/main resolve through stale Nix flake cache to revision 460e577ed8dd rather than local main 339143a6df30. — evidence: cmd:nix flake metadata --refresh github:LiGoldragon/CriomOS/main#output HTTP 403 and Revision 460e577ed8dd09bf24ae6d52f71228c5790f332f; basis: output; reopen: if authenticated GitHub access or Nix flake resolution behavior changes
- Exact revision refs for CriomOS and CriomOS-home resolved correctly and showed CriomOS 339143a6df30 containing CriomOS-home a2ac795f0e21. — evidence: cmd:nix flake metadata --refresh github:LiGoldragon/CriomOS?rev=339143a6df30...#output Revision 339143a6df30 and criomos-home a2ac795f0e21; basis: output; reopen: if those commits are unavailable or remote history is rewritten
- Zeus FullOS Boot deploy using exact CriomOS revision 339143a6df30 and builder prometheus succeeded. — evidence: cmd:lojix-cli '(FullOs goldragon zeus ... Boot (Some prometheus) None)'#output zeus_fullos_boot=success; basis: output; reopen: if Zeus is redeployed, rolled back, or boot profile changes
- After the Zeus Boot deploy, Zeus's system profile matched the deployed generation but /run/current-system did not, because Boot installs the next boot generation without live switching. — evidence: cmd:ssh root@zeus final state check#output system_profile_matches_deploy=yes current_system_matches_deploy=no; basis: output; reopen: if Zeus reboots, switches, rolls back, or /run/current-system changes
- Direct HomeOnly Activate for bird on Zeus built and copied the closure but failed because direct SSH as bird was not authorized. — evidence: cmd:lojix-cli '(HomeOnly goldragon zeus bird ... Activate (Some prometheus) None)'#stderr bird@zeus.goldragon.criome Permission denied; basis: output; reopen: if bird direct SSH authorization changes or lojix-cli remote HomeOnly activation changes
- Root SSH to Zeus works while direct SSH as bird failed in this session. — evidence: cmd:ssh probes for root@zeus.goldragon.criome and bird@zeus.goldragon.criome#output root ssh probe ok; bird ssh probe failed Permission denied; basis: output; reopen: if SSH keys, users, or host access policy changes
- The first root-run bird Home Manager activation leaked root's runtime environment into dconf and failed with permission errors. — evidence: cmd:ssh root@zeus root-run bird activation#stderr dconf unable to open /root/.nix-profile/share/dconf/profile/user and Could not connect Permission denied; basis: output; reopen: if the activation method or environment handling changes
- Rerunning bird Home Manager activation from root with HOME=/home/bird, USER=bird, LOGNAME=bird, XDG_RUNTIME_DIR=/run/user/1000, and bird's DBus session bus succeeded. — evidence: cmd:ssh root@zeus clean bird home activation#output zeus_bird_clean_home_activate=success; basis: output; reopen: if bird's user id, runtime directory, Home Manager activation, or session bus changes
- Bird's Home Manager profile matched the activated generation and bird had zero failed user units after daemon-reload/reset-failed. — evidence: cmd:ssh root@zeus final state check#output bird_home_profile_matches_activate=yes bird_failed_user_units=0; basis: output; reopen: if bird's Home Manager profile, user units, or activated generation changes
- Bird's live Niri config reload succeeded by discovering the socket under /run/user/1000 and setting NIRI_SOCKET for niri msg action load-config-file. — evidence: cmd:ssh root@zeus niri socket reload#output niri_socket=present niri_reload=success; basis: output; reopen: if bird's Niri process, socket path, or compositor config changes
- Bird's Pi profile now has pi-continue: the symlink exists, pi list contains packages/pi-continue, and fresh Pi RPC command discovery registers /continue. — evidence: cmd:ssh root@zeus bird Pi state check#output bird_pi_continue_symlink=yes bird_pi_list_contains_continue=yes bird_pi_continue_command_registered=yes; basis: output; reopen: if bird's Home Manager profile, Pi settings, or Pi packages change
- The deploy-wrapper research report was written at /home/li/primary/reports/pi-operator/1-zeus-deploy-and-lojix-wrapper-research-2026-06-04.md and pushed on primary commit 07af836d. — evidence: cmd:jj commit/push in /home/li/primary#output main | pi-operator: record zeus deploy wrapper research at 07af836d; basis: output; reopen: if primary history is rewritten or the report is edited
- Primary bead primary-yt58 tracks packaging a lojix deploy wrapper with logs, redaction, exact refs, and postchecks, and is in progress. — evidence: cmd:bd create and bd show primary-yt58#output primary-yt58 and IN_PROGRESS; basis: output; reopen: if primary-yt58 status, scope, or acceptance criteria changes
- The pi-operator lock is currently held for primary-yt58 on /git/github.com/LiGoldragon/CriomOS-home. — evidence: cmd:./tools/orchestrate claim pi-operator '[primary-yt58]' /git/github.com/LiGoldragon/CriomOS-home#output pi-operator.lock [primary-yt58]; basis: output; reopen: if the pi-operator lock is released or changed
- CriomOS-home has uncommitted lojix-run helper changes adding checks/lojix-run/default.nix and packages/lojix-run/default.nix and modifying flake.nix and modules/home/profiles/min/default.nix. — evidence: cmd:jj diff --summary in /git/github.com/LiGoldragon/CriomOS-home#output A checks/lojix-run/default.nix; M flake.nix; M modules/home/profiles/min/default.nix; A packages/lojix-run/default.nix; basis: output; reopen: if the CriomOS-home working copy is edited, reverted, committed, or abandoned
- The in-progress lojix-run Nix check currently fails or at least surfaces Python validation problems: an invalid escape sequence around a left-parenthesis pattern and multiple E501 line-too-long errors in packages/lojix-run/default.nix. — evidence: cmd:nix build .#checks.x86_64-linux.lojix-run --no-link --print-build-logs#build-log SyntaxWarning invalid escape sequence and E501 line too long; basis: test; reopen: if packages/lojix-run/default.nix is edited and the lojix-run check is rerun
- Prometheus current/default boot entry was previously generation 48 and llama-router used llama-cpp-9404. — evidence: previous-brief:Critical Context#Prometheus generation 48 and llama-cpp-9404; basis: output; reopen: if Prometheus is redeployed, rebooted, or llama-router configuration changes

## Learned
- Write cloud fresh from the desired shape; the old prototype can be mined for ideas but is not implementation authority. — source: previous brief Key Decisions; Spirit 2629
- cloud owns provider execution, with Cloudflare DNS and redirects first; domain-criome owns provider-neutral domain meaning and projection. — source: previous brief Goal and Key Decisions
- Signal contract repos stay wire-only, while daemon runtime repos own Nexus/SEMA schemas and runtime logic. — source: previous brief Key Decisions
- Generated schema artifacts are foundation but must remain generator-exact; regenerate or restore exact output instead of committing formatter-only drift. — source: previous brief Key Decisions
- Pi threshold compaction is manual in core; pi-continue is the chosen workaround for same-session continuation after Pi stops on length. — source: /home/li/.local/share/criomos/pi/package/src/core/agent-session.ts:1783-1785 and previous brief Key Decisions
- Persistent Pi extensions belong in CriomOS-home as Nix derivations exposed through stable ~/.pi/agent/packages symlinks, not in stateful ~/.pi/agent npm installs or npmCommand mutation. — source: user@previous-cycle Pi install correction and /git/github.com/LiGoldragon/CriomOS-home/docs/pi-extensions.md
- When GitHub API rate limiting makes Nix branch refs stale, exact pushed revision refs are a safe deploy workaround for CriomOS and CriomOS-home. — source: cmd:nix flake metadata --refresh github:LiGoldragon/CriomOS/main and exact-rev metadata comparison
- lojix-cli HomeOnly remote activation currently assumes direct SSH as the target user; valid root-only access requires a clean runuser fallback with the target user's HOME, runtime dir, DBus session bus, and profile path. — source: session experience: bird direct SSH failed, root clean activation succeeded
- Root-spawned Niri reload cannot assume NIRI_SOCKET; discover the compositor socket under /run/user/<uid> and run niri msg as the target user with that socket. — source: cmd:ssh root@zeus niri reload attempts#missing NIRI_SOCKET then niri_reload=success
- Avoid naming a zsh remote variable path because zsh treats lowercase path specially and can break command lookup; use command_path or another neutral name. — source: session experience: bird Pi state check failed with runuser command not found after assigning path
- lojix wrapper work should preserve lojix-cli's one-NOTA request surface; near-term shell/Python helper can standardize logs/redaction/exact refs/postchecks, while long-term lojix should own typed deploy ledgers. — source: /home/li/primary/reports/pi-operator/1-zeus-deploy-and-lojix-wrapper-research-2026-06-04.md
- A FullOS Boot deploy updates the target system profile for next boot but does not live switch /run/current-system; use Switch or reboot if live system equality is required. — source: cmd:ssh root@zeus final state check#system_profile_matches_deploy=yes current_system_matches_deploy=no
- For human-facing deploy reports, print action, node, line counts, hashes, and postcheck booleans rather than raw logs or store paths. — source: /home/li/primary/reports/pi-operator/1-zeus-deploy-and-lojix-wrapper-research-2026-06-04.md

## Open
- Does the new lojix-run helper pass its Nix check after fixing Python validation errors? — verifies: Edit /git/github.com/LiGoldragon/CriomOS-home/packages/lojix-run/default.nix to resolve the invalid escape and E501 errors, then run nix build .#checks.x86_64-linux.lojix-run --no-link --print-build-logs and observe exit 0.
- Does primary-yt58's acceptance pass enough to commit and close the deploy-wrapper task? — verifies: After the lojix-run check passes, run a smoke or documented check showing one-argument behavior, redaction, exact-ref handling, and postcheck booleans; commit/push CriomOS-home and update bd primary-yt58 accordingly.
- Should CriomOS pin a future CriomOS-home commit that contains lojix-run, and should that profile be activated on ouranos or Zeus? — verifies: Either update /git/github.com/LiGoldragon/CriomOS/flake.lock to the new CriomOS-home commit and deploy, or record an explicit decision that the helper remains available only from CriomOS-home until a later system pin.
- Should Zeus be rebooted or live-switched into the deployed 339143a6 system generation? — verifies: Get human approval or a clear deploy policy, then run the appropriate Boot/Switch/reboot path and confirm /run/current-system matches the deployed generation if live equality is required.
- Does pi-continue actually keep this Pi session working across threshold compaction during the long keep-working run? — verifies: Observe the agent resume after Pi threshold compaction and complete at least one post-compaction tool/action batch without manual restart, then report the result.
- How should cloud/src/schema_runtime.rs be connected to the daemon socket request/response flow? — verifies: Read cloud/src/daemon.rs, cloud/src/runtime.rs, cloud/src/client.rs, and cloud/src/schema_runtime.rs, implement routing through generated cloud::schema::{nexus,sema} types where practical, and add tests that exercise daemon socket requests.
- How should live Cloudflare IO and durable or redb-backed state enter the fresh cloud schema-runtime path? — verifies: Design and implement provider IO plus a durable store for policies, plans, approvals, and last-known provider state; add checks proving schema_runtime paths use the store and provider adapter.
- Can the first Cloudflare DNS slice list zones, list records, validate desired records, prepare a plan, require meta approval, and apply approved changes? — verifies: Implement the DNS flow in cloud with tests or a safe live dry-run using a confirmed token handle, then run cargo check/tests.
- Which Cloudflare token path is canonical: expected gopass:cloudflare/api-token or existing cloudflare.com/token? — verifies: Ask the human or inspect gopass entry names without revealing secret bytes; close by copying/renaming only by handle and verifying by path/status/byte count.
- How should Cloudflare redirect read/write support be implemented? — verifies: Research Cloudflare Rulesets/Page Rules APIs, add provider-neutral cloud/domain shape if needed, and implement after the DNS path is solid.
- What Prometheus safety work remains for swap, restart-loop cleanup, console or OOB access, llama-server token, and BootOnce deploy safety? — verifies: Inspect Prometheus current config/state, apply the next safe maintenance slice, and capture commands and outcomes without exposing secrets.
- Can browser-use ever safely control the live default Chrome profile, or should Playwright Extension remain the only working path? — verifies: Run only isolated-profile tests or explicit human-approved live-profile tests; close with a report and updated guidance.
- When will Spirit private records and the privacy selector be live in deployed production spirit? — verifies: Check deployed spirit version/records and implementation status, then update the open item when production supports private records safely.
- What local AI toolkit curation remains on Prometheus or designated AI nodes? — verifies: Review open reports/beads for local AI toolkit prefetching and run only node-appropriate Nix/cache/model maintenance.

## Next
- Fix /git/github.com/LiGoldragon/CriomOS-home/packages/lojix-run/default.nix so the generated Python passes Nix's Python validation, especially the invalid left-parenthesis escape and E501 long lines reported by the latest build. → The next lojix-run check gets past the current Python validation failures or surfaces a new concrete error.
- Rerun nix build .#checks.x86_64-linux.lojix-run --no-link --print-build-logs in /git/github.com/LiGoldragon/CriomOS-home. → A clean exit proves the helper package/check builds, or the failure log gives the next fix.
- Inspect jj diff/status in CriomOS-home after the check and run any additional smallest relevant profile/package build needed to prove lojix-run is in the min deployment package surface. → The working copy has only intended changes and enough Nix evidence for commit.
- Commit and push the CriomOS-home lojix-run helper change with jj, moving main directly. → CriomOS-home main has a pushed commit for the helper, or a blocker explains why it was not committed.
- Update primary-yt58 with the check/commit outcome and close it if the acceptance criteria are met. → The bead state matches reality and no hidden follow-up is lost.
- Decide whether to propagate the new CriomOS-home commit through CriomOS flake.lock and deploy/activate it on ouranos or Zeus. → Either CriomOS pins and deploys the helper profile, or deferral is explicit.
- Release the pi-operator lock after the CriomOS-home helper work is committed, deferred, or safely reverted. → orchestrate/pi-operator.lock returns to idle or clearly names any remaining held scope.
- Continue the pi-continue keep-working test by checking fresh intent/open beads and selecting the next safe maintenance item if the helper work finishes. → The session keeps producing useful work after compaction, or failure to continue becomes evidence about pi-continue.
- When returning to cloud work, claim/check cloud is free before editing. → No lock conflict with other cloud/operator lanes.
- Connect cloud/src/schema_runtime.rs to daemon socket request/response flow using generated cloud::schema::{nexus,sema} types where practical. → Daemon paths exercise the fresh schema runtime instead of only isolated tests.
- Implement Cloudflare DNS read path for zones and records, then plan/approval/apply flow for DNS changes. → The first safe Cloudflare DNS slice works before redirect/ruleset support begins.
- Align the Cloudflare gopass token path by handle only before live provider calls. → Live Cloudflare testing is unblocked without exposing token bytes.
- Keep Prometheus safety and local-AI work separate from the cloud implementation lane. → System maintenance does not corrupt the cloud repo workflow or credentials boundary.
