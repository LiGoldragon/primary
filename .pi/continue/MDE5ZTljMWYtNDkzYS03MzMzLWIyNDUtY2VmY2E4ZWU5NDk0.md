## Task
Investigate why Pi with `openai-codex/gpt-5.5` is failing with repeated `fetch failed`, without exposing secrets or running unrelated live actions.

## Done When
The failure mode is tied to an evidenced cause or narrowed to a precise remaining boundary, with a safe remediation or next diagnostic path reported to the human.

## Forbid
- Use `jj`; work on `main`; commit/push completed work. — source: previous-compaction-summary:Constraints & Preferences
- Do not edit `/git/github.com/LiGoldragon/lojix` while system-designer holds the lojix coordination lock. — source: previous-compaction-summary:Constraints & Preferences
- Do not run remediation/deploy/session/live activation commands without explicit user go-ahead. — source: previous-compaction-summary:Constraints & Preferences
- Do not run QEMU-backed NixOS VM checks except on an explicitly designated/authorized VM-testing host; verify the boundary or ask first. — source: previous-compaction-summary:Constraints & Preferences; Spirit record qnf8
- Do not treat the interrupted CriomOS-test-cluster VM batch as evidence; only structural checks are valid after the user correction. — source: previous-compaction-summary:Critical Context

## Established
- Horizon pan-cluster domain schema/projection work landed on main at commit 072334a9 with `horizon: add cluster domain configuration`. — evidence: commit:072334a9; basis: output; reopen: if Horizon main is rewound or the domain projection files are edited after 072334a9
- CriomOS DNS consumer work landed on main at commit 154b0402 with `CriomOS: emit public cluster DNS aliases`. — evidence: commit:154b0402; basis: output; reopen: if CriomOS main is rewound or `modules/nixos/network/dnsmasq.nix`/resolver policy checks change after 154b0402
- CriomOS-test-cluster local lock/proposal/fixture migration exists in the working copy and remains uncommitted. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-test-cluster && jj st#output:AGENTS.md INTENT.md clusters fixtures flake.lock modified; basis: output; reopen: if CriomOS-test-cluster is committed, restored, or rebased
- CriomOS-test-cluster proposal domain tail syntax accepted as `(criome [fieldlab.criome.net])`; rejected syntax produced `expected DomainConfiguration to hold 2 root objects, found 3`. — evidence: previous-compaction-summary:Progress Done CriomOS-test-cluster proposal fixtures migrated; basis: output; reopen: if Horizon proposal parser or CriomOS-test-cluster cluster NOTA syntax changes
- CriomOS-test-cluster non-VM structural checks `projections-match-fieldlab`, `pod-missing-super-node-rejected`, and `multiple-tailnet-controllers-rejected` passed earlier after the domain seam migration. — evidence: previous-compaction-summary:Progress Done non-VM structural checks passed; basis: test; reopen: if CriomOS-test-cluster lock, clusters, fixtures, or those checks change
- CriomOS-test-cluster core non-VM structural checks also passed this cycle for projections, rejection checks, `cluster-contracts`, and `full-module-contracts`. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-test-cluster && nix build --no-link --print-build-logs .#checks.x86_64-linux.projections-match-fieldlab .#checks.x86_64-linux.pod-missing-super-node-rejected .#checks.x86_64-linux.multiple-tailnet-controllers-rejected .#checks.x86_64-linux.cluster-contracts .#checks.x86_64-linux.full-module-contracts#exit-0; basis: test; reopen: if CriomOS-test-cluster lock, clusters, fixtures, or those checks change
- Workspace testing skill now documents that QEMU-backed NixOS VM checks run only on explicitly designated or authorized VM-testing hosts/builders. — evidence: /home/li/primary/skills/testing.md:69; basis: observed; reopen: if `/home/li/primary/skills/testing.md` changes around the QEMU-backed VM checks section
- The VM-testing host boundary documentation was committed and pushed on primary main at commit 49a6f0b5. — evidence: cmd:cd /home/li/primary && jj commit -m 'testing: document VM-testing host boundary' && jj bookmark set main -r @- && jj git push --bookmark main#output:main kpomonwr 49a6f0b5; basis: output; reopen: if primary main is rewound or `skills/testing.md` changes around the VM-testing section
- CriomOS-test-cluster `AGENTS.md` working copy now says its `vm-*` checks and `lojix-deploy-smoke` require an explicitly authorized VM-testing host/builder, while structural/eval checks can run on ordinary hosts. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/AGENTS.md:20; basis: observed; reopen: if `AGENTS.md` changes around the test rules or the working copy is restored
- CriomOS-test-cluster `INTENT.md` working copy now lists VM-testing host authorization as a non-negotiable for QEMU-backed checks. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/INTENT.md:5; basis: observed; reopen: if `INTENT.md` changes around the non-negotiables or the working copy is restored
- Cloud-designer report 66 handed cloud-operator bead `primary-hpkj` for DigitalOcean Phase-1 live hardening and Tier-2 follow-up. — evidence: /home/li/primary/reports/cloud-designer/66-cloud-operator-handoff.md:1; basis: doc; reopen: if report 66 is rewritten or bead primary-hpkj is retired/replaced
- Cloud P0 gopass path fix was already on cloud main at commit 7f190c36, changing DigitalOcean token lookup to `digitalocean.com/api-token`. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && jj st && rg -n 'digitalocean.com/api-token' flake.nix#output:Parent commit 7f190c36 main; basis: output; reopen: if cloud main is rewound before 7f190c36 or `flake.nix` changes the DigitalOcean gopass path
- Cloud main now includes hardened DigitalOcean Phase-1 non-live integration at commit 3b38cdd8. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && jj commit -m 'cloud: harden DigitalOcean live test path' && jj bookmark set main -r @- && jj git push --bookmark main#output:main 3b38cdd8 cloud: harden DigitalOcean live test path; basis: output; reopen: if cloud main is rewound before 3b38cdd8 or the DigitalOcean integration files change
- Cloud `tests/digitalocean_live.rs` is an ignored live test that generates a per-run ED25519 SSH key, creates a unique key/droplet name, performs a pre-droplet key write, polls for Running, and cleans up droplet/key through RAII plus explicit deletes. — evidence: /git/github.com/LiGoldragon/cloud/tests/digitalocean_live.rs:1; basis: observed; reopen: if `tests/digitalocean_live.rs` changes
- Cloud DigitalOcean API trait and `HttpApi` now include `delete_ssh_key` for live-test cleanup. — evidence: /git/github.com/LiGoldragon/cloud/src/digitalocean.rs:120; basis: observed; reopen: if `src/digitalocean.rs` changes around the DigitalOcean `Api` trait or `HttpApi` impl
- Cloud now has `examples/write_config.rs` to emit a binary rkyv `DaemonConfiguration` file for daemon startup. — evidence: /git/github.com/LiGoldragon/cloud/examples/write_config.rs:1; basis: observed; reopen: if `examples/write_config.rs` changes or is removed
- Cloud flake now exposes a DigitalOcean-enabled package/app surface and Nix-owned DigitalOcean live-test app/checks. — evidence: /git/github.com/LiGoldragon/cloud/flake.nix:82; basis: observed; reopen: if `flake.nix` changes around packages, apps, or DigitalOcean checks
- Cloud non-live validation passed after the DigitalOcean hardening changes. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && nix flake check --print-build-logs#exit-0; basis: test; reopen: if any cloud source, test, Cargo, or flake file changes
- Cloud DigitalOcean package build passed after the hardening changes. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && nix build .#digitalocean --no-link --print-build-logs#exit-0; basis: test; reopen: if any cloud source, Cargo, or flake file changes
- Cloud feature-enabled clippy passed for `digitalocean,cloudflare` across all targets after the hardening changes. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && cargo clippy --locked --features digitalocean,cloudflare --all-targets -- -D warnings#exit-0; basis: test; reopen: if any cloud Rust source or Cargo file changes
- Cloud-operator report 391 documents the DigitalOcean Phase-1 handoff audit and landed non-live integration, but it is still only in primary working copy because other lanes have uncommitted primary changes. — evidence: /home/li/primary/reports/cloud-operator/391-digitalocean-phase1-handoff-landed.md:1; basis: observed; reopen: if primary commits report 391, restores it, or rewrites it
- Bead `primary-hpkj` remains open with a note that P0-P2 non-live cloud integration landed at 3b38cdd8 and Tier-2 live remains unrun. — evidence: cmd:cd /home/li/primary && bd show primary-hpkj#output:NOTES Cloud-operator landed P0-P2 non-live integration on cloud main at 3b38cdd8; basis: output; reopen: if bead `primary-hpkj` is closed, edited, or reassigned
- The Pi failure screenshot shows repeated `fetch failed` for `gpt5.5` without a more specific error visible in the screenshot. — evidence: /tmp/pi-clipboard-cc1fb180-a959-4862-855c-0e7ea4fbed06.png; basis: observed; reopen: if a different screenshot/log shows a more specific error for the same failure
- Pi settings select default provider `openai-codex` and default model `gpt-5.5`, with `openai-codex/gpt-5.5` enabled. — evidence: cmd:python3 redact /home/li/.pi/agent/settings.json#output:defaultProvider openai-codex defaultModel gpt-5.5 enabledModels openai-codex/gpt-5.5; basis: output; reopen: if `/home/li/.pi/agent/settings.json` changes
- Pi auth has an `openai-codex` OAuth credential with access token, refresh token, account id, and expiry 2026-06-23T09:42:28Z, so the local auth record is present and not expired during this investigation. — evidence: cmd:python3 inspect /home/li/.pi/agent/auth.json openai-codex redacted#output:expires_iso_utc 2026-06-23T09:42:28Z seconds_until_expiry 322324; basis: output; reopen: if `/home/li/.pi/agent/auth.json` changes or current time passes the expiry
- Basic DNS/TLS reachability to OpenAI/ChatGPT/Auth domains works from the host; unauthenticated HEAD requests returned HTTP 403/401 rather than DNS or TLS failure. — evidence: cmd:getent hosts chatgpt.com api.openai.com auth.openai.com && python3 HEAD https://chatgpt.com https://api.openai.com/v1/models https://auth.openai.com#output:403 401 403; basis: output; reopen: if network conditions change or later probes fail before HTTP status
- The installed Pi tree's only literal `gpt-5.5` search hit was the GitLab Duo custom-provider example, while the active settings point to built-in `openai-codex/gpt-5.5`. — evidence: cmd:rg -n 'gpt-5\.5|openai-codex' /home/li/.local/share/criomos/pi/package /home/li/.pi#output:custom-provider-gitlab-duo gpt-5.5 plus settings openai-codex/gpt-5.5; basis: output; reopen: if Pi packages/extensions/settings change

## Learned
- For VM/QEMU checks, documentation alone is not enough; add mechanical Nix feature gating so ordinary builders report the boundary instead of starting QEMU. — source: previous-compaction-summary:Key Decisions QEMU prevention should be mechanical
- Do not assume remote Nix builder dispatch is safe for VM/QEMU; Nix previously sent VM work to `ssh-ng://nix-ssh@prometheus.goldragon.criome`, so VM checks still need explicit VM-testing authorization. — source: previous-compaction-summary:Key Decisions Do not assume remote builder dispatch is safe
- CriomOS default `system`/`horizon` stubs intentionally fail; use temporary override flakes for targeted structural/policy checks. — source: previous-compaction-summary:Constraints & Preferences
- Pan-cluster domains live in Horizon `DomainConfiguration`; CriomOS consumes projected data rather than deriving public-domain policy itself. — source: previous-compaction-summary:Key Decisions
- CriomOS-test-cluster proposal shape is positional; domain tail is `(criome [fieldlab.criome.net])`, not `(DomainConfiguration ...)`. — source: previous-compaction-summary:Key Decisions
- A committed live cloud test needs both in-test cleanup and an external sweep; Rust `Drop` handles normal unwind paths but not SIGKILL or every process-kill window. — source: /home/li/primary/reports/cloud-operator/391-digitalocean-phase1-handoff-landed.md:17
- For Cloud DigitalOcean write-scope probing, a stable `ensure_ssh_key` name can be read-only if the key already exists; generate a unique throwaway SSH key/name to force a pre-droplet key creation. — source: /git/github.com/LiGoldragon/cloud/tests/digitalocean_live.rs:1
- In cloud, `nix flake check` evaluates packages but does not prove a package output builds; explicitly `nix build .#digitalocean --no-link --print-build-logs` for the DO package. — source: session experience: flake check evaluated packages, then explicit nix build .#digitalocean proved the package
- The public `signal_cloud::IpAddress` used by cloud exposes `as_str()`, while the generated schema type inspected elsewhere exposes `payload()`; use the public root accessor in cloud tests. — source: session experience: `digitalocean-live-test-compiles` failed on `.payload()` and passed after switching to `.as_str()`
- If the orchestration claim helper reports an overlap with an idle or missing lock, inspect `orchestrate/*.lock` before touching shared repos; this session manually appended the cloud claim after confirming no visible holder. — source: cmd:cd /home/li/primary && cat orchestrate/cloud-operator.lock && ls -la orchestrate | rg 'cloud|maintainer' && ./tools/orchestrate status cloud-maintainer

## Open
- What exact Pi code path turns the `openai-codex/gpt-5.5` failure into generic `fetch failed`? — verifies: Locate the installed/bundled OpenAI Codex provider implementation or runtime stack trace/session log for the failing request, without printing tokens.
- Is the current Pi `fetch failed` caused by provider endpoint/API drift, OAuth account/token rejection despite non-expired local auth, a proxy/IPv6/network issue, or a Pi bug in request shaping? — verifies: Run a redacted minimal diagnostic that captures endpoint, HTTP status/error class, and request path for `openai-codex/gpt-5.5`, or reproduce through Pi with debug logs that redact Authorization.
- Where are Pi session logs for the screenshot failure stored on this machine? — verifies: Find Pi session/transcript/log files corresponding to the failing run and inspect redacted error metadata around the failed model request.
- CriomOS-test-cluster still needs a mechanical VM/QEMU guard such as `requiredSystemFeatures = [ "criomos-vm-testing" ];` on every QEMU/`runNixOSTest` derivation plus a clear preflight/error. — verifies: Read `flake.nix` and check definitions, add the guard to every VM-backed derivation, and run non-VM eval/build checks proving ordinary hosts do not start QEMU.
- CriomOS-test-cluster local domain seam migration plus VM-boundary documentation remains uncommitted. — verifies: After guard/source-constraint decisions, `jj st` in CriomOS-test-cluster is clean and `jj log -r main` shows the migration commit pushed.
- CriomOS-test-cluster `source-constraints` check surfaced a non-VM blocker from the newer CriomOS input involving `modules/nixos/llm.nix: goldragon`. — verifies: Rerun `.#checks.x86_64-linux.source-constraints` with full logs, identify the exact constraint violation, and decide whether to fix in CriomOS or adjust the consumer pin.
- Tier-2 DigitalOcean full-daemon live chain remains unrun after cloud commit 3b38cdd8. — verifies: With explicit human live authorization, run the Nix-owned Tier-2 sequence against real DigitalOcean, capture create/observe/destroy evidence, and confirm no leftover billable resources.
- Bead `primary-hpkj` remains open because acceptance requires Tier-1 live green on main and Tier-2 full-daemon live create-observe-destroy green against real DigitalOcean. — verifies: Run authorized live checks on cloud main and update/close bead `primary-hpkj` with droplet id, IPv4, teardown evidence, and clean inventory.
- Primary report `reports/cloud-operator/391-digitalocean-phase1-handoff-landed.md` is not committed because primary also contains other lanes' uncommitted report/protocol changes. — verifies: Coordinate with the owning lane or wait until primary is safe to commit; then commit/push all primary working-copy changes according to primary's whole-working-copy rule.
- LojixOS rename/config-crate split and `lojix-daemon` production cutover remain pending after the domain seam. — verifies: When lojix coordination permits, inspect the current lojix repos, update stale Horizon pins, implement the split/cutover, and run relevant gates.
- The `lojix` horizon-lib pin/update remains pending while system-designer holds the lojix coordination lock/authorization is unresolved. — verifies: `orchestrate/system-designer.lock` no longer claims lojix or the human explicitly authorizes coordination, then update the pin and run the agreed gates.
- VM/QEMU checks remain blocked until run on an explicitly authorized VM-testing host or mechanically guarded by Nix feature gating. — verifies: Human names the authorized VM-testing host/builder, or guarded derivations refuse to evaluate/build on ordinary hosts without starting QEMU.
- `spirit-nspawn-can-build` remains blocked by private/missing `nota-derive` fetch. — verifies: Dependencies are made available or the check is adjusted with explicit overrides, then the check exits 0.
- Full CriomOS flake check remains blocked by default stubs/private fetch paths unless proper overrides/deps are supplied. — verifies: Run full CriomOS flake check with correct overrides/dependencies and capture exit 0, or record the exact remaining failing attr.

## Next
- Continue the Pi investigation by locating the installed OpenAI Codex provider implementation and the failing session log/trace for `openai-codex/gpt-5.5`, keeping credentials redacted. → A concrete error path beyond generic `fetch failed`, or a documented boundary showing which logs/code are unavailable.
- If provider code/logs are found, run only safe redacted diagnostics to distinguish network, OAuth, endpoint/API, and request-shaping failures. → A short diagnosis the human can act on without exposing tokens.
- Report the Pi `gpt-5.5` failure findings to the human with the evidence already gathered: settings/auth present, basic OpenAI reachability works, and the remaining narrowed suspect. → The human knows the likely cause and the next remediation or escalation step.
- Do not commit primary yet; coordinate around the uncommitted cloud-designer/designer primary files before landing report 391 and bead/protocol changes. → Primary history does not accidentally sweep another lane's in-progress work.
- For CriomOS-test-cluster, rerun/inspect the `source-constraints` failure with full logs and decide whether it must be fixed before committing the domain seam migration. → A concrete fix or documented blocker for the non-VM source-constraints check.
- Implement VM/QEMU guard in CriomOS-test-cluster on every VM-backed check, then rerun only non-VM structural/eval checks. → Ordinary hosts cannot accidentally start QEMU, and safe checks still pass.
- Commit and push the CriomOS-test-cluster lock/proposal/fixture/docs migration plus VM guard once non-VM blockers are resolved or explicitly documented. → CriomOS-test-cluster main contains the downstream domain seam migration without unauthorized VM execution.
- Only with explicit human authorization, run cloud Tier-2 full-daemon DigitalOcean live chain from cloud main and update bead `primary-hpkj`. → Tier-2 live evidence or a precise live blocker, with no leftover billable resources.
- When lojix coordination permits, update lojix's stale `horizon-lib` pin to current Horizon and run relevant lojix gates. → Lojix is unblocked for the rename/config-crate split on current Horizon contracts.
- Continue LojixOS rename/config-crate split design/implementation after the safe domain seam is fully landed. → The original LojixOS production-transition goal resumes from a clean, guarded downstream state.
