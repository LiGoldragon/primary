## Task
Implement and explain safe VM-testing defaults: gate CriomOS-test-cluster VM checks on explicit builder features in an isolated branch, and outline how Lojix should become the safe typed Nix invocation layer.

## Done When
VM-backed checks require `nixos-test` plus `criomos-vm-testing`, a non-VM check proves the gate without QEMU, the worktree branch is committed and pushed, and the user receives the requested explanation and design path.

## Forbid
- Do not expose OAuth tokens, API keys, or Authorization headers. — source: previous brief Constraints & Preferences
- Do not run remediation/deploy/session/live activation commands without explicit user go-ahead. — source: previous brief Constraints & Preferences
- Do not run QEMU-backed NixOS VM checks except on an explicitly designated/authorized VM-testing host. — source: previous brief Constraints & Preferences
- Do not treat the interrupted CriomOS-test-cluster VM batch as evidence; only structural checks are valid after the user correction. — source: previous brief Constraints & Preferences
- Do not edit `/git/github.com/LiGoldragon/lojix` while system-designer holds the lojix coordination lock. — source: previous brief Constraints & Preferences
- Do not commit primary yet; other lanes have uncommitted primary changes. — source: previous brief Constraints & Preferences
- Previous forbid remains: do not use `jj`; do not work on `main`; do not commit/push completed work. — source: previous brief Constraints & Preferences
- Do not bypass the active `cloud-maintainer` lock on `/git/github.com/LiGoldragon/cloud` for the Cloudflare credential-path patch. — source: cmd:./tools/orchestrate claim pi-operator /git/github.com/LiGoldragon/cloud/flake.nix ...#lock-conflict-output
- Do not bypass the active `system-designer` lock on `/git/github.com/LiGoldragon/goldragon` for the doris data merge unless the psyche explicitly authorizes override. — source: cmd:./tools/orchestrate status#system-designer-lock-output

## Established
- Pi settings select `defaultProvider=openai-codex`, `defaultModel=gpt-5.5`, and enable `openai-codex/gpt-5.5`; stored OAuth auth and minimal no-session Pi request were good during the prior investigation. — evidence: previous-compaction-summary:Progress/Done#Pi-settings-auth-minimal-request; basis: output; reopen: if Pi settings, Pi auth storage, or the Pi provider package changes
- The diagnosed Pi `fetch failed` was a Pi OpenAI Codex transport-layer failure, not missing config, expired auth, or a global OpenAI outage. — evidence: ~/.pi/agent/sessions/--home-li-primary--/2026-06-06T08-49-16-602Z_019e9c1f-493a-7333-b245-cefca8ee9494.jsonl:9319-9322; basis: observed; reopen: if a newer failing session persists `error.cause` or shows a different provider/model/auth failure
- The Pi diagnosis report exists at `/home/li/primary/reports/pi-operator/10-openai-codex-gpt-5-5-fetch-failed.md`. — evidence: /home/li/primary/reports/pi-operator/10-openai-codex-gpt-5-5-fetch-failed.md; basis: observed; reopen: if the report is removed or replaced
- Horizon pan-cluster domain schema/projection work landed at commit `072334a9`. — evidence: previous-compaction-summary:Progress/Done#commit-072334a9; basis: output; reopen: if horizon-rs main moves to revert or supersede that commit
- CriomOS DNS consumer work landed at commit `154b0402`. — evidence: previous-compaction-summary:Progress/Done#commit-154b0402; basis: output; reopen: if CriomOS main moves to revert or supersede that commit
- Workspace testing guidance documents that QEMU-backed NixOS VM checks run only on explicitly authorized VM-testing hosts/builders, committed and pushed at primary commit `49a6f0b5`. — evidence: previous-compaction-summary:Progress/Done#primary-commit-49a6f0b5; basis: doc; reopen: if `/home/li/primary/skills/testing.md` changes around the VM-testing host rule
- Cloud main includes hardened DigitalOcean Phase-1 non-live integration at commit `3b38cdd8`; non-live validation previously passed `nix flake check`, `nix build .#digitalocean`, and feature-enabled clippy. — evidence: previous-compaction-summary:Progress/Done#cloud-commit-3b38cdd8; basis: test; reopen: if `/git/github.com/LiGoldragon/cloud` changes around DigitalOcean integration or validation is rerun and fails
- CriomOS-home owns user Home Manager profile configuration, while hexis is the mechanism for managed mutable Pi JSON settings. — evidence: previous-compaction-summary:Turn Context/Early Progress#CriomOS-home-hexis-boundary; basis: doc; reopen: if CriomOS-home or hexis architecture docs change that ownership boundary
- `/git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/pi-models.nix` was edited to add `piSettingsConfig.transport = "websocket"` and hexis mode `"/transport" = "always"`. — evidence: edit:/git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/pi-models.nix#2-blocks-replaced; basis: observed; reopen: if `modules/home/profiles/min/pi-models.nix` changes around `piSettingsConfig` or hexis `modes`
- The Pi Home profile change evaluated far enough to produce the `pi-harness-profile` derivation path successfully. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-home && nix eval --raw .#checks.x86_64-linux.pi-harness-profile.drvPath#stdout-ok; basis: test; reopen: if CriomOS-home inputs or `modules/home/profiles/min/pi-models.nix` change
- `nix flake show --all-systems` in CriomOS-home failed on an existing unsupported-system package issue unrelated to the Pi transport edit. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-home && nix flake show --json --all-systems#stderr-unsupported-system; basis: output; reopen: if CriomOS-home flake/package outputs change and `nix flake show --all-systems` is rerun
- `/git/github.com/LiGoldragon/cloud` currently encodes the Cloudflare token path as `cloudflare/api-token` in the flarectl wrapper. — evidence: /git/github.com/LiGoldragon/cloud/flake.nix:46; basis: observed; reopen: if `/git/github.com/LiGoldragon/cloud/flake.nix` changes around the flarectl wrapper
- The corrected secret at `gopass cloudflare.com/api-token` is usable for direct Cloudflare zone listing, returning HTTP 200 success with five visible zones. — evidence: cmd:python3 Cloudflare direct checks for cloudflare.com/api-token#stdout-zone-list-http-200-count-5; basis: output; reopen: if the gopass secret changes or Cloudflare API responses change
- `flarectl --json zone list` with `CF_API_TOKEN` populated from `gopass cloudflare.com/api-token` exits 0 and returns a JSON list count of five. — evidence: cmd:flarectl --json zone list with CF_API_TOKEN from gopass cloudflare.com/api-token#stdout-exit-0-count-5; basis: test; reopen: if the gopass secret, flarectl, Cloudflare account, or wrapper path changes
- Direct read-only DNS-record listing with the corrected Cloudflare token succeeds for all five visible zones, with record counts 1, 10, 1, 0, and 1. — evidence: cmd:python3 Cloudflare DNS records per zone#stdout-zone-record-counts; basis: output; reopen: if the gopass secret, Cloudflare zones, or token permissions change
- Cloud non-live checks for the Cloudflare feature passed `cargo test --locked --features cloudflare --all-targets`. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && cargo test --locked --features cloudflare --all-targets#test-result-ok; basis: test; reopen: if `/git/github.com/LiGoldragon/cloud` source or lockfile changes
- Cloud non-live clippy passed `cargo clippy --locked --features cloudflare --all-targets -- -D warnings`. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && cargo clippy --locked --features cloudflare --all-targets -- -D warnings#exit-0; basis: test; reopen: if `/git/github.com/LiGoldragon/cloud` source or lockfile changes
- Cloud default package derivation evaluation passed with `nix eval --raw .#packages.x86_64-linux.default.drvPath`. — evidence: cmd:cd /git/github.com/LiGoldragon/cloud && nix eval --raw .#packages.x86_64-linux.default.drvPath#stdout-ok; basis: test; reopen: if `/git/github.com/LiGoldragon/cloud/flake.nix` or flake inputs change
- Spirit record `nsi2` was changed in place to make `cloudflare.com/api-token` the canonical cloud Cloudflare token path. — evidence: cmd:spirit /tmp/cloud-cloudflare-path-change.nota#stdout-RecordChanged-nsi2; basis: output; reopen: if Spirit record `nsi2` is changed again or retired
- The cloud Cloudflare token test report exists and was updated at `/home/li/primary/reports/pi-operator/11-cloud-cloudflare-token-test.md`. — evidence: /home/li/primary/reports/pi-operator/11-cloud-cloudflare-token-test.md; basis: observed; reopen: if that report is removed or superseded
- Spirit records `878r`, `5pf6`, `zeqq`, and `q4gd` were observed for WebHost/doris, low-trust cloud nodes, provision-on-role, and NodeSpecies-derived facets. — evidence: cmd:spirit PublicTextSearch [doris cloud-node WebHost website Zola] and [NodeSpecies BehavesAs TypeIs cloud-node]#RecordsObserved; basis: output; reopen: if those Spirit records are changed or retired
- `horizon-rs` branch `cloud-designer-cloud-node-species` was integrated into `main` via merge commit `bd1cc2c1` and pushed; validation passed `cargo test --locked` and `cargo clippy --locked --all-targets -- -D warnings`. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/horizon-rs#main-bd1cc2c1 and cmd:cargo test/clippy on merge tree#passed; basis: test; reopen: if horizon-rs main moves off that merge or relevant files change
- `horizon-rs` branch `cloud-designer-web-host` was integrated into `main` and pushed at commit `4a0e29fe`, adding `NodeService::WebHost`; validation passed `cargo test --locked` and clippy before push. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/horizon-rs#main-4a0e29fe; basis: test; reopen: if horizon-rs main moves off `4a0e29fe` or WebHost files change
- Bead `primary-n98t` received notes for the horizon-rs cloud-node-species and web-host operator landings. — evidence: cmd:bd note primary-n98t#stdout-Note-added; basis: output; reopen: if bead `primary-n98t` is changed to remove or supersede those notes
- The operator-side cloud-node/WebHost landing report exists at `/home/li/primary/reports/pi-operator/12-cloud-node-operator-side.md`. — evidence: /home/li/primary/reports/pi-operator/12-cloud-node-operator-side.md; basis: observed; reopen: if that report is removed or superseded
- `goldragon` branch `cloud-designer-cloud-node-data` exists and is pushed, but remains unmerged because `/git/github.com/LiGoldragon/goldragon` is held by `system-designer`. — evidence: cmd:cd /git/github.com/LiGoldragon/goldragon && jj bookmark list --all#cloud-designer-cloud-node-data and cmd:orchestrate status#system-designer-goldragon-lock; basis: output; reopen: if the system-designer lock clears or goldragon main moves to include the branch
- A separate CriomOS worktree `/home/li/wt/github.com/LiGoldragon/CriomOS/pi-operator-web-host-testing` was created from CriomOS main for WebHost testing/module work. — evidence: cmd:jj workspace add --name pi-operator-web-host-testing --revision main ...#Created-workspace; basis: output; reopen: if the worktree is removed or rebased
- CriomOS branch/bookmark `pi-operator-web-host-testing` was pushed and currently points at commit `297d7e0f` in the worktree, after commits adding the WebHost module, Zola fixture fixes, and fixture template witness. — evidence: cmd:cd /home/li/wt/github.com/LiGoldragon/CriomOS/pi-operator-web-host-testing && jj log --no-graph -r pi-operator-web-host-testing#297d7e0f; basis: output; reopen: if the bookmark moves or is deleted
- The CriomOS WebHost branch adds `modules/nixos/web-host.nix`, imports it from `modules/nixos/criomos.nix`, and wires `checks.x86_64-linux.web-host-policy` in `flake.nix`. — evidence: cmd:jj diff --stat in CriomOS pi-operator-web-host-testing#files-added-and-modified; basis: observed; reopen: if the branch files change or the branch is rebased
- The CriomOS WebHost policy check builds a Zola fixture and verifies no nginx/firewall without WebHost plus nginx, ACME, ports 80/443, and rendered artifact content with WebHost. — evidence: /home/li/primary/reports/pi-operator/13-web-host-testing-worktree.md; basis: doc; reopen: if `checks/web-host-policy/default.nix` changes or the report is superseded
- Touched-file formatting passed for the CriomOS WebHost branch using `nix fmt -- --check modules/nixos/web-host.nix modules/nixos/criomos.nix checks/web-host-policy/default.nix flake.nix`. — evidence: cmd:nix fmt -- --check modules/nixos/web-host.nix modules/nixos/criomos.nix checks/web-host-policy/default.nix flake.nix#exit-0; basis: test; reopen: if any of those touched files change
- The CriomOS WebHost pure check passed with synthetic `system` and `horizon` overrides and no QEMU. — evidence: cmd:nix build --refresh --override-input system path:/tmp/criomos-system-x86_64 --override-input horizon path:/tmp/criomos-horizon-minimal .#checks.x86_64-linux.web-host-policy --no-link#build-success; basis: test; reopen: if the branch files, synthetic overrides, or flake inputs change
- Bead `primary-unig` was noted with the CriomOS WebHost testing/module branch and validation status. — evidence: cmd:bd note primary-unig#stdout-Note-added; basis: output; reopen: if bead `primary-unig` is changed to remove or supersede that note
- The WebHost testing worktree report exists at `/home/li/primary/reports/pi-operator/13-web-host-testing-worktree.md`. — evidence: /home/li/primary/reports/pi-operator/13-web-host-testing-worktree.md; basis: observed; reopen: if that report is removed or superseded
- Spirit record `75pw` was accepted for the direction that practical Nix build/test/deploy invocations should default to a safe typed Lojix interface that verifies eligible workers and builder placement before emitting Nix commands. — evidence: cmd:spirit /tmp/lojix-safe-nix-default.nota#stdout-RecordAccepted-75pw; basis: output; reopen: if Spirit record `75pw` is changed or retired
- A separate CriomOS-test-cluster worktree `/home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates` was created from `main` for VM feature gating work. — evidence: cmd:jj workspace add --name pi-operator-vm-feature-gates --revision main ...#Created-workspace; basis: output; reopen: if the worktree is removed or rebased
- The main `/git/github.com/LiGoldragon/CriomOS-test-cluster` checkout had uncommitted cloud-operator changes when the VM feature-gate branch was started. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-test-cluster && jj status#Working-copy-changes; basis: output; reopen: if the main checkout becomes clean or those changes are committed
- `CriomOS-test-cluster` has no `ARCHITECTURE.md` at the main checkout path during this cycle. — evidence: read:/git/github.com/LiGoldragon/CriomOS-test-cluster/ARCHITECTURE.md#ENOENT; basis: observed; reopen: if `/git/github.com/LiGoldragon/CriomOS-test-cluster/ARCHITECTURE.md` is added
- In the VM feature-gate worktree, `lib/mkVmTest.nix` was edited so the generated `runNixOSTest` derivation appends `requiredSystemFeatures = [ "nixos-test" "criomos-vm-testing" ]` via `overrideAttrs`. — evidence: edit:/home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates/lib/mkVmTest.nix#requiredSystemFeatures-block; basis: observed; reopen: if `lib/mkVmTest.nix` changes or a pure derivation-attribute check fails
- In the VM feature-gate worktree, `lib/mkDeployTest.nix` was edited so the `lojix-deploy-smoke` `runNixOSTest` derivation appends `requiredSystemFeatures = [ "nixos-test" "criomos-vm-testing" ]` via `overrideAttrs`. — evidence: edit:/home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates/lib/mkDeployTest.nix#requiredSystemFeatures-block; basis: observed; reopen: if `lib/mkDeployTest.nix` changes or a pure derivation-attribute check fails
- The orchestration helper path `/home/li/primary/tools/orchestrate` and `./tools/orchestrate` failed with code 127 during the latest turn, even though `/home/li/primary/orchestrate` exists. — evidence: cmd:/home/li/primary/tools/orchestrate claim ...#code-127 and cmd:ls -la /home/li/primary#orchestrate-dir-tools-empty; basis: output; reopen: if the helper path reappears or a different orchestrate invocation succeeds

## Learned
- For Pi persistent mutable JSON settings, set Home Manager declarations in CriomOS-home and use hexis per-key modes; `transport = "websocket"` should be `always`-managed so Home activation reasserts it. — source: session experience: edited CriomOS-home `pi-models.nix` and validated `pi-harness-profile` derivation
- Cloudflare permissions are not the same as credential kind: the original 32-byte value at `cloudflare.com/api-token` was an account id and failed as an auth header, while the corrected 53-byte token can list zones and DNS records. — source: cmd:Cloudflare direct API and flarectl checks before and after token replacement
- The Cloudflare `/user/tokens/verify` endpoint returned `401 Invalid API Token` even while zone and DNS-record read paths succeeded, so actual cloud read-path tests are more relevant than that endpoint alone for this token. — source: cmd:Cloudflare corrected-token checks#verify-401-zone-200-records-200
- Do not rewrite pushed immutable designer branches in jj; if a pushed branch diverges from current main, make an operator merge commit and validate the merge tree. — source: session experience: `jj rebase -b cloud-designer-cloud-node-species -d main` failed on immutable commits, merge commit `bd1cc2c1` succeeded
- CriomOS pure checks often need synthetic `system` and `horizon` flake overrides because the default stubs intentionally throw; build the named check with overrides rather than treating the stub failure as a module failure. — source: cmd:nix build WebHost policy check#no-system-and-no-horizon-stub-failures-then-success-with-overrides
- Whole-tree `nix fmt -- --check .` in CriomOS currently fails on pre-existing unformatted files; for branch validation, run touched-file formatting checks unless the task is to normalize the whole tree. — source: cmd:nix fmt -- --check .#pre-existing-unformatted-files
- Current Zola rejects the old `highlight_code = false` config field, and a minimal Zola fixture needs a template to render markdown content instead of Zola’s default welcome page. — source: session experience: WebHost policy check failures fixed by removing `highlight_code` and adding `site/templates/index.html`
- For QEMU/NixOS-test safety, the repo-side default should gate VM-backed checks with both Nix’s `nixos-test` feature and a workspace-specific `criomos-vm-testing` feature; a pure check should prove the derivation attributes without booting QEMU. — source: user@turn-prefix latest plus session edits to `mkVmTest.nix` and `mkDeployTest.nix`
- The safe long-term shape for practical Nix calls is a typed Lojix layer that takes operation intent, containment, required features, and builder policy, verifies eligible workers, then emits raw Nix invocations only after safety constraints pass. — source: Spirit record 75pw
- The Immich agentic media mirror should be maintained in tandem with WebHost as an eventual public-hosting use case, but the Spirit capture for that coupling was not completed before the latest user interruption. — source: user@history-to-summarize Immich prompt and failed `spirit /tmp/immich-webhost-intent.nota` attempts

## Open
- Finish the current CriomOS-test-cluster VM feature-gate implementation: add a pure non-VM check that proves `mkVmTest` and `mkDeployTest` derivations require both `nixos-test` and `criomos-vm-testing`, wire it into `flake.nix`, format, validate, commit, and push the worktree branch. — verifies: In `/home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates`, add the check, run touched-file `nix fmt -- --check`, run a non-QEMU `nix build` or `nix eval` of the new check, then `jj commit -m ...`, set/push bookmark `pi-operator-vm-feature-gates`, and confirm `jj status` clean.
- Explain to the user how the implemented VM gate works and how the broader Lojix safe Nix language should work by default. — verifies: After the branch is validated, answer with repo-side implementation, system-side requirement for builder features, and Lojix design: typed operation, capability requirements, containment class, builder discovery/scheduling, and fail-closed raw Nix emission.
- Prometheus or another authorized VM-testing host still needs system-side builder configuration advertising `nixos-test` and `criomos-vm-testing`. — verifies: A system-designer/system-operator branch or committed cluster/system config shows the selected builder’s `supportedFeatures` include both strings, and a dispatch-only check shows VM-test derivations select that builder without running QEMU on the workstation.
- Retry or otherwise complete the Immich/WebHost Spirit capture after fixing `/tmp/immich-webhost-intent.nota`; the last retry failed because referents were not lowercase kebab-case and was edited but not rerun before interruption. — verifies: Run `spirit /tmp/immich-webhost-intent.nota` after confirming balanced NOTA and lowercase referents; close this when stdout is `RecordAccepted <id>` or an existing record is changed instead.
- Patch `/git/github.com/LiGoldragon/cloud` to use `cloudflare.com/api-token` in `flake.nix`, `README.md`, `docs/first-cloudflare-slice.md`, and `INTENT.md` once the cloud-maintainer lock clears or the psyche authorizes override. — verifies: Cloud repo diff replaces `cloudflare/api-token` with `cloudflare.com/api-token`, docs and Spirit intent agree, non-live cloud checks pass, and the patch is committed by an authorized lane.
- Merge `goldragon` branch `cloud-designer-cloud-node-data` once the `system-designer` goldragon lock clears or the psyche authorizes override. — verifies: `goldragon` main includes the doris CloudNode data branch, validation for the cluster data passes, and bead `primary-n98t` records the landing.
- Integrate the CriomOS WebHost testing/module branch `pi-operator-web-host-testing` when locks and design sequencing permit. — verifies: Review branch `297d7e0f` or its successor, run the named WebHost policy check, merge to the appropriate integration path, and update bead `primary-unig`.
- Decide and implement the four WebHost product decisions that gate the NixOS module and doris provisioning: renderer default, source model, config surface, and whether to provision doris now. — verifies: The psyche answers the four decisions or an accepted Spirit record covers them; then CriomOS/goldragon/cloud work proceeds accordingly.
- CriomOS-test-cluster `source-constraints` still has the prior non-VM blocker involving newer CriomOS input and `modules/nixos/llm.nix: goldragon`. — verifies: Rerun the structural `source-constraints` check on the relevant branch and either get a clean pass or document/patch the remaining offender.
- Cloud Tier-2 DigitalOcean full-daemon live create-observe-destroy remains blocked pending explicit human live authorization. — verifies: The human explicitly authorizes the live chain; the live test runs with cleanup and external sweep; bead `primary-hpkj` is updated or closed.
- `spirit-nspawn-can-build` remains blocked by private/missing `nota-derive` fetch. — verifies: The private dependency/fetch path is supplied or replaced and the build check is rerun successfully.
- Full CriomOS flake check remains blocked by default stubs/private fetch paths unless proper overrides/deps are supplied. — verifies: Run `nix flake check` with the proper system/horizon/deployment overrides and required private dependencies, or document remaining blockers.
- LojixOS rename/config-crate split, `lojix-daemon` production cutover, and `lojix` `horizon-lib` pin/update remain pending while coordination lock/authorization is unresolved. — verifies: Coordination lock clears or human authorizes, then the lojix worktree is updated, validated, and committed/pushed by the correct lane.

## Next
- Continue in `/home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates` and add the pure non-VM check that inspects the VM/deploy test derivations for `requiredSystemFeatures` containing both `nixos-test` and `criomos-vm-testing`. → A wired `checks.*` witness proves the safety gate without starting QEMU.
- Run touched-file formatting and a non-QEMU validation command for the new CriomOS-test-cluster check; avoid `nix flake check` or any command that can start QEMU locally. → Validation output suitable to cite in the user explanation and report.
- Commit and push bookmark `pi-operator-vm-feature-gates` from the isolated worktree, then confirm `jj status` is clean. → The implementation is durable and shareable without touching the locked main checkout.
- Write or update a concise pi-operator report for the VM feature-gate branch and add a bead note if `bd` is available. → Workspace memory records what changed, how it was validated, and what remains system-side.
- Answer the user with the implementation status and the requested explanation of how the safe Lojix Nix subset should become the default. → The human gets both the concrete repo-side change and the architectural path for Lojix-managed builder selection.
- After the current VM-gate response, retry `spirit /tmp/immich-webhost-intent.nota` or otherwise capture the Immich/WebHost coupling correctly. → The durable Immich media-mirror public-hosting direction is recorded instead of left in a failed temp file.
