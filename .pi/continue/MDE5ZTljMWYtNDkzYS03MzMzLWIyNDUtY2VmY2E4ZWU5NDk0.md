## Task
As cloud-operator, implement and test the cloud-designer report-55 VM-testing handoff: merge the horizon-test-vm branches in dependency order, update pins/fixtures, run strong gates, then proceed to Unit 3/live/Prometheus follow-on work within the safety envelope.

## Done When
Done when horizon-rs, CriomOS, and CriomOS-test-cluster mains are updated and pushed with the relevant gates green, subagent findings handled, working copies clean, and any live/Prometheus work either implemented with explicit authorization for live runs or clearly deferred.

## Forbid
- Do not run remediation/deploy/session commands without explicit user go-ahead. — source: previous-compaction-summary#Constraints
- Live Home/Niri/desktop-affecting activation requires explicit user authorization. — source: previous-compaction-summary#Constraints
- Do not run HomeOnly Activate locally on a target as the target user; if authorized deploy path fails, stop/ask. — source: previous-compaction-summary#Constraints
- Use jj; do not use git CLI directly except documented escape hatches. — source: previous-compaction-summary#Constraints
- Work directly on main for primary; commit/push completed work. — source: previous-compaction-summary#Constraints
- Use lojix-cli directly for current production Stack A; do not trust lojix-run exact-ref rewrite behavior until fixed. — source: previous-compaction-summary#Constraints
- Cloud work must use cloud-operator; desktop/app diagnosis uses system-operator. — source: previous-compaction-summary#Constraints
- Do not log intent from cloud-designer’s Lojix production-cutover research. — source: previous-compaction-summary#Constraints
- Treat secrets parity as mandatory for production cutover; secret-free zeus build/eval is non-authoritative only. — source: previous-compaction-summary#Constraints
- Do not add a generic build timeout for Lojix parity; only activate-SSH needs cancel-kill behavior per research. — source: previous-compaction-summary#Constraints
- Ignored real-nix daemon/deploy smoke tests should not be run unless explicitly authorized. — source: previous-compaction-summary#Constraints
- Do not use reports/operator for this lane; cloud-operator reports go under reports/cloud-operator. — source: user@history:your-the-cloude-operator-not-operator

## Established
- Primary report reports/operator/359-lojix-modern-component-audit-2026-06-10.md was written and pushed in primary commit a2139ff6a42c. — evidence: previous-compaction-summary#Progress-Done; basis: output; reopen: if primary history is rewritten to remove commit a2139ff6a42c or the report path changes
- signal-lojix was modernized and pushed through commits 97a41186929c and 8f6c498a0f90; root Cargo workspace tests passed with and without nota-text. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if signal-lojix main is rewritten before those commits or the contract layout is replaced
- meta-signal-lojix was modernized and pushed at 2e69371b9cb0; root workspace was added, signal-lojix was switched to a git dependency, generated schema refreshed, and tests passed with and without nota-text. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if meta-signal-lojix main is rewritten before 2e69371b9cb0 or generated schema surfaces are replaced
- Lojix binary-only daemon startup slice was committed and pushed as 6a58f7c4cc64, making lojix-daemon require triad_runtime signal-file startup and reject inline NOTA and .nota startup files. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if lojix daemon startup code changes or daemon startup accepts inline NOTA or .nota paths again
- After binary-startup work, direct daemon rejection checks returned exit 2 for inline NOTA and .nota file arguments with stderr `(DaemonRejected [component argument error: expected a signal-encoded file path])`. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if daemon argument parsing or rejection text changes
- Lojix CLI NOTA edge was implemented, tested, committed, and pushed as e47f3642058f on main. — evidence: previous-compaction-summary#Early-Progress; basis: test; reopen: if lojix client parsing/rendering code changes or main is rewritten before e47f3642058f
- Lojix owner socket peer credential checks were implemented with OwnerPeerAuthority, tested under both feature gates, documented, committed, and pushed as 55577f0f6e3d. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/lojix#main-55577f0f; basis: test; reopen: if lojix owner-socket authorization code or main history changes
- Lojix DaemonConfiguration::write_rkyv_file was added, build_smoke was updated to use it, daemon_configuration round-trip test passed, and the slice was committed and pushed as f530de28d2ce. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/lojix#main-f530de28; basis: test; reopen: if DaemonConfiguration rkyv read/write code changes or main history changes
- Cloud-designer flattened lojix, signal-lojix, and meta-signal-lojix after f530de28; Lojix paths shifted from triad-port/src to src and main reached 13192bce at that point. — evidence: cmd:find . -maxdepth 2 -type f in /git/github.com/LiGoldragon/lojix#shows-./src/client.rs-./src/daemon.rs-./src/lib.rs; basis: output; reopen: if the Lojix repo layout changes again
- Current Lojix main later advanced to 538fdebf with REAL hermetic Test-op dispatch proven end-to-end; signal-lojix main is cc8bbf32 and meta-signal-lojix main is 1dbecc08 for the Test-op shared types. — evidence: cmd:jj bookmark list --all across lojix signal-lojix meta-signal-lojix#main-538fdebf-cc8bbf32-1dbecc08; basis: output; reopen: if any of those repos advance or are rewritten
- Spirit record oh9l decides Lojix cutover durable state is durable-first on sema-engine before real cutover, including live-generation-set, GC-roots, event-log state, and self-resume. — evidence: cmd:spirit "(Lookup oh9l)"#RecordFound-oh9l; basis: output; reopen: if Spirit record oh9l is clarified, superseded, or retired
- Spirit record lc28 decides substituter resolution moves into the Lojix daemon provisionally, with the wire reverting to bare node names and implementation marked must-be-replaced-by-better-design. — evidence: cmd:spirit "(Lookup lc28)"#RecordFound-lc28; basis: output; reopen: if Spirit record lc28 is clarified, superseded, or retired
- Cloud-designer report 55 is the actionable handoff for cloud-operator to merge horizon-test-vm branches and implement remaining VM-testing work. — evidence: /home/li/primary/reports/cloud-designer/55-operator-implementation-handoff.md:1; basis: doc; reopen: if report 55 is edited or superseded
- Cloud-operator review report was moved to reports/cloud-operator/387-cloud-designer-vm-testing-review.md and primary main was pushed at c0b946a2. — evidence: cmd:jj bookmark list --all in /home/li/primary#main-c0b946a2; basis: output; reopen: if primary main advances with a move or rename of report 387
- The user authorized implementing the report-55 handoff, asked to test it well, and allowed subagents. — evidence: user@turn-prefix:go-ahead-test-it-well-you-can-use-subagents; basis: user; reopen: if the user narrows or revokes that authorization
- Three reviewer subagents were dispatched non-blocking for horizon-rs, CriomOS, and CriomOS-test-cluster handoff review under run a8a47971-c0dd-407d-b4d7-1d9e9a395784. — evidence: subagent:a8a47971-c0dd-407d-b4d7-1d9e9a395784#dispatch; basis: output; reopen: if the subagent run is interrupted or returns findings that change the plan
- The orchestrate claim helper failed before claiming because orchestrate-cli could not fetch stale nota-codec revision f761421c383121a04df761a444a3778d1c0b137d. — evidence: cmd:./tools/orchestrate claim cloud-operator ...#exit-101-nota-codec-f761421c-not-found; basis: output; reopen: if tools/orchestrate is fixed and a claim is successfully acquired
- horizon-rs main was fast-forwarded to horizon-test-vm@origin commit 214e6816d27e and pushed. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/horizon-rs#main-214e6816; basis: output; reopen: if horizon-rs main advances or is rewritten
- horizon-rs gates cargo build, cargo test, and cargo clippy -- -D warnings passed after main was moved to 214e6816d27e. — evidence: cmd:cargo build && cargo test && cargo clippy -- -D warnings in /git/github.com/LiGoldragon/horizon-rs#finished-test-profile-and-no-error; basis: test; reopen: if horizon-rs source or Cargo inputs change
- CriomOS horizon-test-vm branch originally forked from the older next line; it was rebased onto current main 856c79d7 with conflicts resolved, preserving newer Spirit/Lojix pins and VM-testing additions. — evidence: cmd:jj log -r 'main::horizon-test-vm' in /git/github.com/LiGoldragon/CriomOS#rebased-stack-ends-206e4444; basis: output; reopen: if CriomOS main advances or the rebased stack is replaced
- CriomOS main was pushed at 206e4444 after integrating VM-testing modules. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/CriomOS#main-206e4444; basis: output; reopen: if CriomOS main advances or is rewritten
- CriomOS full nix flake check without overrides is expected to fail because the default system input throws `CriomOS: no system input was provided`. — evidence: cmd:nix flake check --refresh in /git/github.com/LiGoldragon/CriomOS#error-no-system-input-was-provided; basis: output; reopen: if CriomOS default flake inputs are changed to evaluable defaults
- CriomOS full nix flake check with temporary overrides progressed but hit a pre-existing private/missing nota-derive fetch in repository-receive-role-policy. — evidence: cmd:nix flake check --refresh --override-input system ... --override-input horizon ... in /git/github.com/LiGoldragon/CriomOS#nota-derive-fetch-failure; basis: output; reopen: if the repository-receive-role-policy dependency graph or private fetch availability changes
- CriomOS-test-cluster horizon-test-vm branch was checked out as a child of 46febf36; result symlink was present and removed in the working copy. — evidence: cmd:ls -l result in /git/github.com/LiGoldragon/CriomOS-test-cluster#result-symlink-to-nix-store; basis: output; reopen: if the result symlink is reintroduced or the integration commit changes
- CriomOS-test-cluster flake.nix was edited so criomos.url and horizon.url point to github:LiGoldragon/CriomOS/main and github:LiGoldragon/horizon-rs/main instead of horizon-test-vm. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/flake.nix:7; basis: observed; reopen: if flake.nix changes around the criomos or horizon inputs
- GitHub API rate limiting blocked nix flake lock updates for CriomOS-test-cluster, so the lock nodes were manually refreshed from local source hashes to horizon-rs 214e6816, CriomOS 206e4444, and Lojix 538fdebf. — evidence: cmd:nix flake lock --update-input horizon --update-input criomos --update-input lojix#error-403-api-rate-limit; basis: output; reopen: if nix flake lock succeeds normally or the lockfile is regenerated
- CriomOS-test-cluster fieldlab.nota and auxiliary negative fixtures were updated from stale Machine arities to the 12-field Machine shape with disk_gb, location, and super_nodes tails. — evidence: cmd:rg -n "\((Metal|Pod)" clusters/*.nota in /git/github.com/LiGoldragon/CriomOS-test-cluster#all-lines-show-tail-fields; basis: observed; reopen: if cluster fixture Machine records change
- CriomOS-test-cluster horizon fixtures atlas, beacon, cedar, dune, mercury, edge-desktop, and base-home were regenerated with the merged horizon-cli after Machine arity fixes. — evidence: cmd:/git/github.com/LiGoldragon/horizon-rs/target/debug/horizon-cli --cluster fieldlab --node ... > fixtures/horizon/*.json#jj-diff-fixtures-updated; basis: output; reopen: if clusters/fieldlab.nota, horizon-rs, or fixture JSON changes
- As of subagent status check, run a8a47971-c0dd-407d-b4d7-1d9e9a395784 was still running with one reviewer active, two reviewer children failed, and zero of three completed. — evidence: subagent:status-a8a47971-c0dd-407d-b4d7-1d9e9a395784#running-1-running-2-failed-0-of-3-done; basis: output; reopen: if the subagent run status changes
- Available subagent logs for run a8a47971-c0dd-407d-b4d7-1d9e9a395784 contained partial context and no blocking finding before local validation continued. — evidence: /tmp/pi-subagents-uid-1001/async-subagent-runs/a8a47971-c0dd-407d-b4d7-1d9e9a395784/output-0.log:1; basis: observed; reopen: if a completed subagent output adds findings
- The orchestrate claim helper still fails with exit 101 on the same stale nota-codec f761421c383121a04df761a444a3778d1c0b137d dependency before recording a cloud-operator claim. — evidence: cmd:./tools/orchestrate claim cloud-operator /git/.../complex.nix ...#exit-101-nota-codec-f761421c-not-found; basis: output; reopen: if tools/orchestrate is fixed or a claim is successfully acquired
- The auxiliary negative fixtures now fail for their intended Horizon projection reasons after the RouterInterfaces backup-wireless arity was updated. — evidence: cmd:horizon-cli --cluster fieldlab --node atlas < clusters/fieldlab-pod-missing-super-node.nota and fieldlab-two-controllers.nota#missing-super-node-and-multiple-tailnet-controller-errors; basis: output; reopen: if clusters/fieldlab-pod-missing-super-node.nota, clusters/fieldlab-two-controllers.nota, or RouterInterfaces schema changes
- CriomOS aggregate imports modules/nixos/complex.nix for every node. — evidence: /git/github.com/LiGoldragon/CriomOS/modules/nixos/criomos.nix:17; basis: observed; reopen: if the CriomOS module aggregate import list changes
- CriomOS modules/nixos/complex.nix now accepts deployment.includeComplex with a default of true and wraps its config in lib.mkIf includeComplex. — evidence: /git/github.com/LiGoldragon/CriomOS/modules/nixos/complex.nix:8; basis: observed; reopen: if modules/nixos/complex.nix changes around includeComplex or the Clavifaber service
- CriomOS-test-cluster mkVmTest.nix threads deployment.includeComplex = false into hermetic VM guests. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/mkVmTest.nix:374; basis: observed; reopen: if lib/mkVmTest.nix changes around guest specialArgs deployment
- CriomOS-test-cluster mkDeployTest.nix threads deployment.includeComplex = false into the deploy smoke guest configuration. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/mkDeployTest.nix:416; basis: observed; reopen: if lib/mkDeployTest.nix changes around deployment specialArgs
- CriomOS-test-cluster deploy-flake.nix threads deployment.includeComplex = false in both generated deploy-flake text and in-process toplevelFor derivation. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/deploy-flake.nix:148; basis: observed; reopen: if lib/deploy-flake.nix changes around deployment specialArgs
- After the includeComplex change, CriomOS-test-cluster vm-mercury passed with path overrides for horizon, criomos, and lojix. — evidence: cmd:nix build --no-write-lock-file --override-input horizon path:/git/github.com/LiGoldragon/horizon-rs --override-input criomos path:/git/github.com/LiGoldragon/CriomOS --override-input lojix path:/git/github.com/LiGoldragon/lojix .#checks.x86_64-linux.vm-mercury#PASS-vm-mercury; basis: test; reopen: if CriomOS, CriomOS-test-cluster, horizon-rs, lojix, or relevant flake inputs change
- After the includeComplex change, CriomOS-test-cluster vm-base-home passed with path overrides for horizon, criomos, and lojix. — evidence: cmd:nix build loop .#checks.x86_64-linux.vm-base-home#PASS-vm-base-home; basis: test; reopen: if CriomOS, CriomOS-test-cluster, horizon-rs, lojix, CriomOS-home, or relevant flake inputs change
- After the includeComplex change, CriomOS-test-cluster vm-dune still failed without a local chroma override. — evidence: cmd:nix build loop .#checks.x86_64-linux.vm-dune#FAIL-vm-dune-code-1; basis: test; reopen: if CriomOS-home/chroma pins change or vm-dune is rerun successfully without the local chroma override
- CriomOS-test-cluster vm-dune passed when overriding criomos/criomos-home/chroma to the local /git/github.com/LiGoldragon/chroma checkout. — evidence: cmd:nix build --override-input criomos/criomos-home/chroma path:/git/github.com/LiGoldragon/chroma .#checks.x86_64-linux.vm-dune#PASS-vm-dune-local-chroma; basis: test; reopen: if CriomOS-home's chroma input changes or vm-dune behavior changes
- Chroma main is commit 6879a393123ec147b2d8ddbe92caec498eb5646e on both local and origin bookmarks. — evidence: cmd:jj log -r main in /git/github.com/LiGoldragon/chroma#6879a393123ec147b2d8ddbe92caec498eb5646e; basis: output; reopen: if chroma main advances or is rewritten
- Normal CriomOS-home nix flake lock --update-input chroma was blocked by GitHub API HTTP 403 rate limiting. — evidence: cmd:nix flake lock --update-input chroma in /git/github.com/LiGoldragon/CriomOS-home#HTTP-403-rate-limit; basis: output; reopen: if GitHub API rate limiting clears and a normal lock update succeeds
- CriomOS-home flake.lock was updated so input chroma now points to github:LiGoldragon/chroma/6879a393123ec147b2d8ddbe92caec498eb5646e with narHash sha256-KTFFfMfx+BXXko1nzIE3XT275zHBqFjpYzPqwXcjrdk=, but the change is not yet committed in the transcript. — evidence: cmd:nix flake lock --override-input chroma github:LiGoldragon/chroma/6879a393123ec147b2d8ddbe92caec498eb5646e in /git/github.com/LiGoldragon/CriomOS-home#Updated-input-chroma-6879a393; basis: output; reopen: if CriomOS-home flake.lock changes again or the change is committed and pushed

## Learned
- This lane is cloud-operator; substantive review or implementation reports must go under reports/cloud-operator, not reports/operator. — source: user@history:your-the-cloude-operator-not-operator
- After cloud-designer flattening, do not reintroduce Lojix root workspaces or triad-port paths; current Lojix code paths are under repo-root src/ and tests/. — source: cmd:find . -maxdepth 2 -type f in /git/github.com/LiGoldragon/lojix#src-layout
- For forwarded cloud-designer prompts, gap-check Spirit and technical content instead of blindly duplicating intent capture; recent Spirit lookups found oh9l and lc28 but not gdgv. — source: cmd:spirit Lookup gdgv/oh9l/lc28#gdgv-not-found-oh9l-lc28-found
- Report 55 merge order is horizon-rs first, CriomOS second, CriomOS-test-cluster last because only the test-cluster carries cross-repo flake pins. — source: /home/li/primary/reports/cloud-designer/55-operator-implementation-handoff.md:1
- When CriomOS feature work forked from an immutable next bookmark, rebasing the stack onto current main with --ignore-immutable and pushing only main allowed conflict resolution without treating the shared next bookmark as the deliverable. — source: cmd:jj rebase --ignore-immutable -s next -d main in /git/github.com/LiGoldragon/CriomOS#rebased-7-commits
- For CriomOS checks, create temporary system and horizon override flakes from a synthetic fixture; default stubs intentionally throw and are not useful for full flake checks. — source: /git/github.com/LiGoldragon/CriomOS/stubs/no-system/flake.nix:1
- GitHub API rate limiting can block nix flake lock; use local path overrides for testing and, when necessary, copy already-reviewed locked nodes or local prefetch hashes instead of fetching new upstream revisions. — source: cmd:nix flake lock --update-input ...#HTTP-403-rate-limit
- horizon-rs Machine schema now requires the super_nodes tail; stale Nota fixtures fail projection before the intended invariant is tested. — source: cmd:nix log /nix/store/a9rm0mljw5hza4smgfm2fhfs1icqc86i-pod-missing-super-node-rejected.drv#Machine-arity-drift
- Subagent quiet-attention nudges are not necessarily blockers; inspect run status, then continue independent work if agents are still running. — source: subagent:a8a47971-c0dd-407d-b4d7-1d9e9a395784#status-running-3-agents
- For hermetic CriomOS VM tests, prefer deployment.includeComplex=false over stubbing Clavifaber when the complex-publication service is not the invariant under test; production default stays includeComplex=true. — source: /git/github.com/LiGoldragon/CriomOS/modules/nixos/complex.nix:8 and /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/mkVmTest.nix:374
- Do not disable deployment.includeHome to dodge Edge VM package failures; standardTest.nix uses includeHome to assert the kept-home activation invariant for role-derived standard tests. — source: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/standardTest.nix:1
- If Edge VM tests fail on private nota-derive after includeComplex is disabled, test with a local chroma override; vm-dune passing under local chroma identified stale CriomOS-home chroma pin as the blocker. — source: cmd:nix build --override-input criomos/criomos-home/chroma path:/git/github.com/LiGoldragon/chroma .#checks.x86_64-linux.vm-dune#PASS-vm-dune-local-chroma
- When GitHub API rate limiting blocks a flake input refresh, an exact-commit override can still update the lock if the full commit id is correct; a wrong truncated Chroma SHA produced a 404 before the correct 6879a393123ec147b2d8ddbe92caec498eb5646e succeeded. — source: cmd:nix flake lock --override-input chroma github:LiGoldragon/chroma/6879a393123ec147b2d8ddbe92caec498eb5646e#Updated-input-chroma

## Open
- Did the three reviewer subagents finish, and did any report findings require changes to the integration plan? — verifies: Run subagent status for a8a47971-c0dd-407d-b4d7-1d9e9a395784, read any output files under reports/cloud-operator/subagents/, and incorporate or explicitly dismiss each finding.
- Did the latest CriomOS-test-cluster cheap structural gate rerun fully pass after fixture fixes? — verifies: Inspect the command result or rerun nix build with --no-write-lock-file and path overrides for projections-match-fieldlab, pod-missing-super-node-rejected, multiple-tailnet-controllers-rejected, cluster-contracts, full-module-contracts, and spirit-nspawn-can-build.
- CriomOS-test-cluster integration changes are still uncommitted and not pushed to main. — verifies: Run jj status in /git/github.com/LiGoldragon/CriomOS-test-cluster, inspect diffs, commit the integration fix, set main to the commit, push main, and confirm @origin.
- The three runNixOSTest checks from the VM-testing suite still need to be run post-merge with current main pins. — verifies: Run the report-55 specified x86_64 CriomOS-test-cluster runNixOSTest checks, likely edge-desktop-boots-greeter, base-home-activates, and lojix-deploy-smoke, using path overrides if GitHub API rate limiting persists.
- Whether the manually refreshed CriomOS-test-cluster flake.lock uses correct GitHub narHashes for horizon-rs 214e6816, CriomOS 206e4444, and Lojix 538fdebf remains to be confirmed against a non-rate-limited lock refresh. — verifies: When GitHub API rate limit clears, run nix flake lock/update for horizon, criomos, and lojix or nix flake check without path overrides and compare lockfile hashes.
- Unit 3 scoped image-exchange emission from report 55 remains to be implemented after branch integration. — verifies: Follow report 55 Unit 3 file/line guidance, implement scoped image-exchange emission in CriomOS, and run the specified Nix checks.
- The live path that removes LiveNotYetEnabled and performs the real host-untouched cycle remains to be implemented, but first live run is psyche-gated. — verifies: Implement code paths and dry/hermetic checks from report 55; before any live run on Prometheus or routers, get explicit user authorization for that live run.
- Prometheus goldragon wiring remains open, including stale-arity Machine records in real goldragon/datom.nota before appending Prometheus VmHost data. — verifies: After integration gates pass, inspect goldragon/datom.nota, add the Machine None None [] tails to the five records, add Prometheus VmHost/test-VM nodes/TestDefaults per report 55, and run horizon projection checks.
- The VM-testing principle in reports/cloud-designer/50-general-vm-testing-interface/intent-capture.md is still pending Spirit capture because Spirit writes require store migration. — verifies: After spirit-migrate-store or maintainer resolution, record the pending principle in Spirit and cite the accepted record.
- tools/orchestrate claim is still broken on primary due stale nota-codec revision f761421c, so the cloud-operator claim was not recorded. — verifies: Fix or bypass orchestrate-cli dependency resolution, then run tools/orchestrate claim cloud-operator for the active repo surfaces and confirm the lock/claim state.
- CriomOS full nix flake check remains blocked by repository-receive-role-policy fetching private/missing nota-derive after system/horizon overrides. — verifies: Fix or provide access to the nota-derive dependency, then rerun full nix flake check with proper system/horizon overrides.
- Production Stack-B cutover parity still has broader non-VM gaps including secrets parity, packaging/systemd/request-builder surface, and safe non-activation test path. — verifies: Implement and test the remaining report-40 cutover gates without live deployment unless explicitly authorized.
- Real deploy/activation remains blocked without explicit authorization despite the user authorizing hermetic handoff testing. — verifies: Ask for and receive explicit live-run/deploy/activation authorization naming the target and operation.
- Legacy lojix-run ExactReferenceRewriter behavior remains untrusted for current production Stack A. — verifies: Read and fix lojix-run exact-ref rewrite behavior or replace it with the new daemon/request-builder path, then add tests proving exact ref behavior.
- Do the prior CriomOS targeted checks still pass after the new deployment.includeComplex edit in modules/nixos/complex.nix? — verifies: Rerun the targeted CriomOS nix builds with temporary system/horizon overrides for vm-testing-prometheus-policy, nspawn-role-policy, metal-firmware-policy, router-wifi-horizon-policy, and nix-role-policy after the includeComplex change.
- Is the CriomOS-home chroma lock update sufficient without a direct local chroma override? — verifies: Run the failing CriomOS-test-cluster vm-dune check with path override to the modified CriomOS-home checkout or after committing/pinning that checkout, but without --override-input criomos/criomos-home/chroma path:/git/github.com/LiGoldragon/chroma.
- CriomOS-home chroma lock update is uncommitted and not pushed, so downstream CriomOS cannot pin it yet. — verifies: Run an appropriate validation for /git/github.com/LiGoldragon/CriomOS-home, commit the flake.lock change with jj, set main to the commit, push main, and confirm @origin.
- CriomOS needs to pin the pushed CriomOS-home chroma-lock fix and carry the includeComplex production-default edit on main. — verifies: After CriomOS-home is pushed, update CriomOS flake.lock/input to that commit, rerun targeted checks, commit modules/nixos/complex.nix plus lock update, set main, push main, and confirm @origin.
- CriomOS-test-cluster VM gates must be rerun against committed/pinned CriomOS and CriomOS-home state, not only local path overrides. — verifies: After CriomOS and CriomOS-home are pushed and test-cluster lock/pins are refreshed, rerun vm-base-home, vm-dune, vm-edge-desktop, vm-mercury, and lojix-deploy-smoke with no direct chroma override and record pass/fail.

## Next
- In /git/github.com/LiGoldragon/CriomOS-test-cluster, run vm-dune with path overrides for horizon, criomos, lojix, and the modified /git/github.com/LiGoldragon/CriomOS-home checkout, but without a direct chroma override. → Confirm the CriomOS-home flake.lock chroma bump fixes the Edge VM failure through the real nested pin.
- Validate, commit, set main, push, and confirm /git/github.com/LiGoldragon/CriomOS-home with only the chroma flake.lock update. → CriomOS-home main provides a pushed commit that downstream CriomOS can pin.
- Update /git/github.com/LiGoldragon/CriomOS to the pushed CriomOS-home commit, keep the includeComplex default-on edit, rerun the affected targeted CriomOS checks, commit, set main, push, and confirm @origin. → CriomOS main carries the production-safe includeComplex gate and the fixed home dependency chain.
- In /git/github.com/LiGoldragon/CriomOS-test-cluster, refresh pins/lock to the new CriomOS main as needed, inspect jj status, and rerun the cheap structural gates after the Machine and RouterInterfaces fixture fixes plus includeComplex edits. → Structural checks either pass on the current integration state or expose a concrete failure to fix.
- Run the VM-testing runNixOSTest suite from CriomOS-test-cluster, including vm-base-home, vm-dune, vm-edge-desktop, vm-mercury, and lojix-deploy-smoke, without a direct chroma override once downstream pins are refreshed. → Hermetic boot/deploy checks prove the merged VM-testing system on current mains, or the remaining blockers are explicit.
- Commit the CriomOS-test-cluster integration cleanup, set main to the commit, push main, and confirm @origin. → The third report-55 branch is integrated to main with current horizon-rs, CriomOS, CriomOS-home, Chroma, and Lojix pins.
- Check subagent run a8a47971-c0dd-407d-b4d7-1d9e9a395784 status again and read any newly completed reviewer outputs. → Know whether the degraded reviewer run produced any late findings before final user status.
- Confirm horizon-rs, CriomOS-home, CriomOS, CriomOS-test-cluster, and primary working copies are clean and bookmarks match origin. → Integration state is auditable and ready for user status or follow-on work.
- Report concise status to the user, including gates passed, any blocked gates, subagent status, and whether the dependency-chain fix was pushed. → The human gets an accurate checkpoint before Unit 3/live/Prometheus follow-on work.
- Proceed to report-55 Unit 3 scoped image-exchange emission. → CriomOS emits the scoped image-exchange surface required by the handoff.
- Implement the report-55 live path and Prometheus wiring only within the host-untouched/user-namespace/no-sudo/no-router-switch safety envelope. → The live/multi-host remainder is ready for explicit first-live-run authorization.
