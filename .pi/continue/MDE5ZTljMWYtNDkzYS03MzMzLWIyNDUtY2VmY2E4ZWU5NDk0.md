## Task
Implement safe VM-check feature gating and explain a Lojix-mediated safe Nix-call default, while continuing WebHost/cloud branch work without violating active locks.

## Done When
VM-backed CriomOS-test-cluster checks require nixos-test plus criomos-vm-testing and have a pure guard check committed and pushed on a branch, no QEMU is run locally, and the human gets a concise explanation of how Lojix should verify and schedule safe Nix calls.

## Forbid
- Do not expose OAuth tokens, API keys, or Authorization headers. — source: previous-summary#Constraints-&-Preferences
- Do not run remediation/deploy/session/live activation commands without explicit user go-ahead. — source: previous-summary#Constraints-&-Preferences
- Do not run QEMU-backed NixOS VM checks except on an explicitly designated/authorized VM-testing host. — source: previous-summary#Constraints-&-Preferences
- Do not treat the interrupted CriomOS-test-cluster VM batch as evidence; only structural checks are valid after the user correction. — source: previous-summary#Constraints-&-Preferences
- Do not edit /git/github.com/LiGoldragon/lojix while system-designer holds the lojix coordination lock. — source: previous-summary#Constraints-&-Preferences
- Do not commit primary yet; other lanes have uncommitted primary changes. — source: previous-summary#Constraints-&-Preferences

## Established
- Pi settings through CriomOS-home now declare transport = websocket and manage /transport with hexis mode always in modules/home/profiles/min/pi-models.nix. — evidence: /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/pi-models.nix#transport; basis: observed; reopen: if /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/pi-models.nix changes around piSettingsConfig or hexis modes
- The CriomOS-home Pi harness profile derivation evaluates after the transport edit. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-home && nix eval --raw .#checks.x86_64-linux.pi-harness-profile.drvPath#ok; basis: output; reopen: if CriomOS-home flake, pi-models.nix, hexis inputs, or Pi package inputs change
- Cloudflare token testing report exists at reports/pi-operator/11-cloud-cloudflare-token-test.md and records the safe non-secret outcomes. — evidence: /home/li/primary/reports/pi-operator/11-cloud-cloudflare-token-test.md#Results; basis: observed; reopen: if the report is edited or new Cloudflare credential tests supersede it
- The corrected gopass cloudflare.com/api-token works for the actual Cloudflare read paths: direct zone listing returns HTTP 200 with 5 visible zones, flarectl zone list exits 0 with 5 zones, and DNS-record listing succeeds for all visible zones. — evidence: cmd:direct Cloudflare API plus flarectl tests with CF_API_TOKEN from gopass cloudflare.com/api-token#zone-list-http-200-flarectl-exit-0-records-http-200; basis: output; reopen: if the gopass secret at cloudflare.com/api-token changes, Cloudflare permissions change, or cloud changes its Cloudflare auth path
- Spirit record nsi2 was changed so cloud's canonical Cloudflare token path is cloudflare.com/api-token. — evidence: cmd:spirit /tmp/cloud-cloudflare-path-change.nota#RecordChanged-nsi2; basis: output; reopen: if Spirit record nsi2 is superseded or changed again
- The cloud repo still encodes cloudflare/api-token in its flarectl wrapper/docs, and a code/doc patch to cloudflare.com/api-token was blocked by an active cloud-maintainer lock. — evidence: /git/github.com/LiGoldragon/cloud/flake.nix:46; basis: observed; reopen: if /git/github.com/LiGoldragon/cloud/flake.nix, README.md, or docs/first-cloudflare-slice.md changes around the Cloudflare gopass path or the cloud-maintainer lock clears and the patch lands
- horizon-rs main integrated cloud-designer-cloud-node-species through operator merge commit bd1cc2c1 after cargo test --locked and cargo clippy --locked --all-targets -- -D warnings passed. — evidence: cmd:cd /git/github.com/LiGoldragon/horizon-rs && jj log --no-graph -r main#bd1cc2c1; basis: output; reopen: if horizon-rs main moves before the cloud-node species changes or tests are later found invalid
- horizon-rs main integrated cloud-designer-web-host and main/main@origin point at 4a0e29fe, adding NodeService::WebHost after cargo test --locked and cargo clippy --locked --all-targets -- -D warnings passed. — evidence: cmd:cd /git/github.com/LiGoldragon/horizon-rs && jj log --no-graph -r main#4a0e29fe; basis: output; reopen: if horizon-rs main moves before 4a0e29fe or the WebHost type is reverted
- Operator-side cloud-node/WebHost landing report exists and records the horizon-rs landings plus the goldragon lock blocker. — evidence: /home/li/primary/reports/pi-operator/12-cloud-node-operator-side.md#horizon-rs; basis: observed; reopen: if the report is edited or goldragon is later integrated
- A CriomOS WebHost testing worktree branch exists at /home/li/wt/github.com/LiGoldragon/CriomOS/pi-operator-web-host-testing, bookmark pi-operator-web-host-testing, pushed at 297d7e0f. — evidence: cmd:cd /home/li/wt/github.com/LiGoldragon/CriomOS/pi-operator-web-host-testing && jj log --no-graph -r pi-operator-web-host-testing#297d7e0f; basis: output; reopen: if the bookmark moves, the worktree is removed, or the branch is merged/replaced
- The CriomOS WebHost branch adds an inert-by-default web-host.nix module plus checks.x86_64-linux.web-host-policy that builds a Zola fixture and verifies nginx, ACME, firewall, and static artifact behavior. — evidence: /home/li/primary/reports/pi-operator/13-web-host-testing-worktree.md#Implemented-slice; basis: observed; reopen: if the pi-operator-web-host-testing branch changes around modules/nixos/web-host.nix, flake.nix, or checks/web-host-policy
- The CriomOS WebHost policy check passed with synthetic system and horizon inputs. — evidence: cmd:nix build --refresh --override-input system path:/tmp/criomos-system-x86_64 --override-input horizon path:/tmp/criomos-horizon-minimal .#checks.x86_64-linux.web-host-policy --no-link#exit-0; basis: test; reopen: if the CriomOS WebHost branch, synthetic fixture inputs, Zola, or relevant NixOS module behavior changes
- Spirit accepted record 75pw for making practical Nix build, test, and deploy invocations default to a safe typed Lojix interface that verifies eligible remote workers and builder capabilities before emitting Nix calls. — evidence: cmd:spirit /tmp/lojix-safe-nix-default.nota#RecordAccepted-75pw; basis: output; reopen: if Spirit record 75pw is superseded or the human retracts the Lojix-safe-Nix direction
- A separate CriomOS-test-cluster worktree was created at /home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates from main for VM feature gate work. — evidence: cmd:jj workspace add --name pi-operator-vm-feature-gates --revision main /home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates#workspace-created; basis: output; reopen: if the worktree is removed, rebased, or replaced
- The VM feature gate worktree currently edits lib/mkVmTest.nix and lib/mkDeployTest.nix so generated runNixOSTest derivations append requiredSystemFeatures nixos-test and criomos-vm-testing via overrideAttrs. — evidence: /home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates/lib/mkVmTest.nix#requiredSystemFeatures; basis: observed; reopen: if the VM feature gate branch changes around lib/mkVmTest.nix or lib/mkDeployTest.nix
- The main CriomOS-test-cluster checkout has unrelated uncommitted cloud-operator changes, so current VM feature gate work is isolated in a separate worktree rather than touching /git/github.com/LiGoldragon/CriomOS-test-cluster. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-test-cluster && jj status#working-copy-changes; basis: output; reopen: if the main CriomOS-test-cluster checkout becomes clean or the lock owner releases/merges those changes
- Pi openai-codex/gpt-5.5 fetch failed diagnosis report exists at reports/pi-operator/10-openai-codex-gpt-5-5-fetch-failed.md. — evidence: /home/li/primary/reports/pi-operator/10-openai-codex-gpt-5-5-fetch-failed.md#diagnosis; basis: observed; reopen: if Pi provider code, Pi version, or the report is superseded by new failure diagnostics
- The screenshot-matching Pi failure session recorded provider=openai-codex, model=gpt-5.5, api=openai-codex-responses, stop=error, errorMessage=fetch failed, and zero usage. — evidence: ~/.pi/agent/sessions/--home-li-primary--/2026-06-06T08-49-16-602Z_019e9c1f-493a-7333-b245-cefca8ee9494.jsonl:9319; basis: observed; reopen: if a later Pi failure has different persisted provider/model/api/error/usage facts
- Cloud main includes hardened DigitalOcean Phase-1 non-live integration at commit 3b38cdd8 and prior non-live validation passed. — evidence: previous-summary#Progress-Done-cloud-commit-3b38cdd8; basis: observed; reopen: if /git/github.com/LiGoldragon/cloud main moves before 3b38cdd8 or DigitalOcean integration is reverted
- Primary testing guidance documents QEMU-backed NixOS VM checks only on explicitly authorized VM-testing hosts/builders and was committed at primary commit 49a6f0b5. — evidence: /home/li/primary/skills/testing.md#QEMU-backed-NixOS-VM-checks; basis: doc; reopen: if /home/li/primary/skills/testing.md changes around VM testing host policy

## Learned
- CriomOS-home owns Pi user Home Manager profile changes, while hexis is the right mechanism for managed mutable Pi settings JSON such as ~/.pi/agent/settings.json. — source: session experience: Pi transport default work in /git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/pi-models.nix
- Cloudflare /user/tokens/verify can disagree with practical read-path tests; for cloud's current implementation, direct zone/DNS reads and flarectl zone list are the relevant non-destructive credential checks. — source: session experience: corrected cloudflare.com/api-token produced token-verify 401 but zone/DNS reads and flarectl succeeded
- For pushed immutable jj designer branches that are not fast-forwards, create an operator merge commit rather than attempting to rebase immutable commits. — source: session experience: horizon-rs cloud-designer-cloud-node-species was immutable and merged at bd1cc2c1
- CriomOS default flake stubs intentionally throw without materialized system and horizon inputs; pure branch checks may need explicit synthetic --override-input system and horizon values. — source: session experience: WebHost policy check required /tmp/criomos-system-x86_64 and /tmp/criomos-horizon-minimal
- Whole-tree nix fmt --check in CriomOS currently fails on pre-existing unformatted files, so use touched-file formatting checks unless the task is a global format cleanup. — source: cmd:nix fmt -- --check .#pre-existing-unformatted-files
- A Zola fixture needs a current-format config and at least a minimal template to witness rendered markdown content; otherwise Zola may render its default no-template page. — source: session experience: WebHost policy fixture failed on old highlight_code and then on missing template before passing
- VM/QEMU safety should be enforced mechanically on derivations with requiredSystemFeatures and backed by a non-VM structural guard check; documentation alone is insufficient. — source: current task and prior QEMU prevention decision

## Open
- Does the CriomOS-test-cluster VM feature gate branch fully prove every VM-backed check requires nixos-test and criomos-vm-testing without running QEMU? — verifies: Add and wire a pure check that inspects the mkVmTest and mkDeployTest derivations' requiredSystemFeatures, then run only non-VM/eval checks on /home/li/wt/github.com/LiGoldragon/CriomOS-test-cluster/pi-operator-vm-feature-gates.
- Is Prometheus the authorized VM-testing builder and should it advertise nixos-test plus criomos-vm-testing? — verifies: Get system-designer/system-operator or human confirmation, then patch the appropriate goldragon/CriomOS builder configuration once the active goldragon/CriomOS locks permit it.
- How should Lojix's safe Nix subset be shaped as the default for practical build/test/deploy calls? — verifies: Explain a typed operation model with required capabilities, containment level, builder-policy planning, eligibility checks, scheduling, and final Nix command emission; later capture or implement in Lojix contracts if accepted.
- Can the Immich-backed agentic media mirror be carried in tandem with WebHost as an eventual public static-hosting use case? — verifies: Retry the Spirit record with lowercase kebab-case referents after the failed attempt in /tmp/immich-webhost-intent.nota, then add an implementation/report note or branch witness that ties selected static public presentation to WebHost without exposing private originals.
- When can cloud's Cloudflare credential path be patched from cloudflare/api-token to cloudflare.com/api-token? — verifies: Wait for the cloud-maintainer lock to clear or receive explicit override authorization, then edit /git/github.com/LiGoldragon/cloud/flake.nix, README.md, docs/first-cloudflare-slice.md, and INTENT.md as needed and run non-live checks.
- When can goldragon cloud-designer-cloud-node-data be merged so doris is declared on main? — verifies: Wait for the system-designer goldragon lock to clear or receive explicit override authorization, then integrate the pushed branch and validate projection fixtures.
- What are the final production WebHost choices for renderer, source model, config surface, and provisioning doris now? — verifies: Get explicit human decisions for Zola vs Hugo, build-time render vs runtime self-update, datom.nota cluster-authored vs user-facing config surface, and whether to provision doris for the web host role.
- Does CriomOS-test-cluster source-constraints still have the non-VM blocker involving newer CriomOS input and modules/nixos/llm.nix: goldragon? — verifies: Rerun the structural source-constraints check after current branch edits and inspect failures without running VM/QEMU checks.
- Can cloud Tier-2 full-daemon DigitalOcean live create-observe-destroy be run and primary-hpkj closed? — verifies: Only after explicit human live authorization, run the documented live chain with cleanup/sweep and update bead primary-hpkj.
- When can lojix pin/update and LojixOS split/cutover work resume? — verifies: Confirm coordination locks permit lojix/meta-signal-lojix/signal-lojix work, then continue from the pending pin/update and production cutover plan.

## Next
- Finish the CriomOS-test-cluster VM feature gate branch by adding a pure requiredSystemFeatures guard check that covers both lib/mkVmTest.nix and lib/mkDeployTest.nix. → A non-QEMU check proves VM-backed tests require nixos-test and criomos-vm-testing.
- Wire the new guard check into flake.nix, run touched-file formatting, inspect the diff, commit, set the pi-operator-vm-feature-gates bookmark, and push. → The branch is a clean pushed checkpoint that does not touch the locked main checkout.
- Run only non-VM/eval validation for the VM feature gate branch, avoiding QEMU and live deploy commands. → Validation evidence distinguishes structural safety checks from VM execution.
- Explain to the human how the safe Lojix Nix subset should work: typed operation, required capabilities, containment level, builder inventory, scheduler, and controlled Nix invocation. → The human receives the requested implementation explanation and path to making it default.
- Retry the Immich/WebHost Spirit capture with lowercase referents and update the WebHost report or branch notes as appropriate. → The Immich agentic media mirror is durably tied to WebHost as an eventual public-hosting use case without exposing private media.
- When locks permit, patch cloud's Cloudflare gopass path to cloudflare.com/api-token and merge the remaining goldragon doris-data branch. → Credential wiring and doris declaration catch up with the already captured intent and horizon-rs WebHost type.
