## Task
Implement the Lojix modern-component/production-readiness work from the Spirit audit and cloud-designer cutover research, without logging new intent from that research and without deploying live systems unless explicitly authorized.

## Done When
Lojix, signal-lojix, and meta-signal-lojix are Spirit-modern enough for the agreed cutover path: contract hygiene/tests, CLI NOTA edge, binary daemon startup, packaging/systemd/request-builder, build-correctness parity including secrets and firmware, durable state, and relevant tests are green, committed, and pushed.

## Forbid
- Do not run remediation/deploy/session commands without explicit user go-ahead. — source: previous-brief Constraints & Preferences
- Live Home/Niri/desktop-affecting activation requires explicit user authorization. — source: previous-brief Constraints & Preferences
- Do not run HomeOnly Activate locally on a target as the target user; if the authorized deploy path fails, stop/ask rather than bypassing through local target-user activation. — source: previous-brief Constraints & Preferences
- Use jj; do not use git CLI directly except the documented escape hatches. — source: previous-brief Constraints & Preferences
- Work directly on main for primary; commit/push completed work. — source: previous-brief Constraints & Preferences
- Use lojix-cli directly for current production Stack A; do not trust lojix-run exact-ref rewrite behavior until fixed. — source: previous-brief Constraints & Preferences
- Cloud work must use cloud-operator; desktop/app diagnosis uses system-operator. — source: previous-brief Constraints & Preferences
- For VSCodium on zeus, start read-only; do not activate Home or change profiles unless user confirms fix path. — source: previous-brief Constraints & Preferences
- Direct ssh bird@zeus.goldragon.criome is not authorized/available; root SSH works and can inspect bird via runuser. — source: previous-brief Constraints & Preferences
- Do not log intent from cloud-designer's Lojix production-cutover research. — source: user@turn-prefix

## Established
- CriOMOS-home now disables the managed VSCodium Rust Analyzer default by removing the Rust Analyzer extension and server-path setting, with no Home activation run. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS-home && jj bookmark list --all#main-6624b145-codium-disable-rust-analyzer-by-default; basis: output; reopen: if /git/github.com/LiGoldragon/CriomOS-home/modules/home/vscodium/vscodium/default.nix changes around the VSCodium extensions or Rust Analyzer settings
- The live Ouranos GTK settings for new GTK app launches were corrected to dark: dconf gtk-theme adw-gtk3-dark, icon-theme Papirus-Dark, and GTK 3/4 settings.ini prefer dark. — evidence: cmd:dconf write /org/gnome/desktop/interface/gtk-theme 'adw-gtk3-dark' && sed -n '1,40p' ~/.config/gtk-3.0/settings.ini#AFTER-GTK3_SETTINGS; basis: output; reopen: if Chroma reapplies a light GTK theme, settings.ini changes, or Transmission Remote GTK still launches light after restart
- Chroma source pinned by CriOMOS-home already contains dark GTK3 behavior: dark mode writes adw-gtk3-dark and Papirus-Dark and writes GTK settings.ini. — evidence: cmd:jj file show -r 66ed03bf44efdb97e4576f2e3191084e0059fbfe src/theme.rs | rg 'gtk-theme|adw-gtk3|settings.ini'#lines-486-529; basis: observed; reopen: if CriOMOS-home repins Chroma to a revision without those writes or Chroma theme code changes
- Primary report reports/operator/359-lojix-modern-component-audit-2026-06-10.md was written and pushed in primary commit a2139ff6a42c. — evidence: cmd:cd /home/li/primary && jj bookmark list --all | rg '^main|@origin:.*a2139ff6'#origin-a2139ff6; basis: output; reopen: if the report is edited, superseded, or removed
- The Lojix audit found the right triad outline but ten major gaps: NOTA daemon startup, missing CLI NOTA edge, deprecated/default text dependencies, path deps, in-memory store, token-only subscriptions, handwritten daemon shell, missing contract witnesses, no root flake/package surface, and doc/version drift. — evidence: reports/operator/359-lojix-modern-component-audit-2026-06-10.md:1; basis: doc; reopen: if the report is superseded or the corresponding gaps are implemented and tested
- The operator lock is currently held for /git/github.com/LiGoldragon/lojix, /git/github.com/LiGoldragon/signal-lojix, and /git/github.com/LiGoldragon/meta-signal-lojix for Lojix modern component implementation. — evidence: cmd:tools/orchestrate claim operator /git/github.com/LiGoldragon/signal-lojix /git/github.com/LiGoldragon/meta-signal-lojix /git/github.com/LiGoldragon/lojix -- lojix modern component implementation from audit#operator.lock; basis: output; reopen: if tools/orchestrate status shows the operator lock no longer includes these paths
- signal-lojix was modernized and pushed: commit 97a41186929c removed unconditional text-codec/default text deps, refreshed generated schema against current emitter, and added dependency/frame/NOTA tests. — evidence: cmd:cd /git/github.com/LiGoldragon/signal-lojix && jj bookmark list --all | rg '^main|@origin:.*97a41186'#origin-97a41186; basis: output; reopen: if signal-lojix main advances or its triad-port Cargo/test/schema files change
- signal-lojix now exposes a root Cargo workspace pointing at triad-port, pushed as commit 8f6c498a0f90 so git dependencies can resolve the package. — evidence: cmd:cd /git/github.com/LiGoldragon/signal-lojix && jj bookmark list --all | rg '^main|@origin:.*8f6c498a'#origin-8f6c498a; basis: output; reopen: if /git/github.com/LiGoldragon/signal-lojix/Cargo.toml changes or signal-lojix main advances
- signal-lojix tests passed with and without nota-text after modernization. — evidence: cmd:cd /git/github.com/LiGoldragon/signal-lojix/triad-port && cargo test --no-default-features && cargo test --no-default-features --features nota-text#test-result-ok; basis: test; reopen: if signal-lojix source, schema, Cargo files, or tests change
- meta-signal-lojix was modernized and pushed: commit 2e69371b9cb0 added root workspace exposure, switched signal-lojix to a git dependency, removed unconditional text-codec/default text deps, refreshed generated schema, and added dependency/frame/NOTA tests. — evidence: cmd:cd /git/github.com/LiGoldragon/meta-signal-lojix && jj bookmark list --all | rg '^main|@origin:.*2e69371b'#origin-2e69371b; basis: output; reopen: if meta-signal-lojix main advances or its Cargo/test/schema files change
- meta-signal-lojix tests passed with and without nota-text after modernization. — evidence: cmd:cd /git/github.com/LiGoldragon/meta-signal-lojix/triad-port && cargo test --no-default-features && cargo test --no-default-features --features nota-text#test-result-ok; basis: test; reopen: if meta-signal-lojix source, schema, Cargo files, or tests change
- Lojix triad-port Cargo.toml has been edited in the working copy to consume signal-lojix and meta-signal-lojix from git main instead of sibling path dependencies. — evidence: edit:/git/github.com/LiGoldragon/lojix/triad-port/Cargo.toml#meta-signal-lojix-and-signal-lojix-git-deps; basis: observed; reopen: if /git/github.com/LiGoldragon/lojix/triad-port/Cargo.toml changes again before commit
- Lojix cargo update moved the daemon crate to current-generation dependencies including signal-frame 0.2.1, schema-rust-next 0.5.3, triad-runtime 0.6.1, signal-lojix 8f6c498a, and meta-signal-lojix 2e69371b. — evidence: cmd:cd /git/github.com/LiGoldragon/lojix/triad-port && cargo update -p signal-frame -p signal-frame-macros -p schema-rust-next -p nota-next -p triad-runtime#Locking-9-packages; basis: output; reopen: if /git/github.com/LiGoldragon/lojix/triad-port/Cargo.lock changes again
- Lojix compile currently fails after dependency upgrades because triad-runtime renamed Actor* listener APIs to Async* and the newer schema emitter makes tuple payload fields private with new/payload/into_payload methods. — evidence: cmd:cd /git/github.com/LiGoldragon/lojix/triad-port && LOJIX_UPDATE_SCHEMA_ARTIFACTS=1 cargo test --no-default-features#error-unresolved-ActorListenerSocket-and-private-fields; basis: output; reopen: if cargo test --no-default-features passes or fails with different errors after further edits
- Lojix src/daemon.rs has been partially ported in the working copy from Actor* listener names to Async* names and OriginRoute::new. — evidence: edit:/git/github.com/LiGoldragon/lojix/triad-port/src/daemon.rs#AsyncListenerSocket-AsyncMultiConnectionRuntime-OriginRoute-new; basis: observed; reopen: if /git/github.com/LiGoldragon/lojix/triad-port/src/daemon.rs changes again before commit
- Lojix src/lib.rs has been partially ported in the working copy to initialize generated table wrappers with ::new and to add StoreState helper methods for table mutation. — evidence: edit:/git/github.com/LiGoldragon/lojix/triad-port/src/lib.rs#StoreState-wrapper-helpers; basis: observed; reopen: if /git/github.com/LiGoldragon/lojix/triad-port/src/lib.rs changes again before commit
- Cloud-designer report 40 says the new Lojix daemon build/eval spine works end-to-end in real-nix tests, but real-node deploy is blocked by activation/copy effect problems, hygiene gaps, build-correctness parity, missing operational surface, and durable state. — evidence: reports/cloud-designer/40-lojix-cutover-plan/6-cutover-plan.md:1; basis: doc; reopen: if report 40 is superseded or Lojix production-readiness tests are rerun with different results
- Cloud-designer report 40 says CriOMOS is already orchestrator-agnostic, only CriOMOS-home pins lojix-cli for the binary, and zeus is a secret-free first non-activation target for parity testing. — evidence: reports/cloud-designer/40-lojix-cutover-plan/6-cutover-plan.md:1; basis: doc; reopen: if CriOMOS/CriOMOS-home flake inputs or the cloud cutover report are updated
- The human clarified that production uses secrets, so secret-free build-only runs are not production parity. — evidence: user@turn-prefix; basis: user; reopen: if the human explicitly redefines the production cutover standard
- Prior local FullOS activation on Ouranos completed after explicit authorization, produced system generation /nix/store/h53wb1827n6ab1nf7q2vh530kpyf9sd4-nixos-system-ouranos-26.05.20260422.0726a0e, and left systemctl is-system-running at running with no failed system/user units. — evidence: previous-brief:Activated latest local FullOs after explicit user authorization; basis: output; reopen: if Ouranos is rebuilt/activated again or system health changes
- bird Home redeploy to zeus is blocked by unsigned dispatcher-built outputs under zeus require-sigs=true and by unavailable direct bird@zeus SSH activation transport. — evidence: reports/system-operator/207-bird-zeus-home-and-vscodium-root-cause-2026-06-10.md:1; basis: doc; reopen: if lojix copy/build signing or zeus bird SSH activation transport is fixed and tested
- Ouranos desktop jitter/freezes correlated strongest with memory/swap pressure: zram full, disk swap in use, rust-analyzer around 9.7-9.9 GiB swapped, and heavy agent/editor scopes peaking multiple GiB. — evidence: previous-brief:Diagnosed Ouranos desktop jitter/freezes; basis: output; reopen: if fresh performance data points to a different dominant cause
- CriOMOS-home medium video production tools were added in commit 1fb10022010c and CriOMOS repinned them in commit 3de771185fd9; whisperx was not added. — evidence: previous-brief:Installed ordinary video production tools and repinned CriOMOS; basis: output; reopen: if CriOMOS-home or CriOMOS flake pins/profile package selections change
- Ethernet connectivity on Ouranos was restored without Home activation; latest observed enp0s31f6 address was 10.18.0.100/24 with default via 10.18.0.1 metric 100 and Wi-Fi fallback metric 600. — evidence: previous-brief:Ethernet latest observed; basis: output; reopen: if network routes/addresses change or connectivity regresses

## Learned
- For Lojix modernization, contract hygiene must happen before daemon work because the daemon depends on generated signal-lojix/meta-signal-lojix surfaces and their current signal-frame/schema-rust-next versions. — source: session experience: signal-lojix and meta-signal-lojix modernization preceded Lojix cargo update
- A crate living only under triad-port cannot be consumed as a normal git dependency until the repo exposes a root Cargo workspace including triad-port. — source: session experience: signal-lojix root Cargo.toml was needed before meta-signal-lojix could git-depend on it
- The current schema-rust-next emitter wraps aliases/tuple payloads more strictly; handwritten code must use generated ::new, payload, into_payload, and From conversions rather than tuple constructors or .0 fields. — source: cmd:LOJIX_UPDATE_SCHEMA_ARTIFACTS=1 cargo test --no-default-features#error-private-wrapper-porting
- triad-runtime 0.6.1 uses AsyncListenerSocket, AsyncMultiConnectionRuntime, AsyncMultiListenerDaemon, and AsyncMultiListenerDaemonError instead of the older Actor* listener names. — source: cmd:rg -n 'AsyncListenerSocket::new|AsyncMultiConnectionRuntime' /git/github.com/LiGoldragon/triad-runtime/src /git/github.com/LiGoldragon/spirit/src/schema/daemon.rs
- Do not add a generic build timeout for Lojix parity; lojix-cli permits cold builds that can take hours, and the cutover research says only activate ssh needs cancel-kill behavior. — source: reports/cloud-designer/40-lojix-cutover-plan/6-cutover-plan.md:1
- For production cutover, treat secrets parity as mandatory; secret-free zeus build/eval can be a non-authoritative Stack-B parity smoke but not a production substitute. — source: user@turn-prefix plus reports/cloud-designer/40-lojix-cutover-plan/2-secrets-gate-and-secret-free-nodes.md
- The OsOnly firmware divergence is a one-line build-correctness fix in lojix schema_runtime.rs around the firmware input generation; cloud research reported daemon_OsOnly true versus lojixCli_OsOnly false. — source: user@turn-prefix
- Spirit Observe is routine guidance; on current Lojix work, observe/refresh is appropriate but do not record the cloud-designer research as new intent. — source: user@turn-prefix
- Transmission Remote GTK is GTK3 and can require actual gtk-theme adw-gtk3-dark rather than only portal color-scheme prefer-dark; Chroma failures can leave dconf partially applied. — source: cmd:dconf read /org/gnome/desktop/interface/gtk-theme#before-adw-gtk3-after-adw-gtk3-dark
- Clean temporary VSCodium state plus disabled extensions can isolate profile/extension issues; absence of Claude/Codex in that clean state is expected, not itself a failure. — source: previous-brief:VSCodium clean-window result
- Operational corrections/status are not automatically Spirit intent; remove overcaptures and use conservative Spirit write-gate discipline. — source: previous-brief:Spirit discipline

## Open
- Does Lojix triad-port compile and test after the Async runtime and generated-wrapper port is completed? — verifies: Finish replacing old Actor* names, tuple constructors, and .0 accesses; run cd /git/github.com/LiGoldragon/lojix/triad-port && LOJIX_UPDATE_SCHEMA_ARTIFACTS=1 cargo test --no-default-features, then cargo test --no-default-features --features nota-text if the feature remains.
- Which direct generated tuple-field accesses remain in Lojix after the current partial StoreState helper port? — verifies: Run rg -n '\\.0|HomeMaterialization\(|MaterializedInputs\(|RejectedWatch\(|OriginRoute\(' /git/github.com/LiGoldragon/lojix/triad-port/src /git/github.com/LiGoldragon/lojix/triad-port/tests and replace remaining generated-field access with constructors/accessors.
- How should Lojix implement binary-only daemon startup while preserving a text edge for launch tooling? — verifies: Design and implement a rkyv daemon configuration type, likely contract-owned, plus a writer/request-builder helper; prove lojix-daemon accepts exactly one binary file and default dependency tree excludes nota-next/nota-codec.
- How should Lojix CLI parse and render NOTA while disambiguating ordinary versus meta requests? — verifies: Implement feature-gated NOTA parse/render in /git/github.com/LiGoldragon/lojix/triad-port/src/client.rs, add tests for inline NOTA, NOTA file, signal file, ordinary/meta classification, and reply NOTA output.
- Where should substituter and secrets resolution live for Stack-B parity: daemon Horizon read versus request-builder pre-resolution? — verifies: Use Spirit Observe/intent refresh and code/report evidence; decide implementation owner before changing the wire/request-builder shape.
- Can a non-authoritative zeus Stack-B build/eval parity run proceed before durable state, or must durable sema-engine/redb land first for any consumer migration? — verifies: Clarify with the human after explaining in-memory-first versus durable-first; production authority requires secrets parity and likely durable state.
- What exact Nix package, systemd module, and request-builder shape should Lojix ship for first cutover packaging? — verifies: Read remaining cloud-designer report 40 files, inspect CriOMOS-home current lojix-cli package use, then implement and test a package/module without activating deployments.
- Does the Lojix OsOnly firmware divergence still exist after dependency/schema updates, and is the one-line schema_runtime.rs fix at about line 1627 still correct? — verifies: Compare Stack A lojix-cli and Stack B materialized eval outputs for OsOnly firmware fields; patch schema_runtime.rs and add a regression test.
- Can Lojix deploy a real node safely after copy/activate effect bodies are fixed? — verifies: Implement signed copy, switch-to-configuration path, real ssh host resolution, and cancel-kill on activate ssh; test only with explicit user authorization and start with non-activation eval/build parity.
- Does Lojix durable state need to be implemented before any production consumer migration? — verifies: Ask/explain the in-memory-first versus durable-first tradeoff; if durable-first, implement sema-engine/redb for live set, GC roots, event log, and container lifecycle with restart-persistence tests.
- How should the generated daemon shell replace Lojix's handwritten daemon loop? — verifies: After Lojix compiles on current contracts, add schema-emitted daemon module analogous to Spirit's ComponentDaemon hooks and prove existing socket/request tests still pass.
- Do real pushed subscription events need to land before first build/eval cutover or only before production observability? — verifies: Clarify cutover gate ordering; implement schema/signal-frame event roots and daemon publishing when required.
- Fix lojix-cli deploy path so Home deploys do not copy unsigned dispatcher-built outputs to signature-enforcing targets. — verifies: Change build/copy path so target copies signed/substitutable outputs or builds remotely; test against a require-sigs target or equivalent integration.
- Fix Home activation transport so operator-initiated activation does not require unavailable direct bird@zeus SSH, or explicitly provision intended bird@zeus credentials. — verifies: Run an authorized Home deploy dry run or activation-path test proving activation transport works without local target-user HomeOnly Activate.
- Verify post-FullOS Home state for stale bad generated session.slice/background.slice files if needed. — verifies: Read generated Home unit files after current activation and confirm no stale broad parent-slice ownership remains.
- Fix lojix-run ExactReferenceRewriter behavior so operator-facing main refs are not rewritten or malformed. — verifies: Add/adjust lojix-run tests for main refs and exact refs, then run the package test suite.
- Investigate exact HomeOnly logout cause only with user authorization. — verifies: Obtain explicit authorization, reproduce or inspect logs safely, and identify the unit/action that terminates the compositor session.
- Validate staged criomos-ui-priority.service live only in a safe window. — verifies: With user-approved safe window, activate/observe the service and confirm runtime properties protect UI components without owning parent slices.
- Implement safe agent/Nix workload resource limiting, workload scopes, pressure visibility, and kill controls. — verifies: Land configured scopes/limits and tests or live observations showing Niri/UI components remain responsive under workload pressure.
- Retire or durably fix broken legacy persona-spirit-daemon-v0.1.1.service. — verifies: Identify owning repo/unit, remove or replace legacy service, and confirm no failed unit remains.
- Deploy cameractrls to Zeus/bird when a safe authorized path exists. — verifies: Use an authorized deploy path and confirm cameractrls is present in bird's Zeus profile.
- Review suspect Spirit record fteq, and possibly kipw, 8iqe, t2ja, only if the psyche wants cleanup. — verifies: Ask/receive explicit cleanup request, then inspect recent Spirit records and retire/fix only confirmed overcaptures.

## Next
- Resume in /git/github.com/LiGoldragon/lojix/triad-port and finish the compile port by replacing remaining generated tuple-field access with generated constructors/accessors, using StoreState helper methods where needed. → LOJIX_UPDATE_SCHEMA_ARTIFACTS=1 cargo test --no-default-features gets past the current Async/runtime-wrapper errors or reveals the next concrete compile failure.
- Run a Spirit Observe/refresh for Lojix/cutover context without recording the cloud-designer research as intent. → Current intent guidance is refreshed for implementation ordering while honoring the user's no-intent-log instruction.
- Read the remaining cloud-designer report 40 files, especially build/test ground truth, secrets gate, firmware divergence, consumer migration, and constraints/watchfors, and map each gate to code tasks. → The Lojix implementation order reflects grounded cutover constraints instead of only the earlier audit report.
- After Lojix compiles, add daemon-level dependency-boundary tests and keep default daemon builds free of nota-next/nota-codec unless a text feature explicitly opts in. → The daemon matches Spirit's binary-default dependency discipline.
- Implement the CLI NOTA text edge in Lojix and tests for inline NOTA, NOTA files, rkyv files, ordinary/meta classification, and NOTA reply rendering. → Operators can hand-drive Deploy/Query requests without a separate encoder, while daemon communication remains rkyv.
- Implement binary-only lojix-daemon startup plus a writer/request-builder for configuration or deploy request frames. → lojix-daemon rejects NOTA startup and accepts exactly one binary startup object, matching workspace daemon rules.
- Patch and test the OsOnly firmware parity divergence reported by cloud-designer research. → Stack-B OsOnly materialization matches Stack-A firmware input behavior.
- Add packaging/systemd/request-builder surfaces for non-activating Stack-B build/eval parity, starting with zeus only if explicitly authorized for the run. → A real node parity path exists without activation and without treating secret-free smoke as production.
- Plan and then implement durable sema-engine/redb state for Lojix live set, GC roots, event log, and container lifecycle if production authority remains the target before consumer migration. → Daemon restart preserves deploy authority state and passes persistence tests.
- Commit and push each completed green slice with jj, keeping the operator lock until the Lojix implementation work is either complete or explicitly handed off. → No large uncommitted multi-repo partial state is left without a clear test boundary.
