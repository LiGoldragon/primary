## Task
As cloud-operator, implement the LojixOS rename/config-crate split and new lojix-daemon production transition, including the pan-cluster public-domain configuration path, using the latest cloud-designer reports as the cutover design source.

## Done When
Done when the LojixOS/CriomOS configuration split and Horizon/domain changes are implemented and tested, lojix consumes the current Horizon/config path through the daemon, relevant repos are clean and pushed, and production daemon transition tests have either run under the user's authorization or are clearly blocked with the exact blocker.

## Forbid
- Do not run remediation/deploy/session commands without explicit user go-ahead. — source: previous-compaction-summary#Constraints
- Live Home/Niri/desktop-affecting activation requires explicit user authorization. — source: previous-compaction-summary#Constraints
- Do not run HomeOnly Activate locally on a target as the target user; if authorized deploy path fails, stop/ask. — source: previous-compaction-summary#Constraints
- Use jj; do not use git CLI directly except documented escape hatches. — source: previous-compaction-summary#Constraints
- Work directly on main for primary; commit/push completed work. — source: previous-compaction-summary#Constraints
- Use lojix-cli directly for current production Stack A; do not trust lojix-run exact-ref rewrite behavior until fixed. — source: previous-compaction-summary#Constraints
- Cloud work must use cloud-operator; desktop/app diagnosis uses system-operator. — source: previous-compaction-summary#Constraints
- Do not log intent from cloud-designer’s Lojix production-cutover research. — source: previous-compaction-summary#Constraints
- Treat secrets parity as mandatory for production cutover; secret-free zeus build/eval is non-authoritative only. — source: previous-compaction-summary#Constraints
- Do not add a generic build timeout for Lojix parity; only activate-SSH needs cancel-kill behavior per research. — source: previous-compaction-summary#Constraints
- Ignored real-nix daemon/deploy smoke tests should not be run unless explicitly authorized. — source: previous-compaction-summary#Constraints
- Do not use reports/operator for this lane; cloud-operator reports go under reports/cloud-operator. — source: user@history:your-the-cloude-operator-not-operator
- Do not use Syncthing in the Android phone media mirror path; it is too heavy for the phone. — source: user@history:syncthing-too-heavy

## Established
- Primary report reports/operator/359-lojix-modern-component-audit-2026-06-10.md was written and pushed in primary commit a2139ff6a42c. — evidence: previous-compaction-summary#Progress-Done; basis: output; reopen: if primary history is rewritten to remove commit a2139ff6a42c or the report path changes
- signal-lojix was modernized and pushed through commits 97a41186929c and 8f6c498a0f90; root Cargo workspace tests passed with and without nota-text. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if signal-lojix main is rewritten before those commits or the contract layout is replaced
- meta-signal-lojix was modernized and pushed at 2e69371b9cb0; root workspace was added, signal-lojix was switched to a git dependency, generated schema refreshed, and tests passed with and without nota-text. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if meta-signal-lojix main is rewritten before 2e69371b9cb0 or generated schema surfaces are replaced
- Lojix binary-only daemon startup slice was committed and pushed as 6a58f7c4cc64, making lojix-daemon require triad_runtime signal-file startup and reject inline NOTA and .nota startup files. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if lojix daemon startup code changes or daemon startup accepts inline NOTA or .nota paths again
- After binary-startup work, direct daemon rejection checks returned exit 2 for inline NOTA and .nota file arguments with stderr `(DaemonRejected [component argument error: expected a signal-encoded file path])`. — evidence: previous-compaction-summary#Progress-Done; basis: test; reopen: if daemon argument parsing or rejection text changes
- Lojix CLI NOTA edge was implemented, tested, committed, and pushed as e47f3642058f on main. — evidence: previous-compaction-summary#Early-Progress; basis: test; reopen: if lojix client parsing/rendering code changes or main is rewritten before e47f3642058f
- Lojix owner socket peer credential checks were implemented with OwnerPeerAuthority, tested under both feature gates, documented, committed, and pushed as 55577f0f6e3d. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/lojix#main-55577f0f; basis: test; reopen: if lojix owner-socket authorization code or main history changes
- Lojix DaemonConfiguration::write_rkyv_file was added, build_smoke was updated to use it, daemon_configuration round-trip test passed, and the slice was committed and pushed as f530de28d2ce. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/lojix#main-f530de28; basis: test; reopen: if DaemonConfiguration rkyv read/write code changes or main history changes
- Cloud-designer flattened lojix, signal-lojix, and meta-signal-lojix after f530de28; Lojix paths shifted from triad-port/src to src and main reached 13192bce at that point. — evidence: cmd:find . -maxdepth 2 -type f in /git/github.com/LiGoldragon/lojix#shows-./src/client.rs-./src/daemon.rs-./src/lib.rs; basis: output; reopen: if the Lojix repo layout changes again
- Spirit record oh9l decides Lojix cutover durable state is durable-first on sema-engine before real cutover, including live-generation-set, GC-roots, event-log state, and self-resume. — evidence: cmd:spirit `(Lookup oh9l)`#RecordFound-oh9l; basis: output; reopen: if Spirit record oh9l is clarified, superseded, or retired
- Spirit record lc28 decides substituter resolution moves into the Lojix daemon provisionally, with the wire reverting to bare node names and implementation marked must-be-replaced-by-better-design. — evidence: cmd:spirit `(Lookup lc28)`#RecordFound-lc28; basis: output; reopen: if Spirit record lc28 is clarified, superseded, or retired
- Cloud-designer report 55 is the actionable handoff for cloud-operator to merge horizon-test-vm branches and implement remaining VM-testing work. — evidence: /home/li/primary/reports/cloud-designer/55-operator-implementation-handoff.md:1; basis: doc; reopen: if report 55 is edited or superseded
- Cloud-operator review report was moved to reports/cloud-operator/387-cloud-designer-vm-testing-review.md and primary main was pushed at c0b946a2. — evidence: cmd:jj bookmark list --all in /home/li/primary#main-c0b946a2; basis: output; reopen: if primary main advances with a move or rename of report 387
- The user authorized implementing the report-55 handoff, asked to test it well, and allowed subagents. — evidence: user@turn-prefix:go-ahead-test-it-well-you-can-use-subagents; basis: user; reopen: if the user narrows or revokes that authorization
- Three reviewer subagents were dispatched non-blocking for horizon-rs, CriomOS, and CriomOS-test-cluster handoff review under run a8a47971-c0dd-407d-b4d7-1d9e9a395784. — evidence: subagent:a8a47971-c0dd-407d-b4d7-1d9e9a395784#dispatch; basis: output; reopen: if the subagent run is interrupted or returns findings that change the plan
- Reviewer subagent run a8a47971-c0dd-407d-b4d7-1d9e9a395784 finished failed with 0/3 done and 3 failed: one connection error, one missing /home/li/primary/progress.md, and one context-size overflow. — evidence: subagent:status-a8a47971-c0dd-407d-b4d7-1d9e9a395784#failed-0-of-3-done-3-failed; basis: output; reopen: if a reviewer child is revived or new reviewer output is produced
- The orchestrate claim helper failed before claiming because orchestrate-cli could not fetch stale nota-codec revision f761421c383121a04df761a444a3778d1c0b137d. — evidence: cmd:./tools/orchestrate claim cloud-operator ...#exit-101-nota-codec-f761421c-not-found; basis: output; reopen: if tools/orchestrate is fixed and a claim is successfully acquired
- Retrying tools/orchestrate claim for the Unit 3 CriomOS paths still failed with the same nota-codec f761421c revision-not-found dependency error. — evidence: cmd:./tools/orchestrate claim cloud-operator /git/github.com/LiGoldragon/CriomOS/modules/nixos/test-vm-host.nix ...#exit-101-nota-codec-f761421c-not-found; basis: output; reopen: if tools/orchestrate is fixed and a claim is successfully acquired
- CriomOS full nix flake check without overrides is expected to fail because the default system input throws `CriomOS: no system input was provided`. — evidence: cmd:nix flake check --refresh in /git/github.com/LiGoldragon/CriomOS#error-no-system-input-was-provided; basis: output; reopen: if CriomOS default flake inputs are changed to evaluable defaults
- CriomOS full nix flake check with temporary overrides progressed but hit a pre-existing private/missing nota-derive fetch in repository-receive-role-policy. — evidence: cmd:nix flake check --refresh --override-input system ... --override-input horizon ... in /git/github.com/LiGoldragon/CriomOS#nota-derive-fetch-failure; basis: output; reopen: if the repository-receive-role-policy dependency graph or private fetch availability changes
- CriomOS aggregate imports modules/nixos/complex.nix for every node. — evidence: /git/github.com/LiGoldragon/CriomOS/modules/nixos/criomos.nix:17; basis: observed; reopen: if the CriomOS module aggregate import list changes
- CriomOS modules/nixos/complex.nix accepts deployment.includeComplex with a default of true and wraps its config in lib.mkIf includeComplex. — evidence: /git/github.com/LiGoldragon/CriomOS/modules/nixos/complex.nix:8; basis: observed; reopen: if modules/nixos/complex.nix changes around includeComplex or the Clavifaber service
- CriomOS-test-cluster mkVmTest.nix threads deployment.includeComplex = false into hermetic VM guests. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/mkVmTest.nix:374; basis: observed; reopen: if lib/mkVmTest.nix changes around guest specialArgs deployment
- CriomOS-test-cluster mkDeployTest.nix threads deployment.includeComplex = false into the deploy smoke guest configuration. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/mkDeployTest.nix:416; basis: observed; reopen: if lib/mkDeployTest.nix changes around deployment specialArgs
- CriomOS-test-cluster deploy-flake.nix threads deployment.includeComplex = false in both generated deploy-flake text and in-process toplevelFor derivation. — evidence: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/deploy-flake.nix:148; basis: observed; reopen: if lib/deploy-flake.nix changes around deployment specialArgs
- Chroma main is commit 6879a393123ec147b2d8ddbe92caec498eb5646e on both local and origin bookmarks. — evidence: cmd:jj log -r main in /git/github.com/LiGoldragon/chroma#6879a393123ec147b2d8ddbe92caec498eb5646e; basis: output; reopen: if chroma main advances or is rewritten
- Normal CriomOS-home nix flake lock --update-input chroma was blocked by GitHub API HTTP 403 rate limiting. — evidence: cmd:nix flake lock --update-input chroma in /git/github.com/LiGoldragon/CriomOS-home#HTTP-403-rate-limit; basis: output; reopen: if GitHub API rate limiting clears and a normal lock update succeeds
- CriomOS-home flake.lock chroma bump was committed and pushed to main as a0c391a0. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/CriomOS-home#main-a0c391a0-origin-a0c391a0; basis: output; reopen: if CriomOS-home main advances or is rewritten
- Lojix current pushed main is b1a05212e3a3e56e8cdd000adcc4b76b6de37a09. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/lojix#main-b1a05212-origin-b1a05212; basis: output; reopen: if Lojix main advances or is rewritten
- Lojix flake.nix source filtering includes regular `.nota` files, so tests/fixtures/host-set-cluster.nota is included by the current Nix package source. — evidence: /git/github.com/LiGoldragon/lojix/flake.nix:32; basis: observed; reopen: if lojix flake.nix source filtering changes
- Repinning CriomOS-test-cluster top-level lojix to b1a05212 eliminated the earlier Lojix package fixture build failure; the remaining failure moved into the VM deploy-smoke path. — evidence: cmd:nix build .#checks.x86_64-linux.lojix-deploy-smoke after lojix repin#no-include_str-fixture-failure-and-VM-timeout; basis: test; reopen: if CriomOS-test-cluster lojix input changes or Lojix package source filtering changes
- CriomOS-test-cluster mkDeployTest.nix was updated so lojix-write-configuration uses the current ConfigurationWriteRequest shape with TestDefaults before the output path. — evidence: cmd:jj diff --no-pager -- lib/mkDeployTest.nix#ConfigurationWriteRequest-includes-TestDefaults; basis: observed; reopen: if mkDeployTest.nix or lojix-write-configuration request shape changes
- CriomOS-test-cluster mkDeployTest.nix bounds the explicit target SSH reachability probe with `timeout 30s ssh -n` and `ConnectTimeout=10`, preventing that probe from consuming the whole VM-test budget. — evidence: cmd:jj diff --no-pager -- lib/mkDeployTest.nix#timeout-30s-ssh-n-probe; basis: observed; reopen: if mkDeployTest.nix changes around the SSH reachability probe
- After horizon-rs main advanced, CriomOS-test-cluster fieldlab NOTA fixtures were canonicalized by removing bracket delimiters from bare-safe atoms, then projections-match-fieldlab, pod-missing-super-node-rejected, and multiple-tailnet-controllers-rejected passed. — evidence: cmd:nix build projections-match-fieldlab pod-missing-super-node-rejected multiple-tailnet-controllers-rejected#PASS-canonical-horizon-2; basis: test; reopen: if clusters/*.nota, horizon-rs, or fixture JSON changes
- CriomOS-test-cluster spirit-nspawn-can-build failed in the final structural gate run because a Spirit/persona path attempted a private or missing nota-derive fetch. — evidence: cmd:nix build .#checks.x86_64-linux.spirit-nspawn-can-build#Repository-not-found-nota-derive-class-failure; basis: test; reopen: if nota-derive access or the Spirit nspawn dependency graph changes
- CriomOS Unit 3 scoped image-exchange emission was implemented in modules/nixos/test-vm-host.nix, adding host-set-based peer public-key computation and additive nix.settings.extra-trusted-public-keys emission. — evidence: cmd:jj diff --no-pager -- modules/nixos/test-vm-host.nix flake.nix checks/image-exchange-keys-scoped-to-co-hosts/default.nix#imageExchangePublicKeys-extra-trusted-public-keys; basis: observed; reopen: if modules/nixos/test-vm-host.nix changes around hostSetOf, coHostedTestVirtualMachines, imageExchangePublicKeys, or extra-trusted-public-keys
- CriomOS added and wired the image-exchange-keys-scoped-to-co-hosts check, proving co-host keys are scoped to declared TestVm host sets and cluster trusted-public-keys remain present. — evidence: cmd:nix build --override-input system path:<tmp> --override-input horizon path:<atlas-fixture> .#checks.x86_64-linux.image-exchange-keys-scoped-to-co-hosts#PASS; basis: test; reopen: if checks/image-exchange-keys-scoped-to-co-hosts/default.nix, modules/nixos/test-vm-host.nix, modules/nixos/nix/client.nix, or flake.nix check wiring changes
- CriomOS targeted policy gate set passed after Unit 3 using temporary system and fieldlab atlas horizon overrides: image-exchange-keys-scoped-to-co-hosts, vm-testing-prometheus-policy, nspawn-role-policy, metal-firmware-policy, router-wifi-horizon-policy, and nix-role-policy. — evidence: cmd:nix build --override-input system path:<tmp> --override-input horizon path:<atlas-fixture> image-exchange-keys-scoped-to-co-hosts vm-testing-prometheus-policy nspawn-role-policy metal-firmware-policy router-wifi-horizon-policy nix-role-policy#PASS; basis: test; reopen: if CriomOS source, flake.lock, or the temporary-check assumptions change
- CriomOS local main and origin are now 6646275dbe314f607e979d26486a30a4535d4a48 with a clean working copy after later main advancement. — evidence: cmd:jj log/st in /git/github.com/LiGoldragon/CriomOS#main-6646275d-clean; basis: output; reopen: if CriomOS main advances, is rewritten, or the working copy changes
- CriomOS-test-cluster main and origin are now 18441971980f with a clean working copy after the latest consumer repin. — evidence: cmd:jj bookmark list --all && jj st in /git/github.com/LiGoldragon/CriomOS-test-cluster#main-18441971-origin-18441971-clean; basis: output; reopen: if CriomOS-test-cluster main advances, is rewritten, or the working copy changes
- CriomOS-test-cluster flake.lock now pins the criomos input to 6646275dbe314f607e979d26486a30a4535d4a48, horizon_2 to 087a3b6dda8f2d28a429fb7096d5054ef0577c00, and the active root lojix node lojix_2 to b1a05212e3a3e56e8cdd000adcc4b76b6de37a09. — evidence: cmd:nix flake lock --override-input criomos github:LiGoldragon/CriomOS/6646275d... && python lock inspection in /git/github.com/LiGoldragon/CriomOS-test-cluster#criomos-6646275d-horizon-087a3b6d-root-lojix_2-b1a05212; basis: output; reopen: if CriomOS-test-cluster flake.lock changes around criomos, horizon, or lojix
- CriomOS-test-cluster repinning to current CriomOS main 6646275d also advanced the nested criomos/criomos-home input from a0c391a06bd9a790cf79a2273051e407ace11e65 to 865c47e1541022ef83f09d4dd214eba195b0d8d0. — evidence: cmd:nix flake lock --override-input criomos github:LiGoldragon/CriomOS/6646275d...#Updated-input-criomos-criomos-home-a0c391a-to-865c47e; basis: output; reopen: if CriomOS-test-cluster flake.lock or the CriomOS input changes again
- CriomOS-test-cluster lib/deploy-flake.nix now generates deploy flake URLs from the copied parent flake.lock original GitHub input forms, including ?ref=main when present, instead of exact locked revision URLs. — evidence: edit:/git/github.com/LiGoldragon/CriomOS-test-cluster/lib/deploy-flake.nix#originalGithubUrl-referenceSuffix; basis: observed; reopen: if lib/deploy-flake.nix changes around originalGithubUrl, nixpkgsUrl, criomosLibUrl, or criomosUrl
- The deploy-flake original-URL fix was committed and pushed as 542ba95b0fc3 before the later repin. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/CriomOS-test-cluster#main-542ba95b-origin-542ba95b; basis: output; reopen: if CriomOS-test-cluster history is rewritten before 542ba95b
- The latest CriomOS-test-cluster consumer repin was committed and pushed as 18441971980f. — evidence: cmd:jj bookmark list --all in /git/github.com/LiGoldragon/CriomOS-test-cluster#main-18441971-origin-18441971; basis: output; reopen: if CriomOS-test-cluster main advances or is rewritten
- Published CriomOS-test-cluster main 18441971980f passed the relevant structural and VM/deploy gate batch: projections-match-fieldlab, pod-missing-super-node-rejected, multiple-tailnet-controllers-rejected, cluster-contracts, full-module-contracts, vm-mercury, lojix-deploy-smoke, vm-base-home, vm-dune, and vm-edge-desktop. — evidence: cmd:nix build --refresh --no-link --print-build-logs github:LiGoldragon/CriomOS-test-cluster/main#checks.x86_64-linux.{projections-match-fieldlab,pod-missing-super-node-rejected,multiple-tailnet-controllers-rejected,cluster-contracts,full-module-contracts,vm-mercury,lojix-deploy-smoke,vm-base-home,vm-dune,vm-edge-desktop}#completed-without-error; basis: test; reopen: if CriomOS-test-cluster main advances, any named check changes, or any relevant input pin changes
- Published CriomOS-test-cluster main still fails spirit-nspawn-can-build through the private or missing nota-derive fetch path. — evidence: cmd:nix build --refresh --no-link --print-build-logs github:LiGoldragon/CriomOS-test-cluster/main#checks.x86_64-linux.spirit-nspawn-can-build#Failed-to-fetch-git-repository-https-github.com-LiGoldragon-nota-derive.git; basis: test; reopen: if nota-derive access or the Spirit nspawn dependency graph changes
- horizon-rs local main is 087a3b6dda8f2d28a429fb7096d5054ef0577c00 with a clean working copy. — evidence: cmd:jj log && jj st in /git/github.com/LiGoldragon/horizon-rs#main-087a3b6d-clean; basis: output; reopen: if horizon-rs main advances, is rewritten, or the working copy changes
- signal-lojix main and origin are 970da115 with a clean working copy. — evidence: cmd:jj bookmark list --all && jj st in /git/github.com/LiGoldragon/signal-lojix#main-970da115-origin-970da115-clean; basis: output; reopen: if signal-lojix main advances, is rewritten, or the working copy changes
- meta-signal-lojix main and origin are 26b617c2 with a clean working copy. — evidence: cmd:jj bookmark list --all && jj st in /git/github.com/LiGoldragon/meta-signal-lojix#main-26b617c2-origin-26b617c2-clean; basis: output; reopen: if meta-signal-lojix main advances, is rewritten, or the working copy changes
- horizon-rs current main 087a3b6dda8f2d28a429fb7096d5054ef0577c00 passed cargo build, cargo test, and cargo clippy -- -D warnings; the cargo test run executed 135 tests. — evidence: cmd:cd /git/github.com/LiGoldragon/horizon-rs && cargo build && cargo test && cargo clippy -- -D warnings#Finished-build-test-clippy-135-tests; basis: test; reopen: if horizon-rs source, Cargo.lock, or dependency pins change
- horizon-rs repo-local bead scan did not run because no beads database was found, so no new bead state was initialized. — evidence: cmd:cd /git/github.com/LiGoldragon/horizon-rs && bd list --status open --flat --limit 20#error-no-beads-database-found; basis: output; reopen: if a horizon-rs beads database is initialized or BEADS_DIR is configured
- CriomOS repo-local bead scan did not run because no beads database was found, so no new bead state was initialized. — evidence: cmd:cd /git/github.com/LiGoldragon/CriomOS && bd list --status open --flat --limit 20#error-no-beads-database-found; basis: output; reopen: if a CriomOS beads database is initialized or BEADS_DIR is configured
- Current CriomOS main passed image-exchange-keys-scoped-to-co-hosts, vm-testing-prometheus-policy, nspawn-role-policy, metal-firmware-policy, router-wifi-horizon-policy, and nix-role-policy using temporary synthetic system and fieldlab horizon overrides. — evidence: cmd:nix build --refresh --no-link --print-build-logs --override-input system path:<tmp> --override-input horizon path:<fieldlab-atlas> github:LiGoldragon/CriomOS/main#checks.x86_64-linux.{image-exchange-keys-scoped-to-co-hosts,vm-testing-prometheus-policy,nspawn-role-policy,metal-firmware-policy,router-wifi-horizon-policy,nix-role-policy}#completed-without-error; basis: test; reopen: if CriomOS source, flake.lock, or the temporary-check assumptions change
- horizon-rs, CriomOS, CriomOS-test-cluster, CriomOS-home, lojix, signal-lojix, and meta-signal-lojix had clean working copies and local main matched main@origin at the closeout check. — evidence: cmd:repo parity loop over horizon-rs CriomOS CriomOS-test-cluster CriomOS-home lojix signal-lojix meta-signal-lojix#parity-clean-all-code-repos; basis: output; reopen: if any listed repo working copy changes or its main/main@origin relation changes
- CriomOS flake.lock root criomos-home was 865c47e1541022ef83f09d4dd214eba195b0d8d0 and both CriomOS and CriomOS-home resolved Chroma to 6879a393123ec147b2d8ddbe92caec498eb5646e. — evidence: cmd:python inspect /git/github.com/LiGoldragon/CriomOS/flake.lock and /git/github.com/LiGoldragon/CriomOS-home/flake.lock#criomos-home-865c47e-chroma-6879a393; basis: output; reopen: if CriomOS or CriomOS-home flake.lock changes around criomos-home or chroma
- Cloud-operator closeout report reports/cloud-operator/388-Handover-vm-testing-closeout.md was written, committed, and pushed on primary main at 4137916dfd40. — evidence: cmd:jj log --no-graph -r main|main@origin in /home/li/primary#main-4137916d-origin-4137916d-clean; basis: output; reopen: if primary history is rewritten before 4137916d or report 388 is moved or edited
- Report 388 states the report-55 implementation handoff is landed through the safe hermetic surface, with horizon-rs, CriomOS, and CriomOS-test-cluster dependency order complete and no live remediation, deploy, activation, router switch, or Prometheus live operation run. — evidence: /home/li/primary/reports/cloud-operator/388-Handover-vm-testing-closeout.md:1; basis: doc; reopen: if report 388 is edited or superseded
- The user stated Syncthing is too heavy for the phone and the phone media mirror should improve Android/Criome WiFi AP name resolution before falling back to the regular domain. — evidence: user@history:syncthing-too-heavy; basis: user; reopen: if the user re-allows Syncthing for the Android phone path or changes the mobile name-resolution priority
- Spirit record vgon captures the constraint that Syncthing is excluded from the phone media mirror path because it is too heavy for the phone, while Immich remains the phone uploader/gallery candidate. — evidence: cmd:spirit Record Syncthing phone media mirror constraint#RecordAccepted-vgon; basis: output; reopen: if Spirit record vgon is clarified, superseded, or retired
- Spirit record 87ts captures the decision that mobile Android clients on the Criome WiFi AP should get near-native name resolution for cluster services before falling back to ordinary public DNS names. — evidence: cmd:spirit Record Android AP name-resolution decision#RecordAccepted-87ts; basis: output; reopen: if Spirit record 87ts is clarified, superseded, or retired
- Spirit record iwbt captures the decision that cluster configuration and Horizon should carry the public-domain mapping for ordinary DNS fallback, with criome.net assigned per cluster and goldragon owning goldragon.criome.net, while the exact NOTA shape remains open. — evidence: cmd:spirit Record public-domain/Horizon cluster-data decision#RecordAccepted-iwbt; basis: output; reopen: if Spirit record iwbt is clarified, superseded, or retired
- Cloud-operator report reports/cloud-operator/389-Proposal-mobile-media-domain-resolution-poc.md was written, committed, and pushed on primary main at 42b9c11e7120. — evidence: cmd:jj log --no-graph -r main|main@origin in /home/li/primary#main-42b9c11e-origin-42b9c11e-clean; basis: output; reopen: if primary history is rewritten before 42b9c11e or report 389 is moved or edited
- Report 389 proposes two name planes for Immich/mobile media: internal `.criome` and phone-friendly `goldragon.criome.net`, with AP-local private resolution and no Syncthing on Android. — evidence: /home/li/primary/reports/cloud-operator/389-Proposal-mobile-media-domain-resolution-poc.md:1; basis: doc; reopen: if report 389 is edited or superseded
- horizon-rs ClusterProposal currently has nodes, users, domains, and trust fields; DomainProposal currently carries only DomainSpecies; DomainSpecies currently has only Cloudflare. — evidence: /git/github.com/LiGoldragon/horizon-rs/lib/src/proposal.rs:24 and /git/github.com/LiGoldragon/horizon-rs/lib/src/species.rs:169; basis: observed; reopen: if horizon-rs proposal.rs or species.rs changes around ClusterProposal, DomainProposal, or DomainSpecies
- horizon-rs currently hardcodes `criome` in DomainName::for_tailnet and CriomeDomainName::for_node, and hardcodes `criome.net` in user email and Matrix identity derivation. — evidence: /git/github.com/LiGoldragon/horizon-rs/lib/src/name.rs:79 and /git/github.com/LiGoldragon/horizon-rs/lib/src/user.rs:89; basis: observed; reopen: if horizon-rs name.rs or user.rs changes around domain derivation
- CriomOS network/dnsmasq.nix already renders router-local address records from horizon node data, including entry.criomeDomainName and entry.nixCacheDomain aliases, for router nodes. — evidence: /git/github.com/LiGoldragon/CriomOS/modules/nixos/network/dnsmasq.nix:43; basis: observed; reopen: if CriomOS modules/nixos/network/dnsmasq.nix changes around mkPrimaryRecords or localAddressRecords
- Cloud-operator report reports/cloud-operator/390-Proposal-pan-cluster-domain-and-lojixos-extraction.md was written, committed, and pushed on primary main at bd01009c6e36. — evidence: cmd:jj log --no-graph -r main|main@origin in /home/li/primary#main-bd01009c-origin-bd01009c-clean; basis: output; reopen: if primary history is rewritten before bd01009c or report 390 is moved or edited
- Report 390 proposes adding a Horizon cluster-level domain_configuration with internal_suffix and public_cluster_domains, moving hardcoded `.criome` and `criome.net` derivations into projected configuration, and using that seam before any LojixOS extraction. — evidence: /home/li/primary/reports/cloud-operator/390-Proposal-pan-cluster-domain-and-lojixos-extraction.md:1; basis: doc; reopen: if report 390 is edited or superseded
- The user directed implementation of the LojixOS split as a rename-and-configuration split: LojixOS is the generic OS substrate, while CriomOS is a simple per-deploy configuration crate with a thin NOTA data tree that lojix precompiles for consumption. — evidence: user@turn-prefix:lets-implement-lojix-os-split; basis: user; reopen: if the user changes or retracts the LojixOS/CriomOS split direction
- Spirit record 1hyg captures the LojixOS split as a rename-and-configuration split with CriomOS as a per-deploy configuration crate containing a thin NOTA data tree precompiled by lojix. — evidence: cmd:spirit /tmp/lojixos-record.nota#RecordAccepted-1hyg; basis: output; reopen: if Spirit record 1hyg is clarified, superseded, or retired
- The user said the team is green for transitioning everything to the new lojix daemon and testing it in production, and pointed to cloud-designer's latest reports for details. — evidence: user@turn-prefix:lets-implement-lojix-os-split; basis: user; reopen: if the user narrows or revokes production-transition authorization
- Spirit record bsg1 already states the lojix production cutover is active now: the lojix daemon becomes the production deploy path and legacy lojix-cli is retired per node. — evidence: cmd:spirit PublicTextSearch [lojix daemon production transition]#RecordsObserved-bsg1; basis: output; reopen: if Spirit record bsg1 is clarified, superseded, or retired
- Spirit record h03z already states lojix integrates with criome-based authentication now as part of the production cutover. — evidence: cmd:spirit PublicTextSearch [lojix daemon production transition]#RecordsObserved-h03z; basis: output; reopen: if Spirit record h03z is clarified, superseded, or retired
- The lojix daemon repo already depends on horizon-lib from horizon-rs branch main and imports ClusterProposal, Horizon, and Viewpoint from horizon_lib in src/schema_runtime.rs. — evidence: /git/github.com/LiGoldragon/lojix/Cargo.toml:35 and /git/github.com/LiGoldragon/lojix/src/schema_runtime.rs:22; basis: observed; reopen: if lojix Cargo.toml or src/schema_runtime.rs changes around horizon-lib usage
- lojix src/schema_runtime.rs already parses a NOTA ClusterProposal and calls horizon projection code for materialization paths. — evidence: /git/github.com/LiGoldragon/lojix/src/schema_runtime.rs:458; basis: observed; reopen: if lojix src/schema_runtime.rs changes around ClusterProposal parsing or projection
- lojix Cargo.lock currently pins horizon-lib to git rev 214e6816d27ef19d9218311b6bb65f64c88881b7, which is older than current horizon-rs main 087a3b6dda8f2d28a429fb7096d5054ef0577c00. — evidence: /git/github.com/LiGoldragon/lojix/Cargo.lock:280; basis: observed; reopen: if lojix Cargo.lock is updated around horizon-lib or horizon-rs main changes
- Cloud-designer report 52 says the refreshed lojix daemon with two CLIs, durable sema-engine state, disconnect-surviving deploy jobs, and live Prometheus host-untouched proof is on lojix main, with remaining Prometheus VM-host work designed but awaiting a decision at that time. — evidence: /home/li/primary/reports/cloud-designer/52-lojix-and-vm-testing-synthesis.md:1; basis: doc; reopen: if report 52 is edited or superseded
- Cloud-designer report 60 says the DigitalOcean provider landed on cloud main with 9/9 DigitalOcean tests passing and is ready for a live test once a token exists at gopass digitalocean/api-token. — evidence: /home/li/primary/reports/cloud-designer/60-digitalocean-landed-livetest-ready.md:1; basis: doc; reopen: if report 60 is edited or superseded
- Cloud-designer report 61 designs a supervised browser workflow for minting a DigitalOcean token and explicitly says nothing in that workflow was executed. — evidence: /home/li/primary/reports/cloud-designer/61-browser-token-workflow.md:1; basis: doc; reopen: if report 61 is edited or superseded
- Cloud-designer report 62 says browser-use tooling was implemented and deployed in CriomOS-home Large profile at generation 799, with a neutral-page smoke test against local Gemma 4 and no account touched. — evidence: /home/li/primary/reports/cloud-designer/62-browser-tools-home-deploy.md:1; basis: doc; reopen: if report 62 is edited or superseded

## Learned
- This lane is cloud-operator; substantive review or implementation reports must go under reports/cloud-operator, not reports/operator. — source: user@history:your-the-cloude-operator-not-operator
- After cloud-designer flattening, do not reintroduce Lojix root workspaces or triad-port paths; current Lojix code paths are under repo-root src/ and tests/. — source: cmd:find . -maxdepth 2 -type f in /git/github.com/LiGoldragon/lojix#src-layout
- For forwarded cloud-designer prompts, gap-check Spirit and technical content instead of blindly duplicating intent capture; recent Spirit lookups found oh9l and lc28 but not gdgv. — source: cmd:spirit Lookup gdgv/oh9l/lc28#gdgv-not-found-oh9l-lc28-found
- Report 55 merge order is horizon-rs first, CriomOS second, CriomOS-test-cluster last because only the test-cluster carries cross-repo flake pins. — source: /home/li/primary/reports/cloud-designer/55-operator-implementation-handoff.md:1
- When CriomOS feature work forked from an immutable next bookmark, rebasing the stack onto current main with --ignore-immutable and pushing only main allowed conflict resolution without treating the shared next bookmark as the deliverable. — source: cmd:jj rebase --ignore-immutable -s next -d main in /git/github.com/LiGoldragon/CriomOS#rebased-7-commits
- For CriomOS checks, create temporary system and horizon override flakes from a synthetic fixture or copied real projection; default stubs intentionally throw and are not useful for full flake checks. — source: /git/github.com/LiGoldragon/CriomOS/stubs/no-system/flake.nix:1
- GitHub API rate limiting can block nix flake lock; use local path overrides for testing and, when necessary, copy already-reviewed locked nodes or local prefetch hashes instead of fetching new upstream revisions. — source: cmd:nix flake lock --update-input ...#HTTP-403-rate-limit
- horizon-rs Machine schema now requires the super_nodes tail; stale Nota fixtures fail projection before the intended invariant is tested. — source: cmd:nix log /nix/store/a9rm0mljw5hza4smgfm2fhfs1icqc86i-pod-missing-super-node-rejected.drv#Machine-arity-drift
- Subagent quiet-attention nudges are not necessarily blockers; inspect run status, then continue independent work if agents are still running. — source: subagent:a8a47971-c0dd-407d-b4d7-1d9e9a395784#status-running-3-agents
- For hermetic CriomOS VM tests, prefer deployment.includeComplex=false over stubbing Clavifaber when the complex-publication service is not the invariant under test; production default stays includeComplex=true. — source: /git/github.com/LiGoldragon/CriomOS/modules/nixos/complex.nix:8 and /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/mkVmTest.nix:374
- Do not disable deployment.includeHome to dodge Edge VM package failures; standardTest.nix uses includeHome to assert the kept-home activation invariant for role-derived standard tests. — source: /git/github.com/LiGoldragon/CriomOS-test-cluster/lib/standardTest.nix:1
- If Edge VM tests fail on private nota-derive after includeComplex is disabled, test with a local chroma override; vm-dune passing under local chroma identified stale CriomOS-home chroma pin as the blocker. — source: cmd:nix build --override-input criomos/criomos-home/chroma path:/git/github.com/LiGoldragon/chroma .#checks.x86_64-linux.vm-dune#PASS-vm-dune-local-chroma
- When GitHub API rate limiting blocks a flake input refresh, an exact-commit override can still update the lock if the full commit id is correct; a wrong truncated Chroma SHA produced a 404 before the correct 6879a393123ec147b2d8ddbe92caec498eb5646e succeeded. — source: cmd:nix flake lock --override-input chroma github:LiGoldragon/chroma/6879a393123ec147b2d8ddbe92caec498eb5646e#Updated-input-chroma
- Use `jj log --no-graph --color never --template 'commit_id'` when shell-substituting a commit id; plain jj log can include graph glyphs that corrupt flake URLs. — source: cmd:nix flake lock --override-input criomos github:LiGoldragon/CriomOS/◆#path-error-then-no-graph-retry-succeeded
- Current Lojix `lojix-write-configuration` requires TestDefaults in ConfigurationWriteRequest before the output path; older six-field requests make the daemon service exit before socket creation. — source: /git/github.com/LiGoldragon/lojix/tests/write_configuration.rs:15
- In VM tests, an SSH reachability probe can hang after the target accepts the key; use `timeout` and `ssh -n` so the probe fails crisply instead of consuming the full runNixOSTest budget. — source: cmd:nix build .#checks.x86_64-linux.lojix-deploy-smoke#PASS-after-timeout-30s-ssh-n-probe
- Current horizon-rs main enforces canonical NOTA strings: bracket delimiters are rejected for bare-safe atoms, including paths, CIDRs, hashes, and IPv6 literals with no whitespace or structural delimiters. — source: cmd:nix build .#checks.x86_64-linux.projections-match-fieldlab#FAIL-before-broad-canonicalization-then-PASS-canonical-horizon-2
- A Lojix package failure on include_str!(tests/fixtures/*.nota) inside test-cluster can be a stale top-level Lojix pin; current Lojix main’s flake source filter includes .nota fixtures. — source: cmd:nix build .#checks.x86_64-linux.lojix-deploy-smoke#package-failure-gone-after-lojix-b1a05212-repin
- CriomOS-test-cluster spirit-nspawn-can-build is not currently a clean VM-integration witness because it can fail through the Spirit/persona private nota-derive dependency path. — source: cmd:nix build .#checks.x86_64-linux.spirit-nspawn-can-build#Repository-not-found
- For Unit 3 image-exchange checks, assert cluster trusted-public-keys as a subset rather than exact equality because NixOS can retain default cache keys in nix.settings.trusted-public-keys. — source: cmd:nix build .#checks.x86_64-linux.image-exchange-keys-scoped-to-co-hosts#failed-exact-trusted-keys-then-pass-subset
- For offline deploy-flake VM tests, the generated flake input URLs must match the copied flake.lock original node shape; mismatched exact-rev URLs cause Nix to update metadata and fetch GitHub inside the sandbox/offline path. — source: session experience: final combined CriomOS-test-cluster run after CriomOS ab4d7d72 pin hit ProposalSourceUnreachable until lib/deploy-flake.nix was changed to derive URLs from parentLock originals
- For CriomOS-test-cluster deploy-flake, deriving generated URLs from parentLock.nodes.<node>.original, including ?ref=main when present, preserves the copied flake.lock offline; deriving from locked revs still made Nix treat the input as changed and update inside the VM. — source: edit:/git/github.com/LiGoldragon/CriomOS-test-cluster/lib/deploy-flake.nix#originalGithubUrl-referenceSuffix and cmd:nix build github:LiGoldragon/CriomOS-test-cluster/main#checks.x86_64-linux.lojix-deploy-smoke#PASS
- When a flake.lock has both stale and active nodes for the same repo, inspect nodes.root.inputs to identify the active root input; CriomOS-test-cluster root lojix was lojix_2 at b1a05212 while a stale transitive node named lojix remained at 9d4eae3. — source: cmd:python inspect flake.lock in /git/github.com/LiGoldragon/CriomOS-test-cluster#root-lojix_2-b1a05212-stale-lojix-9d4eae3
- CriomOS main can advance concurrently during test-cluster finalization, so re-read CriomOS main and repin the consumer lock immediately before final published-ref gates. — source: cmd:jj log in /git/github.com/LiGoldragon/CriomOS#main-6646275d-after-test-cluster-542ba95b
- Primary reports may sweep existing shared working-copy edits because primary policy is whole-working-copy commits; report 390's commit also included an already-dirty system-designer report edit. — source: cmd:jj commit -m 'cloud-operator: sketch pan-cluster domain config'#included-system-designer-report-edit
- For mobile media access, treat Syncthing as excluded on Android and solve phone-native access through Immich plus AP-local DNS and ordinary public-domain TLS names. — source: Spirit records vgon, 87ts, iwbt
- The public `criome.net` value should be modeled as a public cluster domain suffix/root such as `goldragon.criome.net`, not as a literal top-level domain; service FQDNs should derive from service identity plus the cluster domain. — source: /home/li/primary/reports/cloud-operator/390-Proposal-pan-cluster-domain-and-lojixos-extraction.md:1
- Do not overload horizon-rs DomainSpecies::Cloudflare to mean public service-zone mapping; current `domains` is provider-flavored and too thin, so add or redesign an explicit domain configuration shape. — source: /git/github.com/LiGoldragon/horizon-rs/lib/src/species.rs:169 and /home/li/primary/reports/cloud-operator/389-Proposal-mobile-media-domain-resolution-poc.md:1
- The current LojixOS direction is not a broad extraction-first refactor; it is a rename-and-configuration split where CriomOS becomes a thin per-deploy NOTA config crate that lojix precompiles. — source: Spirit record 1hyg
- The answer to whether lojix uses horizon-rs as a library is yes for the current daemon: lojix depends on horizon-lib and uses ClusterProposal::project paths in schema_runtime.rs, but its Cargo.lock horizon rev is stale. — source: /git/github.com/LiGoldragon/lojix/Cargo.toml:35 and /git/github.com/LiGoldragon/lojix/Cargo.lock:280

## Open
- CriomOS-test-cluster spirit-nspawn-can-build remains blocked by a private or missing nota-derive fetch through the Spirit/persona dependency path. — verifies: Fix or provide access to the nota-derive dependency or update the Spirit/persona lock graph, then rerun nix build --no-write-lock-file .#checks.x86_64-linux.spirit-nspawn-can-build.
- The live path that removes LiveNotYetEnabled and performs the real host-untouched cycle remains to be implemented, but first live run is psyche-gated for any operation outside the newly authorized lojix daemon production-transition scope. — verifies: Implement code paths and dry/hermetic checks from report 55; before any live run not covered by the Lojix daemon transition authorization, get explicit user authorization for that live run.
- Prometheus goldragon wiring remains open, including stale-arity Machine records in real goldragon/datom.nota before appending Prometheus VmHost data. — verifies: After integration gates pass, inspect goldragon/datom.nota, add the Machine None None [] tails to the five records, add Prometheus VmHost/test-VM nodes/TestDefaults per report 55, and run horizon projection checks.
- The VM-testing principle in reports/cloud-designer/50-general-vm-testing-interface/intent-capture.md is still pending Spirit capture because Spirit writes require store migration. — verifies: After spirit-migrate-store or maintainer resolution, record the pending principle in Spirit and cite the accepted record.
- tools/orchestrate claim is still broken on primary due stale nota-codec revision f761421c, so the cloud-operator claim was not recorded. — verifies: Fix or bypass orchestrate-cli dependency resolution, then run tools/orchestrate claim cloud-operator for the active repo surfaces and confirm the lock/claim state.
- CriomOS full nix flake check remains blocked by repository-receive-role-policy fetching private/missing nota-derive after system/horizon overrides. — verifies: Fix or provide access to the nota-derive dependency, then rerun full nix flake check with proper system/horizon overrides.
- Production Stack-B cutover parity still has broader non-VM gaps including secrets parity, packaging/systemd/request-builder surface, and safe non-activation test path. — verifies: Implement and test the remaining report-40 cutover gates without live deployment unless explicitly authorized.
- Legacy lojix-run ExactReferenceRewriter behavior remains untrusted for current production Stack A. — verifies: Read and fix lojix-run exact-ref rewrite behavior or replace it with the new daemon/request-builder path, then add tests proving exact ref behavior.
- Which exact production nodes and operations should be first in the newly authorized lojix daemon cutover sequence, and what stop conditions should guard each activation? — verifies: Read cloud-designer reports 44-49 plus any latest cutover report, extract the runbook and target order, then either cite a report/user message that names the sequence or ask the user for the missing target/operation details.
- How should the LojixOS rename/config-crate split be reflected in repositories and crate names without disrupting the immediate daemon production transition? — verifies: Draft or implement the concrete repo/crate plan showing which existing CriomOS/CriomOS-home files move or rename, where the thin CriomOS NOTA config crate lives, and how lojix precompiles it; close with code diffs or a committed design report.
- Does lojix still build and pass its relevant tests after updating horizon-lib from stale rev 214e6816d27ef19d9218311b6bb65f64c88881b7 to current horizon-rs main and after adding any config-crate precompile path? — verifies: Update the lojix horizon-lib dependency as needed, run the relevant cargo and Nix gates in /git/github.com/LiGoldragon/lojix, and capture pass/fail output.
- What exact Horizon schema should implement `domain_configuration` now that the user ratified a per-deploy config crate and LojixOS rename split? — verifies: Patch horizon-rs with the chosen types, update goldragon/test fixtures, and prove decode/projection tests for internal suffix plus public cluster domain such as goldragon.criome.net.
- Can CriomOS or the future LojixOS render AP-local DNS aliases for both internal `.criome` and public `goldragon.criome.net` names from projected Horizon/config data without hardcoded service or cluster strings? — verifies: Add or update a CriomOS/LojixOS Nix check, likely extending resolver-role-policy or an Immich-specific check, proving generated dnsmasq records for both name planes.

## Next
- Finish reading the detailed cloud-designer lojix cutover reports 44-49 and current lojix repo guidance before editing code. → The implementation plan is grounded in the latest daemon production-cutover runbooks rather than the earlier high-level summary alone.
- Answer and operationalize the horizon-rs dependency question by updating lojix's horizon-lib pin if needed and running the relevant lojix gates. → The new lojix daemon uses current Horizon schema/projection code, or the exact compatibility failure is explicit.
- Design the minimal first implementation slice for the LojixOS rename/config-crate split: repo/crate naming, CriomOS thin NOTA config tree, and lojix precompile tool path. → The user-ratified split is concrete enough to edit repos without drifting into speculative broad extraction.
- Implement Horizon domain configuration for internal suffix plus public cluster domain, update fixtures, and run horizon-rs build/test/clippy gates. → Pan-cluster public-domain configuration exists in projected Horizon and hardcoded `criome.net`/`.criome` paths are reduced or explicitly queued.
- Update CriomOS or the new LojixOS surface to consume projected domain configuration for DNS aliases and add a Nix policy check. → AP-local name resolution can render both internal `.criome` and public `goldragon.criome.net` names from data.
- Proceed with the new lojix daemon production transition only through the user-authorized scope, using cloud-designer runbooks and stopping at any unhandled secrets, auth, or activation safety blocker. → Production testing advances without reverting to legacy lojix-cli assumptions or exceeding the authorization boundary.
- Implement the remaining report-55 live path and Prometheus wiring only within the host-untouched/user-namespace/no-sudo/no-router-switch safety envelope and only after explicit first-live-run authorization for any live operation not covered by the lojix daemon cutover authorization. → The live/multi-host remainder is ready for explicit operation-specific authorization without violating the safety envelope.
