# "they need a naming scheme. They need a naming standard."

## 2026-08-14T00:27+02:00 — the name coming in dynamically for each flow

Context (agent-authored, separate from the psyche's words): stated
while working out sibling-flow coexistence; the psyche identifies why
aspect names were being multiplied, and replaces that with dynamic
per-flow names for inter-flow messaging. Ellipses mark trims.

> So I've had another thought, which is that I realized one of the
> reasons I was using these aspect names was for later on when
> messaging kicks in and we need a way for agents, for flows, sorry,
> I'm going to start changing my vocabulary. We need a way for flows.
> I want a way for flows to be able to exchange, to communicate. So to
> interact, right? So I need flows to be able to interact, which means
> they need a way to be, they need a naming scheme. They need a naming
> standard. So this is where the idea like design and all that. And I
> thought I was going to extend the names because I wanted more than
> one design flow. So I was like, oh, okay, we're going to do like
> ideation and consideration and all these complicated terms. But it's
> really just a mind bender. The goal is to simplify cognitive cost.
> So design is what I want to think about. But then we need the name,
> right? So I see the name coming in dynamically for each flow. And so,
> for example, and this is something I implemented in the Pi harness
> sort of before, where the first prompt, and I'm not stuck on this
> mechanism because I think the name can evolve as the flow evolves.
> But for simplicity, I was using the first prompt and I was sending
> the load, the text of the first prompt to another separate LLM call
> that would think of a name that this flow would best represent,
> right? So, for example, you're working on, I don't know, like the
> awareness aspects design or something. Well, it's a design flow,
> right? So there's the design is sort of going to be either the
> prefix or the suffix, and then it has a more specific name, a
> complex name, like I was saying. It's going to be more than one
> word. So that's how I see it because names have to be useful. And if
> the only thing that other flows see when they get a list of flows
> they can interact with is like something like design awareness
> aspect flows or awareness aspect harness or something like that,
> that right away gives them a really good idea of what this flow is
> about and whether or not what they're trying to convey or what
> they're trying to share should reach that flow or another one.

— psyche, 2026-08-14T00:27+02:00 (Design sibling flow 1030529c),
dictated.
