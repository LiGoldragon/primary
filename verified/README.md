Facts about harnesses and tools, verified by a witness (code read or probe).
One file per subject. Each fact is a `## ` heading (one sentence).
Under it: `- <date> · session <short-id> · method: code read <source> | probe: <command>` then 1–4 lines of the witness, quoted or tightly summarised.
Keep unknowns unknown; never upgrade a claim into a fact.
