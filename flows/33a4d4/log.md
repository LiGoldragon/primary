# Chroma location and schema upgrade

Investigating the laptop location failure and realizing the requested San Cristóbal override and Chroma schema upgrade.

- The living requested Chroma history recovery, bearings on location/output failures, a San Cristóbal location hotfix, and current Ethos/Datom with redundant single-field structs removed.
- Dispatched `remember` for historical evidence, `location` for live location/output state, and `chroma_upgrade` for current schema/dependency scope.
- `remember` recovered historical Chroma/Emacs projection decisions and their later corrections; these are historical claims, not present-runtime witnesses. It committed and pushed preexisting primary changes unchanged as `283810db3ee1` before this flow wrote artifacts.
- `location` reported a live active Chroma 0.4.0, State.{ Light 6500 85 }, SolarClockUnavailable, and GeoClue fixes rejected at 3149 m and 26000 m against Chroma’s 1 km acceptance threshold. It identified an existing GeoClue static-source hotfix scoped to the laptop.
- `chroma_upgrade` read upstream Chroma 0.4.0 and identified 11 single-field schema wrappers, older pinned dependencies, a stale regeneration tool, and CLI/config consumers requiring coordinated migration.
- Dispatched implementation to `location` for the host-only static override and to `chroma_upgrade` for the Chroma/Home migration and validation.
- Read-only investigation roles could not perform implementation; dispatched dedicated `implement` and `hotfix` workers instead. `implement` owns Chroma and Home schema consumers; `hotfix` owns the Horizon/goldragon/CriomOS fixed-location path.
- `remember` observed a transient warmth mismatch during a GammaRelay panic/restart, then witnessed systemd restart Chroma and restore convergence at 2700 K / 85%. The earlier snapshot did not establish a missing recovery mechanism. Location remained unavailable.
- The living confirmed Prometheus is available again after the move and authorized obvious improvements within the requested topics while away. Relayed builder availability and expanded implementation authority to both workers; dispatched a bounded read-only investigation of the GammaRelay panic.
- `implement` pushed Chroma 0.5.0 (`490698247879`) and Home consumer migration (`23ef5e5f7322`). It reported 118 active local tests passing and all four configured remote Chroma checks passing on Prometheus. Home remote checks remained in progress at this checkpoint.
- `hotfix` pushed Horizon 0.9 schema (`49746ba3`) and CriomOS GeoClue consumer (`7995b3fe`); its Horizon remote check passed. An initial investigation of the legacy proposal was superseded by finding current Goldragon main’s cluster definition and deterministic composer. No source data gap remained; cluster/Lojix pin migration and deployment were still in progress.
- `remember` found no newer GammaRelay package release addressing the witnessed panic. The exact D-Bus trigger remained unknown, so no speculative output-backend patch was made.
- `implement` completed Home’s two remote checks, corrected two stale resident-test requests, and pushed `fed36908166c`. It activated Chroma/Home independently of location. Its live witness was Chroma Light/2700 K/85%, GammaRelay 2700/0.85, Emacs Applied revision 40; solar clock still unavailable. The outgoing old daemon briefly rejected the newly seeded config, then the new daemon started cleanly after the ordered restart.
- Location composition exposed an IPv6 round-trip defect in Datom string projection. `remember` diagnosed separator-bearing strings and initially gave the wrong delimiter recommendation, then inverted its own authority evidence. It corrected both: pinned Protos and newer Vision agree on guillemet strings; the earlier curly-string convention is superseded. No protocol redesign or skill edit was made.
- `hotfix` published Datom 0.25.7 (`9bffbf14`) and Horizon pin `28614f33`; its remote Horizon suite passed eight tests including IPv6 and fixed-location regressions. Further Goldragon legacy structural migration remained in progress. Requested `implement` to carry the codec fix into Chroma/Home as well.
- `remember` located the supported self-host Lojix upgrade route: immutable daemon-free bootstrap with BootOnce/LocalBootstrapV1. Current system activation needs system authority; reboot is explicitly approval-gated by the operating-system skill. Directed `hotfix` to finish source and BuildOnly evidence without bootstrapping or rebooting.
- `remember` resolved the final reported cluster field error: the Prometheus model is Option<String>, so its old single-position struct payload becomes a direct string. Actual structured optional payloads remain structured.
- `implement` completed and activated final Chroma `7fddaabe56c9` and Home `a35ebb50f9c2`, using Datom 0.25.7 `99a9e8c9`. It reported passing separator-string regression, remote sandbox reload-transition witness, and both Home remote checks. Final live Chroma/GammaRelay state converged at Light/2700 K/85%. It released its edit locks and concluded the isolated Home workspace.
- `hotfix` reported successful Goldragon `199f5eb107d9` remote composition/check, including Ouranos’s exact fixed-location values, published external config `74a4ad35f7a7`, and successful Lojix `23f09f28accc` remote tests. Final CriomOS pin and BuildOnly system proof remained in progress.
- Complete-system BuildOnly did not pass. Its materialized Horizon carried the exact San Cristóbal values. Successive evaluation failures exposed non-router module eagerness and stale ownership witnesses; `hotfix` repaired those, aligned source pins, and retained failed terminal evidence. Its earlier Home mismatch diagnosis was superseded by the actual stale-witness finding.
- Final blocking evidence: current Horizon provides typed keyboard, so `useColemak` maps to `keyboard == "Colemak"`; it does not provide semantic CPU vendor, ThinkPad family/RPi3b board class, or lid-switch action policies consumed by legacy CriomOS metal code. `remember` verified the absence and reported it to `hotfix`. No host/model-name heuristics or default policies were invented.
- `hotfix` landed the exact Colemak mapping in CriomOS `acc3feab4d99492276a353a80bbccffac9d6fb58`. Its final additional source refs are Home `caffe9a17cc5830d64f838f7d5cf74d9b2b5bf3a`, Goldragon `199f5eb107d91b9a468cafd09d1b6dc7119df70e`, and Horizon config `74a4ad35f7a709f5f9cfc6eff74f986d9f2c5a0e`. The final complete-system source has no successful BuildOnly witness; component checks do not substitute for it.
- No location/system activation or reboot occurred. Chroma/Home’s independent activation remains the completed runtime outcome. The fixed-location definition is published and component-checked, but the laptop location remains on the prior system generation.
- Both implementation subflows released their locks and concluded their clean isolated jj workspaces. The hotfix workspaces’ inert directories remain because recursive removal was rejected; no further deletion was attempted.
- Next design work: give the required CPU/platform/lid policies an explicit typed home, or explicitly rule their retirement, then finish the system build. Privileged bootstrap and reboot remain later steps, with explicit reboot approval required.
