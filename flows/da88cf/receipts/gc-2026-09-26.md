# nix-gc.timer run — 2026-09-26

Host: ouranos. Run window: 00:00:00 CST start, 00:02:38 CST exit, Result=success
(`ExecMainStartTimestamp`/`ExecMainExitTimestamp` via `systemctl show nix-gc.service`).

Freed line (journalctl): `10707 store paths deleted, 9.3 GiB freed`.

`df -h /`: 916G size, 804G used, 66G avail, 93% use — before was 92% / 71G avail;
avail dropped by ~5G despite the 9.3 GiB freed (other writes landed concurrently;
not investigated further here).

Stale Prometheus system closure
(`/nix/store/71wfmszzhg2lw8ggafky1q3aa7cak4bf-nixos-system-prometheus-...`,
receipts/stale-root-348e7b.md): **gone** — `nix path-info` errors `path ... is
not valid`.

Six protected paths (receipts/gcroot-messenger-clj.md) — all still present
(`nix path-info` succeeds for each): messenger-clj-0.2.5, flow-0.12.2,
message-0.12.0, and all three Qwen3.5-122B-A10B-Q4_K_M shards.
