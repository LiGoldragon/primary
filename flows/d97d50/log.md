# Flow d97d50

Working instruction: remember `4ad49f` at depth 1 and debug ChatGPT Desktop crashing shortly after its window appears. This prompt creates no psyche record.

Remembered: 4ad49f — depth 1. Its log, two reports, five witnesses, and last model response were read; it has no psyche records. That flow restored and deployed the vendor-stock Desktop boundary at Home `c025a681df31d55b9035364c01e2f6c8d7b59c1c` through CriomOS `663fefb70f9962c6751fd0d87b07f8cfef01ef9e`, with Lojix deployment 148 recorded `Completed/Succeeded`. Its live witness directly observed the active vendor-identical ASAR, regular bundled Core, unchanged persistent Codex owner, and a real Electron process tree surviving more than five seconds; all three diagnostic Desktop launches later self-exited. The remembered flow left the exit cause, long-lived stability, new-chat behavior, and same-thread resume explicitly unknown; its last response says `home-273` remained open for a manual GUI witness.

2026-09-03 — Three child flows investigated the runtime crash, declarative boundary, and stock failure mechanisms. The runtime witness directly found six current-package `SIGILL` coredumps, including a fresh reproduction, all in the application Git worker at Node `GetNodeReport`/`TriggerNodeReport`; the faulting instruction is an intentional `ud1` trap. Fresh temporary-profile Wayland, X11, and GPU-disabled launches each survived 15 seconds, and the relevant journals contain no matching i915, GPU, or Wayland fault. The declarative witness found that deployment 149 advanced the surrounding CriomOS/Home selection but reused the exact ChatGPT 26.831 package output from deployment 148; the signed current OpenAI repository instead carries 26.901, without an observed updater-driven exit. Static stock-code inspection maps the only `process.report.getReport()` call to `detect-libc` under `@parcel/watcher`, reached from the Git worker's working-tree watcher, and maps it exactly to the retired historical `SKIP_PROCESS_REPORT` patch target. This makes that call path the leading cause, but the dynamic JavaScript caller and the existing-profile trigger remain unproven; an Electron-as-Node discriminator was inconclusive because the packaged entry point launched the GUI in both test and control runs. No source, live profile, user data, service, or operating-system state was changed.
