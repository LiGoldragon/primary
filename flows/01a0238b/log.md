# Realization flow 01a0238b — Chroma–Emacs theme projection

The psyche approved a public `chroma-emacs` repository, a resident
`chroma-theme-mode`, acknowledged Chroma D-Bus projection, and Home-owned
Ignis generation. The design is complete and tracked by `primary-77d`;
repository creation and implementation have not begun.

Remembered: 01a020ff — depth 1

2026-08-21 — Continued the Emacs-only theme miss investigated by flow
`01a020ff`. Recovered that Chroma's one-shot `emacsclient` concern erased
the transient error, ignored unsuccessful exit status, and had no retry or
postcondition check. The exact rejected Lisp condition remains unknowable.

2026-08-21 — Reacquired the written psyche and inspected the existing
`CriomOS-emacs`, Chroma, and `criomos-home` boundaries. The existing
`CriomOS-emacs` is an unfinished whole-distribution scaffold, Chroma has no
public D-Bus service, and the Home Emacs build supports D-Bus.

2026-08-21 — The psyche ruled that the plugin gets a new public repository,
selected D-Bus, kept Ignis generation in `criomos-home`, and approved the
complete implementation shape recorded in `reports/emacsAdapterDesign.md`.
No implementation source or runtime state changed.

2026-08-22 — Migrated this flow from the legacy session and global psyche
records into the flow protocol. Transcript-proven raw psyche moved to
`vision/emacsPlugin.md`; source observations and provenance moved to
`witnesses/`; the accepted design moved to `reports/`. The superseded
current-flow legacy files were removed after the new artifacts were checked.
