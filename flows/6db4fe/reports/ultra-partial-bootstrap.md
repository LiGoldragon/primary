# Psyche Ultra Low partial bootstrap continuation

The retained Claude process is still running in its original pane/terminal and native UUID. The first guarded bootstrap sent the provisional `/rename` and `/spirit`; both have native transcript events, including a separate `/spirit` expansion. The controller stopped before the second skill because it demanded effort metadata that this transcript did not provide. The bounded transcript has 37 records; its native attachment and assistant message report the exact dated Haiku model, and neither reports effort. The installed `claude agents --json` lists this session/PID/cwd/status without effort. The process argv records `--effort medium` as a requested setting, not a native observation of execution effort. No independent current effort witness was found in the supported local read-only interfaces inspected. An assistant's own statement is not a verifier.

The new explicit `--continue-partial` path requires the prior failed state, 03's bounded partial-observation record and its exact raw-transcript snapshot digest, a new absent receipt path, and the exact Herdr target. Before input, it checks the unchanged title and one `/spirit` command/expansion, no later skill expansion or unrecorded native command, exact model with unknown effort, the same running PID/start time/argv/native session/cwd, Herdr idle state, and isolated job environment. Any changed transcript or process fails closed. It begins at the next skill in the manifest, does not send `/rename` or `/spirit`, and runs the remaining skills and source bundle through the existing native receipt checks. The new output receipt is created exclusively only after those checks succeed. Failed states and the incomplete first attempt remain immutable.

The receipt separates `requested_identity` (dated model, medium effort from the manifest and process argv) from `observed_identity` (dated model, `effort: null` if still absent). In that case `effort_evidence` is `process-argv-requested-only` and readiness is `native-context-model-verified-effort-unobserved-title-pending`. This grade does **not** authorize final-title/ready promotion in the current controller. If a later independent native record supplies exact medium effort, the normal verified grade applies. Owner acceptance of a requested-only effort policy, if desired, is a separate decision; no such policy is inferred here.

The source lane has not sent native input or changed the pane/process. 03 owns any managed runtime continuation after syncing the tested revision, reserving a fresh output receipt path, and rerunning the exact preflight. If that preflight fails or another record appears, stop and diagnose; do not replay empty-bootstrap or the first skill. A continuation that fails mid-turn also needs a new cursor diagnosis, not blind command replay.

Source: `tools/claude-native-seat-refresh.py`; fixture: `tools/claude-native-seat-refresh.test.py`. The local fixture proves first-skill skip, missing-effort receipt grading, stale snapshot refusal, and exclusive output. A passive live preflight against the retained session passed before this source publication; it is not a receipt for the remaining native turns. Remote Nix fixture result and source revision are recorded after completion.

Verification: `python3 tools/claude-native-seat-refresh.test.py` passed. The configured remote-only Nix check `nix build --no-link --print-out-paths .#checks.x86_64-linux.native-seat-fixtures --max-jobs 0 --option fallback false` passed on Prometheus, output `/nix/store/0glpfmnx18g11zapvws89n6nyja41vr3-primary-native-seat-fixtures`. A second live read-only preflight passed after the final guard changes. This is source/initial-state proof; the remaining skill turns and requested-only effort acceptance are not proven.
