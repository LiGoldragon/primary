# FlowMessage operational skill

**Owner and boundary:** Field Astra flow `6db4fe` owns only the authored Curriculum skill and generated Codex/Claude projections. Mind Medium `2c61af` leads the final FlowMessage API and disposable/negative acceptance evidence. Retained Flow/Message component writers hold their existing reserved checkouts. This report does not authorize a competing component edit or release any protected lock.

**Current grade:** contract outline only. The inspected Mind report `flows/2c61af/reports/flow-session-lock-slice.md` explicitly marks its Flow hold design as unimplemented and untested. The reserved Flow checkout currently has an uncommitted `store.rs` change; the reserved Message checkout has no new working-copy change. `flows/2c61af/reports/message-recipient-selection.md` finds that the inspected Message baseline accepts a vector of targets, without a dedicated exactly-one-recipient semantic codec. No final FlowMessage typed API, CLI signature, installed service, end-to-end receipt, or release version has been supplied to the skillwriter yet. Do not use a draft skill to assert deployment.

**Chosen skill surface:** `Curriculum/skills/flow-message.md` was committed temporarily as a guarded source-only draft at `b7fcf4f5`, but the canonical generator scans every `skills/*.md`. The scanned source path was removed at `2c15ef56` and the exact draft is retained below until API/test receipts arrive. A disposable canonical Generate/Check returned `Generated.{ 66 21 }` and `Checked.{ 66 21 }`, asserted both universal role passages in a generated role packet, and asserted FlowMessage absent from the generated skill catalog. Both Curriculum commits are remote-verified. No main-flow dependency, native profile entry, primary projection, or runtime installation is claimed. The skill remains distinct from `messaging`, `flow-communication`, and `testing-datom-messaging`; a generated file alone would not prove native expansion.

**Required semantics to bind, without inventing signatures:** typed single-recipient admission and delivery; Acquire's durable commit is the admission linearization point. A pre-BeginRefresh permit may finish its admitted attempt; BeginRefresh gates all later Acquire calls even while that permit remains active. There is no second consume boundary at transport. ReleaseConfirmed after matching `CONFIRMED` evidence performs an exact compare-and-swap that clears the permit only; it does not clear `RefreshHeld`. Ambiguous transport, deadline, or cancellation retain the permit, and Ready/Reattach refuse until reconciliation. Successor-ready compare-and-swap then precedes queued delivery. Transport success does not imply target Read. The final skill must cite actual API names, source revision, and observed tests supplied by Mind, distinguishing callable current behavior from design intent.

**Authored composition boundary:** current Curriculum has `roles.datom` and `skills/*.md`, not the older manifest files described by `reports/CurriculumManifestMap-2026-08-21.md`. The typed role record supports universal modules and per-harness-surface insertions. Current `curriculum-deploy/src/roles.rs::packet` has no aspect/power/profile selector or core-versus-extended programming selector, so this writer has not added dormant modules or invented model tiers. Accepted launch profiles are current primary `flows/.../profile.json` records and are owned by Field Astra successor `03e825` under separate launcher/projection locks. Mind Low's consumer adapter contract must state the typed selector and expansion witness before authored aspect/power composition can be connected without a false install claim. Lock #3825 reserves `Curriculum/roles.datom` and `Curriculum/skills/flow-message.md` only.

`roles.datom` now contains only the universal `spirit-role` and `intent-role` modules, using short exact passages from accepted existing sources, and includes both in its universal vector. This source is at Curriculum `10b6875d`, remote-verified, but primary's generated role packets still need the successor's canonical projection/install receipt. Proposed conditional IDs (`aspect-psyche|mind|field`, `power-high|medium|low|ultra-low`, `programming-core|extended`) were submitted to Mind Medium and Mind Low as proposals only; neither acceptance nor runtime selector is observed. Direct living Core programming words in `flows/05c604/vision/layers.md` are “what is good, what is right, what is wrong,” with less ongoing detail. `flows/108ab0/vision/operational-coreAndExtendedVision.md` distinguishes broad, more certain Core Vision from less reviewed Extended Vision, but does not establish that programming has the same split. Extended programming content remains unresolved; no programming skill list or default selector was invented.

**Corrected producer map:** retained `signal-flow/night-messaging-0ab019` has authored `ethos/signal.ethos`; `src/generated/signal.rs` is build.rs output, `tests/contract.rs` is its contract test, and `src/lib.rs` only reexports. This writer touches none of those. The Flow/Message signal schemas and component tests remain with their reserved owners.

The signal-message producer at `5f85868a` has a `SingleFlowRecipient` wrapper around `FlowIdentifier`, but no nonempty semantic codec at that boundary. Until the owned validation extension lands, the consumer must reject an empty recipient before Flow Acquire. This producer source grade is separate from a final tested API, consumer build, or installation.

**Native receipt boundary:** `flows/03e825/reports/native-launcher-receipt-interface.md` records the supported `native-seat-launch.mjs` CLI, not an importable `buildPlan`/`launch`/`finalizeNativeTitle` library API. Its plan is JSON v2; receipt v3 moves from created/adopting to pending exact native thread/turn/generation, requested model/effort, and prompt/source/skill digests. Thread or rollout verification reaches `verified/native-context-verified-title-pending`; `--activate` is a separate activation-started grade. After the flow claims its ID, `--finalize-title` verifies the claim marker, exact native identity and harness, and title readback, then reaches `ready/native-ready`. HM binding and Message acceptance are separate. Historical verified receipts lack ready-title fields. The future FlowMessage skill/profile install must use this CLI and witness the skill's actual expanded native record; neither a generated catalog file nor a verified first-turn receipt alone establishes the full route/delivery grade.

**Store-lifetime rollout gate:** the retained Flow writer's current contract is a nonblocking exclusive guard for the canonical store identity, acquired before `Engine::open` and held until after engine drop. `StoreAlreadyOpen` contention, lock or I/O failure, and unsupported path aliases fail closed. The persistent sibling lock is never unlinked or replaced; losing the process releases the guard, not the lockfile path. It covers cooperative writers only: an old daemon or raw Engine bypass is outside this proof. Before migration or switching runtimes, Field must quiesce the old runtime and witness no bypass writer, preserving the lockfile for rollback. Required remote Nix cases include same- and two-process contention, reopen after drop/termination, persisted gate/permit, path aliases, engine-open-failure drop, and no deletion race. `rustfmt` alone is not compile or test evidence. These are rollout requirements, not a passed test claim by this writer.

Source-grounded SEMA context does not relax that gate: the inspected `sema-engine` 0.16 → `sema` 0.1.1 → `redb` 4.3 path defaults to one exclusive writer, and redb source says independently opening the same physical database, including in one process, conflicts with `DatabaseAlreadyOpen`. That claim has no behavioral two-opener test here. `commit_atomic` holds a write lock across preflight and one SEMA write, but has no expected-value compare-and-swap if future multiprocess access is enabled. Keep the sibling-lock rule until an exact two-opener proof and a Root/retained-writer redundancy decision; none is claimed in this report.

**Pending acceptance:** final API/schema location and version, explicit owner acceptance, actual component tests including disposable end-to-end and negative cases, generated projection check, native catalog/skill expansion receipt if a new seat is launched, scoped source and primary commits, remote verification, and lock release. No production recipient is contacted merely to test this skill.

## Exact guarded draft, not installed

```markdown
---
description: A flow is using an installed typed FlowMessage admission and delivery path.
dependencies: [datom, messaging, refresh]
---

Use this path only after the selected Flow and Message components, generated signal contracts, and the exact target binding have an installed, witnessed API. Resolve one explicit nonempty recipient and reject an empty recipient before Acquire; a vector-shaped transport field does not authorize broadcast.

Acquire is the admission linearization point. An accepted acquire commits one permit against the current exact Flow binding. A permit committed before BeginRefresh may finish its admitted delivery attempt; BeginRefresh closes new admission immediately, including when that earlier permit remains active. There is no second consume or admission boundary at transport.

The sender keeps the permit through delivery and waits for the matching typed CONFIRMED evidence. ReleaseConfirmed must compare the same current Flow identity, binding, nonce, and generation; it clears that permit only, not RefreshHeld. Transport acceptance, terminal presentation, and a sender timeout are not CONFIRMED or Read. An ambiguous transport outcome, deadline, or cancellation retains the durable permit and requires reconciliation; none may call a Flow unlock shortcut.

Ready or reattach while a permit is ambiguous must refuse. After reconciliation, a successor-ready compare-and-swap must witness the exact current binding and generation before queued delivery proceeds. A restart, changed pane, or display name alone does not substitute for this binding proof. Report submitted, transported, presented, read, and completed at their separate observed grades.

Use the component's final typed request and response variants, not an improvised Datom or string wrapper. The exact callable names, release response, reconciliation operation, and installed version must be read from the current generated signal contract and API before making a command. If that contract or deployment receipt is unavailable, hold the operation and report the missing interface rather than guessing.

Before switching a Flow store/runtime, Field quiesces the old runtime, proves no bypass writer remains, and retains the persistent sibling lockfile for rollback. A cooperative store-open guard must be held before engine open through after engine drop; contention and uncertain lock/path state refuse the switch. Do not treat a formatting pass or a native ready title as this rollout proof.
```
