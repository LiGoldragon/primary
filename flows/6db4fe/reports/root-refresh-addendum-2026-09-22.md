# Root Field wake-up round addendum — 2026-09-22

The living requests **one wake-up round** for OpenAI contexts over **100,000** and Claude contexts over **200,000**. Each selected owner writes its own addendum into its lane handoff. This is root `6db4fe`'s coordination addendum, not a fresh native launch or a claim that all seats need replacement. The root-reported 14:29 UTC last-request context proxy for `6db4fe` was **114,638** tokens; it is **not** proof of resident context. Confirm actual native context and the selected owner's declaration at launch.

Field `03e825` owns the existing staged native refresh: accepted model and effort, the current profile, expanded native context, exact identity and title, HM/Herdr binding, and explicit work acceptance. Do not invent another role slot, duplicate launcher or automatic predecessor retirement. Root `6db4fe` retains Field network/VM coordination and delegates bounded work. Child work does **not** transfer automatically: keep the `6db4fe` route and relay later worker results to `03e825` until the successor explicitly accepts them or each worker completes.

The sole delegated Field network/guest worker is `claude_usage_research` through root `6db4fe`. Current Prometheus recovery is unresolved. The [corrected network report](prometheus-builder-recovery-2026-09-22.md) at `1625fe7a` and [worker continuity addendum](field-worker-continuity-2026-09-22.md) at `fb52913a` are **dated evidence**, not fresh peer health. The only network configuration change in this outage incident was `never-default=yes` and `ignore-auto-dns=yes` for IPv4/IPv6 on the existing saved Wi-Fi profile; rollback is documented. Its association did not recover. The proposed twice-per-hour AP observation was **not executed**. No cause has been proved.

Existing guest reservation **#4373** remains Field `6db4fe`'s sole `microvm@vm-testing` controller scope. Current **Prometheus** guest state is unknown while the peer route is unavailable; an earlier Ouranos-local unit read was invalid and discarded. The [guest preflight fixture](../flow-message-existing-guest/README.md) at `c6c2b50c` has **five passing local fake-transport tests** and a wrong-host guard; it has no remote compilation, boot, model login or Flow/Message pair proof. Do not start even a base guest for the Flow/Message trial before one coherent generated-and-tested Flow/Message/Signal graph plus Field parity receipt. Do not use a second controller, second VM or local build fallback.

Mind Medium and retained component owner `f72ab7` have source-graph work awaiting remote validation. No new Flow/Message production pair is accepted or deployed by this addendum. The Codex worker owns Curriculum/testing continuity under its existing **#3825** scope; do not reassign that work to Mind Low. Its [continuity report](curriculum-testing-continuity-2026-09-22.md) is on Primary branch `field/curriculum-testing-continuity-6db4fe` at `f55f0118` (worker-reported). Authored tester source and projection checks are distinct from a missing installed Primary testing-worker paragraph or any native tester expansion receipt; a fixture is not a ready Ultra seat. Psyche High and Psyche Low/Sonnet are commissioned for a whole-state flashbook that distinguishes authored source, compiled artifacts, installed runtime and end-to-end proof. Network reliability and durable USB integration remain open with Sol coordination. Preserve the pending large Field visual report as **historical data**, not a current census.

Relay this addendum and each owner's separately published evidence through the established root-to-`03e825` handoff. Use short visible Flow IDs and full identities only in exact machine integrity receipts. Do not wake seats merely to get a status acknowledgement. A pointer or delivery of this file alone is **not** successor acceptance.
