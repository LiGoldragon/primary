# Prometheus builder reachability: current bounded recovery attempt

**Observation:** 2026-09-22 11:26–11:29 UTC (05:26–05:29 local), initiated on Ouranos. Prometheus and its configured Nix builder are **not reachable by the witnessed paths**. This does not identify a hardware, power, peer OS, radio, cable, or Ouranos driver fault. There was no build, VM boot, service restart, reboot, or firewall change.

| Path / layer | Current evidence | Limit |
| --- | --- | --- |
| Ouranos upstream | Built-in Ethernet `enp0s31f6` remains the sole default route, via `192.168.1.1`. | Ouranos Internet does not prove Prometheus Internet. |
| Ouranos USB share | Exact ASIX interface `enp0s20f0u1c2` is `UP,LOWER_UP`, `10.44.0.1/24`, with the existing NM shared profile and DNS/DHCP listeners. Current RX stayed unchanged across a three-second sample while TX rose; its lease file remains empty and the former Prometheus `.148` neighbor is `FAILED`. | Cumulative RX is now 1.74M packets, so the older *zero cumulative RX* description is stale. The old address might no longer be assigned. Link indication and active DHCP service do not prove a current peer, nor locate the fault. |
| Wi-Fi management | One fresh disconnected-client scan listed the saved `goldragon.criome` AP at signal 50–59, channel 6, WPA3. The saved profile uses SAE, with no fixed BSSID, interface or cloned MAC; radio is unblocked and regulatory channel permits operation. A single explicit saved-profile activation timed out. NM recorded association taking too long; kernel logged three authentication transmissions without a reply. No Wi-Fi IP or management route was acquired. | A beacon and authentication timeout do not prove the AP host's state or that a secret is wrong. NM auto-retry logs show other attempts; none were initiated as an additional manual repair. No AP scan/reset or key read occurred. |
| Yggdrasil / configured builder | Ouranos's daemon and tunnel are active. Its last logged Prometheus peer session disconnected over Wi-Fi link-local on Sep 21 at 16:16 local; no current peer session is demonstrated. The configured builder remains `ssh-ng://nix-ssh@prometheus.goldragon.criome`. Name resolution selects its known Ygg address and `yggTun`; **one** current TCP/22 connect timed out after 7 seconds at 11:27:53 UTC. | TCP did not reach SSH, so no host-key, configured Nix identity or store handshake could run. An active local daemon/tunnel alone proves no peer reachability. |

The prior [builder receipt](configured-builder-transport-2026-09-21.md) was at 01:20 UTC and is historical; this report gives the new current check. `/etc/nix/machines` still specifies only Prometheus with a root-owned SSH key. A direct `li` Nix-store probe would substitute the wrong identity; no such probe was made.

## Reversible local action

Before the one saved-profile reconnect, I changed only the existing `goldragon.criome` NM profile: `ipv4.never-default=yes`, `ipv6.never-default=yes`, `ipv4.ignore-auto-dns=yes`, and `ipv6.ignore-auto-dns=yes`. Their prestate was `no` for all four. This prevents a later successful management association from replacing Ouranos's wired default or DNS. After the timed-out activation, the wired default remained unchanged. I retained these safety settings for the existing autoconnect profile; exact rollback is `nmcli con mod goldragon.criome ipv4.never-default no ipv4.ignore-auto-dns no ipv6.never-default no ipv6.ignore-auto-dns no`. No connection, secret, or interface was deleted or recreated.

## Next discriminating access

The next useful input is a **current Prometheus-side console or independently verified management path**, not another identical Ouranos SSH/association retry. A peer-side operator can read `hostname`, `ip -br link`, `ip -br addr`, `ip route`, `networkctl status eno1`, bridge/AP service state and bounded hostapd/networkd/kernel logs, and inspect whether its Ethernet uplink receives a DHCP offer and its AP receives authentication frames. This distinguishes an absent peer interface/DHCP path from an AP authentication failure; it still requires observed outputs before changing either service. Existing Zeus reachability mentioned by the living is not an established route *from Ouranos*; no invented jump address was tried. If peer access confirms a specific local NM share defect, this worker can make a scoped reversible repair and retest the configured builder identity once.

The Flow/Message graph's Cargo dependency and generated-pair gates are separate. The existing `microvm@vm-testing` reservation remains stopped and unchanged.
