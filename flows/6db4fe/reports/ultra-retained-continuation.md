# Psyche Ultra retained pre-start continuation

The second authorized Ultra attempt stopped after creating one Herdr pane and reserving one native UUID, before `agent start`. Its immutable evidence is `flows/03e825/psyche-ultra-native-attempt2/state.json` and `reports/psyche-ultra-retry.md`. The retained pane is `wD:p8`, terminal `term_65c0377a0776b56`, UUID `f61ea06b-b5ba-45be-8313-0afe7d84fd03`. A read-only `pane process-info` still found only its zsh shell. The original state is not a launch receipt or a Flow identity.

`tools/native-batch-refresh.mjs::prepareClaudePaneEnvironment` previously sent `herdr pane run` through a wrapper that unconditionally parsed stdout as JSON. The failed state has `Unexpected end of JSON input`, but no stack or captured raw command response, so the original live failure is consistent with this source boundary rather than uniquely proven by the state. The repaired source treats `pane run` as an exit-status action and still requires the structured `pane wait-output` marker. A disposable fake-Herdr fixture returned successful empty stdout for `pane run`; the previous wrapper would fail there, while the repaired worker reached the fixture's deliberately failing `agent start`. No live Claude input was used to prove the fix.

The new `continue-retained --failed-state OLD --manifest CURRENT --state NEW --expected-current-model MODEL` path is restricted to a single failed pre-start Claude attempt with that exact error, an existing pane/terminal/native UUID, and no receipt. It never calls `tab create` or allocates another UUID. It creates a separate continuation state and receipts directory while preserving the failed state bytes. The caller must state the exact accepted current model; the current profile, manifest, and that argument must agree. The former alias and the newly accepted precise Haiku model are recorded separately in the new state's `profileTransition` with their profile digests; only a same-family model correction is permitted, while identity, effort, fresh status, role, and route must remain consistent. Immediately before touching the pane, its worker checks the old state digest, exact pane/terminal/workspace/cwd, one shell process, no Herdr agent on the name/pane/terminal, no Claude agent/process/transcript for the UUID, and an empty non-symlink native job directory. It re-applies the isolated environment command to the same shell and then follows the normal native-start and skill receipt gates. Any changed or ambiguous condition fails closed. Direct `worker --state OLD` now rejects a failed state before resource creation.

For the authorized executor, use the current canonical single-seat manifest after Sol updates the Ultra profile to exclude hash-bearing operational reports and select the living's accepted precise `claude-haiku-4-5-20251001` model. Existing Psyche Low remains untouched; the earlier Low-first launch plan is not a new continuation prerequisite. From a Herdr-managed pane, first run `validate --manifest CURRENT`; verify the rendered `role_prompt` and `custom_system_prompt` are free of long revision/digest noise. Then run `continue-retained --failed-state flows/03e825/psyche-ultra-native-attempt2/state.json --manifest CURRENT --state NEW_ABSOLUTE_STATE_PATH --expected-current-model claude-haiku-4-5-20251001`, where `NEW_ABSOLUTE_STATE_PATH` does not exist and is under a separately reserved attempt directory. Read that new state and its receipts; native readiness, own Flow claim, canonical title, exact route, and acceptance remain separate gates. Do not run `start` or `worker` against the failed state. This report is source/fixture evidence only; Field `03e825` owns any live continuation.

The local fixture exercises empty stdout, preservation of pane and UUID, refusal of a replayed failed worker, rejection of a different failure, and rejection when the native UUID is already in Claude's agent list. The final coherent repository check `nix build --no-link --print-out-paths .#checks.x86_64-linux.native-seat-fixtures --max-jobs 0 --option fallback false` succeeded remotely on Prometheus at derivation `/nix/store/6n0zkxprzqy4vsvs2gfizp6fw2ls6rpk-primary-native-seat-fixtures.drv`, output `/nix/store/cvwldv99hws3piliz83qxvn7h5xc6bsl-primary-native-seat-fixtures`. Its build log reports all three native seat fixture groups passed. The final source revision is the scoped push carrying this report; no runtime continuation is claimed here.
