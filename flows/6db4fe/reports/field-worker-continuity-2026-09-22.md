# Field worker continuity for the Sep 22 refresh

Prepared **2026-09-22 14:27 UTC / 08:27 local** from existing receipts. This addendum performs **no fresh network, guest or production probe**. It is worker evidence for Field `6db4fe`; it is not acceptance or transfer by successor `03e825`.

## Network and builder

The authoritative current attempt is [Prometheus recovery report](prometheus-builder-recovery-2026-09-22.md) at `1625fe7a`, incorporating Psyche High's C1–C9 audit. Its measurements were at **11:26–11:29 UTC**, already about three hours old when this addendum was written. Ouranos kept its wired default route. Its USB share had carrier, a DNS/DHCP listener, no current lease and a flat RX delta during the short sample. A fresh scan saw the saved Prometheus AP again after roughly eleven hours without observation; one bounded saved-profile reconnect stopped inside 802.11 authentication before IP. The configured Prometheus Yggdrasil TCP/22 path timed out once. **No root cause, Prometheus Internet, usable builder store, or recovery was proved.** Do not promote an old timeout or an active local Ygg daemon into a current peer-health claim.

The one actual configuration change in this outage incident was to the existing saved Wi-Fi profile: IPv4/IPv6 `never-default=yes` and `ignore-auto-dns=yes`. It protects Ouranos's wired default/DNS if that profile later associates, but it did **not** repair association. Prestate, exact rollback and later route-interpretation effect are in the recovery report. The report proposes two non-disruptive AP-presence scans over one hour, each labelled local and UTC; this series was **not executed**, no timer was installed and no further network retry was made. A Prometheus-side console or independently verified management witness remains the causal access dependency; the previous console question has already been asked and should not be duplicated.

## Sole existing guest and test boundary

Orchestrate reservation **#4373** holds `/var/lib/microvms/vm-testing` for Field `6db4fe`; this worker has delegated implementation responsibility through `6db4fe`. The only permitted guest controller is the existing Prometheus `microvm@vm-testing.service`. **Current Prometheus guest unit/image/process state is unknown** because the peer route was not reachable in the last attempt. An earlier unit/image read accidentally made on **Ouranos** is invalid for Prometheus readiness and was discarded. The older Prometheus remote readback in [the VM trial handoff](../../9ddcbc/reports/flow-message-vm-trial-2026-09-21.md) is dated evidence, not a current-state claim. No guest start/stop, second VM or model login was performed by this worker.

The [existing-guest fixture](../flow-message-existing-guest/README.md) at `c6c2b50c` contains a strict known-host-key, exact-host preflight that rejects a wrong/unreachable host before any unit/image/process read. Its **five focused local tests passed**. The example manifest deliberately leaves all six immutable graph revisions and three generated-check/Field-parity receipts unset; its plan reports the trial blocked and cannot authorize or start it. The typed one-recipient CLI→Nexus→recipient receipt→Flow confirmed-release cases, Raw versus FlowLocked distinction, and identity/ambiguity negatives are a **source test contract**, not executed parity. A coherent generated-and-tested Flow/Message/Signal/meta graph plus accepted Field parity remains the no-boot gate. Native Luna process-only guest login is only an untested interface proposal; Haiku has no accepted process-only login path under the stated requirement. SSH access to a guest would not establish either model login.

## Production parity and continuing ownership

At the **Sep 21** [runtime audit](session-inventory-runtime-audit.md), production Flow was a local binary with no witnessed immutable build revision, and Message was an installed 0.12.0 store binary that differed from the delivery candidate. That was a dated read, **not a Sep 22 process readback**. No coherent deployed Flow/Message pair, generated signal parity, successful remote gate, or one-recipient production delivery receipt has been provided to this worker. Sol `753e69` and the retained component owners have newer source integration work; this addendum does **not** classify their current branches as a tested pair or repin consumers. Retained Field `9ddcbc` owns the consumer rollout under its existing reservation. No production unit, binary, manifest, socket, credential or service was changed here.

The remaining Field work is: restore a verified Prometheus management/builder route from peer-side evidence; obtain the component owners' immutable generated-and-tested graph and accepted Field parity fixture; inspect the **actual Prometheus** guest preimage; resolve process-only native login before model subjects; and only then run the single existing guest trial with guest-only stores/sockets and exact rollback. Production deployment has a separate owner and acceptance sequence. Field `6db4fe` must explicitly relay this worker's results and ownership obligations to `03e825` during refresh; a collaboration handle, reservation or live process does **not** transfer automatically. There is no witnessed new-owner acceptance in this worker report. Keep `6db4fe` and protected retained routes available until that relay and the parent lifecycle gate are complete.

## Separate flashbook evidence for Psyche

The home network beacon was seen again in the morning after it was absent from the previous evening's scan; its return time within the roughly eleven-hour unwatched gap is unknown. A single join attempt sent authentication frames without an observed answer and stopped before an address was assigned. The cable-share sample still showed no new incoming frames or lease; the overlay service being alive locally did not prove the other machine was reachable. The only change was to make the laptop's saved Wi-Fi entry unable to take over its working wired Internet or DNS if it connects later; that was protection, **not a repair**. The dated evidence and limits belong to the corrected network report; this paragraph is a separable content seed, not a published flashbook or instruction to wake a model.
