# Field refresh handoff — presentation package

## Presentation state

The activity aggregates are in `data/activity/`. They are metadata-only, seven-day local-history measurements. Codex `input_tokens` includes cached input and must not be added to the cached column. Claude input, cache-read, cache-creation, and output are separate fields. The explicit Codex event-active metric is a capped event-duration proxy, never session span, compute time, or person-hours. The installed Flow/Message runtime pair and source parity remain unproven; no production readiness follows from the visual data.

## Current priorities

1. The living directed all twelve main flows to delegate bounded inspection, testing, and implementation through subagent calls; ordinary routine checks should default to Luna. The authored main-flow correction is pending projection installation and staged refresh receipts.
2. The current Field Astra root refresh remains gated on the new skill installation/source receipt, immutable focused handoff, validated profile/native context, identity, exact HM binding, acceptance, and worker relay. The retained root route remains in place until then.
3. Flow/Message VM candidate work and the visible Raw versus FlowLocked contract demonstration are current work. Runtime/source pair parity, lifecycle, sender attestation, and rollback gates remain open.

## Active ownership and locks

- Presentation/data: lock 4285, Field world worktree.
- Verbatim native-history capture: lock 4328, this capture file.
- Curriculum authored `roles.datom` and `skills/flow-message.md`: lock 3825; this handoff does not modify them.
- Field 03 owns lifecycle/projection execution; Field Sol owns accepted profile/lean source handoff; existing Terra and Codex workers own their disjoint source lanes.

## Direct-conversation boundary

The active direct conversation contains the newer subagent-only main-flow correction. It is not quoted as a native-history record here because no exact native record was independently verified during this capture. The exact verified native record is in `../../vision/livingHistoryCapture20260921.md`.
