# Current root refresh handoff

This is a current checkpoint for a future explicit handoff. It does not launch, edit a profile, withdraw a route, transfer a collaborator, deploy, or authorize a VM boot.

## Retained root and continuity

The root `6db4fe` route remains the worker-continuity courier until a successor explicitly accepts both the handoff and relay mechanism. Existing collaborators are not transferred by implication. Terra results continue through the root to Field `03e825`; receipt of this report is not a read or acceptance witness.

## Network and USB gateway

Terra owns CriomOS/Ouranos runtime and source work exclusively. Network evidence remains progressive record `4428` and read-only report `716cccd0`; no change is claimed here.

Current producer/source milestones are Horizon `b45d6ad4`, Signal `ff023945`, and Meta `631d832e` on main. Lojix remains at `c4bba4fa` with old pins and retains Sol lanes `4045`, `4240`, `4251`, `4072`, and `4062`. Goldragon is `a911515c`; CriomOS consumer is branch-only `9842`. No activation follows from these revisions. The USB audit is read-only and complete; its exact module/migration/hotplug cases remain with the retained worker and are relayed through the root.

## Flow, Message, and VM

The sole VM controller is Terra under lock `4373`, using existing `microvm@vm-testing`. It is stopped; its runner exists and its root image is absent. Report `c3256baf` specifies rollback as stopping the same unit and retaining state. No base boot is permitted until a coherent generated-and-tested pair and Field parity fixture exist.

The login interface report is `bb5c3be1`: Codex ephemeral process support is source evidence only; SSH host access does not prove guest host key, sshd, or account login. Claude process-only login remains unresolved, with no accepted temporary-filesystem exception.

Flow dispatch source `6d23` has no usable result: Cargo lock mismatch and DNS-derived Git fetch remain graph-owner `f72` work. The Mentci report `c742` finds the configured cache/builder unreachable; it does not authorize VM work.

## Testing-role and refresh gates

Curriculum tester source `c9c39549` is authored but not installed. Low evidence `a818` proves only context shape; it has no alias/skill-ID native injection. Completion remains: Low consumer wiring, Medium selector, and Field `03e825` installation plus native Luna receipt. Root retains lock `3825` for `roles.datom` and `skills/flow-message.md`; no change is made here.

A root refresh still requires explicit successor acceptance, focused handoff/source validation, native context and model/effort proof, own identity, exact HM binding, worker relay, and the testing-role gates above.
