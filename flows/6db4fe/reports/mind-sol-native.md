# Fresh Mind Sol native seat — 2026-09-21

The fresh Mind Medium seat is live on Codex `gpt-5.6-sol` at medium effort. Its Flow ID is `2c61af`; its exact native thread is `01a0c492-7939-7081-82d7-a512c61af5e0`. It is attached to Herdr session `messaging-build`, pane `wM:p6`, terminal `term_65bffdf50723a52`, agent `mind-sol`. The pane is idle and interactively ready. HM has the same exact Flow, session, pane, terminal, and native-thread binding. A prompt sent through HM reached the target, which replied `HM_READY 2c61af-01a0c492`. The target then explicitly accepted its fresh Mind Medium role and context/usage sensing as open work under current Mind Astra owner `4b0f60`, without claiming an ownership transfer. Mind Astra's specific acceptance of this new seat remains pending.

The native `thread/start` returned the thread ID before the target's first turn. `tools/native-seat-launch.mjs` wrote `flows/6db4fe/mind-sol-native/receipts/mind-sol.json` before `turn/start`. That first turn submitted 17 native `UserInput` skill records, including `main-flow`, plus the lean, source-labelled prompt from `flows/6db4fe/mind-sol-native/profile.json`. The exact native rollout `~/.codex/sessions/2026/09/21/rollout-2026-09-21T17-25-28-01a0c492-7939-7081-82d7-a512c61af5e0.jsonl` shows model `gpt-5.6-sol`, effort `medium`, the matching first-turn ID and prompt digest, and separate expanded `response_item` bodies for **all 17 skill sources**. The target's first response reported context present and used no tools. `verifyRolloutReceipt` now checks each expanded body as well as the typed input record; its matching fixture and absent-expansion rejection pass. The model's first response said it could not itself verify structured skill injection, which is a limitation of self-report, not of the rollout witness.

The app-server's `thread/read` returned the turn but omitted the expanded `turn_context` needed by the strict read verifier, so the receipt grade comes from the exact native rollout. The launcher initially read the rollout after the first turn and returned `native-full-bundle-rollout-witnessed`; subsequent activation and HM turns append to that same file. The receipt records the observed file digest at verification time; it is not a stable final-file digest while the thread remains live. The launcher fixture passed after adding app-server creation gated to this authorized fresh Mind Sol profile, a receipt-before-turn assertion, and expanded-source checks. `tools/native-batch-refresh.mjs validate` passed for the profile manifest before launch.

Activation claimed `2c61af`, obtained an `agent-intercom.intercom_whoami` witness, and spawned one acknowledgement subagent because the launcher activation text requested it. The child native rollout `~/.codex/sessions/2026/09/21/rollout-2026-09-21T17-26-36-01a0c493-8190-7d50-8082-70612544fce0.jsonl` reports `gpt-5.6-terra` medium, default role, no tool calls. That violated the temporary AGENTS.md constraint, which permits Terra subagents only for actual implementation. The launcher activation text was corrected to request only a harmless **direct** structured tool witness and explicitly no subagent; this seat was not restarted to retest it. The target was sent the constraint correction. The main Mind Sol seat's native identity and context receipts are separate from that child.

The target thread was attached with supported `codex resume 01a0c492-7939-7081-82d7-a512c61af5e0 --remote unix:///home/li/.codex/app-server-control/app-server-control.sock` in the new Herdr tab. An exact app-server `thread/read` checked ID, rollout path, and model immediately before `thread/name/set`; readback and `thread/name/updated` both confirmed the user-facing name `Mind Sol`. Herdr and HM retain their functional `mind-sol` name. No existing seat was retired, rebound, or renamed by this launch.

An earlier empty CLI-first test in owned tab `wM:t5` exposed native UUID `01a0c48c-e857-7df3-b86c-6cc6d11d4c25`, but `thread/resume` returned `no rollout found`; no model turn or Flow identity was created. That exact test tab was closed. This established why a fresh CLI session cannot be adopted by app-server `thread/read` before persistence; the supported app-server-first path above was used instead.

## Sources and checks

- Authored role direction: `Vision/modelRoles.md`; `flows/b81560/vision/operational-mindMediumIsSol.md`; `flows/9e7ea5/reports/refresh-handoff.md`; `flows/1cb440/reports/refresh-handoff.md` (receipt-first procedure); `flows/1cb440/reports/mind-astra-of-0ab019-native-readiness.md` (same-thread remote attach pattern).
- Native implementation: `tools/native-seat-launch.mjs`, `tools/native-seat-launch.test.mjs`, `tools/native-batch-refresh.mjs`; installed `codex app-server generate-ts` protocol for `ThreadStartParams`, `TurnStartParams`, and typed `UserInput`; installed `codex resume --help` for `--remote`.
- Live receipts: exact native rollout above; `flows/6db4fe/mind-sol-native/receipts/mind-sol.json`; Herdr `agent get mind-sol` and `pane get wM:p6`; HM registry `~/.local/state/hacky-messenger/2c61af.json`; target-side pane marker and role-acceptance response; app-server name readback and `thread/name/updated` (temporary exact-name receipt `/tmp/mind-sol-name-receipt.json`).
- Tests: `node tools/native-seat-launch.test.mjs` passed; `node tools/native-batch-refresh.test.mjs` and manifest `validate` passed before launch. Owner acceptance remains a distinct open readiness gate.
