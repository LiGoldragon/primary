# Ultra retained-pane marker repair

The managed continuation recorded at `flows/03e825/psyche-ultra-native-continuation/state.json` failed before `agent start` because `herdr pane wait-output` returned a timeout for the environment marker. The retained `wD:p8` pane still contains only its zsh shell and the same terminal/native UUID. Its visible terminal output shows the completed old UUID-only marker twice. This proves the shell command ran at least once, but neither old occurrence is a fresh witness for another continuation. The first and second failed attempt states and the failed continuation state remain separate and unchanged.

Passive reproduction after the failure: `wait-output` with the exact old marker, `--source recent --lines 30`, timed out. `--source recent-unwrapped --lines 30` also timed out in this audit; with 50 lines it returned an exact match, as Sol independently observed. `--source visible --lines 30` immediately returned the exact matched line and pane ID. The source/line window therefore changes the result for already present output. The installed Herdr implementation's deeper buffer behavior is not established here. The repair selects `visible` with a bounded 50-line window, rather than increasing the timeout as a substitute for a source fix.

Before each pane command, the repaired launcher generates a new 24-hex nonce and records its complete expected marker in the new attempt state. It prints the native UUID and nonce as separate shell `printf` arguments, so an echoed command does not contain the complete marker. It requires `wait-output` to return the exact pane ID and an exactly equal `matched_line`. Historical UUID-only markers cannot satisfy that check. `pane run` remains an exit-status action; structured `tab create`, `pane get`, `process-info`, `agent start`, and `wait-output` still require JSON. The continuation boundary accepts only the immutable original pre-start parse failure or the exact following marker-timeout failure referencing that original state. Any other failure, changed ancestor, existing native process/session/transcript, nonempty job directory, or pane/terminal mismatch is refused before input.

The fixture checks the command/marker separation, source `visible`, stale marker refusal before `agent start`, exact same-resource continuation from the two-state failure chain, and rejection of different failure or existing native session. The local fixture passed. The final coherent remote Nix command `nix build --no-link --print-out-paths .#checks.x86_64-linux.native-seat-fixtures --max-jobs 0 --option fallback false` succeeded on Prometheus at derivation `/nix/store/0irnadzklixs8brxwfpjwgym65f6alhx-primary-native-seat-fixtures.drv`, output `/nix/store/9idy2x1rwac7yv1h0vspkwbir6qk3w3m-primary-native-seat-fixtures`; its build log lists all three native seat fixture groups passed. This is a source and disposable-fixture repair only. Field `03e825` owns any later managed continuation after current profile and prompt hygiene revalidation; no live retry was made by this worker.
