# Canonical Flow-ID title alignment checkpoint

Observed 2026-09-21 15:40 UTC. The living's current naming instruction and `flows/7091ea/log.md` establish a short Flow ID for the Codex persisted thread title and Herdr pane label, and `flow-<id>` for the Herdr agent/HM route. Each seat keeps its own Flow ID. A shared tab has multiple seats and must not be renamed for one; the dry-run records its exact tab ID and pane count. The Herdr tab label remains unchanged for now, including single-pane tabs, because this task is per-pane alignment.

`tools/canonical-title-alignment.mjs` now provides a bounded exact-binding dry-run. It reads the HM native UUID, harness, session, pane, terminal and name, then exact Herdr agent/pane/tab metadata and the Codex app-server thread identity. The default CLI accepts explicit six-character Flow IDs, returns target and before-state with planned operations, and blocks missing or mismatched identity. `--apply` is deliberately disabled at this checkpoint: the proposed guarded mutation/rollback sequence has not yet passed same-target tests and cannot be used as-is. The next owner must fix rollback order and test partial failures, prove title readback, HM guarded same-target rebind, and target-side route receipt before enabling apply. Do not take a dry-run as a mutation receipt.

The adjacent machine receipt `canonical-title-alignment-receipts.json` is a read-only dry-run over 15 selected known Flow IDs. Fourteen exact native/HM/Herdr bindings were planned; `0ab019` was blocked because its HM registration lacks a native thread ID. No title, Herdr route, pane, tab, HM binding, or running seat was changed. Current hold set: Mind `2c61af`, `e798f3`, `23d977` and Field `c88918` while Mind sends work; Field `6db4fe`, `7091ea`, `395aed`, `9ddcbc` plus new Astra/Sol successors during launch and refresh. Revalidate all exact bindings after holds clear, as this snapshot is not an apply permit.

Claude native session title remains a separate harness case. The documented interactive `/rename` command can name the current session, but this worker has not established a supported external programmatic rename of an already running session or a no-model, no-queued-input injection/verification path. The script marks Claude title `pending-supported-interactive-rename`; it must not edit transcripts or resume/restart a seat. Herdr/HM alignment can proceed after exact native/terminal and route checks, and a target-side read witness is owed if HM is rebound. No such witness is claimed here.

This checkpoint hands the unfinished apply adapter to the Field Astra successor while current Field Astra prioritizes the requested Datom testing skill and its refresh. The skill and successor launch must not assume these names have been aligned.
