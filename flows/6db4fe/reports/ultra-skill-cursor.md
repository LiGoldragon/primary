# Psyche Ultra Low skill-turn cursor

The retained session has native expansions for twelve of thirteen required skills. The failed helper read aggregate transcript identity after `/visual-report-from-md` and combined that skill's Sonnet turn with a later Haiku turn. The installed generated skill declares `model: sonnet` and `kind: subagent`; both Sonnet assistant records have `attributionSkill: visual-report-from-md` in the same native session. The later `/main-flow` assistant records are exact dated Haiku with no effort field. This is concrete skill-scoped model evidence, not evidence that the base seat was changed to Sonnet or that base Haiku effort became medium. The only medium effort record belongs to the Sonnet skill turn.

The revised controller accepts only 03's exact 12-command snapshot and its raw transcript digest. It verifies each command's native expansion and session/attribution, requires dated Haiku model for every ordinary skill turn, and requires the installed Sonnet override declaration for the sole visual-report turn. It also rechecks the same live process UUID/PID/start time, model/effort argv, Herdr pane and terminal, idle state, and isolated job environment. It then sends only `/refresh`, waits for that skill's own attributed assistant model receipt, and sends the source bundle. It never replays the earlier twelve skills or provisional title. A changed transcript, extra command, wrong model/attribution, or missing verifier fails closed.

A successful output receipt will keep requested model/effort separate from the observed **base-turn** model/effort. Unless `/refresh` or the source turn provides a Haiku effort field, observed base effort remains `null`, and the receipt remains below the controller's final-title/ready gate. Sonnet's medium effort is excluded. The source check and passive live cursor preflight prove the initial guard only; 03 is sole executor of the remaining native turns. No input was sent by this source lane and no failed attempt was overwritten.

Source: `tools/claude-native-seat-refresh.py`; fixture: `tools/claude-native-seat-refresh.test.py`. The fixture covers the Sonnet override among Haiku skill turns, stale effort separation, exact remaining `/refresh` only, and no replay. Local fixture passed; remote Nix result is recorded after completion.

Verification: `python3 tools/claude-native-seat-refresh.test.py` passed after the final negative override fixture. The final coherent `nix build --no-link --print-out-paths .#checks.x86_64-linux.native-seat-fixtures --max-jobs 0 --option fallback false` passed remotely on Prometheus with output `/nix/store/snbli6v9rqhjaykmm9hjqjj5y6b97l49-primary-native-seat-fixtures`. This does not prove runtime completion or native readiness.
