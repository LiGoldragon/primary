# Psyche Ultra Low: running-session bootstrap repair

The retained Ultra session is running in the original Herdr pane and terminal with the reserved native UUID and exact dated Haiku model. The last managed attempt failed before its first native input because Claude had not yet created a transcript JSONL. This is a valid state for a new, unprompted native session; the old controller required the transcript prematurely. The original failed states remain evidence and must not be retried as workers.

The controller now has an explicit `--bootstrap-running-empty` path, requiring `--failed-state`, `--receipt`, `--refresh`, `--acknowledge-live-refresh`, and all four exact Herdr target fields. Before any input, it corroborates the prior failure's single-seat phase, route, native UUID, model and effort; requires the absent transcript and absent new receipt; checks Herdr idle and interactive state; correlates the exact Claude process argv/PID/start time with `claude agents --json`; checks the exact pane and scoped job directory/environment; and rejects nonempty or symlinked job state. The failed-state path is only corroborating evidence, not an identity authority. Missing or contradictory process evidence fails closed. After the first input, normal title, skill expansion, source and model observation continue; only after success is the new receipt created exclusively. A process match alone is **not** native-context or readiness acceptance.

Source: `tools/claude-native-seat-refresh.py`; fixtures: `tools/claude-native-seat-refresh.test.py`. The fixture covers the empty-transcript first turn, rejected later-stage failure, existing transcript and receipt, symlinked receipt, wrong process model/start time/foreign identity, duplicate bootstrap, and exclusive receipt. The actual retained state and live running process passed the new preflight read-only with a disposable *output path*. No command, prompt, slash command, or key was sent to the native seat by this source lane. Remote Nix fixture result and source revision are recorded after build completion.

03 remains the sole runtime executor. For one managed continuation, it must first synchronize the tested immutable source, reserve a fresh output receipt directory/path, and recheck the same retained pane, terminal, UUID, PID/start time, profile, idle interactive state, empty transcript and job directory. It then invokes the controller with the exact current manifest, failed state, Herdr binding, and new receipt path. Any newly present transcript, receipt, changed process, missing verifier, or failed source check stops this path. This action does not create a pane or UUID, restart Claude, or replay failed worker state. If the first native input fails after passing preflight, preserve that new evidence and diagnose its phase separately; do not repeat the empty-bootstrap command.

Verification: `python3 tools/claude-native-seat-refresh.test.py` passed locally. Remote-only `nix build --no-link --print-out-paths .#checks.x86_64-linux.native-seat-fixtures --max-jobs 0 --option fallback false` passed on Prometheus with output `/nix/store/lnqknx44clskj08wyh6521lydzfkc4ay-primary-native-seat-fixtures`. The live read-only preflight passed against the running retained session; this proves the guarded **initial state**, not subsequent model turns or readiness. At verification time, the output receipt was intentionally absent.
