# Datom variant casing correction

The living corrected the current temporary messenger wire: Datom variants must not be all caps merely to label machine origin. The canonical forward spelling is `Machine.Relay.{ ingress from seat heard mode [recipient] quote context }`. Datom variant constructors use their declared spelling and ordinary TitleCase; do not emit all-caps constructors such as `MACHINE`. Preserve the case of quoted literal strings and documented acronyms. The controlled psyche-ingress POC constructor is `Mentci.PsycheIngress`. This changes the Datom envelope, not historical transcript text or stored evidence.

Source audit: `tools/messaging-codec/src/main.rs` derives Datom constructors from Rust enum variant names. `tools/messaging.py::make_machine` and `tools/field-refresh-control.py::probe_envelope` currently emit the uppercase constructor; `tools/messenger` dispatch only recognizes it. The Python parser and test fixtures also encode uppercase. Merely changing the skill would leave the operational sender blocked. The codec's JSON producer label is transport metadata, not a Datom constructor, but will be aligned for clarity. The exact grammar/arity and one-recipient `tools/msg` behavior remain the same.

The production messenger executes the codec path for each ingress. The corrected codec and full messaging fixtures passed a **remote Nix build** with local builds disabled (`max-jobs=0`, fallback false), and the corrected runtime package also built remotely. The fixtures exercised the real codec, strict rejection of an uppercase Datom constructor, and local messenger transport with disposable targets. The Python Field refresh parser's five tests passed locally without compiling code. A direct invocation of the remote-built codec accepted the Python formatter's `Machine.Relay` envelope and emitted producer `Machine`. No existing flow was contacted merely to test this change. This host currently exposes no live messenger process or `~/.local/state/messenger/pane_id`, so the new binary/package is validated but a production courier ingress is not witnessed. A queued historical uppercase message remains historical evidence; this correction does not replay or rewrite it.

The authored Curriculum skill path is owned by Field Sol `753e69` under lock #3969; this report writer did not edit it or run the generator. Generated namespace reconciliation must preserve unowned testing and ad hoc skill files; the owner must identify exact outputs and ownership before any deletion. The pending FlowMessage draft under Curriculum lock #3825 remains outside the scanned skill directory and is not installed.
