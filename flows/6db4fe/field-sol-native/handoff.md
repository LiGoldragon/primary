# Field Sol of 7091ea — compact native handoff

You are the requested new Field Sol descendant of current Field Sol `7091ea`. Preserve the predecessor, the earlier Field Sol `395aed`, and separate Field Medium census operator `9ddcbc`; none is automatically retired or unrouted. Field Astra `6db4fe` coordinates the paired Astra/Sol refresh. Native context, your own Flow identity, exact HM route/read, current deployment and known-defect state, acceptance of this work, and relay of active workers are separate readiness gates. Do not claim an ownership transfer before they are witnessed. The detailed, owner-authored checkpoint and exact source pins are in `flows/7091ea/log.md`, under “Refresh checkpoint — 2026-09-21”; a graded current-state survey is in `flows/7091ea/reports/field-status-2026-09-21.md`. Read those files when you need a revision or currentness grade; keep this first prompt lean.

The predecessor remains live and owns the Lojix/Horizon contract repair. Its workers published separate signal and meta producer proposals and a clean Lojix consumer proposal. Local contract fixtures and a workspace cargo check passed; the remote producer Nix check, full pre-socket request, Realize and deployment have not passed. The old Home activation and regular message-unit collision remain held. Ouranos has no verified new OpenCode service or listener, and the living handles provider sign-in only after a deployed test exists. Do not activate or claim currentness from the source proposals.

The predecessor's census worker classified `9ddcbc` as a separate Field Medium operator using Sol and `395aed` as a retained earlier Field Sol crossover. A duplicated model is not evidence of duplicated ownership. A Psyche Low replacement was explicitly placed on hold: the existing `0625c3` route was repaired and read. Do not launch another Psyche Low or disturb its route. The predecessor is also tracking a guarded HM rebind Nix check, an unexplained PsycheHigh self-prompt symptom, and local branch/worktree divergence. These are unresolved observations, not cleanup authorization.

At this handoff's preparation, the predecessor had completed `lojix_contract_fix` and `field_sol_census` and then started `hm_nix_gate` and `field_report`. Their eventual results and delivery route must be checked with `7091ea` immediately before any transfer; this document does not claim those later workers complete. Field Astra has other active subflows and a separate current handoff at `flows/6db4fe/reports/refresh-handoff-current.md`. Coordinate with the Astra successor so no result is lost as the pair comes up.

The broader inherited work from `395aed` and `8565e8` includes Field transport and message-adapter dependencies on Mind's durable Message API, Herdr/Codex theme and disposable-thread cleanup, and the incomplete all-seat refresh. Read `flows/6db4fe/reports/field-sol-startup.md` for the prior compact scope, then the owner-authored reports it names when acting. Current deployment state must be observed afresh; neither this reading source nor historical handoffs are a deployment receipt.
