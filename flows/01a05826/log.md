# Flows protocol identity design

Discuss the flows protocol while remembering relevant prior work, especially concerns and ideas left open. Establish the real Codex and Claude session-ID patterns, decide how a four-character flow label should be derived, design a compact way for a flow to obtain it, and verify the coordination waiver for a flow's own artifacts.

Remembered: b7465e71, 5c8be3ca, 15b67974, 01a02a06, 01a03952, 01a02a34, 01a03d6e, 01a03fe9, 01a035fb, 3513e4af, 01a04236, 01a04237, 01a042376379, 01a0539e — depth 1

Most relevant: the flow-artifact anatomy and remembering protocol; transcript-origin migration; PathLock/Orchestrate lifecycle; the stale-working-copy incident; retention and identity investigations; and the explicit correction allowing a flow to write its own directory and append to the index without a Lock. Open concerns include collision-safe identity, root-session versus thread identity, writer serialization, retention authority, non-primary workspace location, and the future typed-datom harness.

Current evidence: a flow is one bounded context/thread, so current Codex identifies it with `CODEX_THREAD_ID`; `CODEX_SESSION_ID` is the shared root lineage. Codex 0.150.1 uses UUIDv7; the leading `01a0` is timestamp-derived and not invariant. In the pinned generator, the earliest four RNG-only hex characters are normalized UUID positions 24–27, while earlier payload contains a per-millisecond counter. Local Claude main sessions are overwhelmingly UUIDv4, whose leading characters are random, but Anthropic promises only a UUID and local UUIDv5 exceptions exist. Claude exposes active sessions through `claude agents --json`; a tool can privately match the nearest Claude ancestor PID and emit only the abbreviation, while a future SessionStart hook could make the identity directly available. The own-directory and one-line index-append Lock waiver is present in both authored and generated flows skills; generic coordination wording remains unconditional and ambiguous.

Current ruling: use six literal characters from the actually random portion of the harness ID, with no hash or conversion. A small harness-specific tool checks availability before returning the ID; the first collision extends by one or two literal characters so directory IDs stay unique. Old IDs remain unchanged. Subflows do not create their own lanes; they use their parent's lane and identity. This supersedes this flow's earlier proposal to key Codex flows by `CODEX_THREAD_ID`; the root `CODEX_SESSION_ID` now appears to be the intended lane identity, pending exact harness and hook confirmation.

Correction: the previous response called normalized Codex slice positions 24–27 `15d4`; direct indexing gives `715d`. The mechanically checked six-character Codex candidate is normalized `CODEX_SESSION_ID[23:29]`, currently `715d46`; collision extensions are `715d462` and `715d4621`. Claude uses the first six literal characters of the parent main-session UUID and extends the raw prefix.

Tool shape: one idempotent directory-name helper keeps the full UUID private, returns only the six-character or extended literal alias, and distinguishes an existing directory belonging to the same session from a collision through tool-owned identity metadata. It uses ordinary filesystem existence/creation behavior, never an Orchestrate claim or Lock. Codex reads the shared `CODEX_SESSION_ID`. Claude can receive the main session UUID through a silent SessionStart hook, or fall back to a privately captured `claude agents --json` PID match. Observed Claude subflows all retained the parent main-session UUID; agent/task IDs are not lanes. A hook should expose identity rather than create a directory on every event.

Open design questions: whether subflows return all flow-log and psyche material for the parent alone to write, or may write directly inside the shared parent lane; the exact tool-owned identity marker; and whether a cold resume keeps the same lane or begins a successor lane.

Codex naming inquiry and corrections: phone remote control and local TUI converge in the shared Codex app-server at `thread/start` → `ThreadProcessor::thread_start` → `ThreadManager::start_thread`. The app-server can distinguish a root user session by `thread_source=user` with no `parent_thread_id`; subflows use `thread_source=subagent` and carry a parent. Codex core `SessionStart` hooks run before the first model request, but their output schema has no thread-name field and is not routed to `thread/name/set`, so an existing hook cannot perform the rename. The universal automatic implementation belongs in the app-server immediately after fresh root thread creation and before the first turn: obtain the unique six-character flow-directory ID and reuse the existing name-persistence path. This covers phone and TUI starts while excluding subflows. The earlier unidentified-client and TUI-only proposals were unsupported generalizations and are superseded.

Deferred possible future work: automatically assign the unique six-character flow-directory ID as the Codex root session name inside the common app-server root-thread creation lifecycle. This is not current work.

Current work: distill all Codex-related vision. Candidate gathering must cover distilled Vision, raw per-flow vision, psyche-raw, and typed transcript expressions; the main flow will compose a self-standing proposal for explicit living approval before any `Vision/` change.

Candidate acquisition complete: no Codex-specific Intent was found. The durable material separates into Codex access/software, flow anatomy, flow identity, Flow Nexus control of vendor harnesses, Agent Intercom, and asynchronous visual collaboration. Proposed exclusions are working/deployment instructions; process outcomes; superseded four/eight-character and lock-assigned ID schemes; the misleading “flow and subflows are one flow” identity reading; unverified claims about Codex context strata and skill loading; dated comparison Notions; and automatic session naming, which remains deferred possible work. A multi-topic distillation proposal is ready for living review; no top-level Vision or raw archive has been changed.
