# Realize Protos, Datomic, and Ethos-zero on the Portion pivot

This flow realizes the Portion-pivot stack in incremental, delete-and-rewrite vertical slices: Protos first, then Datomic, then Ethos-zero, ending with regenerated Orchestrate and a witnessed round trip through the deployed stack.

Current slice: M0 correction of the Ethos library-map reader and emitter. The completed Portion-pivot and consumer migrations remain historical evidence; their authored library maps and generated declarations are being corrected because they use an invented Rust-transcript dialect.

Working instructions:

- Treat the supplied realization brief as design flow db97561c's articulation and recommendations, not as new living-psyche vision.
- Confirm every choice marked `(flow)` with the living before building on it; carry forward choices marked `(ruled)` only after remembering their cited records.
- Delete and rewrite in place, one vertical slice at a time; prove, version, commit, push, and log each completed slice.
- Do not create beads. Do not edit `Vision/`. Keep frozen repositories out of scope.
- Use subflows for all task work; the root flow manages the work and the conversation with the living.
- The living supplied Claude's exact corrective working instruction on 2026-08-29. It supersedes db97561c's earlier realization prompt on library-file syntax only: `Library.{major minor patch}`, imports, then one body with positional `[types] [kinds] [associations]` sections; direct structural type declarations; separator-defined kind receivers; and `Type.[Kind ...]` associations.
- The correction does not rule or change the kind vocabulary, costume kinds, invented type names, or six data-syntax atoms.
- When a required form is absent from the corrective instruction, stop and ask the living. The earlier instruction to keep going and use best judgment does not authorize filling a syntax gap.
- Delete the `Schema` branch and its Rust-transcript vocabulary first. The map is upstream and generated Rust changes to match it.
- Before M1, show the living the complete rewritten `protos.ethos`. Before M3, show one complete consumer map.

Open now:

- M0: in Ethos-zero, replace the invented `Schema` reader/emitter with the exact `Library` file grammar, prove the canonical example emits compiling Rust, and prove every named invented form faults with an Extent.
- M1: after living review of the full proposed `protos.ethos`, make the Protos map upstream and its declarations generated.
- M2: apply the same upstream-map treatment to Datomic and Ethos-zero and remove the two stale root signal maps.
- M3: after living review of one full consumer map, correct Chroma, Horizon, Synchronizer, Relative Age Display, Claude Answers, and Chronos and repin the new stack.
- M4: sweep the new stack for every forbidden invented form and fast-forward the primary checkouts.

Remembered: db97561c, 2ef42163, 04db2fd2, b675f3d9, 06196cc7 — depth 2. The Portion-pivot realization brief is an operationally adopted design-flow proposal, not new vision; exact earlier living rulings ground contextual bidirectional parsing, direction asymmetry, Portion's structural anatomy, opacity, enclosure treatment, the `embody` verb, and the `ShapeDefined` name, while the exact kind capabilities and map encoding remain open.

P0 ground:

- The proposed semantic map has been reconstructed without treating the existing Rust as architecture.
- `repos/protos` is clean at `0.8.0`; `main` equals `main@origin` at `3b190f9fc2c2a074ceeb6ababfea89e3dd504996`.
- No `protos.ethos` exists and no existing authored Ethos example settles the Protos map envelope or type–kind association syntax.
- Primary coordination records contain unrelated dirty state; preserve it.

Operational adoption: after seeing the P0 map, the living instructed, "dont stop again. use your best judgment. Im going to bed and want to come back to a lot of work done." This authorizes the flow to settle remaining design-flow choices by best judgment and keep advancing; it is a working instruction, not a vision ruling.

Scope extension after the main terminus: "If you get everything done and well tested, start hunting down recently edited repos that still use the (by then) older version of datom, and port repos that still use dotos/nota/schema to the latest (by then) datom and ethos". This supersedes the earlier freeze only after Protos, Datomic, Ethos-zero, and Orchestrate are complete and well tested.

Protos complete:

- P0 `952d0325a6940c703dc6605681583adb1424f920`: deleted the old source/tests/checks, authored `protos.ethos`, bumped to 0.9.0.
- P1 `19097dbac0bb03f8923323817ebe5ab142551655`: structural delineation with UTF-8 half-open extents and generated-tree properties.
- P2 `0196dc2d18c6ba6958ee590e80c8e8eab92df04f`: canonical flat printing and bidirectional identity properties.
- P3 `d077a2416325229e25f4e9c8a04a6ca5cb717cfe`: normalized and hashed Text, Prospective<T>, and the Embodiable/Embodied/Textualizable/ShapeDefined anatomy plane.
- Each slice's new behavioral test was witnessed failing before implementation. Final `cargo test`, strict clippy, fmt, and `nix flake check` passed; the repository is clean and all four commits are pushed.

Protos correction: `cb881fcf6751561a041ae7718b3521af6ebaf9b0` (0.10.0) aligned dialect boundaries in map and Rust, added canonical parenthetical escaping, removed `PortionForm`, expanded fault tests, passed Cargo/clippy/fmt/Nix, and was pushed clean.

Further Protos correction complete: `49b1a5d261aa797109b2de98153a99859a539f97` (0.11.0) replaced false-positive round trips with invariant-preserving Portion construction, computed arity, `BareSafe`, typed Text/Prospective association, split structural/opaque enclosure anatomy, canonical projection through the sole delineator/printer, and aligned map/docs. `cargo test` 8/8, strict clippy, fmt, and `nix flake check -L` passed; the commit is pushed, the repo clean, and no Protos lock remains.

Protos final audit correction: `4ff4152edb87bc0c376dd1caf4ebcffd22b46653` completed the authored map for FaultProblem, Layout, Printing, and DelineatedText and aligned all parenthetical escape documentation. Runtime/version remains 0.11.0; Cargo, strict clippy/fmt, and Nix passed; pushed clean.

Datomic preparation: remote rename authority is available and the canonical checkout is clean, but the diverged `epic-datom-path-locks-20260822` branch and its clean peer workspaces are preserved as peer-owned. Rename only canonical main after the corrected Protos lock is drained; do not move, delete, or retarget the epic workspaces.

Datomic D0–D4 landed in `b87ba9b932543cd509664fccd72beb2e85a2ced9` (0.6.0): GitHub and Cargo identity renamed to Datomic; canonical checkout moved with an old-path filesystem redirect preserving the peer epic; legacy source/tests deleted; `datomic.ethos` authored; Portion-only Boolean, integer, finite decimal, String, Vector, alternating guillemet Map, Option, positional struct/enum/unit/payload anatomy, hand-declared Orchestrate witnesses, and typed Text edge implemented. Cargo, strict clippy/fmt, and Nix passed; pushed clean.

Datomic bare-string closure: Protos `92a61bf1baf3442657e522d3da6f63cf374ee10a` (0.12.0) added context-aware string bare-safety and Portion canonical content; Datomic `ccd4cf6efb5bec43a085ae1f2e21841a4d409ae3` (0.6.1) repinned and proved `a.b`/`a!b`/`a:b`, spaced curly opaque, UTF-8, balanced curly, and typed-edge round trips without a dialect scanner. Both repos passed Cargo, strict clippy/fmt, and Nix and were pushed clean.

Datomic audit correction in progress: an adversarial read found numeric character parsing in the dialect, an outbound re-read after printing, non-total raw `f64` and `String` implementations, duplicate-map data loss, incomplete full-contract D3/D4 witnesses, stale documentation, and material test gaps. Protos is adding neutral expected-scalar Portion questions; Datomic is moving to invariant-bearing finite decimal and representable string embodied types, duplicate-key faults, direct writer Text, full Orchestrate fixtures, and external-oracle tests before Ethos-zero starts.

Protos scalar/outbound foundation: `05198b1f89fe8c7e5c0d47131075ad1f7bd61795` (0.13.0) added extent-bearing signed integer/finite point-decimal Portion anatomy and zero-read writer Text retagging; `7e2bba7d48c62de53b3f93cb6053a490bbd6cf3b` (0.14.0) added cloneable invariant Portions and Protos-owned outbound integer/decimal/string construction. Fail-first contracts and Cargo/clippy/fmt/Nix passed; both pushed clean.

Datomic audit correction complete: `79ea53b26d6aaa29deaa76d214fb35b074ba4ed9` (0.7.0) repinned Protos 0.14, removed dialect numeric scanning and outbound rereading, introduced invariant-bearing `FiniteDecimal` and `DatomicString`, rejects duplicate map keys at their Extents, completed the approved Orchestrate request/reply and D4 fixtures, and replaced stale map/docs/agent surfaces. Cargo, strict clippy/fmt, and Nix passed; pushed clean with no lock.

Datomic final audit closure: `ffb0ffa316285ab56e50fbc035cb8c14380f4665` completed byte-identical D4 public-edge fixtures for every approved Orchestrate request/reply/refusal root, exact malformed map/vector/string Extents, the authored fault taxonomy/declaration pattern, and current Protos documentation. No runtime/version change; Cargo locked, strict clippy/fmt, and Nix passed; pushed clean.

Ethos-zero lifecycle ground: `ethos-monolith` is clean with no peer workspaces and may be renamed directly to public `ethos-zero`; `signal-ethos-zero` and `meta-signal-ethos-zero` do not exist and may be created public. Existing dirty Orchestrate/signal worktrees and all peer workspaces remain untouched until E4.

Ethos-zero E0–E2 intermediate `a1a0061bd145a6e03750c6c9b37f10641f918730`: public/local `ethos-monolith` renamed to `ethos-zero`; legacy source/tests/checks deleted; headed Interface/Schema map authored; Portion-only reader, manifest import resolution, and quote/syn/rustfmt/rustc emitter passed nine tests and remote Nix. Strict E2 remains in progress for inline-derived types, full Datomic D3 anatomy generation, headed Protos/Datomic maps, and durable structural comparison with the map-owned handwritten contracts.

Ethos-zero strict E2 complete: Protos `2f605fd689679a24e4ef83a643a2cd066cb12186`; Datomic `fae7f3ebd208357f83a5c9dbdaa0f8328c7d6857` plus Protos identity pin `4baeaac79de265bfeb019e8dcdf7124daa9ecac5`; Ethos-zero `b0830fb9fc8e17d6cea6546b4ef055c38e94ab55`. Full real maps emit; executable generated Orchestrate anatomy round-trips; associations compile/fail correctly; 46/46 map-owned public contracts match (Protos 22 types/aliases + 13 traits, Datomic 6 types + 5 traits). Cargo/strict clippy/fmt and Nix passed in all three; pushed clean.

E3 bootstrap WIP was preserved before strict E2 correction on remote bookmark `e3-bootstrap-wip-01a04a30` at `df691e13d0bae878edc29db7a409362003bfd5a5`; it contains the workspace/Nexus/CLI shell but not self-regeneration or Sema persistence. Resume by integrating it from current Ethos-zero main, not by moving main backward.

Ethos-zero generation-zero signals landed and audited-corrected: initial 0.1.0 contracts were replaced by breaking 0.2.0 `signal-ethos-zero` `ef56ce7d4be107ec844cd4d6d2169fc4db90b715` and `meta-signal-ethos-zero` `c5578c9de85a34ea4f7d1d2bac6b6c91afe64a53`. Frames now carry and validate protocol, channel contract ID, and wire revision (ordinary 1/2, meta 2/2); every root/refusal has Datomic specification and rkyv frame witnesses; UPGRADES documents coordinated deployment. Fail-first, Cargo/strict clippy/fmt, and Nix evaluation passed; both pushed clean.

E2 acceptance reopened after independent proof audit: the `b0830fb9` suite parses but does not Rust-compile the full emitted Protos/Datomic maps; its negative association fixture invents substitute types instead of removing an actual map association; its ordinary/meta Interface fixtures abbreviate copied schemas instead of consuming the pinned real Orchestrate `.ethos` sources; and section entries currently collide with declared type names (`Lock`, `Configure`). The Datomic map also omits the private invariant layouts used by retained engine implementations. Therefore the earlier “strict E2 complete” statement above records what the producing subflow reported, but is not the current acceptance state. A correction subflow is closing these exact witnesses before E3 is resumed.

E2 strict-core correction landed, acceptance still open: Datomic 0.7.1 `8b17abc5296e0d41b6a4adf45f71eb9a90f7241b` records the exact private invariant layouts in its map; Ethos-zero `a752943fa5889013fe7c26f5c331683430912379` adds schema-library emission, full real-source AST splice compilation for Protos and Datomic, a semantic negative that removes the actual `ContentHashable for Text<T>` impl, and section references that no longer collide with declared `Lock`/`Configure` types. The local 22-test/fmt/strict-clippy suite passed. E2 remains incomplete until real pinned ordinary/meta Orchestrate `.ethos` files generate, compile, and exercise the approved string paths, and until the expanded Nix check is observed green.

E2 acceptance complete: Ethos-zero `dd5283a8fbad7328b70d23dc27029f5e8a33cb6b` flake-pins signal-orchestrate `6fc8c5b7f1880b73461a4ffa863a3f8952245c0a` and meta-signal-orchestrate `d4dd208cd6e10254075a0c311a8e8a14a1ff3f8d`, reads their real `ethos/signal.ethos` files through `FileReader`, emits and Rust-compiles ordinary/meta crates against exact Protos `2f605fd...` and Datomic `8b17abc...`, and executes Lock/request/reply, Configure/request, angle `<Kind>`, and balanced-parentheses String round trips. Fail-first exposed the real Reply ownership of LockRejected and non-hermetic fixture lockfiles; both were corrected. Local 23/23, fmt, strict clippy, and complete remote `nix flake check -L` passed. E3 may now rebase its preserved WIP onto this head.

E2 executable-default correction reopened by read-critical audit: `dd5283a8` emits `loop {}` for every schema method marked `Default.Yes`; the structural comparison erases method bodies, and the splice witnesses compile without calling those generated defaults. This is a real placeholder for the Protos `Textualizable::textualize` and Datomic `Datomic::textualize` defaults, so E2 is not complete until the generated map behavior is executable and tested. The audit's initial concern that `dd5283a8` was not Git-published was disproved by a fresh authoritative GitHub clone; only local tracking metadata was stale.

E2 final acceptance complete after executable-default audit correction: Protos `bfde3b878dd3de2991d7f605b59f57a13ef8f20b`, Datomic `b670c72d0c2cb94ad1e39b372271f6569d91e214`, and Ethos-zero 0.2.0 `30e5ef67db35f8605c42a3451d80418325989724` are pushed. `Default.Yes` was replaced by structural map-owned chains lowered as tokens; the fail-first full-map executable witness bounded the old Protos `loop {}` at ten seconds, then asserted exact generated outputs `generated-default` and `“generated default”`. All 24 local tests, fmt, strict clippy, full remote Nix, and authoritative GitHub main verification passed. The independent audit found no other placeholder anatomy. E3 must rebase its preserved WireContract WIP onto this final head before continuing.

E3 producer slice complete: Ethos-zero 0.3.0 `0836a1ff89a7807ae25c36d87bf3ae350c6a60c4` cleanly extracted the WireContract generator capability from the combined WIP and pushed it to main. It emits native rkyv wire types, frame/envelope roots and constants, Datomic anatomy including non-scalar alias delegation, and compiles real ordinary/meta generated modules beside an external hand-owned codec. Exact locked-source 26/26 tests, fmt, strict clippy, full remote Nix, and fresh GitHub clone passed. Signal repositories were not changed in this producer commit; their self-regeneration is the next E3 slice.

E3 producer acceptance reopened by read-critical audit: 0.3.0 string wire aliases publicly wrap arbitrary Rust `String`, while their emitted `Datomic::portion` uses `DatomicString::try_from(...).expect(...)`; native construction can therefore panic on an unrepresentable Datomic string. The unsupported TupleStruct path also silently emits no Datomic anatomy instead of returning a typed generation fault. All other WireContract structure and the E2 regression surface audited clean. A producer-first correction is required before signal regeneration.

E3 producer final acceptance complete: Ethos-zero 0.3.1 `a0e21ed4fc68c1d3f76d38cc931f3d8f82353a0a` makes wire String aliases private checked newtypes, removes the panic path, and rejects TupleStruct with a typed Declaration fault before emitting any item. Exact Nix-path 28/28 tests include executable invariant and real ordinary/meta generated-module plus hand-codec boundaries; fmt, strict all-target/all-feature clippy, full remote Nix, and fresh GitHub clone passed. No signal repo changed during the correction. Signal self-regeneration may now proceed on 0.3.1.

E3 self-hosting signal slice complete: producer Ethos-zero 0.5.0 `f043fc46c02e52308163855f23cb0ae290f970c7` adds refusal/event frame roots and structural Datomic anatomy for all Request/Reply/Refusal/Stream roots. Ordinary signal-ethos-zero 0.3/wire 3 `493742df956610aa829436d1b2da7568c0c7b82d` and meta-signal-ethos-zero 0.3/wire 3 `19f10780cc8dac630c6f72ec0c9a09be6a80c182` add Subscribe/Unsubscribe lifecycle inputs, stream events, and typed InvalidRelativePath refusal. Each repo owns generated `src/generated/signal.rs`, hand-owned `src/codec.rs`, and wiring-only lib; its test invokes producer f043, rustfmts, and byte-compares exactly. Ordinary four and meta three executable tests cover all Datomic roots, rkyv envelopes, malformed/version/contract/wire rejection, and lifecycle/refusal/event behavior. Local cargo/fmt/strict clippy, full remote Nix, fresh-clone regeneration, and cleanliness passed for all three; locks released.

E3 runtime/bootstrap complete: Ethos-zero main `2309e5b968b8702f2ecc679cd577b3e2bce0e99e` adds the 0.6.0 runtime workspace crates `ethos-zero-nexus`, ordinary `ethos-zero`, and meta `ethos-zero-meta` while retaining producer 0.5.0. It pins ordinary/meta signals `493742df...`/`19f10780...` and sema-engine `7158e550...`; one mailbox actor owns the typed Engine with schema-v1 configuration and assembly record families, atomic initialization/upsert, XDG state/socket defaults, Datomic-only manifest/CLI edges, two binary rkyv sockets, strict InvalidRelativePath refusal, file-write-before-store policy, and ordered lifecycle events. Seven Nexus tests cover persistence reopen, serialization, sockets, roots/refusals, ordinary/meta observe-subscribe-unsubscribe/configure/event behavior, malformed frames, and CLI one-argument/no-flags. Local fmt/strict workspace clippy, full remote workspace Nix including all producer tests, and clean fresh-clone Nix passed; lock released. E4 regeneration/Orchestrate integration is next.

E3 runtime acceptance reopened by read-critical audit before deployment: the no-XDG-runtime Nexus fallback double-appends `ethos-zero-nexus` while CLIs append once; lexical containment follows symlinks outside source roots; persisted socket-path Configure values do not rebind listeners; frame readers allocate an unbounded u32 length; ordinary subscriptions broadcast unrelated files and unsubscribe removes all equal values rather than the caller's stream; meta has the same ownership issue; encode failures silently become zero-byte writes. Exact signal regeneration/roots/pins/Sema ownership audited clean. These runtime boundary issues must be patched and fully reproved before E4 deploy; E4 signal-contract regeneration may proceed independently.

E4 signal-contract slice complete: signal-orchestrate 0.18.0 `a597f1ae991059f6c2029b59a4773b2f77a62064` and meta-signal-orchestrate 0.12.0 `5cdf35a989f273b84f20a802855a03b6593b376d` are generated from their actual Ethos sources with Interface/channel revisions 0.3.0/1:6 and 0.2.0/2:5. They pin Ethos-zero `2309e5b...`, Protos `bfde3...`, and Datomic `b670...`; remove build.rs, ethos-monolith, signal-frame, Dotos, and legacy Datom dependencies; own generated signal modules plus separate codecs; and prove byte-identical rustfmt regeneration, complete Request/Reply/Refusal Datomic roots including `Observed.Locks.[]`, and rkyv frame validation. Both passed local Cargo/fmt/strict clippy, full remote Nix, and fresh-clone no-diff regeneration. The canonical ordinary checkout's preexisting dirty patch remains untouched; its reply-root intent was preserved in stronger generated coverage. Locks released; Orchestrate itself has not yet changed or deployed.

E3 runtime audit correction complete: Ethos-zero `2d25c653f4da63389463009e808c1c7fe8b78874` versions the runtime workspace 0.7.0 and hardens every audited boundary: unified state fallback, 16 MiB frame bounds in server and both CLIs, symlink-safe source/artifact containment, typed rejection of non-operational socket Configure changes while preserving source-manifest persistence, session/file-scoped ordinary subscriptions, session-scoped meta ownership, and explicit encode-error propagation. Eleven Nexus plus two tests per CLI, strict workspace clippy, and full remote Nix passed; pushed and lock released. E4 live deployment is no longer gated by the E3 audit.

Coordination correction: an E4 subflow accidentally created and claimed new Bead `orchestrate-3xf` despite the explicit no-new-beads instruction. It dry-ran and deleted exactly that unattached record immediately (no dependencies or text references) and ceased Bead use; code work remained under the flow and Orchestrate lock only.

E4 Orchestrate integration complete, deployment still pending: Orchestrate 0.26 `dadd537bbd2ed2ffc5260fffc5735f9f020cc774` pins Datomic `b670...`, Protos `bfde...`, ordinary signal `a597...`, and meta signal `5cdf...`; removes direct legacy Datom/Dotos/signal-frame dependencies; and ports transport, CLI, and store logic to generated Request/Reply/Refusal roots and validated little-endian rkyv frames. The canonical dirty checkout remained untouched while its intended canonical Datomic docs/CLI and live Lock/Observe/Refusal/Release coverage were preserved in the clean integration. Twelve tests, fmt, strict clippy, full remote Nix, and clean fresh-clone Cargo/Nix passed. Breaking-upgrade/preflight guidance landed. No service/deployment action occurred; lock released. Live cutover waits only on the final E3 runtime audit correction under lock 118.

E3 runtime final audit correction complete: Ethos-zero 0.7.1 `a0850a80a7c1eaa5fb548c5a53fa92cdea12a8fa` makes real socket subscription ownership/unsubscribe/disconnect work for ordinary and meta clients, atomically migrates pre-0.7 persisted socket paths to the actual bound paths while preserving the source manifest, and replaces check-then-write containment with descriptor-relative `openat`/no-follow traversal, exclusive synced temporary creation, and same-parent `renameat`. Synthetic session tests were removed; real socket lifecycle, disconnect cleanup, and active symlink-swap stress were added. Nexus 12/12, fmt, strict workspace clippy, exact-path full remote Nix, and clean fresh-clone full Nix passed; lock released. E4 deployment may proceed after a final independent verification and live preflight.

E3 runtime strict ordering correction complete: independent audit found a race where an Event could reach the outbound queue before the Subscribe snapshot Reply. Ethos-zero 0.7.2 `060cc7a77033cac79648a409582238c1f86d4204` sequences Reply(Observed) before activating event delivery. Real Unix-socket stress tests force 24 ordinary Generate and 24 meta Configure races and assert snapshot-before-event; fmt, 14 Nexus tests, strict workspace clippy, full remote Nix, and clean fresh-clone verification passed. Lock released.

E4 declarative cutover prepared, live service unchanged: CriomOS-home `0530bcd80cce53bcd876f9ecf5947ed43698a2b4` pins Orchestrate 0.26 `dadd...` and documents stop/preflight/witness/rollback; consumer CriomOS `93fdd1d26439637cfc7666e35fafa655d3a36e3c` advances Home and its direct Orchestrate ownership check. Fresh clones and exact pin/package checks passed. Full closure requires Lojix materialization of the system/horizon inputs; no override was fabricated. Live Orchestrate remains healthy 0.25 with its Sema file and both sockets; the 0.26 preflight correctly cannot acquire the store while 0.25 runs. A Lojix-skill subflow is discovering the exact existing proposal/transport/builder fields for a non-activating immutable Realize before the brief's authorized cutover.

E4 terminus deployed and witnessed: Lojix deployment 85 non-activating Realize terminally succeeded and materialized the exact CriomOS `93fdd1...` Home generation. The live 0.25 Nexus was then stopped; exact dadd preflight passed `active legacy PathLock rows: 0`; Lojix deployment 86 ActivateNow advanced through Submitted/Building/Copying/Activated/Completed and terminally succeeded. The current profile is revision `93fdd1...`; Orchestrate 0.26 runs from `/nix/store/pbjprrhnas2vijypwz87zrnzla92f8d5-orchestrate-0.26.0`, PID 2052947 at witness, zero restarts, both sockets present. Live canonical Datomic witnesses preserved unrelated lock 19, acquired lock 122, returned typed duplicate refusal, released via the ruled scalar form `Release.122`, returned typed unknown-release refusal, and finished with only lock 19. Idempotent meta Configure returned current paths without restart. Rollback remains deployment 84 / revision `ee0ae00...` / prior profile. The temporary empty witness directory was removed; no source or Bead edits occurred during cutover.

Main Portion-pivot terminus is complete and well tested. The living's conditional scope extension is now active: begin migrating recently edited consumers from older Datom/Dotos/Nota/schema to the final Protos/Datomic/Ethos stack. Read-only inventory prioritizes Horizon plus its goldragon data/CriomOS chain, then the self-contained Chroma consumer; signal-bound Lojix/Agent and broad active legacy repositories remain deferred unless their coordinated contracts enter scope.

Post-terminus producer extension landed for real consumer schemas: Ethos-zero producer 0.6 `9f652dfc90bc440303b11a33c3d5d4134937c202` implements the ruled explicit named-field form `Name.Type`, preserving semantic/repeated Rust field names while keeping Datomic anatomy positional by declaration order. Executable tests cover repeated Magnitude fields, nested Option/Vector, and map fields. Full remote Nix passed build/fmt/clippy/doc, 30 exact source-linked producer tests, and all 14 Nexus runtime tests; pushed and fresh-clone clean. Horizon and Chroma migrations now target this producer.

Post-terminus data-library emitter completed: Ethos-zero 0.7.0 `60dd93a9a64386fec5dbc5729dd9b1edf5f4fa10` adds `RustEmitter::datomic_library()`, emitting Schema declarations plus executable Datomic anatomy only—no kind-trait duplicates, roots, frames, channels, rkyv, or build-time generation. Ethos-zero 0.7.1 `b922afba278d5e1430f0a52ce49898c3263e87bc` scopes ordinary value derives to that data-library output so generated public unit enums are usable without contaminating legacy D3 splices. Both passed exact full Nix; 0.7.1 ran 31 producer contract and 14 Nexus tests. Horizon and Chroma repinned to b922.

Post-terminus Chroma migration complete: Chroma 0.3.0 `34e47a0fdd1bf2e636b2df280c3bf6e3720643e9` replaces Dotos request/reply/config parsing with an authored Ethos Schema, committed `datomic_library()` output, and explicit rustfmt byte-identity regeneration. Runtime semantic conversions preserve brightness/time/theme/warmth/schedule validation; `config.datom` replaces the legacy path; Dotos dependencies, derives, parsers, writers, code/tests/tools references are gone (UPGRADES alone records the rejected old path). It pins final Protos `bfde...`, Datomic `b670...`, and Ethos-zero `b922...`. Post-repin fmt, strict clippy, full locked tests, remote Nix, fresh-clone generated tests, and no-legacy scan passed; lock released.

Post-terminus Horizon proposal chain complete: Horizon 0.5.0 `c53a96d31c299528c049e64bf9536cc28f554644` removes Dotos dependencies/parsers/derives, authors an Ethos Schema, explicitly regenerates committed D3 anatomy against its existing serde/validation domain types, and moves the CLI to `Text<ClusterProposal>`. Generated anatomy owns plain enums; checked hand D3 owns semantic names/addresses/domains, u32-bounded Machine/IO records, and data-bearing proposal services. Full fmt, workspace tests, strict clippy, Nix, and clean fresh-clone regeneration passed. Goldragon `aef3a9362e4364b9549f380c1154214b52cb8fff` replaces its Horizon `datom.dotos` with byte-stable `proposal.datomic`; a one-off uncommitted pinned-old-decoder/new-encoder bridge proved semantic JSON identity and was removed from product/workspace registration. Fresh Horizon projects all eight declared nodes. The unrelated `synchronizer.dotos` remains explicitly for its separate SynchronizerConfig owner; that migration is next. Horizon lock 123 and the clean stale producer lock 124 were released; temporary bridge/source evidence remains recoverable under `/tmp`.

Post-terminus SynchronizerConfig chain complete: Horizon 0.5.1 `f8c5808466a47c2fd741cf0b119d73e8ba2add3d` moves its authored Ethos map into the published `horizon-lib` package after Synchronizer's full Nix proof exposed that Cargo vendoring omitted the former workspace-external file. Horizon's 128 workspace tests, strict clippy, Nix vendored closure, and fresh clone passed. Synchronizer 0.3.0 `7d4494442446b18afe96b8fd96f10e1d80233591` removes Nota dependencies, derives, manual codecs, and its second count-based Horizon proposal parser; it pins final Protos/Datomic/Ethos-zero and corrected Horizon, owns an authored Schema plus byte-identical committed D3 anatomy, and uses typed Datomic Text edges for configuration, reports, release trains, and real Horizon role resolution. Its full tests (51 passed, one intentionally stateful network test ignored), strict clippy, Nix, clean fresh clone, generated contract, and release-train byte regeneration passed. Goldragon `55a17d934afa70e0877f8c04e3d4577ddc5d7600` replaces `synchronizer.dotos` with validated `synchronizer.datomic`; a one-shot pinned-old decoder/new writer changed only the required cluster-source `datom.nota` to `proposal.datomic` and emitted bytes identical to the committed result. No legacy product `.nota`/`.dotos` remains in the three heads. GitHub mains agree, workspaces are clean, locks 126/128 are released, and recoverable old evidence remains at `/tmp/goldragon-synchronizer.dotos.pre-datomic`.

Next-consumer inventory: among clean, independent, recently functional product consumers, `relative-age-display` was the safest bounded Dotos migration (one concentrated typed boundary, no in-estate Cargo consumers, no signal/meta coupling), followed by `claude-answers` and then broader `chronos`. `curriculum-deploy` is newer but changing its canonical Curriculum roles data requires explicit psyche approval under that repository's instructions, so it is not touched. Signal-bound Lojix/Agent/version-projection and the generic language/bridge repositories remain deferred.

Post-terminus `relative-age-display` migration complete: 0.4.0 `82f01006a86b166f4b1c1d86777a28e5ab88480c` replaces its Dotos boundary with exact final stack pins, a source-owned Ethos Schema, committed data-only Datomic anatomy, byte-exact regeneration, and a typed Text/Portion public edge. The fail-first witness, refusal/round-trip ladder, fmt, 20/20 locked all-target tests, strict clippy, full remote Nix build/test/doc/fmt/clippy, and exact fresh-clone test passed. Nix initially exposed that the old Cargo source filter omitted root `.ethos`; including it closed the hermetic source boundary. No Dotos/Nota legacy product references remain, the GitHub main and fresh clone agree, the workspace is clean, and lock 127 is released. `claude-answers` is the next active bounded migration.

Post-terminus `claude-answers` migration complete: 0.3.0 first landed at `118d9e78cb61` and final proof correction `e637388fb09d`. The CLI's single Query boundary now uses exact final pins and typed `Text<Query>`/Portion; Dotos dependencies/derives/product references are gone. Its authored Ethos Schema records Latest, All, Session.String, File.String, and recursive `Grep.{selection.Box<Query> needle.String}`. Because pinned Datomic has no generic `Datomic for Box<T>`, byte generator output is legitimately inapplicable; checked hand D3 preserves recursive public/domain semantics. The first map witness merely counted declarations, so release audit reopened lock 130 and replaced it with an exact fail-first AST witness for every variant, payload name/type, `Box<Query>` application, and Datomic embody/portion/textualize capability, tied to complete typed round-trip and malformed-shape refusal tests. Local fmt/docs/strict clippy/17 locked all-target tests, exact `nix flake check -L --no-write-lock-file`, and a clean fresh-clone replay of the same gates passed. GitHub main agrees, canonical/fresh worktrees are clean, and locks 129/130 are released. `chronos` is the next active independent migration.

Post-terminus Chronos migration complete: Chronos 0.2.0 `43703adb0c1d1fbb2d262eea7b9f79213d98e7f1` removes its Dotos text codecs while preserving the independent rkyv Unix-socket wire exactly. It pins final Protos/Datomic/Ethos-zero, owns `.ethos/data/chronos.ethos` plus byte-identical committed `datomic_library()` output, and uses checked hand D3 for semantic domain values and request/reply roots. An invariant-bearing `ErrorMessage` closes the previously panic-prone arbitrary-String outbound path. All six original behavioral suites were ported rather than discarded; 66 all-target tests pass, with the single documentation example intentionally ignored. Fmt, docs with warnings denied, strict clippy, exact full remote Nix, and a clean fresh-clone replay passed. GitHub main agrees, the feature workspace is clean, and lock 132 is released.

Two later-consumer migrations are active in parallel under exact path locks: CriomOS-test-cluster lock 133 is atomically converting three synthetic Horizon proposal fixtures after an exact historical four-root typed decoder at Horizon `214e6816...` proved current Datomic semantic JSON identity and all seven old/new projection viewpoints; CriomOS-home is beginning the bounded Chroma 0.3 configuration/pin migration, with psyche search and typed semantic preservation required before product edits.

Post-terminus CriomOS-test-cluster fixture migration landed: `33374c02d3c16c521bf863ce466590b5b389deb2` replaces its three synthetic Horizon `.dotos` inputs with canonical `.datomic`, pins Horizon 0.5.1 `f8c580...`, and regenerates all nine current proposal viewpoints. The new `alpha.json` and `beta.json` close an existing fixture omission for two declared hosted Pods already required by automatic VM derivations. A one-shot typed bridge at the exact compatible historical four-root Horizon `214e6816...` proved semantic JSON identity for all three proposals and normalized equality for the seven prior viewpoints; the current projection gate covers all nine. Both rejection checks, cluster contracts, full module contracts, flake evaluation, and a clean fresh-clone replay passed. Full `nix flake check -L --no-write-lock-file` remains non-green for two exact pre-existing downstream defects reproduced on baseline `6e34f9e5`: pinned CriomOS violates the cluster's source constraint through a comment containing `goldragon`, then pinned Spirit tries to fetch the removed standalone `nota-derive.git`. Neither constraint was weakened nor unrelated input mutated. GitHub main agrees, worktrees are clean, and lock 133 is released; those owner-chain defects remain explicit follow-up work.

Chroma producer acceptance reopened during the CriomOS-home consumer proof: exact Chroma `34e47a0...` uses Crane's `cleanCargoSource`, which excludes root `chroma.ethos`, while its committed `ethos_contract` integration test reads that file at runtime. Consequently the real consumer-built Nix package cannot run its own contract suite hermetically. Source-tree Cargo proofs remain green, but this is a producer packaging defect, not a Home failure. Home is held as an unpushed structured commit under locks 135/136; a Chroma patch is repairing and reproving the producer first, after which Home and CriomOS will pin only the corrected published revision.

Chroma producer acceptance correction complete: Chroma 0.3.1 `1b626d9dc325459be6c825d0c5a59a7d245d1edd` replaces `cleanCargoSource` with Crane's pruned Cargo source plus one exact root `chroma.ethos` regular-file filter, so the committed generation contract is hermetic without admitting unrelated files. Once that gate ran, the previously hidden sandbox ShellCheck/runtime path exposed a second portability bug: pure C-locale builders emitted literal `\\u201c` text from Bash Unicode escapes, causing typed Datomic to retain a nonexistent Ghostty template path. The sandbox now constructs canonical curly delimiters from locale-independent UTF-8 octets in ASCII shell source, verifies accepted Datomic operations, generated Light config, and two Ghostty reloads, and uses bounded 15-second readiness waits. Fmt, locked tests including both Ethos contracts, strict all-target/all-feature clippy, authoritative full remote Nix including sandbox-terminal, and a clean fresh-clone cached replay passed. GitHub main agrees, the workspace is clean, and locks 138/139 are released. CriomOS-home has repinned only this corrected published revision and is rerunning its consumer gates.

CriomOS-home Chroma consumer migration complete: `0ae8e4953f03bfcd007bf723d7f8547742de9300` pins Chroma 0.3.1 `1b626d9d...`, replaces declarative `config.dotos` seeding with canonical positional `config.datom`, removes the obsolete file during activation, renames and strengthens the config check, and updates the resident integration's stale `SetTheme.(...)` requests to generated `SetTheme.{...}` struct payloads. A typed old Chroma 0.2.5 witness embodied the exact legacy config and rejected the Datomic candidate; the new check now executes the actual Home activation output, verifies only the new file exists, and starts Chroma 0.3.1 through successful typed config decode. The resident Chroma/Emacs cycle, config runtime gate, and exact Chroma strict clippy passed. Unoverridden Home Nix fails only at its intentional missing-system stub; the legitimate projected full suite evaluates both new checks and then reproduces the exact pre-existing `orchestrate-wrapper-fallback` fixture failure on baseline `0530bcd...`. GitHub main and clean fresh clone agree, and locks 135/136 are released. CriomOS is advancing this published Home pin and its ownership witness under lock 140.

CriomOS Chroma consumer advance complete: `2ea09dd5625be0ad795ce8cb93f012a6739e1a7e` consists of a network-neutral comment repair plus the exact Home `0ae8e495...` pin and matching `lojix-ownership` expected revision; its transitive lock resolves Chroma `1b626d9d...`. The former `goldragon` literal no longer violates CriomOS-test-cluster's source constraint; residual cluster/host facts in three unrelated existing modules remain outside this bounded change. An initially reported green ownership build was corrected after its Nix client was found still live: authoritative targeted/full commands both exit 1 before reaching migration checks at the existing `MS2130 UVC patch must be reviewed for the selected kernel` evaluation guard. Exact baseline CriomOS `93fdd1d...` with the same temporary system projection reproduces the identical first failure. Clean fresh-clone static assertions prove Home lock equals ownership expectation and the transitive Chroma pin is exact. GitHub main agrees, all proof clients are gone, workspace/baseline/fresh clones are clean, lock 140 is released, and no deployment occurred.

CriomOS-test-cluster owner-chain propagation complete: `03d4952e9efceb445da9f792e77d3063d9b2a4d1` advances the direct CriomOS lock to `2ea09dd...`, resolving Home `0ae8e495...` and Chroma `1b626d9d...`. The lock audit found 262 changed nodes, all in the published CriomOS closure; root input names and every independent direct root lock remain unchanged, while four explicit follows move intentionally. All five Horizon/Datomic projection/rejection/cluster/module gates and whole-flake evaluation replayed green in a clean fresh clone. Source-constraints no longer reports the old LLM comment but now reaches pre-existing host facts in three upstream CriomOS test/user modules; full Nix still reaches the baseline Spirit lock's removed standalone `nota-derive.git` fetch. No constraint or unrelated pin was weakened. GitHub main agrees, workspace/fresh clone are clean, lock 141 is released, and only unrelated live lock 19 remains.

Further consumer audit: current `dotos-config` `6111a2bc...` is an explicitly frozen legacy reference whose public API is Dotos-specific and whose remote-main direct Rust consumer count is zero. A compatibility-free Datomic rewrite cannot preserve `InlineDotos`, `.dotos`, `DotosDecode`, or its installer macros; the honest future slice belongs in a selected live Persona component, not this frozen repository. Current Spirit `008d8ca0...` likewise has no safe leaf: its domain, ordinary signal, meta signal, and runtime are an atomic schema-rust/Sema/Dotos train; the existing `.ethos` drafts use unsupported Nexus/Sema roots and are explicitly psyche-unseen. Read-only audits made no source/lock changes.

Current-Spirit test-cluster advance was tested and deliberately abandoned without commit: a narrowly audited 47-node closure moved only Spirit to `008d8ca0...`, but the nspawn/runner gate still reached the removed derive fetch through the separate historical `persona-spirit` input. More importantly, the direct criome-auth witness built and booted the updated daemons, then current meta-Spirit rejected the historical seven-root NOTA Import because it now expects four root objects. No shim or fixture rewrite was inferred. The test-cluster lock was restored exactly to Spirit `9e413baa...`, lock 142 was released, workspace is clean, and evidence is retained under `/tmp/criomos-test-cluster-datomic-horizon-20260829/spirit-008d-witness-api-mismatch.md`.
