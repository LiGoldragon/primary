# Network-first refresh handoff — 2026-09-22

Owner of this documentation pass: Field Ultra Low Luna `c88918`. This is a
transport and work handoff only. It launches no seat, creates no Flow, and
changes no route, runtime, or source.

## Controller and successor boundary

Field High `03e825` remains the sole controller under retained Orchestrate
round lock `4494`. Its current Field High/Astra medium successor is the `03`
successor, not `6db4fe`. The old `03` route remains until the successor has a
worker-result relay and an explicit controller transfer. Sol `753e69` is the
executor and must provide the accepted native Codex profile, receipt-first
native context, own Flow ID, HM/Herdr binding, target-side route marker, and
explicit acceptance. Terra `0347d0` independently tests Sol's exact revision;
Luna documents and commits only the accepted exact tree.

The retained worker and ownership boundaries remain: root `6db4fe` owns the
Prometheus guest `4373`, network worker relay, and Curriculum `3825`; operator
`9ddcbc` retains `3776`; f72's locks `2836` and `2862`, Mind `3830`, and Low
`3847` remain held/unreachable as reported. No autoreap or implicit relay is
authorized. The fallback preflight is Primary `bf4200bc`; the pure pending
helper at Flow `481b6909` is not wired; the full MetaSignal assertion remains
blocked; the pending helper documentation is Primary `056510bd6`.

## Network-first work state

The bounded objective is to restore Prometheus/WiFi/network connectivity
through live, reversible Field fixes, with Sol implementing and Terra testing.
Mind's fresh network Nexus owner handles source and local-build work. Current
evidence says USB host health is present but no peer response is witnessed;
WiFi authenticates before receiving an IP; and no actual Lojix gateway JSON
exists. These observations do not establish a cause or authorize activation,
reboot, broad scanning, or a local fallback build.

The source and owner reports record the relevant evidence and open gates:

- `flows/753e69/reports/measured-refresh-owner-addendum-2026-09-22.md`
  records the Sol profile/receipt, the exact network observations, and the
  absence of a coherent typed gateway, installed parity, or E2E receipt.
- `flows/753e69/reports/network-path-plan-2026-09-21.md` distinguishes
  Ouranos's bounded local observations from historical peer claims and keeps
  the Prometheus downstream bridge separate from an unproved upstream path.
- `flows/4b0f60/reports/network-nexus-refresh-handoff.md` is the submitted
  Mind network-owner request; it is not a native acceptance or build receipt.

## Psyche emergency hold

Psyche High `wD:p9`, terminal
`term_65c138019438357`, UUID
`836818cc-83ab-4657-8b8f-1414f887559c` is HOLD/no-input because the original
process witness is missing. No input or retry is authorized. Psyche High
`1b8ac0` remains intact; Psyche Low `0625c3` and Medium `b80` are unchanged.
The hold is not a closure or refresh receipt.

## Typed-skills and acceptance limits

The design may preserve namespaces and foreign/unknown/testing outputs, with
selective generation restricted to proven owned outputs. It must not claim
all skills installed, native acceptance, route readiness, or production
deployment from a source proposal or report pointer. Existing captured round
addenda remain the provenance bundle; no secrets or private home data are
copied here.

## Sources

- Current living transport-only handoff and exact ownership/lock boundaries,
  received in the c88918 transcript on 2026-09-22.
- `flows/753e69/reports/measured-refresh-owner-addendum-2026-09-22.md`.
- `flows/753e69/reports/network-path-plan-2026-09-21.md`.
- `flows/4b0f60/reports/network-nexus-refresh-handoff.md`.
- `flows/03e825/refresh-round-20260922/field-medium/addendum-1.md` and
  `field-retained/addendum-1.md`.
- `flows/03e825/refresh-round-20260922/psyche-high/addendum-1.md` and
  `current-work-delta.md`.
- Primary revisions `bf4200bc` and `056510bd6` as supplied in the handoff.
