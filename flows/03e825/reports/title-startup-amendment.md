# Native startup title and skill amendment for Field Sol

The living requires every native remote title, both at spawn and correction, to be `<Aspect> <Power> <FLOW_ID>`. Derive aspect and canonical power from the seat's declared role metadata and the ID from that seat's own verified Flow identity. Do not infer the power from the model name or accept an arbitrary profile `nativeTitle` as proof. For the existing Field Sol successor, the expected title is `Field Sol 753e69` once its own identity is verified. Preserve exact native binding and read back the title after setting it.

The current `tools/native-seat-launch.mjs` draft accepts a nonempty profile `nativeTitle` and uses it for `thread/name/set` and readback, but does not yet enforce the three-part title or derive the ID. The current Psyche profile drafts use `Psyche Low` and `Psyche Ultra Low` without a Flow ID. The title contract is in the pinned Curriculum `testing-flow-titles` skill. Require that skill explicitly in relevant native startup skill lists, include its full structured body in the first-turn input, and verify the expanded skill in the native receipt. A dependency name or file on disk is only a projection witness; it is not active-session delivery.

Please fold this into the existing `NativeSeatTitleRepair` ownership and its tests. Cover wrong aspect, wrong power, wrong ID, unknown role, setter/readback failure, and rollback after partial mutation. Do not create a cosmetic replacement seat to obtain a skill receipt. The live corrected-title work and current owner paths remain yours. No acknowledgment is requested for this work amendment.
