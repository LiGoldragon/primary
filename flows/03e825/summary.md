# Field Astra 03e825

Native Field Astra, refreshed from `6db4fe`. Native model and effort preserved. The living verified startup context receipt before authorizing identity claim. Direct `flow-id codex --flows-root /home/li/primary/flows` returned `03e825`; a direct structured clock call supplied a harmless tool witness. The main flow received both initial and typed probes and acknowledged them. It read the predecessor's current handoff and accepted the offered ongoing work. These observations do not establish full paired Field readiness or retire a predecessor.

Remembered: 6db4fe — depth 1. Its supplied current handoff and directly read summary preserve ownership boundaries, retained routes, open title-script implementation, native skill propagation, runtime parity, and conditional cleanup gates. Predecessor report claims remain attributed to their origin.

The living's current request is preserved in `vision/remoteTitlesAndSkillDeployment.md`: stop expensive hash-bearing machine message noise, enforce remote titles as aspect, power, flow ID in spawning and correction, update flow/testing skills, deploy technical fix skills, and explain the actual deployment pipeline.

Current delegated work:

- `skill_deployment_audit`: read-only Luna investigation of authored sources, manifests, generation, runtime skill receipt, and deployment gaps.
- `titles_and_skill_fix`: Terra implementation of title sources and tests, with predecessor write ownership and Orchestrate coordination required before editing shared paths.
- `message_noise_fix`: Terra investigation of message source, bounded notice to stop gratuitous probes, and exact proposed instruction correction.

Preserve stable Codex app server and routes `7091ea`, `395aed`, and `9ddcbc`. No reboot, garbage collection, or predecessor retirement has been performed by this flow.

## Title and deployment results

The title implementation subflow reported Curriculum source landing `c0d89898` and Primary title-script landing `50e4b790`, both checked against the real remotes. The main-flow and correction skills now specify aspect, canonical power, and the seat's own Flow ID; the new `testing-flow-titles` skill tests spawning and correction. Eight script tests passed, including route mismatch and partial-write rollback. A fresh disposable subflow receiving the complete new skill chose explicit power over model name, withheld an unknown power, preserved a shared tab when terminal binding differed, and required rollback/readback after partial failure. This is behavioral prompt evidence, not native catalog injection or live adapter evidence.

The implementation subflow set and read back fourteen source-backed Codex main titles through the native app server. Routes and shared tabs were retained. Six other seats lack native IDs or have conflicting live bindings; two task-specific seats lack canonical role evidence. Claude pane observations found pending user input, with one running shell; two other recorded panes were absent. No Claude command was injected into those panes. These are specific remaining live-correction gaps.

The deployment subflow pinned Primary to the pushed Curriculum source and obtained `Generated.{ 66 21 }` and `Checked.{ 66 21 }` against that exact locked source. The deployment commits were pushed and remote ancestry checked. A focused pinned Nix gate could not execute because the configured Prometheus cache repeatedly timed out fetching metadata; retries were stopped without a local build fallback. Dedicated `canonical-title-fixtures` and `native-seat-fixtures` Nix checks are committed and pushed. The local canonical tests, both native Node fixtures, Claude Python fixture, Nix syntax parse, and diff checks passed; this does not claim Nix build execution.

Skill deployment has separate boundaries: authored Curriculum `skills/*.md` plus `roles.datom`; generated harness skill and role files; the consumer's pinned authored-source revision; and actual native skill injection into a running context. Current generation writes Codex skills under `.agents/skills`, Claude skills under `.claude/skills`, and role packets under `.codex`, `.claude`, and `.pi`. It does not write `.codex/skills` or `.pi/skills`. Dependencies are metadata, not automatic context injection. Old manifest wording and skill counts in documentation are stale; no missing manifest is needed to project the new skill.

The useful native startup amendment reached Field Sol `753e69` through one guarded active-turn steer after its queued copy remained unread. The redundant queued copy was deleted. The deployment subflow witnessed structured `main-flow` and `testing-flow-titles` input entries and the owner's explicit acceptance of the new naming rule. Expanded skill bodies were not witnessed after that steer; this is structured-input and target-read evidence, not a full-source expansion claim. See `reports/title-startup-amendment.md`.

Main caught a wrong `Field Sol` power example in that amendment. The testing skill's omitted enum definition was corrected first: both authored `main-flow` and `testing-flow-titles` now explicitly name High, Medium, Low, and Ultra Low, and distinguish Astra/Sol seat labels from power values. A fresh disposable replay rejected the wrong example and insufficient power metadata. The amendment was then corrected. The subsequent authorized `refresh` skill correction is included in the final pinned Curriculum source `99409107`, against which generation and checking again matched 66 skills and 21 role files.

After the prior source owner's lock cleared, this flow's implementation worker completed the launcher integration at Primary revisions `5a7225ba` and `636fae52`, each verified on the real remote. Codex now preserves receipt-only startup, requires the exact post-claim Flow marker/native identity, derives the canonical title, and requires native rename/readback before its title-ready receipt. Claude bootstrap remains title-pending until guarded post-claim finalization; a transcript title event is explicitly reported as UI-readback-pending rather than complete native UI readiness. The batch consumers were updated alongside both controllers. All three controller suites passed, including claim mismatch and rollback cases. A final direct native read by the worker found `Field Medium 753e69`, so no additional title mutation was needed.

The audited `flows/753e69` Psyche profiles are the sole launch inputs. The preparation worker removed its uncommitted duplicate drafts, separately validated Low then Ultra Low as one-seat plans, and witnessed all 10 Low and 11 Ultra Low source hashes matching. Its committed reconciliation is `reports/psyche-profile-preparation.md`. Main directly observed its executor has no `HERDR_ENV`; no launch, external caller workaround, route binding, or retirement occurred.

## Continuing handoff

The predecessor's final work amendment was received without an acknowledgment loop. Main read its updated summary, the raw messaging direction, and the lean-messaging audit. Predecessor source/projection changes reduce full identifiers in conversational prompts and unnecessary flow wakes; they preserve exact machine receipts and typed contracts. The amendment's claim of attached updated skills is distinct from a witnessed native input receipt. This flow has not claimed a new structured receipt from an ordinary file read.

Old `6db4fe` cleanup is now expressly held for active lifecycle coordination. Keep incumbent `7091ea` and protected crossover routes; ownership transfer and broader runtime parity remain open. The living-interaction/STT proposal remains unapproved.

The newer Mind composition and lifecycle requests authorize only a path/owner/reservation proposal and tests/generation gate. No new module mappings, composition reservations, edits, or deployment have been made for those requests. Existing title/deployment work is distinct. The lifecycle instruction preserves acquire as the admission boundary, pre-refresh admitted work, atomic new-admission gating on refresh, permit retention on ambiguity, RefreshHeld after release, and refusal of Ready while reconciliation remains pending. The named matrix is design-only; implementation and deployment grades must remain separate.

The read-only audit found newer actual ownership superseding the initial composition proposal: `6db4fe` holds authored `Curriculum/roles.datom` and `skills/flow-message.md` under lock 3825. Its FlowMessage outline has no callable API/install witness. The later Curriculum draft was deliberately not included in the authorized title deployment pinned to `99409107`.

For the existing lifecycle candidates, f72 retains the Flow store at `/home/li/wt/github.com/LiGoldragon/flow/night-messaging-0ab019/crates/flow-nexus/src/store.rs` under 2836 and Message queue/attempt/deadline/receipt files under `/home/li/wt/github.com/LiGoldragon/message/night-messaging-0ab019/src/` under 2864. The observed Flow candidate exposes `ManagesDeliveryPermits` with `AcquireDelivery`, `BeginRefresh`, `ReleaseConfirmed`, and `ReadyReattach`. Mind Medium holds the disjoint handler glue at `/home/li/wt/github.com/LiGoldragon/flow/mind-medium-delivery-2c61af/crates/flow-nexus/src/lib.rs` under 3830 and the typed producer at `/home/li/wt/github.com/LiGoldragon/signal-message/mind-medium-delivery-2c61af/ethos/signal.ethos` with its generated projection/test under 3831. These are candidate source observations, not deployed parity. An accepted exact adapter path/API for `e798f3` was not located in the bounded audit and is not invented here.

The composition schema's authored path is `/git/github.com/LiGoldragon/curriculum-deploy/curriculum-deploy.ethos`; `ethos-zero` generates `src/generated.rs`. Runtime composition and projection code are `src/roles.rs` and `src/runtime.rs`. The existing `roles.datom` modules are scaffolding, not an accepted new Spirit/Intent/aspect/power/programming mapping. Any new reservation requires agreement with the current owners; this flow made none for that new workstream.
