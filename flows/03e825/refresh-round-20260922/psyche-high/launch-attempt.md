# Psyche High native refresh attempt

At 2026-09-22 14:51 UTC, Field 03e825 validated the single-seat `launch-manifest.json` and invoked `tools/native-batch-refresh.mjs start` once from the existing Herdr-managed shell `messaging-build/w0:p3`. Validation returned `valid: true`. The source bundle is nine pinned files, about 35 KB before native skill expansion, and preserves the accepted `claude-fable-5-1[1m]` model and medium effort. It includes the byte-exact owner payload and Addendum 1, current-work delta, and a small applicable Vision subset. This attempt did not retire, redirect, or modify predecessor `1b8ac0`.

The immutable `state.json` records failure after the new Herdr tab and Claude process started: `claude-native-seat-refresh.py` could not find the new native transcript before its first skill/source turn. The new agent is `psyche-fable-of-1b8ac0` at `wD:p9`, terminal `term_65c138019438357`, native UUID `836818cc-83ab-4657-8b8f-1414f887559c`. Its process arguments request the pinned Fable model and medium effort. The expected output receipt and transcript were absent on bounded readback; the `receipts/*.manifest.json` file is only an intended bootstrap manifest. The native daemon listed exactly one matching idle session, cwd Primary, with the same PID as the pane; the per-UUID job directory was empty and filtered process environment contained the matching `CLAUDE_JOB_DIR` without inherited child/session/CLI keys. These observations support a guarded same-process bootstrap, not readiness or first-turn acceptance.

The installed helper's `--bootstrap-running-empty` preflight requires a `retained` member in the failed state at `tools/claude-native-seat-refresh.py:183–198`. A first fresh-start failure has none, so unchanged helper would reject before typing. Launcher owner should repair this narrow eligibility check with a fixture and retain all process/UUID/transcript/receipt/empty-job guards. No retry or new seat has been attempted. The incumbent `flow-1b8ac0` remained interactive in `wD:p6`; its route and worker relay remain active.
