# Field Sol refresh handoff — 2026-09-20

Incumbent: Field Sol `8565e8`, native thread
`01a0bb1c-5142-7763-9ea6-6638565e8508`, `gpt-5.6-sol`/medium, Herdr
agent `field-sol-of-1f96fc` in `messaging-build`. The successor's direct
predecessor is **8565e8**. A profile, receipt-only start, native identity and
tool witness, live HM route, readiness, and explicit acceptance are required
before ownership transfer. Do not infer predecessor retirement from a new
session. No successor launch or acceptance is claimed here.

## Lean first-turn sources

This handoff; `flows/8565e8/reports/morning-2026-09-20.md` for theme and
activation boundaries; and
`flows/8565e8/reports/lojix-schema-compatibility-addendum.md` for the current
Lojix chain and compile result. Load the role's native structured skills.
These source paths carry no deployment or transfer receipt.

## Open Field work

- **Herdr and Codex theme.** CriomOS-home source `7721387e` and CriomOS pin
  `aa5c151f` passed a focused remote Herdr check; live Home activation is held.
  The live regular legacy TOML and `message-daemon.service` collision require
  the guarded adoption/rollback in the morning report. Astra's same-UUID
  remote reattach corrected one pane's cached palette; a disposable one-turn
  Codex thread resumed with its history. The exact delete/archive route for
  that disposable thread remains unresolved. No live pane or Chroma change.
- **Lojix/Horizon/OpenCode dependency.** Provisional `signal-lojix` 3f550fc2,
  `meta-signal-lojix` a2a42e9d, Lojix consumer 34115703, and independent
  fallback-false 476bc56e are published on proposal bookmarks, not deployed.
  The bounded signal contract run reached Prometheus and exited 1 compiling
  `signal-lojix`; its log and source diagnosis are in the addendum. Horizon
  uses `datom-codec` 0.31.0 (`Composing`) while the signal contract uses
  0.26.3 (`Compositional`). Fix the coherent codec/derive contract, then
  rerun producer checks and prove the full pre-socket Home request before
  Realize or activation. The ouranos daemon was slow, not dead in this run.
- **Messaging adapter.** Mind's durable Message delivery hold remains open.
  Field's proposed Flow adapter scope is only `crates/flow-nexus/src/codex.rs`
  and adjacent tests; no lock or edit. Wait for the persisted typed
  DeliveryBinding/DeliveryHold API, CAS/permit/ack and quiescence semantics,
  and separately owned binding-generation persistence boundary before code.
  Mind owner is `9e7ea5`; writer `f72ab7` holds the Message/Flow candidate
  work. No production message pause or automatic reattach exists yet.
- **Other inherited work.** Field Astra `1cb440` owns lifecycle/launcher and
  OpenCode coordination. The read-only hygiene report is
  `flows/3acb63/reports/night-hygiene-2026-09-20.md`. Zeus/Prometheus update,
  kernel, and GC decisions need intended-generation evidence; no reboot or GC
  was performed here. Psyche home-directory/Drive ontology remains a
  coordination item without an accepted implementation.

## Current coordination

The living directly requested “refresh all flows, and land the vision last
proposed by fable” on 2026-09-20. Fable's five-part proposal is
`flows/f38926/reports/refresh-handoff.md`; Field Sol submitted that direct
approval to Fable `f38926` via HM and asked Field Astra `1cb440` to coordinate
successor launches. HM submission is not a read receipt. Fable was asked to
own the Vision landing; Field Sol has no competing Vision edit. The current main-seat
set reported by Fable is Fable `f38926`, Opus `b81560`, Mind `9e7ea5`, Field
Astra `1cb440`, and Field Sol `8565e8`. Older Mind seats `0ab019` and
`98ac2e` are crossover only. Do not treat stale Herdr registrations as live
flow owners.

No build, deployment, Home activation, reboot, GC, or daemon change is running
from this seat. No active Orchestrate lock is intended to cross transfer.
