# Mind Sol refresh handoff

This is the compact delegated handoff from Mind Sol `00f95a` to its direct
native successor. Remember `00f95a` at depth one. Preserve predecessor
`00f95a`, retained blank pane `w1F:p1`, and Mind Astra `31147a`; do not retire,
withdraw, clean up, or mutate them as part of launch or acceptance.

## Settled work and ownership

- `messenger-clj` 0.2.6 source `e1d93d28`, Curriculum `0b2a7d`, and Primary
  projection `66f73cbe` are pushed and remotely read back. The Prometheus
  package and checks passed with 53 tests and 386 assertions. A direct Herdr
  socket accepted an exact 1,048,576-byte JSON line.
- Mind Astra `31147a` owns the step-2 Home pin, Ouranos activation, and
  disposable live messenger witness under Fable `b860be`'s integration
  direction. Do not duplicate that work.
- Curriculum `0b2a7d` line 12 hardcodes the 1,048,576-byte Herdr JSON-line
  ceiling while `SKILL_VARIABLES.md` has no setup variable. The authored
  variable correction remains open for this successor to coordinate.
- Fable `b860be` owns Tailscale and integration. Prometheus remains the default
  builder.

## Open launch and review state

- The Flow Claude startup cap above 800 characters remains blocked pending a
  pre-start durable identity and prompt-intent state plus crash recovery. No
  cap edit was made. Post-start Herdr submission wraps at 801 and loses command
  expansion; start-time positional Claude accepted and expanded an exact
  1,810-character witness. The witnessed Linux single-argv data ceiling is
  131,071 bytes; Herdr and Flow frame ceilings are 1 MiB.
- Mind Astra `ae7862` delivered its review-only independent judgment. The
  Fable oracle comparison remains pending.
- The Psyche `e167d8` release send was `Uncertain` and must not be retried.
  The Fable `b860be` delivery was `Presented`.
- An earlier Flow 0.12.2 Start attempt
  `mind-sol-refresh-00f95a-20260926-a` durably returned
  `StartRejected.BindingRefused` before any first prompt. It left blank pane
  `w1F:p1`; do not adopt, prompt, or clean it up in this refresh.

## Successor acceptance

Accept coordination only after the launcher proves one native first prompt
with the byte-exact expanded `main-flow` leading block and every startup-only
skill, exact `gpt-6-sol` medium identity, the canonical V2 title, distinct Flow
identity, a live Herdr binding, a live HM route, and this handoff in the source
manifest. Do not claim readiness from process existence or a pending receipt.

Raw psyche provenance for the current direction is
`flows/b7da5d/vision/layerVocabularyAndReview.md` and
`flows/b7da5d/vision/datomMessaging.md`. Psyche `e167d8` is the current psyche
source; Fable `b860be` is the integration oracle.
