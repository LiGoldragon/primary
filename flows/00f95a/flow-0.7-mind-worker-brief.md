# Flow 0.7.0 Ouranos deployment worker

You are one native Terra implementation subflow of Mind `00f95a`.

`$subflow FLOW_ID=00f95a FLOW_DIRECTORY=/home/li/primary/flows/00f95a`

Mind is liable for this one-time deployment dispatch and its outcome. The
living's standing deploy-now and decentralized-redeploy direction permits this
execution because designated Field `504461` is finished, no concurrent worker,
build, deployment job, or consumer lock was observed before dispatch, and the
Psyche High route session was not found. Do not create a Flow identity, title,
or another worker. This is the only Flow 0.7 deployment attempt authorized by
this brief.

## Fixed outcome

Activate canonical Flow `0.7.0` revision
`812053c5c7c8ee41f56f59712feee95f27a1a680` on Ouranos once, through one
coherent declarative Home/CriomOS consumer chain and a typed Lojix activation.
Then establish bounded acceptance evidence: the installed binary reports
`0.7.0` through `--version`, `List` succeeds, and one disposable `Start` is
accepted. Use no stale `00869477` revision and never duplicate deployment.

## Boundaries

- Recheck exact Orchestrate locks and runtime jobs before touching a consumer
  or submitting activation. Stop and return a receipt if another Flow 0.7
  build, worker, deployment, activation, or overlapping consumer lock exists.
- Keep lock `6094` (`HmPythonRegistryFreeze00f95a`) intact. The heartbeat is
  masked and remains so until a typed reader's Home pin and activation are
  actually verified. It is outside this deployment unless the selected
  coherent consumer explicitly requires read-only gate evidence.
- `5f38bc`'s separate messenger consumer activity stays paused. Do not change
  messenger-clj, legacy locks, Flow source or contract revisions, or tests
  beyond the selected coherent consumer activation scope.
- Do not improvise a builder, transport, proposal, request shape, or Lojix
  backend. Inspect the current typed Lojix/deployment interface and return the
  exact unavailable-interface or builder blocker before mutation.

## Starting evidence

- Candidate consumer commits are Home `e6f60a145baadd16ddb1ec05caa6cb3ba876e9a9`
  and CriomOS `58b4c6b98263fbf633258a9a14cf6e3ea9909c7`. They are candidate
  evidence only. Independently inspect their exact changes and current
  upstream state before accepting, merging, or replacing either.
- No installed version, successful `List`, accepted `Start`, build, or typed
  activation receipt is established by this brief.
- Source handoff and prior observations are in `flows/504461/log.md` and
  `flows/00f95a/messages/2026-09-25T20-29-00-222906592Z-504461-8503a952-890f-4b3a-9b8b-75fa4767947c.md`.

## Work

1. Inspect consumer topology, candidate diffs, locks, running jobs, and
   deployed Ouranos state. Select one current Home/CriomOS integration path;
   record why its pins agree. Claim only its required files and activation
   boundary.
2. Make the smallest integration/repin needed for the fixed revision, build
   through the supported Nix path, and invoke only an inspectable typed Lojix
   activation. Do not activate twice.
3. After activation, read the installed Flow binary and service. Verify
   literal `--version`, perform `List`, and submit one cheap disposable
   `Start` with a bounded prompt. Record the returned Flow identity, native
   prompt acceptance/readiness evidence, and supported cleanup result.
4. Run proportionate checks. Commit and push only files you own after commit
   scope verification; read the remote revision back. Preserve unrelated work.

## Return to Mind `00f95a`

Return a concise durable receipt with chosen consumer base and exact
revisions; lock observations/releases; changes, commit and remote readback or
why none; build and typed Lojix request/outcome; installed binary/service
path; literal version/List/Start/cleanup outcomes; and all blockers with their
source command or file. State plainly if activation did not occur.
