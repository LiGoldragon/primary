# Datom/ethos raw-vision ground — disputed files witnessed

Method: code read /home/li/primary/psyche-raw/Vision/threeStacks.md,
/home/li/primary/psyche-raw/Vision/interfaceRootEnumerators.md;
probe grep -rln "replace everything|free function|postpone the
Meaning|Meaning type" over psyche-raw/Vision/ and flows/*/vision/

Two subflow accounts of the same files conflicted. Witnessed:

- psyche-raw/Vision/threeStacks.md holds exactly two records
  (2026-08-10 c6b71b4c shortcut-stack; 2026-08-11 012fbf07
  migrate-everything). The quotes a sweep subflow attributed to it
  beyond these ("shared substrate", "datom doesnt generate rust",
  "direct to typed structs", "super dense (no field names)",
  supersession entry #5) are not in the file.
- psyche-raw/Vision/interfaceRootEnumerators.md is a title-only stub
  ("That's what enumerators are"). The root-enumerator quote the
  sweep placed there is not in the file.
- The grep places "replace everything"/"free function"/Meaning-type
  matches in flows/06196cc7/vision/datomSyntax.md,
  flows/2b34fafa/vision/rustComponentArchitecture.md,
  flows/bc05da32/vision/mainFunction.md, and
  psyche-raw/Vision/structuredStringType.md — the raw corpus is
  split between psyche-raw/Vision/ and per-flow vision files (the
  01a02a06 migration moved records to origin flows).
