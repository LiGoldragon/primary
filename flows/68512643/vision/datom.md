# Datom distillation review corrections

## 2026-08-23, 68512643-8

Context (agent, brief): three corrections given while reviewing the
datom distillation statements. On the name (statement 1):

> chosen for it's energetic power

On the migration line (statement 4, which read "the old stack —
Schema and NOTA"):

> schema is the abandoned ancestor of ethos, not datom. so this line
> could be ambiguous, and misses dotos, the other temporary name
> Nota took

On the interface shape (statement 6, which read "each variant's
data its options"):

> another enum if sub operations are wanted, and strut or vector for
> final options. a struct could embed further sub operations even,
> or any combination imaginable really.

— psyche, 2026-08-23 (Designer session 68512643), typed.
