# Nexus instead of daemon

## 2026-08-23, 68512643-6

Context (agent, brief): ruling given while reviewing the
ethos-monolith distillation's daemon wording; also directs a search
for the existing wording on Nexus being the component.

> search the wording on Nexus being the component. going forward
> Nexus by itself (versus Nexus kernel, the runtime engine) is
> synonymous with our specifically designed "daemon" - so we will
> use Nexus instead of daemon. and the executables will be
> <component>-nexus instead of -daemon.

— psyche, 2026-08-23 (Designer session 68512643), typed.
