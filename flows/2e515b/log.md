# Field Sol 2e515b

- 2026-09-24: Direct recovery startup from the already-proven next Codex server; this is not a Flow-mediated launch. Launcher first-prompt receipt was reported recorded by the living. `flow-id codex --flows-root /home/li/primary/flows` claimed `2e515b` after the prior host fault was repaired. The predecessor `9ddcbc` is retained.
- Startup gates in progress: exact native model and effort readback, title readback, live route witness, and predecessor depth-one remembrance. The current living recovery instruction supersedes the handoff's earlier EB/Luna prelaunch sequence.
- Remembered: 9ddcbc — depth 1. Its retained handoff and final native response show a context-cost stop, no implementation resumption, and a required `gpt-6-sol` medium successor. Its earlier Flow-first sequence is superseded only for this narrow direct recovery. Predecessor routing and retirement remain untouched.
- Root native transcript `01a0d4f4-34a7-7c91-8524-01a2e515b827` reports `gpt-6-sol` at medium effort. A `gpt-5.6-terra` context belonged to a child worker, not this main. Initial prompt acceptance, exact title, and live route readbacks remain under verification.
- Read-only recovery workers were dispatched for handoff and identity checks and for Flow/Herdr/Home deployment inventory. The installed Flow service is still 0.3.0; coherent deployment and acceptance remain open with their existing owners.
- Startup blocked: native transcript inspection found multiple initial user records and a composite prompt that does not byte-match the selected `main-flow` leading block. The `BOOTSTRAP_READY` response was accepted, but it is not the required one-block/source-match receipt; ambiguity does not permit resending it in this session.
- Title `Field Sol 2e515b` is the correct model-derived target but was not set or read back: the protocol exposes `thread/name/set`, while the available supported live proxy returned no response. This root has no `HERDR_ENV` managed-pane identity, so exact HM/Herdr binding and route readback are deferred. No predecessor was changed.
