# Flow 7648a0

Working instruction: remember `d97d50` at depth 1 and use its transcript; update ChatGPT Desktop to the latest release before debugging, make it reliable on Wayland, prove no old Desktop/Codex adaptation remains, add the update-first debugging rule to the correct authored skill and regenerate projections, and complete related bead `home-273`. This prompt creates no psyche record.

Remembered: d97d50 — depth 1. Its log, two reports, and runtime witness were read; it has no psyche records. Child flow `/root/runtime_crash` directly observed six 26.831 `SIGILL` coredumps in the ChatGPT Git worker at Node `GetNodeReport`/`TriggerNodeReport`, including a fresh reproduction. Child flow `/root/failure_mechanisms` mapped the stock bundle's sole `process.report.getReport()` occurrence to `detect-libc` under `@parcel/watcher` and the Git working-tree watcher, matching the retired `SKIP_PROCESS_REPORT` patch target, while leaving the exact dynamic caller unproved. Child flow `/root/declarative_state` directly observed that live deployment 149 still selected ChatGPT 26.831 and that OpenAI's signed repository had advanced to 26.901. Fresh temporary-profile Wayland, X11, and GPU-disabled starts survived their 15-second bounds, weakening a generic Wayland/GPU cause; existing profile state remained a hypothesis. The remembered flow made no changes and left update, repair, authenticated long-lived runtime stability, new-chat behavior, and same-thread resume open. The configured `transcript` helper is absent from `PATH`; recovery of the originating harness record and last model response is in progress.
