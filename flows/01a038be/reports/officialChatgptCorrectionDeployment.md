# Official ChatGPT Linux correction is deployed

Deployment `63` completed for `li` on Ouranos. The obsolete converted
Codex-Desktop integration has been removed from the active Home generation.
The medium graphical projection now uses the official ChatGPT Linux package
from the audited `llm-agents` input, while retaining the one shared Codex CLI
derivation for the terminal and the ChatGPT wrapper.

The activation exposes `chatgpt.desktop`, makes it the default handler for
`x-scheme-handler/codex`, and preserves the prior Claude Desktop callback
registration. No old remote-control, mobile, Computer Use, or shared
app-server feature claim remains: the official Linux preview does not provide
that former integration surface.

## Evidence

- Home source and focused Prometheus-only proof: `2fb9f089fafb`.
- CriomOS exact consumer pin and Prometheus-only top-level proof:
  `e8b3e8e9951b`.
- Lojix terminal node-ledger result: submitted deployment `63`, terminal
  record `1489`, `Completed/Succeeded`, and Current.
- Active profile verifies official ChatGPT `26.818.61809`, shared Codex CLI
  `0.149.1`, the ChatGPT MIME default, unchanged Claude MIME default, and both
  URI-capable desktop entries; see the
  [deployment witness](../witnesses/officialChatgptCorrectionDeployment.md).
- [Living's stateful-installation ruling](../vision/installingSoftwareStatefully.md)
  remains in force; this was a declarative activation, not an installer.
