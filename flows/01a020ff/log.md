# Emacs theme-projection investigation

Investigated Emacs missing an otherwise successful Chroma Dark projection. The
final boundary is an unacknowledged one-shot `emacsclient` adapter that erased
the exact rejected condition; a literal unexpanded Lisp path is an additional
defect, not the proved immediate cause. No source or live mutation occurred.
