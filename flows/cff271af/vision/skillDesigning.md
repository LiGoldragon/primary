# Skill designing

## 2026-08-22 — nexus becomes software-design; everything runtime is a Nexus; libraries remain

Context (agent, brief): the flow had listed the approval stack and
noted the tuple lines were destined for the software-design draft,
not the nexus skill, per the overlap-deferred ruling. The psyche,
verbatim:

> let's review it all then. I think nexus becomes software-design,
> as we design everything using a nexus going forward (the runtime
> part of course; libraries are still needed sometimes like with
> datom, and maybe others you can name (trait libraries))

Carried: the probable merge foreseen 2026-08-22 ("we'll probably end
up merging them") now given direction — nexus merges INTO
software-design; the Nexus is the runtime shape of everything we
design; libraries stay as a category (datom named; trait libraries
suggested; the psyche invited the flow to name the kinds).
