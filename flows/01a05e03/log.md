# Wispr Flow Linux modifier-release repair

This flow repairs the Wispr Flow Linux helper so input injection cannot leave `LEFTMETA` pressed, adds a behavioral regression test, advances the CriomOS package pin, deploys the immutable result through Lojix, and verifies the live modifier state.

Open: remember the relevant flows and psyche evidence; isolate and lock both repositories; reproduce the failure; implement, test, version, land, deploy, and inspect live state.

Remembered: 01a05d25, 01a0539e, 01a05209, 01a05588 — depth 1. The live EVIOCGKEY witness isolated held `LEFTMETA` to the `Wispr Flow Linux Helper` virtual keyboard; keyd reset and external release injection did not clear it, while restarting only the helper replaced the virtual device and immediately restored the desktop. The prior helper source diagnosis points to `UInput::chord()` restoring physically held modifiers onto a different virtual device and also identifies early-return cleanup as an unproved second risk. Wispr is a contingency/future Listener backend; this repair must not reopen Status-overlay work or read the authenticated profile. Producer packaging is pinned through the Wispr Linux package and then CriomOS/Home, with immutable producer-before-consumer landing and Lojix deployment. The documented `transcript` command is unavailable, so the last responses were recovered by bounded role-filtered reads from the matching Codex session JSONL files; 01a05d25 has no flow directory.

Open: establish current repository/workspace/lock and live state; reproduce the causal source hypothesis with a failing behavioral test; implement, version, land producer before consumers, deploy, and prove the helper virtual device has no held modifier after the Meta path.

Correction: the first helper Nix-check invocation used the configured remote builder list but omitted `--max-jobs 0`; Nix therefore started some local fixed-output derivations even while the Rust dependency builds were dispatched to Prometheus. The invocation was interrupted. All further Nix builds explicitly disable local jobs and name the configured machines file.

Witnessed red/green: the pre-fix regression left `LEFTMETA` held on the helper virtual device when Meta was physically held elsewhere, and a forced write failure left synthetic `LEFTCTRL` held. The repaired event path first releases physically held modifiers on its own virtual device, never restores physical state there, and unwinds every attempted synthetic key/modifier while preserving the first error. All 10 Rust tests, formatting, clippy, and the remote-only Nix package build pass. Helper commit `d750541ab0ef0702db9258e3dece2eb130b35af8` is pushed to `LiGoldragon/helper`; immutable release `v0.1.3` was published by successful workflow run `33542140243` for x86_64 and aarch64.

Correction: the first packaging draft used raw release-asset SHA-256 values for the helper URLs. `runtime-inputs.nix` sets `executable = true`, so Nix checks the executable NAR serialization instead. The remote contract build exposed the mismatch before commit; the pins were replaced with hashes produced by `nix store prefetch-file --executable` and the contract is being rerun.

Producer and consumer landing completed in immutable order. Packaging commit `003ccc2890c1709c7e966e96db5d4cde5ee82813` pins fork release `v0.1.3`, adds a runtime version contract against the auto-patched binary, documents modifier ownership, and passed the helper contract plus Linux patch check on Prometheus; the existing standalone runtime-input contract still requires the proprietary installer by design. Home `fa476c9e00b81fa025739bd20eba32108228d055` pins that packaging commit, and CriomOS `7cd12262874fc5f6c1ed133dc3ef56c669d29959` pins Home. All three main bookmarks are pushed. Direct push to `wispr-flow-linux/helper` was refused by upstream permissions, so the helper commit and release are on `LiGoldragon/helper`, and upstream pull request 11 carries the same commit.

Deployment and live witness completed. Lojix Ouranos CompleteHost deployment 137 (`Evaluate`) and 138 (`ActivateNow`) both terminalized `Completed` / `Succeeded` for exact CriomOS revision `7cd12262874fc5f6c1ed133dc3ef56c669d29959`. Ouranos is generation 178; `/run/current-system` and the persistent system profile resolve to the same new closure. Activation did not restart the already-open Wispr process, so its declared transient user service was restarted once through `/home/li/.nix-profile/bin/wispr-flow`. The resulting live helper is version `0.1.3`, has the published x86_64 asset SHA-256, and is owned by the new package closure.

Final modifier proof: a transient instance of the deployed binary inside the live FHS namespace acknowledged a real uinput `SimulateKeyPress` using synthetic `Command` plus otherwise inert `F24`. While both virtual keyboards existed, EVIOCGKEY reported `none` for each Wispr device and every physical/virtual input device. After the transient instance exited, the production helper alone remained at `0.1.3`, and a second all-device probe again reported `none`. No reboot occurred.
