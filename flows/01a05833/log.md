# Flow 01a05833

Deploy the current Codex/Claude generation to Ouranos and Zeus after remembering flow `01a05487`, auditing CriomOS worktree sprawl, and verifying that the prior updates were correctly implemented and remain current upstream.

Completed:
- Recovered typed deployment authority on Ouranos through the explicitly approved runtime start of the already-declared unit and deployed the updated Codex/Claude generation there.
- Corrected the reusable per-profile remote-control workspace and Claude trust migration exposed by Zeus's materialized and live activation gates.
- Deployed Zeus through typed Realize, TestActivation, and ActivateNow gates and recorded the session-continuity outcome on both hosts.

Remembered: 01a05487 — depth 1. The persistent-server direction is shared Codex/Claude service ownership rooted in `primary`, without introducing Nexus. Existing Codex clients do not transparently migrate across a server activation; cold resume may work after a clean turn and writer-lock release, but shared multi-owner `CODEX_HOME` continuity was not proven. Claude continuity remained unknown.

Constraints carried from written psyche: Codex is declared once and the same derivation is consumed everywhere; Claude Desktop must use the declarative Claude Code rather than mutable self-installed state; deployments use the typed Lojix/meta-Lojix interface, not setup-specific scripts or ad-hoc flags.

Audit findings: upstream Codex `0.151.0`, Claude Code CLI and VSIX `2.1.251`. CriomOS-home main correctly carried the CLI packages but its VSIX remained `2.1.250`; CriomOS still consumed an older Home revision with Codex `0.150.1` and Claude `2.1.250`. Worktree audit found 29 clean/main-contained/no-lock/no-live-process physical workspaces safe to retire and 31 to preserve. Ouranos live owners remained on the older packages. Zeus's Codex owner PID changed during read-only probes for an unknown external cause.

Worktree cleanup completed: the 29 audited-safe registered workspaces were forgotten and their exact physical directories moved to recoverable Trash with restore metadata. No audited source path remains; 31 uncertain/substantive physical directories were preserved. Registered counts after cleanup: CriomOS-home 18, CriomOS 28.

Control-plane findings: installed Orchestrate 0.26 expects scalar `Release.<id>`; the braced skill spelling faulted. Lojix 0.20 has a witnessed product/scalar adapter mismatch for `ByDeployment` and the same latent defect for `ByGeneration`; source needs behavioral regression tests and a declarative update before deployment terminal state can be trusted.

Source integration completed: CriomOS-home commit `0548644f797c` pins the official Claude Code VSIX `2.1.251`. Lojix `0.20.1` commit `8bb4b0a1c781` repairs both product adapters; remote regression went red on the prior EOF and green 2/2 on real daemon/client exchanges. CriomOS commit `25ebf8b8a417` pins both producer revisions and passed static metadata, formatting, and closure assertions. Full materialized Realize and live deployment remain.

Deployment progress: Ouranos UE Realize 106 failed because it incorrectly targeted standalone Home; corrected consumer-composed UE Realize 107 succeeded. CompleteHost Realize 108 succeeded. TestActivation 109 stopped `lojix-daemon` from its own service cgroup and did not replace it or record terminal state; current system generation remains old. Codex/Claude owners and all 59 observed Codex socket peers remained unchanged across the failed boundary. No ActivateNow, user activation, Zeus deployment, reboot, or direct runtime recovery followed.

Self-activation diagnosis: same-host ActivateNow already hands activation to a PID-1 transient and reconciles the live profile/closure on successor startup. TestActivation remained on foreground self-SSH; the candidate stopped `lojix-daemon.service` with `KillMode=control-group`, killing the activation child before replacement or terminal record. The source correction will extend the existing handoff to TestActivation with test-specific transient-result reconciliation and no persistent-profile inference. Typed authority recovery still requires explicit approval for the reversible runtime start of the already-declared daemon unit.

Self-activation correction landed: Lojix `0.20.2` commit `34a8e9c2e6af` routes same-host TestActivation through the canonical PID-1 transient `<deployment>.service`, observes registration/result through systemd D-Bus subscriptions, lets a successor reconcile the retained unit, terminalizes before cleanup, and leaves the persistent profile unchanged. The real typed NixOS VM went red on `0.20.1` and green on `0.20.2`; 92/92 library tests passed. No schema or public wire change. Reboot-era missing-handoff policy remains an explicit design question.

CriomOS commit `2719b05354c07018062e4a32161199a9f1229e4b` now pins Lojix `34a8e9c2e6af3d6dbc2b8ad83c43758f5fdb16ca` while preserving Home `0548644f797ca873cada2309a64c46aad4e318a0`. Package eval, exact metadata pins, changed-file format, and lock archive closure passed; full no-build flake check reached the intentional unmaterialized `system` stub. Typed authority remains offline after deployment 109; the narrow recovery is the explicitly approved runtime start of the already-declared `lojix-daemon.service`.

Authority: the living explicitly approved `systemctl start lojix-daemon.service` on Ouranos and confirmed root SSH access on all hosts. The restored 0.20.0 controller will Realize the new immutable revision, then cross directly with its already-supported same-host ActivateNow handoff; it must not retry the known-broken old TestActivation path before 0.20.2 is live.

Continuity report during Ouranos ActivateNow 112: the living reported that the remote session was killed and had to be restarted manually. This is a direct claim pending process/journal correlation; current remote access is recovery, not continuity. Zeus remains held until Ouranos terminal state and successor health are witnessed.

Zeus rollout: Ouranos authority is live on Lojix 0.20.2. Both Zeus root endpoints were witnessed as the same machine; no `li` user manager/session/owner was running. CompleteHost Realize 113 failed before build/activation because materialized evaluation asserted that the enabled Claude Remote Control policy requires an explicit absolute non-home workingDirectory. The direct `deploy zeus` instruction authorizes the necessary reusable profile/cluster source correction rooted in the configured Primary workspace; Zeus remains untouched until that correction is pushed and the exact eval is green.

Zeus materialized-eval repair: the live Bird account home is `/home/bird`, and `/home/bird/primary` already exists as a Bird-owned 0755 directory; no directory was created. CriomOS-home commit `7e75f38ee9f006b3ef0381e447f709187b8c66df` makes an enabled Claude Remote Control profile derive its default working directory as `${config.home.homeDirectory}/primary` while still rejecting the account home itself. CriomOS commit `4d2b922973f915681a5bc01ad2409206706d95e4` pins that Home revision, removes the `li`-specific injection, and covers two keyed users in the consumer contract. The exact four-input Zeus top-level evaluation that failed on `2719b053` terminated green on `4d2b9229`; no build or host mutation occurred. A broader scoped check exposed pre-existing composition drift, repaired stale producer expectations and a missing users-module fixture, then stopped at the unrelated existing MS2130 kernel-review assertion. Both repositories are pushed and clean, and edit locks 452/453 are released. Typed Zeus deployment has resumed from the green immutable source.

Zeus activation stop gate: CompleteHost Realize 114 succeeded. Before activation, Bird had user-manager PID 3384, two active sessions, Codex Remote Control PID 3413 with an active control socket, and several active Codex/Claude clients. TestActivation 115 terminalized `Succeeded`, and Bird's manager/sessions survived, but the old Codex owner/client/socket stopped. The candidate Codex 0.151.0 unit restarted with status 200/CHDIR because its rendered WorkingDirectory was still the literal `/home/li/primary`; Bird cannot traverse `/home/li` while its own `/home/bird/primary` is accessible. The candidate Claude 2.1.251 unit used `/home/bird/primary` but restarted after an explicit workspace-untrusted refusal. The persistent profile remains the old generation, as required for TestActivation. No ActivateNow was submitted. Authored-source trace found the Codex literal in Home's agent-intercom profile and no declarative Claude project-trust entry; a reusable account-derived unit/trust merge correction is underway.

Zeus final correction and deployment: successive live TestActivation gates exposed and stopped on three real declarative defects before persistence: the Codex unit's Li-literal working directory, obsolete scalar Claude trust, and Hexis's inability to traverse that legacy scalar during migration. The final authored repair is CriomOS-home `122bc1d10be7abb4d75542720a1e7c6594fb88bf`, consumed by CriomOS `c1ad61aee2a38a2f813cad6b62421f9ae7503fac`. It derives each service workspace from the profile home, declares structured Claude trust, and runs an idempotent pre-Hexis canonicalizer only for scalar legacy entries; existing project objects remain byte-identical. The generated-script contract captured the exact old Hexis failure and then proved scalar migration, the unchanged Hexis merge, sibling/unrelated JSON preservation, and two-user behavior. Exact four-input Zeus top-level and materialized Bird/Li activation-script gates passed. The broader ownership check remains blocked only by the unrelated pre-existing MS2130 kernel-review assertion.

Typed Zeus deployment completed: CompleteHost Realize 120 succeeded at ledger 3005, TestActivation 121 succeeded at 3043, and ActivateNow 122 succeeded at 3081 (latest query 3085). `/run/current-system` and the persistent system profile both resolve to the final closure. Bird and Li Home Manager units succeeded. Bird's user-manager PID 3384 and sessions 4/3 survived the final test and persistent activation; corrected Codex PID 521513 and Claude PID 543072 also remained unchanged across the final 16-second post-activation window, active/success with zero restarts, correct `/home/bird/primary`, and a live Codex control socket. Direct versions are Codex 0.151.0 and Claude Code 2.1.251; structured trust is present. Bird's declared min profile intentionally excludes VSCodium extensions, so the VSIX is not applicable on Zeus; the official 2.1.251 VSIX remains locked and packaged for medium-profile consumers. Ouranos Lojix 0.20.2 is active with ordinary/owner sockets and store. No reboot or manual runtime fix occurred during the final Zeus rollout.

Continuity conclusion: the final Zeus activation preserved the observed Bird sessions, manager, and corrected service PIDs. An earlier failed Zeus TestActivation did stop the old Codex Remote Control owner/client/socket, so end-to-end continuity was not preserved across the entire remediation sequence. Separately, the living directly reported that the Ouranos session during ActivateNow 112 was killed and had to be restarted manually; current access is therefore recovery, not continuity.
