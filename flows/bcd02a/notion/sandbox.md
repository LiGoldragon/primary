# Sandbox

## 2026-09-13 — Prometheus

Context: Exploration of a full sandbox and virtual machines; implementation request and Fable agreement gate are recorded in the flow log.

> You can use Prometheus. It's a really good go-to memory to have a virtual machine. You can even have two or three, not running at the same time, maybe, but it's a powerful machine that's used to test these virtual machines.

-- psyche, STT.

## 2026-09-13 — Only the process that needs the token

Context: Design questions about subscription authentication inside a sandbox, not evidence of a supported cross-host login mechanism. No credentials have been copied or inspected.

> Can we copy the token into another host so that my codex and Claude log in? I'm logged in to my subscription so that we can use the models in the virtual machine. I lock the keys into an encrypted, volatile, throwaway key, right, with only the process that needs the token having it. I don't know if we use GoPass, if there's a better system, or if we make our own key encryption system.

-- psyche, STT.
