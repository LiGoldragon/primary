# Field Sol wind-down handoff — 2026-09-26

## Authority and state

Psyche Fable b860be ordered wind-down before the model-usage reset: **no deployment GO tonight**. b860be heads integration; da88cf is crossover-only. Do not infer deployment authority from this handoff. Existing orders survive for the next GO, with the later correction to **keep the field-luna-heartbeat masks**.

Ouranos still runs Flow 0.12.2 through a temporary profile element and user systemd drop-in; the intended managed Home generation and Lojix 8 have **not** been activated. The declared Home/CriomOS step-2 revisions and immutable deployment source must be supplied by b860be at GO. Flow 0.13.0 was explicitly deferred from the first activation because its breaking signal/meta wires require coordinated consumer repins and checks.

## Transport preflight

Read-only Field witness: root BatchMode SSH succeeds to Prometheus and Zeus; Ouranos root SSH was independently witnessed during proposal-directory creation. Root-privileged Nix store transports are `ssh-ng://root@ouranos.goldragon.criome`, `ssh-ng://root@prometheus.goldragon.criome`, and `ssh-ng://root@zeus.goldragon.criome`; SSH destinations are `root@` each corresponding FQDN. Zeus answers over Yggdrasil. Verify each route again immediately before a new deployment attempt, especially the destination selected by Lojix. Prometheus is booted with 45 boot entries and only terminal historical Lojix records; Zeus is booted with 13 boot entries and no Lojix history, so its next deployment is its first. The preflight HM relay to b860be was Uncertain, not presented/read.

## Ouranos deployment evidence

- Deployment 32 from CriomOS `3e2cc8be83f03859dc4fafe6ddba17280c488e22` used `NoSecrets` and failed during Eval on the OpenCodeTesting password-secret assertion; no derivation or activation.
- `secrets/opencodeServerPassword.sops` was rewrapped on Prometheus as root to both Prometheus and Ouranos age recipients, without plaintext display. Only that encrypted file changed in goldragon/secrets main `0d462a339e06fc9a90c10affbde0e08d6829744d`; canonical remote readback matched.
- The user manager had stale `LOJIX_OWNER_SOCKET=/run/lojix/owner.sock`; it was set to `/run/lojix/meta.sock`, and subsequent CLI commands used the declared socket explicitly. A reused unrooted Horizon proposal had been removed by midnight GC. A new `goldragon#horizon-definition` output was built with `--max-jobs 0 --option builders @/etc/nix/machines --option fallback false`, with Prometheus SSH offload directly witnessed. Its GC root is under `/var/lib/lojix-proposals/`, parent mode 0700 owned by li. Lojix rejects a SourcePath through that GC-root symlink; submit the verified canonical `/nix/store/.../horizon-definition.datom` target while retaining the root link. The current pre-switch Nexus 7 expects goldragon main's Horizon 0.12 data.
- Deployment 33 from immutable CriomOS `e6a83edc7e71254cae5a9d233c01fefc3f7f9b57`, `SecretsDirectory./git/github.com/LiGoldragon/goldragon/secrets`, canonical rooted proposal: Evaluate **Completed/Succeeded**, marker 800. Independent ByDeployment and ByNode queries showed its admission. Deployment 34 Realize derived `/nix/store/353n58zkn4h4isbdwl1m5j3x5kzyg8kc-nixos-system-ouranos-26.11.20260813.0e251e2.drv`, built through Prometheus, then **Failed/BuildFailed**, marker 820, on fixed-output `field-clj-deps.drv`: declared `sha256-HNlkGVUe61CxFetUQ/bJwGuhAPAaTEgvk1FCgdpZ1MU=`, got `sha256-vsJ2Q7yDWpDQvAl1GlcIjRqKm0XxB4xv26H72BV9CtM=`. This was not a disk-space failure. No TestActivation, ActivateNow, cleanup, GC, root removal, or blind retry occurred. HM failure reports to b860be and e167d8 reached Transported only.
- b860be reports field-clj main `a2c278d3` fixes the dependency hash and is being repinned in the Home step-2 line. That is a report, **not** a witnessed successful host build or activation. Ouranos had 20 GiB free at b860be's last report after unrelated collection; Gemma pre-seed and the three Qwen shards remain rooted. Never GC or remove those roots on a build failure without b860be's ruling.

## Tailnet mint evidence

Fable's corrected gopass rule: producer `| gopass cat NAME >/dev/null`; every gopass read must use `gopass cat NAME </dev/null | consumer`, because harness pipe stdin otherwise makes cat overwrite the entry with zero bytes. `gopass insert --force` is not byte-exact for binary and is not authorized. The real-store scratch self-test was completed and the scratch entry removed. Headscale `li` user ID 1 was read back. Ouranos and Prometheus age recipients were verified; Mirror Alpha, Mirror Beta, and vm-testing recipient derivation failed, so their preauth secrets are **not minted**.

Five encrypted files were committed and remote-verified on goldragon/secrets main `3e6ecfa92944c0113b192314f9c4a66cb33f1cb7`: `headscaleTlsCertificate.sops`, `headscaleTlsKey.sops`, `tailnetCertificateAuthorityKey.sops`, `tailnetPreauthKeyOuranos.sops`, `tailnetPreauthKeyPrometheus.sops`. The public CA record was committed and remote-verified on goldragon bookmark `tailnet-repair-da88cf` at `e8ce1e42be4644b4e96cac1bcba6a8b1b4232563`, after merging the OpenCode recipient rotation from main. CA name constraint and server certificate verification passed. The worktree was clean and lock released. No tailnet deployment. An independent flake check ended without retained terminal output: result **unknown**, not green. b860be reports later goldragon main `ddf27e0c` includes CA, five secrets, and Ouranos UsbDownlink `10.44.0.0/24`, with check green; that is b860be's claim pending direct readback by next Field flow.

## Next sequence, only after b860be's GO

1. Confirm immutable step-2 CriomOS, Home, field-clj, goldragon, Lojix/Horizon revisions and exact pinned closure. Use Lojix `Deploy.Host`, `RequireImmutable`, `CompleteHost`, `SecretsDirectory`, and a rooted **canonical non-symlink** proposal; evaluate on Ouranos, build derivations on Prometheus (local fallback only after a witnessed remote-build failure and b860be ruling). Ouranos: Evaluate → Realize → TestActivation. **Before ActivateNow**, from Prometheus witness its default route and DNS through Ouranos's newly declared USB downlink and working Internet. This is mandatory because the temporary daisy-chain profile/firewall/script are removed in that same generation; rollback without the hop would cut Prometheus's uplink. Then ActivateNow and witness declared Flow, Message, Lojix Nexus, HM package, Herdr panes, no failed units. Cleanup the temporary Flow drop-in/profile and messenger links/root only after the declared replacements are verified; set/import user-manager `LOJIX_OWNER_SOCKET=/run/lojix/meta.sock`. **Keep** field-luna-heartbeat masks, Gemma roots, Qwen roots, and transient Herdr server unless a later explicit ruling changes them.
2. Prometheus: Evaluate → Realize → ScheduleBootOnce with rollback, stages chained so no GC gap. Follow b860be's Gemma pre-seed/root and available-space guidance; do not duplicate the copy. Witness boot/network/remote access before considering it done.
3. Zeus: the living's typed order was “Make sure Zeus is updated.” Its first Lojix deployment must use the explicit answering Yggdrasil SSH destination and root Nix store transport. CompleteHost Evaluate → Realize → TestActivation → ActivateNow only if test and remote-access witnesses hold. Do not substitute old hand-deployed generation evidence for a Lojix result.
4. Tailnet secret consumers and the three skipped host keys require fresh verified recipients and b860be's integration/ruling; the minted files alone are not deployment evidence.

No automatic Flow refresh, retirement, stale-lock release, or transcript deletion follows from this handoff.
