# Fable companion context manifest

Prepared 2026-09-18 for the persistent Fable companion. This stages the
whole written-psyche corpus by stable repository roots rather than duplicating
it into another psyche record:

- `Vision/**/*.md` — reviewed distilled vision.
- `vision-raw/**/*.md` — legacy undistilled vision.
- `flows/*/vision/**/*.md` — flow-local raw vision, including the newest
  records.

The companion starts in `/home/li/primary`, so these are directly available
at the stated paths. Staged means available and named in its opening brief; it
does **not** mean every file has been read. The companion must list its actual
read set in its response with date and provenance.

## Required initial reads

- `flows/908786/vision/vision-led-audit-and-fable.md` — direct living request,
  2026-09-18, primary source for this Fable setup.
- `flows/908786/vision/context-refresh.md` and `default-effort.md` — direct
  refresh and medium-default direction.
- `flows/1ac573/vision/operational-fableFlowAndOpusComparison.md` — propagated
  operational context; it identifies the direct source above.
- `flows/1ac573/vision/operational-effortIsAlwaysMedium.md`,
  `operational-modelRoles.md`, and `operational-olderOpusIs46.md` — current
  model and effort context, with their recorded provenance.
- `flows/1ac573/vision/operational-privateLayerCoreLayer.md` and `AGENTS.md`
  — the unresolved private/core wording and the earlier NOT ACTIVE charter.
- `Vision/flowNexus.md`, `Vision/nexus.md`, and `Vision/remembering.md` —
  applicable distilled context.

## Current psyche-stack situation

- Old Opus flow `1ac573` is preserved. The direct record does not transfer its
  role.
- Persisted Opus flow `af762b` is a distinct live companion.
- Astra refresh flow `893603` is the current Codex primary successor.
- Fable is a new evidence-review companion, explicitly selected as installed
  `claude-fable-5-1[1m]` at medium effort. This is not an Opus switch.

## Questions to preserve

- The direct Fable request was addressed to Astra and asks for an
  evidence-based discussion of uncertainty. It does not decide that any model
  is categorically better.
- The current older-Opus selection is 4.6 with million context, while Fable
  remains explicitly selected for this new companion. These are distinct
  seats, not a reason to switch an existing session.
- All harness effort is medium. Capability/model choice and effort are
  separate decisions.
- The private charter is explicitly NOT ACTIVE until a third open-source seat
  runs. Later words about setting up a private/core repository leave activation
  and repository scope unresolved; no activation or repository creation is
  authorized here.
