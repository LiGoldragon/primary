# Astra medium successor — preparation, not a launch receipt

This is the ordered continuation brief for Flow908786's successor. It is
prepared while the current Message/Flow integration is still in progress.
Do not launch from it until the predecessor adds the actual deployment and
recipient-consumption receipt below. The older temporary prompt composed from
the seat launcher contains obsolete topology and must not be used as-is.

## 1. Identity and current instruction

The successor is the primary Psyche Mind Astra implementation flow, using
`gpt-6-astra` with reasoning effort `medium`. It continues the work; it is not
a relay-only worker. Every subflow defaults to medium as well. Preserve model
identifiers unless a task explicitly changes them. No Sol subagents.

The living's exact instruction, heard by predecessor908786, is recorded once
at `flows/908786/vision/default-effort.md`. It directs medium defaults across
models and subflows and asks Astra to restart on medium after the next main
goal. Source publication alone has not been substituted for that goal.

Claim a new Flow identity through the supported identity tool, register the
new managed Herdr agent in session `messaging-build`, and send the actual
model/effort, Flow ID and first active task to predecessor908786 and the
current coordinating peers. Keep the predecessor available until a real
return message and persistence/readiness witness establish the handoff.
Do not reuse another flow's identity or kill existing primary6852f4.

## 2. Read in this order

1. Repository instructions and `SKILL_VARIABLES.md`; applicable skills by
   their triggers and the available skill interface.
2. `flows/908786/vision/default-effort.md` for the direct living correction.
3. `flows/6034cc/reports/astra-launch.md` in full for the ordered original
   vision, including section10. Its earlier runtime reports are historical;
   reconcile them using the newer evidence below.
4. `flows/908786/log.md` for the tested takeover, persistence repair,
   outward notification and current implementation decisions.
5. `flows/908786/reports/flow-herdr-routing.md` and
   `flows/908786/reports/message-herdr-routing.md` for exact source/check
   evidence and its limitations.
6. `flows/908786/reports/default-effort-provenance.md` for authored versus
   installed defaults.
7. `/home/li/secondary/flows/348e7b/reports/current-v6-flow-acceptance-gate.md`
   in full, plus its referenced client-adoption evidence. Component reports
   do not replace this deployment gate.
8. The completed milestone receipt in section7 below and any newer native
   peer messages. Resolve discrepancies rather than averaging claims.

## 3. What the living wants and what remains

The original outcome is working Message and Flow components. Routability
precedes launching more flows: a launcher that creates unreachable sessions
does not satisfy that outcome. Opus1ac573 directly accepted this ordering.

The living also explicitly requested agents sending messages to the psyche.
A Herdr terminal-notification bridge now has independent `shown:true`
witnesses. This is useful working transport, not a proper Message receipt or
proof of human reading. The subsequent proper Message outward target is owned
by Astra and must be distinct from a Flow identity. Use a notification/inbox
endpoint; never inject notifications into the living's partially typed input.

Another concrete follow-up is a read-only public query/list operation for
durable Nexus delivery receipts. At Message55657f4e an ambiguous nonretryable
Parked receipt can only be recovered by replaying its exact original Deliver
request. This is an operator-observability gap, not permission to retry an
ambiguous send. Implement a tested protocol increment without silently
changing the graph currently being deployed.

The broader Flow launch/refresh work remains after routing acceptance. A new
Astra launch preset is not itself a correction to the existing medium effort
defaults. Do not let that expansion delay the current routing acceptance.

## 4. Paired work and communication

- Recovery primary6852f4 remains live and owns isolated Home/CriomOS
  integration. Its native thread is
  `01a0b100-35ad-7221-9be5-2eb6852f4159`.
- Secondary348e7b owns realization, deployment, backup, rollback and live
  verification. Its native thread is
  `01a0a11f-6130-70e2-80b1-796348e7b086`.
- Persisted Opus `af762b`, Herdr agent `psyche-opus-persisted`, is reachable
  in `messaging-build`. It accepted independent source-review scope and
  returned actual replies. Its native saved session is
  `af762bcb-3007-49c1-9c54-4396c300a3c9`.
- Old Opus1ac573 is preserved. The living has not separately confirmed a
  role transfer. Do not describe Astra's working pair designation as a
  living ruling or retire the old session.
- Worker6034cc completed its tested handoff and yielded. No further worker
  acknowledgment loop is needed.

Use the tested messaging route described by the final deployment receipt.
Until it is accepted, the existing authorized bridge is `hm-send` with the
sender's own `FLOW_ID` for registered peers, and `codex queue --thread ...`
for those recovery threads. Do not use `codex exec resume` as a messaging
proxy; it attempts another session writer. Never proxy a harness action that
automatic approval review rejected in another flow.

Persisted Opus has a growing native transcript and managed interactive
readiness. The old session's fresh export was rejected and was not proxied;
its earlier export and handoff remain. Native persistence, an export and
successful delivery are distinct claims.

## 5. Published components and bounded guarantees

| Component | Revision |
| --- | --- |
| Flow | `61d765e4814035c2c0a1424e670a1b62da3d10b6` |
| Message | `55657f4e90716071b55fa9dc133eb7d2083b7182` |
| signal-flow | `968ae3b00a64ebcb1ef05e21a0a55cee55ad015c` |
| meta-signal-flow | `aa6104012e1d9c9b9650590e502c51fbea902e81` |
| signal-message, direct v4 | `37c3e5b75b05f86b7dc27198bb4523ccbd495e4b` |
| meta-signal-message | `87a54b0a1cbc9aa02f9ff62e46c6ffca52f9ec25` |
| signal-message, meta's transitive v3 | `a9708f3384af18129cb1c983ffed850c4d631e46` |

The reports contain the complete relevant dependency/check evidence. The two
signal-message revisions are deliberate graph distinctions; do not collapse
them into one invented revision.

Flow keeps a claimed immutable Herdr identity binding in a side table while
preserving v5 rows. A stale persisted Herdr route is unavailable and parks
native fallback. A native-only older row retains the direct native protocol;
that is not UI composer injection or evidence of a migration requirement.

Message validates route identity and a known blank composer, submits a
canonical Datom once, and checks endpoint identity afterward. Accepted is
transport submission only. Ambiguous submission stays durably nonretryable
Parked. Herdr supplies no compare-and-swap operation: human input appearing
between the blank snapshot and submission remains a real race. Do not relax
the nonblank guard or claim atomic protection.

## 6. Runtime and deployment boundaries

Before this candidate, secondary independently observed Ouranos/li running
Nix Message0.12 with a mutable local Flow executable. Both services and their
ordinary/meta sockets were active. Astra's earlier observation identifies
fresh `messenger-v6.sema`; preserved v3 stores are not evidence of migration.
The old0.11.1/schema3 stopped-copy report was true historically and is not
the current service report. No legacy retry or migration belongs in this
increment.

Message requires one binary configuration argument. Its writer is
`message-write-configuration`; `message-nexus` is not a no-argument daemon.
The existing archive is `message-daemon.signal`; any candidate archive path
change must be declaratively owned and covered by current-pair restoration.
Flow starts without an argument; typed meta Configure persists its ordinary
and meta socket paths and reports NexusRestartRequired.

Primary6852f4 owns the final Home/CriomOS graph. Secondary must first realize
the immutable candidate, capture coherent client/unit/config/state ownership,
and prepare the compatible restoration and target-local watchdog before
separately gated activation. The historical Home profile does not represent
the current pair. Preserve local client overrides rather than deleting them
to hide mismatches. Never restore0.11.1 against v6 or overwrite new traffic
with an old store backup. No two writers, restamping or v3 mixing.

All build execution follows the configured remote-builder policy. A cache
realization is not a fresh test execution. Tests, realized generation, live
unit identity and recipient consumption each need their own evidence.

## 7. Milestone receipt — incomplete; do not launch yet

- Exact Home/CriomOS integration revisions and full consumer lock: pending.
- Named integration-check receipts and realized generation: pending.
- Current-pair capture, tested restore/watchdog and activation result: pending.
- Live matching client/service identities and recipient consumption: pending.
- Final medium-defaults activation status: pending. The published Home
  proposal is `bfea9669aa4ce33c09111430d5935ea33ed6182f`; installed Codex and
  Claude already use medium, but both Pi settings still used high at the last
  audit. Source publication alone did not change them.
- Successor native persistence, actual model/effort, claimed Flow identity,
  registration and return-message receipt: pending.

The predecessor must replace these pending entries with observed results
before using this brief for the requested post-goal restart.
