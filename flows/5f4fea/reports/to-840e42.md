# Refresh receipt for Flow 840e42

**Recorded:** 2026-09-15

This is a local launch receipt and handoff pointer.  It is not a prompt-relay
delivery receipt to Claude 840e42.

## Claude pair facts

- Flow identity: `840e42` (six characters).
- Claude daemon session: `840e42bb-b2cd-42eb-a9ec-7659a5b13ded`.
- Claude lane: `/home/li/primary/.claude/worktrees/flow-840e42/flows/840e42`.
- The local label observed in readiness was `primary-claude-pending [eafe83]`.
- The required Claude prompt-relay test-user-turn witness remains pending in
  this report.  A readiness announcement is not that witness.

## Codex refresh handoff and launch

The reviewable handoff is
`flows/5f4fea/handoff/codex-refresh-840e42.md`, committed as `6796753aa`.
It carries the pending order `31, 35, 34, 40, 37, 32, 36, 38, 41, 39`, the
claim-versus-receipt distinctions, and the fact that `5f8b46f` is a proposal,
not main.

At 2026-09-15 local time, the existing supported launcher was invoked as:

```sh
env -i HOME=/home/li PATH=/home/li/.nix-profile/bin:/run/current-system/sw/bin \
  XDG_RUNTIME_DIR=/run/user/1001 \
  DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1001/bus \
  WAYLAND_DISPLAY=wayland-1 XDG_CURRENT_DESKTOP=niri XDG_SESSION_TYPE=wayland \
  codex-desktop fresh \
  /home/li/wt/primary-5f4fea/flows/5f4fea/handoff/codex-refresh-840e42.md \
  /home/li/wt/primary-5f4fea
```

It started a Ghostty remote client, then observed Codex PID `1310554` with
the handoff as its initial prompt.  The process was in `user@1001.service`,
outside `codex-remote-control.service`; the app-server itself remained active
at PID `2087`.  No service was restarted.

`codex-desktop fresh` accepts only a prompt file and working directory.  Its
supported status command reports the service only; it does not expose a new
Codex thread UUID.  Therefore no successor thread identity or native skill
injection receipt is claimed here.  The fresh handoff itself states that
native skills were not newly loaded by the handoff.

The first invocation without the user DBus environment failed before launch
with: `Failed to connect to user scope bus via local transport`; it created no
successor.  The invocation above is the successful local launch witness.

Use the supported secondary 57a7aa stopgap or another proven native route to
notify Flow 840e42.  Do not treat AgentIntercom acknowledgement as delivery.

## Later verified Codex transcript receipt

The launched successor's session metadata identifies its actual Codex thread as
`01a0a715-2d5d-7342-b278-1dbcf78795bd`, with cwd
`/home/li/wt/primary-5f4fea` and model `gpt-6-astra`. Its first user record is
the handoff before the later item 42 update; changing a handoff file does not
change an already launched session.

At 22:00:08 local time it stated that runtime identity and the depth-1 memory
of `5f4fea`; its reported Intercom identifier was `codex-1310655-2b4abff6`.
It also stated that native main-flow/psyche injection was pending, so no
native-load claim is made here.

The original eligible human hook-vision source at
`2026-09-15T22:01:31.436Z` was sent to the exact thread through the supported
app-server relay. The accepted turn was
`01a0a719-e09f-7962-8c2e-7c93f0f022fc`; the successor replied at 22:04:55 with
a proposal only and retained `no zero payload` as an ambiguity. It did not
install hooks. That reply does not establish a pair with Flow 840e42 and does
not satisfy the pending Claude relay-user-turn witness.

## Current proposal queue sent to the Codex successor

Order: `31 (with 45), 35, 34, 40, 37, 42, 43, 32, 36, 44, 38, 41, 39`.

- **31 / 45:** subscribe only to witnessed available hooks. Propose a Flow
  event-log Ethos with scalar integer/boolean/enum payloads, typed Flow and
  session IDs, and classified errors without message text. Original text stays
  retrievable from harness logs while it exists. Future GC is not deletion
  authorization. Inventory actual Claude/Codex fields and mapping variants;
  cross-session delivery notices remain approval-held.
- **43:** propose an Ethos-addressed web-app map with addressed elements,
  actions, and a semantic fingerprint that ignores style. `webapi:` beside
  `git:` and longest-match resolution are proposals, not grammar claims. Use a
  Git-server repository per map, subject changes to Psyche approval; Cloudflare
  is the first provider object, owned by secondary/Codex implementation.
- **44:** perform only a read-only inventory of available browser or
  computer-use access on ouranos, Playwright/headless CriomOS availability,
  prior web-agent work, and gaps for a self-teaching mapping job. No install.

The full verbatim web-vision source and all other current proposal details are
in `flows/5f4fea/handoff/codex-refresh-840e42.md` at `b8a924cc2`.

The existing Codex thread accepted the web-vision peer turn
`01a0a71d-88fb-7a31-aaf4-36011ce8c94a`. Its envelope explicitly identifies the
root thread and source record and labels the transport peer-to-peer; it does
not claim human provenance. A turn acceptance is not a readiness or pair
receipt.
