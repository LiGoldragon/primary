# Codex refresh handoff

## Identity and pair

You are a fresh Codex successor to root flow `5f4fea`. Claim your actual
identity before creating a lane. Pair with primary Claude Flow `840e42`, whose
daemon session is `840e42bb-b2cd-42eb-a9ec-7659a5b13ded`; its current local
label is `primary-claude-pending [eafe83]`. Its actual lane is
`/home/li/primary/.claude/worktrees/flow-840e42/flows/840e42`. Remember
predecessor 5f4fea at depth 1; do not recursively refresh.

## Current facts and receipts

The Claude launch used the full-context system prompt (134125 bytes) and a
revised 139691-byte user prompt containing full recursive Vision and Intent,
the ten relayed human prompts, fd0f97 log and vision, and nine open decisions.
It is daemon-rostered and remote control has an active bridge. Its required
prompt-relay user-turn proof remains pending because it was waiting on a
permission prompt. Do not call that proof complete. The completion hook is
skipped for this wave. AgentIntercom acknowledgement is never a delivery
receipt for fd0f97.

The stronger main/subflow text is proposal commit `5f8b46f`, not main. It must
remove residual Beads language if approved. Main-only wording does not apply to
subflows. Native skills have not been newly loaded by this handoff; do not
claim otherwise.

## Priority order

31, 35, 34, 40, 37, 42, 32, 36, 38, 41, 39.

31: finish the actual relay-user-turn witness and record the daemon alias,
session, remote bridge, and cgroup facts without inventing a rename API.

35: Flow owns the flow-name registry and turn-idleness query/subscription;
Message asks Flow and must not duplicate that registry.

34: Psyche is a typed Signal/Nexus proposal for Spirit, Intent, Vision and
Notion records, bounded domains, and migration planning only.

40: propose one single-string MCP typed component enum
`Orchestrate|Message|Persona|Psyche|Flow`, CLI-to-Signal socket-to-Datom-result;
it replaces send-subflow mechanics only when approved and implemented.

37: quota work and 38 audit are running/awaiting their owners; preserve source
versus claim distinctions.

42: prepare a repository-split proposal only. Separate universal primary
machinery (skills, tools, Flow conventions) from Persona data (Vision, Intent,
flows, vision-raw, and reports). Private or saleable classification is unknown
and must not be inferred. Propose `universalprimary`, `personatemplate`, and
per-person namespaces; cover moves, stays, ordering, sizes, Git-server
namespaces, access boundaries, GitHub mirrors, and secondary/Codex/cloud
implementation ownership. Make no moves or repository changes.

## Flow hooks vision — living source relayed at 22:01:31

Notify Flow about harness events, including agent comments where available, and
let Flow decide. Keep a compact typed event log using enums, scalars, and
booleans; store explicit strings only when needed. Classify known error strings
into an enum while retaining a reference to the original details. The phrase
`no zero payload` is ambiguous and is preserved verbatim rather than
interpreted as unit variants. GC was mentioned in the vision; it does not
authorize deletion of existing logs. Retention rules and a full call record
must exist before erasure, consistently. Hook availability for all comments is
unverified. This is proposal context only, with no activation beyond the
already-approved successor proof.

32: identifier types remain a Signal-library proof-of-concept proposal.

36: retain the pending architecture work without deployment.

41: propose a model-call record of inputs, model/access, output, Persona/Sema
hooks and transcript relationship; no install.

39: prepare Latin, Sanskrit and Spanish language-web proposal, not an adopted
vocabulary.

All items above are proposal-only unless a separately witnessed receipt says
otherwise. No host deploy, service restart, global installation, shared-HEAD
move, rebase, or force push.

## Authoritative local records

- `flows/5f4fea/log.md`
- `flows/5f4fea/reports/to-fd0f97.md`
- `flows/5f4fea/reports/to-840e42.md` when present
- `flows/840e42bb/launch.md` is an uncommitted incorrect eight-character lane
  artifact; preserve it but do not treat it as Flow 840e42's lane.
- `/home/li/primary/flows/fd0f97/log.md` and `vision/`

Report paired status through the supported secondary 57a7aa stopgap or a
proven native route. Do not forge prompt-relay provenance.
