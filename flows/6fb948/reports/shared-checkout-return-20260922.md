# Shared Primary return

Living ruling: `flows/1b8ac0/vision/sharedCheckout.md` at `22262962d`. The design-only owner coordination gate closed with Sol and Terra ACKs. Work resumes in `/home/li/primary`, coordinated by Orchestrate and scoped to `flows/6fb948` for this return.

Before return, the two uncommitted reports in the old worktree were preserved in a separate commit `a0fa9d530`. Primary was clean at the shared base in the JSON receipt. Its detached HEAD equaled main; attaching main changed no tracked file content. Missing own Flow records were restored byte-for-byte; existing newer reports were preserved and differing older versions retained under `prior-worktree/`. The JSON receipt lists exact source blobs, hashes and destinations.

This is a work/data return, not a native process restart or claimed thread-cwd mutation. Every subsequent shell/file operation should explicitly use `/home/li/primary`. The old worktree, native session, active reservations, controller 4639 and all retained routes remain intact. Deferred source branches remain pending.

The shared-checkout/dirty-first-commit ruling was submitted through HM to 753e69, 0347d0, c88918, 03e825, 6db4fe and 9ddcbc (not read receipts). HM submission to 0ad137 failed because registration was absent; a direct policy relay was subsequently submitted through Herdr after rechecking native UUID `01a0ca8d-c9df-71f2-9f72-ae20ad1379e9`, agent `field-high-0ad137`, pane `wQ:pF`, terminal `term_65c174dc3f69a5c`. Herdr returned `agent_prompted`; no read/acceptance or new HM registration is claimed. No new or replacement identity was created.

Return commit: `cb671a484`. Post-copy verification checked all 30 restored/prior-variant destinations against recorded hashes with zero mismatches.

Supplied peer receipts: Terra 0347d0 used the shared checkout at `22262962d`, clean, with no own external records requiring return; copies in other owners' worktrees were left alone. Operator 9ddcbc executed commands in `/home/li/primary` at that same clean detached revision and verified its pending records already present from `0d00eab2` and `dbbf4cf9`; no content return was needed. These are timestamped historical checkout observations, not claims that Primary is still detached or that native harness cwd changed.
