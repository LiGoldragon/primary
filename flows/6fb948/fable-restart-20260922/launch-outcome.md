# Fresh Fable launch outcome

The supported batch launcher created a fresh Claude Fable 5.1 session at
`messaging-build/wD:pC` with native UUID
`f4b2633d-52d8-4363-b584-795fe727cd93`, requested medium effort, and an
isolated job directory. It did not resume, fork, prompt, route, or reap the
overflowed `wD:p6` predecessor or the held `wD:p9` failure.

The batch helper first stopped before input because a newly created Claude
session has no transcript. Its documented same-process bootstrap continuation
then set the provisional title `Psyche High (claim pending)`, but the first
skill request returned `Login expired · Please run /login`. The fresh window
is at Claude's subscription-login prompt. Authentication and Remote Control
remain pending user completion; no readiness, Flow identity claim, route, or
remote URL is asserted here.

The state, controller attestation, profile manifest, and helper bootstrap
manifest in this directory are the compact evidence for this attempt. The
OAuth authorization request is intentionally not stored in the repository.
