# USB gateway implementation start — 2026-09-21

Field Sol `753e69` coordinates under Field Astra `6db4fe`. The living accepted the positional Horizon shape `UsbIpv4Gateway { downstream: Interface, downstream_mac: MacAddress, gateway: Ipv4Cidr, uplink: Interface }` as architecture, not deployment acceptance. The full validity and consumer gates are in the 19:14:02Z relay to this flow and `horizon-usb-gateway-contract.md`.

Four nonoverlapping Orchestrate lanes were reserved: Horizon initial `4014` (released after its first commit), Goldragon `4015` (`proposal.datomic`), Lojix `4016` (isolated `horizon-contract-repin-8565e8` workspace), and CriomOS `4017` (`node-services.nix`, `network/default.nix`, network capability module, checks in isolated `usb-share-6db4fe`). Goldragon `4015` and Lojix `4016` were then released without edits because their owner was `6db4fe` while the assigned writers are Field Sol subflows; those writers are taking fresh exact reservations under `753e69` before editing. CriomOS `4017` remains with Field Astra's own collaborator. None touches the `f72ab7` Flow, Message, or signal-flow locks. The workspaces were clean at reservation; they are Jujutsu working copies, so `git status` was not a valid inspection there.

The existing Field Sol child `own_commit_verify` published initial Horizon producer `944ad387684426da7648ded61d259d88df86802f` on remote `usb-gateway-6db4fe`. The worker's first full SHA was mistyped; this value was directly resolved from the remote bookmark. Independent review found that its duplicate check allowed two different gateway services on one node. A later constrained Prometheus test exposed a missing `serde_json` dev dependency, so the earlier successful build did not compile the new fixture. Under locks `4042` and `4043`, the worker published corrected remote revision `37416e10c00cc6a22c73528759df5b15bd6d8428`, independently read back by Field Sol; the remote-only check then passed and both correction locks were released. Its observed-MAC normalized serde JSON fixture and exact SHA-256 are in `usb-gateway-json-fixture.md`. That fixture is not yet an actual Goldragon projection receipt.

Field Astra's 19:19:22Z ownership correction assigns CriomOS lane `4017` and Ouranos runtime to its existing `/root/claude_usage_research` collaborator. The retained `field-terra` pane is not the implementation writer. That collaborator also has priority Claude-isolation work, so source preparation may progress while its tests wait. It must make no live Ouranos mutation or activation for this integration before checks, and must choose exactly one NAT owner/backend. Field Sol's existing children `integration_ownership` and `mind_gateway_route` accepted Goldragon and Lojix under fresh exact own-flow locks `4044` and `4045`, respectively. Lojix is preparing an intended pin candidate; Goldragon will validate all nodes and typed materialization with it before its own push, then Lojix will publish the coherent pin. No downstream source revision or activation is claimed here.

Curriculum authored `skills/testing-datom-messaging.md` was corrected from `MACHINE.Relay` to `Machine.Relay`, commit `67ca9ab60a56982b28e51eefaccc0a7efbceed61`, pushed and remote-read at `origin/main`. It changes exactly one authored file; temporary lock `4011` was released. Primary generated projection is still stale. Field Astra successor `03e825` alone owns the projection: use an empty disposable Generate root and install only the two exact owned outputs after preimage ownership/digest proof and a rendered plan. No generated files were edited here.

The network's wired proof precedes the living's intentional Zeus unplug. Do not troubleshoot that cable or treat the current unplugged path as an implementation failure.
