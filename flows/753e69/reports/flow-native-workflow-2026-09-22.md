# Field Flow CLI and native lifecycle gate — 2026-09-22

Owner: Field Medium Sol `753e69`. This is a source and passive runtime checkpoint, not a deployment, route transfer, native refresh, or process restart.

## Cause and current receipts

`flow-nexus.service` runs PID `3727637`, started 2026-09-18 08:14:46 CST, from `/home/li/.local/bin/flow-nexus`. The installed CLI and Nexus files are dated September 17. At this check, the process executable and installed Nexus file both hash to `98f68df32f144efde2fd53c14e8afe35dce984db1f0ce957b63b281d6f923a0e`; the installed CLI hashes to `a34e3770250f40947330aa7d34f89bc9af01cc9d84b22cea28b5ad161cad8f59`. No immutable source revision for either installed binary is attested. `flow resolve 753e69` and `flow resolve 03e825` each returned `RecipientResolutionRejected.UnknownFlow` on 2026-09-22. This is an actual installed CLI/Nexus answer, not a missing native main-flow receipt.

Both Field native launch receipts say `status: verified` and have exact native IDs matching their HM registry rows: `753e69` is `01a0c4ad-12bd-74c2-9c21-7a4753e69b3d` at historical `wQ:pA`; `03e825` is `01a0c4a7-2590-7ae3-8625-bee03e82586d` at historical `wQ:p9`. Those pane records are not current live-route proof; both were earlier observed `pane_not_found`. The installed Flow CLI has only positional `start`, `restart`, and `resolve`. It does not expose a native Claude refresh, HM import, or current-seat switch. Current candidate Flow source has a typed meta `RegisterFlow` operation, but candidate source is not installed and current seats have no established Flow registration.

The newer Flow candidate in the retained f72 lane has a corrected mutable `candidate` at `crates/flow-nexus/src/store.rs:727`; this inspection does not prove a remote build. A prior remote Nix check of an earlier candidate failed at that line before the VM could start. Mind and f72 retain core ownership. The 6db launcher source owner has published a guarded Claude helper change at Primary `49c03f7f`, included in `8a03d3f9`; local fixture/syntax passed, remote Nix remains blocked by cache timeout. No live partial-session acceptance follows from that source change.

Field Ultra Low's read-only source review found another bounded repair in the candidate: a failed Codex `thread/start` can leave a persisted pending Flow row that is not resumable (`crates/flow-nexus/src/lib.rs:140-170`, test in `store.rs:2097-2114`). Candidate `BeginRefresh` and route validation refuse stale bindings, but the old installed CLI cannot exercise that typed lifecycle. These core changes belong to the existing f72/Mind lock, not this Field preflight path.

## Field implementation

`tools/field-flow-preflight.mjs` is a passive gate. It compares the exact native receipt ID to the HM registry row, invokes the installed positional `flow resolve <six-hex-id>` and, only if that syntax fails, the candidate's one-Datom `flow ResolveRecipient.<six-hex-id>`, hashes the installed CLI and Nexus plus an explicitly supplied `/proc/<pid>/exe`, and prints graded JSON. Both queries are read-only. It never registers, sends, launches, refreshes, restarts, or changes a route. Without externally supplied current Herdr direct-read evidence and a reviewed source/installed attestation, it leaves those grades unverified and `readyForFlowLifecycle` false. A positive gate requires exact active Flow resolution including native ID and ready endpoint/Herdr route, a matching Herdr direct-read record observed within five minutes, and a matching remote-check/source-revision attestation for the exact installed and running bytes, also observed within five minutes. The operator/controller remains responsible for the truth and provenance of those external receipts; this passive script is not an identity authority. The focused fixture checks blocked current state, gated positive/negative paths, and the candidate Datom CLI syntax.

Example passive invocation:

```sh
node tools/field-flow-preflight.mjs --flow 753e69 --receipt flows/6db4fe/field-sol-native/receipts/field-sol.json --registry /home/li/.local/state/hacky-messenger/753e69.json --nexus-pid 3727637
```

Local focused test: `node --test tools/field-flow-preflight.test.mjs` passed 3/3. The same script against installed `753e69` and `03e825` reported native/HM ID match, process/installed Nexus hash match, `unknown-flow`, current Herdr binding `unverified`, source revision parity `unproven`, and lifecycle readiness false. Terra's first independent test found that the initial implementation had no positive path; that defect was corrected. Terra then independently tested the final source SHA-256 `e8cfc0e27e69414ad4fa04a1a6724ac2c2da7f8d3826a1d39afcb215d643f134` and test SHA-256 `54732b2fe5646232643f2da9dee522309d0d67b7b0127cbdeb5123c6773cd891`: 3/3 tests passed; legacy `UnknownFlow` triggered no fallback; candidate syntax refusal triggered exactly one typed read-only fallback; mismatch and absent currentness remained blocked. The positive fixture used controlled, unsigned JSON and establishes no production readiness. Luna documentation/commit acceptance remains pending.

## Working procedure and remaining gates

For current seats, use the existing native receipt and the exact current Herdr/HM route, then run the passive preflight. Treat `UnknownFlow`, a stale pane, or missing source parity as a hold. A managed native create or refresh belongs only to the existing controller and launcher profile, with its receipt, exact model/effort, title and HM target readback; `flow restart` is a same-thread nudge and cannot substitute for refresh. A typed Flow cutover requires Mind/f72's coherent source graph, remote build and VM result, a guarded current-seat registration/import with exact native and route evidence, installed binary/source parity, and an end-to-end disposable recipient receipt. No running service, socket, Sema store, route, or held successor pane was touched here.

Field High `03e825` alone controls round `4494` and the held Psyche High successor remains without input. Retained `6db4fe` forwards launcher worker results. Field Low `0347d0` independently tests this exact Field implementation; Field Ultra Low `c88918` receives a later explicit lock handoff for final documentation and scoped commit. The existing CriomOS network owner must first identify a disjoint source path and correlate the phone failure; observed Wi-Fi authentication fails before IP, while a USB downstream has carrier/DHCP/DNS but no RX/lease. Those observations do not justify a speculative router or live Ouranos change.
