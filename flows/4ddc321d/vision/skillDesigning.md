# skillDesigning

## 2026-08-26 — removal is better than addition

Said on being offered a replacement line instructing that
earlier-injected skills keep governing across turns:

> context does this. Removal is better than addition, when the
> expected behavior is the desired behavior. That would be a good line
> for skill-design
>
> so it sounds like that entire skill block could potentially be
> removed.

Flow reading, not the psyche's words: the injected skill text already
sits in the conversation; the model's expected behavior over standing
context is to honor it — so no instruction is needed, and adding one
is worse than adding nothing. The principle line for the
skill-designing skill: "Removal is better than addition, when the
expected behavior is the desired behavior."
