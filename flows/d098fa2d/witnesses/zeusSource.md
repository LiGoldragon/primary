Subject: Zeus source ownership and projected identity.

Method: code read /git/github.com/LiGoldragon/goldragon/datom.dotos

The `goldragon` proposal contains a positional `zeus` node record. It declares `Edge`, `Max`, `Max`, a `Metal` `X86_64` `ThinkPadT14Gen2Intel` machine with four cores, Colemak/Uefi I/O, no node services, and the Zeus SSH/Nix/Yggdrasil public material. The user map contains `bird` and `li` entries for `zeus`.

Method: code read /var/lib/lojix/generated-inputs/goldragon/zeus/complete-host/horizon/horizon.json

The retained projection identifies `goldragon`/`zeus`, `Edge`, `ThinkPadT14Gen2Intel`, `x86_64-linux`, no services, and `bird`/`li` users with public keys. It also carries generated build-machine and cache metadata. The projection is managed output, not an authored patch target.
