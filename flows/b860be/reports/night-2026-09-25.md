# The night of 2026-09-25: what was done while you slept (successor edition)

**Successor edition, by flow b860be (Psyche Fable, successor of da88cf).** Sections 1 to 12 below are da88cf's report `flows/da88cf/reports/night-2026-09-25.md` (last written about 00:23 host clock, 2026-09-26), copied without rewriting; only this title line changed. Every **PENDING** in sections 1 to 12 is da88cf's as of that time. What happened after it was written, and which of those PENDING items have since moved, is in section 13, "Since the handoff (successor b860be)".

Draft source for the morning book (one visual page, no images). Written by a subflow of flow da88cf from what was settled by about 21:45 on the machines' own clocks. Anything still running is marked **PENDING:** and will be filled in by the final pass before 06:30.

How to read this page:

- **Seen** means a subflow saw it in command output or a file.
- **Claimed** means another agent session reported it and nobody here re-checked it.
- **Flow's inference** means da88cf reasoned it out. It is not an observation.
- **Unknown** means nobody established it.

Names with six hex characters, such as da88cf, b7da5d or 00f95a, are agent sessions ("flows"). Agents made these names up. da88cf is the Fable seat that led the night. Other names that agents invented are marked *(agent name)* the first time they appear.

**About the clock.** The times attached to your words (about 21:50 to 22:15) come from the relaying agent, 88475f. Both machines' own clocks put Prometheus's power-on at 21:15:44, while the relays put it at about 21:57. So the relay times run about 40 minutes ahead of the machines. Unknown: which clock the relays used. Machine times below are marked "host clock".

## 1. What you asked for tonight

Your words, verbatim, in the order you said them, with the relay times. Words in [brackets] are 88475f's transcription corrections.

1. ~21:50: "Why was it working before? Would it help if I rebooted it? We need to use Prometheus."
2. ~21:52: "Here I'm rebooting him now."
3. ~21:55: "Well I haven't rebooted it. Should I? Do you have any way to talk to Prometheus? Is it turned off? Well there you go. That's the problem."
4. ~21:57: "So I started it. Now that should help. It wasn't even running. I guess we had a power outage or something."
5. ~22:00: "Give it, I don't know, 30 seconds. You should be able to test it. Make sure we move all the building to Prometheus and get all the fixes deployed to it."
6. ~22:05: "Okay well, you can get that Tailscale problem figured out and fixed. Make sure you follow the topology of the cluster, data, and administrator roles and features in order to add data in order to know which host does what. I leave it to Fable's best judgment because I'm going to sleep and I want this to be done. You can present me with what has been done afterwards but I want you to stop doing [Nix] builds and [Nix] tests on [ouranos] and move everything to Prometheus now."
7. ~22:08: "You can't really write down literally the skills but we're moving back to remote builders. Of course you can fall back to local building but I guess you don't have a way to wake me up if something goes wrong. I'll hear the laptop running because it's next to me. Prometheus should be doing the builds and running the fan hard so I shouldn't hear my laptop run really hard most of the time."
8. ~22:10: "We should prioritize using [Nix] builds for everything. That way we maximize the remote building aspect and maybe you can get a low-powered subflow to make sure Prometheus has lots of disk space by garbage collecting and cleaning up build directories and such like that. We had changed the models we wanted to have serve locally on the AI side but I'don't know what the status of that is. You can present that to me in the book in the morning."
9. ~22:15: "And do some tests. See if the lambda [sic] daisy chain setup works once you've deployed it properly and there are no hotfixes to make it work. Just keep running new code, testing things and recovering if you need to, mainly using [Claude] subworkers to maximize our usage of the [Claude] overnight and get everything working nicely. Also on the message and flow development, which you'll get started on when you refresh ..."

Transcription notes from 88475f: "next" was corrected to "Nix", "Uranus" to "ouranos", "Nick's" to "Nix", and "cloud" to "Claude". The "Claude" correction is uncertain. "lambda" was kept as spoken. The last sentence is unfinished, so nothing was built on it.

How da88cf read the builder instructions: item 7 refines item 6. The flow ruled that Prometheus builds by default. A local build on the laptop is allowed only after a remote build has visibly failed, and each such fallback is reported with the remote error. It is never routine. This ruling went to all 11 live agent sessions.

## 2. What happened to Prometheus

**Seen:**

- Prometheus's previous boot log ends at **12:55:13** (host clock) in the middle of routine work. There is no shutdown sequence.
- When it started again, its journal reported that it had been **"uncleanly shut down"**.
- The kernel came up at **21:15:44** (host clock). By 21:17:24 it answered ping and ssh from ouranos.
- Both network links on ouranos flapped between 12:57 and 13:06, including the link to the house router.
- No crash record (pstore) newer than 09-24 exists, so this was not a Wi-Fi-driver crash.
- It runs cleanly now: no failed units, and 995 GB free on its store.

**Return timeline (host clock):**

| Time | Event |
|---|---|
| 12:55:13 | Last line of the previous boot |
| ~21:15 | You power it on (your ~21:57) |
| 21:15:44 | Kernel up |
| 21:16:26 | First build client connects |
| 21:17:15, 21:17:24 | 88475f's two test builds arrive from ouranos |
| 21:17:24 | First check by da88cf: ping and ssh work |
| 21:18:21–23 | da88cf's own test build runs on Prometheus and returns |

**Flow's inference:** a mains power loss or a forced power-off (a held button). The logs cannot tell these apart. A short press of the button would have left a clean shutdown in the log, and there is none. Your "power outage" fits.

**Remote building was seen working again.** ouranos sent a small test build to Prometheus over the "Yggdrasil" link. *Yggdrasil* is the encrypted mesh network the cluster uses to reach its own machines by name. Prometheus built it and sent the result back. Its own log shows the matching session and a "succeeded". Tailscale played no part: the tailnet was down the whole time (section 4).

**"Why was it working before?"** Remote building rode the Yggdrasil link over the cable, not the tailnet. It stopped only because Prometheus was off, and a reboot would not have helped a machine that was powered down.

**Restore on AC power loss: needs your hands at the box.** Nothing in CriomOS sets this, and no remote check can read it.

- The machine is a GMKtec EVO-X2 with BIOS 1.11.
- At boot, enter the firmware setup and find the option for the power state after AC loss. Its name varies: "Restore on AC Power Loss", "State After G3" or "AC Back".
- Set it to **Power On** or **Last State**, so Prometheus comes back by itself after a power cut.
- The power strip and the socket are also worth a look.

**Settled:** the disk clean-up did not run separately; it rides the nightly garbage collection (Nix's own sweep of anything nothing points to any more), which fires at 00:00 host clock. It ran, seen: 00:00:00 to 00:02:38, and freed 9.3 GiB across 10,707 store paths. The stale, never-used Prometheus system copy noted earlier is confirmed gone. The six things deliberately protected from this sweep (the messenger-clj package, Flow 0.12.2, the message package, and the three Qwen model file pieces) are all still present, as intended. Despite the sweep, ouranos's disk reads slightly fuller afterward, 93% instead of 92%: other, unrelated writes landed in the same window and were not investigated further.

## 3. Wi-Fi from Prometheus

**What exists (seen):**

- Prometheus is your Wi-Fi access point. It broadcasts again since the power-on.
- Network name: **goldragon.criome**.
- Security: **WPA3-SAE**. This is the newest WPA3 personal mode, and it uses a password.
- 2.4 GHz, channel 6. It hands out addresses in 10.18.0.x and shares Prometheus's Internet.
- This is already a declared feature in cluster data: Prometheus carries the "Router" capability.
- **The password** is the one you set. It is stored encrypted in the goldragon secrets as the entry named **routerWifiSaePasswords**. No agent has read it.
- Assumption: you have it in your own records. Unknown: whether your own key can decrypt that entry, since it is encrypted to the machines' keys.
- **How to see it, if you need to (settled, value not touched):** the entry has exactly one encrypted field, named `data`. On ouranos, it decrypts with whichever key you hold that matches it, your age key or your GPG key, through the `sops` decrypt command. On Prometheus itself, the same entry is already decrypted automatically at boot, using the machine's own key, and readable as root with no unlocking step. The decrypted field can hold one password or several lines, one per allowed device; the encrypted form alone does not say which.
- A backup access point, "criome-backup", is declared on a USB radio. **Seen:** that ASUS dongle has been unplugged since 06-26, and a SanDisk USB stick sits in its port. The declaration is correct, and the fix is physical (see question 13).

**What was wrong (seen):**

- **No country is set.** CriomOS falls back to Poland ("PL") because cluster data has no field for the country. Evidence that you are in Mexico:
  - both machines' time zones are America/Mexico_City;
  - ouranos's fixed location in cluster data is near San Cristóbal de las Casas, Chiapas;
  - ouranos's own radio reports MX;
  - you wrote "since moving to Mexico".
  - At 2.4 GHz, Poland and Mexico both allow 20 dBm, so the country matters for legality and for the 5/6 GHz rules, not for tonight's range.
- **The "3 dBm" transmit power is a display bug, not real power.** The kernel's driver for this chip never fills in the value it reports. Upstream fixed this in kernel 7.2. Prometheus runs 7.1.8.
- **The access point's log is too quiet.** hostapd, the access-point program, logs at level 2. At that level, WPA3 password and handshake failures are not recorded, so a failed phone connection leaves no trace. Seen: on 09-21, a device with a randomized address kept getting dropped ("did not acknowledge authentication response", then "inactivity"). That matches your report that day that the phone "went to get an IP address … and then it just disconnected".
- **The laptop was taken off this Wi-Fi by an agent, not by you.** On 09-24, agent d8df70 turned off auto-join for goldragon.criome on ouranos. It called the step "Wi-Fi A" *(agent name)* and meant it to stop Prometheus's Wi-Fi driver crashing. No quote of yours asks for this.

**What changed tonight:**

- da88cf decided on a declared fix and sent an implementer:
  - add a country field to cluster data (a breaking schema change, with a version bump);
  - set Prometheus to **MX**;
  - remove the Poland fallback in CriomOS;
  - declare a more verbose access-point log.
- The work sits on bookmarks named wifi-country-da88cf *(agent name)*.
- **Settled:** the country field is written into the shared schema (the horizon-rs 0.13.0 change described in section 6), with an MX fixture for Mexico. The CriomOS side requires that field, sets the kernel's regulatory domain from it, raises the access point's log detail, and adds a new check. Along the way a second, unrelated real defect was found and fixed: the setting for 802.11n support was being compared in the wrong letter case, so it was silently off on Prometheus. These checks passed on Prometheus after the fix.
- **PENDING:** whether this reaches Prometheus. It waits on the same repin train and deploy window as the tailnet change (sections 6 and 8).

**Connecting your phone or laptop in the morning:**

1. On the phone, open Wi-Fi, choose **goldragon.criome**, and enter your password.
2. If the phone offers a security choice, pick **WPA3-Personal** (SAE).
3. You should get an address starting 10.18.0. Open any web page to confirm Internet.
4. For the laptop (ouranos), auto-join is still off. Choose goldragon.criome in the network menu once. Its wired Internet stays first, because the saved profile never makes Wi-Fi the default route.

**Open questions:**

- Does your phone support WPA3-SAE? da88cf's hypothesis, not checked: the network is WPA3-only, and an older phone without SAE would fail at exactly this step. Once the louder log is deployed, a failed attempt will show why.
- Should ouranos rejoin this Wi-Fi automatically (reversing "Wi-Fi A")?
- One caution, flow's inference: ouranos must not take its *default* route from this Wi-Fi, because Prometheus gets its Internet from ouranos. That would be a loop.

## 4. Tailscale

**Cause, in plain words (seen):**

- *Headscale* is the self-hosted coordination server for the tailnet. It runs on **ouranos**, the only machine with the "tailnet controller" role in cluster data.
- Its security certificate was self-made on 2026-03-13 under the **old cluster name** (ouranos.maisiliym.criome). It was never renewed, because the renewal step skips if any certificate file already exists.
- Nothing tells the machines to trust that certificate, so ouranos's own Tailscale client refuses it. That refusal has happened about every 20–40 seconds since at least 2026-06-05, the oldest log kept, about 309,000 times in all. No login has succeeded in that whole period.
- Prometheus was **never enrolled** at all.
- Someone set ouranos's login address to 127.0.0.1 (the machine talking to itself) by hand; no module sets it.
- **The tailnet carries nothing live tonight.** It did not cause the build outage.
- Unknown: how it once worked before June. A saved profile ("li", node 2) shows that a login succeeded at some point.

**Decision taken, under your "I leave it to Fable's best judgment":**

- **Trust.** A cluster certificate authority (*CA*: one cluster-owned key that signs certificates) is declared in cluster data and installed on every machine. It is private, needs no public DNS, and works over Yggdrasil. The alternative was a public Let's Encrypt certificate, which would publish a name and need a Cloudflare token. A narrower option, trusting only the one Headscale certificate, was also weighed. The CA was kept because you said "the criome cluster is the trusted network".
- **Controller.** The controller stays on ouranos for now. Moving it to Prometheus is your call (question 7).
- **Joining.** Each machine joins by a small declared start-up step. It reads a one-time join key (a *preauth key*) from an encrypted secret file, without ever putting the key on a command line. The login address comes from cluster data, and the step runs only when the machine needs to log in. There is one reusable key per host.
- **Membership.** Membership is unchanged: ouranos and Prometheus, plus the declared test VMs.
- **Reset.** ouranos's hand-set login may be force-reset. Nothing depends on it.
- **Secrets.** A Field agent (b7da5d, "Field Sol") mints the secrets on ouranos as root and pipes them straight into the encrypted file, so no agent ever sees them. Seen: root on ouranos is reachable by key without a password. da88cf read your "get it fixed … best judgment" as the permission that the earlier plan said it needed.
- **Why:** it is the smallest change that follows the cluster topology and roles you asked for, keeps the network private, and puts every fact in cluster data.

**What was written (settled).** The tailnet-repair-da88cf bookmarks *(agent name)* carry the design above in code: horizon-rs's 0.13.0 change (section 6) adds a `TailnetController` record holding an optional certificate authority, and a `TailnetClient` record per machine holding only a reference to its secret, never the secret itself. CriomOS gained modules for tailnet roles, the trust list, a Headscale declaration that reads its certificate from an encrypted secret with chain verification, and a sign-in step that takes no key on its command line. goldragon's own cluster-data records (marking which machines carry which role) had not been started as of the last interim; that work is bundled into the same implementer's pass together with the Wi-Fi country and USB-downlink records (section 6).

**What was deployed:** minting of the tailnet secrets for the two machines already safe to change, ouranos and Prometheus, is authorized and, by Field's own account, done; the two mirror machines and the disposable test machine were left unminted (see the Questions section). None of this has reached a live machine yet, because it still needs the goldragon record above merged onto main first (see "goldragon" in the table above).

**Ouranos's own activation is separately PENDING, and has been blocked twice tonight, for two different reasons — said plainly:**
1. The first attempt (deployment 32) was blocked because the request that carried it left out its secrets entirely, a mistake in how the request was assembled, not a defect in Lojix. That is now understood and fixed (see "OpenCode secret and deployment 32" in section 6).
2. A second attempt is now blocked by a plain client-side error: Lojix's own tool reports it cannot find a file it expects, even though the machine it is talking to is listening and answering. This is under diagnosis now and is not yet explained.

The done sign for the tailnet specifically is `tailscale status` listing both machines; that has not happened.

**What remains:**

- Whether Tailscale should stop sending logs to Tailscale Inc. (it does now), and whether to keep using Tailscale's public relay servers.
- Whether the controller moves to Prometheus.
- Whether tailnet membership should follow the cluster trust values (question 8).

## 5. Building

**Prometheus by default (seen):**

- ouranos's builder list names Prometheus with 6 parallel jobs.
- ouranos itself is set to 0 local jobs, through an undeclared user setting from 09-12.
- Since Prometheus returned, only remote builds have been seen.

**Ruling on fallback:** see section 1. Local builds are allowed only after a visible remote failure, and each one is reported. da88cf also adopted a plan step to take ouranos off the cluster-data builder list, so that "no local jobs" becomes declared instead of hand-set.

**Builder capacity finding (settled).** Prometheus has 32 threads and 124 GB of memory, but the cluster data only ever declared it 6 parallel build jobs (one field drives both ouranos's list of remote builders and Prometheus's own limit on itself), so it was running under half-saturated all night. Separately, the machine's declared core count in cluster data was stale, and CriomOS's own wiring copies the job count into the core count rather than treating them independently, against the practice the Nix builder documentation recommends for a dedicated builder. The declared fix: raise Prometheus to 8 parallel jobs (leaving its own core limit uncoupled, a follow-up for later, not tonight), correct the stale core figure, and remove ouranos from the builder list entirely. This is folded into the same goldragon-data bookmark as the tailnet and Wi-Fi-country changes (section 4), so it lands with that deploy. **PENDING:** whether that change landed.

**Hotfixes found around Prometheus's absence, and their fate.** A *hotfix* here means a change made by hand outside the declared configuration.

| Hotfix | Seen / claimed | Fate |
|---|---|---|
| ouranos user nix setting: 0 local jobs, plus a plaintext GitHub token | Seen, since 09-12 (predates tonight) | Job count: to become declared, folded into the builder-capacity fix above (PENDING deploy). Token: stays until you rotate it (section 9). |
| 305 local build attempts on ouranos while Prometheus was off (14:08–20:49 host clock), mostly Flow and messenger-clj | Seen; per-command flags only, nothing persistent | Nothing to undo. Let the disk clean-up take them once Prometheus has rebuilt what is live. |
| Flow 0.12.2 run from a hand-installed profile entry plus a unit override on ouranos | Seen | The declared pin landed in CriomOS-home (00f95a's claim). The hand entries are removed only after the Home activation. **PENDING.** |
| messenger-clj 0.2.5 (every `hm-*` messaging command) hand-linked with no protection from garbage collection | Seen | Protected with a temporary GC root before the midnight clean-up (a *GC root* is a marker that stops Nix deleting something). The declared package landed in CriomOS-home (00f95a's claim). Links removed after activation: **PENDING.** |
| Heartbeat unit hand-disabled (13:13) | Seen; actor unknown | Stays disabled until the Home activation brings the fixed unit. **PENDING.** |
| Prometheus running system 55, built from an unmerged branch and switched in by hand over root ssh on 09-24 | Seen | To be replaced by a main-built system through Lojix (section 6). **PENDING.** |
| Stale user flake-registry pin (horizon → a missing temporary folder) | Seen | **Removed** (receipt). |
| Other agents' own hotfix lists | Claimed | 38de5b none persisted; 504461 empty; f5a74e none; e71dab none; 5f38bc two historical build realizations (provenance, not builder fallback). |

## 6. Integration

*Integration* means bringing finished branches into each repository's main line, so that a deploy from main carries every fix. da88cf ruled: **integration before any deploy**, with one integrator.

**Plan in order:**

1. Lojix 7.0.0 onto Lojix main.
2. The model list onto CriomOS-lib main.
3. The diverged Prometheus Home line (the Herdr, Message, Codex-next and Claude-bypass repairs) onto CriomOS-home main.
4. CriomOS main takes the one USB-downlink commit and the new pins.
5. Then the tailnet and Wi-Fi-country changes.
6. Then deploys: ouranos first, then Prometheus by a single scheduled "boot once" reboot with rollback. CriomOS's architecture requires boot-once for the large-AI node. da88cf read your "get all the fixes deployed to it" as covering one such reboot.

**Why a deploy of main tonight would have hurt Prometheus (seen):** main lacked the branch's 13 commits, pinned the old Lojix 6.0.0, and still pinned the old 17-model list. That list is what broke the 09-24 deploy attempt (number 31).

**Per repository, as settled so far:**

| Repository | Merged / kept | Discarded | Final revision |
|---|---|---|---|
| CriomOS-lib | models-da88cf *(agent name)*: Gemma beside Qwen3.5. It now **equals main** (seen). | — | 6db67c3b (seen) |
| CriomOS-home | 00f95a pinned Flow 0.12.2 and messenger-clj 0.2.5 in main 5f14f9da (claimed; checks passed on Prometheus). Diverged Home line merged: every file conflict was resolved (the newer Flow and messenger-clj kept, the branch's message revision taken as the one the running line already uses, both check lists merged). | Superseded Home-pin chains (luna6/opencode, flow07). The OpenCode commit 926d9d6e is **kept** on one bookmark for a future integrator. | Main is **4a9d85d7** (pushed, verified). A further set of fixes — the codex-next restart-loop handoff and the Blueprint fix below — sits pushed but unmerged on bookmark home-fixes-da88cf, which also carries two Herdr-related module fixes (below). That bookmark's own full list of 92 checks now evaluates cleanly; the earlier blocking failure over an unlicensed Android tool is gone. Folded into the same second integration branch as CriomOS above. |
| CriomOS | USB bus-role commit da85c4a, rebased alone, plus the reconcile unit from e92a336 | e0aef3d (never had a bookmark, nothing to delete); the MAC chain de5ac1b; usb-gateway-consumer-6db4fe (already on main). flow07-ouranos-b7da5d, re-checked against its true merge-base rather than main's current tip, carried no unique work and was **deleted**. | Main moved twice: first to **3e2cc8be** (pins lojix a67f5773, Home 4a9d85d7, CriomOS-lib 6db67c3b), then to **e6a83edc**, a lock-only move that repins lojix straight to 8.0.0 (below). A second integration branch then combined the Wi-Fi, other fixes, USB-downlink and tailnet work together with that new lojix pin, with no textual conflicts; merged cleanly. Its 92-item counterpart on the Home side (below) evaluates cleanly against it. Both hosts' full configurations evaluate against it up to the point the tailnet's still-missing certificate and secrets stop them, by design (section 4); everything below that point already carries the intended settings (Wi-Fi country and logging on Prometheus, the USB downlink wiring and the enrollment step on ouranos). Six checks still fail to even evaluate, all pre-existing (not caused by tonight's work): an old kernel-version pin, three checks missing pieces of test setup, and one, a lock check, whose own test fixture is now found to be missing a needed field. These six are assigned to a repair pass (below); two are already fixed on it. The checks that could be tried did pass, and were built on Prometheus: the Wi-Fi radio settings, the Wi-Fi policy, the USB-downlink wiring check, the corrected flake-registry entries (below), and the tailnet declaration; and both scripted end-to-end machine tests passed too — one proving a machine can enroll in the tailnet, the other proving the whole three-hop USB chain end to end inside virtual machines (section 8). |
| lojix | 7.0.0 (a67f5773) onto main | the MAC chain 387c13b | Main is now **8.0.0 (f090da07)**, by way of the version-repin train below, stacked on the row-quarantine work. All 14 of its own checks passed on Prometheus, including 145 individual tests. A snapshot check of the live data store, taken and repaired for reading only (nothing live was touched), found nothing that fails to read under the new version and no in-progress deploy row sitting around, so no extra repair step should be needed at deploy time — though that check must be repeated right at the deploy window, since this reading is already a little old. lojix 8.0.0 is now pinned on CriomOS main **e6a83edc** (a lock-file-only change) so Field's ouranos deploy can carry it. |
| horizon-rs | reset onto the consumers' line (`ee8d6f8`, 0.12.0), abandoning the corrective 0.5.1 restore | the MAC chain fed0a12; usb-gateway-6db4fe (already merged) | **Done.** Published on main as **0.13.0 (a3ddaf86)**: adds the tailnet records, the required Wi-Fi country field, and the new UsbDownlink capability, code regenerated, its own contract tests passed on Prometheus. This unblocks section 8. |
| goldragon | — | the MAC chain 1781f07; a911515 (matched to the branch holding the daisy-chain report's obsolete data format, and deleted) | **PENDING.** The record changes (tailnet trust, Wi-Fi country, UsbDownlink) are written on their own bookmark now, not yet on main: five secret names for enrollment, ouranos's tailnet references with no certificate authority recorded yet, its USB-downlink address range, and Prometheus's corrected country, builder count and processor-core count. Main itself has since moved separately, to rotate in a fixed OpenCode password (below); that move is not yet folded into this bookmark. Nothing here reaches a machine until the certificate and secrets below are minted and this bookmark is merged onto main. |
| messenger-clj | — | m6 (obsolete) and m1 (deferred until the message registry migrates). Both bookmarks deleted, provenance kept (00f95a's claim). | dfcf91f0 |

Branch deletions, held until the integrator reported the new mains, are **done** (receipt). Every ruled discard across CriomOS, horizon-rs, goldragon, lojix and CriomOS-home was proven and deleted. One side effect: pushing CriomOS-home's deletions also flushed three unrelated, already-locally-deleted bookmarks from earlier work in the same checkout; all three were checked and found to be ancestors of Home main, so nothing was lost.

Scale, seen: 207 repositories and 506 checkout folders exist. Among the deploy-path repositories, CriomOS has 83 branches, CriomOS-home 106, lojix 29 and flow 46. About 14 CriomOS workspaces hold uncommitted, undescribed changes that nobody has inspected.

**Stale workspaces (settled).** A separate audit had found 16 checkout copies (a *workspace* here is a second working copy of a repository, checked out beside the main one) that held nothing but a single trivial, already-superseded settings-file removal, plus one, home-canonical-llm-packages, that duplicated work already on Home main. All 16 are now forgotten (the workspace record removed; the on-disk folder itself is left for you, not deleted). Nothing else was touched.

**The horizon-rs repin train (settled shape).** A *repin* is moving a repository's declared dependency on another to a newer revision, cascading through everyone who in turn depends on it. Because horizon-rs's 0.13.0 change alters what is stored on the wire and on disk, everything under it needs its own version bump, not just a silent update: signal-lojix moves 5.0.0→6.0.0, meta-signal-lojix 6.0.0→7.0.0, lojix 7.0.0→8.0.0, then CriomOS repins lojix, then ouranos is deployed (which restarts the Lojix Nexus, the always-on service that carries out deploys, as 8.0.0), then goldragon's data and the hosts that read it. Two of signal-lojix's and meta-signal-lojix's own main lines had drifted onto the abandoned 0.5.1 horizon-rs line and must be reset before the train can branch from them.

The dangerous step is the Nexus restart itself: it re-reads every stored row at startup, and a row still written in the old shape would make it refuse to start, with no way back on this store version. **Settled:** lojix 8.0.0 now quarantines any row it cannot read at startup instead of refusing to start — it sets the unreadable row aside by itself, one log line each, and rebuilds only its own working configuration from its own built-in defaults, never from an old backup file. A test proves the point directly: a genuine old-format row, saved from before tonight, is now set aside rather than read, and the store still starts and answers queries normally. The train itself is done: the two libraries lojix depends on for its wire format each took their own version step, and lojix's own version moved to 8.0.0 on top of the quarantine work; every check on all three passed on Prometheus (145 tests, in lojix's case), and all three now sit on their own project's main line. A direct look at a copy of the real, currently-running data store (repaired for reading only; the live copy was never touched) found nothing that would be quarantined and no work-in-progress deploy record sitting around — so, as things stood at that snapshot, the restart should need no extra recovery step. That snapshot must be retaken right before the real restart, since it can go stale. **PENDING:** the ouranos and Prometheus deploys that carry this new version out to the machines (see the blockers above).

**Temporary things, integrated or discarded.** This is the wave-2 plan *(agent name)*: 56 inventory rows. Planned counts: 20 integrate, 17 discard, 11 need your word, 6 unknown, 1 keep, 1 follows another row. ⚠ marks rows that would break something live if removed too early.

| # | Item | Plan | Status |
|---|---|---|---|
| 1 | Flow 0.12.2 as a hand profile entry | integrate (Home pin) | pin landed (claimed); activation PENDING |
| 2 ⚠ | Flow Nexus unit override | delete after 1 activates | PENDING |
| 3 ⚠ | messenger-clj 0.2.5 hand links | integrate (Home package) | package landed (claimed); link removal PENDING |
| 4 | temporary GC root for 3 | discard after 3 | created (receipt); removal PENDING |
| 5 | retired messenger artifacts | discard after archiving | PENDING |
| 6 | stray `message-write-configuration` binary | discard | PENDING |
| 7 | `cloud-maintainer-chrome` (drives a DigitalOcean login) | your word | question 12 |
| 8 | heartbeat hand-disabled | integrate as feature | kept disabled until Home activation; PENDING |
| 9–13 | redundant or dead unit leftovers (limits drop-in, inert drop-ins, dji backup, swaync mask, failing fleet-cleanup timer) | discard | PENDING |
| 14–17 | monitors: core-checkup, field-census, field-checkup-shadow, field-luna-research | your word | question 12 |
| 18 | field-monitor-98eb43 timer | discard when 98eb43 ends | PENDING |
| 19 | cf7879 overnight batch unit | discard | PENDING |
| 20 ⚠ | Codex-next recovery unit holding the socket | integrate (declared handoff) | designed and committed, unmerged (Home bookmark, section 6); its check passed on Prometheus; the switch-over itself still waits for a quiet window (question 11) and the Home activation |
| 21 ⚠ | Herdr server started by hand | integrate; the handover needs your word | question 10 |
| 22 | unity-local-poc transient | discard | PENDING |
| 23 | 66–67 leaked test-harness processes | discard | e71dab held back; advice sent; PENDING |
| 24 ⚠ | ouranos USB firewall hook (daisy chain) | integrate as feature | section 8; PENDING |
| 25 | ouranos system 188 switched by hand outside Lojix | redeploy through Lojix | PENDING |
| 26 | Home claimed three ways | your word; tonight Home arrives through the system deploy only | question 6 |
| 27 | ouranos user nix builder settings | integrate (ouranos off the builder list) | adopted, with a concrete number now decided (Prometheus raised to 8 jobs, section 5); folded into the goldragon-data bookmark; PENDING deploy |
| 28 ⚠ | plaintext GitHub token | your word | question 5 |
| 29 | stale user registry pin | discard | **done** (receipt) |
| 30 | declared flake registry malformed ("owner is missing" warning on every nix call) | integrate | fix committed and pushed, unmerged (CriomOS bookmark, section 6), with a new shape-checking test; not yet evaluated or built, waiting for "builds open" |
| 31 | `nix-local-stack` tool | keep | kept |
| 32–33 | ~100 local-build GC roots; 305 unsigned local builds | discard via disk clean-up | PENDING |
| 34–36 | Headscale certificate, hand-set login address, no trust anchor | integrate | CA decision made and written into code (section 4: the cluster's own certificate authority, trusted on every machine); PENDING deploy |
| 37 | tailnet enrollment key | the plan asked for your word; da88cf decided under your delegation | PENDING |
| 38 | Tailscale uploads logs to Tailscale Inc. | integrate | left for your word (question 9) |
| 39 | controller on a roaming laptop | your word | question 7 |
| 40 | field-clj declared, not deployed | deploy | PENDING |
| 41 | Lojix socket variable wrong | integrate | settled: no source change needed. The declaration is already right; the long-running user session simply kept an old value from before it was fixed, and a relogin clears it |
| 42 | Prometheus system 55 unknown to Lojix | redeploy (boot-once) | PENDING |
| 43 | Prometheus manual roots | discard | PENDING |
| 44 | Prometheus stale Home profile | follows 26 | follows 26 |
| 45 | `mpd` failed on Prometheus and zeus | unknown | not addressed |
| 46 | zeus has no Lojix record | first Lojix deploy | PENDING |
| 47 | tiger, balboa, mirror-alpha/beta, vm-testing unreachable | unknown | not addressed |
| 48 | stale materialization for ouranos | fresh request | the flow found this is not a Lojix blocker (a hand-supplied June tree); PENDING |
| 49 | 5f38bc historical build realizations | likely discard | PENDING |
| 50 | ouranos Wi-Fi auto-join off ("Wi-Fi A") | your word | section 3 |
| 51–52 | messenger-clj m6 / m1 | review | m6 obsolete, m1 deferred, both deleted (claimed) |
| 53–54 | 5f38bc pin branches; all other branches | review | **done**; see the table above |
| 55 | Prometheus cannot fetch private field-clj | later; evaluate on ouranos, build on Prometheus | as planned |
| 56 | BIOS restore on AC loss | your hands | section 2 |
| N1–N4 | new USB-gateway schema, Lojix pin, data, lock bump | integrate | the shape was changed to UsbDownlink (section 8): the schema is published (horizon-rs 0.13.0) and the CriomOS consumer is committed and its checks pass, unmerged; the Lojix pin, cluster data and the VM test remain PENDING |

**What could not be done, and why:**

- The Home fixes (the Codex-next handoff and the registry fix) could not be committed while the integrator held the locks on CriomOS and CriomOS-home; both are now committed on the bookmarks named above, after the locks released.
- Nothing that needs your hands, your password or your ruling was done (section 11).

**The Blueprint defect (settled cause).** *Blueprint* is the tool both CriomOS and CriomOS-home use to assemble their full set of checks automatically from files on disk. It reads every check file to find its metadata before anything can filter or skip one, so a single check that fails to even evaluate breaks the whole set, for every other check, in both repositories at once — this is why so many unrelated checks above could not be run tonight. Three separate causes were found, none new tonight:

- CriomOS-home has a group of checks that need an unfree (non-free-licensed) Android packaging tool; the repository already has a way to allow that tool by name for its own explicit check calls, but Blueprint's own first pass evaluates the same files before that allowance applies, so it refuses regardless. Widening the allowed-name list does not fix it — it only fails on the next unfree name.
- A single Wi-Fi voice-assistant check has read the wrong field from the window-manager's configuration since 09-18, hidden until the point above is fixed.
- CriomOS's own ownership check still hard-coded an old Home revision to compare against, unchanged for 11 pins.
- The fix moves the affected CriomOS-home checks to a second, explicitly-called location that Blueprint does not auto-scan, fixes the misread field, and deletes the stale hard-coded revision in CriomOS (the lock already enforces the real one). Both halves are written; the Home side's full check list now evaluates cleanly under it.
- **Settled, what the final pass found.** With that fix in, the CriomOS side still cannot evaluate six checks: the same unlicensed-tool problem does not recur, but an old kernel-version pin, three checks each missing a piece of test setup, and one check's own test fixture (found only once the earlier failures cleared out of the way) are all pre-existing and unrelated to tonight's changes. A follow-up pass, running in parallel with everything else tonight, was aimed at two of the six; the outcome is folded into "wave 3" below, still **PENDING**.

**Herdr module fixes (settled).** Herdr is the terminal/session-management layer these agents run inside. Two of its declared Home checks were failing, and in both cases the check was innocent and the module was wrong:
- One check makes sure a stale integration hook left behind by Herdr's Codex helper is cleaned up. The module was only cleaning up hooks belonging to the current user's own home folder; a copy left behind by a home that had since moved elsewhere was missed, so it kept reporting the agent's state twice. The module now removes any such stale hook regardless of whose home it names. Separately, the check itself compared the wrong exact text and could never have passed even before that; both are now fixed and proven by a real build on Prometheus.
- The second check makes sure a backup of the person's Herdr settings, taken before Home Manager (the Home configuration tool) ever touched them, is kept safe. In one case, the module had been recording Home Manager's own already-changed file as if it were that original backup, which is wrong. It now insists a genuine original backup already exists before treating a later linked file as trustworthy, and refuses rather than guessing. Also fixed and proven by a real build on Prometheus. No live settings or running session were touched by any of this.

**OpenCode secret and deployment 32 (settled cause).** OpenCode is a coding-assistant capability CriomOS can run; testing it needs a password kept as an encrypted secret. Tonight's first deploy attempt toward ouranos, Lojix's own deployment number 32, failed before anything was built, because the request that carried it was built from an older template that supplies no secrets at all. This was checked directly against Lojix's own code and data: Lojix used the correct, current cluster data throughout; the request itself was simply the wrong shape. **Not a Lojix defect.** The real password was traced and found to be stored correctly, just not reaching that request; because only Prometheus's own key could unlock it, Field rotated it in place on Prometheus itself as root, adding ouranos as an allowed reader, with the plaintext value never leaving Prometheus. That rotation is done, committed and pushed. The next ouranos deploy attempt was resubmitted with the correct secrets included (see "ouranos activation" below for what happened next).

**Building through the laptop, and a design for skipping it (settled finding; a fix is in progress).** Lojix's current way of running a build looks up what to build without ever leaving the laptop, which is safe, but then, for a build on another machine, still brings every finished result back onto the laptop's own disk before it is sent on to where it is actually needed — so every model file bound for Prometheus lands on the laptop first, sometimes more than once. An older way, used up to several versions back and once again by hand two nights ago, built straight into the far machine's own storage and skipped the laptop entirely, but it broke because a later change made even the look-up step travel over the same fragile connection used for building, which can freeze the whole thing. The chosen fix keeps the look-up step local, exactly as it is tonight (so nothing freezes), and sends only the already-worked-out build instructions to the far machine to be built there, so no model bytes would ever touch the laptop's disk. This changes nothing about how any other tool's data is shaped or stored. It is being written now as the next lojix version; if it is finished and proven in time, tonight's Prometheus deploy uses it, and if not, Prometheus's deploy takes the slower, laptop-staged path instead.

**Flow's own version, corrected twice (settled).** Flow (the software that tracks each agent session) had its 0.13.0 version superseded before it was ever adopted, once a bug was found in how it decides a session has ended: before the fix, a session whose terminal window had simply closed could be mistaken for one that was formally and permanently ended, when only an explicit, deliberate command should mean that. The fix distinguishes a closed window ("Exited," which can be revisited) from a deliberate end ("Retired," which cannot). The corrected version, 0.14.0, and everything that depends on it, now carry that fix; Prometheus is rebuilding the check for it now. **PENDING:** Flow 0.14.0's verdict.

**A third wave of work (dispatched; PENDING).** With time and this session's usage still available after the above, a further, smaller round of work was started: turning some informally-running monitoring timers found tonight (see "Things found," below) into a proper, optional piece of Home configuration that stays off unless turned on; turning the informally-started Herdr server (also below) into a proper background service instead of a hand-started one; and widening the check-repair pass above to aim at all six of tonight's pre-existing check failures rather than just two. **PENDING:** all three results.

## 7. Local AI models (the status you asked for)

**What you said on 09-16 (typed, verbatim):** "Yeah, we almost don't want most of these models. Let's just keep Gemma and the latest Quan that we have, maybe, and remove everything else. Let's get the best model that we've determined on our own research to be the only dependency, and then we can garbage collect. Just keep one model at all times, right? Let's pick one that you keep, at least, so that the machine remains operative. After your garbage collection, it still has a model to operate from. Tell me which models you picked, and we don't need Nematron. I think that's a pretty garbage model, or Lama or any of that."

**What was done then, and where it diverged (seen):**

- The shared model list in CriomOS-lib was cut to **one** model, Qwen3.5-122B-A10B. **Gemma was dropped**, against your word. The agent's question to you about Gemma was never answered.
- The Qwen kept is the biggest one, which was already loaded at start-up. It is not the newest: Qwen3.6 entries existed.
- CriomOS main still pinned the **August list of 17 models**. So Prometheus, even tonight, serves **17 models** from its llama.cpp router on port 11434. **707 GB** of model files are held by the running system and by 24 hand-made roots from March.
- A Qwen-only Prometheus system was built on 09-17 but never switched in. Its leftover root on ouranos held about 80 GiB.
- browser-use in your Home is hard-wired to the Gemma model gemma-4-26b-a4b. Dropping Gemma would have broken it.

**What was restored tonight (seen):**

- CriomOS-lib now lists exactly **gemma-4-26b-a4b** (taken from the August list) and **qwen3.5-122b-a10b**. This is the minimum that follows your words.
- The change is on CriomOS-lib main.
- **PENDING:** CriomOS pinning that list, and Prometheus running it. The 17 models and 707 GB stay until then.

**The Qwen "latest" fork (your ruling):**

- Keep **3.5-122B** (current, largest).
- Or **3.6-35B-A3B** (newer, in the old list).
- Or **Qwen3.8-27B**, which exists only on an unmerged proposal branch with verified hashes.

**What remains:**

- Your pick of Qwen, and whether to add Laguna S (its license awaits your confirmation). Motif 3 does not fit.
- The CriomOS pin bump and the deploy.
- Removing the 24 old hand-made model roots on Prometheus, which frees most of the 707 GB (flow's inference).
- Seen: the model listing on port 11434 answers **without the API key**.
- Unknown: whether the speech-to-text models you mentioned on 09-13 are still wanted. None are declared.

## 8. Daisy chain

The *daisy chain* here is ouranos → Prometheus → zeus by cable. ouranos's built-in port is its uplink, and its USB Ethernet feeds Prometheus. Prometheus's USB Ethernet feeds zeus.

**Declared versus hotfix (seen):**

- The chain carries Internet to zeus right now. At 21:31 (host clock), zeus fetched a web page with its wired port forced and got success.
- But it works **only through hotfixes**:
  - on ouranos, a hand-made shared connection ("prometheus-share-temporary") plus a root firewall hook;
  - on Prometheus, system 55, built from an unmerged branch.
- On main, Prometheus's USB rule matches by driver name, and that is proven to fail on renamed links.
- So your condition, "deployed … properly and there are no hotfixes", is **not met yet**, and tonight's pass does not count as your test.
- Caution: zeus is also on another Wi-Fi ("Mega_2.4G_1896"). A test that does not force the wired port passes falsely.
- Three NATs sit in series (Prometheus, ouranos, the house router). *NAT* is address sharing. The testing rule prefers one.

**Decision:**

- One declared capability, **UsbDownlink** *(agent name)*. Any USB Ethernet is a downlink, chosen **by its USB bus role, not by name or hardware address**. This follows your "no matter how we plug things in … Kind of statelessly".
- It carries only the downlink subnet (ouranos 10.44.0.1/24; Prometheus 10.18.0.1/24).
- The earlier chain that matched hardware (MAC) addresses, across four repositories, is discarded.
- One Horizon version is used by both Lojix and goldragon.

**Blocker (horizon-rs, seen):**

- The main line of horizon-rs, the schema library, was replaced on 09-21 by an older interface (version 0.5.1). No consumer can follow it.
- Consumers also disagree: Lojix pins 0.10.1 and goldragon needs 0.12.0.
- da88cf decided to reset horizon-rs main onto the consumers' line, once the Wi-Fi implementer reports who made that commit and whether it is live work. horizon-rs work is queued behind the Wi-Fi change.
- **Settled: the reset is done.** horizon-rs main now carries the tailnet, Wi-Fi-country and UsbDownlink changes together as version 0.13.0, based on the consumers' line rather than the abandoned one.

**What was written (settled).** The CriomOS side of UsbDownlink is committed on its own bookmark (section 6): any USB Ethernet link is joined into one bridge and served its own address range by the router-style DHCP server already used elsewhere, with one firewall and address-sharing owner per machine, network manager kept off the USB links entirely, and warnings printed (not acted on) naming the two ouranos hotfixes so they can be cleared by hand once this is accepted. Every check that could run outside a live network passed on Prometheus.

**Settled: the three-hop chain now passes end to end, inside virtual machines.** Its first run anywhere failed on a defect in the test's own stand-in Internet server, not in the daisy-chain design: the stand-in server tried to start answering on an address before that address had actually appeared on its own network interface. Once the test was told to wait for the address first (a fix to the test only, nothing about the real design), a full run passed all seven parts: the stand-in Internet itself comes up; the first hop (ouranos's uplink) reaches it; the second hop (ouranos's USB downlink to Prometheus) gets an address, resolves names, and fetches a page over its forced wired connection; the third hop (Prometheus acting as house router) shares its connection the same way; the fourth hop (the client behind Prometheus) does the same, and the upstream side sees the correct address for it; unplugging and replugging a link is tolerated; and losing the upstream link correctly takes Internet away downstream. It also settles, as a side finding, that the USB networking devices these hops depend on work correctly inside virtual test machines. **This is proof inside VMs, not on the real machines.**

**PENDING:**

- the UsbDownlink cluster data (which subnet belongs to which machine) and the Lojix pin that would carry it — already written, but held back behind the same missing tailnet certificate and secrets as section 4;
- merging the CriomOS bookmark into main (bundled with the Wi-Fi, tailnet and other fixes above, held back the same way);
- the deploys (Prometheus, then ouranos);
- **the live, on-hardware test**, graded per hop as source-published, installed-running or end-to-end-proven — the VM proof above does not stand in for it, and needs the deploys above first.

The unplug-and-replug cases still need your hands on the real hardware and stay unknown until then.

## 9. Things found that you should know

- **A GitHub token in plain text.** A GitHub access token sits readable in ouranos's user Nix settings. Its value was not copied anywhere. It must stay for now, because ouranos needs it to read private repositories such as field-clj. Recommendation: treat it as exposed, rotate it, and let it be stored encrypted.
- **Undeclared monitors** run on ouranos from working copies, not from the declared configuration:
  - core-checkup (every 30 min);
  - field-census (every 5 min);
  - field-checkup-shadow;
  - field-luna-research;
  - a failing fleet-cleanup timer whose source is gone;
  - a `cloud-maintainer-chrome` script that drives a DigitalOcean login. Its owner is unknown.
- **The Codex-next loop.**
  - The declared Codex-next server has restarted about **42,845 times**, every 2 seconds, since 09-24.
  - A hand-started "recovery" copy holds its socket, and live Codex-next sessions run on that copy.
  - A declared handoff is designed and committed (unmerged, section 6): the two services are set never to run at once, and the service is stopped from restarting more than 5 times a minute. Its check, run directly against the declared configuration, matches; the built version also matches every field of the copy actually running. Switching over stops the copy and its sessions, so it still waits for a quiet window.
- **The Herdr server is transient.** The Herdr server behind every open pane was started by hand on 09-19 as a temporary unit. Moving it under a declared unit means one restart that closes all panes.
- **Disk.** ouranos was at 92% full. A stale 80 GiB root (the never-used Qwen-only Prometheus system) was removed after checks. The 00:00 clean-up ran and freed 9.3 GiB (section 2), but ouranos still reads 93% full, one point fuller than before, because other, unrelated writes landed in the same window.
- **Home claimed three ways.** On ouranos, three mechanisms each claim a different Home version, and none agree:
  - the system's Home module;
  - a standalone Home profile from a failed deploy;
  - Lojix's record.

  Tonight Home arrives only through the system deploy.
- Also seen:
  - ouranos's running system (188) and Prometheus's (55) were both switched in by hand outside Lojix;
  - the tailnet has not worked since at least June;
  - Prometheus's Wi-Fi works, but its country is wrong.

## 10. Stale messaging routes, lock cleanup, and psyche recovery

**Stale route retirement (settled, final grade).** Your grant authorized retiring stale rows in the agents' own internal messaging registry (a *route* is one entry in it, naming a session that can be reached) once each was individually checked to have no live process, no open terminal pane, and no current seat. One subflow was named sole executor and retired exactly 24 named rows, none outside the list. Review found the work correct in scope, but only witnessed by its own executor, not independently: at the time of the check, 13 of the 24 retired rows still had an existing terminal pane with no agent bound to it ("agentless"), which the strict rule should have excluded. Those 13 were followed up and confirmed idle leftover shells belonging to the very sessions just retired, and were closed; nothing was undone, since a retired route can always be re-registered. **Final grade: B** — every retirement itself checks out, but proven only by the retiring agent's own account, not by a second witness.

Eight Orchestrate locks (a reservation recorded through the shared lock tool, holding a set of paths for one agent session) held by those now-retired sessions were re-verified and released, with a full record of the check. A further four locks held by an already-retired session whose branch of work was separately ruled abandoned were released the same way once its own retirement and its successor's retirement were both confirmed.

**Settled, a further batch found and released tonight.** A separate agent session, tied to old work on the USB-networking schema and unreachable by any live route, was found still holding another 24 Orchestrate locks. Each was independently re-checked the same careful way — no live session, nobody claiming to have taken over its work, and nothing left uncommitted under the paths it held — before being released. One of those locks named a file that tonight's own tailnet work deletes; it turned out to sit only in that absent session's own separate copy of the repository, never touched by tonight's changes, so nothing already in progress was disturbed. **Total stale Orchestrate locks released tonight: 36.**

**Psyche recovery (settled counts).** Two long transcripts were combed for words of yours that were never written into a vision record. Of ten candidate lines in one, five were already recorded, three were working instructions, one a process complaint, and one needs your reading before it can be recorded (below). Of 147 candidate turns in the other, 68 were already recorded, one new line was recovered and recorded ("The default effort is medium"), 35 were working instructions, 25 were questions or process talk, nine were clarifications not worth recording, one trailed off unfinished, and three more need your reading (below).

Four passages await your word before they are recorded, because the words alone do not settle what they mean:

- You asked "Do you not count?" after a proposed rule left Psyche out of who owns delegated work. Did you mean Psyche owns such work beside Mind and Field, or only that the specific line was wrong?
- You said a session not reachable by Remote Control does not exist, after being told of two panes carrying the same title. Is that a standing rule for every session going forward, or a remark on that one pane?
- You said 800 characters is not a long prompt, when told the plan was one 800-character opening line. Does this only restate what you had already said about prompts needing to be long, so nothing new needs recording?
- You asked why two named skills should not be loadable by an agent, and questioned whether two others are useful at all. Do you want those made loadable, and the other two reviewed for removal?

## 11. Questions needing your ruling

1. **Which Qwen?** Keep 3.5-122B (largest, current), or switch to 3.6-35B-A3B, or Qwen3.8-27B from the proposal branch? Gemma 26B is restored beside it. Add Laguna S now (68 GiB; license to confirm)?
2. **Phone and Wi-Fi.** Does your phone connect to goldragon.criome with WPA3 (SAE)? If it fails, may an agent watch the access-point log while you retry?
3. **Laptop on Wi-Fi.** Should ouranos rejoin goldragon.criome automatically, reversing the 09-24 "Wi-Fi A" that an agent made?
4. **Power.** Will you set "Restore on AC power loss" to On in Prometheus's firmware, so it comes back by itself after a power cut?
5. **GitHub token.** May we treat the token in ouranos's Nix settings as exposed? You rotate it, and the new one goes into an encrypted secret. The old line stays until then, because private repos need it.
6. **Who installs Home.** Should Home come with the system deploy, or through its own Lojix "user environment" deploy? Today three mechanisms claim three different versions.
7. **Where Headscale lives.** Keep the tailnet controller on the laptop, which roams and sleeps, or move it to Prometheus, which is always on?
8. **Who is in the tailnet.** You said "those are the trusted nodes based on the trust value there in the cluster". Should membership follow trust? For example, tiger and zeus are trust Max but not tailnet members, while mirror-alpha and mirror-beta are members with no trust entry.
9. **Tailscale Inc.** Stop Tailscale sending logs to Tailscale Inc.? Keep using Tailscale's public relay servers? On 09-10, Headscale refused to start 29 times because it could not reach them from behind a hotel-style portal.
10. **Herdr restart.** When may we restart the Herdr server under its declared unit? It closes every open pane once.
11. **Codex-next cutover.** When is a quiet window to move Codex-next sessions from the hand-started copy to the declared server? It ends the attached sessions.
12. **Monitors.** Keep or drop core-checkup, field-census, field-checkup-shadow, field-luna-research, and the cloud-maintainer-chrome DigitalOcean script?
13. **Backup Wi-Fi radio.** Plug the ASUS dongle back into Prometheus in place of the SanDisk stick? Or declare the dongle's new port name?
14. **Daisy chain NAT.** Prometheus is both the house Wi-Fi router, with its own address sharing, and a downstream of ouranos. Accept three address-sharing layers in series as a named nested setup, or should one owner do it?
15. **Prometheus reboot.** da88cf read "get all the fixes deployed to it" as allowing one scheduled boot-once reboot of Prometheus with rollback. The reboot briefly drops your Wi-Fi, zeus and the builder. Confirm, or name a time.
16. **OpenCode in Home.** Your earlier request for OpenCode in Home is kept as open work, not done tonight; its saved content is kept safe for a future integrator. Separately, and not the same question: the password OpenCode's own testing feature needs was found correctly stored but was not reaching tonight's deploy request; that mismatch is now fixed and has nothing to do with whether OpenCode itself joins Home. Still wanted?
17. **Unfinished words.** "Also on the message and flow development, which you'll get started on when you refresh ..." What did you want started?
18. **"lambda daisy chain".** Was "lambda" a mis-transcription? Of what?
19. **Model listing.** Is it acceptable that Prometheus's model list answers on the network without the API key?
20. **Speech-to-text.** Are the speech-to-text models you mentioned on 09-13 for Prometheus still wanted? None are declared.
21. **Tailnet secrets for the test machines.** Minting tonight reached only ouranos and Prometheus, the two machines already proven safe to change. The two mirror machines and the disposable VM-testing machine were left without their tailnet secrets. Should they be minted too, and should that follow the same trust-based membership question raised in question 8?
22. **A known inefficiency in the build tool, not fixed tonight.** One field in the shared cluster-data language sets a machine's building capacity two ways at once — how many builds it runs side by side, and how many processor cores each one may use — where the underlying build tool's own guidance says these should be set independently. Separating them is planned as a future change to that shared language. No ruling needed; noted here so it is not lost.
23. **Three departures from tonight's usual rules, for your awareness, not your ruling:**
    - One subflow, repairing a copy of Lojix's own stored data, built a small one-off tool directly on the laptop with the ordinary Rust build tool, in order to safely open one file — the one place tonight where "Prometheus always builds" was not followed, reported here as that rule requires.
    - A receipt recording one piece of tonight's clean-up was saved into the shared records using the older, no-longer-approved version-control commands directly, instead of the newer ones this project uses everywhere else.
    - A separate subflow, on finding other agents' unrelated, uncommitted changes already sitting in a shared working copy it needed to write in, committed those other changes first, as their own commit, before doing its own work — exactly as the standing rule for a dirty working copy requires, but worth knowing since it means other agents' unfinished work was committed on their behalf without asking them.

## 12. Receipts

- vision/prometheus.md, vision/clusterData.md, vision/daisyChain.md
- log.md
- reports/prometheus-return.md
- reports/wifi-prometheus.md, reports/wifi-power.md
- reports/headscale-repair.md
- reports/prometheus-pending.md
- reports/local-models-status.md
- reports/daisy-chain-plan.md
- reports/wave2-plan.md
- reports/inventory-system.md, reports/inventory-branches.md
- reports/home-fixes.md
- receipts/gcroot-messenger-clj.md, receipts/stale-root-348e7b.md, receipts/registry-pin-removed.md
- receipts/broadcast-prometheus-only.md, receipts/messages.md
- flows/f5a74e/reports/codex-next-unit.md (Codex-next evidence)
- reports/integration-1.md (integration step 1: the CriomOS-lib, CriomOS-home and CriomOS main moves, and lojix's stalled check)
- reports/step2-fixes.md (the codex-next handoff, flake-registry and lojix-ownership fixes)
- reports/blueprint-check-fix.md (the Blueprint aggregate-check defect and its diffs)
- reports/builder-capacity.md (Prometheus's builder-capacity finding)
- reports/usb-downlink-impl.md (the UsbDownlink consumer and its test plan)
- reports/repin-train.md (the horizon-rs repin train map)
- reports/prometheus-deploy-risk.md (how a Lojix deploy stages through ouranos's own store)
- reports/psyche-recovery.md (the transcript recovery counts and the four proposals)
- reports/wifi-password-path.md (the Wi-Fi password's location and retrieval steps)
- receipts/branches-deleted.md (every ruled branch discard, proven and deleted)
- receipts/workspaces-forgotten.md (the 16 stale checkout copies forgotten)
- receipts/stale-locks-released.md (all 36 stale locks released tonight, in three batches)
- reports/integration-2.md (the second integration branches, their check and VM-test results, and the blockers)
- reports/repin-train-run.md (the version-repin train's run, its checks, and the data-store snapshot)
- reports/lojix-store-tolerance.md (the row-quarantine change and its tests)
- reports/lojix-realize-on-target.md (the design for building on the target machine's own storage)
- reports/lojix-secrets-input.md (deployment 32's cause)
- reports/opencode-secret.md (the OpenCode password trace)
- receipts/gc-2026-09-26.md (the midnight clean-up result)
- **PENDING:** receipts for the tailnet deploy, the Wi-Fi deploy, the live on-hardware daisy-chain test, and Flow 0.14.0's verdict.

## 13. Since the handoff (successor b860be)

*Clock: this section was given its final refresh at `Sat Sep 26 02:09:50 AM CST 2026` (`date`, host time), by a subflow of b860be, from `flows/b860be/reports/handoff.md` (the state at wind-down) and `flows/b860be/log.md` (everything after the entry "Night report section 13 refreshed"). Earlier "host" times reported in this flow's messages were relay-based estimates running about 50 minutes ahead of the machines' own clocks; times below are host time.*

How to read this section — the grades used are the ones this flow always uses, kept plain:

- **Witnessed** means a subflow launched by b860be saw the thing directly — in command output, on a remote machine, or in a file — b860be's own eyes, or its subflow's.
- **Claimed by X** means a message from flow X (another agent session) reported it, and nobody here re-checked it independently. A claim is relayed as a claim, never upgraded to a witnessed fact just because it was written down.
- **Inferred** means b860be reasoned it out from what was read or witnessed. It is not itself an observation, and is marked as such below.
- Names with six hex characters (b860be, da88cf, b7da5d, e167d8, and so on) are agent sessions this project calls "flows" — each is a separate run of an AI agent given a role for the night. A flow's own invented codenames for its work are explained once, in parentheses, the first time they appear.

### 13.1 What landed and was witnessed

- **goldragon (the cluster's shared configuration repository) main moved to ddf27e0c.** This is a merge of the secrets commit (3e6ecfa9) with the tailnet certificate-authority bookmark (e8ce1e42) — "tailnet" is the private mesh network joining the cluster's machines, and a bookmark is this project's name for a named branch. The merged data carries: the tailnet's certificate authority, ouranos's declared downlink network (10.44.0.0/24, the range of addresses ouranos hands out over its USB connection to Prometheus), five pre-authorization names for joining the tailnet, and Prometheus's declared build capacity (8 build slots, 16 GB of usable memory). Five encrypted secrets files are present and readable by their intended machines; two more sets of secrets (for two mirror hosts and a test host) are declared in the configuration but were not created ("minted") tonight. A full configuration check ("nix flake check") passed on the Prometheus build machine after the merge. *Witnessed.*
- **field-clj (a Clojure programming-language build) was fixed.** The cause, seen directly: field-clj's dependency-fetching step reads whichever Clojure command-line tool is installed on the machine doing the fetch, and bakes that tool's exact output into a fixed hash. ouranos's system carries Clojure 1.12.5; field-clj's own separate lock file assumes 1.12.6 — so two different, both internally-consistent, hash values existed depending on where the fetch ran. Each of the two hashes was rebuilt twice on Prometheus and came out identical both times (so the mismatch is not random flakiness — it is genuinely two different, deterministic outputs). Fix: field-clj's own dependency file now pins Clojure 1.12.6 explicitly, removing the ambiguity. field-clj main is now a2c278d3 on the real remote; its package and tests pass on Prometheus under both versions of the underlying package set. Left open: nobody actually looked inside the wrong-hash output to confirm only the Clojure version differs (see 13.3); and any other Clojure build that inherits its Clojure version rather than pinning it — named tonight: messenger-clj — can hit the same mismatch. *Witnessed* (receipt `receipts/field-clj-deps-hash-2026-09-26.md`).
- **ouranos disk space recovered.** A leftover temporary reference to an old, unused system version (26.05) was removed, then a garbage collection (deleting store data nothing points to any more) freed 9.5 GiB across 7,773 files. Free space on ouranos rose to 20 GiB. The two local AI model file-sets (called Gemma and Qwen here) were left untouched, as ruled. *Witnessed* (receipt on main).
- **goldragon's check passed again, and its main moved a second time.** After the mint above, the merged goldragon revision (e8ce1e42) was checked once more on Prometheus and passed, confirming ouranos's declared network, the certificate authority, the five pre-authorization names, and Prometheus's declared build capacity were all present. goldragon main was then moved to carry this data (see the ddf27e0c bullet above); deploys tonight use that revision. *Witnessed* (receipts `receipts/goldragon-check-2026-09-26.md`, `receipts/goldragon-main-2026-09-26.md`).
- **Zeus (a ThinkPad T14 Gen2 laptop, one of the cluster's machines) was checked directly.** It appears in the cluster's configuration data with three roles: Edge, LowPower, and HardwareVideo. It now answers over the cluster's private mesh network (an earlier check tonight found that route dead); it has 188 GiB free; it runs a system that was installed by hand rather than through Lojix (the cluster's deployment tool), and Lojix has no deployment record for it at all — tonight's planned deployment to Zeus would be its first ever, with no prior Lojix state to roll back to if it goes wrong. *Witnessed* (receipt `receipts/zeus-probe-2026-09-26.md`).
- **The Lojix deployment record for ouranos was read directly.** At the point read, deployment 32 (an attempt to apply the new configuration to ouranos) had failed at the evaluation stage — before any files changed on the machine. Deployments 33 and 34 came later and are covered as claims in 13.2, since they were not re-read directly by b860be after they reached their final state.
- **The mint-pipe problem was found and ruled on directly by a b860be subflow, not merely relayed.** The secret-storage tool (gopass) silently switches into write mode whenever its input is piped rather than typed at a terminal, so any script reading a secret through a piped command was overwriting it with nothing. Proven with a throwaway secret and a throwaway cryptographic key: reading with input explicitly redirected from an empty source (`</dev/null`) returns the real bytes; reading without it returns zero bytes. The ruling — read every secret with `</dev/null`, skip the secret-storage tool entirely for the pre-authorization keys, keep only ouranos and Prometheus as recipients for now — was sent to b7da5d directly and is what let the mint above actually complete.

### 13.2 What is claimed by other seats (not independently re-checked by b860be)

- **b7da5d (the Field seat, run on a different agent harness than this Fable seat), claims:**
  - Deployment 33 (the "evaluate" stage — checking the new configuration is valid without changing anything) succeeded. Deployment 34 (the "realize" stage — actually building it) failed: the build tried to fetch field-clj's dependencies and hit the very hash mismatch fixed above; nothing was activated, tested, or cleaned up afterward. The fix has not yet been carried through a fresh deployment attempt.
  - The three machines' exact remote-access addresses (ouranos, Prometheus, and Zeus, each reached at `ssh-ng://root@<name>.goldragon.criome`) all resolve and answer. Prometheus is on its current, already-running system version with 45 entries in its boot history and a finished (not stuck) deployment history; Zeus is on its current, already-running version with 13 boot-history entries and no deployment history, matching a hand install.
  - The tailnet mint (creating the shared certificate authority and each machine's own key) is complete for the "verified subset": ouranos, Prometheus, the certificate authority itself, and the server certificate. The three additional recipients (two mirror machines and a test machine) were deliberately skipped for tonight.
- **The integration subflow (working under b860be, its own claim about work still in progress at wind-down):**
  - Home's own configuration bundle (called CriomOS-home) reached a combined bookmark, 7dd9e666, joining: the version pins for two internal services (flow and message), a set of new off-by-default monitoring options (called "wave-3 Home"), an on/off switch for a heartbeat service, and the field-clj a2c278d3 fix above. It evaluates cleanly against ouranos's live configuration data.
  - The host-level configuration bundle (called CriomOS) reached a combined bookmark, also named integration-2-b860be, joining: a patch removing a temporary network workaround ("the hotfix"), the Home bundle above, six repaired pre-existing check failures ("wave-3 checks"), and a filter keeping whole-machine build targets out of the automated check set. Both machines' full configurations evaluate against the cluster data, but the actual builds on the Prometheus build machine had not finished running when work stopped — so the rule requiring green builds before moving either bundle's main line was never satisfied.
- **da88cf (Fable, the predecessor seat that handed off to b860be earlier tonight), claims:** a newer version of the deployment tool itself (lojix 8.1.0, which builds each machine's update on that machine rather than staging it through ouranos first) is ready and tested — 15 automated checks pass, including a new two-machine test — but by ruling it is deliberately not used tonight, to keep tonight's already-complex ouranos update from changing two things at once (see 13.4).

### 13.3 Inferences

- **Inferred:** the deployment-34 build failure and the field-clj fix are almost certainly the same event — the hash the build expected (starting "HNlk…") matches field-clj's own declared value, and the hash it actually got (starting "vsJ2…") matches what Home's older-Clojure toolchain would produce. But nobody opened the wrong-hash output to look inside it, so it remains an inference — not a witnessed fact — that only the Clojure tool version differs and nothing else in the dependency tree changed.
- **Inferred:** any other Clojure build in this project that inherits its Clojure version from whatever's installed, rather than pinning it explicitly — messenger-clj was named tonight as one — can hit the identical mismatch. This follows from the same mechanism just fixed in field-clj, but messenger-clj itself was not tested tonight.
- **Inferred:** with goldragon main now carrying the merged secrets and certificate authority, and the field-clj fix now on its own main, what stands between tonight's state and a working ouranos/Prometheus/Zeus deployment is mainly procedural — finishing the integrator's builds, then retrying the deploy — rather than a further undiagnosed technical problem. This is reasoned from the state of the two blocking revisions, not from a fresh deploy attempt that was actually run and witnessed.

### 13.4 What did not happen tonight, and why

- **No main line moved for either configuration bundle.** Both the Home bundle and the host-level bundle evaluate cleanly against the cluster's live data, but neither had a completed, witnessed build pass on the Prometheus build machine before wind-down, and the rule in force requires green builds before either main line is moved.
- **Deployment 34 (the ouranos rebuild) failed and its fix was not yet retried.** It failed on the field-clj dependency-hash mismatch described in 13.1; the fix landed on field-clj's own main line afterward, but no new deployment attempt carrying that fix had run by wind-down.
- **No deployment reached ouranos, Prometheus, or Zeus.** Ouranos's own deployment sequence stopped after the failed build; the Prometheus deployment that was meant to follow it never started; Zeus — which the living had explicitly asked to be brought up to date — was only checked for whether it answers and what the cluster's configuration data says about it. It never received a first deployment through Lojix, the cluster's deployment tool.
- **Wind-down, at 99% weekly model usage.** At approximately 02:00-02:30 host time, the living reported (through e167d8) that weekly model usage across Claude seats stood at 99%, resetting 06:59 America/Mexico_City, and ordered the Claude-run seats to wind down and write down their state before being stopped mid-task. b860be told its subflows to stop starting new work, land what was ready, release any held locks, and write notes; it wrote its own handoff (`flows/b860be/reports/handoff.md`) and this book's final refresh before anything else. Codex-run seats (Field Sol, b7da5d) are not limited by this usage cap and were told they may keep working under the rulings already written in the handoff, each gate still witnessed before acting on it. Those wind-down messages reached both b7da5d and e167d8 (Transported — sent and accepted, not yet re-checked as read).

**Questions for the living**, carried from the handoff and restated in plain terms:

- Which monitoring scripts (wave-3 Home declared five: core-checkup, field-census, field-checkup-shadow, field-luna-research, and one named field-monitor-98eb43) should actually be turned on, and where should each run? None is enabled yet.
- When can the shared terminal-sharing server (called Herdr here) be handed over to the new declared version, and where should the roster of machines for the "core-checkup" monitor come from?
- When should the newer, faster deployment tool (lojix 8.1.0, which builds each target machine's update on that machine instead of staging everything through ouranos) replace tonight's version (8.0.0)? It is tested and ready but was deliberately held back tonight.
- Is the safeguard planned for the second ouranos deployment — testing that Prometheus's network route survives before finalizing — enough protection against the removed network hotfix, or should rolling back ouranos to an earlier version be blocked outright until the hotfix's network profile is restored some other way?
- Who owns fixing the one package that still fails to build on Prometheus (criome-deps, version 0.9.0) and the one check that depends on it?
- Who supplies the machine identities (SSH host keys) needed to finish the tailnet mint for the two mirror machines and the test machine that were skipped tonight?
- What is the right window to actually deploy to Zeus, given it has no prior deployment history through Lojix to compare against or fall back to if something goes wrong?
- Should the transient, frequently-restarting version of a network-control service (`codex-remote-control-next-recovery`, restarting roughly every 2 seconds and currently holding around 863 active tasks) be cut over to its intended, stable, declared replacement — and if so, what counts as a quiet-enough moment to do it without dropping live connections?

### 13.5 Sources

- `flows/b860be/reports/handoff.md`, read whole — the state at wind-down, the rulings in force, and the book items, used as the spine for this section.
- `flows/b860be/log.md`, the entries after "Night report section 13 refreshed" (15 entries, from the wave-2 dispatch through "Handoff and summary written now").
- `flows/b860be/receipts/field-clj-deps-hash-2026-09-26.md`
- `flows/b860be/receipts/goldragon-check-2026-09-26.md`
- `flows/b860be/receipts/goldragon-main-2026-09-26.md`
- `flows/b860be/receipts/zeus-probe-2026-09-26.md`
- `flows/b860be/receipts/ouranos-store-2026-09-26.md` and the GC receipt on main, cited by the log and not separately re-read here.
