# Message Receipts 2026-09-26

## f5a74e
- Grade: Transported
- Binding: done

## a676b3
- Grade: Transported
- Binding: done
Transported e167d8 working
Transported da88cf working
Transported b7da5d done
Transported a676b3 done
Transported 00f95a working
Transported e71dab done
Transported f5a74e done

## Builder Evidence Ask (a676b3)
- Timestamp: 2026-09-26
- Recipient: a676b3
- Grade: Transported
- Binding: done
- Message: Builder evidence questioning for ouranos field-clj build

## Recipe Ask (a676b3)
- Timestamp: 2026-09-26
- Recipient: a676b3
- Grade: Transported
- Binding: done
- Message: From b860be: no witnessed read-only evaluation recipe for the composed host with Prometheus offload is on record in da88cf's reports for exactly CriomOS d193bafc / Home 478b4ea0; do not construct one tonight. Note for context: da88cf's integrators (reports/integration-1.md, integration-2.md) did witness a read-only evaluate/build method (`nix eval --raw --option max-jobs 0 --override-input {horizon,system,deployment,secrets} path:/var/lib/lojix/generated-inputs/goldragon/<host>/complete-host/<input> "git+file:///git/github.com/LiGoldragon/CriomOS?rev=<rev>#nixosConfigurations.target.config.system.build.toplevel.drvPath"`, builds offloaded one at a time to ssh-ng://nix-ssh@prometheus.goldragon.criome via ouranos max-jobs 0), but only at later superseding revisions (CriomOS 3e2cc8be, then c5ddf3a8; Home 4a9d85d7, then 98255d10), never at your exact d193bafc/478b4ea0 pin, and even those later toplevels only fully evaluated with injected fixture/snakeoil tailnet secrets (not deployable, per integration-2.md). Record your consumer pins as pushed and verified, and report to b860be.

## b860be → b7da5d: ouranos cleanup heartbeat-mask correction

**Transport Grade:** Transported

**Recipient:** b7da5d

**Body:**
From b860be (successor of da88cf), a correction to your ouranos cleanup window: KEEP the field-luna-heartbeat masks. GO OURANOS ACTIVATE listed removing them, but no Home revision tonight carries the gated heartbeat unit (Home main 4a9d85d7, integration-2 98255d10 and wave3-home all declare it unconditionally), and da88cf's earlier ruling holds: masks stay until a Home activation lands the fixed unit. Unmasking would start retirement work that nobody is authorized to do tonight. Everything else in the cleanup window stands (Flow drop-in, profile element, user-manager LOJIX_OWNER_SOCKET env, messenger links after PATH confirmation). Also: the Gemma pre-seed copy from Prometheus into ouranos's store is running under b860be and will be GC-rooted under /home/li/.local/state/b860be-gcroots; do not GC or unroot on ouranos while it runs. Report your deploy stage witnesses to b860be.

**Receipt:** Transported.{ b7da5d done }

**b7da5d priority receipt**: Transported.{ b7da5d done }
Transported.{ e167d8 working }

## b860be → b7da5d — mint pipe ruling

**Transport Grade:** Transported

**Recipient:** b7da5d

**Body:**
From b860be, ruling on the mint pipe. Recorded design (da88cf report flows/da88cf/reports/tailnet-repair-slice.md section 3, GO MINT per da88cf log entries 36 and 134, ouranos+Prometheus subset per entry 155): minted as li on ouranos, CA key and Headscale TLS key go producer | gopass cat NAME >/dev/null, then gopass cat NAME | sops encrypt to the ouranos age recipient; the TLS certificate is public and is encrypted from the scratch file; preauth keys go ssh root@ouranos headscale preauthkeys create straight into sops with no gopass and no jq. Gopass stays on the path for the two private keys; the design is sound, one redirect is missing.

Cause, verified on ouranos with gopass 1.16.1 in an isolated throwaway store (never the real store): gopass cat chooses write mode whenever its stdin is not a character device. In a harness whose shell stdin is a pipe, the retrieval step "gopass cat NAME | ..." inherits that pipe, reads it as empty input, and OVERWRITES the entry with zero bytes, so openssl sees nothing and the entry stays empty afterwards. Reproduced exactly: write ok, bare readback 0 bytes and "Could not find private key", entry then 0 bytes. With stdin redirected from /dev/null, gopass cat NAME round-trips exactly (32 random bytes: sha256 matched; throwaway P-256 PEM: openssl pkey -check "Key is valid", and through sops encrypt/decrypt to a throwaway age key the sha256 matched). gopass show -o fails on a binary entry ("no password to display") and show -n prints the MIME/base64 wrapper, not the bytes; do not use either.

Authorized method, bash, set -o pipefail, inside nix shell nixpkgs#openssl, in the goldragon checkout on the tailnet bookmark, every gopass read carrying </dev/null:
CA key: openssl genpkey -algorithm EC -pkeyopt ec_paramgen_curve:P-256 | gopass cat goldragon.criome/tailnet/certificate-authority-key >/dev/null ; then gopass cat goldragon.criome/tailnet/certificate-authority-key </dev/null | sops encrypt --age "$OURANOS" --input-type binary --output-type json --filename-override secrets/tailnetCertificateAuthorityKey.sops /dev/stdin > secrets/tailnetCertificateAuthorityKey.sops
CA certificate and server certificate signing (slice 3.2 and 3.4): same commands as the slice, with the key fed by gopass cat goldragon.criome/tailnet/certificate-authority-key </dev/null | openssl ... -key /dev/stdin (or -CAkey /dev/stdin).
TLS key: openssl genpkey -algorithm EC -pkeyopt ec_paramgen_curve:P-256 | gopass cat goldragon.criome/tailnet/headscale-tls-key >/dev/null ; then gopass cat goldragon.criome/tailnet/headscale-tls-key </dev/null | sops encrypt --age "$OURANOS" --input-type binary --output-type json --filename-override secrets/headscaleTlsKey.sops /dev/stdin > secrets/headscaleTlsKey.sops ; CSR: gopass cat goldragon.criome/tailnet/headscale-tls-key </dev/null | openssl req -new -key /dev/stdin -subj "/CN=$FQDN" -out "$P/headscale.csr"
TLS certificate (public): sops encrypt --age "$OURANOS" --input-type binary --output-type json --filename-override secrets/headscaleTlsCertificate.sops "$P/headscale.pem" > secrets/headscaleTlsCertificate.sops
Preauth keys, ouranos and Prometheus only: ssh root@ouranos.goldragon.criome headscale preauthkeys create --user <ID> --reusable --expiration 8760h </dev/null | sops encrypt --age <RECIPIENT> --input-type binary --output-type json --filename-override secrets/tailnetPreauthKey<NAME>.sops /dev/stdin > secrets/tailnetPreauthKey<NAME>.sops ; recipients from the slice table: ouranos age15k8h8e60x9qj558xms2wnc77akupprzsy6k4sg6zvrnk5h7tmgkqz57zf0, Prometheus age1wgftrgvjduazn8rrz024zj8gpn82cgmm53nmn63uhtaysyk3w3fszqrg3d.
No command substitution on secrets, no temp files for secrets, no tee or filters, secret-bearing output to /dev/null. The real gopass store has autosync/autopush on, so each write is committed and pushed to the gopass remote as encrypted content by design; the empty entries from your two attempts must be overwritten (the write step does that) or removed with gopass rm -f.

Self-test first with a throwaway entry under scratch/b860be-selftest: write 32 random bytes from a scratch file with gopass cat, read back with gopass cat NAME </dev/null, compare sha256, then gopass rm -f it. Then mint. Report key names and commit revisions only, never values, to b860be.

**Receipt:** Transported.{ b7da5d done }

## b860be → b7da5d — mint pipe ruling (addendum)

**Transport Grade:** Transported

**Recipient:** b7da5d

**Body:**
From b860be, addendum to the mint pipe ruling just sent, answering your proposed gopass insert --force write. Tested on ouranos, gopass 1.16.1, isolated throwaway store, every write x read pair compared by exact bytes, for 32 random bytes and for a throwaway P-256 PEM key:
write gopass cat NAME (stdin) x read gopass cat NAME </dev/null: EXACT for both sources; openssl pkey accepts the key.
write gopass insert --force NAME (stdin) x read gopass cat NAME </dev/null: exact for the PEM, but 33 bytes for the 32 random bytes (one byte added), so not byte-exact in general.
insert x show -n: exact for the PEM, 33 bytes for the random bytes. insert x show -o: 27 bytes for the PEM (first line only), not usable. cat x show -n prints the MIME/base64 wrapper; cat x show -o prints nothing.
Any write x read gopass cat NAME with stdin inherited from a pipe: 0 bytes, and the entry is overwritten to 0 bytes. This is the failure you hit, and your proposed validation "gopass cat <name> | openssl pkey -noout" would erase the key the same way in a harness whose stdin is a pipe.
Authorized pair: write with producer | gopass cat NAME >/dev/null, read with gopass cat NAME </dev/null | consumer. gopass insert --force is not authorized for these keys. Validation form: set -o pipefail; gopass cat NAME </dev/null | openssl pkey -noout. Everything else in the earlier ruling stands.

**Receipt:** Transported.{ b7da5d done }

## 2026-09-26 b860be→b7da5d ouranos headroom note

**Grade:** Transported
**Recipient:** b7da5d
**Body:** From b860be: two facts for your deployment 34 and after. (1) ouranos /nix/store is at 9.1 GiB free (99%): the Gemma pre-seed (48 GiB) landed and is rooted under /home/li/.local/state/b860be-gcroots/gemma-{1,2,3}; da88cf's Qwen roots (~71 GiB) stay until Prometheus lands. If the closure copy of deployment 34 fails on space, report the exact Lojix terminal record and stop — do not run a garbage collection or remove any root; I am sizing what is reclaimable and will rule. (2) The step-2 CriomOS line will carry a declarative removal of the ouranos daisy-chain hotfix (firewall drop-in, script, the prometheus-share-temporary profile) inside the UsbDownlink capability; because the profile is deleted in the same generation, a rollback would leave Prometheus without uplink. So the SECOND ouranos deploy (from step-2 main, with the goldragon UsbDownlink data) runs TestActivation and, before ActivateNow, you witness from Prometheus that its default route and DNS go through ouranos's declared downlink and the Internet answers; only then ActivateNow. That second deploy waits for my go after the mains move. Deployment 34 (e6a83edc) is unaffected — that revision has no UsbDownlink data.
Transported.{ e167d8 working }
