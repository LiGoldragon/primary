# Voice Psyche — operational first draft

Voice Psyche keeps the living conversation available while subflows perform investigation and execution.

## Published

Voice Psyche is an authored, user-only Curriculum source. The living must invoke it manually; it is not model-auto-loadable.

Curriculum main: `b42c5193` — `skills/voice-psyche.md`.

Primary main: `997cbf8af` — generated `.agents` and `.claude` projections.

Generation returned `Generated.{ 51 21 }`; the drift gate returned `Checked.{ 51 21 }`.

## Operational vision recorded

1. **Voice coordination.** The main flow stays conversational while subflows do task work.
2. **Herdr-hosted flow.** A desired new Codex flow can message other processes and should be reachable through the desktop application with interactive voice.
3. **Programmatic refresh.** A successor should receive witnessed knowledge, readings, labelled inferences, relevant distilled vision, and needed skills from source files rather than a model rewriting everything.
4. **Operational knowledge and maintenance.** The living explores operational skills as a mind/knowledge base and testing as practical work that keeps production alive. This is recorded as exploratory analogy, not an assertion that every production fix is a test.

## Evidence and current boundary

Prior records describe a shared managed Codex app-server, remote-control phone path, and terminal TUI. They do not prove that the same flow is discoverable in the desktop application with interactive voice.

The exact acceptance requirement is desktop discovery of the shared successor plus interactive voice on that same flow. No successor has been launched or claimed delivered.

This local implementation environment's `herdr agent list` returned `server_not_running` for `/home/li/.config/herdr/herdr.sock`. That is not evidence about the cluster host; no duplicate server was started.

## Next proof

Identify the intended Herdr host and session, inspect the existing managed app-server topology, assemble the successor prompt from source records programmatically, launch only through that existing topology, and witness the exact thread in the desktop application before declaring success.
