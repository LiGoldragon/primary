# Flow be2534

Working instruction: determine why Codex suddenly asks the living to trust hooks, identify those hooks and their provenance, and determine why new sessions repeatedly select Terra instead of Sol. Present a concise, visual explanation and a proposed fix only; do not change hook or model configuration. This prompt creates no psyche record.

2026-09-03 — Read-only witnesses identified two active plugin-supplied `Stop` hooks, one each from the enabled OpenAI-bundled Browser and Chrome plugins; both signal `node_repl.turn_ended`. The normal global and project hook files remain absent. A stale isolated Orca runtime still contains its eight historical hooks, but no current normal launcher selects that runtime home; prompt-to-manifest attribution remains an inference because the prompt event itself was not recorded.

2026-09-03 — The repeated root-model reset is owned by CriomOS-home: its authored `codexConfig` declares top-level Terra/high, and Home activation force-writes those live paths. The current live file says Sol/xhigh only because it was changed after the persistent app-server began; interactive selection is thread-local and does not repair the authored default or reload the daemon. The obsolete preserved `[orchestrator]` table and an unconsumed Terra `non-orchestrator.config.toml` artifact are cleanup findings, not the active root selector. No hook, Codex, Home, or operating-system configuration was changed.
