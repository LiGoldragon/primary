# ChatGPT Desktop stock-boundary implementation

Thread `01a06365-854f-72a2-9ad1-ab5a4a34e500`, flow `4ad49f`.

## Observations

- The exact Home product write set was locked as Orchestrate lock `535` after an `Observe.Locks` snapshot; the parent-reserved report path remains under lock `531`.
- The Home worktree initially contained the expected dirty sanitizer candidate: changes to `checks/desktop-app-support/default.nix` and `owned-agents/chatgpt/patch-asar.py`, plus `owned-agents/chatgpt/__pycache__/patch-asar.cpython-314.pyc`.
- That candidate was preserved without alteration in pushed Home revision `0b0a96835b7b` (`Preserve unreviewed ChatGPT sanitizer candidate as evidence`). The working copy was then clean.
- The audit witnesses establish that the persistent `codex-remote-control` owner, terminal routing, phone Remote Control, Agent Intercom, VSCodium sidebar, full-access defaults, and Wayland launcher behavior are independent of the Desktop bridge. They are out of this rollback write set.
- The independently extracted fixed-output vendor archive contains a regular executable `resources/codex` and an ASAR with SHA-256 `9745ec1195897c019533d08e8415ab81a3c4e59e845403fdfea42ce1272fe954`. The prior packaged ASAR differed in 1,355 bytes. The vendor Core reports `codex-cli 0.152.1` from an isolated temporary home and looks only for its private control socket there.
- The first immutable focused Nix run at `47cc83d1` exited `1` during evaluation because the new wrapper probe lacked the package `version` passthru; this was a test-fixture defect, not a behavioral witness. The probe was repaired in pushed `af7c6774`.
- The corrected immutable pre-change Nix check at `af7c6774` ran on configured remote builder `prometheus` and exited `1` in `desktop-app-support-contract`. Its first product assertion is an exact `cmp` of the independently extracted vendor `app.asar` and the packaged ASAR; the check’s source confirms that is the failing contract, and the independent witness observed the same comparison as exit `1`.

## Implemented rollback

The rollback removes the ASAR patch invocation and source, restores the copied vendor resource tree (including its own Core), removes all Desktop-specific persistent-owner environment forcing, and disconnects the ChatGPT factory from the canonical Codex package. The durable check compares the packaged ASAR/Core to an independently extracted fixed-output archive, exercises the retained Core in a private temporary home (including private stdio Core start and clean EOF exit), and invokes the generated wrapper with inherited vendor variables plus Wayland selection. `UPGRADES.md` now records the breaking deployment boundary.

## Final result

### Correction — tracked generated bytecode

The prior final account incorrectly treated the d56 source tree as audit-clean.
Independent review found that `owned-agents/chatgpt/__pycache__/patch-asar.cpython-314.pyc`, preserved in evidence commit `0b0a9683`, was still tracked at d56. The brief sentence that led to the mistake was: “commit and push that existing dirty work unchanged in its own clearly named evidence commit before replacing it.” I preserved the candidate as instructed but failed to replace its generated bytecode artifact along with the patcher. The quoted instruction does not authorize retaining it at the rollback tip.

The correction adds a Nix-evaluated repository-structure invariant requiring
the ChatGPT `__pycache__` directory to be absent. Immutable revision
`8b13f23a` was seen red: evaluation exited `1` at that assertion while the
tracked artifact remained. The artifact is deleted from the rollback tip;
the evidence revision remains recoverable. The structural assertion is a
repository-integrity gate, not a substitute for the packaged-byte and
private-Core behavioral witnesses.

- `b3286e8e` (`Restore ChatGPT Desktop vendor boundary`) contains the rollback. It was rebased over the concurrent unrelated `c8bbc9da` Home main advance before push.
- `baaf4afa` removes the stale `python3` native input left after deleting the patcher.
- `d56f1153` adds the retained vendor Core private-start witness. It was the first pushed Home rollback tip, but is superseded by the generated-bytecode correction.
- The final `nix eval` of `d56f1153` with the materialized ouranos `system` and `horizon` inputs exited `0`.
- The final focused `nix build --refresh --no-link` of `d56f1153#checks.x86_64-linux.desktop-app-support`, with only the configured `prometheus` remote builder, exited `0`.
- `8b13f23a` adds the structural bytecode-absence invariant. Its immutable
  evaluation exited `1` with `assertion '(! ((builtins).pathExists
  chatgptGeneratedBytecodeDirectory))' failed`, establishing the intended
  correction red state.
- `c025a681df31d55b9035364c01e2f6c8d7b59c1c` removes the tracked bytecode
  and is pushed as Home `main`. Its independent immutable `nix eval` exited
  `0`; its retained `nix build --refresh --no-link` focused
  `desktop-app-support` check, using the configured remote builders, also
  exited `0`.

No deployment, GUI launch, service restart, installed-state modification, or user-data/config/cache/conversation access was performed. The mutable current OpenAI index no longer proves the historical 26.831 package record; the checked vendor bytes remain the declared fixed-output archive and the archive-member signature evidence recorded in the pristine witness.

## Sources

- [Desktop rollback audit](/home/li/primary/flows/ea1e56/reports/desktop-persistent-codex-audit.md:1)
- [Source inventory](/home/li/primary/flows/ea1e56/reports/source-inventory.md:1)
- [Current desktop/Codex state witness](/home/li/primary/flows/ea1e56/witnesses/current-desktop-codex-state.md:1)
- [Pristine stock-boundary witness](/home/li/primary/flows/4ad49f/witnesses/pristine-stock-boundary.md:1)
- [Preserved Home evidence revision](https://github.com/LiGoldragon/CriomOS-home/commit/0b0a96835b7b)
- [Final Home rollback revision](https://github.com/LiGoldragon/CriomOS-home/commit/c025a681df31d55b9035364c01e2f6c8d7b59c1c)
