# CriomOS-home rollback review witness

Flow `4ad49f`. This is a read-only review of immutable Home revision
`c025a681df31d55b9035364c01e2f6c8d7b59c1c` against baseline
`90a12633cc60148b62bc47fd44957e6165727094` and the stock-Desktop audit target.

## Method

I inspected the Home revision graph, immutable file lists and file contents
with `jj`; compared the target and baseline source deltas; inspected the
recorded immutable red/green check evidence; inspected the current CriomOS
consumer pin and the prior lock snapshots; and compared c025 directly with
d56.
I did not rerun the expensive check, evaluate or build Nix, deploy, launch a
GUI, restart a service, or read user data. The only authored output is this
parent-reserved witness.

## Observations

### Target source and rollback boundary

- Home `main`, `main@git`, and `main@origin` resolve to c025. The complete
  linear ancestry from baseline is:
  `0b0a96835b7b` Preserve unreviewed ChatGPT sanitizer candidate as evidence;
  `47cc83d17753` Add stock ChatGPT boundary witness;
  `af7c67746dda` Repair stock boundary wrapper probe fixture;
  `c8bbc9dacd87` Pin Claude flow ID parity;
  `b3286e8e158e` Restore ChatGPT Desktop vendor boundary;
  `baaf4afaf650` Remove retired ChatGPT patcher input;
  `d56f11539bf3` Exercise bundled ChatGPT Core privately;
  `8b13f23ac636` Guard ChatGPT source against generated bytecode; and
  `c025a681df31` Remove generated ChatGPT patcher bytecode.
- The only changes after d56 are exactly the narrow structural invariant in
  `checks/desktop-app-support/default.nix` and deletion of the generated
  bytecode file. The invariant is an evaluation-time
  `assert !(builtins.pathExists ../../owned-agents/chatgpt/__pycache__)`;
  no product package, wrapper, module, check behavior, documentation, or
  independent surface changed after d56.
- `c8bbc9dacd87` is the only unrelated intervening product commit. Its
  changed files are Home architecture/readme/upgrade prose, the `flow-id`
  check, and the harness input/lock. It does not change the ChatGPT package,
  Codex owner, TUI, Agent Intercom, sidebar, or permission policy. c025
  includes it as an ancestor; it is not a hidden fork or merge. The corrected
  c025 tip retains the same ancestry and adds no unrelated product change.
- At c025, `owned-agents/chatgpt/patch-asar.py` is absent and the ChatGPT
  unwrapped derivation has no Python patcher input or patch invocation. Its
  install phase checks for the vendor `usr/lib/chatgpt/resources/codex` and
  copies the complete vendor tree with `cp -a`; it does not remove or replace
  `resources/codex`. The outer wrapper retains only conditional Wayland and
  requested command-line flags; the local-daemon and CLI/App Tools environment
  forcing is gone.
- The canonical package factory no longer passes `codexPackage` into ChatGPT,
  and the minimum profile calls ChatGPT without a Codex package. The same
  canonical Codex package remains explicitly used by the persistent owner,
  terminal TUI, and Agent Intercom paths.
- The c025 target file list contains no `__pycache__` directory and no `.pyc`
  file. The generated artifact added by `0b0a96835b7b` is deleted by c025;
  the prior evidence commit remains separate and recoverable. The explicit
  no-generated-bytecode condition is therefore satisfied at the reviewed tip.

### Check and recorded evidence

- The c025 `desktop-app-support` check compares the packaged `app.asar` byte
  for byte with an independently extracted fixed-output vendor Debian archive,
  and does the same for the bundled `resources/codex` executable. It then
  runs the retained Core with a private `HOME`, XDG roots, and `CODEX_HOME`:
  `--version` must report a CLI version, private stdio `app-server` must exit
  cleanly, and a private daemon-version probe must not find an owner.
- The check invokes a generated ChatGPT wrapper with inherited vendor
  selection variables and Wayland variables, and asserts the exact resulting
  variables and duplicated `--ozone-platform=wayland` flags. These are
  behavioral/package and byte contracts, not marker-only searches.
- The recorded immutable evidence says the corrected pre-change check at
  `af7c67746dda` was red on the exact ASAR comparison, the bytecode invariant
  at `8b13f23ac636` was red while the tracked artifact remained, and the
  corrected c025 eval and focused remote build both exited 0. I accept those
  as recorded claims; this review did not rerun any check.

### Unchanged independent surfaces

- `owned-agents/codex/tui.nix`, `owned-agents/codex/default.nix`,
  `modules/home/profiles/min/codex-permission-defaults.nix`,
  `modules/home/vscodium/vscodium/default.nix`, and
  `packages/agent-intercom/default.nix` have identical SHA-256 source bytes
  at baseline and c025.
- In `modules/home/profiles/min/agent-intercom.nix`, the functional regions
  beginning at the Agent Intercom package, MCP declaration, and
  `codex-remote-control` service are byte-identical; only the ChatGPT factory
  argument and adjacent explanatory comment changed. The service still uses
  the canonical Codex package, Unix app-server, `UMask=0077`, and restart
  policy. The module and ChatGPT wrapper retain the Wayland argument.
- The recorded stock-boundary and deployment witnesses establish that the
  persistent owner, terminal TUI routing, phone Remote Control, Agent
  Intercom, VSCodium sidebar, and full-access defaults are independent of the
  retired Desktop crossing. No target diff touches those implementation files.

### Upgrade prose and consumer/lock state

- The c025-inherited `UPGRADES.md` section “ChatGPT Desktop stock vendor boundary” correctly
  calls the change out as removal of the custom crossing, states vendor ASAR
  and Core byte identity, preserves Wayland, says Desktop is not a client of
  the persistent owner, retains terminal/phone ownership and the independent
  Agent Intercom/sidebar/full-access surfaces, and documents the exact package
  check. It gives the required complete-generation declarative deployment
  boundary and forbids installed-file patching or `resources/codex` replacement.
- The first lock snapshot showed c8’s owning lock `543`
  (`criomos-claude-flow-id-parity`, flow `01a05e95`) live. The later snapshot
  showed lock `543` absent, so that lock is now released. The current active
  consumer lock is the parent’s `545` (`criomos-home-rollback-consumer`, flow
  `4ad49f`), which owns the CriomOS pin/documentation paths.
- The prior consumer revision `e9159065` pinned d56 in both `flake.nix` and
  `flake.lock`; the corrected consumer revision `663fefb7` now pins c025 in
  both files. Thus c8’s live-lock race no longer makes the
  producer unsafe to consume; the consumer remains under its parent-owned
  lock and evaluation/Realize state is outside this witness.

## Scope verdict

The c025 source rollback is scope-clean for removing the ChatGPT-to-persistent-
Codex crossing: vendor resources are restored by source and enforced by exact
byte checks; patching, env forcing, and canonical Codex injection are removed;
the independent Wayland, owner, TUI, phone, Agent Intercom, sidebar, and
full-access surfaces remain intact. The only prior defect—the generated
ChatGPT patcher `.pyc` at d56—is deleted at c025 and guarded by the narrow
structural invariant. The reviewed tip is audit-clean on the stated source,
scope, and recorded check evidence.

## Unknowns

- The parent’s current consumer evaluation and any subsequent deployment
  terminal state were not observed here.
- No live GUI launch/new-chat/resume was performed; the final green evidence
  is an immutable package/Core check and Nix evaluation/build, not an
  authenticated graphical Desktop smoke.

## Sources

- `/home/li/NON_MANAGEMENT_AGENTS.md`
- `/home/li/.codex/skills/child-flow/SKILL.md`
- `/home/li/.codex/skills/flow-evidence/SKILL.md`
- `/home/li/.codex/skills/behavior/SKILL.md`
- `/home/li/.codex/skills/testing/SKILL.md`
- `/home/li/.codex/skills/nix-workflow/SKILL.md`
- `/home/li/.codex/skills/main-feature-integration/SKILL.md`
- `/home/li/.codex/skills/psyche/SKILL.md`
- `/home/li/primary/flows/4ad49f/reports/implementation.md`
- `/home/li/primary/flows/4ad49f/witnesses/pristine-stock-boundary.md`
- `/home/li/primary/flows/4ad49f/witnesses/deployment-boundary.md`
- `/home/li/primary/flows/ea1e56/reports/desktop-persistent-codex-audit.md`
- `CriomOS-home` immutable revisions `90a12633cc60148b62bc47fd44957e6165727094`, `d56f11539bf3430859e09c5e09e410d5504b49a4`, `8b13f23ac636a2a1dd8e22aa6dab49c92a5dc423`, and `c025a681df31d55b9035364c01e2f6c8d7b59c1c`, inspected with `jj`
- `/git/github.com/LiGoldragon/CriomOS/flake.nix` and `flake.lock` at corrected consumer revision `663fefb7`
- Read-only `orchestrate 'Observe.Locks'` snapshots taken during this review
