# Ethos-monolith is the center

## 2026-08-25T01:16:56+02:00 — realization charter

The living set the center and the proving relationship:

> Get ethos-monolith working: the center of this work is real Ethos → Rust
> emission. The proving project is a simple orchestrate Nexus — MVP, a
> normal and a meta socket, dead-simple Datom-syntax path reservation for
> edit coordination — whose wire interfaces are written in Ethos and whose
> interface Rust must come out of ethos-monolith. The old orchestrate code
> is not sacred; the fresh simple component replaces it, old code at most
> inspiration for the map.
