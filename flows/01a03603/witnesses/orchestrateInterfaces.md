# Orchestrate interface generation witness

## Source and generation

Method: code read `flows/01a02a34/reports/pathLockEpic.md` and the cited
historical heads.

The ordinary baseline was signal-orchestrate `a038c5c04fea`, version 0.15.0,
ContractId 1, WireRevision 3. The meta baseline was meta-signal-orchestrate
`f1dec7e3f7b0`, version 0.9.0, ContractId 2, WireRevision 2. The current
generated contracts retain the IDs and use ordinary revision 4 and meta
revision 3.

Method: code read `flows/01a02fd5/vision/interfaces.md`,
`flows/01a02fd5/vision/metaOrchestrate.md`,
`flows/e06e4c07/vision/nexus.md`, and
`flows/98fbfa47/vision/metaSignalNotOptional.md`.

These records require Ethos-owned interfaces and a distinct mandatory meta
signal boundary. The completed component directories therefore contain only
`signal.ethos`, `nexus.ethos`, and `sema.ethos`; the latter two are meaningful
empty Interface modules. `build.rs` invokes one
`ethos_monolith::generate::ComponentGeneration` for the directory and rejects
committed output that does not match. Every committed generated module begins
`// @generated by ethos-monolith from Ethos source`.

Method: probe from signal-orchestrate at `3de1c5d51bb7`.

`cargo test` passed the direct carrier and frame witness. It proved:

```text
PathLock.{orchestrate-interfaces [/git/github.com/LiGoldragon/signal-orchestrate] (generated contract witness)}
PathLockRegistered.{orchestrate-interfaces [/git/github.com/LiGoldragon/signal-orchestrate] (generated contract witness)}
PathLockRelease.{orchestrate-interfaces}
PathLockReleased.{orchestrate-interfaces}
```

It also parses and round-trips the closed duplicate-name and unknown-release
refusals, asserts generated `OrchestrateWire` binding ContractId 1 / revision
4, and encodes then decodes a generated `Frame` request.

Method: probe from meta-signal-orchestrate at `adf0300257ea`.

`cargo test` passed the direct carrier and frame witness. It proved:

```text
Configure.{/tmp/orchestrate.redb /tmp/orchestrate.sock /tmp/meta-orchestrate.sock}
Configured.{/tmp/orchestrate.redb /tmp/orchestrate.sock /tmp/meta-orchestrate.sock}
```

It also parses and round-trips `ConfigurationRejected` with closed
`StorePathImmutable`, asserts generated `MetaOrchestrateWire` binding
ContractId 2 / revision 3, and encodes then decodes a generated `Frame`
request.

Method: probe in both contract repositories after pinning
ethos-monolith `cc3ee3221401bf4edec0e6c9b1c1b2ce35e28ff6`.

Both completed successfully:

```text
cargo fmt --check
cargo test
cargo clippy --all-targets -- -D warnings
nix flake check --no-build -L
```

The Nix evaluations include a durable `test-generated-contract` derivation in
each repository. Both contract worktrees were clean after their pushed commits.
