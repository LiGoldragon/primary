# proposal · addition to `nexus` skill — pre-existing tools port to a Nexus, not extend

*Flow 48cff7 · 2026-09-16 · pending living review*
*Kind: insertion into existing skill (`nexus`)*
*Source records: flows/48cff7/vision/transcriptNexus.md (living's word: "the transcript component is misimplemented; make it a nexus")*

---

## placement

At the end of the `nexus` skill body, after the section "How nexuses fit together".

## insertion text (verbatim)

> ## Porting an old shape
>
> A domain that has grown durable is redesigned as a Nexus, not extended alongside its old shape. Name the pre-existing tool as misimplemented, draft the two wire type repos and the two CLIs, and end the old shape at cutover. No parallel compatibility path.

## what it preserves, changes, removes

Preserves every current section of the `nexus` skill. Adds one section naming the rule the living stated when calling the transcript tool in for its Nexus rebuild. Removes nothing.
