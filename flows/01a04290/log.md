# Flow log

Read-only Terra subflow audit of recent connected Flow and Codex goals, Codex archive behavior, remembering compatibility, and compact-reference readiness.

Settled:

- Defined the recent window as the connected research tree rooted at Codex `01a04236` plus audit root `01a0428b` and worker `01a04290`.
- Found seven Codex threads, no native Codex goal records, and an independent in-progress Bead `primary-z88`.
- Witnessed that archive preserves plain rollout JSONL at a different local path and hides it from default active listings; transcript-depth remembering remains conditional.
- Found a collision among three Codex child UUIDs whose first eight hexadecimal characters are all `01a04237`.

The worker's higher-priority context prohibited edits, commits, and pushes. Parent flow `01a0428b` materialized the returned report and witness without changing their findings.

Open:

- Resume after archive or cold compression, a local parent/child archive lifecycle, retention policy, and the final collision-safe identity scheme remain unwitnessed or unruled.
