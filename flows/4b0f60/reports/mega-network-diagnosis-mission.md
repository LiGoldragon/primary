# Mega network diagnosis mission packet

**Owner acceptance:** Mind Astra `4b0f60` preserves the existing native Mind
profile **gpt-6-astra / medium** and route ownership. This is a specialized
Terra diagnostic task packet, not a refresh, new seat, harness launch, skill
injection receipt, network mutation, restart, retirement, or replacement.
The reported 42% last-input proxy is not exact occupancy; the native-verified
greater-than-40 gate remains unmet.

**Controller and acceptance:** preserve Field High `6fb948` and reservation
`4639`. The coordinator-verified Terra `0347d0` task profile is
**gpt-5.6-terra / medium**, exact native thread
`01a0c0f9-c28a-7cd3-a3eb-ac60347d0559`; Luna `c88918` is
**gpt-5.6-luna / medium**. These task profiles are preserved, not spawn
instructions. Terra's acceptance of this mission is **Pending until
acknowledged**. Preserve Curriculum `89992a2` projection gates,
CriomOS-home `a2358ef3`, CriomOS `cef11110` Claude-activation gates, and the
existing Flow/Message locks.

## Bounded task

Validate the current two-host path, read-only and under the approved existing
owner, with observed time, redacted command/result, exit class, selected route
and source-quality attached to every conclusion. Do not output secrets,
perform builds or local fallback, scan, reset a router, change NAT/AP/routes,
or alter authentication.

The living topology supplies the desired candidates: Ouranos wired
`192.168.1.5` via `192.168.1.1` remains the Internet/DNS default. Both nodes
are intended to use `Mega_2.4G_1896` for a direct same-LAN management path;
USB `10.44.0.1` to historical Prometheus `10.44.0.148` is fallback. Same SSID
does not prove peer reachability: verify assigned addresses, prefix, selected
route and the two exact peers without a scan. Wi-Fi reassociation from
`goldragon.criome` is a later Field mutation only; this packet does not perform
or authorize it. The existing wired default remains intact. Any Wi-Fi
never-default/route policy is a decision for the existing NetworkManager source
owner after interface facts are known.

The user's relay is the latest attributed Luna USB-hostname and Terra strict
ordinary Ygg/USB-SSH recovery evidence. It is not independently witnessed in
this artifact, so exact recovery time and cause remain unknown. Earlier source
evidence of failed USB lease/ARP and Ygg TCP is valid at its observation time,
not a current confirmed failure. Correlate USB, Ygg, and Wi-Fi timelines
separately. Do not assign one cause to their drops.

For each route, collect only: link/carrier, association/BSSID when relevant,
IPv4/DHCP lease, route selection, neighbor/ARP, DNS result, TCP transport and
ordinary SSH phase. Establish chronology at the first failed layer:
link/carrier → association → DHCP → ARP → TCP → SSH host trust → user-key
authentication → remote command/store protocol. Return a root cause only when
the evidence selects one; otherwise return `Unknown` with the first failed
layer and a conditional minimal-fix design. No automatic route, NAT, AP, or
authentication change follows.

## Builder identity boundary

Configured Nix semantics are distinct from ordinary user SSH. `/etc/nix/machines`
uses `ssh-ng://nix-ssh@prometheus.goldragon.criome`, with a configured
root-owned identity file, pinned public host key, builder options/features and
remote `sshServe` authorization through dispatcher public keys. A direct `li` SSH success, or
default-key store attempt, is not a Nix-daemon proof. A `nix-ssh` denial after
TCP identifies a later identity/dispatcher/protocol question, not Wi-Fi or
daemon absence.

Only after TCP is established, run a no-build daemon/store handshake using the
actual configured builder identity, pinned host key, and exact options. Record
the selected identity/options without exposing key material. Missing identity
or options leaves the result `Unknown`; do not recommend an authentication
change. Success criteria are repeatable direct Mega path evidence for both
nodes, then a configured-identity Nix daemon/store handshake. Neither proves a
build, runtime activation, or durable network configuration.

## Conditional outcome

Fix only the first observed failed layer and only through separately authorized
existing owner procedures: association versus DHCP/ARP versus TCP versus
configured Nix authorization. Preserve the wired default and single controller.
No fix is selected while the result remains unknown.

## Sources

- Current living relay through `0ad137`: desired topology and mission limits;
  latest attributed reachability evidence, not independently observed here.
- `flows/6db4fe/reports/prometheus-builder-recovery-2026-09-22.md`: dated
  USB/Wi-Fi/Ygg failure observations and preserved wired-default safeguards.
- `/root/census_contract_design` 2026-09-23 source reading: Nix builder
  `ssh-ng` identity/options and phase separation; no live test performed.
- `flows/4b0f60/reports/network-nexus-refresh-handoff.md` and
  `network-nexus-design.md`: retained Mind ownership and single-controller
  design boundary.
