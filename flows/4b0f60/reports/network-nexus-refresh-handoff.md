# Network Nexus refresh handoff

**Current status — 2026-09-22:** Mind High `4b0f60` retains Network Nexus
architecture and source-design ownership. The immediately preceding living
authorization for a Mind successor remains a historical authorized refresh
receipt; the latest instruction supersedes its immediate execution. The active
exact native Codex refresh is **Field High successor of `03e825`**, coordinated
by Field Sol `753` on branch
`field-astra-refresh-03e825-753e69@691764cfb`. This handoff records the
network brief for that Field succession after exact route acceptance. It does
not request a Mind launch, seat transfer, duplicate controller, or new
implementation writer.

## Succession and acceptance

The Field successor must receive its exact route acceptance before it acts.
Mind network design ownership continues. The held Psyche High Claude target
remains protected; retain `0ab019`/`f72ab7`, crossover routes `9e7ea5`,
`98ac2e`, `0ab019`, and their native handles. No successor inherits a native
handle, and no retirement or blind route send follows from this report.

The prior supplied Mind profile, **gpt-6-astra / medium**, is recorded only as
the incumbent refresh context. It is not a profile claim for the Field
successor. The prior fifteen-skill bundle—`spirit`, `main-flow`, `psyche`,
`behavior`, `correction`, `vocabulary`, `testing`, `subflow`,
`edit-coordination`, `flow-evidence`, `prompt-crafting`, `codex-harness`,
`refresh`, `herdr`, and `messaging`—still requires a current accepted native
structured-bundle and source-injection receipt where it applies. A catalog,
file, or literal token is not injection; no missing `refresh` source is
invented.

Previous local audit and coordination returns are complete and acknowledged.
This publisher's and the new delivery coordinator's final returns still need
relay acknowledgement before any later transfer. Return through the accepted
Field route and current `4b0f60` ownership, without assuming a successor
acceptance.

## Current network brief

Field-attributed evidence says Ouranos's wired management, default route and
DNS are healthy. Its USB share at `10.44.0.1` has link and a DHCP listener, but
no Prometheus lease and failed ARP/ICMP. Prometheus historically used `eno1`
at `10.44.0.148` via that gateway, and its `wlp195s0`/`br-lan` AP at
`10.18.0.1` worked historically. Current Ygg TCP/22 fails; local Wi-Fi is not
participating. This narrows the current defect to Prometheus's cable-facing
NIC, DHCP, ARP, or client path without identifying a cause. No Ouranos restart
or source change is supported. The next evidence is correlated both-end
NIC/DHCP/ARP capture by existing Field owners.

Field Terra `034` owns bounded Prometheus repair coordinated through Field
`6db4fe`; `6db4fe` retains USB/runtime and VM `4373`. Field `9dd3776` retains
AP/consumer rollback under `3776`. This Field succession neither blocks nor
duplicates those lanes.

## Network Nexus design and gates

One Network Nexus owns typed network reconciliation; it must not create a
second network controller. Goldragon declares desired `NodeService`; Horizon
projects the node-enabled capability; Lojix materializes it; CriomOS applies
it through the selected NetworkManager or networkd owner. Kernel link,
addresses, routes, DNS, and connectivity remain independent ordinary
observations with quality and freshness. Desired configuration is durable and
separate from observation.

The read-only typed interface provides an initial snapshot and real events.
Privileged reconciliation uses one generation-bound, idempotent plan with
stale/refusal/evidence records and bounded rollback. It covers USB, Wi-Fi,
uplink and connectivity. An optional Ouranos API may be used only while wired
Internet is healthy; that condition does not authorize activation.

The USB source anchor remains proposed rather than installed:
`NodeService::UsbIpv4Gateway { downstreamInterface, MAC, gatewayIPv4Cidr,
uplink }`. It selects by typed capability, never hostname; exactly one owner
controls downstream link, NAT, DHCP and DNS. The source chain is Horizon
`b45d6ad` and candidate Signal-Lojix `01ae2b1e`, Meta-Signal-Lojix `8fb526c4`,
Lojix `6b299ec1`, Goldragon `a911515c`, and CriomOS `9842f51a`. These do not
prove a materialized graph, local build, deployment, or E2E result. Network
design/source-local build gates proceed only after Field route acceptance:
remote Nix without local fallback, real upstream pins, and Crane/vendor
availability; no fabricated locks or local materialization.

Preserve Flow/Message lanes: f72 via `0ab019` (`2836` Flow store, `2862`
Signal Flow, `2864` Message), Medium `2c61af` and library `3830`, Low
`e7983847`, and protected `553901`, `1834`, `1835`, `542`. Published candidates
Message bridge `18e14e`, Raw `3ba5d946`, Flow store `87bd74bd` have no compiled
graph receipt. Meta-Signal-Flow `93d867a` has a verifier API, but its handler
returns `VerifierUnavailable`; an attester registry/provider needs a separate
exact reservation and no authentication bypass.

VM work uses only the existing guest after a coherent pair build receipt,
process-only native authentication and rollback; it copies no credentials and
precedes any paid Haiku/Luna call. Production requires typed consistent
migration, lossless capture, post-snapshot queued/ambiguous handling, rollback
and `9dd3776` sequencing. No current deployment is claimed.

## Sources

- Latest living correction and current Field succession authority, relayed by
  `4b0f60`; authority for scope, not a native/route acceptance receipt.
- `flows/6db4fe/reports/prometheus-builder-recovery-2026-09-22.md` and
  `flows/753e69/reports/prometheus-phone-wifi-2026-09-21.md`: attributed
  network observations and limits.
- `flows/753e69/reports/horizon-usb-gateway-contract.md` and
  `usb-gateway-integration-ownership.md`: typed USB source and ownership map.
- Accepted Mind Flow/Message handoff and completed local child returns: no new
  socket, native, build, deployment or route probe.
- Full reported anchors: Message
  `18e14e3571c549fe6b57fbe8fdf46a1a240ca2c5`, Raw
  `3ba5d946527163a8fd545d9968e4e23db83d0642`, Flow
  `87bd74bd4c995d8947440e78d18c0cd6a9ee9597`; none proves a compiled graph.
