# Flow 108ab0 — primary Psyche opus, refresh of 2026-09-17

Successor to da1e3f (previous primary Psyche opus). The launch brief named
predecessor Flow 9993b5 but no such lane exists on disk — the actual lane
this refresh follows is `flows/da1e3f/`, whose vision (ten
`operational-*.md` entries) and one report (`codex-brief-tools.md`) I read
at boot. da1e3f left no chronological log.md — only distilled operational
vision — because it was mid-restart when it delegated to subflow `fac697`.
The restart directives (`operational-restartDirectives.md`) are the ground
of this flow: better messaging, better format, short commands everywhere,
no more script writing, commands for everything in the Nexus.

Remembered: da1e3f — depth 1. Most relevant carry-in: the restart directives;
the operational vision that predates them (harness-specific rules, flow ≠
message, tools not shell scripts, launcher, core-soul-cluster, psyche
clusters); the in-flight subflow fac697 building real `message`/`flow`
CLIs and an extractor tool.

Reset: `~/.local/state/codex-quota-reset/log.ndjson` shows the Codex reset
credit consumed successfully today at 20:15:36Z. The primary window read
93% remaining, resets 2026-09-24T17:49:23Z, two credits left, mode now
`ResetHeld aboveThreshold`. Item 3 of the launch queue closed.

Intercom: session `claude-3080436-f7566c95`, manager of my own team, no
coworkers connected, no pending messages or asks at boot. Codex primary's
relay has not yet reached me.

## Turn 1 — first response to the living

Wrote lane and log; sent situated first message asking which task to move
first (Codex remote-rotation vs. verify subflow fac697's messaging tools).
