# Claude successor v7 — built from the v6 tooling by flow f55ec8

Unlaunched Claude packet only. V5 remains the immutable provenance for the
already started Codex successor d9961c; do not restart Codex from this packet.

Run `python3 assemble.py`, then `python3 claude-launch.py --cwd <independent-clone>`.
Dry-run is the default; `--launch` is the live path and needs the living's word.
The complete base replaces stock context with --system-prompt-file. Source and
artifact manifests carry byte counts and hashes; complete skill bodies are
packaged inputs, not a native skill-loader receipt for the assembling flow.

V7 changes from v6:
- Frozen sources: `sources/f55ec8/` (this lane's log.md, branches.md, all
  vision/*.md and ten reports, handoffToSuccessor.md first) replaces efa157's
  log and reports. `sources/efa157/` keeps ordersToCodex-2026-09-16.md and all
  of efa157's vision/, which still carry the living's own words.
- Base header: succeeds f55ec8 at depth one, remembers efa157 and 840e42 by
  name; pair d9961c with cf7879 primary until recorded handoff; the operating
  facts f55ec8 witnessed on 2026-09-16 (classifier refusals and the orders-file
  route, worktree isolation's refusal of complex substitution, subflow
  paraphrase, Opus 5 seats, refresh at thirty percent, the idea-book form, the
  hourly quota line); the one authorized reset consume already dispatched.
- First prompt: identity claimed after start via flow-id (the launcher-claim
  design is owed, not built); EnterWorktree before edits; handoffToSuccessor.md
  read first; the three readiness routes, the middle one carried from a file.
- Name and remote control: primary-claude-successor-f55ec8.

The exact living-specified Authority sentence remains first in the base header,
ahead of all embedded records. Post-start identity, roster, own scope, remote
control and prompt user-turn witnesses remain required. Dry-run's bounded
daemon-envelope model is not a dispatch or an actual record receipt.
