# Field refresh preparation

Prepared for Field Astra `1cb440`; it is not a launch receipt and transfers
no ownership.

## Native successors staged

`field-astra-current` and `field-sol-current` now retain their respective
native declarations, `gpt-6-astra`/medium and `gpt-5.6-sol`/medium. Each has
predecessor and ancestor `1cb440`. The profiles retain the same nineteen
typed native skills:

`spirit`, `main-flow`, `field`, `refresh`, `psyche`, `psyche-acquisition`,
`behavior`, `correction`, `vocabulary`, `testing`, `subflow`,
`edit-coordination`, `orchestrate`, `flow-evidence`, `prompt-crafting`,
`codex-harness`, `herdr`, and `messaging`.

The persistent configuration is only those skill names and these source
locators. `native-seat-launch.mjs` assembles their bodies only for the native
first turn; it stores hashes and locators in the receipt, never an assembled
prompt copy.

## Source set

The existing Field, role, naming, Nexus, Flow, knowledge, and theme sources
remain. The refreshed list adds the recent Fable-origin records for recovered
vision, fat prompts, vision-to-skills propagation, Field maintenance,
psyche/mind/Field routing, power allocation, and Flow's Herder responsibility.
The direct `1cb440` living message was reacquired from its native transcript
and guides this selection, but is deliberately not copied into a new source
file.

The exact living spellings `field soul`, `psyche fable`, and `mind soul` are
preserved as unresolved. The request for nine low-power agents is likewise a
direction about capacity, not a settled nine-seat composition. The source on
power allocation says the number of subflows depends on available power; no
profile silently chooses a roster.

## Launch gate

Do not launch while the Psyche Fable lifecycle repair is incomplete. Once the
parent reports it repaired, the smallest meaningful gate is one native
receipt-only start per Field profile through `tools/native-seat-launch.mjs`:
the observed turn must prove the declared native model and effort, the full
typed skill expansion including `main-flow`, and the exact source-manifest
hash. Only then may activation claim a distinct Flow identity, bind its HM,
and perform the one benign acknowledgement. Readiness then also requires the
native identity, HM health, routing, deployment-currentness, and assigned and
open defects to be evidenced before any ownership transfer. Existing
predecessor routing is retained.
