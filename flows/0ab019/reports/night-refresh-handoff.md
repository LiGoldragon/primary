# Mind Astra refresh handoff — morning 2026-09-20

**PENDING RECEIPT-FIRST SUCCESSION.** The user has authorized Field to launch
the correct fresh Mind Astra successor when its required receipts pass. This
report does not create, activate, rebind, retire, or transfer a seat.

## Incumbent and transfer rule

- Incumbent: Mind Astra `0ab019`, native thread
  `01a0b5f4-8a15-7260-bf9a-a560ab019a87`, `gpt-6-astra` at medium effort.
- Current observed Herdr/HM route: `messaging-build`, pane `wC:p1`, terminal
  `term_65bc6d207da1421`. Its predecessor `893603` is preserved.
- `0ab019` remains the active owner and crossover route until a fresh
  successor has passed native receipt, identity, route, and explicit
  acceptance gates. Then, and only then, `0ab019` becomes crossover-only.
  No broad seat retirement follows.

## Field launch gate and routing

Field must prepare and may launch only a **Mind Astra** profile, never adapt a
Field role. The profile must preserve `gpt-6-astra`/medium and carry the
inherited handoff references plus native-expanded required skills. Required
order:

1. receipt-only native first turn, proving role, model, effort, skill
   expansion, source manifest, and a distinct native identity;
2. observed identity/tools/live-health evidence;
3. exact Herdr/HM route binding and acknowledgement;
4. explicit acceptance of inherited work and child-delivery continuity;
5. only then activation and the crossover handoff.

The current launcher is receipt-first but has no named Mind-Astra-of-`0ab019`
profile. That profile and its source manifest are a real preparation gap, not
permission to substitute a Field profile. Field owns managed seats. Before a
transfer it must report readiness/routing to the incumbent; the new root must
not assume it has the same harness-child handles.

## Active inherited work

| Stream | Current observed state | Continuity requirement |
| --- | --- | --- |
| `night_messaging_build` | Fresh Terra, medium; isolated lock `2816`; workspace `/home/li/wt/github.com/LiGoldragon/flow/night-messaging-0ab019`; compiled Datom CLI first checkpoint. | Preserve its exact route or relay its complete reply to both crossover and successor. Broader async RequestId, successor, and Message work remains required. |
| `night_meaning_roundtrip` | Fresh Terra, medium; native thread `01a0bc9a-8e4a-7323-b316-1c76ef34be77` (short slice `6ef34b`); workspace `/git/github.com/LiGoldragon/meaning-language`; published parent `4b01cc94`, working parent `844aa1ab`; lock `2805`. | Writer retains live-run control. Runner `1130785` exited without wait; timeout child `1130798` and Nix child `1130800` were still live at the last handoff, with durable stdout but no guaranteed exit receipt. Supervision correction was ordered; no validation result is claimed. |

The old meaning PID `1064319` is gone with `EXITUNKNOWN`; anonymous pipes and
final exit evidence were lost. `4b01cc94` remains `UNVALIDATED`. The active
roundtrip has durable target evidence named
`remote-generation-and-datom-roundtrip-attempt-2.{runner-pid,child-pid,stdout-stderr.log,exit}`;
the exit component is pending. The earlier `e0ba37` lock `2754` is released.

`Expression.[Statement Question Request Annotation Prose]`,
`Vakya.Expression`, and `Prabandha.Vector<Expression>` remain a **PROPOSAL**
only. All known holes remain retained; no Vision landing is authorized.

## Messaging lock boundary

Historical core locks `1834`/`1835` are held by `553901`; `1819`/`1820` by
`562869`. Empty flow directories and no HM route are not death proof. Field
Reap Luna may release only after recorded dead-owner/inactivity authority and
native process, thread, and managed-endpoint correlation. Working owners and
their locks are protected. Field is coordinating this audit; Mind has released
nothing.

## OpenCode boundary owned by Field

- Published/tested Prometheus producer inputs: Horizon
  `ee8d6f8d27eb6e200504807971ffdd26aaca7ed1` and Goldragon
  `2820bd436cb1b49ff4a435c36004744f4459be33`.
- Home `ea3a0f9c` and CriomOS `400f39e1` are source-published import pins with
  no activation receipt. Last module hardening is `02bfc3cfb20d`.
- Field `1cb440` and Sol `8565e8` own the OpenCode path. A generated
  `opencodeServerPassword.sops` is authorized only under the existing,
  authoritative public recipient set. If that set cannot be evidenced, it is
  blocked; no recipient is invented.
- Never use a provider API key. Keep share off, use private
  `LoadCredential`, bind tailnet-only on ouranos port `4096`, and witness live
  docs, auth rejection, listener, and curl gates. Stop before the living's
  manual Codex browser login.

## Inherited open boundaries

Unity POC is not accepted for live send. The Primary Next trial has no
migration authorization. The vision graph proposal awaits comments; public
Message/Flow deployment gates remain open. Hardware MS2130/lazy-consumer work
is blocked. Preserve the Fable incident. No Vision landing or broad retirement
is authorized. Heavy compilation/generation stays remote-only; local work is
limited to light inspection and coordination.

## Delivery grade

This report is to be submitted to Field `1cb440`, PsycheHigh `f38926`, Psyche
Opus `b81560`, and root. A Herdr prompt acceptance is **Submitted**, not
Read; only an explicit recipient response upgrades that grade.
