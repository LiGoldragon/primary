# Flow ea1e56

Working instruction: remember cf0ed9, identify all implementations and hacks used to wire the desktop application to the persistent Codex server, and provide the evidence needed to decide what should be removed. This flow is an investigation; it does not authorize removing or changing those implementations.

Remembered: cf0ed9 — depth 1. Its psyche record, log, four reports, two witnesses, and last model response were recovered. Most relevant: the stock rollback was requested but not performed; the deployed Desktop remained customized; ordinary Desktop `thread/start` still failed after successful local-daemon initialization; successive ASAR patches targeted several different `codex_app` producers, including one syntax-breaking patch that was later corrected; and the final sanitizer attempt failed its check and remained uncommitted and undeployed. The current deployment boundary was lightly re-witnessed through Lojix: user-environment deployment 144 is the current completed activation, superseding cf0ed9's deployments 140 and 141. Current source and runtime details remain with the independent inventory witnesses rather than being inferred from the remembered flow.

Inventory settled: the selected user-environment revision is `90a12633cc60`; the active Desktop-specific crossing consists of local-daemon wrapper variables, the managed `resources/codex` link, and committed ASAR rewrites, while a wider sanitizer/check and generated bytecode remain only in the dirty working copy. The older Agent Intercom shared-socket patch/service, unofficial `codex-desktop-linux` branch, direct `CODEX_CLI_PATH` wrapper, and packaged proxy gate are historical and already deleted or superseded. The persistent owner and terminal routing are independent. No product source, deployment, service, process, configuration, database, cache, or conversation was changed by this flow.
