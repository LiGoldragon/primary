# Rust layering

## 2026-09-14 — A short high-level layer that reads like baby code; implementation blocks in separate files; part of the Rust standard skill

Context: closing paragraph of the metaNexus comment on the audit page.

> We're going to have to force it by making the rest of the code have multiple layers. At the higher layer it looks kind of like baby code and all of the implementation blocks are where the logic lies. These are separate files. The high-level view of the code is a really short high-level view of this process calling this method and it's very simple. There are only a few matches and all the bigger implementation blocks, where all of the functions are, are in a different file. We're going to make this part of the Rust standard skill writing.

-- psyche, typed, artifact comment.
