# Virtual machine

## 2026-09-14 — A full virtual machine testing node on Prometheus, implemented and deployed by the second layer once tested; its anatomy shown first

Context: "UE" is left as transcribed, read as "full VM" by the flow. "Neary desktop" is left as transcribed; the flow reads it as the living's desktop compositor.

> I want you to start and make sure that the second layer implements and deploys, once you've tested it and it works well, a system for starting so you can use a virtual machine, a full UE virtual machine on Prometheus. Create this raw virtual machine testing node if it doesn't exist already.
>
> Show me your anatomy of it at some point so I can maybe point stuff out or agree. It's going to synchronize the layers: the five pairs of agents and/or five triads of flows, populated on the right windows in the Neary desktop if the desktop is running. Each should be two, side by side, half and half of the screen, with the right font, so that everything shows up nicely around what I've got going on my desktop right now.
>
> I might have changed the size of the terminal, but whatever I'm using now is good for me. I think that's kind of the standard size for a laptop, like 12 inches, whatever I have.

-- psyche, STT.

## 2026-09-14 — The third layer debugs in the virtual machine with a dummy desktop, observing and taking screenshots, because it is secure

> You want to send that to maybe the third layer to debug and try and figure out what's wrong with it. They can use the virtual machine with the full GUI to run a test node of this, where they can observe the desktop and take screenshots, because it's secure, right? They can just take screenshots. There's no data or anything, just a dummy desktop with a dummy name and everything.

-- psyche, STT.
