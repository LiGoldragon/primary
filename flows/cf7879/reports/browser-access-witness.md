# Browser access — item 44 witness

Read-only inventory, 2026-09-16. No browser package was installed and no user browsing profile or credential store was read.

The session exposes the in-app Browser skill, browser-client module and Node browser execution tool. The prescribed bootstrap failed before a connection existed: `Cannot find module /home/li/.codex/plugins/cache/openai-bundled/browser/26.901.51231/scripts/browser-service.mjs imported from /tmp/.tmpIhTqqJ/trusted-worker.js`. The available skill/client files were under version `26.831.21537`; this is a runtime dependency/version mismatch observation, not evidence of a connected browser. No tab was opened or controlled. The Browser skill body was read from its filesystem under developer instructions; this is not a native project-skill injection receipt.

`command -v` found `/home/li/.nix-profile/bin/browser` and `/home/li/.nix-profile/bin/google-chrome`. `browser --help` identifies browser-use and documents CDP, MCP, state and screenshot operations. These are installed command surfaces only. No CLI connection, browser launch or authenticated website operation was tested; no fallback was used to bypass the failed plugin connection. The web search tool worked earlier for public dictionary lookup, which is not browser automation.

A self-teaching map job needs an actually connected browser, a bounded read-only training task, explicit origin/path scope, observed DOM/accessible names or supported API structure, semantic change fixtures, and a reviewable mapping proposal. Reuse stable meaning-bearing anchors; a CSS-only change should not retrain the map. A changed form destination, required input or action meaning must suspend the cached action for review. Do not store cookies/tokens in the public mapping repository. Current readiness: mapping anatomy proposed, browser connection unproved, unattended job not launched. The plugin/runtime mismatch needs packaging-owner repair before this browser path can be witnessed.
