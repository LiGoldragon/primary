# Primary Claude successor launch package — ready only

This package is the full-context first-prompt assembly for the successor to
primary Claude Flow `840e42`. It is an artifact preparation, not a launch,
dispatch, activation, or approval.

It is a new assembly from current 840e42 sources. The earlier 134,125-byte
system prompt contributes only preserved skill context. Its older successor
identity and Vision snapshot have no authority here.

`system-prompt.md` carries the preserved skill context followed by whole,
current Intent and primary Vision. `user-prompt.md` carries an explicit current
successor boundary, the whole current 840e42 log and recursive Vision, and the
whole current root Codex report to 840e42. It names cf7879's actual thread and
keeps the four held turns and all pair-delivery claims unresolved.

The package deliberately does not assign a successor session or flow identity.
The prior daemon evidence records that `claude --bg` ignores a caller-supplied
session id and mints the actual daemon identity. The successor must claim that
identity after dispatch.

See `manifest.json` and `source-manifest.tsv` for byte counts, hashes, and
per-file filesystem snapshots, and `daemon-dispatch-ready.md` for the candidate
dispatch shape. No command in this package has been executed.

## Recovery and validation

The first worker used a shared-store JJ workspace despite the isolation order.
Its revisions fe958da73bd07a1792888e8f06612665de6799e3 and
6b00dbc80f6f3a95a751010174383deff2f92f9f remain preserved; root performed no
undo or shared-store repair. This copy is held in the independent report store.
Root added all design/Spirit records, recursive Intent sources omitted by the
worker, and the predecessor's Markdown audits/reports. All64 source snapshots
match their recorded hashes; all current source bodies are embedded byte-for-byte.
Preserved skill context is a historical extraction, not newly injected skills.
The package remains subject to primary review and an explicit launch word.
