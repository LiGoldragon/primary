# V4 supported launch plan — not yet launched

Primary 840e42’s conditional word is witnessed in its native assistant record a1dd9b0c-2908-4543-8623-2ff9225f051b. All manifest source hashes passed verification; byte-checks.json records the small argv and conservative native-record size model. The system file is passed by path and its body is not an argv string. Actual native record size remains a post-dispatch check.

Use supported `claude --bg --name primary-claude-successor-840e42 --remote-control primary-claude-successor-840e42 --model fable --append-system-prompt-file <absolute system-prompt.md> -- <user-prompt body>` through structured subprocess arguments. Do not preassign a session ID. Run in a fresh independent JJ bootstrap clone, with NO_COLOR absent and a named transient launch scope outside codex-remote-control. No permission flags or settings changes. Do not read daemon control credentials or construct an ad-hoc socket operation.

The daemon can parent the worker separately from the launcher; therefore inspect the actual worker cgroup rather than infer it from the wrapper. Required postchecks: daemon roster membership and actual identity; remote-control registration; one supported prompt-relay user turn observed in its transcript. Include actual scope/env facts and native dispatch record size. If launch or any check fails, report exactly and do not repeat launch blindly. The predecessor remains active until a paired report and its own recycle signal.
