# Successor review reconciliation

This review arrived after the recorded v2 correction, v4 assembly and authorized 08:06Z launch of efa157. It does not undo that chronology or authorize a duplicate successor. The original package is preserved at [the archived v4 directory](../claude-successor-840e42-ready/README.md); its “not launched” label describes assembly time, not current status.

## V2 requirements

The content review found all requested additions in v4: the exact ten relayed turns and nine decisions with headers; consolidated items/order31–51; Nexus/subflow body snapshots; raw topic vision; and the predecessor, pairing, routing, night-loop, held-setting, fork, four-turn, morning-report and recycle boundaries. These are embedded source bodies, not new native skill receipts. The retained dispatch source was `dispatch/rejected/840e42bb.json` after the original path disappeared; its content is not a dispatch acceptance receipt.

`manifest.json` records fresh byte/hash checks of v4 artifacts and current snapshots of predecessor840e42 log, Codex paired report, actual successorefa157 log and the morning review. The embedded report necessarily precedes this review's own append. These read-time snapshots do not claim a single atomic cross-repository tip.

## Current launch facts and remaining gaps

Native launch of efa157 has CLI exit0, roster, active bridge, actual identity and prompt-relay user-turn receipts in the paired report. Four held original turns reached efa157; they remain unproved in predecessor840e42. The two launch defects remain: actual worker inherited the daemon's terminal cgroup, and inherited stdin appended launcher text to the first user record. Do not report the recorded CLI launch as absent, or these defects as repaired.

The later helper proposal `3ae16d9018ba7fae1794ea4d93f0bd64e0c1b27b` prepares structured `claude --bg` arguments, Fable, a distinct name, remote control, appended system file, explicit cwd, NO_COLOR removal and disconnected stdin. Local/remote mock and actual v4 prepare-only checks passed. It has not proved an actual daemon worker in its own scope. Wrapping the launcher in a scope did not satisfy that requirement.

The executable interface and postchecks remain in [the dispatch plan](../claude-successor-840e42-ready/daemon-dispatch-ready.md), interpreted with these corrections. No new executable dispatch was run. A future launch needs a new explicit word, a fresh independent workspace and name, a supported own-worker-scope solution, and fresh roster/bridge/transcript checks. Existing efa157 must not be duplicated merely because this older review arrived.

## Current boundaries

Codex remains cf7879, thread01a0a715-2d5d-7342-b278-1dbcf78795bd. The historical predecessor relationships and verbatim sources are preserved. Current840e42 routing uses the published report because its inbound native messages are approval-held; historical request text naming native messages is retained as history, not taken as a delivery witness. The two proposed permission settings remain decisions for the living. No settings, live service, shared HEAD or main was changed by this review.
