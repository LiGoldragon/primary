# Claude successor v6 — order 8

Unlaunched Claude packet only. V5 remains the immutable provenance for the
already launched Codex successor d9961c; do not relaunch Codex from this packet.

Run `python3 assemble.py`, then `python3 claude-launch.py --cwd <independent-clone>`.
Dry-run is the default. Add `--launch` only after efa157's launch word.
The complete base replaces stock context with --system-prompt-file. Source and
artifact manifests carry byte counts/hashes; complete skill bodies are packaged
inputs, not a new native skill-loader receipt for the assembling flow.

Order 8 changes: explicit Authority precedence and historical-stock note;
d9961c pair with pending cf7879 transfer stated; EnterWorktree and supported
harness routing rules; nexus body and current lane reports/log; explicit first
prompt identity/memory/first order/decisions/three routes; historical banner.

Post-launch identity, roster, own scope, remote control and actual prompt
user-turn witnesses remain required. Dry-run's bounded daemon-envelope model
is not a dispatch or actual record receipt. No launch is performed for review.
