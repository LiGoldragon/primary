# Flow 01a05487

Remember `01a047d2`, then explain what it said about “Creating or messaging Desktop threads through the app,” especially whether the Desktop app creates a messaging system between threads.

## State

- Remembered: `01a047d2` — depth 2. Recovered its final response and the exact Option 2/Option 3 passage: Desktop App Tools were a private Electron-host callback channel (`create_thread`, `send_message_to_thread`, fork/handoff, sidebar operations), separate from ordinary app-server conversation threads.
- Settled answer: the tools let a thinking machine ask the Desktop host to create a thread or inject a message into a selected thread, potentially from work occurring in another thread; they were not a general autonomous peer-to-peer messaging bus between threads.
- The living ruled Option 2: implement Desktop as an ordinary client of the persistent shared server, without the private Desktop App Tools channel; also update Claude and Codex TUI and GUI packages to their latest versions in CriomOS-home.
- Correction received: a nexus is a kind of thing, and “thinking machine” must remain explicit where shortening it to “machine” makes the referent ambiguous. Exact vocabulary-skill edits are to be proposed, not applied without approval.
- Settled implementation: ChatGPT now uses its native local-daemon transport to the persistent Codex owner. The ASAR patch makes `getConfigOverrides` empty, removing the private `codex_app`/App Tools route; the package removes the resource CLI override. The owner has `WorkingDirectory=/home/li/primary`.
- Settled package identities: Codex stable `0.151.0` (upstream commit `78c290807ce710180111df227df3b7a4fe845452`), ChatGPT `26.825.51511`, Claude Code `2.1.251` (upstream commit `37534ac596d80cefb02d272f036adba4ba055d2c`), and Claude Desktop `1.40609.0`.
- Landed: CriomOS-home commit `64d4784969e8f82cd9b4a732e8c27e8a5e90929c` implements Option 2 and the four version updates; follow-up commit `49dc1e12c6824353215ffb298d2698f6327cf0b6` repairs the refreshed Claude Desktop manager contract extractor. Both are pushed on `main`.
- Proof: all four packages built through remote-only Nix paths. Codex and Claude Code version/help smokes passed; Codex remote-control, ChatGPT desktop support, and Claude Desktop EGL/launcher/declared-CLI contracts passed after the extractor repair.
- Repository terminal state: `main`, `main@git`, and `main@origin` at `49dc1e12`; clean worktree; Locks `415`–`418` released.
- Skill-design audit: the authored vocabulary source is `Curriculum/skills/vocabulary.md`; no reference collection contains an equivalent naming rule. Minimal proposal: “Use a defined full term when shortening it makes the referent ambiguous.” and “Do not use a common kind noun bare as though it were the proper name of one instance.”
- Separate correction audit: authored `Curriculum/skills/orchestrate.md` says `Release.{<lock-id>}`, while current Orchestrate origin tests and the live protocol use scalar `Release.<id>`.
- Open: explicit approval of those authored Curriculum edits and regeneration. A signed-in GUI/live-daemon smoke needs activation authority and remains unclaimed.
- New question: determine whether the pushed CriomOS-home generation can be deployed and the persistent Codex app-server moved to `0.151.0` without disturbing running Codex sessions. This question authorizes inspection and an answer, not deployment or restart.
- Correction: the Orchestrate proposal is the direct one-token replacement inside the existing example, not a new “Replace …” instruction in the skill.
- Availability constraint: the living is going out and must not lose remote access. Do not deploy, restart, stop, signal, or replace the live Codex service unless reconnection is known for sure. Investigate a parallel new server and explicit remote cutover as a possible non-disruptive alternative; this is exploration, not authorization to start it.
- Notion under assessment: define two services, one pinned to the old executable so it is not restarted and a second using the update, then roll sessions across without disturbing the old service.
- Live continuity audit: the running owner is Codex `0.150.1`, owns the remote-control process and five attached TUI clients, and has no socket activation, reload, handoff, or client reconnect guarantee. A prior stop timed out and SIGKILLed the service cgroup. Do not restart it while remote access is required.
- Source continuity audit: clients connect once and treat disconnect as fatal. A second server can bind another socket, but existing clients do not migrate; sharing live `CODEX_HOME` risks competing in-memory thread ownership, while an isolated root lacks the old server’s threads and enrollment. Two services are therefore a possible new-session blue/green design, not transparent session preservation.
- Deployment correction landed: `a8ea62c88d51…` removes the two stale Desktop-gate consumers; `d0c217fc67a7…` repairs the canonical wrapper assertion; `04177e63325587cb8ad1458c3d0829a9df63f6f2` accepts the embedded ASAR marker. Exact Edge + medium evaluation, remote `codex-remote-control-vm`, and forced remote `desktop-app-support` are green. `main` and `origin/main` are `04177e63`; clean worktree; Locks `420` and `421` released.
- Safe staging distinction: Lojix `UserEnvironment.Realize` only realizes the closure; `SetProfile` changes the target profile without running Home activation; `ActivateNow` runs activation. Even staging awaits a repaired revision and explicit deployment authority.
- Current recommendation: the revision is repaired, but while the living is away only non-activating `Realize` is continuity-safe. Do not restart or activate the canonical service. A two-service rollout could preserve old sessions only by leaving the old unit byte-identical and routing new clients explicitly to an isolated new service; it cannot transparently migrate sessions and needs further design for state and remote-control enrollment.
- Design inquiry: find the clean end-shape for continually introducing new Codex server generations when some flows may always remain active, without accumulating an ever-growing set of remote entries on phone clients.
- Psyche search: the rolling two-service shape remains Notion. Existing Vision says “one server for everything,” rooted in `primary`, and not to introduce a nexus for this; no prior ruling defines server-generation affinity, transparent migration, drain, or a stable remote identity across versions.
- Central anatomy: temporary multiple phone-visible remotes are technically possible with isolated service/socket/state identities, but the clean one-remote design needs a stable Codex-owned ingress with versioned backends. To prevent immortal flows pinning generations forever, decide whether affinity lasts for a whole flow or only through an active turn, permitting durable thread resume on the current generation between turns.
- Conditional vision: the living is for moving a Flow to a new server generation between turns if that continuity is genuinely possible. Clarify that current Codex supports durable cold thread resume but not transparent connected-flow migration; establish precisely what `CODEX_HOME` persists and what new machinery would be required.
- CODEX_HOME anatomy: it is Codex’s durable per-user state root, normally `~/.codex`, holding configuration, canonical session rollout JSONL, SQLite indexes/state, logs/caches, and the default control socket; credentials and SQLite may be separately located. Live connections, subscriptions, in-flight turns, callbacks, active-writer ownership, and ephemeral threads remain process memory.
- Feasibility settled: after a clean turn and after the old server releases the thread-writer lock, a fresh server can `initialize` and cold `thread/resume` the same durable thread ID from its rollout. Under the defined vocabulary this is a successor Flow preserving thread continuity, not the same bounded Flow/process. Current clients disconnect fatally and do not migrate transparently.
- Clean transparent migration would require a stable protocol-terminating Codex ingress, durable thread-to-generation epochs/fencing, a quiesce/checkpoint/release barrier, backend resume/resubscription, request/notification correlation, and rollback. Limit the first design to clean turn boundaries with no pending approval, tool, background work, or subflow.
- Evidence gap acknowledged: prior work inspected Codex source/tests and the live deployment, but had not researched how other Codex operators upgrade active app-server/remote-control deployments. Delegating official-mechanism and community-practice research before drawing further conclusions.
- Operator research (2026-09-01): official Codex updates are sequential stop/restart with graceful drain, client disconnect, and forced kill after the grace window—not rolling server handoff. Public operators mostly accept disconnect/reconnect, manually recover stale daemons, or keep one supervised server and stop only after clients exit; no firsthand zero-disruption active-turn migration report was found.
- Strong official primitive: Codex `0.151.0` tests two app-server processes sharing `CODEX_HOME` with distinct `CODEX_SQLITE_HOME`; the secondary is rejected while the primary owns the thread writer, then resumes the same thread and starts its next turn after graceful primary shutdown.
- New upstream direction: PRs `#41916` and `#41918` on current Codex main add TUI client-side reconnect/resume preserving cached transcript, drafts, settings, and unavailable conversations. This is post-disconnect recovery, not server-side preservation of an in-flight turn, and inclusion in packaged `0.151.0` is not established.
- Negative reports: updates have dropped all attached clients and killed long turns after the drain limit; concurrent app-servers sharing a full live home have reportedly caused writer conflicts, duplicate continuations, state/router failures, or runaway processes. No maintained rolling-upgrade procedure or multi-owner remote-control contract was found.
