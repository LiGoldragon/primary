# Canonical Lojix proposal migration and Wispr activation

Remembered: 01a05209 — depth 1. Wispr packaging and declarative Home/CriomOS integration are complete; producer `9e991e50…`, Home `6201c493…`, and CriomOS `e3c9d714…` carry the Status-window transparency and keyboard-class active-seat `uaccess` repairs. The immutable Ouranos target realized, but Lojix 0.19.2 rejected deployments because its legacy ClusterProposal reader accepts only stale `goldragon/datom.dotos`, while canonical goldragon authority is `proposal.datom`. No compatible canonical proposal source was deployable, and adding a legacy duplicate was explicitly rejected as backward compatibility.

Ruled work: migrate deployed Lojix to consume canonical `goldragon/proposal.datom` with no compatibility duplicate; prove and deploy that migration; then activate CriomOS `e3c9d714` and verify the Wispr shortcut and Status-window repairs live. Related bead `CriomOS-home home-y3l` is already closed.

Correction: the machine prompt and remembered flow used the nonexistent `proposal.datomic` name. The living corrected the canonical artifact to `goldragon/proposal.datom`. Wrong-suffix implementation remained uncommitted and undeployed; all subflows were redirected before continuation.

No psyche record: the machine prompt is an execution instruction.

Canonical migration is published: Goldragon remote `main` `fbfd565389f3bb19572e28e8945d121fbedf84f4` exposes the sole proposal authority as `proposal.datom` and updates Synchronizer/records; Lojix remote `main` `76400200d42438af9275de66a0a7c94e7a43d850` releases 0.20.0 with exact `proposal.datom` admission and Horizon `Text<ClusterProposal>` embodiment; CriomOS remote `main` `d85bf804214bee5f57dc3d2cded7ba436f849a39` is the successor of e3c9 and pins that Lojix revision. The pre-fix remote test rejected a valid `.datom` proposal with `ProposalSourceUnreachable`; after the fix, remote test, fmt, clippy, package, daemon, and fresh-startup checks passed, and Goldragon projected all eight declared nodes through Horizon `f8c580…`. No compatibility proposal remains.

Correction: contradictory post-push reports treated Jujutsu's stale colocated local Git branch `main@git` as the fetched remote head. Direct forge `refs/heads/main` and Jujutsu `main@origin` prove the pushes succeeded; `main@git` is not remote evidence and fetch does not advance it. A redundant Lojix rebase onto the stale branch was stopped before commit or push. Goldragon's managed checkout was advanced to the verified remote head and now exposes readable `/git/github.com/LiGoldragon/goldragon/proposal.datom` with no `.datomic` artifact.

Deployment conflict: exact CriomOS `e3c9d714` pins legacy Lojix `33b8b6b…` as the systemd daemon package, so activating it would restore 0.19.2 and undo the migration. The living authorized implementing, testing, and deploying the associated successor CriomOS revision that preserves e3c9's Wispr changes while pinning Lojix `76400200…`. Successor `d85bf804…` is published. Its corrected no-transport Bootstrap BuildOnly request reached `BootstrapTerminal.Succeeded`, produced a durable GC root and protected terminal receipt, and an independent Prometheus-only realization from the four generated inputs equaled the rooted closure exactly. No reboot or unmanaged runtime override is authorized. Ouranos remains at generation 91 / CriomOS `9338e54…`; rejected attempts 92/93 had no effect.

The living confirmed deployment of fixed Lojix on Ouranos, selecting the established `ssh-ng://root@ouranos.goldragon.criome` Nix-store URI and `root@ouranos.goldragon.criome` SSH destination. The exact rooted closure copied successfully; content integrity, `switch-to-configuration check`, and `dry-activate` passed. Dry activation would stop/start `lojix-daemon`, `accounts-daemon`, and `polkit`; reload `dbus-broker`; restart `home-manager-li`, `nix-daemon`, and `systemd-udevd`; and remove nothing.

Live switch is held at the provenance gate: Ouranos requires signatures and already trusts the configured Prometheus cache public key, but the copied closure is unsigned, so ordinary `nix store verify` exits 2 while `--no-trust` content verification passes. The designed repair is a one-shot privileged signing of the exact d85 closure on Prometheus with its existing nix-serve key, then publication/copy through the existing cache path; no declarative trust weakening is needed. Explicit authority for that signing operation is pending. No profile change, live switch, activation, runtime override, or reboot occurred; generation 91 and Lojix 0.19.2 remain current.

Flow index registration is pending because `flows/index.md` is held by Lock 19 for flow 01a0433a.
