# Flow log

- Machine instruction: remember flow `4647d2`; help the living establish a legitimate, supportable way for the paid Wispr Flow service to power Listener on the living's station, including what must be observed and how to approach Wispr without implying an unauthorized agenda. Preserve credentials outside machine context and repository state.
- `FLOW_ID`: `b2da01`. `FLOW_DIRECTORY`: `/home/li/primary/flows/b2da01`.
- Correction: the loaded `main-flow` skill omitted the remembering operation. The parent wrongly narrated `remember 4647d2` as though the identifier's meaning needed interpretation. The living identified the instruction failure. Exact replacement instructions were proposed in the thread and await the living's approval before any skill edit.
- All flows are one subjectivity. Remembered: `4647d2` — depth 1. Most relevant here: the prior flow stopped private request replay, then ended with its Desktop observer interrupted and its observer locks released; no successful Wispr request or transcript was witnessed. The isolated Listener 0.17 provider architecture and consolidated `wispr-flow/credentials` consumer remain branch-only, while current production Listener is 0.14.0 and the running Wispr Desktop 1.6.7 process proves neither authentication nor transcription. Official entitlement to reuse the private desktop backend remains unknown.
- The living approved the positive remembering repair and the two `skill-designing` lines proposed in the thread. A child flow was dispatched to edit the authored Curriculum sources, regenerate consumer packets, test, commit, and push; the withdrawn negative guard is excluded.
- Public-source research found Wispr's documented Voice Interface API: WebSocket streaming with partial/final results and a REST fallback. Public API access requires Wispr approval and separate Developer Platform billing; the public material does not establish that a paid Flow subscription includes it. Wispr publicly supports Android beta, not a native Linux desktop application. Its published terms restrict private-interface reverse engineering, so the selected path is to request account-bound approval for Listener through the documented API before further live interoperability work.
- The approved skill repair was authored and pushed in Curriculum commit `3872f967b29fafd70951c4b7a188213ba1d4ea4b`; direct generation, repeated generated-output checks, and the remote-backed Nix flake check passed. Primary pins that revision and carries the generated `main-flow` and `skill-designing` packets in published commit `e1f8f33aad020dcab4a8122e5372fe1fafe554dd`. That shared Primary commit also contains this flow's log and an unrelated flow's witness; the skill child did not run or rewrite the commit, and the actor/cause of the concurrent commit was not established.
