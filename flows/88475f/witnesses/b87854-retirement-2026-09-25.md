# Witness: b87854 retirement

Method: direct inspection via `hm-list`, `hm-heartbeat-state`, and `herdr pane list --session messaging-build`, run from flow 88475f (subflow acting under 88475f's identity) on 2026-09-25.

## Observed before retirement

- `hm-list`: flow column showed `-` for agent `psyche-opus-b87854`, state `done`. No route entry for `b87854` existed in `hm-heartbeat-state` (it was never registered — see `flows/b87854/log.md`, "Registration blocked; not forced").
- `herdr pane list --session messaging-build` showed pane `wD:pQ`, tab `wD:tF`, terminal `term_65c40f03ab16676`, `agent_session.value = b8785453-ff53-4e5a-9a74-e21edb554467` (b87854's own Claude session id), `agent_status: "done"`.
- For comparison, flow `9c7514` (PsycheV2.{ Sonnet 9c7514 }) is a distinct pane: `wD:pW`, tab `wD:tK`, terminal `term_65c55829fcb1088`, `agent_session.value = 9c7514c1-9da9-48b9-b5af-025c4f38f468`, `agent_status: "done"`. Both `wD:pQ` and `wD:pW` currently display the terminal title "Psyche Opus 077114" — confirming the brief's note that b87854 and 9c7514 share title storage — but their pane ids, tab ids, terminal ids, and agent_session values are all distinct. Only `wD:pQ` was targeted.

## Authority

The living ruled on 2026-09-25 to retire flow b87854 now (per the main flow 88475f's brief to this subflow), citing: stale Opus seat from a failed morning refresh, transcript off, already told to stand down by e51411, closing its pane earlier blocked by a permission classifier.

## Sources

- `hm-list`, `hm-heartbeat-state` output (2026-09-25, this session).
- `herdr pane list --session messaging-build` output (2026-09-25, this session).
- `/home/li/primary/flows/b87854/log.md`.
