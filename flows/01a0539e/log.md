# Remaining Wispr shortcut repair on Ouranos

Machine instruction: remember flows 01a052bb and 01a05209; independently audit repository, deployment, and live state; implement, prove, deploy, and verify the remaining Wispr shortcut repair live on Ouranos. Preserve the existing Wispr session without reading its contents. Do not reboot.

Remembered: 01a052bb — depth 1. Canonical Goldragon proposal migration and Lojix 0.20.0 were proven and deployed through the published CriomOS successor `d85bf804…`; Ouranos reached generation 170 without reboot. Wispr's Status window was proven floating after relaunch with the authenticated session preserved. The remaining failure is keyboard-class event ACL/capture: intended keyboard event nodes still lacked `user:li` ACL while an unrelated system-control event had seat ACL.

Remembered: 01a05209 — depth 1. Wispr Flow 1.6.7 is packaged from a user-supplied installer; Home owns the client and CriomOS owns narrow active-seat input capability. Producer `9e991e50…`, Home `6201c493…`, and CriomOS `e3c9d714…` introduced the Status-window and keyboard-class `uaccess` repairs; the latter was carried into deployed successor `d85bf804…`. The authenticated session lives under a mode-0700 user-data directory and its contents must not be read. Focused Escape and `/dev/uinput` worked; physical keyboard event readability and global shortcut capture did not.

No psyche record: this is a machine execution instruction, not vision.

Current state: CriomOS bug bead `CriomOS-eg3` tracks the repair and remains open for the final living shortcut witness. Initial immutable revision `62d0e1c7…` added the activation-owned narrow refresh and a behavioral VM check; deployment 94 switched to generation 171, where physical devices gained `uaccess` but not `seat` or user ACL because the rule still ran too late in `99-local.rules`. Final pushed revision `c647e9d4868c4a20fd593b3ea32e8721ecf73f21` moves the physical-only rule to packaged `70-wispr-physical-keyboard-uaccess.rules`, before systemd seat/uaccess processing, while retaining the narrow refresh. The strengthened remote VM witness failed before that ordering repair and passed after it in 18.93 seconds; the adjacent laptop/keyd gate also passed. Lojix deployment 95 reached terminal Succeeded and switched Ouranos to generation 172 without reboot. Live event0/event1 are physical, `seat`+`uaccess`, and have `user:1001:rw-`; virtual event20 has none. The refresh unit is active and successful. No manual ACL, unmanaged trigger, broad input access, or reboot occurred.

Wispr session preservation: Wispr was not running after deployment. It was relaunched through the same transient `wispr-flow-first-run.service` and public profile launcher. The mode-0700 user-data directory was inspected only by metadata; `session.json` inode, size, and mtime remained unchanged and its contents were never opened or hashed. Hub and floating Status windows returned. The Wispr helper now holds event0/event1 and not event20, proving consumption of the repaired physical-device boundary without reading event bytes.

Failed living witnesses after generation 172: physical Ctrl+Meta produced no visible response despite correct ACLs and the helper's open physical-device descriptors. The Status window was also not repaired: unhovered it collapsed to a small control; hovered it expanded into a large opaque magenta rectangle; language selection worked, while the other visible control appeared inert. The earlier proof conflated “floating” compositor classification with correct transparency, geometry, rendering, and interaction.

Code diagnosis: the packaged app had no valid Linux PTT fallback for persisted profiles; a Mac-only `-1` PTT entry cannot match Linux helper VK events, whose left Ctrl+left Meta shape is `162+91`. The launcher also selected native Ozone Wayland whenever `WAYLAND_DISPLAY` existed, but the fixed 440×320 Status overlay depends on transparent composition and native window placement/resizing operations unsupported by this Electron Wayland backend. A reversible XWayland launch produced a genuinely transparent isolated Status X surface while leaving `session.json` metadata unchanged.

Successor realized: producer `fix/nix-runtime` `11b2ee0a411f89daedbbff5bd22b640f6a1ddb9a` normalizes only absent/platform-invalid PTT maps to `162+91:ptt`, preserves valid custom PTT bindings and unrelated shortcuts, and selects X11/XWayland whenever `DISPLAY` exists, falling back to native Wayland only when X11 is unavailable. Its red remote behavioral check failed both prior contracts; final remote evaluation, executable startup-contract/launcher checks, runtime check, and full Wispr 1.6.7 package build passed. Home `c46f9df321f29e7956520483b1d0fe99bb8c8291` pins that producer; CriomOS `2c12cd3ac5907991ea3a520a56654598c2b1bb5b` pins Home. Lojix deployment 96 Evaluate and deployment 97 ActivateNow both reached terminal Succeeded; Ouranos is generation 173 without reboot. The configured Nix context used the same distributed-builder shape as deployment 95, but Lojix's terminal record does not expose the executing builder host, so no more specific build-origin claim is made.

Live successor state: Wispr was launched once through the unchanged profile wrapper and transient `wispr-flow-first-run` mechanism, with no backend override. Main, GPU, and renderer processes carry `--ozone-platform=x11`; Niri exposes floating Status and Hub. The helper holds physical event0/event1 and not virtual event20; physical ACLs remain, virtual access remains excluded. `session.json` was statted only and its size/mtime/ctime remained unchanged; contents and authentication data were never read.

Second failed living witness after generation 173: hovering still turned the full Status surface into the opaque magenta rectangle; left Ctrl+left Meta produced no purple feedback in the shortcut recorder and no global response. This disproved treating an unhovered X-surface alpha sample as a Status repair and disproved treating open physical-device descriptors plus preference normalization as a shortcut repair.

Final diagnoses: Niri, not Electron, painted the exact `#e040a0` rectangle. Focus-follow-mouse made the frameless transparent Status window active when its hover alpha poll enabled input; Niri's SSD-less focus-ring default then used Home's Base0D active color as a solid background across the exact 440×320 bounds. Separately, event20 is definitively `keyd virtual keyboard` (`0fac:0ade`), not Wispr output. Keyd targets/grabs the built-in physical event0 and emits logical keys through event20, but the physical-only uaccess repair excluded event20. A hermetic producer VM proved pinned helper v0.1.2 correctly emits the exact fd-3 Ctrl/Meta frame sequence `162,91,91,162`, disconfirming a helper/protocol defect.

Final successor realized: producer coverage revision `fbb71248567f28ab66b23ada44760fcae8e11d9b` adds the hermetic helper fd-3 VM check without changing product behavior. Home `7314ec94b2fc68780067f9068effb6f3736c57bb` adds a Status-only Niri rule matching `wispr-flow` and both initial/live Status titles, disables compositor background drawing and the focus ring, and pins the producer coverage revision. CriomOS `9818e6f5fd1eebf84618eaed7e7a053c4eb49837` narrowly admits keyboard-classified `keyd virtual keyboard` with exact `0fac:0ade` identity alongside physical keyboards while excluding an explicitly classified unrelated virtual keyboard, and pins Home. The corrected pre-fix OS VM failed because keyd lacked seat/uaccess; the post-fix VM proved physical+exact-keyd admission, unrelated-virtual exclusion, and refresh recovery. Home's generated KDL passed Niri validation; the adjacent keyd gate passed.

Final deployment: Lojix deployment 98 Evaluate and 99 ActivateNow reached terminal `Completed/Succeeded`; Ouranos generation 174 is current without reboot. Live event20 has `seat`+`uaccess` and the `li` ACL. Wispr was launched once through the unchanged profile wrapper, remains on X11/XWayland, and its helper holds physical event0/event1 plus keyd event20. Bead `CriomOS-eg3` remains open only for the living witness: hover must show no magenta fill/outline, the shortcut recorder must show left Ctrl+left Meta feedback, and the global chord must respond.

Remembering completion: the exact final model response of 01a05209 handed the canonical-proposal migration and Wispr activation to the next realization; its mistaken `.datomic` suffix is superseded by that flow's annotation to `proposal.datom`. The exact final response of 01a052bb is the machine prompt that initiated this flow, after explicitly excluding incidental Codium observations from scope.

Correction: this flow initially appended its `flows/index.md` row before observing the remembered stale Lock 19. The active lock snapshot proved the conflict. Installed Orchestrate 0.26.0 and transcript evidence show the current scalar release form `Release.<id>`, whereas the loaded orchestrate skill's `Release.{<id>}` form fails with a Protos fault. The orphaned lock belonged to nonexistent flow directory `01a0433a`; it was released through the witnessed current form, and this flow then acquired Lock 389 over its flow directory and index. The index row is now reserved. The orchestrate skill was not edited because psyche-interraction requires explicit approval before every skill edit.

Continued living witness: holding left Control+Meta now records speech and inserts the transcription on release, proving that global capture and dictation work. There is no visible recording feedback. A surface appears to exist on one monitor arrangement because hovering an otherwise invisible area removes the focused-window outline, but it disappears under another Niri multi-screen arrangement. The application as a whole launches floating and follows the active workspace instead of entering Niri's tiling layout. The living also wants a shortcut-driven recording mode that does not require holding the chord. An unexplained Nix workload is consuming substantial CPU with no visible attached harness.

Current work: read-only subflows are independently identifying the Nix workload, diagnosing the Status surface and Niri floating behavior/available toggle binding, and mapping Wispr's non-hold recording capability plus the prior transcript handoff. No process has been stopped and no product or system change has yet been made in this continuation.

Flow-process correction: this continuation unnecessarily acquired Lock 406 before editing its own `log.md` and psyche record, by incorrectly carrying a prior shared-index coordination event into private flow-file ownership. The living clarified that a flow is the sole editor of its own files and that its one-line append to the append-only index also needs no coordination. The living approved this exact Curriculum rule: “A flow edits files under its own `flows/<short-id>/` directory and appends its own line to `flows/index.md` without acquiring a Lock.” A realization subflow is applying it to the authored skill, regenerating derived trees, validating, committing, and pushing.

CPU investigation result: the load is PID 3180901, a user-owned `nix eval --show-trace --raw .#checks.x86_64-linux.wispr-status-niri-rule.drvPath --override-input system path:/tmp/wispr-home-system.sUEFdr` in `/git/github.com/LiGoldragon/CriOMOS-home`. It began through `codex-remote-control.service` during this flow, has no build children, and consumed about 99% of one core for more than two hours with roughly 2.67 GB RSS. Its stdout/stderr remain held by the Codex app-server; the client is not an OS orphan, although its old Nix-daemon request worker was reparented after a daemon restart. The temporary override path no longer exists. No process was signalled or changed.

Direction changed: do not invest further in repairing Wispr's floating Status overlay. A Niri status-bar surface is a better eventual desktop integration, but the actual destination is the living's own Listener Nexus using Wispr as a transcription provider. Keep the production Listener running. Recover the already-realized provider work, obtain the authenticated desktop credential through a secret-preserving boundary, and run an isolated sandbox Listener witness that cannot interfere with production.

Remembering in progress: 01a04e75 — depth 2 — its log establishes Listener 0.17.0 feature head `71a194df8dc6dfb232049473481e67b405153e49`, private Wispr gRPC support, request-time gopass delivery, Wispr-first/OpenAI fallback, provider health, durable work, and pause-aware segmentation. Main integration, gopass provisioning, live interoperability, and activation were not completed. The continuation is recovering the exact final response and lightly checking the present repo/live state before using that work.

Remembered: 01a04e75 — depth 2 — exact final response and latest correction read from transcript. The work is isolated on pushed Listener bookmark `listener-wispr-01a04e75` at `71a194df8dc6dfb232049473481e67b405153e49` with four pinned companion feature refs; all component mains and Home/CriomOS remain on Listener 0.14.0. Production `listener.service` is active with its existing sockets and stores. The 0.17.0 feature passed its full synthetic Nix gate and package build but never performed a real Wispr call. The final correction emphasizes that `wispr-flow-linux` repackages the proprietary Electron desktop client and its normal cloud behavior with Linux integration; it is not an independent API implementation.

Credential/protocol discovery: the desktop profile stores either a Supabase `access_token` or WorkOS `accessToken` in mode-protected `session.json`; only metadata has been read. Listener 0.17.0 expects fixed `gopass` entries and has no stdin/fd credential interface. Its inferred `inference.wisprflow.com` gRPC route differs from the desktop bundle's Baseten route and extra backend/model/environment headers, so compatibility remains unproved. The isolated witness must keep credentials out of flow context and logs, use no production sockets/stores/audio/clipboard, and disable OpenAI fallback.

Insertion discovery: Linux Wispr owns the clipboard and synthesizes plain Ctrl+V through X11 XTEST here after active-window detection, with optional clipboard restoration. Codex TUI interprets that chord as image paste, explaining the observed error. The destination can change during recognition, and clipboard ownership/restoration introduces further races.

Recognition discovery: personal vocabulary may bias `Nix`; correction-driven learning depends on proper-noun extraction accepting the correction. A deterministic replacement rule would also rewrite genuine “Knicks.”

Process correction landed: Curriculum commit `4f5fcba049c3` and primary generated-evidence commit `350a7d771085` were pushed after generation and checks passed. The `flows` skill now contains exactly the living-approved no-Lock rule for a flow's own directory and its index append.

Runaway evaluation resolved: after exact PID/owner/argv revalidation, TERM did not stop PID 3180901, so the delegated worker revalidated again and sent KILL. The evaluator and its stale request worker exited; Codex app-server, the current TUI, Wispr, and the healthy Nix daemon were untouched.

Sandbox realization dispatched: one implementation subflow is working from Listener 0.17.0 in a new isolated worktree/bookmark, with Wispr-only policy, unique sockets/stores, no production audio/captures/clipboard, and a known non-private speech fixture. Because Listener lacks a safe credential consumer contract, the expected minimal boundary is an inherited file descriptor read and parsed inside the process; session material may not appear in arguments, environment, tool output, logs, temp files, clipboard, or commits. A read-critical subflow is independently auditing the protocol and isolation before the witness is trusted. No main integration or deployment is authorized in this round.

Fallback desktop direction: keep Wispr Desktop installed as a contingency if Listener fails, but make Listener the ordinary surface and Wispr only its future provider backend. The forced X11/XWayland launch was introduced solely to repair the Status overlay and is now contrary to the desired stack; narrowly undo it, return the main desktop window to Niri's native tiling flow, and retain the repaired physical+keyd shortcut input boundary. Status-overlay quality is explicitly secondary.

Listener widget clarification: the Listener status-bar widget already exists; the earlier proposal to add one was an unwitnessed assumption. The living envisions extending it with a visible cue when an active recording enters the pause-seeking cutoff window, so they know to leave an obvious pause before the hard chunk limit; the cue should clear when the pause closes the chunk and starts the next one. Anatomy remains open before implementation: the cue's urgency/modalities and the exact beginning of the warning window.

Listener operation design round: the living ruled streaming capture and ordinary capture as different typed operations/messages, with providers able to support both modes. A third typed operation accepts the path of an already-recorded audio file and performs non-streaming transcription. Wispr may become the first streaming provider, and a future visual may show provider partial transcripts in real time. Before implementation, clarify whether file-transcription results only return to the caller or also enter Listener's durable history/delivery path, and establish the partial-transcript visual's ownership and persistence once the backend evidence is complete.

Visual-client realization authorized: research current shell-native and compatible visual surfaces for live partial transcription, prefer integration with the existing Listener widget/Noctalia shell when it is the best fit, then package the chosen client in the Home/CriomOS line, test it against the Listener feature, deploy it, and verify it live. Sequence repository writes after the native-Wayland worker to avoid Home/CriomOS collision.

Worktree correction: the previous Listener feature worktree was present and unclaimed. The sandbox worker found unrelated dirt in the main checkout; management then explicitly chose a new `jj workspace` from the prior feature head rather than first assigning the existing feature worktree. This was not caused by a Lock and created an unnecessary extra workspace. All Listener work remains based on the prior feature head; before closeout, consolidate the accepted line into one Listener worktree and conclude the redundant workspace(s).

Native-Wayland fallback deployed: producer `b984155c`, Home `ea461ec7`, and CriomOS `858d6968` are pushed; Lojix deployment 102 reached terminal Succeeded and made the immutable OS revision current without reboot. Wispr was relaunched through the existing profile service. All six processes use Wayland flags and none use X11 flags; Hub is tiled/non-floating while Status remains floating. Session metadata and physical+keyd helper boundaries are unchanged. The app exposes start/stop hands-free deeplinks but no native global toggle action; no binding was invented. Free candidates include `Mod+Shift+H`, `Mod+Shift+R`, and `Mod+Alt+R`, with Ctrl-prefixed bindings intentionally avoided because they could begin PTT.

Isolated Wispr backend witness result: pushed Listener sandbox commit `04c5ba2aba60b5e2c49de86e740f33c88161b0b4` on bookmark `listener-wispr-sandbox-01a0539e`, based directly on the prior 0.17.0 head. Its dedicated one-shot binary uses inherited descriptors, sandbox-only paths, Wispr-only routing, direct TLS with proxy/redirect disabled, and no daemon/runtime/audio device/OpenAI/retry/system clipboard. Before the later informed-access hold, it made two single submissions of the non-private phrase “Listener sandbox confirms the blue comet.” The original two-message encoding failed with a redacted provider-protocol failure; a corrected Desktop-1.6.7-shaped request with UUIDs, Linux identity, bundle descriptor, combined init/audio/final commit failed identically. Neither returned a transcript; whether either reached inference or billing is unknown because trailers were deliberately suppressed. Session metadata was unchanged and no refresh/write occurred. Nineteen library tests and the package build passed; the full flake check is blocked only by inherited formatting in `tests/runtime.rs`. No third private-backend request is authorized.

Sandbox workspace closeout: the redundant clean `listener-wispr-01a04e75` workspace was unregistered without deleting its directory; commits remain reachable. `listener-wispr-sandbox` is the sole tracked Wispr feature workspace, alongside the default workspace and an unrelated pre-existing `parallel-transcription-healing` workspace. The worker recorded and stopped a prohibited early `/nix/store` name traversal; it exposed store path names only, not credentials or session data.

Streaming research: Desktop 1.6.7 genuinely sends init/context/audio packets over a bidirectional transcription stream and receives intermediate states containing progress, raw/formatted text, candidate beams, and audio metrics before final result. The official Wispr Voice API separately offers WebSocket partial/final results, recommends roughly one-second audio chunks, and describes rough intermediate transcripts around 30-second intervals, but access is approval-based and metered; consumer Flow Pro does not officially grant it or private desktop-backend reuse. OpenAI's current Realtime transcription API exposes delta/final events but is materially more expensive than its batch transcription models.

Visual candidate selected: extend the existing Noctalia `criomos/listener-level` plugin with a persistent shell-native multiline panel consuming a new private Listener transcript stream. Keep `status.sock` text-free; store partial/final text only in Noctalia memory, reject stale revisions, and avoid notification spam. This fits the pinned shell lifecycle and styling better than raw Quickshell, Mako replacement notifications, SwayNotificationCenter, or numeric-only wob. A realization subflow is reusing the completed native-Wayland Home and CriomOS workspaces; no new worktree is being created.

Transcript visual deployed: Home `5f8b26502d26ac32d6f35fb9806c2ec3f51c87ec` and CriomOS `e066e993ba72cb45d8713a7bd02e6ca52d3c5b91` are pushed. Lojix evaluation 104 and ActivateNow deployment/generation 105 succeeded and are current without reboot. The existing Noctalia plugin now has a bounded multiline panel and strict private transcript-stream parser. Synthetic tests proved ordered updates, stale rejection, closed clearing, status-text rejection, and no notification calls; pinned plugin lint passed. Live, a temporary socket was created only after proving production `transcript.sock` absent; partial/final events opened and rendered the known synthetic phrase, then `closed` dismissed it. Production Listener retained the same PID and sockets; the temporary socket and screenshot were removed and the phrase was absent from shell cache/state. Remaining shell limitation: Noctalia can toggle a panel but cannot query/idempotently open it, so a manual mid-stream panel change reconciles on the next terminal/disconnect transition.

Final worktree closeout dispatched: conclude only the clean, landed producer/Home/CriomOS workspaces from this flow; keep the single continuing Listener Wispr sandbox workspace and all unrelated/default workspaces. No directory deletion is authorized.

Worktree closeout complete: the three exact clean `wispr-native-wayland` producer/Home/CriomOS workspaces were unregistered through `jj workspace forget`; their directories remain on disk and no files were deleted. Default and unrelated workspaces, including `parallel-transcription-healing`, were preserved. One tracked Wispr Listener feature workspace remains. Orchestrate reports no active Locks. The daemon's documented worktree observe/conclude shapes faulted, so no successful daemon conclusion claim is made.

Delivery ruling: existing-file transcription should follow the living's ordinary Listener behavior—write the transcription to clipboard and durable transcription history, with no automatic focused-application injection. Verify that production 0.14 and feature 0.17 do not already have an injection surface before treating this as unchanged behavior.

Hands-free shortcut anatomy: start and stop must use the same modifiers and prioritize comfortable one-hand reach on the living's Colemak layout over mnemonic letters. Inspect actual physical-key placement and current Niri conflicts before proposing the pair; the earlier mixed-modifier proposal is superseded.

New feasibility inquiry: determine whether the installed proprietary Wispr Desktop can supply a minimal authenticated local streaming proxy for Listener by reusing its own client path, rather than reconstructing the undocumented Baseten protocol. Keep observations, hypotheses, and unknown licensing/support boundaries separate; do not access session contents, edit the bundle, or make backend calls during inquiry.
