# Claude Code and Desktop connectivity

The living corrected a speech-to-text error: the subject is Claude Code, not a cloud server. The inquiry is to remember all prior work bearing on Claude Code and whether the intended connection was ever completed; establish whether the desktop application can connect to Claude Code today; map the relevant components and protocols; assess reliability and common real-world use; and explain the implementation shape if it is absent.

Correction source: the earlier prompt said, “Remember everything that has to do with the cloud server,” which led to the mistaken subject. The intended words were “Claude Code.” No authored skill produced the error.

Remembered: 01a0338f, 01a038be, 01a03e02, 01a03f47, 01a0437d, 01a04524, 01a047d2, 674a4dab, 01a05487 — depth whole relevant chain. The most relevant result is that Claude Desktop's declared-local-Code path and the persistent native Remote Control owner were both realized in source, with the latter deployed, but no retained signed-in Desktop acceptance witness closed either path.

Settled by current witnesses: Claude Code and Claude Desktop are packaged declaratively. The currently running generation is Claude Code 2.1.250 and Desktop 1.37937.3; source has 2.1.251 and 1.40609.0 but was not witnessed activated. Desktop's ASAR is patched fail-closed to the declared Claude Code executable. A live `claude remote-control --spawn=same-dir` user service owns sessions rooted at `/home/li/primary` and reconnects through Anthropic's relay. Agent Intercom is separate: Claude Code uses MCP over stdio and a local Unix-socket broker; it is not Desktop's Remote Control transport.

Correction from the living's current use: Claude Desktop does discover and control the locally running Remote Control owner. This supplies the missing human-visible acceptance evidence. The remaining limitation is that the remote session does not expose the local session's `bypass permissions` choice. The earlier conclusion that Desktop access was unknown is superseded.

Conclusion: the intended Desktop-to-Remote-Control path works end to end. The present inquiry is now specifically whether bypass mode can be established by the local service owner or whether Anthropic's Remote Control boundary deliberately disallows it. A custom desktop client would use the Claude Agent SDK and own a Claude subprocess per session; MCP would expose tools, not control the conversation.

Open: the ownership and protocol semantics of Remote Control permission mode, and whether the desired bypass behavior can be configured safely on the locally running owner.

Settled by direct local CLI witness on Claude Code 2.1.251: Remote Control permission mode belongs to the local owner. `claude remote-control --help` supports `--permission-mode bypassPermissions` for spawned sessions. The live service starts only `claude remote-control --spawn=same-dir`, so it currently receives the default mode. Anthropic's remote UI deliberately omits Auto and Bypass; a remote client cannot toggle them. The supported host form is `claude remote-control --spawn=same-dir --permission-mode bypassPermissions`. The global `--dangerously-skip-permissions` alias was not established for this subcommand and should not be used.

Open design boundary: enabling this on the persistent, auto-restarting owner rooted at `/home/li/primary` would make its spawned same-directory sessions bypass prompts without container or VM isolation. Decide whether that owner should always start bypassed, whether a separately isolated owner is wanted, or whether remote sessions should retain prompted permissions.

Ruling: the living chose to start the existing server with the flag. Realization dispatched: add the documented `--permission-mode bypassPermissions` to the declarative Remote Control owner, update its contract, validate, commit and push, deploy the user environment through Lojix, and verify the live service command. No reboot or non-declarative runtime override is authorized.

Realized: CriomOS-home main `3320400338bd3e989d137bb43e8297f7b5d66565` adds the permission-mode flag and updates the focused contract; CriomOS main `e6d23efa4626b625c585dcabc49a2401cd2b930c` pins it. Both were pushed cleanly. Lojix UserEnvironment Realize deployment 129 and activation deployment 130 both reached `Completed / Succeeded`. Live witness after activation: the user unit is active at PID 857561, and both systemd ExecStart and the running process argv are exactly `claude remote-control --spawn=same-dir --permission-mode bypassPermissions`. No reboot or direct runtime mutation occurred. All write Locks were released.

Correction: the prior claim that new remote sessions will use bypass mode outran its witness. What was verified was the restarted owner's live argv containing `--permission-mode bypassPermissions`; session-level inheritance was not observed. The living reports that a newly started remote chat does not show Bypass. Because Anthropic deliberately omits Bypass from the remote selector, absence in the UI may be expected, but actual tool behavior or a session-level runtime witness is still required. Diagnose whether the new chat reaches the restarted owner and inherits the host mode, whether it uses a stale remote owner, or whether the flag does not govern the Desktop-created path.

Diagnosed before the final UI witness: exactly one Remote Control owner was active, PID 857561, started at the deployment activation time with the correct argv and zero restarts. The previous owner stopped then; sanitized journal events showed session creation after the new owner started. A second restart or new remote registration was unnecessary. The documentation's restricted remote choices were mistakenly interpreted to mean the UI would never report the host's effective Bypass mode; the living's direct witness below corrects that interpretation.

Acceptance witnessed by the living: in a newly started remote chat, the permission button initially does not show Bypass; after the first prompt, it changes on its own to `Bypass permissions`. This closes session-level propagation and clarifies the UI lifecycle: the effective host mode becomes visible only after the remote session is initialized by its first turn. The declarative change works end to end.
