# Port: curriculum-deploy

Report for flow 6329f1.

## Result

curriculum-deploy 0.3.0 ported to the ProtoformStack train and merged
to main at 8ca4d4a5.

## Main rev

`origin/main`: `8ca4d4a53e1bc9e103c6c974a2ca73c056bd0731`

## Diff verdict

All four generated trees (.claude, .agents, .codex, .pi/agents) are
byte-identical between:
- the new curriculum-deploy 0.3.0 generating from Curriculum 5716f71a
- primary's rendered trees from the old curriculum-deploy 0.2.5

The only filesystem difference is `.pi/continue/` (Pi session storage,
not a generated tree).

## Summary

1. All hand-written DatomRealizing/DatomTextualizing (708 lines in roles.rs,
   148 refs in runtime.rs) replaced by generated Datomic impls from an ethos
   Library declaration in `curriculum-deploy.ethos`.
2. Request root changed to a data enum: `Generate.{ /curriculum /primary }`
   is the whole request; the `CurriculumRequest.{ ... }` wrapper is accepted
   for backward compatibility.
3. Types generated by ethos-zero 1.1.0 into `src/generated.rs`, committed
   with a freshness test through the ethos-zero library.
4. Pins: protos 56c683ec, datomic a27f9b8e (new URL), ethos-zero 31c5984c
   (dev-dependency). Curriculum bumped to 5716f71a.
5. All 5 cargo tests pass, clippy clean, fmt clean, all 6 nix flake checks
   pass (build, test, external-data, fmt, clippy, and the external-data check
   against the pinned Curriculum).
6. Three API deviations documented in reports/api-deviations.md: Library mode
   omits derives (worked around with generated_ext.rs), Meaning/Text boundary
   in legacy Curriculum data (normalized in application code), paths starting
   with `.` in datom (curly-quoted by datomize, old inventory format unreadable).

## Primary flake input bump

The main flow must update primary's flake.nix:

```
curriculum-deploy = {
  url = "github:LiGoldragon/curriculum-deploy/8ca4d4a53e1b";
  inputs.curriculum.follows = "curriculum";
};
```

And update the Curriculum input to match:

```
curriculum = {
  url = "github:LiGoldragon/Curriculum/5716f71aae7d0927ed01e02ad0cf2edd9f4aadac";
  flake = false;
};
```

Then regenerate: `nix run .#generate-skills 'Generate.{ /git/github.com/LiGoldragon/Curriculum /home/li/primary }'`

The invocation format changed: the new binary accepts `Generate.{ ... }` directly.
The old `CurriculumRequest.{ Generate.{ ... } }` form still works.

## Undone

- The Curriculum's `roles.datom` still uses parenthesized `(...)` text for
  string values. curriculum-deploy normalizes Meaning to Text, but the
  Curriculum should migrate to curly quotes (`\u{201C}...\u{201D}`).
- The old `generated-role-outputs.datom` in primary uses unquoted dotted
  paths that the new parser cannot read. The cleanup for those stale files
  will be skipped on the first regeneration; the regeneration itself
  overwrites all current role files.

## Sources

- curriculum-deploy origin/main 8ca4d4a5 (witnessed)
- nix flake check (6 checks, all passed, local build)
- cargo test with CURRICULUM_TEST_DATA_ROOT=/git/github.com/LiGoldragon/Curriculum (5 tests, all passed)
- diff -rq of .claude, .agents, .codex, .pi/agents between primary and scratch generation (byte-identical)
- reports/api-deviations.md (3 deviations)
