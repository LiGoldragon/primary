# Flow 01a05cd5

Remember the recent Zeus OS deployment and determine why `wispr-flow` is unavailable in Bird's session. Treat the prior deployment as a claim until deployment records, declarative source, and Zeus live state independently witness what was built, activated, and exposed to Bird.

Completed:
- Recovered the prior Zeus and Wispr handoffs, diagnosed the configuration fault, and recorded the correction to the stale `min` attribution.
- Made Bird Max like Li, moved Wispr from a Li-only gate to the cumulative Medium tier, retained the ChatGPT Desktop resolver repair, and pushed the owning revisions.
- Deployed the combined immutable revision to Zeus over the witnessed non-LAN hostname/Yggdrasil transport through Realize, TestActivation, and ActivateNow gates.
- Verified the live/persistent generation, Bird's Home and session services, Wispr executable/desktop exposure, and the ChatGPT resolver artifact without launching ChatGPT or reading Wispr secrets.

Relevant written psyche: a full CriomOS deployment to Zeus is expected to change the user environment; fixes belong in universal CriomOS-home composition and must not hardwire Bird or Zeus. The present request is a working diagnosis instruction, not a new Vision entry.

Correction: the prior deployment log says “Bird's declared min profile intentionally excludes VSCodium extensions.” This witnesses a source selection, not living approval. The living explicitly says they never ruled that Bird's profile should be reduced. Trace the origin of the `min` assignment rather than treating it as intent.

Ruled target: Bird keeps her own account-specific settings but receives the same profile size/tier as Li; the existing `min` selection is wrong. Realization and deployment now follow after the source and live witnesses identify the exact owning declaration.

Live/deployment witness: Lojix deployment 122 terminalized `Succeeded` for immutable CriomOS `c1ad61aee2a38a2f813cad6b62421f9ae7503fac`, which pins Home `122bc1d10be7abb4d75542720a1e7c6594fb88bf`; Zeus is live on that generation. Bird has no Wispr executable, desktop entry, unit, process, or application-index entry, while Li has the packaged executable and desktop entry. The deployed Home source independently hard-gates Wispr with `user.name == "li"`. Current Goldragon proposal projects `bird Medium li Max`.

Correction to the initial diagnosis: the prior flow's `min` wording is not a current-source witness and the exact proposal revision consumed by deployment 122 remains unknown. Current source says `Medium`, not `min`; either way it is not Li's `Max`. The decisive Wispr exclusion is the separate Li-only username gate, so the realization must correct both the profile-tier mismatch and the package ownership model without introducing a Bird-specific gate.

Remembered: 01a05833 — depth 1. Its exact final response reports Zeus Realize 120, TestActivation 121, and ActivateNow 122 succeeded on CriomOS `c1ad61ae` / Home `122bc1d1`, preserving Bird's final-session manager and Codex/Claude services without reboot. The rollout verified Codex, Claude, Lojix, profiles, and session continuity; it never promised or witnessed Wispr on Zeus/Bird. Wispr had been an explicit Li package deployed and witnessed only on Ouranos. No typed user message approved a reduced/min Bird profile; the specific `min` attribution first appeared in assistant commentary.

Source realization pushed: Goldragon `2a139455ba6d` changes Bird `Medium` to `Max`; Home `a74adda5f4af` replaces the Li username Wispr gate with the universal `user.size.max` predicate; Home `35013ded85cb` adds the focused tier check; CriomOS `7b9d0880dbc2` pins that Home revision. All affected mains are pushed and clean. The focused check reaches the intentionally required materialized-system boundary and cannot evaluate standalone. No deployment was inferred from incomplete Query output; the exact successful Zeus request tuple is being recovered from the prior transcript before resuming typed materialization and activation.

Correction: recovering the exact prior deployment tuple did not establish that its direct-LAN store transport remained current. The living states Zeus is not on LAN. Deployment 125 had already been admitted as CompleteHost Realize against the stale `ssh-ng://root@192.168.18.95` route before this correction arrived; no TestActivation or ActivateNow was submitted. Observe 125 only to terminal, reject that route for further requests, verify a current non-LAN transport pair, and combine the Bird/Wispr change with the just-completed ChatGPT Desktop fix before any new staged rollout.

Correction: Wispr belongs to the Medium profile tier, not Max. Bird still belongs at Max like Li. Replace the pushed `user.size.max` Wispr gate with the repository's Medium-tier ownership semantics and prove that Max users inherit it; deployment 125's revision must not be activated.

Remembered: 01a05c80 — depth 1. ChatGPT Desktop resolver repair Home `b04edb442f52` / CriomOS `69a946b1d0e5` is pushed and remains in current main ancestry. Realize 123 and ActivateNow 124 succeeded; a fresh ChatGPT PID connected by WebSocket to the unchanged persistent Codex owner and did not spawn a second server. The combined Zeus revision must retain these commits.

Deployment 125 terminalized `Succeeded` as Realize only for the superseded Max-tier Wispr revision; no TestActivation or ActivateNow was submitted, so it did not alter Zeus live or persistent state. Horizon's magnitude ladder witnesses that Max sets `medium=true`; the corrected gate is `user.size.medium` with below-Medium absent, Medium present, and Max inherited cases.

Current transport witness: reject `192.168.18.95`, which routes via LAN. `zeus.goldragon.criome` resolves over `yggTun`; strict root SSH identifies Zeus and `nix store info --store ssh-ng://root@zeus.goldragon.criome` reports a trusted Nix 2.35.1 store. The valid pair is `(ssh-ng://root@zeus.goldragon.criome root@zeus.goldragon.criome)`. Current combined candidate CriomOS `2929538c510ce2f84bf5317cfe21f450d5140b9d` pins Home `cf21965b7934364fcd6edf3edcbedf60a72d5972`; the latter contains the Wispr and ChatGPT fixes. Goldragon proposal `2a139455ba6d2f71c3ba60bf56452c0be446f0d3` separately projects Bird as Max.

Corrected Zeus rollout completed over the hostname/Yggdrasil pair: CompleteHost Realize 126 succeeded at ledger 3182, TestActivation 127 succeeded at 3220, and ActivateNow 128 succeeded at 3258 for immutable CriomOS `2929538c510ce2f84bf5317cfe21f450d5140b9d`. Lojix records 128 Current/LiveActivation; Zeus is generation 68 and `/run/current-system` equals the persistent system profile. Bird's Home Manager, user manager, sessions 4/3, Codex, Claude, and Niri are active; her profile exposes an executable `wispr-flow` and `wispr-flow.desktop`. No reboot, direct runtime patch, or Wispr secret/content read occurred.

Final source: Goldragon `2a139455ba6d2f71c3ba60bf56452c0be446f0d3`; Home `cf21965b7934364fcd6edf3edcbedf60a72d5972`; CriomOS `2929538c510ce2f84bf5317cfe21f450d5140b9d`. The active Bird profile also contains the ChatGPT `resources/codex` resolver candidate; ChatGPT itself was not launched. `desktop-file-validate` is absent on Zeus, so the active profile symlink and application exposure—not validator output—witness the desktop entry.

Historical child-lane consolidation: the non-LAN transport and ancestry witness
is retained with this root lane. Its settled route and terminal deployment
observation are already incorporated in the current transport account above.
