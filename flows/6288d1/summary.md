# Flow 6288d1 successor handoff

## Why this flow is refreshing

The living directly ruled that this Mind main accumulated too much context and
crossed its role boundary by implementing source instead of dispatching the
work. The successor must remain a coordinating main: dispatch implementation
before the first implementation edit or command, review returned evidence,
and refresh again before coordination context becomes overloaded.

The main-flow skill needs an operational correction. Field `9ddcbc` currently
owns the authored Curriculum `main-flow.md`/`refresh.md` paths under lock 5184.
A bounded worker named `main_flow_skill_correction` is auditing the exact
source and will hand an exact patch to that owner without overlapping it.

## Durable completed work

- Recent Psyche for Flow report: `flows/6288d1/reports/recent-psyche-for-flow.md`.
- Transition/hold proposal:
  `flows/6288d1/reports/transition-hold-operational-flow-nexus-proposal.md`.
- MetaBindExisting v2 contract: meta-signal-flow
  `71e92386ac0318485ce7eb3bbfab250ad44c68b3`.
- MetaBindExisting Flow runtime: Flow
  `6ed7d1742995647930c8a1d2b484f18bf7061f9c`.
- Exact source and test handoff:
  `flows/6288d1/reports/meta-bind-existing-v2-source-handoff.md`, Primary
  `aeafa8751878ceba1a2466e85769835c7b533402`.
- That Flow source passed the living-authorized local full fallback: all 15
  flake checks, including 44 Flow Nexus tests. Prometheus cache access had
  timed out before the fallback. The sources were not deployed by this seat.
- Generated flashbook illustrations were completed and published earlier;
  their receipts are in this flow's log/transcript.

## Active implementation — do not duplicate

Worker `flow_codex_next_finish` owns completion of the isolated worktree:

`/home/li/wt/github.com/LiGoldragon/flow/mind-sol-6288d1-codex-next`

Orchestrate lock: 5200. Parent: Flow `6ed7d174`. The worker was explicitly
authorized to adopt and correct the uncommitted edits mistakenly made by this
main, run the tests, commit, push, and return an immutable revision.

Accepted endpoint contract:

- Home exposes a home-setting-only immutable wrapper for each Codex runtime.
  It sets exact `CODEX_HOME`, passes arguments unchanged, and adds no remote or
  model default.
- Flow selects one exact configured tuple: client, socket, home, and model set.
- Next includes observed `gpt-6-astra`, `gpt-6-sol`, and `gpt-6-luna`; stable
  includes supported stable models such as `gpt-5.6-terra`.
- Flow passes the wrapper through Herdr's new explicit executable selection,
  owns exactly one `--remote unix://...`, and uses the same wrapper/socket/home
  for proxy, startup turn, receipt, and refresh.
- Unmapped or multiply mapped models fail closed. No PATH client fallback.
- A new Start remains Pending until the exact native startup receipt is
  observed. Existing bindings are not migrated from current process/model
  inference. Legacy v2 Restart may fail closed rather than guess an endpoint.
- Field `9ddcbc` owns Primary launcher/startup/title source. Field owns
  Herdr/Home executable selection and packaging. `eb7bae` is the sole deploy
  executor. Mind must not edit those paths or launch around Flow.

Before the correction handoff, the current worktree compiled and its original
44 Flow Nexus tests passed. Field then identified two issues: no PATH proxy
fallback and no duplicate `--remote`. The worker received those corrections
and the final wrapper contract; only its returned immutable revision is a
candidate.

## Confirmation follow-up

Mind `47764b` published additive MetaConfirmExisting contract short
`9ae6e62`, based on the v2-compatible meta contract; four focused round trips
passed. It did not edit Flow runtime or ordinary signal-flow.

This follow-up is not required for the first new Flow Start. A new Start owns
its native launch receipt and can promote its new binding. Confirmation is
needed only to promote an already-running imported seat from
RegisteredUnconfirmed/Pending without fabricating readiness. Integration must
remain a separate slice after the first working Flow-mediated next-server
Start.

## Live priority and routing

The living's top priority is one real Flow-mediated next-server Codex Start,
built, deployed, and tested by Field; then refresh all Codex mains through
Flow. Stable predecessors remain until their successors are ready with
handover. Claude main refresh follows only after the Codex path is proven.

Do not route to old Psyche High `836818` or phased-out Psyche Medium `b80e55`.
Current Psyche High is `752e0f`; current Psyche Medium is `e51411`. Field
`9ddcbc` is the active launcher/source coordinator; Field High `9e735b`
coordinates deployment, and `eb7bae` executes it.

## Other queued work

WiFi Phase 1 is ready but lower priority than Flow launch. Sources:

- `flows/b80e55/reports/wifi-roaming-research.md`
- `flows/752e0f/reports/wifi-roaming-review.md`

It requires 802.11r/k/v across the two Prometheus BSS configurations, matching
mobility domains, corrected SSID and positional clavifaber operator text, plus
roam/regression tests. It has not been claimed or implemented by this flow.

## Sources

- This flow's native transcript and routed machine messages, 2026-09-23 to
  2026-09-24.
- The durable reports and immutable revisions named above.
- The living's direct refresh and delegation correction in the current turn.
