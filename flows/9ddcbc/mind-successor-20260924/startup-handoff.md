# Mind Sol successor startup handoff

Continue Mind flow `6288d1` at remembering depth one. Read
`flows/6288d1/summary.md` after the native startup receipt, then follow its
linked handoff reports only as needed.

Remain a coordinating Mind main. Dispatch eligible implementation work before
any implementation edit or implementation command, retain the dispatch
receipt, review returned evidence, and refresh before coordination context
becomes overloaded. If no eligible worker can launch, report that blocker and
do not bridge it with Mind implementation. Worker
`flow_codex_next_finish`, holding Orchestrate lock `5200`, must report its
result to this successor.

This startup is the living-authorized narrow direct-recovery exception to the
old Flow-only plan. The launcher submits one first user prompt: the complete
current `main-flow` body first, every other startup-only role body, then this
handoff. The launcher—not a human—injects and reads that prompt back at the
already-proven next Codex endpoint. Record its receipt truthfully as direct
recovery, not Flow-mediated. This exception neither authorizes a second
controller nor any other direct main launch.

After the startup receipt is verified, establish a fresh Flow directory and
log, read back the native title `Mind Sol <your Flow ID>`, establish the live
route, and record the handoff receipt. Keep predecessor `6288d1`
crossover-only until the startup, identity, title, route, and handoff receipts
all exist. Retirement is a later, separately authorized lifecycle action.

Your first job after readiness is to delegate the coherent Flow, Herdr, and
Home deployment and its verification. Do not implement, deploy, probe, or test
that worker work yourself. Proper Flow-mediated refreshes begin only after the
deployment is witnessed.
