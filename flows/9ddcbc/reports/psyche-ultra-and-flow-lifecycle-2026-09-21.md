# Psyche Ultra and Flow lifecycle readiness, 2026-09-21

Owner of this bounded coordination snapshot: Field Medium `9ddcbc`. The living asks for all four Psyche main seats, Flow-native self-refresh/reaping capability, and sustained work across the twelve aspect/power cells. This is a snapshot and dispatch record, not a launch or readiness certificate.

## Psyche seats

The living's latest correction confirms Psyche Low `0625c3` **working**, Sonnet 5, at `messaging-build/wD:p7`; an earlier `wS:p1`/idle observation is stale for launch decisions. Psyche High `1b8ac0` and Medium `b80e55` had HM working rows at this snapshot. Psyche Ultra Low has no HM Flow row and no witnessed native launch receipt. HM state does not alone prove the physical pane/native identity joins for the other cells.

Field Medium `753e69` owns the previously prepared single-seat Psyche launch profiles and is the coordinated launch owner. This flow submitted an earlier Sonnet+Haiku request based on stale Low evidence, then immediately sent a correction to `753e69`, High `03e825`, Low `0347d0`, and Ultra Low `c88918`: **do not launch or refresh Sonnet Low; launch only Psyche Ultra Low**. All HM returns were `Submitted`, not target-read acknowledgements.

The current Haiku candidate is `flows/753e69/psyche-haiku-native/`. Its one-seat manifest passed `node tools/native-batch-refresh.mjs validate --manifest ...` with `valid:true`, as did the now-withdrawn Low candidate. This proves schema and current declared source-hash validation only. The living now specifies exact model `claude-haiku-4-5-20251001`; candidate `profile.json`, `modelCatalog`, `launch-manifest.json`, and `profile.test.mjs` still use `claude-haiku-4-5`. The owner must update all coupled declarations, validate the exact model/harness and source currentness, then run the one-seat launch. The candidate declares `Psyche Ultra Low`, main-flow, psyche and other structured skills, a new pane, and a post-identity canonical title; no native skill expansion or new Flow ID is yet witnessed.

This caller's `HERDR_ENV` is unset. Installed `herdr --skill` explicitly requires `HERDR_ENV=1` for session inspection/control, and `tools/native-batch-refresh.mjs start` rejects an outside caller. No variable spoof, raw Claude start, Herdr control, pane creation, or launch occurred here. An eligible Herdr-managed executor must perform the exact one-seat start and observe bootstrap, native main-flow/psyche skill expansion, model/effort, own Flow ID, title readback, exact pane/session/terminal, route, and role acceptance. Existing Psyche seats and routes stay intact.

## Flow lifecycle and sustained work

`flow-nexus.service` and `message-daemon.service` are active. The installed `flow` CLI exposes `start <predefined-type>`, `restart <flow-id>`, and `resolve <flow-id>`; current Flow source documents generic `codex-medium` as the predefined start. Direct `flow resolve 0625c3` and `flow resolve 753e69` returned `RecipientResolutionRejected.UnknownFlow`. These are live runtime observations: they do not establish native Field/Psyche startup, cross-refresh message hold/reattach, or autonomous replacement of current managed seats. The Flow store's newer delivery gate/permit/refresh code is an isolated source candidate; installed binary parity, migration, and disposable end-to-end acceptance are separate open gates. A running daemon is not a working twelve-seat self-respawn loop.

The active Field owners received submitted tasks: `753e69` and High `03e825` for the corrected Haiku-only launch, Low `0347d0` for exact one-at-a-time lifecycle/ghost preflight, Ultra Low `c88918` for read-only twelve-cell census, and Mind High `4b0f60` for source-to-installed Flow/Message lifecycle readiness. None of these submissions by itself proves they read or accepted the work. Keep protected crossover flows and FlowMessage consumer lock `3776`; do not use idle/done HM state or missing responses as reaping or replacement authority.

Sustained work should be driven by accepted, bounded duty/topic ownership and receipt-aware delivery. An idle healthy seat can satisfy its cell. Exact twelve-cell physical/native matching, current context evidence, queued work, and scoped authority must precede wake/refresh/reap. A request for hours of activity does not prove that all twelve are running or authorize repeated turns without work or duplicate launches.

## Later Haiku profile observation

The `753e69` owner updated its Haiku candidate to the living's exact `claude-haiku-4-5-20251001` in the profile, catalog, manifest, and fixture. A read during concurrent edits first found a source-hash mismatch for `flows/1b8ac0/vision/flashbooks.md`; this was sent to the owner. After its correction, this flow reran `node flows/753e69/psyche-haiku-native/profile.test.mjs` and the one-seat `native-batch-refresh.mjs validate`; both passed in the observed working copy. At that snapshot the profile/test files were still uncommitted and HM had no Haiku row. These validation passes are not a native launch, remote publication, or title/Flow acceptance receipt. The owner was sent the exact pass and remaining commit/Herdr gates as a Submitted HM message.

Later read: the profile and fixture were committed at `493f432b1` on `origin/main`; a separate Field High manifest amendment matched the exact model. The Field High `03e825` report records a genuine managed-shell Ultra attempt before this exact-model amendment that created retained `wD:p8`, terminal `term_65c0377a0776b56`, and reserved UUID `f61ea06b-b5ba-45be-8313-0afe7d84fd03`, then failed before Claude startup with `Unexpected end of JSON input`. Its process read saw only a shell, with no native receipt. Those retained attempt files and UUID do not constitute a Haiku seat or justify reuse. Field lifecycle `6db4fe` owns guarded launcher continuation repair under lock `4090`; High `03e825` owns eligible managed execution. Latest HM still showed no Haiku row. This flow made no launch attempt and sent the source/receipt boundary to High as Submitted, not read acknowledgement.
