# Flow/Message existing-guest acceptance — 2026-09-21

Field Medium 9ddcbc owns CriomOS-home consumer reservation 3776 and this acceptance boundary. Field 6db4fe holds reservation 4373 on Prometheus `/var/lib/microvms/vm-testing`; retained Terra `claude_usage_research` is its sole runtime executor through 6db4fe. Mind Medium 2c61af selects the immutable integrated producer graph; f72ab7 retains its component writer scope. Mind's mode plan names Field03 as a semi-sandbox/native-auth ownership lead, not as the guest reservation holder. A passive 2026-09-22T01:14:27Z census placed Field03 `03e825` at `wQ:p9`, native `01a0c4a7-2590-7ae3-8625-bee03e82586d`, terminal `term_65c003095a6af53`, with a `done` terminal hint; that is route evidence, not guest ownership or acceptance. No second VM/controller, production cutover, or model login is included here.

## Current grades

- **Source:** signal-flow v3 `1a8bb198642ac0cad5b99f4926283efae81a3a8b` exists on `night-messaging-flow-delivery-signal-v3-f72ab7`; the living relay reports its contract test passed. Meta-signal-flow `c61099aa77471872cd13cc317d4ca42d1e0dee1c` on `mind-medium-meta-flow-v3-2c61af` now repins its normal and build dependency plus lock to that v3. signal-message `fb57e6c18e6477892dfc971d121b0bc05185ac98` on `mind-medium-signal-message-refusals-2c61af` adds typed Raw/FlowLocked refusals. Remote branch heads for these three revisions were read back. Flow handler `6d23cd7e` is source-only and fails closed without verified handoff; f72 retains final consumer `Cargo.toml`/`Cargo.lock`/vendor coherence and the Message bridge. These are component facts, not one generated-and-tested runnable graph. The earlier Flow `1b57de01` remote check failed E0596 before VM runtime. Its stale lock/unvendored Git failure is a source graph issue distinct from the later Prometheus cache/SSH outage.
- **Fixture:** `flows/6db4fe/flow-message-vm/flake.nix` evaluates a separate `runNixOSTest` guest against old Flow/Message pins. The living narrowed execution to the existing `microvm@vm-testing.service`, so that fixture remains unbuilt/unbooted and is not a ready command for this trial. No accepted existing-guest package/image overlay and single-recipient typed CLI fixture has been returned.
- **Existing-guest preflight:** `flows/6db4fe/flow-message-existing-guest/` at Primary `c6c2b50c` contains four files: README, `guest_preflight.py`, example manifest, and five focused tests. Its `plan` validates six immutable graph fields plus generated-contract, remote-check, and Field-parity receipts; all nine inputs are unset in the example. Its `inspect` is read-only, refuses wrong/unreachable host before unit or image reads, and cannot build/start/login/test. I reran the five focused tests with bytecode writes disabled; all passed. This is a fixture **source** receipt, not guest or pair readiness.
- **Runtime:** the earlier Prometheus witness from 6db4fe had the existing unit loaded/static/inactive, runner executable, and `root.img`, `booted`, `vmt0` absent. These are historical facts. A later preflight ran on **Ouranos**, the wrong host, and is discarded entirely for Prometheus readiness. A single strict-host-key SSH attempt from this flow later timed out on port 22; current Prometheus guest/host state is **unknown** and needs reobservation by the controller. Neither timeout nor wrong-host state is evidence of a guest boot, disappearance, or failure.
- **Transport:** Field's fresh receipt at 01:20:24 UTC records one 8.008-second TCP/22 timeout for the sole configured Prometheus `ssh-ng` endpoint. It establishes endpoint transport failure for that attempt only; it says nothing about guest state, daemon builder, authentication, store or candidate source. No build or retry accompanied it.
- **Transport/read:** no disposable delivery attempt, transport confirmation, recipient read, application acceptance, or completion receipt exists for this graph. Each grade must remain separate.

## Required existing-guest fixture

Mind returns exact immutable Flow, Message, signal-flow, signal-message, meta and CLI revisions, lock graph, generated projection freshness, bridge/API compatibility, and remote Nix checks. Field 6db4fe then prepares a **guest-specific** parity fixture that installs those exact built output paths into the existing vm-testing guest, with its actual service binary/config/socket/store paths, typed CLI commands and one disposable recipient. Record source revision → derivation → output hash → guest path → running `/proc` executable hash before any message. The fixture must prove that it uses only the already declared guest, not the separate `runNixOSTest` check. The current CriomOS-home branch e1096a16 and production units do not supply that fixture.

The approved VM start interface, once those gates pass and the host is reachable, is the existing strict-host-key Prometheus route invoking `systemctl start microvm@vm-testing.service` under controller 4373. This is an interface, **not a ready run command**: no graph, parity fixture, or current reachability receipt exists. Every preflight and fixture first asserts the SSH target's pinned host-key/endpoint and observed node identity are Prometheus, before **any** unit, guest path, process read or mutation. A negative fixture that points the same command at Ouranos must refuse before those reads. A hostname string alone is insufficient without the pinned endpoint identity. After target assertion, record `current`/`toplevel` links, unit state, guest directory, root-image existence/digest, and active QEMU/tap set. After start, require the exact unit/PID/tap and guest SSH host-key trust; no key, token, home, agent forwarding, or login state is copied to the guest.

## Single-recipient acceptance and rollback

Use one disposable logical recipient, one binding generation and one stable source-event identifier. Raw mode, if selected, is explicitly UID-authorized, unattributed and unlocked: prove exact one-recipient transport state, reject forged sender/PID/permit claims, and make **no recipient-read claim or acknowledgement probe** from transport confirmation. FlowLocked, only if implemented and enabled in the integrated graph, must acquire one durable permit for the exact binding, preserve refresh admission as a separate gate, submit once, release only on matching confirmed transport evidence, and retain an ambiguous attempt across restart without automatic replay or Raw fallback. Test busy, stale binding, refresh race, and failed successor separately; do not broadcast. A recipient Read or application Accepted/Completed grade requires its own target-side evidence.

Before each case, take or identify a consistent disposable guest-state checkpoint and record schema version, queued/held/ambiguous rows and hashes. Preserve post-checkpoint writes and ambiguous attempts in a failure or rollback; restoring an old disk image alone is not lossless rollback. Stop only `microvm@vm-testing.service`, verify QEMU/tap exit, and retain logs and state before removing only run-created scratch against the preimage. Production services and stores remain untouched. No Haiku/Luna process is required for Flow/Message transport proof; [6db4fe's native-login report](../../6db4fe/reports/vm-native-login-interface.md) separately marks Luna's ephemeral app-server path as a proposal and Haiku's strict process-only interface unavailable.

## Disjoint implementation paths and readiness

Field 9ddcbc may prepare installed-pair/declarative parity in its existing lock 3776 paths under `/git/github.com/LiGoldragon/CriomOS-home`: `modules/home/profiles/min/message.nix`, `modules/home/profiles/min/flow.nix`, `checks/message-service-path/default.nix`, `checks/flow-service-path/default.nix`, and `modules/home/default.nix`. Its report/rollback contract lives only in `flows/9ddcbc/reports/`. Home `flake.nix`/`flake.lock` pins and check registration need their separate exact reservation after tested producer revisions. Field 6db4fe/Terra owns the existing guest state under lock 4373 and chooses any disjoint guest-specific fixture source path; 9ddcbc does not edit `flows/6db4fe/flow-message-vm/`, `CriomOS/modules/nixos/test-vm-host.nix`, or the guest filesystem. The existing `flows/6db4fe/flow-message-vm` flake is a separate guest proposal and cannot be run under the current scope.

**Accepted source division:** Terra through Field 6db4fe owns only `flows/6db4fe/flow-message-existing-guest/{README.md,guest_preflight.py,manifest.example.json,test_guest_preflight.py}` and any later explicitly reserved new file in that same directory for the existing guest. Its source reservation 4455 was released after the c6c2b50c publication; lock 4373 remains the guest-runtime reservation, not a blanket source-write grant. I own only the five CriomOS-home 3776 paths and `flows/9ddcbc/reports/`. These are physically disjoint; I accept the division and will not edit Terra's fixture. New source edits need Terra's exact renewed reservation. Field03's native-auth ownership lead does not supersede either lock.

**Parity input contract from my lane:** before a guest case, supply one immutable six-revision graph with generated/projection and remote check receipts, exact CLI/Nexus built output paths and SHA-256, effective guest config/socket/store locations, guest image and running `/proc` binary hashes, and a one-to-one mapping to my staged CriomOS-home consumer package/config/unit paths. The current Home message default/store differs from production, and Flow is disabled/unpinned; a comparison report must show the discrepancy explicitly until a separate flake reservation and tested pins make it match. For each typed case Mind must provide its **actual compiled** Raw or FlowLocked request/response syntax, one recipient and binding generation where applicable, UID/peer attribution policy, stable source-event ID, and the precise transport, recipient-read, and application outcomes the current API can observe. Do not infer or invent a CLI command from the prose case list. FlowLocked unavailable must refuse without Raw fallback.

**Rollback input contract from my lane:** capture target-host identity first, then unit/current/toplevel links, guest image and state preimage, output/config hashes, schema/migration version, store owner exclusivity and exact queued/held/ambiguous records before the attempt. Record every post-checkpoint event and its attempt/binding; test stop of only the reserved guest and process/tap disappearance; preserve post-checkpoint records in a demonstrated restore or forward-recovery path. A prior image alone is insufficient. Production rollback is separate: old binaries cannot be restored against incompatible new rows without lossless reverse migration or a forward repair. All receipts must carry source, build, guest-runtime, transport, read and application grades separately.

The first honest next checkpoint is an immutable graph plus an **existing-guest** parity fixture, with target-host assertion and a controller-approved exact run command. Neither fixture nor command is ready yet. Until then, component tests stay at source grade and no guest-start grade is claimed.
