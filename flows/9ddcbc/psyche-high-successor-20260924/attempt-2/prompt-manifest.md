# Psyche High concise prompt manifest

The successor received one curated first prompt of about five kilobytes.

Included directly:

- the living's alignment purpose and current preference for lean prompts;
- Psyche High responsibilities and aspect boundaries;
- predecessor 836818 at depth one and the known early-exit ordering failure;
- current Flow, Message, refresh, messaging, and Prometheus state at summary level;
- the required Flow claim, HM registration, supported title, and readiness marker;
- the no-noise rule for inter-seat messages;
- a bounded native skill list.

Referenced or summarized rather than embedded:

- the predecessor's full transcript and log;
- the earlier Psyche High payload chain;
- the two full Prometheus reports;
- raw Flow and Message build logs and deployment receipts;
- exact native identifiers, full source revisions, content digests, store paths, and process details;
- historic flashbooks and unrelated architecture reports;
- the prior 313 KB assembled packet.

Omitted because it is operational noise for this role:

- duplicate handoff text;
- command transcripts;
- full hashes and UUIDs;
- pane and inode evidence;
- stale routing rows;
- exhaustive skill text.

Exact prompt and acceptance evidence are stored beside this manifest and under `receipts/`.
