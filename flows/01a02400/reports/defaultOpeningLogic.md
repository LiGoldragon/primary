# Default opening logic report

The 2026-08-21 migration is complete as a record, not as a new configuration action. The written psyche authorized Chrome as the ordinary web fallback and the bounded switch to a `handlr-regex` opening engine behind `xdg-open`; it explicitly left the exact Zoom and Element URL rules, Zoom installation, and broader routing design for later. The legacy context for the `2. yes` Vision entry classifies accepted routing scope as both custom schemes and selected ordinary web URLs; it identifies Element/Matrix and Zoom meeting links as examples, while leaving exact accepted URL sets deferred.

The historical realization's deployment claim is now independently witnessed: published CriomOS-home commit `3aad4cad674926f0e921590079d285d8fcaef028` is pinned exactly by published CriomOS commit `90da2a6f82ecd73a40850303abaa4e911a20c51f`, and ordinary Lojix query reports deployment 23 terminally `Completed` with `Some.Succeeded`.

The current browser defaults are Chrome for HTTP, HTTPS, and HTML. The current shell nevertheless resolves the system `xdg-open`, because its PATH places `/run/current-system/sw/bin` before the user profile and lacks `/home/li/.local/bin`; this is a direct observation, not an inference about every graphical process. `handlr` agrees on the web and file handlers except for directories, for which it reports `codium.desktop` while `xdg-mime` reports `org.gnome.Nautilus.desktop`.

The legacy statement that the focused default-opener check had been realized is historical evidence only: its supplied output path is not currently executable, so this migration makes no current green-check claim.

The following are carried claims from the superseded realization, not fresh witnesses: the authored Home shape used a handlr-backed `xdg-open` wrapper; the system `xdg-open` used the portal when `NIXOS_XDG_OPEN_USE_PORTAL=1`; an earlier live association mismatch sent web types to GNOME Epiphany because it named `chromium.desktop`; historical reports described command-compatible handlr routing with editor-specific handling; and browser automation should remain distinct from desktop association. The legacy record also claims that `programs.chromium` wraps Google Chrome, Element Desktop owns its `element:` and `io.element.desktop:` schemes, and Zoom was not installed. Those package and scheme details were not re-probed because their exact routing remains deferred.

The legacy opening-path model, retained as a claim, was:

```text
system-first PATH → xdg-utils xdg-open → desktop portal → desktop MIME/scheme default
user-local wrapper wins PATH → handlr → matching desktop entry
```

The legacy operational history is also retained as claims. The bounded switch was authorized only for the Home-owned opener and Chrome fallback; after remote-only proof, deployment was to follow the recovered flow `01a01bac` procedure: submit `Deploy.UserEnvironment` through `meta-lojix` using strict Dotos and the owner socket, then observe through ordinary-socket `lojix` until terminal. Admission was not completion, and no reboot, emergency mutation, GUI launch, or separate browser-automation action was authorized. The original isolated Home change claimed generated Home Manager session variables and UWSM environment both prefixed `/home/li/.local/bin`, while leaving non-browser file MIME ownership in the minimum profile. Its durable check was said to begin from system-first PATH, source both generated environments, require the local wrapper to win, and invoke the wrapper against a fake Chrome desktop entry without launching a GUI.

The builder chronology is a carried claim: the focused evaluation became green after correcting the `chromium.desktop` versus `google-chrome.desktop` expectation; remote-only build proof was initially blocked by Prometheus timeouts and no alternative configured builder; later the focused remote-only check and exact activation output were said to succeed on Prometheus before the two published commits and deployment 23. This migration does not convert any of those historical build claims into a current green witness, especially because the recorded check output path is now unavailable.

The current-process limitation is a carried claim with a directly observed shell analogue. The legacy realization says niri starts directly and retains its inherited environment with its children; Home Manager activation cannot rewrite that environment, and systemd/D-Bus imports would not update niri-spawned applications. It says a fresh login selects the handlr wrapper while existing shells select the system opener. A graphical logout would terminate applications and requires explicit psyche approval; it was not performed. The direct current-shell witness confirms only the latter system-first selection, not the behavior of every graphical process.

The legacy distinguishes browser association from opener-engine selection: it claims the stale system `xdg-open` uses the portal, whose web defaults already point to Chrome, so association can be effective while selecting handlr remains stale. The direct MIME/default-browser witness establishes the Chrome associations; portal invocation and all-process opener selection remain carried claims rather than new witnesses.

The no-logout system-profile approach is an unimplemented design fork, not a witness: export the same handlr-backed wrapper once for both Home and NixOS, then place it ahead of xdg-utils in the system profile so existing processes resolve it on later invocations. It would make the opening engine system-wide and needs an explicit psyche ruling before any implementation. Importing environment into systemd/D-Bus or reloading niri were described as partial alternatives in the legacy record; neither was attempted here.

No GUI launch, logout, configuration change, environment mutation, or deployment occurred during this migration. The fresh graphical-login witness and the Zoom/Element routing design remain deferred with `home-nhb` blocked.

## Sources

- Flow `01a02400`: `flows/01a02400`
- `flows/01a02400/log.md`
- `flows/01a02400/vision/defaultOpeningLogic.md`
- `flows/01a02400/witnesses/defaultOpeningLogic.md`
- Recovered deployment procedure, flow `01a01bac`: `sessions/realization/2026-08-19T222016.md`
- Implementation check provenance: transcript `rollout-2026-08-21T20-29-06-01a02595-71c1-7471-91f5-57b3008e5419.jsonl`, JSONL record 1159 (ordinal 1158)
- Superseded legacy realization: `sessions/realization/2026-08-21T130824.md`
- Superseded written-psyche record: `psyche-raw/Vision/default-opening-logic.md`
