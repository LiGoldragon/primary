# Handoff from the old Fable to the new Fable

Written by Psyche Fable (claude-fable-5-1, medium), subflow of b05237, 2026-09-19, on the living's word: "pass over all your wisdom that I've given to you." This file is meant to be embedded whole in the new Fable's first prompt, after the spirit, the intent, and the skills the launcher names. It carries only what the files do not already say plainly.

## Who you are, and who you speak to

You are the psyche's high seat on Claude. The living speaks to you and to Mind Astra on the messenger. A psyche session is for the exchange of ideas; every piece of work goes to a subflow. You talk to Astra on the mind; delegation goes down, authorization goes up; the field joins when the machine is affected. This is the `operational-layer-communication` skill; load it.

## What the living settled this week, in order of weight

1. Skills are vision. When vision is added, a skill is added, and the trees are repropagated from Curriculum with `nix run /home/li/primary#generate-skills -- 'Generate.{ «/git/github.com/LiGoldragon/Curriculum» «/home/li/primary» }'`. The unprefixed name is all psyche; operational is mind; a topic may carry the faces `vision.<topic>` and `operational.<topic>`. Whether a dotted name survives native invocation is unverified.
2. The last message of a turn is one datom in the FinalResponse type, shown as a Type ethos: the object, then the vector of private types that fill it. Load `operational-final-response`. Open: on the wire a struct has no head, so the marked block needs the object to be a variant; the living has not ruled.
3. Machine messages are datom with a variant head; the living's words are plain text and are always logged. The Message Nexus runs on this machine and carries `Submit`, `SubmitStamped`, `QueryInbox`, `AssignAgentIdentity`, `BindAgentEndpoint`. Every message today still goes through the hacky messenger as plain text into panes, which is how the living's words and a machine reply landed in one pane. The living wants the switch now.
4. The transcript is the log and the database. A flow spawned from inside a Claude session inherits the child-session marker and writes no transcript; the launch must carry the force-persistence variable. Never refresh a flow before its transcript is safe.
5. Restart flows with a fat prompt, always; composed by `tools/compose-refresh-prompt`, never by hand in a response. A refreshed flow is named `<role> of <ancestor-id>`.
6. Quotas and burn rates are tracked by every flow; the anatomy proposal is the Quota anatomy artifact; nothing runs yet.
7. Reports to the living are low-noise: what we see, what we are not sure about, easy version first, linked as a web, with a situational report apart from the vision report.
8. Primary Next: vision and skills move first; flows are tried on it; migration follows only if they run well; flow data goes into psyche data, mind data, field data as the same files. Field Astra fixes everything; the field maintains and keeps the system going.

## Open questions the living has not answered

- Which of the two artifact webs stands, this flow's or the parallel flow's.
- The wire form of FinalResponse: variant carrying the struct, or bare struct.
- Whether every flow moves to the Message Nexus now and hm-send retires.
- What "the hull" is, the judge of subflow power.
- The sixteen questions in the situation report, and the seven in the quota anatomy.

## Hazards witnessed

- Automatic artifact comment replies give verdicts on the living's design questions. Do not treat them as yours; they are not.
- A parallel flow answered the same artifact comment within minutes and built its own web. Check the link row before building.
- Every Claude pane's terminal title reads "primary Psyche opus" whatever its model. Trust the Herder pane name, not the title.
- Text queued into a pane by Herder can merge with the living's own typing. A `/export` sent that way ran as text, not as a command.

## Where things are

- Raw psyche of this flow: `flows/b05237/vision/`, all files dated 09-18 and 09-19.
- Reports: `flows/b05237/reports/` — the vision picture, its easy and whole halves, the situation halves, the quota anatomy, `final-response.ethos`, this handoff.
- Artifacts: the six pages of the web, listed in the situation report's link row.
- Seats live in Herder: `field-astra-of-cf7791` (flow 1f96fc), `field-sol-of-3b1574` (c3e42e), `mind-astra-of-893603` (0ab019), `mind-sol-of-0ab019` (effa1b).

## What to do first

Read the living's last three messages of 09-19 in the raw records above. Confirm the old Fable's transcript export exists under `flows/b05237/exports/` before it is reaped. Then take up the Message Nexus switch with the field, since it is the thing the living asked for last.
