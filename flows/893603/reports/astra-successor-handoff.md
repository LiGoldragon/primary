# Astra successor handoff

Date: 2026-09-18. Scope: launch and readiness witness only; no integration-path edits.

The supported native launch used the existing app-server Unix WebSocket client (`WS` from `flows/024bc7/tools/codex_wake.py`): initialize, read remote-control status, `thread/start` with `/home/li/primary`, `gpt-6-astra`, and `ephemeral=false`, set the native name `Primary Astra successor · Flow893603`, then `turn/start` with effort `medium` and the ordered successor handoff. The launch returned native thread `01a0b53b-fb0f-7751-95be-e68e26a648b8` and a completed first turn.

The successor's actual native return witnessed Flow `e26a64`, model `gpt-6-astra`, medium effort, lane `/home/li/primary/flows/e26a64`, and persisted native rollout. It reported the working tree clean, preserved the predecessor and all existing sessions, and retained the read-only hardware trace, blocked consumer/Home gate, Message/producer source handoff, pending MS2130/lazy-composition decision, and Fable registration review. The native return was not a managed-pane handoff: read-only Herdr pane/process inspection shows `w1:p5`, terminal `term_65bc26ed43be77`, and its running Codex UI are still the existing Flow893603 owner. The separate app-server thread e26a64 is therefore not proven attached to that pane. The attempted `hm-register e26a64 psyche-mind-astra-refresh --session messaging-build` created an alias record on the existing pane; it must not be used for routing e26a64 messages. No supported managed attachment of the same native e26a64 thread to a new pane was found, and no duplicate successor was launched.

A distinct Herdr pane `w1:pH` was allocated for a same-thread attachment attempt only after the e26a64 turn had completed. `herdr agent start ... resume 01a0b53b-fb0f-7751-95be-e68e26a648b8` failed at TUI bootstrap with `thread ... already has an active writer`; the pane returned to a shell. This is the precise blocker: the app-server thread remains writer-owned even though its first turn completed, and the supported launcher could not attach it to the new pane. The existing Flow893603 pane and session were not touched.

The app-server protocol exposes `thread/unsubscribe`, but a fresh lifecycle client received `status: notSubscribed`; `thread/loaded/list` continued to list e26a64. No supported thread-release or writer-detach method is exposed in the client request surface. I did not archive, delete, interrupt, kill, restart, or override the writer. The messenger CLI exposes only `hm-register`; it has no unregister/quarantine operation. The erroneous e26a64 mapping therefore remains identified as invalid and unrouted, while the valid 893603 mapping and all other registrations remain preserved.

The field reaper was already present and was not duplicated: Flow `33ba2b`, name `field-reaper`, Herdr pane `w1:pG`, terminal `term_65bc3f245852c15`, gpt-5.6-sol medium, interactive-ready. Its registration and native return are owned by `message_receipts`.

The current future-Claude requirement remains unfinished. A future authoritative launcher must set `CLAUDE_CODE_FORCE_SESSION_PERSISTENCE=1` and pass `--remote-control` at creation, then witness an emitted remote endpoint or registered remote status before claiming remote accessibility. No current authoritative launcher was identified here, and no Claude session was launched.

The living's requested latest actual Opus 4.6 Flow was subsequently resolved by read-only native correlation: Flow `b05237`, native Claude session `b052375f-6516-4ad5-900b-68cb8ee2a466`, Herdr pane `w4:p1`, terminal `term_65bc28c7fc9a59`, and rendered/model evidence `claude-opus-4-6[1m]` at medium. The raw living quotes and the Claude persistence/tier/Subflows corrections were submitted once with `FLOW_ID=893603` through the supported Herdr messenger. The recipient visibly acknowledged receipt, logged the energy tiers, and returned: “Acknowledged. Flow b05237 (Opus 4.6) received the relay. Logged the energy tiers: Luna ultra-low, Terra low, Sol/medium reaper exception. Field reaper 33ba2b on Sol/medium noted — no duplicate launched. STT correction (Zapflows=Subflows) retained.” This is a native recipient reply, not merely transport submission.

No hardware approver was assigned. The consumer gate remains blocked; no activation, service, store, or integration operation was performed.
