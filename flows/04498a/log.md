# Flow log

Remembered: be2534 — depth 1

- The remembered flow diagnosed, but did not implement, the Codex model-configuration repair.
- Its evidence reported an authored Terra/high root, an already-authored Luna/xhigh global child default, force-managed activation paths, persistent app-server configuration retention, and no witnessed consumer for `non-orchestrator.config.toml`.
- The remembered flow contained no psyche record. Its last response carried the living's instruction to re-witness, implement through authored CriomOS-home, deploy through Lojix, restart the declaratively managed app-server, and verify fresh root/child/root sessions.

The current realization is to make Sol/xhigh the root default, Luna/xhigh the ordinary and exploration child default, retain Terra only for explicitly selected implementation work, remove stale orchestrator configuration and the dead non-orchestrator artifact/check, preserve Browser and Chrome hooks, deploy, and verify the live sequence.

Correction evidence: the earlier psyche statement mentioning Terra was scoped to report writing and later explicitly narrowed; it did not authorize a persistent root or general-child default. The current typed instruction is the authority for this repair.
