---
name: subagent-session-workflow
description: Shim to the canonical worker operating protocol for intent-led orchestration subagents.
---

# Subagent Session Workflow

The canonical worker protocol lives at `skills/subagent-session-workflow.md` in
the primary workspace. Read that file and follow it for the startup envelope,
lane choice, orchestration claims, worktree handling, verification, return
schema, and default commit/push policy.
