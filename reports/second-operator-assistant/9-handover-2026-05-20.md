## 9 — handover, 2026-05-20

**Lane:** second-operator-assistant
**Date:** 2026-05-20

Coverage of this lane after the heavy context-maintenance sweep that
folded /143, /246-v4, /248, /249 into the standing reports and
retired the two reports whose substance had migrated.

## What landed

Two reports remain in this lane after the sweep, both load-bearing:

- **`7-signal-persona-migration-deeper-holes.md`** — five
  findings about signal-persona's contract-local-verb migration,
  led by Finding 1 (the `signal-frame` macro's
  `validate_record_head_uniqueness` check is incorrect for the
  case-1 variant-wrapped encoding the macro actually emits). The
  report's §3 cascade-sequencing argument is now superseded by
  /246-v4 + /143; the signal-persona-specific Findings 1-4 remain
  unfixed in code and are not blocking the pilot.
- **`8-141-migration-coordination-audit.md`** — v4-aligned
  coordination map for the in-flight /141 / /246-v4 Package 1
  implementation. Names the `Lowering::Command` design extension
  that the implementation reached organically and that v4
  subsequently adopted as Layer 2 of the three-layer model.
  Lists the five v4 refinements the in-flight implementation
  still needs to absorb (§2.6).

Two reports retired during this sweep (substance migrated):

- **`5-nota-three-case-codec-implementation.md`** retired.
  Substance lives in code at `nota-codec` commit `bdd3a61e` (the
  three-case PascalCase codec rewrite, primary-hj63). The pending
  consumer wire-form cascade items it listed (persona-router,
  persona-mind, persona-message, persona-introspect, etc.) defer
  past /143's pivot to "smallest convergence bundle + pilot
  end-to-end."
- **`6-signal-persona-contract-local-verbs-migration.md`**
  retired. Substance distributed: the rename table is in
  `signal-persona/ARCHITECTURE.md` Migration history (commit
  `0b8adc28`); the 10 design problems sharpened into report 7's
  five findings; the reply-naming follow-up codified in
  `skills/contract-repo.md` §"Reply discipline" (commit
  `a7f3a0ee`).

Intent records committed during this lane's work:

- `intent/workspace.nota` 2026-05-19T23:00Z — `--max-jobs 0` Nix
  invocation discipline.
- `intent/workspace.nota` 2026-05-20T00:30:00Z — psyche directive
  authorising /141 as the migration target.

## What's open

Listed in the order the next operator session should pick them up,
following /143's "smallest convergence bundle + pilot" pivot:

1. **Operator-assistant lane is mid-flight on /141 Package 1** in
   a separate worktree at
   `/home/li/wt/.../signal-frame-executor-report-141`. Their
   lock is still held (per `orchestrate/operator-assistant.lock`).
   The work needs realignment to /246-v4 before commit — see
   report 8 §2.6 for the five deltas:
   1. `OperationPlan<Command>` + `BatchPlan<Command>` replace
      `Vec<Command>`.
   2. `Lowering::ComponentEffect` joins as a fourth associated
      type.
   3. `AcceptedOutcome` splits into `Committed` / `OperationAborted` /
      `BatchAborted`; three new enums (`BatchFailureReason`,
      `RetryClassification`, `CommitStatus`) in `signal-frame/src/reply.rs`.
   4. Engine failure produces `Reply::Accepted` with
      `BatchAborted`, NOT kernel `Reply::Rejected`. This reverses
      /141's engine-rejection-as-kernel-rejection per /142's logic
      probe. The /141-named test
      `kernel_rejection_does_not_carry_contract_reply` needs
      renaming/reshaping (suggested:
      `engine_failure_produces_batch_aborted_not_kernel_rejection`).
   5. Observability verbs standardised to `Tap`/`Untap` — the
      signal-frame macro work at `/git/.../signal-frame onxlynko`
      needs retargeting before commit; the contract-author-named
      `open Verb(Filter); close Verb;` grammar from /141 is
      RETIRED per psyche affirmation 2026-05-20T02:00Z.
2. **`signal-sema` becomes payloadless** per /246-v4 §0.5 + /248
   §1. The six variants `Assert | Mutate | Retract | Match |
   Subscribe | Validate` lose their payloads; `SemaOperation`
   becomes pure classification for Layer 3 observation. Tests
   that exercised payload-bearing `SemaOperation` need updating.
3. **`CommandExecutor` trait** per /248 §1: each daemon impls
   `CommandExecutor` for its own Command type. New trait in
   `signal-executor`.
4. **Package 3 — projection bridge** unblocks. v4 §3 lands the
   design as the `ObservedLowering: Lowering` extension trait +
   `ObserverFanout<OperationEvent, EffectEvent>` primitive.
   Operator/142's logic probe at
   `/tmp/signal-frame-executor-246-probe/model.rs` demonstrated
   it compiles without crate-boundary cycles. Whichever operator
   lane picks up Package 3 implements it from /246-v4 §3 + /248
   §1 directly.
5. **The pilot**: drive `repository-ledger` end-to-end per /143's
   pivot. Adopt the v4 `observable` block in
   `signal-repository-ledger`; implement `Lowering`,
   `CommandExecutor`, `ObservedLowering` in `repository-ledger`;
   add the one live test exercising operation-receive →
   command-execute → effect-emit → observer-subscribe → typed
   reply. Owner: operator + signal-repository-ledger maintainer.
6. **Persona daemon cascade waits.** Once the pilot validates
   the pattern, signal-persona-{mind, router, message, harness,
   terminal} cascade onto the settled v4 trait shapes. Report 7
   Findings 1-4 (signal-persona-specific structural cleanup) get
   addressed during that cascade.

## Side notes

- *note:* The `signal-frame` macro's
  `validate_record_head_uniqueness` check (report 7 Finding 1) is
  the root cause of the `ComponentStartup`/`ComponentShutdown`
  scar tissue in signal-persona. Fix is a five-line deletion in
  `signal-frame/macros/src/validate.rs`. Not blocking the pilot
  but blocking any future contract that has two operations
  sharing a payload type (`Start`/`Stop(ComponentName)`,
  `Mount`/`Unmount(Path)`, `Open`/`Close(FileDescriptor)`).
- *possibly useful:* Three lanes touched the /141 work in
  parallel (this lane, operator-assistant, main-worktree
  operator). When the working copy is shared (`/home/li/primary`
  is one jj working copy across all lanes), uncommitted edits
  from multiple lanes commingle in @. The pattern used during
  this session: `jj split <file>` to peel off your contribution,
  then describe it cleanly. Worth a skill if it recurs.
- *undecided:* /249 gap 4 asks whether /246-v4's mandatory
  `Tap`/`Untap` IS the universal observer-hook the psyche
  intended (`intent/component-shape.nota` 2026-05-19T20:00Z) or a
  different mechanism. The two phrases describe similar
  surfaces; the psyche has not been asked. If different, /246-v4
  needs another revision; if same, /249 gap 4 closes.
- *possibly useful:* The `Lowering::Command` extension that the
  in-flight implementation reached organically (before /246-v4
  affirmed it) suggests the workspace's "Layer 2 — Component
  Command" model has strong type-system pull. Daemons that try
  to lower directly to `Vec<SemaOperation>` hit the
  type-system-lie immediately because `SemaOperation` is
  payloadless. Worth a sentence in `skills/contract-repo.md`
  noting that "lowering returns typed Component Commands, not
  bare Sema classes" once v4 lands in code.

## Next-session targets

Pickup points for the next conversation in this lane:

- **Watch for /141 Package 1 commits.** When operator-assistant
  lane (or main-worktree operator) commits, audit against report
  8 §2.6's five v4 deltas. If any commits land /141 literally
  (without v4 alignment), file a follow-up note in report 8.
- **Watch for the pilot work** in `signal-repository-ledger` /
  `repository-ledger`. The first time `Lowering` lands in
  `repository-ledger`, validate it matches /246-v4 §1 + /248 §1.
- **Watch for `signal-sema` payload-stripping.** When `Assert |
  Mutate | Retract | …` variants lose their payloads, audit
  consumers for residual payload assumptions. The pattern is in
  /248 §1.
- **Don't touch signal-persona yet.** Report 7's findings are
  load-bearing but the pilot pivot says infrastructure first.
  Re-engage signal-persona when persona-spirit-daemon work
  begins after the pilot.

## See also

- `reports/designer/246-v4-bundled-fix-deep-design-with-examples.md` — the authoritative implementation-ready spec; absorbed /140-/143's operator analysis chain that drove v4's refinements.
- `reports/designer/248-three-layer-changes-for-operators.md` — per-crate operator change list. Pair with /246-v4.
- `reports/designer/249-component-intent-gap-analysis.md` — adjacent intent gaps; gap 4 + gap 5 intersect with this work.
- `reports/operator-assistant/159-signal-convergence-after-143-248-249.md` — post-sweep operator-side consolidation of the pivot (the "convergence + pilot" message that lived in operator/143 before it retired).
- `reports/second-operator-assistant/7-signal-persona-migration-deeper-holes.md` — sibling report in this lane; signal-persona-specific structural findings deferred until after the pilot.
- `reports/second-operator-assistant/8-141-migration-coordination-audit.md` — sibling report in this lane; coordination map for the in-flight Package 1 work, with §2.6 naming the five v4 deltas the implementation still needs to absorb.
- `skills/context-maintenance.md` — the discipline this handover follows.
- `intent/workspace.nota` 2026-05-19T23:00Z and 2026-05-20T00:30:00Z — the two psyche records from this lane's work.

*Note on dangling cites.* Reports /7 and /8 in this lane still cite /140, /141, /142, /143, /244, /245, /247 by path — those were retired during the concurrent 2026-05-20 context-maintenance sweep across lanes. Their substance lives in /246-v4 + /248 + /249 + /159. A future sweep in this lane should refresh those citations.
