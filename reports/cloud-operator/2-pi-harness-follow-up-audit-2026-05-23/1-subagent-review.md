# 1 — Subagent review: Pi harness follow-up

*Kind: Audit · Topic: pi-harness-follow-up · 2026-05-23*

## Review

- Correct: The `f2e9c929` slice described by `reports/cluster-operator/7-pi-harness-follow-up-after-third-designer-21-2026-05-23.md` mostly matches the third-designer follow-up request. The report closes the hard theme gap from `reports/third-designer/21-audit-cluster-operator-6-pi-harness-2026-05-22.md:49-66`: `theme-switcher.ts` reads the Chroma mode file and applies `criomos-{dark,light}` through Pi's UI (`/git/github.com/LiGoldragon/CriomOS-home/packages/pi-criomos/src/extensions/theme-switcher.ts:8-10`, `:30-47`, `:77-92`). It also honestly keeps live mid-stream palette flipping as an unproven remaining gap (`reports/cluster-operator/7-pi-harness-follow-up-after-third-designer-21-2026-05-23.md:91-95`).

- Correct: The Nix/declarative packaging shape is good in the reported revision: `pi-criomos` emits Pi package metadata listing the themes and both extensions (`/git/github.com/LiGoldragon/CriomOS-home/packages/pi-criomos/default.nix:16-29`) and installs the extension sources into the derivation (`:31-34`). Home Manager exposes stable package paths and Pi settings names home-relative packages, not store paths (`/git/github.com/LiGoldragon/CriomOS-home/modules/home/profiles/min/pi-models.nix:76-80`, `:86-91`), matching the documented invariant in `docs/pi-extensions.md:3-18`.

- Correct: The report does not try to treat this v1 Home Manager profile as a finished `persona-pi` triad. That matches `reports/third-designer/20-pi-as-codex-replacement-design-2026-05-22.md:6-18` (v1 has no new signal contract, daemon, or triad) and the component-triad standard for the eventual `persona-pi` shape (`skills/component-triad.md:73-85`, `:241-273`, `:286-327`). The bead note about Pi headless RPC is directionally right; `reports/designer/281-headless-pi-research.md:82-103` confirms RPC mode exists, while `reports/designer/266-persona-pi-triad-design.md:15-30` keeps the future daemon/signal/policy split.

- Fixed: None. This was review-only; no source/project files were modified. The configured report artifact is the only write.

- Blocker: The current `CriomOS-home` checkout is not the report's `f2e9c929` state, so the report is stale as a current-state artifact. `jj status` shows an uncommitted working copy with `docs/pi-extensions.md`, `packages/pi-criomos/src/extensions/operator-safety.ts`, and `result`/`result-1` changed; `jj bookmark list` shows local `main` at `e7e33210` (`home: Pi yolo mode and flake-input extensions`) and `main@git` at `2701516d` (`home: harden Pi extension tests and safety guard`), both after `f2e9c929`. The local `main` revision removes `operator-safety.ts` from the package metadata (`packages/pi-criomos/default.nix` at `e7e33210:21-25`) and documents the profile as "YOLO-mode" without mutation-confirmation gates (`docs/pi-extensions.md` at `e7e33210:134-136`). Recommended fix: before any deploy or final synthesis, choose the intended revision, clean or split the dirty worktree (including `result*` symlinks), and update the report to say whether `f2e9c929` was superseded by the later safety-hardening or by the later YOLO-mode direction.

- Major: The operator-safety policy in the reported revision is narrower than the report's prose implies. At `f2e9c929`, the `tool_call` handler confirms destructive `bash` commands, but protected-path and dirty-repository checks apply only to Pi `write`/`edit` tool calls (`packages/pi-criomos/src/extensions/operator-safety.ts` at `f2e9c929:164-174`). A non-matching `bash` mutation such as a redirect, `tee`, `python -c`, or `sed -i` can still write a protected path or dirty repository without the protected/dirty confirmation. Recommended fix: either narrow the report wording to "write/edit tool calls" and leave bash mutation as a stated gap, or add a fail-closed/confirming bash-mutation policy before calling the safety slice complete.

- Major: The sub-agent default stance is prompt-only, not an enforceable authorization gate. The workspace hard override requires subagents only when the psyche explicitly asks (`AGENTS.md:205-215`). The reported implementation appends a system-prompt instruction (`operator-safety.ts` at `f2e9c929:153-160`) but does not inspect or block subagent tool calls in the `tool_call` handler (`:164-177`). That does not fully close third-designer's request for a concrete authorization setting or config (`reports/third-designer/21-audit-cluster-operator-6-pi-harness-2026-05-22.md:86-99`). Recommended fix: if Stance A still stands, add a real Pi subagent tool/config gate with confirm/block behavior; if the later YOLO-mode commit reflects new psyche intent, record/manifest that supersession and update this report's safety claims.

- Major: Origin/deploy validation is not yet robust against the exact stale-flake hazard the report observed. The report says GitHub rate limiting caused Nix to use a cached `github:LiGoldragon/CriomOS-home/main` and then corrected the live profile from `path:/git/github.com/LiGoldragon/CriomOS-home` (`reports/cluster-operator/7-pi-harness-follow-up-after-third-designer-21-2026-05-23.md:56-63`). CriomOS process requires push before build and building from origin with `--refresh` before treating package changes as verified (`/git/github.com/LiGoldragon/CriomOS/AGENTS.md:22-25`; `/git/github.com/LiGoldragon/CriomOS-home/skills.md:141-142`). Recommended fix: add an authenticated/refreshing origin build or a documented local-path-only smoke boundary; for FullOS propagation, update the downstream CriomOS lock as part of the change (`/git/github.com/LiGoldragon/CriomOS-home/skills.md:125-129`).

- Minor: The `f2e9c929` validation relied on manual `pi -e ... --list-models` extension-load probes (`reports/cluster-operator/7-pi-harness-follow-up-after-third-designer-21-2026-05-23.md:40-50`). Workspace testing discipline says recurring manual commands are not review evidence until they are versioned as named flake outputs (`skills/testing.md:23-33`). A later check does add a Nix import witness (`/git/github.com/LiGoldragon/CriomOS-home/checks/pi-criomos-extension-load/default.nix:54-59`), which is the right direction. Recommended fix: keep that check, and add targeted witnesses for no-UI blocking, protected-path confirmation, dirty-repo behavior if retained, and theme-switch import/runtime behavior.

- Minor: The target report is a new report without the current report-kind filename/header shape. The filename lacks the closed-set kind required by `skills/reporting.md:369-382`, and the body has no metadata line after the title as required by `skills/reporting.md:544-565`. Recommended fix for the next revision or successor: use a kinded path such as `7-handover-pi-harness-follow-up-2026-05-23.md` or `7-audit-pi-harness-follow-up-2026-05-23.md` and add the `Kind · Topic · Date` metadata line.

- Note: `/home/li/primary/plan.md` and `/home/li/primary/progress.md` were requested as inputs but were not present. I used the target report, the meta-report frame, workspace baseline docs, third-designer reports, and the actual `CriomOS-home` files/revisions instead.
