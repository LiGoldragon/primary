# Frame and Method

## Prompt

The psyche asked pi-operator to review and audit `reports/cluster-operator/7-pi-harness-follow-up-after-third-designer-21-2026-05-23.md` with a subagent against good patterns, intent, and architecture standards. The psyche then asked whether the reloaded Pi session can edit files in dirty git repositories.

## Dirty-repository edit probe

The reloaded Pi session successfully edited `reports/pi-operator/1-pi-operator-safety-dirty-prompt-handoff.md` while `/home/li/primary` already had uncommitted changes from multiple lanes. The edit added a verification note to that report.

## Audit method

This meta-report uses one fresh-context reviewer subagent for read-only audit. The parent pi-operator also reads the target report and relevant Pi/CriomOS-home surfaces, then writes the final synthesis in this directory. The subagent is instructed not to modify project/source files; writing its configured report artifact is allowed.
