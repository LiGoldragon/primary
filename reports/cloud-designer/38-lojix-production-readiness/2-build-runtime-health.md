# build runtime health — Build + runtime health (compiles, tests pass, daemon starts, deps resolve)

cloud-designer, 2026-06-10. lojix main HEAD `7d66d2e`. Audit dimension `build-runtime-health`.

**Finder status:** `ready` → **Verified status (after adversarial re-check):** `ready`

## Net assessment (after re-reading current source)

Build + runtime health itself holds up under adversarial re-reading on HEAD 7d66d2e (confirmed git log): a real (non-incremental) `cargo build --offline` of the lojix crate succeeds (0.95s after touching src/lib.rs, BUILD_EXIT=0), both binaries are present and built today (target/debug/lojix 39MB, lojix-daemon 45MB, dated Jun 10), and all 11 non-ignored tests pass plus the cheap #[ignore]d owner-mode-refusal daemon-binary test. So health is NOT itself the production blocker — I confirm the prior dimension status of 'ready' for BUILD HEALTH narrowly. But the dimension surfaces three genuine production gates that all confirm against source: (1) the daemon VIOLATES the binary-only one-argument hard-override — src/bin/lojix-daemon.rs:17 calls nota_config::ConfigurationSource::from_argv().decode(), and the pinned nota-config commit bd9173a source.rs:99-119 actively NOTA-decodes InlineNota and reads .nota files; the daemon-binary tests prove it by spawning lojix-daemon with an inline NOTA arg '([...] 432 [...] 432 [...])'. (2) The dep graph floats on git branch=main deps and nota-codec.git now 404s (ls-remote exit 128) while still holding 1 live ref in Cargo.lock pinned to f761421c — the offline build survives ONLY because that commit sits in ~/.cargo/git/db; a cold-cache clone or any index-refresh/cargo update will fail to resolve. signal-core.git also 404s but has 0 lock refs, correctly a non-blocker. (3) The activate gate and in-memory-amnesia gates (owned by other dimensions) are confirmed real and, for activate, WORSE than stated: the activate command itself is broken (ssh node 'nix-env -p ... --set \"$CLOSURE\"' with $CLOSURE never assigned and the promised switch-to-configuration never invoked), so the admission rejection is the only thing preventing false live-set writes. Net: clean-reading code that is build-healthy on a warm cache but is NOT production-ready as a deploy tool — it cannot activate, loses all state on restart, parses NOTA in the daemon against the hard rule, and floats on a 404'd dep.

## Findings

### Crate builds clean offline; both binaries produced
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** cd /git/github.com/LiGoldragon/lojix/triad-port && cargo build --offline exits 0 (Finished dev profile, 4.44s incremental; rerun 0.07s). target/debug/lojix (39MB) and target/debug/lojix-daemon (45MB) both produced. cargo --version 1.95.0, edition 2024, rust-version 1.88. All 9 git deps (horizon-rs, nota-codec, nota-config, nota-derive, nota-next, schema-next, schema-rust-next, signal-frame, triad-runtime) are present in ~/.cargo/git/db, so the offline build needs no network on this machine.
- **Evidence:** /git/github.com/LiGoldragon/lojix/triad-port/Cargo.toml:1-48; build log /tmp/lojix_build.log (BUILD_EXIT=0); ls target/debug/lojix{,-daemon}
- **Fresh re-read:** HEAD confirmed 7d66d2ec98ea5fa6b29576c5e67eed80bc14ada0 (git log). Forced a real recompile: `touch src/lib.rs && cargo build --offline` -> 'Compiling lojix v0.3.0 ... Finished dev profile in 0.95s', BUILD_EXIT=0 (not just the prior agent's 0.07s incremental). target/debug/lojix (39775232 B) and target/debug/lojix-daemon (45664192 B) both present, mtime Jun 10 12:18. cargo 1.95.0, Cargo.toml:1-5 edition 2024 / rust-version 1.88.

### All non-ignored tests pass; cheap daemon-binary tests pass when run --ignored
*finder: ℹ️ info → verifier: **refined**, ℹ️ info; production gate: no*

- **State:** cargo test --offline: lib/bin unittests 0 tests; actor_native_runtime 1 passed; build_smoke 0 passed/6 ignored; engine_routing 8 passed/1 ignored; horizon_materialization_contract 2 passed. I then ran two #[ignore]d daemon-binary tests explicitly: permissive_owner_socket_mode_is_refused PASSED (daemon process exits non-zero on owner mode 0o666) and oversized_frame_is_bounded_and_daemon_survives PASSED in 9.78s (daemon binds both sockets, logs 'frame body is too large: 4294967295 bytes', stays up). No test failures anywhere.
- **Evidence:** cargo test --offline full output (TEST_RUN_EXIT=0); tests/build_smoke.rs:177-207 (owner-mode refusal), tests/build_smoke.rs:212-300 (oversized frame); src/daemon.rs:112-117 (validate_owner_socket_mode), src/daemon.rs:25-30 (MAXIMUM_REQUEST_FRAME_BYTES=8MiB, REQUEST_READ_TIMEOUT=10s)
- **Fresh re-read:** Re-ran cargo test --offline: lib/bin 0, actor_native_runtime 1, build_smoke 0/6 ignored, engine_routing 8/1 ignored, horizon_materialization_contract 2 — 11 non-ignored pass, zero failures (matches prior). permissive_owner_socket_mode_is_refused passes offline (0.00s, daemon refuses 0o666). REFINEMENT: the prior agent framed oversized_frame_is_bounded_and_daemon_survives as a 'cheap' offline test, but build_smoke.rs:277-285 shows it sends a REAL dune_system_deploy(Eval) after the hostile frame, which shells to `nix eval`. It passed offline in 9.32s only because `nix` is on PATH (/run/current-system/sw/bin/nix) and the fixture drvPath resolves locally — it is NOT a pure no-network test. The bound itself is real: log 'lojix-daemon[owner]: signal frame error: frame body is too large: 4294967295 bytes', cap MAXIMUM_REQUEST_FRAME_BYTES=8MiB wired into the codec at daemon.rs:144.

### Daemon VIOLATES one-argument binary-only rule: it parses inline NOTA and .nota config
*finder: 🟠 major → verifier: **confirmed**, 🟠 major; production gate: **yes***

- **State:** src/bin/lojix-daemon.rs:17 calls nota_config::ConfigurationSource::from_argv()?.decode()? with NO rkyv-only restriction. ConfigurationSource (nota-config) is a 3-variant enum {InlineNota(String), NotaFile(PathBuf), RkyvFile(PathBuf)} and decode() actively NOTA-decodes inline text and reads .nota files (source.rs). The hard-override states 'daemons accept only binary startup... reject inline NOTA and .nota paths — daemons never parse NOTA, configuration included.' This daemon does the opposite. The build_smoke daemon tests prove it: they spawn lojix-daemon with an INLINE NOTA arg '([ordinary] 432 [owner] 432 [state])' (build_smoke.rs:109-117) and the daemon decodes it — the test doc even says 'config decode (inline NOTA)'. By contrast the CLIENT correctly rejects inline NOTA (src/client.rs:73 -> Error::InlineNotaUnsupported), so the asymmetry is clearly unintended on the daemon side.
- **Evidence:** src/bin/lojix-daemon.rs:1-19; ~/.cargo/git/checkouts/nota-config-e9a03eb257fb0710/bd9173a/src/source.rs:13-24,97-113 (enum + decode); tests/build_smoke.rs:88-89,109-117; src/client.rs:72-73 (client correctly rejects)
- **Fresh re-read:** src/bin/lojix-daemon.rs:16-18 `nota_config::ConfigurationSource::from_argv()?.decode()?` with no rkyv-only guard. Read the pinned commit (Cargo.lock: nota-config bd9173a7): ~/.cargo/git/checkouts/nota-config-e9a03eb257fb0710/bd9173a/src/source.rs:15-25 is the 3-variant enum {InlineNota(String), NotaFile(PathBuf), RkyvFile(PathBuf)}; decode() at :99-119 NOTA-decodes InlineNota text (decode_nota_text, :115) and read_to_string's .nota files (:102-105). build_smoke.rs:109-119 spawns lojix-daemon with an inline NOTA arg and the daemon decodes it; doc at :88 says 'config decode (inline NOTA)'. The CLIENT correctly rejects inline NOTA (client.rs:73 -> Error::InlineNotaUnsupported), so the daemon asymmetry is the violation. Contradicts the hard-override 'daemons never parse NOTA, configuration included'.

### Floating git 'branch main' deps with a 404 repo (nota-codec); offline build survives only via cached pinned commit
*finder: 🟠 major → verifier: **confirmed**, 🟠 major; production gate: **yes***

- **State:** Cargo.toml pins five git deps on branch=main: horizon-rs, nota-codec, nota-config, nota-next (deps) and schema-rust-next (build-dep). git ls-remote https://github.com/LiGoldragon/nota-codec.git returns 'ERROR: Repository not found' (404) — the repo is retired/renamed on the remote. The build works ONLY because Cargo.lock pins nota-codec to commit f761421c and that commit is already in ~/.cargo/git/db/nota-codec-99150e6780a2590a (git cat-file -t confirms it). A fresh clone / clean CI machine running 'cargo build' (or any cargo op that refreshes the git index, e.g. cargo update or a lock-less build) will 404 and fail to resolve. signal-core.git ALSO 404s but is NO LONGER in the lock (0 references) — it was dropped, so it is not a current blocker. nota-config.git resolves fine (HEAD ba689f0). This matches the prior CriomOS-home closure failure and is a build-health time bomb for any consumer that does not have the exact cached commit.
- **Evidence:** Cargo.toml:27-40; git ls-remote nota-codec.git -> 'Repository not found'; git ls-remote signal-core.git -> 'Repository not found'; Cargo.lock:404 (nota-codec pinned f761421c) and grep -c signal-core Cargo.lock = 0; ~/.cargo/git/db/nota-codec-99150e6780a2590a git cat-file -t f761421c = commit
- **Staleness check:** Prior reports flagged nota-codec/signal-core 404; re-verified live today via git ls-remote. The lock-pin-survives-offline situation is machine-specific to a warm cargo cache; re-verify on a clean clone / CI before cutover.
- **Fresh re-read:** Cargo.toml:27-40 pins horizon-lib, nota-codec, nota-config, nota-next and (build-dep) schema-rust-next on branch=main. Live re-check of all 10 LiGoldragon git deps: nota-codec.git ls-remote exit 128 'Repository not found' AND has 1 live ref in Cargo.lock (pinned f761421c383121a04df761a444a3778d1c0b137d, which `git cat-file -t` confirms present in ~/.cargo/git/db/nota-codec-99150e6780a2590a) — real cold-cache/refresh time bomb. signal-core.git also 404s but has 0 lock refs (correctly not a current blocker). nota-config.git resolves (HEAD ba689f0). All other deps OK. Offline build survives only because the warm cargo cache holds the exact pinned commit; a fresh clone or `cargo update` 404s.

### build_smoke is NOT stubbed: drives real nix eval/build/copy/activate via tokio::process::Command
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** The build/eval smoke tests are real, not mocked — they shell out to nix through tokio::process::Command. schema_runtime.rs constructs real 'nix eval' (1703-1721), 'nix build' local+remote (1726-1763), 'nix copy' (1809-1811), and switch-to-configuration/activate (1821, 1839). The fixture is github:LiGoldragon/CriomOS-test-cluster#dune-nspawn-toplevel. These tests are #[ignore]d (network + slow nix) so they did NOT run in my offline pass — pass/fail of a real end-to-end deploy is therefore UNVERIFIED here, only that the code is wired to real nix and the engine pipeline drives it. The full-online proof daemon_binary_socket_roundtrip_eval (build_smoke.rs:90-172) likewise was not run (needs nix + network).
- **Evidence:** tests/build_smoke.rs:1-14,43-55,58-92; src/schema_runtime.rs:1703-1763 (NixCommand eval/build), 1809-1821 (copy/activate), 1839 (Command::new spawn)
- **Staleness check:** End-to-end eval/build/copy/activate success is unverified — the real-nix tests are #[ignore]d. Confirm by running 'cargo test --test build_smoke -- --ignored' on a nix+network host before cutover.
- **Fresh re-read:** schema_runtime.rs NixCommand builds real `nix eval --refresh --raw {attr}.drvPath` (:1713-1722), `nix build --no-link --print-out-paths` local+remote (:1738-1764), `nix copy --to ssh-ng://{node}` (:1807-1817), `ssh {node} 'nix-env ...'` activate (:1819-1829); NixCommand::run (:1838-1843) genuinely spawns via tokio::process::Command::new. `nix` and `ssh` are on PATH in this sandbox. The end-to-end #[ignore]d eval/build tests (build_smoke.rs:57-83, 90-172) still did not run a full deploy here, so real copy/activate success remains unverified — but copy/activate are unreachable anyway (gate below).

### Activate gate is real in source: every activating action is rejected at admission
*finder: 🟠 major → verifier: **confirmed**, 🔴 blocker; production gate: **yes***

- **State:** Confirms the self-stated ARCHITECTURE gate. schema_runtime.rs:525-541 unsupported_deploy_reason admits ONLY System Eval|Build and Home Build; every activating action (System Boot/Switch/Test/BootOnce, Home Profile/Activate) returns DeployRejectionReason::UnsupportedDeployAction with comment 'Activating actions remain rejected because copy/activate is not yet target-safe; accepting them would write false live-set state.' run_copy_closure/run_activate_generation ARE implemented (1313-1340) but unreachable from admission; even the build path finishes without copy/activate for non-activating actions (799-811). Test activating_deploy_is_rejected_until_activate_lands (engine_routing.rs:164-173) asserts Switch -> UnsupportedDeployAction and PASSES. A deploy tool that cannot activate cannot replace lojix-cli.
- **Evidence:** src/schema_runtime.rs:521-541, 799-811, 1313-1340; tests/engine_routing.rs:164-173
- **Fresh re-read:** schema_runtime.rs:525-541 unsupported_deploy_reason admits only System Eval|Build and Home Build; every activating action returns DeployRejectionReason::UnsupportedDeployAction. Build/Eval finish without copy/activate (:794-797, 805-810). Test activating_deploy_is_rejected_until_activate_lands (engine_routing.rs:163-173) asserts Switch -> UnsupportedDeployAction and passed in my run. SHARPENED to blocker: run_activate_generation (:1327-1340) is implemented but its command activate_system (:1819-1829) is itself BROKEN — `ssh {node} 'nix-env -p /nix/var/nix/profiles/system --set "$CLOSURE"'` where $CLOSURE is an unexpanded shell var never assigned the real closure path, and the promised switch-to-configuration (comment :1821) is never invoked. So the admission rejection is the only thing preventing false live-set writes. A deploy tool that cannot activate cannot replace lojix-cli.

### Daemon state is in-memory Mutex-backed; lost on restart (no durable backing)
*finder: 🟠 major → verifier: **confirmed**, 🔴 blocker; production gate: **yes***

- **State:** Confirms the self-stated INTENT gate. src/lib.rs:10-14 module doc: 'In-memory Mutex-backed state backs the SemaEngine for this build... Sema-engine / redb persistence is a noted follow-on.' Store is a single Mutex<StoreState> (lib.rs:176-184); the commit_sequence is 'modeled... for this in-memory build' (149). The live generation set, GC-roots tree, and deploy event log do not survive a daemon restart — directly contradicting the hard-override that 'On restart a daemon self-resumes from persisted SEMA state.' For a deploy orchestrator that owns which generation runs on every node, restart-amnesia is a safety blocker.
- **Evidence:** src/lib.rs:10-14, 121-191 (StoreState, Store: Mutex only, no redb/persist); daemon DaemonConfiguration carries a state_directory but Store::new() takes no path
- **Fresh re-read:** lib.rs:10-12 module doc 'In-memory Mutex-backed state... Sema-engine / redb persistence is a noted follow-on'; Store is Mutex<StoreState> only (lib.rs:179-182), commit_sequence 'modeled... for this in-memory build' (:149). daemon.rs:142 LojixRuntime::new constructs `Store::new()` with NO path — fresh empty state every startup, no load-from-disk. REFINEMENT of the prior note: state_directory_path IS used, but only as the horizon scratch `generated_inputs_directory` (schema_runtime.rs:67), NOT durable state backing — reinforcing zero persistence. grep for redb/persist/save/restore/from_disk in src finds only the 'follow-on' doc comments, no implementation. Live generation set, GC-roots, event log all vanish on restart, contradicting the hard-override 'On restart a daemon self-resumes from persisted SEMA state'. Restart-amnesia is a safety blocker for a tool that owns which generation runs on every node.
