# cutover and intent gates — Cutover discipline + intent/Spirit gates + prior-eval conclusions

cloud-designer, 2026-06-10. lojix main HEAD `7d66d2e`. Audit dimension `cutover-intent-gates`.

**Finder status:** `blocked` → **Verified status (after adversarial re-check):** `blocked`

## Net assessment (after re-reading current source)

All eight prior findings survive an adversarial re-read of current source at HEAD 7d66d2e — none refuted, two refined (both toward MORE blocked, not less). The new lojix stack remains NOT production-ready on the non-code gates: (1) zero consumers migrated — CriomOS/flake.lock and CriomOS-home/flake.lock both pin legacy lojix-cli at rev fc2ff0287f53, with zero references to the new lojix repo/daemon/socket/signal-lojix in either lock, flake.nix, or goldragon; (2) the daemon refuses every activating action (Switch/Boot/Test/BootOnce, Home Profile/Activate) at submit via the reject-guard, before any sema write, while the lojix-cli parity baseline fully activates real nodes (src/deploy.rs:165-194 + activate.rs SystemActivation/HomeActivation/switch-to-configuration); (3) state is in-memory (Store = Mutex<StoreState> with Vec tables + integer counters, no redb/Engine::open) so restart loses all deploy state. Two refinements that make the picture worse, not better: (a) the daemon DOES contain dead activate effect code (run_activate_generation at schema_runtime.rs:1327, run_copy_closure at 1313), but it is both unreachable (reject-guard) AND broken-as-written — activate_system (1819-1829) shells `nix-env --set "$CLOSURE"` over ssh against an unset $CLOSURE variable and never calls switch-to-configuration despite its own comment, so lifting the guard would not yield working activation; (b) the live build genuinely compiles (cargo build --offline → Finished, clean) and the report-29 hardening fixes + horizon-materialization verbs genuinely landed, so the stack can eval+build production closures — but build-only is explicitly not production-ready for a deploy tool. Recorded psyche intent (INTENT.md:16-17 legacy retires AFTER CriomOS migrates; INTENT.md:32-36 durable backing is the next storage cutover; report 26 open questions 1-2: parity bar undefined, in-memory-vs-durable undecided) contains no green-light for production cutover now — these are gates, not approvals. One caveat carries forward unchanged: intent is grounded in INTENT.md + report-cited record IDs, not a fresh live Spirit dump (the deployed spirit-daemon schema was reported out of sync); a live Observe should still confirm no newer record overrides this.

## Findings

### Zero consumers migrated: CriomOS and CriomOS-home flake.lock both pin lojix-cli and reference the new lojix daemon nowhere
*finder: 🔴 blocker → verifier: **confirmed**, 🔴 blocker; production gate: **yes***

- **State:** Both consumer flake.locks pin only legacy lojix-cli; neither flake.lock nor flake.nix references the new 'lojix' repo, the lojix daemon, its socket, or signal-lojix. Grep for new-lojix references in both repos returns empty. CriomOS flake.nix DESCRIBES content-addressed inputs 'from lojix' (system/pkgs/horizon/deployment) as design intent, but those overrides are written today by lojix-cli, not the new daemon. No CriomOS Nix module, systemd unit, or node config wires the new daemon.
- **Evidence:** CriomOS/flake.lock:1065-1086 and :498 pin lojix-cli rev fc2ff0287f53b841a5e180f96d5aac6a2d0295bb; CriomOS-home/flake.lock:739-759,:1328 same; grep for '"lojix"|repo: lojix|lojix-daemon|lojix.sock|signal-lojix' in both repos = empty; CriomOS/flake.nix:2,46,59,63,67 mentions 'lojix' only in prose
- **Fresh re-read:** CriomOS/flake.lock:1065-1086 pins repo lojix-cli rev fc2ff0287f53b841a5e180f96d5aac6a2d0295bb (also referenced :498,:677); CriomOS-home/flake.lock:739-759 same rev fc2ff0287f53 (also :397,:1328). grep for '"lojix"|lojix-daemon|lojix.sock|signal-lojix' in both flake.lock+flake.nix returned no hits. CriomOS/flake.nix mentions 'lojix' only in prose lines 2,46,59,63,67 (design intent describing content-addressed inputs, written today by lojix-cli not the new daemon).

### Consumer-migration path is fully unstarted; the cutover-discipline precondition (cover every consumer before retiring the old) is unmet
*finder: 🔴 blocker; production gate: **yes***

- **State:** Cutover discipline (active-repositories.md:115-120) requires building to feature parity, running both in parallel, switching consumers one at a time, then retiring the old, and forbids retiring before the replacement covers every consumer. The migration path requires CriomOS/CriomOS-home to: (1) add the new lojix flake input, (2) have the daemon (not lojix-cli) write the horizon/system/deployment/secrets override inputs, (3) repoint flake.lock to consume that projection. NONE of this is started in any consumer or in goldragon (goldragon has zero lojix references). Stack B per active-repositories.md:151-157 remains 'smoke-built, not yet deployed' and has 'not been cut over to any node' (line 130-131).
- **Evidence:** active-repositories.md:115-131,151-157; goldragon grep for lojix = empty; CriomOS/CriomOS-home node configs have no new-daemon wiring

### Daemon cannot activate; lojix-cli (the parity baseline) does — the replacement does not cover the activate surface it must replace
*finder: 🔴 blocker → verifier: **refined**, 🔴 blocker; production gate: **yes***

- **State:** unsupported_deploy_reason rejects every System action that is not Eval or Build and every Home mode that is not Build, returning DeployRejected(UnsupportedDeployAction) before any sema write. So Switch/Boot/Test/BootOnce and Home Profile/Activate are refused at submit. The lojix-cli production baseline implements full activation (src/activate.rs SystemActivation/HomeActivation, switch-to-configuration). A deploy tool that cannot activate cannot replace a deploy tool that does. The reject-guard has correctly NARROWED since report 28/29 (production System Build without build_attribute now enters the horizon-materialization effect pipeline per 7d66d2e), but activation is still gated exactly as ARCHITECTURE.md states.
- **Evidence:** lojix/triad-port/src/schema_runtime.rs:521-541 (reject-guard); test activating_deploy_is_rejected_until_activate_lands at engine_routing.rs:164-173 (Switch rejected); ARCHITECTURE.md:16 'Activating deploys still reject until copy/activate is target-safe'; lojix-cli/src/lib.rs:1 + src/build.rs:42-47 + src/deploy.rs:8 (lojix-cli activates)
- **Fresh re-read:** Reject-guard unsupported_deploy_reason (schema_runtime.rs:525-541) returns UnsupportedDeployAction for every System action that is not Eval/Build and every Home mode that is not Build; it is invoked at decide_meta_input (schema_runtime.rs:486-490) which returns DeployRejected BEFORE the sema write at :491-502. Test activating_deploy_is_rejected_until_activate_lands (engine_routing.rs:163-173) confirms Switch is rejected. REFINEMENT (worse, not better): the daemon DOES contain activate effect handlers — run_copy_closure (1313) and run_activate_generation (1327) — but they are both unreachable (guard) AND activate_system (1819-1829) is broken as written: it ssh-runs `nix-env -p /nix/var/nix/profiles/system --set "$CLOSURE"` against an unset $CLOSURE shell var and never calls switch-to-configuration despite its own comment. So even lifting the guard yields no working activation. Baseline lojix-cli genuinely activates: src/deploy.rs:8,165-194 (SystemActivation/HomeActivation, action.activates()), activate.rs:29-63 (switch-to-configuration).

### Live generation set is still in-memory; restart loses all deploy state — INTENT/ARCHITECTURE self-stated gate confirmed in source
*finder: 🔴 blocker; production gate: **yes***

- **State:** The Store is a single Mutex<StoreState> with in-memory event_log Vec and integer sequence counters; no Engine::open, no register_table, no redb, no state_file_path. The doc comment itself says 'In-memory for this build ... the sema-engine / redb durable backing is the noted follow-on.' A deploy daemon that forgets every generation on restart cannot run production (report 26 state-gap 3/4). INTENT.md:33-36 names durable backing 'the next storage cutover.'
- **Evidence:** lojix/triad-port/src/lib.rs:175-198 (Store = Mutex<StoreState>, in-memory comment), lib.rs:147-167 (counters), lib.rs:11 ('Sema-engine / redb ... follow-on'); INTENT.md:33-36; ARCHITECTURE.md:122-123,146

### No recorded psyche intent that lojix should go to production now; the recorded intent is a parity GATE and explicitly-open milestone questions
*finder: ℹ️ info; production gate: no*

- **State:** lojix/INTENT.md (the psyche-derived synthesis) states legacy lojix-cli 'retires AFTER CriomOS migrates to consume this daemon's projection' and durable backing 'remains the next storage cutover' — both gates, not green-lights. The charter records tvbn/fe2j ('reach parity then switch') are cited across the reports as the cutover discipline. Report 26 overview open-questions 1-2 record that the psyche has NOT enumerated the parity bar and has NOT decided whether in-memory-on-a-small-node-set is an acceptable first cutover (v5d4) vs durable-state-first. Intent 2alg (concurrency required) and k6w1 (shared primitive) were satisfied (reports 30/31). I could not run a live Spirit query: the deployed spirit-daemon's wire schema is out of sync with the checked-in spirit/schema/signal.schema (Query arity mismatch), so the deployed daemon rejects the schema-derived query shapes; intent is grounded in INTENT.md + the record IDs the reports cite. NOTE: this rests on a prior-report reading of intent records by ID, not a live Spirit dump.
- **Evidence:** lojix/INTENT.md:16-17,33-36; reports/cloud-designer/26-.../7-overview.md:441-454 (open questions 1-2, parity bar undefined, in-memory milestone undecided); reports 30:1-7 (intent 2alg), 31:5-8 (intent k6w1); live spirit query failed: 'Query to hold 3 root objects' arity mismatch vs deployed daemon
- **Staleness check:** Intent here is read from INTENT.md plus record IDs (tvbn/fe2j/jxi9/v5d4/2alg/k6w1) cited in week-old reports, not a live Spirit dump. A fresh Spirit Observe against the deployed daemon (with its actual schema) should confirm no newer record green-lights production cutover or redefines the parity bar.

### Report 29 (adversarial audit) fixes are genuinely landed in HEAD — verified against source, not just claimed
*finder: ℹ️ info; production gate: no*

- **State:** Commit f84a8cf ('apply adversarial-audit fixes — reject-guard, wire hardening, honest reasons (audit 29)') is in HEAD's ancestry and its fixes are present in current source: the C1 reject-guard (schema_runtime.rs:521-541 + test engine_routing.rs:164), R1 bounded 8 MiB frame cap (daemon.rs:25 MAXIMUM_REQUEST_FRAME_BYTES, :144 LengthPrefixedCodec::new(MaximumFrameLength::new(...))), R3 owner-mode floor refusing other-access (lib.rs:59-61 InsecureOwnerSocketMode, daemon.rs:107-114 mode & 0o007), R2 read timeout (daemon.rs:30 REQUEST_READ_TIMEOUT 10s, :228), and C2 substituter wiring (schema_runtime.rs:261-264,1296). HEAD (7d66d2e) and main are the same commit, so this is on main. Report 29's correctness/robustness conclusions are resolved; its END-TO-END verdict ('cannot deploy a real node', 'not production-ready') still holds for the activate + durable-state + migration gates above.
- **Evidence:** git log shows f84a8cf in HEAD ancestry; schema_runtime.rs:521-541,261-264,1296; daemon.rs:25,30,107-114,144,228; lib.rs:59-61; engine_routing.rs:164

### Report 26 (nexus completeness) gaps partially closed: production build+eval now goes through declared horizon-materialization, but most cutover gaps persist
*finder: 🟠 major; production gate: no*

- **State:** Report 26's largest hole (Nexus gap 1/2: artifact materialization + horizon projection had no declared verb) is now partially addressed: commit 7d66d2e adds a MaterializeHorizon command and FlakeInputOverride to the Nexus catalog, and production System/Home Build without build_attribute now enters that effect pipeline rather than being rejected (contract test horizon_materialization_contract.rs asserts the schema names HorizonMaterializationCommand + (MaterializeHorizon ...) + overrides). However report 26's still-open cutover blockers remain: no working activate (the A2 close), no durable persistence / keyed demoting live-set / rollback ring (state gaps 1-4), secrets effect, remote input staging, and the parity bar itself is undefined. The conclusion 'the current GREEN daemon cannot deploy a real node' has improved to 'can eval+build production closures but cannot activate or persist' — still not production-ready.
- **Evidence:** lojix/triad-port/tests/horizon_materialization_contract.rs (both tests); commit 7d66d2e stat (+516 schema_runtime.rs, new nexus.rs MaterializeHorizon); reports/cloud-designer/26-.../7-overview.md:88-105 (gap list), :376-405 (blocks-cutover ordering); engine_routing.rs production_deploy_without_build_attribute_enters_effect_pipeline
- **Staleness check:** Report 26 is dated 2026-06-05; its gap list predates 7d66d2e. The materialization/projection verbs now EXIST in the catalog, but whether they fully match lojix-cli's artifact.rs/project.rs/secrets behavior was not deep-verified in this dimension (code-correctness dimension owns that). Re-verify the materialization effect actually produces correct override inputs before trusting it as parity.

### active-repositories.md is itself stale on two load-bearing facts (does not block cutover, but misleads the cutover map)
*finder: 🟡 minor; production gate: no*

- **State:** The protocols cutover map states lojix-cli is pinned at rev 4c66b8a6fa55, but both consumer flake.locks actually pin fc2ff0287f53b841a5e180f96d5aac6a2d0295bb. It also cites 'reports/system-operator/134' for the smoke-built-zeus claim, but no report 134 exists in reports/system-operator/ (the directory jumps from earlier numbers to 139). The substantive Stack-B status ('smoke-built, not yet deployed, has not been cut over to any node') still holds and is corroborated by the flake.lock evidence above.
- **Evidence:** active-repositories.md:148 (claims 4c66b8a6fa55) vs CriomOS/flake.lock:1079 (fc2ff0287f53); active-repositories.md:129-130 cites reports/system-operator/134; ls reports/system-operator/ shows 139 as nearest, no 134
