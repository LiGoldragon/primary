# architecture discipline — Architecture & discipline conformance (actor density, one-arg daemon, sockets, typed errors)

cloud-designer, 2026-06-10. lojix main HEAD `7d66d2e`. Audit dimension `architecture-discipline`.

**Finder status:** `partial` → **Verified status (after adversarial re-check):** `partial`

## Net assessment (after re-reading current source)

Every one of the prior agent's 12 findings survives an adversarial re-read of current source at lojix main HEAD 7d66d2e (2026-06-10); none was stale, none was overstated, and the three blockers are real and confirmed against today's code, not week-old reports. The architecture is genuinely on the corrected (actor-native) side of the report-35/36 cloud divergence: the pinned triad-runtime 069ba8e defines RequestGate as a real kameo Actor (actor_runtime.rs:83,349) and ActorMultiListenerDaemon/ActorMultiConnectionRuntime (123,159,165); lojix consumes them (daemon.rs:16-20,90-95,149-173); every nix/ssh effect runs through tokio::process::Command (.output().await, schema_runtime.rs:1838-1843) on per-connection tasks so the actor mailbox never blocks; zero ZST namespaces, zero free functions outside bin main/run, zero unwrap/panic in src; typed thiserror per-crate Error (lib.rs:26-100) with unsafe_code=forbid; two authority-tiered sockets with an owner-mode guard (daemon.rs:74-117). Build is green offline against the pin (v0.3.0) and all 11 non-ignored tests pass — and the test suite itself bakes in the limitations (activating_deploy_is_rejected_until_activate_lands asserts UnsupportedDeployAction; watch_deployments only asserts a token, never a pushed event). Discipline is strong but NOT clean: the no-NOTA-in-daemon override is violated on the config-startup path (nota-config bd9173a decode() parses InlineNota and NotaFile un-gated, source.rs:99-131; lojix-daemon.rs:17 calls it), and the documented push-not-pull subscription bridge is entirely unbuilt (token-handshake stub at schema_runtime.rs:460-475, single-shot client at client.rs:91-115, zero SubscriptionRegistry/StreamingFrameBody hits in src). None of the three discipline items is itself the production gate; the real blockers — activate rejected AND the activate path itself addressing-broken ($CLOSURE is an unset shell var at schema_runtime.rs:1826), in-memory state lost on every restart (lib.rs:120-198, fresh Store per process at daemon.rs:140-146), and zero migrated consumers (CriomOS b63d922 + CriomOS-home 353b9a8 still pin lojix-cli only) — are confirmed-present and owned by the deploy/state/cutover dimensions. lojix is clean-reading, actor-correct, and demonstrably NOT production-ready as a lojix-cli replacement.

## Findings

### Actor density is genuine — lojix is the corrected shape report 35/36 demanded of cloud
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** The daemon consumes triad-runtime's actor-native multi-listener. The pinned triad-runtime (069ba8e, the Cargo.lock pin) defines RequestGate as a real kameo Actor (impl Actor for RequestGate with on_start and Message<AcquireRequestPermit>/Message<RequestGateStatusRequest> handlers, actor_runtime.rs) and ActorMultiListenerDaemon<Runtime> holding Vec<ActorRef<RequestGate>>, spawning each accepted connection on its own tokio task (actor_runtime.rs:732/927). lojix's LojixRuntime impls ActorMultiConnectionRuntime; handle_connection builds a per-request RequestWorker over a shared Arc<Store> (daemon.rs:149-173). Every public noun carries data (Daemon{configuration}, LojixRuntime{store,configuration,codec}, RequestWorker{...}, SchemaRuntime{store,configuration,active_deploy,active_operation}); grep found zero unit-struct ZST namespaces and zero free functions outside fn main/cfg(test). The actor_native_runtime test forbids the old sync markers (BoundedWorkers, spawn_blocking, std::process::Command, UnixStream, impl MultiListenerRuntime, .dispatch()) and requires the actor markers — it passes. This is exactly Proposal B from report 35/1, the migration cloud was urged to make (report 36/8 verdict: cloud still has zero actors).
- **Evidence:** daemon.rs:16-20,58-173; triad-runtime 069ba8e actor_runtime.rs:349-482,732; tests/actor_native_runtime.rs (passes); reports/cloud-designer/35-actor-divergence-forensics/1-forensic-verdict.md; reports/cloud-designer/36-cloud-actor-audit/8-overview.md
- **Fresh re-read:** Pinned triad-runtime 069ba8e (Cargo.lock): actor_runtime.rs:83 'pub struct RequestGate', :349 'impl Actor for RequestGate', :123 'pub trait ActorMultiConnectionRuntime', :159 ActorListenerSocket, :165 ActorMultiListenerDaemon. lojix imports them at daemon.rs:16-20 and impls ActorMultiConnectionRuntime for LojixRuntime (daemon.rs:149-173), building a per-request RequestWorker over Arc<Store>. Every public noun carries data (Daemon{configuration}, LojixRuntime{store,configuration,codec}, RequestWorker, SchemaRuntime{store,configuration,active_deploy,active_operation}). grep: zero unit-struct ZSTs, zero top-level free fns in the 4 core modules. Test daemon_uses_actor_native_listener_runtime PASSES (cargo test, 1 passed).

### Blocking-plane hazard (the report-35 nix-build concern) is correctly avoided
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** report 35 flagged that multi-minute nix builds must never block an actor mailbox. lojix runs every nix/ssh invocation through tokio::process::Command (.output().await, NixCommand::run at schema_runtime.rs:1838-1855) inside run_effect, on the per-connection task tokio::spawn'd by the actor runtime. The build therefore yields to the executor while the subprocess runs and never starves the RequestGate actor mailbox or sibling connections. Concurrency is bounded by RequestGate (a kameo actor) admitting permits up to MAXIMUM_CONCURRENT_REQUESTS=64. This is the blocking-plane-safe shape the skill prescribes — and the direct contrast with cloud's live global Arc<Mutex<Store>> held across a timeout-free flarectl subprocess (report 36/8).
- **Evidence:** schema_runtime.rs:1838-1855,1895-1911; daemon.rs:120-173; triad-runtime 069ba8e actor_runtime.rs:732
- **Fresh re-read:** schema_runtime.rs:1838-1843 NixCommand::run uses tokio::process::Command(...).output().await; run_effect (1895-1911) routes every EffectCommand (ResolveFlakeAuth/MaterializeHorizon/NixEval/NixBuild/CopyClosure/ActivateGeneration/PathInfoGc) through it. These run on the per-connection actor task (daemon.rs:161-172 handle_connection -> RequestWorker::serve). Concurrency capped by RequestConcurrencyLimit::new(MAXIMUM_CONCURRENT_REQUESTS=64) at daemon.rs:91,124. The subprocess await yields to the executor; the RequestGate actor mailbox is never starved.

### Two authority-tiered sockets with owner-socket-mode guard refusing other-access — conforms
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** run_async binds two ActorListenerSocket values tagged ListenerRole::Ordinary (peer-callable signal-lojix) and ListenerRole::Owner (privileged meta-signal-lojix Deploy/Pin/Unpin/Retire), each with_socket_mode from config (daemon.rs:77-85). validate_owner_socket_mode rejects any mode with the other-access bits set (mode & 0o007 != 0 -> Error::InsecureOwnerSocketMode) before binding (daemon.rs:112-117). handle_stream decodes the contract matching the arriving role, and ordinary_reply/meta_reply refuse a cross-tier output (Error::UnexpectedFrame, daemon.rs:304-316). An ignored build_smoke test permissive_owner_socket_mode_is_refused exercises the guard against the real binary.
- **Evidence:** daemon.rs:74-117,185-225,304-316; tests/build_smoke.rs:174-190
- **Fresh re-read:** daemon.rs:77-85 binds two ActorListenerSocket tagged ListenerRole::Ordinary/Owner each with_socket_mode from config; validate_owner_socket_mode (112-117) returns Error::InsecureOwnerSocketMode when mode & 0o007 != 0, called before bind at line 76. serve_ordinary/serve_owner decode the matching contract (194/213); ordinary_reply/meta_reply refuse a cross-tier output via Error::UnexpectedFrame (304-316). Ignored test permissive_owner_socket_mode_is_refused exists at build_smoke.rs:178-179 (spawns the real binary).

### Owner authority rests on socket file mode alone; the available SO_PEERCRED peer-credential gate is unused
*finder: 🟡 minor → verifier: **confirmed**, 🟡 minor; production gate: no*

- **State:** Owner-socket authority is enforced only by the unix file mode guard; there is no peer-credential check. The daemon comment admits this (daemon.rs:108-111: 'no peer-credential check yet — audit R3'). triad-runtime DOES expose a kernel-vouched SO_PEERCRED primitive (ConnectionContext/BindingSurface in triad-runtime process.rs:22-52, re-exported from lib.rs:42), but lojix imports none of it — grep for ConnectionContext/peer_cred/PEERCRED in lojix src returns only the apology comment. So any process that can open the owner socket path (subject to filesystem mode) is authorized; there is no uid/gid assertion on the privileged Deploy/Retire surface.
- **Evidence:** daemon.rs:108-117; triad-runtime/src/process.rs:22-52; triad-runtime/src/lib.rs:42; grep ConnectionContext in lojix src = 0 hits
- **Fresh re-read:** daemon.rs:108-111 comment admits 'no peer-credential check yet — audit R3'. triad-runtime 069ba8e EXPOSES the primitive: process.rs:22-56 ConnectionContext via rustix socket_peercred (kernel SO_PEERCRED), exported lib.rs:39, and is ALREADY constructed inside the actor runtime (actor_runtime.rs:711/791/909 ConnectionContext::from_tokio_stream). grep ConnectionContext/peer_cred/PEERCRED in lojix src = 0 hits. So the privileged Deploy/Retire surface authorizes any process that can open the socket path; no uid/gid assertion. (Mitigated for now since activate is rejected and no consumer connects — but the gap is real.)

### No-NOTA-in-daemon is violated on the config-startup path (daemon parses inline NOTA and .nota)
*finder: 🟠 major → verifier: **confirmed**, 🟠 major; production gate: no*

- **State:** The hard-override states daemons reject inline NOTA and .nota paths and never parse NOTA, configuration included — they accept only a pre-generated rkyv startup message. But lojix-daemon.rs:16-18 calls nota_config::ConfigurationSource::from_argv()?.decode(), and nota-config's decode() (source.rs:99-117) un-conditionally handles all three variants: InlineNota(text) and NotaFile(path) both route to decode_nota_text -> NotaSource::parse(), with NO feature gate. dispatch_single (source.rs:119-133) classifies a '(' -prefixed argv as InlineNota and a '.nota' path as NotaFile. So the production lojix-daemon binary, given an inline (...) arg or a foo.nota path, WILL parse NOTA at startup. (The request path is clean — the daemon decodes only rkyv signal frames and nota-text is default-off in lojix Cargo.toml; this finding is narrowly the config-startup path.) Fix is to make the daemon accept the RkyvFile variant only, or have nota-config gate NOTA decode behind a non-daemon feature.
- **Evidence:** src/bin/lojix-daemon.rs:16-18; nota-config/src/source.rs:99-133 (decode/dispatch_single, no feature gate); lojix Cargo.toml:22-24 (default=[], nota-text off)
- **Fresh re-read:** src/bin/lojix-daemon.rs:17 calls nota_config::ConfigurationSource::from_argv()?.decode(). nota-config pinned bd9173a (Cargo.lock): source.rs:99-113 decode() routes InlineNota(text) AND NotaFile(path) to decode_nota_text -> Decoder::new(text)/C::decode (115-119) with NO feature gate; dispatch_single (121-131) classifies '('-prefixed argv as InlineNota and a '.nota' extension as NotaFile. Only RkyvFile (107-111) skips NOTA parsing. So the production lojix-daemon binary, given '(DaemonConfiguration ...)' or config.nota, parses NOTA at startup — violating 'daemons reject inline NOTA and .nota paths and never parse NOTA, configuration included.' Fix: daemon must accept RkyvFile only, or nota-config must gate text decode behind a non-daemon feature. lojix Cargo.toml:23 default=[] keeps the request path clean (nota-text off); this is narrowly the startup-config path.

### Typed per-crate Error via thiserror, no stringly errors, no unwrap/panic in the daemon path — conforms
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** lib.rs:26-98 defines a single thiserror-derived Error enum with #[from] conversions for every dependency error (io, nota_codec, nota_config, triad_runtime Argument/Frame, both contract SignalFrameError via explicit From impls, horizon). Result<T> aliases it. grep for unwrap()/expect(/panic!/unreachable!/todo!/unimplemented! across hand-written src (excluding unwrap_or and tests) returns ZERO hits; store-lock poisoning maps to Error::StorePoisoned (lib.rs:191-193) rather than panicking. unsafe_code is forbidden crate-wide (Cargo.toml). The daemon never emits an EOF on internal invariant violation — it replies a typed rejection on the same tier (invariant_rejection, daemon.rs:275-302).
- **Evidence:** src/lib.rs:26-100,191-193; Cargo.toml lints unsafe_code=forbid; grep unwrap/panic in src = 0 (excl unwrap_or/tests); daemon.rs:97-105,275-302
- **Fresh re-read:** lib.rs:26-100 single thiserror Error enum with #[from] for io/nota_codec/nota_config/triad_runtime Argument+Frame/horizon, explicit From for both contract SignalFrameError (88-98); Result<T> alias. grep unwrap()/expect(/panic!/unreachable!/todo!/unimplemented! across src (excl unwrap_or/tests) = NONE FOUND. Store-lock poison -> Error::StorePoisoned (lib.rs:191-193). Cargo.toml:46 unsafe_code=forbid. Daemon replies a typed same-tier rejection on invariant violation (daemon.rs:275-302 invariant_rejection), never an EOF.

### Push-not-pull subscription bridge is documented but NOT implemented (token handshake stub)
*finder: 🟠 major → verifier: **confirmed**, 🟠 major; production gate: no*

- **State:** ARCHITECTURE.md (147-161) and INTENT.md (41-45) assert subscription events ride the acceptor outbound lane via StreamingFrameBody::SubscriptionEvent, the bridge is 'downstream of the commit', and 'Push, never poll.' The code does none of this: open_subscription (schema_runtime.rs:460-475) only mints a subscription_token and replies Watching; there is no SubscriptionRegistry, SubscriptionEventPublisher, or StreamingFrameBody anywhere in lojix src (grep = 0 hits, though triad-runtime exports those primitives). The CLI client does a single write_body then a single read_body and returns (client.rs:91-115) — it never holds the socket open to receive pushed events. So WatchDeployments/WatchCacheRetention return a token to a client that immediately disconnects; no deploy/retention event is ever delivered. The push bridge is unbuilt, so any consumer needing live deploy progress (a lojix-cli feature) has no equivalent yet.
- **Evidence:** schema_runtime.rs:453-481; src/client.rs:91-115; ARCHITECTURE.md:147-161; INTENT.md:41-45; grep SubscriptionRegistry/EventPublisher/StreamingFrameBody in lojix src = 0
- **Fresh re-read:** ARCHITECTURE.md:148-159 ('StreamingFrameBody::SubscriptionEvent', 'subscription bridge is downstream of the commit', 'Push, never poll') and INTENT.md:41 ('Subscribers register; the daemon pushes') assert a push bridge. Code: open_subscription (schema_runtime.rs:460-475) only mints next_subscription_token and replies Watching; close_subscription (477-481) replies Unwatched. grep SubscriptionRegistry/SubscriptionEventPublisher/StreamingFrameBody/SubscriptionEvent in lojix src = ZERO hits. Client does one write_body then one read_body and returns (client.rs:91-115) — never holds the socket open. Test watch_deployments_mints_subscription_token (engine_routing.rs:66-78) asserts only token==1, never a delivered event. So WatchDeployments returns a token to a client that immediately disconnects; no live deploy progress is ever delivered — a lojix-cli capability with no equivalent yet.

### CONFIRMED GATE: activating deploys still reject — daemon refuses Boot/Switch/Test/BootOnce and Home Profile/Activate
*finder: 🔴 blocker; production gate: **yes***

- **State:** The self-stated ARCHITECTURE.md gate ('Activating deploys still reject until copy/activate is target-safe', ARCHITECTURE.md:15-16) is enforced in source. unsupported_deploy_reason (schema_runtime.rs:525-541) returns UnsupportedDeployAction for every System action except Eval/Build, and for every Home mode except Build — i.e. all activating actions are rejected up front in decide_meta_input before any sema write (schema_runtime.rs:485-490). The activate path beneath it is also addressing-incomplete: run_activate_generation issues `ssh <node> nix-env -p .../system --set "$CLOSURE"` where $CLOSURE is an unset shell variable (NixCommand::activate_system, schema_runtime.rs:1819-1829), so even if the guard were lifted, activation would set the profile to an empty path. A deploy tool that cannot activate cannot replace lojix-cli — this is the primary production gate, owned by the deploy dimension but confirmed here against source.
- **Evidence:** schema_runtime.rs:521-541,485-490,1819-1829; ARCHITECTURE.md:15-16

### CONFIRMED GATE: live generation set is in-memory; daemon loses all state on restart
*finder: 🔴 blocker → verifier: **confirmed**, 🔴 blocker; production gate: **yes***

- **State:** Store wraps StoreState (the four SEMA tables + sequence counters) in a plain Mutex with Default-empty tables; there is no redb/sema-engine durability (lib.rs:120-198, comment 'In-memory for this build ... the sema-engine / redb durable backing is the noted follow-on'). LojixRuntime::new constructs a fresh Arc::new(Store::new()) per process start (daemon.rs:140-146), and the daemon does not persist or reload state. state_digest is faked as commit_sequence (schema_runtime.rs:425,432), so the database marker carries no real integrity. INTENT.md:32-36 confirms the live set is in-memory with redb backing as 'the next storage cutover'. A deploy orchestrator that forgets its live generation set, GC-roots retention, and event log on every restart cannot durably own production node state — the hard-override even requires the daemon to 'self-resume from persisted SEMA state' on restart, which is impossible here. Owned by the state-durability dimension; confirmed against source.
- **Evidence:** lib.rs:120-198; daemon.rs:140-146; schema_runtime.rs:422-434; INTENT.md:32-36
- **Fresh re-read:** Store wraps StoreState (four SEMA tables + 4 sequence counters) in a plain Mutex with Default-empty tables (lib.rs:120-198, doc 'In-memory for this build ... sema-engine / redb durable backing is the noted follow-on'). LojixRuntime::new builds a fresh Arc::new(Store::new()) every process start (daemon.rs:140-146); nothing persists or reloads. state_digest is faked as commit_sequence (schema_runtime.rs:425,432) so the DatabaseMarker carries no real integrity. INTENT.md:35-36 confirms 'in-memory ... durable redb/sema-engine backing remains the next storage cutover.' The hard-override requires a daemon to 'self-resume from persisted SEMA state' on restart — impossible here. An orchestrator that forgets its live set, GC-roots, and event log on every restart cannot durably own production node state.

### CONFIRMED GATE: zero consumers migrated — CriomOS and CriomOS-home still pin legacy lojix-cli
*finder: 🔴 blocker → verifier: **confirmed**, 🔴 blocker; production gate: **yes***

- **State:** Both production flakes pin the legacy monolith and reference the new lojix daemon nowhere. CriomOS/flake.nix describes consuming content-addressed inputs 'from lojix' but flake.lock pins github repo lojix-cli (CriomOS/flake.lock:1065-1084, node 'lojix-cli'). CriomOS-home/flake.nix:120-121 declares lojix-cli.url = github:LiGoldragon/lojix-cli and a checks/lojix-run check (line 323), with flake.lock pinning lojix-cli (CriomOS-home/flake.lock:739-757). grep for the new daemon (a 'lojix' input distinct from 'lojix-cli') in either flake = none. Migration is a prerequisite for cutover and is entirely unstarted, independent of code quality. Owned by the cutover dimension; confirmed against the live flake locks.
- **Evidence:** CriomOS/flake.nix:2,46-67 + flake.lock:1065-1084; CriomOS-home/flake.nix:120-121,323 + flake.lock:739-757
- **Fresh re-read:** CriomOS HEAD b63d922 (2026-06-10): flake.lock pins node 'lojix-cli' (lines 1065,1078,1084); flake.nix references only lojix-cli; grep for a 'lojix' input distinct from lojix-cli = NONE. CriomOS-home HEAD 353b9a8 (2026-06-10): flake.nix:120-121 lojix-cli.url=github:LiGoldragon/lojix-cli + checks/lojix-run (323); flake.lock pins lojix-cli (739,751,757); grep for new lojix input = NONE. Migration is entirely unstarted at fresh HEADs; cutover prerequisite unmet regardless of code quality.

### Cargo.lock lags triad-runtime main; lojix builds green against its pin but main renamed the actor API
*finder: 🟡 minor → verifier: **confirmed**, 🟡 minor; production gate: no*

- **State:** lojix Cargo.lock pins triad-runtime at 069ba8e (v0.6.0, 2026-06-08) which exports the Actor-prefixed primitives lojix imports (ActorMultiListenerDaemon, ActorListenerSocket, ActorMultiConnectionRuntime, ActorMultiListenerDaemonError). The local triad-runtime main has since advanced to 6ea8316 and RENAMED these to Async-prefixed (AsyncMultiListenerDaemon etc.) and renamed the DaemonConfiguration trait to BindingSurface (triad-runtime lib.rs:26-42; git log 6ea8316/ae2e817/f96bd32). cargo check --offline against the lock is green (verified), so this is lockfile lag, not a present build break — but the next triad-runtime bump on the dep's branch=main will force a rename sweep through daemon.rs imports, and the contract triple is also pinned by path (signal-lojix/meta-signal-lojix path deps). Normal pre-production churn given the no-backward-compat posture, noted so the operator rebase expects it.
- **Evidence:** lojix Cargo.lock (triad-runtime pin 069ba8e); triad-runtime/src/lib.rs:26-42 (current main, Async-prefixed); triad-runtime git log 6ea8316; cargo check --offline = BUILD_OK
- **Fresh re-read:** lojix Cargo.lock pins triad-runtime 069ba8e (v0.6.0) which exports the Actor-prefixed primitives lojix imports. triad-runtime main has advanced to 6ea8316 (2026-06-09) and RENAMED them Async-prefixed (lib.rs:28-30 AsyncMultiListenerDaemon/AsyncMultiConnectionRuntime/AsyncListenerSocket) and DaemonConfiguration -> BindingSurface (lib.rs:42). cargo check --offline against the lock is GREEN (verified, lojix v0.3.0, Finished). Lockfile lag, not a present break; the next branch=main bump forces a rename sweep through daemon.rs imports. Normal pre-production churn under no-backward-compat; noted for the operator rebase.
