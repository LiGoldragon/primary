# wire contract — Wire contract completeness + schema-emission freshness

cloud-designer, 2026-06-10. lojix main HEAD `7d66d2e`. Audit dimension `wire-contract-completeness`.

**Finder status:** `ready` → **Verified status (after adversarial re-check):** `ready`

## Net assessment (after re-reading current source)

On the wire-contract-and-emission axis there is NO production gate, and my fresh re-read on HEAD 7d66d2e (lojix), 0b8a9e3 (signal-lojix), 5cf4824 (meta-signal-lojix) confirms every claim. The signal-lojix / meta-signal-lojix contracts are genuinely wire-only (zero tokio/redb/sema-engine/triad-runtime/horizon in either dependency tree; the only "leak-grep" hits are DatabaseMarker, a wire-observable CommitSequence+StateDigest value the client reads back), fresh (both crates compile clean offline with the UPDATE env var unset via build.rs write_or_check; I forced build.rs reruns and the generated src/schema/lib.rs stayed byte-clean against the committed artifact), and fully wired (all 5 ordinary + 4 meta ops have non-wildcard dispatch arms in schema_runtime.rs; nexus.schema imports both contract roots wholesale so routing cannot drift; both binaries built today 2026-06-10 12:19). The vocabulary is a strict superset of the lojix-cli baseline (baseline LojixRequest has exactly 4 variants and no pin/unpin/retire/rollback verb; SystemAction and HomeMode match one-to-one). The two genuine non-gate items both reproduce exactly: (1) streaming Watch/Unwatch is a handshake stub — the daemon's serve_ordinary is strictly one-shot request/reply with no subscriber registry and no event-push path, so a subscriber gets one Watching token and no stream (minor observability gap, net-new vs baseline, not a parity regression); (2) the substituter wire shape moved node-name resolution from the daemon (baseline from_horizon_nodes) to the client (Vec<ExtraSubstituter{url,public_key}>), a real consumer-migration point. The activation BLOCK is real but is NOT in the wire — HEAD's reject-guard (unsupported_deploy_reason, schema_runtime.rs:525-541) still rejects every activating action (System Boot/Switch/Test/BootOnce, Home Profile/Activate) with UnsupportedDeployAction even after the "materialize horizon inputs for production eval" commit; that genuine production blocker is owned by the runtime/activation dimension, not this one. Net: this dimension is ready; the deploy tool's production-blocking gate lives elsewhere (daemon cannot activate, live set is in-memory, zero migrated consumers).

## Findings

### Generated contract artifacts are FRESH — build.rs write_or_check round-trip passes for both crates
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** Both contract crates build clean offline with the artifact-update env var unset, i.e. in check mode. build.rs (signal-lojix/triad-port/build.rs:30) calls .write_or_check("SIGNAL_LOJIX_UPDATE_SCHEMA_ARTIFACTS") with the expect message 'checked-in signal-lojix schema artifacts are fresh' — a stale generated file would fail the build. `cargo build --offline` succeeded for signal-lojix and meta-signal-lojix, so src/schema/lib.rs in each matches a fresh re-emit of schema/lib.schema. Commit timestamps corroborate: signal generated artifact refreshed 2026-06-07 (commit 0b8a9e3) AFTER its schema last changed 2026-06-05 (ffe791e); meta refreshed 2026-06-07 (5cf4824) after schema 2026-06-05 (b7ad1db).
- **Evidence:** signal-lojix/triad-port/build.rs:23-31; cargo build --offline both crates -> Finished; git log -1 on triad-port/src/schema/lib.rs vs triad-port/schema/lib.schema in both repos
- **Fresh re-read:** signal-lojix/triad-port/build.rs:30 calls .write_or_check("SIGNAL_LOJIX_UPDATE_SCHEMA_ARTIFACTS").expect("checked-in signal-lojix schema artifacts are fresh"); I forced reruns by touching build.rs in both crates — both recompiled (Compiling signal-lojix / meta-signal-lojix) and Finished with exit 0, env var unset = check mode, and git status --porcelain src/schema/lib.rs stayed EMPTY in both, proving the committed artifact is a byte-exact re-emit. Commit order corroborates: signal schema last changed ffe791e 2026-06-05, artifact refreshed 0b8a9e3 2026-06-07; meta schema b7ad1db 2026-06-05, artifact 5cf4824 2026-06-07.

### Contracts are WIRE-ONLY — no Nexus/SEMA/engine/daemon leak (report 37 rule satisfied)
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** Grep across both contract crates' src/ and schema/ for nexus|sema|engine|daemon|runner|effect|listener|tokio|redb|store finds only (a) explanatory comments and (b) DatabaseMarker, which is a legitimate wire-observable type (commit_sequence + state_digest the client reads back), NOT a SEMA-internal type. The Nexus roots, EffectCommand catalog, SEMA read/write unions, Store, and tokio all live in lojix/triad-port (the daemon), never in the contract crates. This is exactly the boundary report 37 §4 established after the psyche caught the prototype embedding Nexus/SEMA in signal-cloud/signal-upgrade contracts; the lojix triad does not repeat that violation.
- **Evidence:** grep nexus|sema|engine|daemon|runner|effect|listener|tokio|redb|store over signal-lojix/triad-port + meta-signal-lojix/triad-port src+schema (only comments + DatabaseMarker); reports/cloud-designer/37-...md:105-130 (wire-only correction + the prior signal-cloud/signal-upgrade violation)
- **Fresh re-read:** Fresh grep nexus|sema|engine|daemon|runner|effect|listener|tokio|redb|store over both crates' src+schema returns only DatabaseMarker (signal lib.schema:58, generated lib.rs:139/624) plus comment lines — no engine type. Stronger: cargo tree --offline for BOTH crates shows ZERO tokio/redb/sema-engine/triad-runtime/horizon anywhere in the dependency tree. DatabaseMarker is { CommitSequence StateDigest } — a wire-observable value the client reads back, not a SEMA-internal. The Nexus roots / SEMA unions / Store / tokio all live only in lojix/triad-port (nexus.schema:30-33 imports lojix:sema:* into the DAEMON, never the contract).

### Daemon handler surface matches the contract exactly — no orphan op, no orphan handler
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** All 5 ordinary ops (Query, WatchDeployments, WatchCacheRetention, Unwatch, CheckHostKeyMaterial) and all 4 meta ops (Deploy, Pin, Unpin, Retire) have dispatch arms in schema_runtime.rs (ordinary arms at 447-456, meta arms at 485-512). The matches are exhaustive Rust matches with no wildcard fallthrough on the op enum, so the successful compile of both binaries (built today, target/debug/lojix-daemon dated Jun 10 12:15) is a type-level proof that every contract Input variant is handled and no handler references a non-contract op. The Nexus schema imports both contract roots wholesale (nexus.schema:26-29), so the routing layer cannot drift from the contract.
- **Evidence:** lojix/triad-port/src/schema_runtime.rs:447-456 (ordinary), 485-512 (meta); lojix/triad-port/schema/nexus.schema:26-29 (SignalInput/SignalOutput = the two contract roots); target/debug/{lojix,lojix-daemon} built Jun 10 12:15
- **Fresh re-read:** Generated enums: ordinary Input has exactly 5 variants (Query/WatchDeployments/WatchCacheRetention/Unwatch/CheckHostKeyMaterial, signal lib.rs:399), meta Input exactly 4 (Deploy/Pin/Unpin/Retire). decide_ordinary_input (schema_runtime.rs:445-458) and decide_meta_input (483-519) match all of them with NO wildcard — the only _ => arms in the file are at 702/710/726/740 inside rejection-reason mapping helpers, not the dispatch. nexus.schema:26-29 imports both contract roots wholesale; SignalInput/Output (nexus.schema:49-50) = the two contract roots. Both binaries built today 2026-06-10 12:19, a type-level proof of exhaustiveness.

### Deploy vocabulary CAN express Build/Profile/Activate, System/Home, and pin/retire — exceeds lojix-cli surface
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** The contract fully expresses the mode/target matrix. meta DeployRequest is [System SystemDeployment | Home HomeDeployment]; SystemDeployment carries SystemAction (Eval/Build/Boot/Switch/Test/BootOnce, signal-lojix/triad-port/schema/lib.schema:55) and DeploymentKind (FullOs/OsOnly/HomeOnly, :54); HomeDeployment carries HomeMode (Build/Profile/Activate, meta-signal-lojix/triad-port/schema/lib.schema:74). Pin/Unpin/Retire requests + replies exist (meta lib.schema:57-58, 110-117). Rollback is modeled as GenerationSlot::Rollback (signal lib.schema:57), a read/observe state rather than a verb. The production baseline lojix-cli LojixRequest has only FOUR variants — FullOs, OsOnly, HomeOnly, CheckHostKeyMaterial (lojix-cli/src/request.rs:51-56) — and NO pin/unpin/retire/rollback verb at all, so the new contract is a superset of baseline vocabulary, not a gap. SystemAction and DeploymentKind match the baseline (lojix-cli/src/build.rs:10-22, request.rs:17-49) one-to-one.
- **Evidence:** meta-signal-lojix/triad-port/schema/lib.schema:54-58,74,87-117; signal-lojix/triad-port/schema/lib.schema:54-57; lojix-cli/src/request.rs:51-56 (4 variants only); lojix-cli/src/build.rs:10-22 (SystemAction parity)
- **Fresh re-read:** Contract: SystemAction Eval/Build/Boot/Switch/Test/BootOnce (signal lib.schema:55), DeploymentKind FullOs/OsOnly/HomeOnly (:54), HomeMode Build/Profile/Activate (meta lib.schema:74), Pin/Unpin/Retire requests+replies (meta lib.schema:57-58,110-117), Rollback modeled as GenerationSlot::Rollback (signal lib.schema:57). Baseline lojix-cli LojixRequest has exactly 4 variants — FullOs/OsOnly/HomeOnly/CheckHostKeyMaterial (request.rs:51-56) — and NO pin/unpin/retire/rollback verb. Baseline SystemAction (build.rs:10-22) and HomeMode Build/Profile/Activate (build.rs:39-43) match one-to-one. New contract is a strict superset.

### lojix triad-port path-deps resolve to the contract repos' main HEAD (no stale path-dep skew)
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** lojix/triad-port/Cargo.toml pins both contracts by path (signal-lojix = ../../signal-lojix/triad-port:32, meta-signal-lojix = ../../meta-signal-lojix/triad-port:31). Both contract repos' triad-port/ working trees are CLEAN vs their committed main HEADs (git status --porcelain triad-port/ empty in both), and those HEADs are the 2026-06-07 'refresh generated contract artifact' commits (signal 0b8a9e3, meta 5cf4824). So the path-dep the daemon compiles against IS the refreshed main contract — no divergence between what lojix builds against and what is committed on the contract mains.
- **Evidence:** lojix/triad-port/Cargo.toml:31-32; git status --porcelain triad-port/ clean in signal-lojix and meta-signal-lojix; git log -1 main of each = 0b8a9e3 / 5cf4824 (the 06-07 refresh commits)
- **Fresh re-read:** lojix/triad-port/Cargo.toml:31-32 pins meta-signal-lojix = ../../meta-signal-lojix/triad-port and signal-lojix = ../../signal-lojix/triad-port. git status --porcelain triad-port/ returns 0 dirty files in BOTH contract repos; HEADs are 0b8a9e3 (signal) and 5cf4824 (meta), the 2026-06-07 refresh commits. The daemon compiles against the exact committed main contracts.

### Streaming Watch/Unwatch is a HANDSHAKE STUB — daemon mints tokens but never pushes event frames
*finder: 🟡 minor → verifier: **confirmed**, 🟡 minor; production gate: no*

- **State:** The contract advertises WatchDeployments/WatchCacheRetention + a DeploymentPhaseEvent and CacheRetentionTransitionEvent payload, but per the schema's own day-one note (signal lib.schema:11-20, decision 2tfa) schema-next cannot yet emit a daemon-pushed event frame, so the two event payloads are namespace records reachable only by the handshake. The daemon honors this honestly but hollowly: open_subscription (schema_runtime.rs:460-475) mints a SubscriptionToken + current commit_sequence and returns Watching; close_subscription (:477-481) echoes Unwatched. There is NO code path that ever pushes a phase/retention event to a held subscription. So live observability of an in-flight deploy is not functional — a consumer can subscribe but receives no stream. This is net-new vs lojix-cli (a one-shot CLI with no streaming), so it is not a parity regression, but it is an incomplete advertised surface and an observability gap for a production orchestrator.
- **Evidence:** signal-lojix/triad-port/schema/lib.schema:11-20,77-111 (handshake + event payloads, day-one decision 2tfa); lojix/triad-port/src/schema_runtime.rs:453-481 (open/close are token-mint + echo, no event push)
- **Staleness check:** Schema header names 'schema-next + schema-rust-next event-frame-emission enhancement' as the follow-on; re-verify whether those engine crates have since gained event-frame emission before treating streaming as deliverable.
- **Fresh re-read:** open_subscription (schema_runtime.rs:460-475) mints next_subscription_token (lib.rs:169-171) + current commit_sequence and replies Watching; close_subscription (:477-481) echoes Unwatched. There is NO subscriber registry, NO channel, NO event-push: grep for push.*event/broadcast/subscribers finds only event-LOG writes (event_log.0.push at 943/1121) readable via Query(ByEventLog), never a held-subscription push. daemon.rs serve_ordinary (:192-209) reads one body, writes one reply, returns — strictly one-shot, no streaming loop after the reply. A subscriber receives one token and no stream. Net-new vs the one-shot lojix-cli, so not a parity regression. Schema header names schema-next/schema-rust-next event-frame-emission as the follow-on (signal lib.schema:11-20) — still not landed; re-verify on each HEAD.

### Substituter wire shape diverges from baseline — client now supplies url+public_key instead of horizon node names
*finder: 🟡 minor → verifier: **confirmed**, 🟡 minor; production gate: no*

- **State:** Baseline lojix-cli takes substituters: Vec<NodeName> and resolves each node against the horizon to derive its cache url + public key server-side (lojix-cli/src/deploy.rs:24,99-100 via ExtraSubstituters::from_horizon_nodes). The new contract takes substituters: Vec<ExtraSubstituter{url, public_key}> directly (meta-signal-lojix/triad-port/schema/lib.schema:76,95,106; generated at src/schema/lib.rs:88-92), pushing substituter resolution to the CLIENT. This is a deliberate wire-only shape (the daemon must not reach into horizon for this), but it is a semantic migration point: existing callers that pass node names will not round-trip, and the resolution logic that lived in lojix-cli must move into whatever builds the request. Not an activation gate, but a real consumer-migration consideration.
- **Evidence:** lojix-cli/src/deploy.rs:24 (Vec<NodeName>),99-100,210 (from_horizon_nodes); meta-signal-lojix/triad-port/schema/lib.schema:76,95,106; meta-signal-lojix/triad-port/src/schema/lib.rs:88-92
- **Fresh re-read:** Baseline: deploy.rs:24 substituters: Vec<NodeName>, resolved server-side via ExtraSubstituters::from_horizon_nodes (deploy.rs:99-100, 210-211 — maps each NodeName to ExtraSubstituter::new(url, public_key) against the horizon). New contract: substituters: Vec<ExtraSubstituter{url, public_key}> directly (meta lib.schema:76,95,106; generated lib.rs:88-92), pushing resolution to the client. Deliberate wire-only shape (daemon must not reach into horizon), but a real migration point: node-name callers won't round-trip and the resolution logic must move into the request builder. Not an activation gate.

### Contract honestly carries UnsupportedDeployAction; the activation BLOCK is in the daemon, not the wire
*finder: 🟠 major → verifier: **confirmed**, 🟠 major; production gate: no*

- **State:** On THIS dimension (wire + emission) there is no gate: the contract can fully express activating deploys (Switch/Boot/Test/BootOnce, Home Profile/Activate). But the daemon's reject-guard accepts ONLY System Eval/Build and Home Build, rejecting every activating action with meta DeployRejectionReason::UnsupportedDeployAction (lojix/triad-port/src/schema_runtime.rs:521-541, comment 'Activating actions remain rejected because copy/activate is not yet target-safe'). The contract's own comment documents this honestly (meta lib.schema:119-124, audit 29 C1/C4). So the wire vocabulary is production-complete while the DAEMON BEHAVIOR is not — the production blocker (cannot activate a real node deploy) is real but lives outside this dimension. Flagged here only to make explicit that contract completeness does NOT imply deploy readiness; cross-reference the runtime/activation dimension.
- **Evidence:** lojix/triad-port/src/schema_runtime.rs:521-541 (unsupported_deploy_reason: only Eval/Build/Home-Build supported); meta-signal-lojix/triad-port/schema/lib.schema:119-124 (UnsupportedDeployAction documented)
- **Staleness check:** The activation rejection is the genuine production gate but is owned by the runtime/activation dimension; re-verify against schema_runtime.rs on each HEAD whether the Eval/Build-only guard has been relaxed.
- **Fresh re-read:** On THIS dimension the wire is complete — the contract fully expresses every activating action. The block is in the daemon and STILL CURRENT at HEAD: unsupported_deploy_reason (schema_runtime.rs:525-541) supports ONLY System Eval|Build (:530-533) and Home Build (:537), rejecting everything else with DeployRejectionReason::UnsupportedDeployAction. The HEAD commit 7d66d2e ("materialize horizon inputs for production eval") touched this function (git log -p shows the diff at 221/228) but kept the Eval|Build / Home-Build-only guard — activating deploys are still rejected. The genuine production blocker (cannot activate a real node deploy) is real but owned by the runtime/activation dimension; flagged here only to prevent reading contract completeness as deploy readiness.
