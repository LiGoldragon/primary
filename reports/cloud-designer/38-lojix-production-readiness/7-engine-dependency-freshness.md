# engine dependency freshness — Engine / core-crate dependency freshness vs upstream HEAD

cloud-designer, 2026-06-10. lojix main HEAD `7d66d2e`. Audit dimension `engine-dependency-freshness`.

**Finder status:** `ready` → **Verified status (after adversarial re-check):** `ready`

## Net assessment (after re-reading current source)

Re-verified every pin against current upstream main (and cargo's git db) on lojix HEAD 7d66d2e today. The headline holds: the offline build is green (cargo check --offline exit 0, 1.34s) against a git-tracked Cargo.lock, so day-to-day builds ARE reproducible, and on the freshness dimension alone there is NO production gate — the real cutover gates (no target-safe activate, in-memory generation set, zero migrated consumers) live elsewhere. The drift numbers are exact: schema-rust-next 19 behind, triad-runtime 9, schema-next 5, nota-config 1; nota-next/horizon-rs/nota-codec current — all clean ff-ancestors of main. BUT the prior agent's most reassuring claim is WRONG and I refute it: it asserted the triad-runtime behind-window renames 'none of which [lojix imports]' and that bumping 'carries no break on bump-for-the-imports-it-uses.' In fact behind-window commit 0e1badc (Mon Jun 8) renames the EXACT four symbols lojix's daemon.rs spine is built on — ActorMultiListenerDaemon, ActorListenerSocket, ActorMultiConnectionRuntime, ActorMultiListenerDaemonError (daemon.rs:17-18,78,83,90,97,149) are GONE at triad-runtime main (now Async*). So a cargo update jumps triad-runtime 9 commits and BREAKS the lojix build, not just rewrites artifacts. This raises the floating-branch risk from 'forward-looking hygiene' to 'a single cargo update red-builds today.' Still not a hard production gate (the lock holds; the fix is a coordinated rename + re-lock), but the freshness picture is more fragile than the prior agent painted: BOTH lagging engine deps (triad-runtime rename, schema-rust-next privatization) turn a naive bump red — one at compile-time, one at the artifact freshness check.

## Findings

### Lock drift recurred: schema-rust-next 19 behind, triad-runtime 9 behind, schema-next 5 behind, nota-config 1 behind
*finder: 🟡 minor → verifier: **confirmed**, 🟡 minor; production gate: no*

- **State:** Cargo.lock pins are behind current upstream main again. triad-runtime: lock 069ba8e vs main 6ea8316 = 9 commits. schema-rust-next: lock 5cadd25 vs main eca4028 = 19 commits. schema-next (transitive): lock 77e71a4 vs main c8ebb39 = 5 commits. nota-config: lock bd9173a vs main ba689f0 = 1 commit. nota-next (d8862b6) and horizon-rs (9fae4a3) are CURRENT. nota-codec (f761421) matches cargo's last-fetched origin/main (2026-05-25). Every lock pin is a clean fast-forward ancestor of its main (no divergence). This is drift that accrued in the ~4 days since report-32's c8c4353 bump; report 32 itself is now superseded (its recommended pins triad-runtime 33b9531 / schema-rust-next 6685e7b are no longer current).
- **Evidence:** /git/github.com/LiGoldragon/lojix/triad-port/Cargo.lock:229,404,414,434,592,601,846; git rev-list --count <pin>..main per repo; merge-base --is-ancestor confirms clean ff for all three; report /home/li/primary/reports/cloud-designer/32-core-crate-refresh-for-lojix/7-overview.md:150-157
- **Staleness check:** Report 32 (2026-06-06) is the prior claim; I re-verified every pin against current upstream main today. Local upstream checkouts had stale FETCH_HEADs, so I cross-checked against cargo's own git db (refs/remotes/origin/main) which agreed with the local main refs.
- **Fresh re-read:** Lock pins (Cargo.lock:592,601,846,414): schema-next 77e71a4, schema-rust-next 5cadd25, triad-runtime 069ba8e, nota-config bd9173a. rev-list --count vs local main today: triad-runtime 069ba8e..6ea8316 = 9; schema-rust-next 5cadd25..eca4028 = 19; schema-next 77e71a4..c8ebb39 = 5; nota-config bd9173a..ba689f0 = 1. merge-base --is-ancestor passes for ALL four (clean ff, no divergence). nota-next d8862b6 / horizon-rs 9fae4a3 / nota-codec f761421 == both local main AND cargo-db origin/main (0 behind, current). Report 32's recommended targets (triad-runtime 33b9531, schema-rust-next 6685e7b, /home/li/primary/reports/cloud-designer/32-core-crate-refresh-for-lojix/7-overview.md:20,52) are both superseded by much-newer main tips. Confirmed exactly as stated.

### Floating branch="main" engine deps with no Nix pin — reproducibility hangs on Cargo.lock alone
*finder: 🟠 major → verifier: **confirmed**, 🟠 major; production gate: no*

- **State:** All seven engine git deps are declared as { git = ..., branch = "main" } with no rev pin in Cargo.toml (lines 27-40). There is NO flake.nix/flake.lock anywhere in the lojix repo, so the git-tracked Cargo.lock is the ONLY thing anchoring revisions. Today that anchor holds (offline build is green), but the lock has already drifted behind the branches it floats on (cargo's cached origin/main for triad-runtime=6ea8316 and schema-rust-next=9f50920 are both ahead of the lock). Any `cargo update`, or a first build on a machine with no lock honored, resolves branch=main and silently jumps 9-19 commits. For a deploy tool that must build node generations deterministically, floating-branch deps with no second (Nix) pin is a production-hygiene weakness: the reproducible artifact depends on a mutable upstream ref plus a lockfile that is not regenerated in lockstep with the branches.
- **Evidence:** /git/github.com/LiGoldragon/lojix/triad-port/Cargo.toml:27-40 (all branch="main"); no flake.nix found under /git/github.com/LiGoldragon/lojix; Cargo.lock git-tracked (git ls-files triad-port/Cargo.lock); cargo-db origin/main (triad-runtime 6ea8316, schema-rust-next 9f50920) ahead of lock pins
- **Fresh re-read:** find under /git/github.com/LiGoldragon/lojix returns NO flake.nix or flake.lock anywhere. Cargo.lock IS git-tracked (git ls-files triad-port/Cargo.lock). All 6 visible engine git deps in Cargo.toml:27-40 declared { git=..., branch="main" } with NO rev= pin (horizon-lib, nota-codec, nota-config, nota-next, triad-runtime, schema-rust-next). cargo's cached origin/main is AHEAD of the lock for the laggards (triad-runtime 6ea8316, schema-rust-next 9f50920 — both > lock pins), so a cargo update WOULD jump (9 / 18 commits respectively, merge-base confirms 0e1badc and 44e472b are both in those jump windows). The git-tracked lock is the sole revision anchor; today it holds (green offline build) but is not regenerated in lockstep with the branches it floats on. Confirmed; severity major stands for a deploy tool that must build deterministically.

### schema-rust-next behind-window is an emission-FORMAT change that will rewrite lojix's checked-in @generated artifacts
*finder: 🟠 major → verifier: **confirmed**, 🟠 major; production gate: no*

- **State:** lojix's src/schema/{nexus,sema}.rs are checked-in `@generated by schema-rust-next` artifacts, and build.rs runs GenerationDriver(...).write_or_check("LOJIX_UPDATE_SCHEMA_ARTIFACTS") so the build fails if the artifacts go stale vs the emitter. The 19-commit schema-rust-next behind-window is dominated by emission-format changes lojix's schema actually triggers: 44e472b (emit schema-declared newtypes with a PRIVATE wrapped field), c0f76c2 (emit new(impl Into<String>) for string newtypes), a259139 (integrate no-alias schema-next + recognize Bytes). lojix's schema uses bare Name-Type newtypes (sema.schema:57 ContainerName String, :68 CommitSequence Integer, :69 StateDigest Integer) and its emitted output today shows PUBLIC newtype fields (nexus.rs:74 `pub struct BuilderNode(pub NodeName)`) — exactly what 44e472b privatizes. So a schema-rust-next bump is NOT byte-identical: it forces an artifact regeneration and re-check-in, and any newtype-field-visibility change is a source-visible API change to the wire types. This is the 'moving emission surface' the brief flagged: lojix rides schema-rust-next for all nexus/sema/wire types (though NOT for the daemon spine — daemon.rs is hand-written, no DaemonEntry/triad_main/BoundedWorkers adoption).
- **Evidence:** /git/github.com/LiGoldragon/lojix/triad-port/src/schema/nexus.rs:1 (@generated), :74 (pub field); build.rs (GenerationDriver.write_or_check); schema/sema.schema:57,68,69 (bare newtypes); schema-rust-next log 5cadd25..eca4028 commits 44e472b/c0f76c2/a259139; schema-next log 77e71a4..c8ebb39 cf4cfb9 (qz6j no-alias)
- **Fresh re-read:** build.rs runs GenerationDriver::new(plan).generate().write_or_check("LOJIX_UPDATE_SCHEMA_ARTIFACTS") and .expect("checked-in lojix runtime schema artifacts are fresh") — build FAILS if artifacts drift vs emitter. nexus.rs:1 is '@generated by schema-rust-next'; nexus.rs:74 'pub struct BuilderNode(pub NodeName)' and sema.rs:1363 'pub struct OriginRoute(pub Integer)' both show PUBLIC newtype fields. Behind-window commits (git log 5cadd25..eca4028) confirmed: 44e472b 'emit schema-declared newtypes with a private wrapped field', c0f76c2 'emit new(impl Into<String>)', a259139 'integrate the no-alias schema-next + recognize Bytes', and 7db6df1 'privatize the runtime identity newtypes (MessageIdentifier/OriginRoute)' — which directly targets the very OriginRoute(pub Integer) lojix emits today. sema.schema:57/68/69 bare newtypes (ContainerName/CommitSequence/StateDigest) confirmed. A bump is NOT byte-identical; it forces artifact regen + a source-visible field-visibility API change. Confirmed.

### triad-runtime 9-behind window carries the BindingSurface rename + EngineRequestError, but lojix imports are unaffected and build is green
*finder: ℹ️ info → verifier: **refuted**, 🟠 major; production gate: no*

- **State:** The triad-runtime behind-window (069ba8e..6ea8316) includes a public API rename DaemonConfiguration->BindingSurface (6ea8316), EngineRequestError + kameo re-export (f96bd32), daemon request concurrency limit (bedbb1d), and a listener-API rename away from actor nouns (0e1badc). lojix imports from triad-runtime are ComponentArgument/ComponentCommand/FrameBody/LengthPrefixedCodec (client.rs:13), ArgumentError/FrameError (lib.rs:38,41), the Runner/NextStep/Nexus* runner surface (schema/nexus.rs), and ContinuationExhausted — none of which the behind-window renames. lojix's own `DaemonConfiguration` struct (lib.rs:107) is a distinct local symbol, not the renamed trait. cargo check --offline against the locked rev finishes clean (triad-runtime v0.6.0 @ 069ba8e). So this gap carries no fix lojix needs and no break on bump-for-the-imports-it-uses; it only matters when/if lojix adopts the emitted daemon (a deferred decision per report 32).
- **Evidence:** triad-runtime log 069ba8e..main (6ea8316/ae2e817/f96bd32/bedbb1d/0e1badc); lojix imports grep src/ + build.rs; cargo check --offline -> Finished dev profile in 2.03s, exit 0
- **Fresh re-read:** REFUTED on the core claim 'none of which the behind-window renames' / 'no break on bump-for-the-imports-it-uses.' daemon.rs:16-19 imports ActorListenerSocket, ActorMultiConnectionRuntime, ActorMultiListenerDaemon, ActorMultiListenerDaemonError from triad_runtime, and USES them heavily (daemon.rs:78,83 ActorListenerSocket::new; :90 ActorMultiListenerDaemon::new; :97-103 ActorMultiListenerDaemonError match; :149 impl ActorMultiConnectionRuntime). Behind-window commit 0e1badc (Mon Jun 8, 'rename task listener API away from actor nouns') renames ALL FOUR: git show 0e1badc shows ActorListenerSocket->AsyncListenerSocket, ActorMultiConnectionRuntime->AsyncMultiConnectionRuntime, ActorMultiListenerDaemon->AsyncMultiListenerDaemon, ActorMultiListenerDaemonError->AsyncMultiListenerDaemonError. git grep confirms ActorMultiListenerDaemon EXISTS at locked rev 069ba8e but is GONE at main. So a triad-runtime bump BREAKS the lojix build (not byte-stable, not import-safe). The prior agent was right that the local DaemonConfiguration struct (lib.rs:107) is crate-local/unaffected by the trait rename (verified: no triad_runtime::DaemonConfiguration import anywhere), but it conflated that with the listener-API rename and wrongly concluded the whole window is harmless. Severity raised info->major: this is the second engine dep that red-builds on a naive bump.

### No behind-ness blocks cutover; build is green and reproducible against the checked-in lock TODAY
*finder: ℹ️ info → verifier: **confirmed**, ℹ️ info; production gate: no*

- **State:** On the dependency-freshness dimension specifically there is NO production gate: lojix HEAD 7d66d2e builds green offline against the locked revisions, the lock is git-tracked (so the artifact is reproducible until someone runs cargo update), and none of the behind-windows contain a known fix that lojix needs-but-lacks. nota-next, horizon-rs and nota-codec are fully current; the lagging deps (schema-rust-next, triad-runtime, schema-next, nota-config) are all clean ancestors of main. The freshness risks are forward-looking (a future bump rewrites generated artifacts; floating branches could jump on cargo update), not present blockers. The actual cutover gates live in other dimensions (activate-not-target-safe per ARCHITECTURE.md, in-memory generation set per INTENT.md, zero migrated consumers).
- **Evidence:** cargo check --offline exit 0; Cargo.lock git-tracked; nota-next d8862b6 / horizon-rs 9fae4a3 / nota-codec f761421 all == upstream main; lock pins are ancestors of main (merge-base --is-ancestor all pass)
- **Fresh re-read:** cargo check --offline on lojix HEAD 7d66d2e: 'Finished dev profile in 1.34s', exit 0 — green and reproducible against the locked revs. Cargo.lock git-tracked. nota-next/horizon-rs/nota-codec fully current; the four laggards are clean ancestors of main (all merge-base --is-ancestor pass). No behind-window contains a fix lojix needs-but-lacks. The freshness risks are real but not present blockers: a future bump red-builds (triad-runtime rename at compile time, schema-rust-next privatization at the artifact freshness check) and floating branches could jump on cargo update. The genuine cutover gates (target-unsafe activate per ARCHITECTURE.md, in-memory generation set per INTENT.md, both flake.locks still pinning legacy lojix-cli) live in other dimensions. Confirmed — though I'd refine the 'risks are forward-looking only' framing: the triad-runtime rename means a bump is breaking RIGHT NOW, just not yet triggered because the lock pins pre-rename.
