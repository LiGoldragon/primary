base_url = "https://criome.org"
title = "CriomOS Web Host POC"
# The default markdown-static renderer variant (SiteRenderer::MarkdownStatic,
# Spirit 878r). A user's site is exactly this shape: config + markdown content.
compile_sass = false
build_search_index = false
