+++
title = "Hello from CriomOS"
+++

This page was **rendered from markdown** by Zola at Nix build time, then
frozen into an immutable `/nix/store` artifact.

The hosting node serves these static files only — no renderer, app server,
or database runs at request time. That is what makes a public web host on a
low-trust cloud node (doris) reliable and secure: there is nothing dynamic to
compromise.
