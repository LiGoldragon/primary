ID p6fx | Decision | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Major engine version upgrades happen atomically in real time with no downtime. The workspace is a continuous runtime; component upgrades coordinate via Persona to swap versions without interrupting service.

ID 1lex | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Prometheus live switch and promotion operations that may disrupt SSH or Wi-Fi must be launched as durable systemd transient units so the operation continues even if the agent's SSH session drops and reconnect is needed.

ID 4hsl | Decision | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Root-level persona engine takes over component upgrade management; CriomOS no longer needs redeploy per component upgrade *orchestration*

ID 6njo | Correction | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: References to Prometheus in the recording-system design (and elsewhere) name a specific cluster node by accident — the canonical reference is the CriomOS role 'large-ai-node' (NodeService::LargeAi per horizon's cluster taxonomy). Prometheus today fills the role; tomorrow a different node may. Workspace design language should name the role, not the host. Refresh existing references to Prometheus that should be role-level. *infrastructure as code*

ID 7m2d | Correction | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Lojix deployment is not the problem to avoid; the safety issue is local resource pressure during build/evaluation, so operator work should keep using lojix while making the workload safe.

ID 8qnc | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Large model prefetches and fixed-output hash discovery for CriomOS large-AI model inventory should run on Prometheus over its wired LAN path, not on the local workstation Wi-Fi. *provisioning*

ID 8vrd | Decision | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Quantized Gemma model variants should be installed through the CriomOS/NixOS large-AI model inventory and deploy path, not left as ad-hoc prefetched files in the target store. *provisioning*

ID b8la | Correction | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Pi operator-safety must not ask permission solely because a repository is dirty.

ID bc6f | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: CriomOS FullOS deploys must carry the latest CriomOS-home fixes by updating the production CriomOS flake lock before deploying Zeus.

ID bev5 | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Home profile activation must persist across reboot; a reboot that restores an older deployed Claude version is a system bug to investigate rather than acceptable behavior.

ID bsg1 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=lojix,lojix-cli
DESC: The psyche has decided to execute the lojix production cutover now, not merely pursue it: the lojix daemon becomes the production deploy path and the legacy lojix-cli is retired per node, standing down the parallel Stack A production and Stack B next deploy burden. This advances the tvbn cutover charter and the se72 validation target from goal to active execution, with the daemon already piloted at parity on the ouranos host. Per-node switchover proceeds.

ID dn3c | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Public-traffic routing during component-version cutover must be lossless (no missed messages, no dropped connections) AND client-transparent (one stable socket; clients do not perform discovery). Rules out CLI-discovery designs that require the client to ask Persona where the active daemon lives

ID jldt | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=spirit,CriomOS-home,CriomOS
DESC: Workspace component deployments must come from main-backed inputs unless the psyche explicitly authorizes a non-main deployment. Operator integration work lands feature-branch substance onto main before deployment.

ID kx32 | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Deploying to prometheus must not knock out its network connectivity. prometheus is the cluster router running hostapd and dnsmasq; a live switch activation restarts those services and drops the connection we reach it through, which is what broke during the gemma deploy. Future deploys to the router node must avoid restarting networking live: use boot-mode activation rather than a live switch, or an approach that preserves connectivity, and prefer out-of-band or console access before any risky activation since there is none today.

ID my4g | Decision | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Persona uses systemd for component daemon management in production via template units (persona-component@<component>:<version>.service) from day one; UnitController trait with systemd-D-Bus backend (prod) + direct-fork backend (tests/sandbox) *orchestration*

ID oso5 | Correction | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Lane identifiers in persona-orchestrate's registry are NOT reserved forever. The active-registry semantics — retiring a lane removes it, active lanes are not renamed, retired names may be reused by later registrations of the same (role, authority) pair — are the current MVP target. Supersedes intent 117/118 (mine) which proposed a persistent (role, authority) -> next ordinal counter for forever-reservation. Operator was already treating the newer psyche clarification as current per second-operator/165 + /166; designer aligns. If durable agent-run history later requires tombstones, that lands as a separate history feature, not as a lane-registry change. *orchestration*

ID oup1 | Correction | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: HomeOnly Activate must not log the user out. A Home-only activation that terminates the active graphical session is a deploy bug, not expected behavior, and deployment tooling must treat that as unacceptable.

ID ufjd | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment;Technology.Software.Operations | referents=prometheus,nix
DESC: A node NixOS configuration is built and realized on that node itself, using the node or its configured remote builder. For large-AI nodes such as prometheus whose model files are tens of gigabytes, the build and any closure realization run on prometheus, and the coordinating or local machine performs evaluation only. Realizing a model-bearing node closure on any other machine pulls the giant model files across the network onto the wrong store and wastes its compute and disk, so node builds stay on the target node. This extends the heavy-work-placement rule 0a9p from AI model prefetch to all node configuration builds, and the deploy path uses lojix-cli on the host so the closure is built and kept on the node.

ID up9q | Decision | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: A lojix deploy or build survives client disconnect. A durable deploy is owned by a job actor that owns the external process and persists job state, reporting status later — the process lifetime is not coupled to the request stream. Cancellation is declared per operation in schema: durable deploys survive disconnect by default; speculative queries or builds may opt into kill-on-drop. No blanket kill-on-drop on effect processes.

ID xhwa | Decision | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment;Technology.Software.Security.Authorization | referents=spirit,criome
DESC: near-term production deploy target for spirit criome-gating is the simplest 1-of-1 local authorization: spirit asks its co-resident local criome daemon to authorize the content-addressed head, and a single local signature, 1-of-1 with no quorum and no multi-machine cluster, suffices to gate propagation. This is the m0p2 bootstrap case promoted to the first production milestone, letting the spirit to criome to router to mirror chain from d6he ship to production without waiting on the full criome cluster; quorum authorization and the multi-machine cluster from jk1w are the subsequent step, not a prerequisite.

ID xv9v | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Prometheus deploys should use the safe BootOnce path rather than Switch until console/out-of-band access and sign-off are in place; avoid leaving an unverified generation staged for the next reboot unless deliberately accepted.

ID yivn | Correction | certainty=High importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Do not run live Home activation or Niri/desktop-affecting deployment in an active session with ongoing agents unless the user explicitly confirms the disruption window. Desktop deploy work must preserve active agent sessions and must not log the user out or restart the session unexpectedly.

ID nfic | Clarification | certainty=Medium importance=Medium | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: For Crayon OS host maintenance, the maintainer has root SSH access on all cluster hosts but cannot SSH as Bird on Zeus; Bird's Zeus home-profile redeploy must use an existing root or maintainer-user path rather than direct Bird SSH.

ID 2qhw | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: lojix-daemon takes scope expansion to handle GitHub-authenticated Nix flake input resolution. The GitHub API rate-limit stale-activation incident (per reports/cluster-operator/7 file:54-64) is a deploy-path problem owned by lojix, not a Pi-package problem. Resolves Open Question 7.2 from reports/system-designer/31 in favor of lojix-daemon scope expansion over skill-level discipline. Mechanism: an authenticated wrapper around the relevant Nix invocation that fetches the user GitHub API key from gopass and injects it into the Nix call (likely via NIX_CONFIG access-tokens). Implementation form (Medium certainty per the parenthetical): a small Rust library that lojix-daemon calls; the library encapsulates the secret-fetch + auth-injection so the rest of lojix-daemon does not handle the token directly. *provisioning*

ID 7ku2 | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Bird persona gets a local operator-controlled operating-system update path on Zeus

ID 9375 | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Legacy persona-spirit daemon slots that remain in the side-by-side deployed set are needed and should be fixed to run, not removed or disabled as the primary fix.

ID gnfx | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Large model prefetches on Prometheus should run as durable target-side systemd work with a target-side periodic monitor, so progress checks do not depend on the operator's local SSH session staying alive.

ID iq11 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Drain-with-mirror is accepted as orchestrates no-downtime upgrade protocol, simplified per designers spirit-in-transition-probe empirical finding: acked == durable, no in-flight message replay needed. Mirror transfers durable state snapshot; in-flight ops drain naturally; atomic socket handoff.

ID mazv | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: The engine target running state is a live orchestrated system: persona - the manager and supervisor of the whole thing - runs and supervises the introspector (persona-introspect), the schema daemon, and the other triad components together as one running orchestrated whole, not just proven in-tree. Moving toward this running orchestrated system is the direction for engine-forward effort. *orchestration*

ID o6s3 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Live Persona agents should be logged into orchestrate and mounted through terminal cells *orchestration*

ID p6k5 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: persona-spirit is the deployed production Spirit while spirit-next is the schema-derived pilot. The production component should carry a clear prominent in-code production marker so agents do not patch the wrong repository when two stacks coexist.

ID v5d4 | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: Passing sandbox testing is a precondition for the lean-stack cutover to main deployment

ID hr3z | Principle | certainty=Minimum importance=Minimum | domains=Technology.Software.Operations.Deployment | referents=(none)
DESC: capability-missing daemons may eventually self-upgrade before unsupported replies

