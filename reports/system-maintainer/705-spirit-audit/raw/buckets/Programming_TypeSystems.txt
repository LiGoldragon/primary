ID 3742 | Principle | certainty=High importance=Minimum | domains=Technology.Software.Programming.TypeSystems;Technology.Software.Programming.Grammars | referents=schema-next,nota-next,TypeReference
DESC: A type's kind must be explicitly marked where the type is introduced — its declaration — never left to syntactic-slot convention. A parameterized type in particular must declare itself as such by an explicit signal on the declaration form: a delimiter, a wrapping structural variant, or a reserved keyword head as the built-ins Vector/Map/Optional already are. The current parameterized declaration head (Work E W R Eff) is identical in surface to a generic application (Work A B) and is recognized as a declaration only because it sits in the namespace key slot; that positional-by-convention recognition at the declaration is the guessing this rule forbids. Name resolution at use sites against an explicit declaration is the legitimate mechanism — exactly how Vector/Map are known — not guessing.

ID 5jac | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: In a schema, a camelCase or lowercase-first name is a FIELD name, an instance; a PascalCase or capital-first name declares or references a TYPE. A PascalCase type can be declared inline at its first use and then reused by the same name later, which refers back to that inline declaration. Resolution is declare-before-use, left to right: the declaration precedes any reference, so single-pass parsing works without a separate pre-pass. This applies the PascalCase-structural and camelCase-instance rule to the schema field-versus-type distinction and adds inline reusable type declarations.

ID ba6d | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.TypeSystems;Technology.Software.Programming.Grammars | referents=nota,nota-next,schema-next,schema-cc
DESC: Pipe-brace traits and impls are data: a catalog that references impls and methods already existing on the Rust side so schema can name them and a method-calling syntax can invoke them from schema-authored bodies. An impl is not a free-standing object that repeats its target; it fuses onto its target type as one combined object - a type declaration carries a trailing pipe-brace impl block with the target implicit, so a type and its trait participations and method signatures are introduced together at the declaration and there are no separate marker objects to associate with their type by hand, because separate objects add too much association complexity. Inside the impl block each entry is a bare trait atom for a marker impl, or a trait atom followed by a bracket of method signatures for a body-bearing trait impl, or a bare method signature for an inherent method; a method signature is the method name then a brace parameter struct then a return type, the same shape as a Work-frame leg, listing a callable signature of a referenced existing impl rather than a generated body. Method calls resolve from the receiver schema shape plus this fused impl catalog. Generating method bodies from schema stays reserved for the narrow mechanical family; deref-by-default is not a goal, it was only scaffolding for impl syntax.

ID hh3z | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.TypeSystems;Technology.Software.Programming.Grammars | referents=nota,nota-next,schema-next
DESC: A type's kind is announced by the delimiter that wraps its value, the same way a brace body means struct and a square-bracket body means enum. Generics use the reserved pipe-parenthesis delimiter (| ... |), pipes inside, mirroring the existing pipe-text string delimiter. A generic declaration is Name (| [params] body |): the pipe-parenthesis marks the type generic, the params are the binder list, and the body is an ordinary struct or enum body nested inside. This assigns meaning to one of the two reserved pipe-bracket extension points named by j9du; nota-next already parses these blocks (PipeParenthesis and PipeBrace delimiters exist), so it is schema-level work, not a seed-parser change. Realizes j9du and satisfies 3742.

ID iypq | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: The entire built-in type-name vocabulary lives in Schema, not NOTA: the scalars String, Integer, Boolean, Path and the composites Vec, Optional, Map, Set, Box are Schema's type-reference vocabulary. NOTA stays pure structure, meaning its delimiters, plus the serialization codec and the value literal None or (Some x). This refines record 1137: the (Vec X) tagged FORM and the value-versus-type distinction still hold, so [X] is a vector value and (Vec X) is the type, but the placement moves from the NOTA layer to the Schema layer, alongside struct and enum declarations. The codec, meaning NotaEncode and NotaDecode for Rust types like Vec and String, stays in nota-next as serialization, separate from where the schema type keyword is defined, so this does not undo nota-next growing the codec.

ID lmzl | Principle | certainty=High importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: Workspace preferred data extension shape is enum or vector-of-enum-variants. Vector IS structurally an enum — gives choice of all things that can be in it. Maps are reserved for namespaces specifically. Bool with True/False PascalCase is conceptually a two-unit-variant enum — this is why NOTA capitalizes them. Variants are unit (bare PascalCase) or data-carrying (parenthesized), in order, positional.

ID rnrg | Clarification | certainty=High importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: NOTA is at heart a hack on the text user interface — using common sense, good patterns, beauty, delimiters, and typed structure. NOTA is a typed language; everything is read as a known type in data-type-theory terms. Structure macro nodes can have structurally different shapes at certain positions because they extend the type-declaration vocabulary, but the macro extension is itself typed, so the node still resolves to a known type. A well-formatted valid NOTA expression decodes reliably to its declared types. This is the deeper rationale for the workspace's NOTA-everywhere direction — typed-text reliability through a beautiful user-facing surface.

ID 5myr | Clarification | certainty=Medium importance=Medium | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: NOTA schema namespace section is a key-value MAP {key value key value} of user-defined types. Keys are the type names; values are the type definitions. Not a flat sequence of separate declarations. An enum declared inline: EnumName (Variant1 Variant2 Variant3) - parens hold the variant names. Enum variants may themselves be enums declared in nested parens. Structs are a different syntactic shape. Universal Unknown injection by macro stays as a behind-the-scenes mechanism; no user-authored Features section needed.

ID 2cuo | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: Everything in the schema reduces to STRUCTS (data-carrying) or UNIT VARIANTS. Vec is a struct with a single field. Enum data-carrying variants ARE structs - the data-carrying portion IS the struct shape. Macros are structs. Even unit variants live inside enum structs in some sense. The reduction simplifies the type system: one canonical kind (struct) plus the unit case. Field naming + composition all derive from this.

ID 2so1 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: Sub-ID is a SignalCore primitive type for entities that are 'a thing' but do not have their own Criome identity. Sub-ID = content-address fingerprint of the launching job's full spec. Use case: agent calls, processes within agents. Stored in ARCA as own type.

ID 3itj | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: Schema-defined type trees are normally 6-7 layers deep: enum-of-struct-of-enum-of-struct-of-enum-of-struct etc. The same types are referenced in multiple places throughout the tree via a TYPE INDEX (a flat index of named types that the tree refers into). Deep nesting is the normal shape, not an exception. The schema engine must traverse and emit through arbitrary depth via canonical recursive walks; no ad-hoc per-layer handling. Cross-reference resolution goes through the type index so the same type appearing in many places resolves to one canonical declaration.

ID 3sq4 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: At a slot whose type is already known to be an enum, the authored value may omit only that enum type name and provide the enum body directly. Thus ((Parse Expression) (Render Expression)) is valid when the surrounding slot supplies the outer enum type; the fully named equivalent would be (SomeEnum ((Parse Expression) (Render Expression))). Only the known outer type name is omitted; variant tags remain named inside the body.

ID 4fwy | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: In schema struct/record declarations field names are DERIVED from PascalCase type names by lowercasing - there is no explicit field name syntax. The form is just a list of type names: Entry (Topic Kind Summary Context Certainty Quote). Each type name lowercases to its field name (topic kind summary etc). Operator/180 added explicit SchemaField name+type to support field-name-divergent-from-type-name cases; this is reversed by the new rule.

ID 4idp | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: In schema member declarations, lowercase names bind fields for code access, while PascalCase creates a reusable type in the current context; a type becomes library/export visible by being declared as a top-level namespace entry. The namespace is conceptually the struct of declared types, while enums represent category divisions with payload data carried by variants.

ID 5386 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: PascalCase booleans are unit variants

ID 7oqk | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: NOTA records do not carry field names; schema names require named type boundaries

ID mchq | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: Divergent field names like field certainty of type Magnitude are achieved by declaring a NEWTYPE giving the type its specific name (Certainty (Magnitude)). Then the schema uses (Certainty) in the field position; the field name derives from Certainty as lowercased certainty. No per-field renaming syntax. If you want a more specific name use a longer/different type name not a per-position override.

ID qvod | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: Add a plane ENVELOPE type per plane - a signal-message type, a nexus-message type, and a sema-message type - thin containers that wrap a message to mark which plane it belongs to, so the engine trait function signatures TYPE-CHECK on the plane: a signal-plane message cannot be passed where a nexus or sema message is expected. This complements the three-engine trait-ordering of records 1028 and 1036 - order is already type-enforced; the envelopes add plane-membership type-enforcement, making the trait chain impossible to mis-wire on both axes at once. Affirmed the three trait-ordered engines as brilliant and extended the design with these plane envelopes. Builds on the three schema types as planes, records 964 and 982.

ID qz6j | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: In schema-next the bare form of a name followed by a type declares a distinct newtype, not a non-distinct alias; this is the safe common intent of the typed-values discipline. Aliases lose the bare form, so they are dropped or kept only as an explicit rarely-used marked form. The single-field struct form is no longer the only way to declare a newtype

ID ugig | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=(none)
DESC: The schema root plane surface is a data-carrying enum named Plane with Signal Nexus and Sema variants; the variants carry the actual plane messages so runtime code can match directly on the plane instead of pairing a thin kind tag with a separate envelope.

ID wqdi | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.TypeSystems | referents=nota-next,schema-next
DESC: Multi-argument type references and applications use NOTA's flat positional form — head then arguments inline, like Map then K then V — not a grouped form that wraps the arguments in their own delimiters. The head already names the construct, so a trained human or LLM reads the positional arguments correctly without extra grouping, and the flat form is fewer characters while staying equally legible. This settles the Map type-reference shape as the flat head-K-V form and generalizes to multi-argument type-reference applications.

