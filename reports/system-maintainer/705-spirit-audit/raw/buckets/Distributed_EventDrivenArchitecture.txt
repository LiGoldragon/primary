ID fc3j | Decision | certainty=High importance=Medium | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Asynchronous representation lives at the data-type level of the Signal Protocol: the message data types themselves carry the correlation IDs and lifecycle state tracking sent/queued/processing/replied transitions.

ID jl3k | Decision | certainty=High importance=Medium | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: The wire protocol is named the Signal Protocol; its messages move through a universal mail mechanism (mailer/dispatcher/push system) that every component shares.

ID q73w | Decision | certainty=High importance=Medium | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Message lifecycle exposes hookable events, including a method-on-message-sent that fires as soon as a message is sent and commits the action through the mail dispatch system, so UI, observers, and other components can react at the message-sent boundary.

ID 8y24 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Every actor has its own schema (a channel-contract per rec 668) declaring two enums: ACTION (the things this actor can DO when asked) and RESPONSE (the things this actor can SAY in reply). A universal Unknown response variant lives on every actor so it can answer 'I don't know what you are asking for' to any unrecognized request. Same schema substrate as messaging/CLI/storage; one mechanism, applied per-actor.

ID g3f2 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: The schema-derived runtime is driven by THREE ENGINES - a signal engine, a nexus engine, and a sema engine - whose traits DEFINE THE ORDER of execution. The signal engine processes the incoming signal and pushes it to the nexus engine; the nexus engine has a trait method invoked when a signal is pushed to it, processes and gates it, and pushes onward to the sema engine; then the flow REVERSES - the sema reply returns through nexus and back out as the signal output. Push happens only on success: an invalid message is answered to the client directly with a rejection without proceeding; a valid signal pushes to nexus, and if nexus accepts it proceeds to sema. The push methods take the next engine as a parameter so the trait signatures themselves sequence signal then nexus then sema then back. Refines records 964 967 970 and 989 990 by naming three engines whose traits encode the ordering.

ID udjq | Principle | certainty=High importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: The schema declares an EFFECT TABLE: a closed mapping from message types to effect types, with the set of possible replies declared per message. Dispatch is match-driven through the interact-trait contact point: incoming message → mapped effect → mapped reply. No ad-hoc dispatch logic, no string-matched routing, no boolean-flag-driven selection. Everything is a structural match. Effects are mapped. Messaging is mapped. Interactions are mapped. Internal work composes interactions of these mapped layers. Match always; map always; never compute when you can match.

ID o8x5 | Decision | certainty=Medium importance=VeryHigh | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: NEXUS is the mail keeper - the in-between runtime layer that owns mail tracking and Signal-to-SEMA translation; when Nexus has the mail, the mail is in the BEING-PROCESSED state; Nexus IS the runtime representation that a mail is being processed; basic Nexus actions are: submitting query to Nexus (execution action) and getting a reply (state change or SEMA reply) which the Nexus then translates back into the Signal reply for the Signal plane; the daemon has THREE EXECUTION CENTERS - Signal (communication messaging), Nexus (execution + mail keeper + translator), SEMA (state); the complete flow is Signal IN -> Nexus accepts mail (mail enters being-processed) -> Nexus translates to SEMA query -> SEMA engine runs and produces state change + SEMA reply -> Nexus receives SEMA reply (mail has reached state and gotten response) -> Nexus translates SEMA reply to Signal response with logging that the response has been seriously received because there has been a response -> Signal OUT; this consolidates records 935 (Communicate + signal-frame + mail + database marker) + 963 (mail-mechanism + on_sent hook) + 964 (three schema types) + 965 (Nexus as execution+IO+UI) - the on_sent hook fires when Signal hands mail TO Nexus; the database marker travels on the SEMA reply that Nexus receives and propagates in the Signal response; UI and external-IO uses of Nexus from 965 are SPECIFIC USES of the more fundamental in-between translator + mail keeper role

ID kgl2 | Principle | certainty=Medium importance=Medium | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Message-sent is an event surface with behavior: the mail dispatch system should emit or commit a typed MessageSent action that hooks, UI layers, routers, or subscribers can react to immediately.

ID 17ss | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Router-acknowledgement = delivery durable on harness-side ack *message queuing*

ID 2tfa | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: The lojix ordinary contract signal-lojix carries the WatchDeployments and WatchCacheRetention streaming subscriptions from day one not deferred. The port accepts being the first component to prove schema-derived streaming-subscription emission through schema-next and schema-rust-next; if the generator cannot yet emit streams that enhancement is on the lojix port path rather than dropping streaming from the contract. Deploy phase events and cache-retention transitions are pushed not polled.

ID 59dr | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Backpressure and deeper runtime-control machinery are deferred future runtime work, not part of the current production-oriented implementation slice.

ID alom | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: The psyche chose Option A from system-designer report 76: message realizes intent l3k4 by gaining its own durable existence-event surface rather than staying a stateless 2-plane trust membrane. message owns the EXISTENCE fact — that an authenticated message was authored, witnessed at the SO_PEERCRED ingress boundary — as a durable event it emits, distinct from the router DELIVERY fact which stays durable on harness-side ack. message becomes feature-bearing not a pure pass-through, resolving the report-75 message-plane question toward the durable existence-event shape with its own state surface. message and router remain SEPARATE components; merging was rejected because the SO_PEERCRED trust boundary cannot move into router.

ID brgo | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: The chosen streaming direction is full schema-derived streaming — not poll-as-permanent and not a lojix-only hand-wired carve-out. Teach schema-next to express an event or stream root with opens and belongs relations; teach schema-rust-next to emit the event frame reaching the signal-frame existing-but-unused StreamingFrameBody and ObservableSet publish-subscribe machinery; and add a push action and subscriber registry to the triad-runtime runner. It is a platform project across the shared engine, made cheaper because the streaming substrate already exists in signal-frame. lojix Watch subscriptions gain real push once it lands; until then the handshake plus poll suffices and does not block cutover. Open question carried: whether the existing signal-frame streaming substrate was deliberate scaffolding ahead of this work or leftover, which most changes the effort estimate.

ID daht | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Signal is the first gate: invalid or unacceptable messages are rejected at the Signal layer and return to the client without reaching Nexus or SEMA. *message queuing*

ID gmlv | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Schema-created base enums define the reaction and action types an actor can receive and emit; execution matches those schema variants to select replies and route work.

ID i99x | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=message,router
DESC: Message gains a direct-delivery fast path: when the sender addresses a target agent by uid and that agent is marked publicly reachable and local, message delivers peer-to-peer without router and establishes delivery itself on the target direct acknowledgement. Router handles gated agents, whose delivery needs channel-grant adjudication, and agents across the network. This explicitly supersedes l3k4 clause two and 17ss for the direct path only: router is the authoritative delivery source for the routed gated-or-remote path, but on the direct path delivery is established by message on a direct ack, not by router on a harness ack. l3k4 clause one, existence to message, and the routed-path delivery authority both remain.

ID l3k4 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Router declares message-delivered as durable fact upon receiving acknowledgement from the harness channel (one per agent); message creates a log event for the existence of the message, but DELIVERY is established only when the router gets the harness-side acknowledgement; the router is the authoritative source for delivery facts *message queuing*

ID s8es | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: SubscribePolicy default is TerminateAtHandover - active subscriptions cleanly terminate at the handover boundary; clients reconnect to next

ID v91q | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Actor execution produces FAN-OUT outputs. When an interaction actor decides an outcome, it emits multiple things in parallel: execution effects, responses returned to the originating actors, AND additional messages dispatched to other actors (typical: send a mutation message to the database actor, send a notification message to the subscription actor, return a reply to the requester - all from one interaction-actor decision). Single-input-multiple-output is the normal shape; the actor never produces just one thing. The schema encodes the fan-out outputs declaratively - per interact-trait variant, the set of possible outputs is fixed and closed.

ID 4vi4 | Clarification | certainty=Low importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Nexus can have a daemon internal interface distinct from external Signal and SEMA surfaces. Inside Nexus there may be a recursive computation engine that can call back into itself and provide a future runtime control substrate for prioritizing actor work handling busy SEMA capacity preserving logging and deciding when to tell clients the system is too busy.

ID fcsg | Decision | certainty=Low importance=Minimum | domains=Technology.Software.Distributed.EventDrivenArchitecture | referents=(none)
DESC: Refines designer 476 per operator's correction. Nexus has a schema-defined decision/effect language for non-SEMA decisions — typed NexusDecision wraps Signal SemaWrite SemaRead Effect; typed NexusEffect carries side-channel variants like Stash Fanout Summarize Drop Enqueue Preempt Cascade. Generated runner loops — Nexus decides; runner executes SEMA and effect actions; completions re-enter Nexus; only NexusDecision::Signal(Output) exits to wire. Side effects are typed without pretending every side effect is already a Signal reply. The LANGUAGE existence is Maximum; the exact variant list and implementation cost remain designer-side proposals at lower magnitude pending evidence.

