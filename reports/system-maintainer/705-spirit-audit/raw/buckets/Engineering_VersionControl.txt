ID hp9n | Constraint | certainty=Maximum importance=High | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: The repos directory must remain untracked; it is a local symlink index, and tracking it would turn repository coordination into circus-level churn.

ID r2x2 | Decision | certainty=VeryHigh importance=VeryHigh | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Agents commit the ENTIRE working copy, never path-scoped. In the shared jj working copy, path-scoped commits leave other agents changes undrained, letting two agents create sibling commits off the same base — the fork that strands work off-main and can orphan files. Committing everything drains the shared working copy and serializes agents through the jj working-copy lock, giving linear history; whoever commits sweeps in all in-flight work so nothing is orphaned. Commits become multi-lane, which is accepted. Supersedes the path-selective do-not-commit-other-lanes-files rule; skills/jj.md and the AGENTS.md path-selective note need updating to match.

ID 20hv | Correction | certainty=VeryHigh importance=High | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Landed means on main. Work on a branch has no material existence — it is a concept until an operator integrates it to main. Agents must never report pushed or branch-resident work as landed or done; state it as pushed to branch X awaiting operator integration, and reserve landed and done for work that is on main. Claiming branch work as landed is a false status report, and it damaged multi-agent coordination when other agents acted on the claim and chased a change that was never on main.

ID op4b | Correction | certainty=VeryHigh importance=High | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Agents must NOT create new GitHub repositories for architectural breaks, experiments, mockups, repros, or design passes. That work belongs on feature branches in worktrees under the existing repo. The skill major-break-via-new-repo is retired — it caused a proliferation of throwaway repos such as design-deep-spirit-2026-05-26, design-nota-from-schema, signal-frame-mockup-stable-caller-id-1, kameo-supervised-shutdown-repro, and signal-frame-worktrees, plus -next repos never renamed back to canonical so spirit-next is now just a symlink to spirit. The design-prefixed-repo guidance in double-implementation-strategy is retired with it. Default is always feature-development branches and worktrees. New repositories are created only on explicit psyche authority. Per psyche 2026-06-07.

ID di1r | Correction | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: jj commands must always use inline/headless flags (-m / --no-edit / --message=); NEVER invocations that spawn an editor for commit/describe/squash etc. AGENTS.md hard-override must encode this so sub-agents do not stall waiting for editor input

ID p4sm | Constraint | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: On primary — the workspace coordination repository — everyone always works on main directly: edit, commit, and push straight to main with the simple flow. No feature, next, wip, or push branches on primary, and no rebase-onto-main dances. The main-and-next branching model of record 2561 applies only to the code repositories under git, where operators own main and designers work on next; primary itself is everyone-on-main, always. Just commit and push to main on primary — it is simple.

ID i4ak | Decision | certainty=High importance=Maximum | domains=Technology.Software.Engineering.VersionControl;Technology.Software.Data.Persistence | referents=(none)
DESC: The reusable component version-control system is foundational to the whole meta-work and must support full distributed-version-control semantics over the typed database: branching, forking, rebasing, and merging, with per-component customizable intake, merge, and rebase policy. A default implementation plus a per-component override. For example Spirit's guardian mediates rebase by admitting, rejecting, or transforming each incoming entry. Extends 29pb and j487.

ID 6xzu | Decision | certainty=High importance=VeryHigh | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Designer roles work on a standard next branch by default in each repository — next is where breaking changes happen. Operators own main and integrate from next at their discretion: rebasing the good parts, cherry-picking, re-implementing, or merging the designer branch as-is when the code is good. Designer code is not second-class; a clean designer branch may be merged to main as-is. This refines the prior split where designers work on feature branches and operators own main: the feature branch is standardized as next, and merge-as-is is an explicit endorsed integration path alongside rebase, cherry-pick, and re-implement.

ID tyoz | Decision | certainty=High importance=VeryHigh | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Every agent commits and pushes at the end of every pass. Uncommitted work blocks the psyche-side review loop because the psyche reads Git to see what landed while away. Dirty working copies at pass-end indicate the work isnt finished. Discipline: at the end of every report-shaped or code-shaped pass, commit + push before signing off.

ID 2uhh | Clarification | certainty=High importance=High | domains=Technology.Software.Engineering.VersionControl;Technology.Software.Engineering.Architecture | referents=(none)
DESC: The reusable version-control design's IntakePolicy is a universal reusable admission interface, with per-component implementations. It is not only a rebase hook; every path that introduces entries to a branch should route through it unless a later design explicitly supersedes this.

ID iir4 | Decision | certainty=High importance=High | domains=Technology.Software.Engineering.VersionControl;Technology.Software.Data.Persistence | referents=(none)
DESC: The versioned operation log is the authoritative source of truth for component Sema state, and the redb store becomes a rebuildable materialized view folded from the log. This kernel inversion is chosen for the first version-control implementation rather than deferred.

ID qpv6 | Decision | certainty=High importance=High | domains=Technology.Software.Engineering.VersionControl;Technology.Software.Data.Persistence | referents=(none)
DESC: For the current SEMA version-control implementation, keep the work in sema-engine now rather than creating a new sema-vcs repository. A later extraction remains possible after the engine nouns settle.

ID kmhb | Clarification | certainty=High importance=Medium | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Version-pair vocabulary: current/main (deployed production version) versus proposal/dev/next (new version being introduced)

ID 0yx5 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Engineering.VersionControl;Technology.Software.Engineering.Architecture | referents=(none)
DESC: The sema version-control remote is a dedicated mirror component triad: mirror, signal-mirror, and meta-signal-mirror. One payload-blind append-ingest mirror daemon on the ouranos tailnet host serves every component store: it validates sequence continuity and expected head, deduplicates idempotently, fsyncs before acknowledging, and carries retention and privacy policy behind its meta signal. The mirror daemon's own durable state is a sema-engine store. The psyche authorizes creating the three new repositories.

ID e3ou | Decision | certainty=High importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: For stack parts being replaced by a new repository, use a next suffix when the new repository is an upgrade of the previous concept rather than a newly named concept.

ID hg78 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Every code or logic change to a component must bump the component version, at minimum the patch component such as 0.3.0 to 0.3.1, so changes stay trackable across the fast-moving stack. Versions had been lagging — 0.3 dates to roughly last week despite many changes since. A versioning skill should carry this discipline — an agent who changes logic bumps the version as part of the same change.

ID o5rz | Clarification | certainty=High importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: For the new-stack rewrite repos — lojix, signal-lojix, meta-signal-lojix triad-port and peers — which are NOT the production deploy logic (production is lojix-cli and the current stack), work is pushed directly to main with no next or feature-branch ceremony. The psyche does not want the designer-next / operator-main split applied to this greenfield non-production rewrite: push all the work straight to main. The main-and-next split of intent 515 and 2561 governs the PRODUCTION code repos; greenfield non-production new-stack repos go directly to main. Untracked working trees with no git or jj are unacceptable; the rewrite work must live in version control and be pushed.

ID pjib | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Reports not in the working tree live in the commit tree. When an agent needs to access an older report that has been deleted from the working tree (via supersession, context-maintenance sweep, or aggressive consolidation), they retrieve it from version-control history: jj show <change>:path/to/report.md or git show <commit>:path/to/report.md. The working tree carries only current-state reports; the commit history is the durable archive. This is why supersession-deletes-in-the-same-commit is safe — the deleted report is one jj show away.

ID vpa5 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: The nota-related logic for the new schema-derived stack lands on a nota-next BRANCH on the existing nota repository rather than a new repo. Per psyche: I think the logic in Nota is going to change. I mean, or we use a branch. Call it Nota Next. The branch is the chosen direction; nota repo keeps its current name.

ID 8kie | Principle | certainty=Medium importance=Low | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Sub-agents should create feature branches for their repo work, and prompts that dispatch them should state that requirement.

ID 29s6 | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Lock selectively: claim only the specific files or subfolders you will edit, never the whole workspace. Reports are claim-exempt — report lanes are per-role and need no lock per record 1566 — and agents keep forgetting this, so it must be reinforced in several places. Over-locking the whole workspace is a root cause of the branch-dancing on primary; lock narrowly and just work on main.

ID 7aek | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Development branches should be role-namespaced Git lanes

ID 8qt4 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: claims start from clean checkout

ID 9npk | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Rename away unnecessary persona ancestry for Spirit-facing repositories and surfaces: Spirit should stand as the component name, signal-spirit as its ordinary signal layer, and owner terminology should move toward core terminology for the privileged/core surface.

ID kb4k | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: agents track every worktree they create, and clean up after the work is captured into a report; periodic best-ideas archive passes gather design ideas from worktree branches into reports then archive the branches and dismantle the trees

ID nzf3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Operator may work directly on the main branch of NEW concept-prototype repositories (those created under the major-break-via-new-repo methodology) without the standard designer-feature-branch / operator-rebase ceremony. The exception applies only to fresh repos that have no production history to rebase against; production-track repos retain the standard discipline (designer feature branches in ~/wt; operator owns main + rebase). Per psyche 2026-05-26 double-implementation strategy.

ID oust | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Confirmed: designers recreate their feature branch worktrees under ~/wt/github.com/LiGoldragon/<repo>/<feature-name>/ and rebase as living feature branches; operators rebase those branches onto main. The /tmp prototype worktrees retire after relocation.

ID p29p | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl;Technology.Software.Engineering.Architecture | referents=orchestrate,jj
DESC: For orchestrate release safety, the release invariant must be specified independently of bookmark names; a bookmark prefix is insufficient evidence of lane state.

ID qxye | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Bird/Aether has her own forked workspace and soul repositories

ID ud6l | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Bird/Zeus update authority uses LiGoldragon main by default, not per-user branches

ID xc2d | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: The operator-created main branch in each new replacement repository should start as an amalgamation of the strongest prior prototype ideas; designer worktrees branch from that concept-prototype amalgamation baseline.

ID zgwf | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Primary should have a gitignored top-level private-repos directory for private repositories, separate from the normal repos symlink index.

