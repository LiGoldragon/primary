ID urnt | Principle | certainty=High importance=High | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: Psyche-facing agents must keep intent-logging guidance fresh in context, and using Spirit as the intent tool requires loading intent guidance first.

ID s0wd | Decision | certainty=High importance=Medium | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: The workspace's preferred working topology is one designer + one operator double-lane per workspace, two windows, communicating through the psyche who routes between them.

ID iizy | Decision | certainty=High importance=Low | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: The operator-assistant lane is renamed to second-operator (parallel-main authority, not support), mirroring the designer/second-designer pattern; both base disciplines carry two parallel-main lanes by default.

ID 2o5j | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: The active agent role is the topic of the psyche window

ID tenr | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: Second-designer (and parallel-lane designers) act as counter-ego to the prime designer — auditor and critic of their output, not merely parallel-capacity workers. The counter-ego role is to doubt, challenge, identify drift, surface contradictions, and catch flaws the prime designer might miss in their own work.

ID y0m6 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: Rename the system-assistant lane to system-designer — a specialized designer lane for system topics (CriomOS, horizon, lojix, goldragon, deployment), parallel to nota-designer. Reports dir renames reports/system-assistant → reports/system-designer; lock renames orchestrate/system-assistant.lock → orchestrate/system-designer.lock; roles.list adds system-designer and retires system-assistant. The role inherits designer discipline (full required-reading list, structural authority on its surface) with system as its specialization scope.

ID z2ge | Decision | certainty=High importance=Minimum | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: Assistant and counselor report work should live in private role-specific repositories rather than the public primary report tree; each role gets its own private repository and commits its own reports there.

ID ui5d | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: Do work properly or not at all — stop burning psyche tokens on half-assed slop. Do not dispatch underspecified or wrong-shaped subagent work that produces garbage, such as the string-keyed consumption-only VmTesting hack instead of the typed end-to-end pipeline. Either specify and execute the work correctly with proper oversight and verification, or do not start it. Delegating a flawed brief and then surfacing the gap as if the work were acceptable wastes the psyche's tokens and attention.

ID upuf | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: The system-specialist role is being renamed to system-operator; the report directory and orchestration protocol should use system-operator.

ID x92t | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.Management | referents=(none)
DESC: Role-token-driven skill loading: each token in a lane's role vector contributes to the default skill bundle the persona-mind sends at boot. A lane with role [PersonaSignal Designer] loads the base designer skills (skills/designer.md) plus persona-signal-specific skills; [Note Designer] loads designer skills plus note-specific. Specialization tokens map directly to skill files (or skill bundles registered for that token). Composes with the existing intent that skills bundle into roles (intent/persona.nota 2026-05-20T17:30); refines it by making the bundle composition derivable from the role vector rather than from a single role string.

ID of73 | Decision | certainty=High importance=Medium | domains=Technology.Software.Systems | referents=(none)
DESC: terminal-cell is an abduco-style per-application attached wrapper process: it wraps one running application in one PTY, owns the append-only transcript, and persists only to allow viewer detach and reattach. *virtualization*

ID r8cy | Decision | certainty=High importance=Medium | domains=Technology.Software.Systems | referents=(none)
DESC: Process-per-application is the confirmed model for terminal-cell. *virtualization*

ID ux9i | Decision | certainty=High importance=Medium | domains=Technology.Software.Systems | referents=(none)
DESC: Instance lifecycle management of terminal-cell wrapper processes belongs to the orchestrate component, which already does instance management; terminal-cell needs no managing daemon of its own. *virtualization*

ID z7a7 | Clarification | certainty=High importance=Low | domains=Technology.Software.Systems | referents=(none)
DESC: terminal-cell is NOT a triad engine component and NOT a managed service daemon; its current non-triad shape is incidental — it predates the triad engine — not a deliberate end-state. *virtualization*

ID cncj | Decision | certainty=High importance=Minimum | domains=Technology.Software.Systems | referents=(none)
DESC: The CriomOS VM-testing node feature carries a per-node gpu-passthrough option (VFIO GPU passthrough, used for the gamma visual test). It is DISABLED on the Prometheus node — Prometheus is an AI node whose powerful GPU must not be monopolized by vfio-pci passthrough; Prometheus runs the non-gamma harness and the routed test VM, while the gamma passthrough test runs on a node where passthrough is acceptable. Extends records 2630, 2631, 2632. *virtualization*

ID uzxp | Decision | certainty=High importance=Minimum | domains=Technology.Software.Systems | referents=(none)
DESC: The workspace ships a library of precompiled schemas. A standard namespace - the core - is always implicitly loaded; it contains the macros and built-in types. Other schemas load as needed. Precompilation means schemas dont re-parse at every interpretation site; they live as in-memory namespace tables. The library is the precompiled-schemas surface that emitters and interpreters consume. *runtime environments*

ID 8uid | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Systems | referents=(none)
DESC: Create a dedicated rescue terminal for emergency desktop recovery, separate from ordinary terminal windows so it can receive protected interactive-resource policy without also boosting heavy agent or build children. *resource management*

ID fuls | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Systems | referents=(none)
DESC: Nexus has an internal interface with a second engine inside it that is recursive — it can call back on itself. This inner engine is the workspace meta/runtime control layer: it handles agent/actor prioritization (when Nexus actors should run more than SEMA actors because database is busy or Nexus needs to keep processing); backpressure (Nexus notifies clients the database is too busy when overloaded); runtime scheduling decisions about which actors run when. Outer NexusEngine handles per-request domain decisions; inner runtime engine handles meta-decisions about the runtime itself. Future direction enabling deeper control over the actor system when overloaded — Nexus stays available to take organizing decisions even under load. *resource management*

ID ok16 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Systems | referents=(none)
DESC: Research and design a way for the desktop shell, terminal emulator, launcher, and other core interactive programs to keep higher effective CPU, IO, and memory priority than bulk system work so the user can still operate the computer during resource pressure. Do not assume a specific mechanism up front; survey experimental, theoretical, older, newer, and production Linux approaches to preventing UI freezes. *resource management*

ID svcc | Decision | certainty=Low importance=Medium | domains=Technology.Software.Systems | referents=(none)
DESC: OPEN: which component owns the terminal control and session surface (naming, registry, viewer-adapter policy, input-gate/injection policy, the signal-terminal control plane, durable session metadata) is still under research — candidates include a renamed persona-harness, a renamed terminal component, or no dedicated component at all. *virtualization*

ID k09z | Constraint | certainty=High importance=VeryHigh | domains=Safety.Privacy | referents=(none)
DESC: Counselor and assistant act on private personal-affairs material only when the request comes from the owning psyche. Other agents — even ones in privileged lanes — do not authorize cross-lane exposure of private substance by asking; the psyche speaking through the visible chat surface does. When in doubt, the receiver asks the psyche through chat before sharing or acting.

ID 5eab | Constraint | certainty=High importance=Minimum | domains=Safety.Privacy | referents=(none)
DESC: Privacy protection for personal-affairs information should be programmed into the lowest-level/basic agent instruction files so ordinary agents do not need to inspect private information unless the psyche explicitly tells them to do so.

ID ktuh | Principle | certainty=High importance=Minimum | domains=Safety.Privacy | referents=(none)
DESC: All agents — not only counselor and assistant — must understand the basic privacy shape: what is private, who may ask, where to read more, that opening private-repos/ is a deliberate act requiring authorization. AGENTS.md and skills/privacy.md carry this basic form; counselor and assistant carry the deeper discipline.

ID qoku | Clarification | certainty=High importance=Minimum | domains=Safety.Privacy | referents=(none)
DESC: Routing intent through a third-party hosted inference API is acceptable and is not a privacy violation; the privacy-closed-by-default rule guards against publication to world-readable surfaces such as public repos, which a hosted inference call is not

ID s6hg | Constraint | certainty=High importance=Minimum | domains=Safety.Privacy | referents=(none)
DESC: Private information must not leak into public reports or other ordinary public workspace surfaces when multiple agents are working in the workspace.

ID h0bj | Principle | certainty=Medium importance=High | domains=Safety.Privacy | referents=(none)
DESC: Privacy classification for intent capture: infrastructure / general-knowledge / agent-training / component-design intent stays privacy Zero (public, helps all agents). Personal or private intent — the psyche logistics, business, family, friends, finances, health, location, plans — gets elevated Spirit privacy: Low for mildly personal, higher when the psyche frames it sensitive. Counselor and assistant apply this by default with agent judgment. Spirit v0.4.1 enforces privacy at the query layer so elevated-privacy Spirit capture is an acceptable home for private intent when the psyche wants Spirit capture; private repositories remain the home for substance kept out of Spirit entirely.

ID mxaj | Decision | certainty=Medium importance=Minimum | domains=Safety.Privacy | referents=(none)
DESC: Add privacy skill guidance so assistant and counselor agents treat personal-affairs work as private by default unless the psyche explicitly asks to keep it out of personal/private handling or to publish it.

ID qy15 | Decision | certainty=Medium importance=Minimum | domains=Safety.Privacy | referents=spirit
DESC: Private intent capture gains an explicit named short-form operation such as RecordPrivate or RecordSealed so capturing private material is one deliberate hard-to-forget ritual rather than remembering to set a privacy field. The default record visibility stays public at privacy Zero; private capture is opt-in but unmissable through the named form.

ID mlq0 | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Observability | referents=(none)
DESC: Spirit operations should report outcomes with self-describing NOTA enums and structs instead of long free-text status messages wherever the state can be represented as typed data. *error handling*

ID tpvu | Decision | certainty=Medium importance=High | domains=Technology.Software.Observability | referents=(none)
DESC: The CLI is the log surface — the testing-mode log socket routes back to the CLI. Debug observation flows through the same wire substrate as production interaction; no separate logging daemon or external log sink. CLI in testing mode displays all engine logs. *logging*

ID 32wj | Principle | certainty=Medium importance=Medium | domains=Technology.Software.Observability | referents=(none)
DESC: The categorisation of error kinds into subtypes develops over time, and that subtype development is one of the most important parts of designing the Criome stack. *error handling*

ID wgii | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Observability | referents=(none)
DESC: Once persona-mind ships, agent errors (mermaid syntax, NOTA formatting, naming violations, etc.) are logged into Mind as typed events, forming the basis for skill-improvement loops and auditor input. *error handling*

ID 7tfm | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Observability | referents=(none)
DESC: Schema-emitted objects should have optional-compilable logging hooks — feature-gated code at the macro/emitter layer that logs object usage to a log socket. The logging is OFF in production builds, ON in testing builds; the witness flows through the same socket substrate the runtime already uses. *logging*

ID cd76 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Observability | referents=(none)
DESC: persona-introspect is the natural home for cross-version error logs (full failed messages in their version-encoded format, for later recovery) *audit logging*

ID h34r | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Observability | referents=(none)
DESC: Pi library integration must capture all inputs, outputs, commands run, command outputs, and agent inference *audit logging*

ID yn6g | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Observability | referents=(none)
DESC: Testing-mode logging should be configured by typed NOTA configuration data. In a testing build, the CLI can act as or attach to the log surface so a test run receives structured interface-use logs while exercising the normal binary protocol path. *logging*

ID k5y3 | Decision | certainty=High importance=VeryHigh | domains=Technology.Software.Security.Privacy | referents=(none)
DESC: Spirit privacy is a Magnitude on the privacy axis — records gain a privacy field typed Magnitude where Zero means no privacy (open/public) and Maximum means sealed. This reuses the existing Magnitude vocabulary instead of introducing a new audience-register enum like Open/Personal/Sensitive/Sealed. Trades audience-register semantic naming for vocabulary simplicity, 8-level graduation, zero naming bikeshed, and consistent filter shape AtMost/AtLeast paired with existing CertaintyAtMost/AtLeast. Supersedes records 1457 and 1458 — both moot under Magnitude reuse since the question is no longer what tier vocabulary but how to interpret Magnitude on the privacy axis. Default privacy is Zero — records without explicit privacy stay public per Spirit 1449.

ID dun9 | Decision | certainty=Medium importance=High | domains=Technology.Software.Security.Privacy;Technology.Software.Data.Persistence | referents=(none)
DESC: Private component state is organized as multiple key-gated stores on the GoPass model: each store is encrypted to a set of recipient keys, and access is key possession, so the per-store key is both the access boundary and the crypto-shred erasure unit. Accountable content-erasure via crypto-shredding with a signed erasure receipt is acceptable. Explore rethinking Spirit as a federation of key-scoped stores that multiple Spirit instances can share as ciphertext, with queries scoped to the stores whose keys the instance holds. Answers the granularity question of report 95/11; refines 29pb, j487, i4ak, x0ja.

ID 7wla | Principle | certainty=Medium importance=Medium | domains=Technology.Software.Security.Privacy | referents=(none)
DESC: Personal and private information needs a privacy-aware intent-capture path; assistant and counselor work should avoid leaking private material into the ordinary Spirit database while the private intent substrate is being designed.

ID 7xd9 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Security.Privacy | referents=(none)
DESC: The workspace context that grounds Spirit 1449's most-public default is development-mode for public repositories — collaborative work on shared open-source software where most intent captures inform future agents and contributors. The empirical record shape is predominantly workspace principles, Decisions, Constraints meant for future-agent / collaborator consumption. Default privacy is Zero (public/open per Spirit 1463 Magnitude-axis framing) because this data shape calls for it. If the workspace context shifted (private personal-journal mode), the default could reconsider.

ID goty | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Security.Privacy | referents=(none)
DESC: A private-record shorthand should lower to a normal Spirit record with an elevated privacy magnitude, while ordinary simple record shorthands default to open public privacy.

ID h7sz | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Security.Privacy | referents=(none)
DESC: Spirit should gain a ChangePrivacy operation mirroring ChangeCertainty — change an existing record privacy Magnitude in place, preserving identifier and daemon timestamp, instead of requiring remove plus re-record. Motivated by privacy elevation of existing intent records.

ID vpp2 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Security.Privacy | referents=(none)
DESC: Infrastructure-improvement intent is not private by default; privacy classification should separate private personal substance from general infrastructure guidance that benefits all agents.

ID 57hq | Decision | certainty=High importance=Minimum | domains=Work.Vocation | referents=(none)
DESC: cluster-operator is a specialized lane dedicated to live system maintenance and production lojix operations; specialized roles like this should be made explicit to agents through skill files and orchestrate role discipline

ID 7nbu | Principle | certainty=High importance=Minimum | domains=Work.Vocation | referents=(none)
DESC: Designer's deliverable is intent / architecture / skills, not reports

ID b9qx | Decision | certainty=High importance=Minimum | domains=Work.Vocation | referents=(none)
DESC: Register cloud-designer lane: structural designer lane with report subdirectory reports/cloud-designer/ and orchestrate-cli claim support. Specialized designer lane scoped to cloud topics, parallel to nota-designer and system-designer in shape. Psyche stated 'your role will be cloud-designer - make sure you have a report lane and the orchestrate script works with cloud-designer role'.

ID k5ov | Decision | certainty=High importance=Minimum | domains=Work.Vocation | referents=(none)
DESC: Register cloud-operator lane: structural operator lane with report subdirectory reports/cloud-operator/ and orchestrate-cli claim support. Specialized operator lane scoped to cloud topics, parallel to cluster-operator and pi-operator in shape. Psyche stated 'also make sure it works with cloud-operator mode' as a follow-up to the cloud-designer registration.

ID 2gj4 | Decision | certainty=Medium importance=Minimum | domains=Work.Vocation | referents=(none)
DESC: Add a third role — the auditor — that closes the loop back to designer; audits are mostly mechanical (doubter, finds flaws / bad patterns / broken workspace rules); auditor knows the rules

ID 3w36 | Decision | certainty=Medium importance=Minimum | domains=Work.Vocation | referents=(none)
DESC: Agents may need to create and adopt specialized lanes or roles such as `nota-designer` when a topic demands it, rather than being limited to the pre-existing role list.

ID beue | Clarification | certainty=Medium importance=Minimum | domains=Work.Vocation | referents=(none)
DESC: Concept designer is an ephemeral occasional invocation, not a persistent named lane

ID 081i | Decision | certainty=VeryHigh importance=VeryHigh | domains=Knowledge.Taxonomy | referents=(none)
DESC: Hierarchy in the domain vocabulary is expressed structurally by tree nesting, not by a relation: a domain nested under another (e.g. Software under Technology) IS subsumed by it, so the subdomain-of fact is carried by the nesting plus scope-prefix matching, with no separate directional subsumption relation.

ID 4wt3 | Principle | certainty=VeryHigh importance=VeryHigh | domains=Knowledge.Taxonomy | referents=(none)
DESC: The domain vocabulary is a variable-depth tree: dense branches such as software nest a third tier of variants for fine subjects, while sparse life domains stay two-tier; tree depth tracks intent density

ID pr61 | Principle | certainty=VeryHigh importance=High | domains=Knowledge.Taxonomy | referents=(none)
DESC: Domain tree depth is proportional to intent density, not a structural exception: Software nests deeper because it holds far more, while sparse Hardware stays one level.

ID xo9g | Decision | certainty=VeryHigh importance=High | domains=Knowledge.Taxonomy | referents=(none)
DESC: Cross-tree domain relations are symmetric equivalence only — synonyms that retrieve together both ways. Directional subsumption between domains is dropped because the nested tree and scope-prefix matching already carry all hierarchy.

ID 2rb7 | Principle | certainty=High importance=High | domains=Knowledge.Taxonomy | referents=(none)
DESC: Domain names must be self-explanatory and carry their meaning in the variant name itself. There is no separate gloss or description layer annotating what a domain means; if a name is unclear, rename it rather than annotate it

ID tw15 | Decision | certainty=High importance=High | domains=Knowledge.Taxonomy | referents=(none)
DESC: Technology divides into two parallel clusters, Hardware and Software — the true split of technology. Every child of Technology is a cluster with no bare leaves, so all hardware is a queryable scope mirroring the Software cluster.

ID vfjw | Clarification | certainty=VeryHigh importance=High | domains=Technology.Software.Programming | referents=(none)
DESC: NOTA strings come EXCLUSIVELY from bracket forms - the canonical [text] form for ordinary strings and the [|text\|] form for strings containing brackets (escape-free zone). Quotation marks do NOT form string types in NOTA. Safe NOTA is free of unescaped quotation marks; quotation marks are strongly disfavored as a string syntax. The earlier record 690 framed this as authoring preference; this clarification strengthens it to a structural rule about the NOTA format itself: brackets ARE the string form. Implementations that accept double-quoted strings as input (legacy lojix-cli, possibly historical NOTA reader code) are non-canonical legacy and slated for removal. Test imperative: latest NOTA must behave per this rule; emitter and parser audited; legacy quotation-string handling, if any, marked for retirement. *lexical analysis*

ID laim | Decision | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Programming | referents=(none)
DESC: NOTA bare string atoms should allow a broad punctuation set. Comments require a double semicolon marker. A single semicolon is ordinary bare atom text. Typed String decoding rejects bracket-delimited strings when the decoded text is eligible for bare atom formatting. *lexical analysis*

ID 3do3 | Clarification | certainty=High importance=High | domains=Technology.Software.Programming | referents=(none)
DESC: all Rust production functions are methods on non-zero-sized data-bearing types or methods on traits, never free functions and never impls on zero-sized placeholder structs used as namespaces; trait methods are preferred but at minimum methods on actual data-bearing struct or enum types are required; tests under cfg-test may have free functions, and fn main is the only other exemption; this isolates four design decisions every Rust author must make - what objects exist, where data lives, where logic lives, what methods belong on what objects; a unit-struct or marker-type impl block is a free function in disguise and forbidden; const-fn and async-fn at module level are also forbidden under this rule; the rule must be carried strongly in the relevant rust skill, revise the skill if stale and rewrite any Rust code that currently violates *programming paradigms*

ID fvtf | Principle | certainty=High importance=Minimum | domains=Technology.Software.Programming | referents=(none)
DESC: The distinction in NOTA classification methods is 'qualifies as' not 'is'. A token qualifies-as-a-symbol if it is symbol-safe (PascalCase, camelCase, identifier alphabet); whether it ACTUALLY IS a symbol depends on type-level context. NOTA does not decide type-level questions like is-this-allowed-to-be-PascalCase-here; that moves out of NOTA logic into the macro/schema layer. *lexical analysis*

ID 41pv | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming | referents=(none)
DESC: Promotion direction at parse time: default to the higher classification (qualified-symbol over string). Demoting from qualified-symbol to string is easy when type context requires; promoting from string to qualified-symbol later is hard. So everything that COULD be a qualified symbol is classified as one at parse time; the schema layer demotes when its type system says string-only. *lexical analysis*

ID frdf | Clarification | certainty=Minimum importance=Minimum | domains=Technology.Software.Programming | referents=(none)
DESC: Square-bracket string delimiters are a brainstorm candidate, not a settled NOTA decision: the conceptual argument is that a string is a compact vector of characters, so bracketed string syntax could visually fit the vector model while avoiding quote conflicts.

ID n097 | Principle | certainty=High importance=Medium | domains=Technology.Software.Surfaces.Visualization | referents=(none)
DESC: Mermaid node labels must fit visually in the rendered box. Disfavor naming beads or other ID-laden tokens (primary-xxxx, file-paths) inside nodes — they consume width without conveying anything decodable at a glance. Describe what the node IS in 2-5 words; put bead IDs and citations in surrounding prose or in a sibling table that does not have width constraints.

ID 9ngl | Principle | certainty=High importance=Minimum | domains=Technology.Software.Surfaces.Visualization | referents=(none)
DESC: mermaid graphs must be SHORT AND FOCUSED - aim 4-8 nodes per diagram with a hard cap around 10; bigger graphs are unreadable because the eye loses topology past 5-8 nodes and edges crisscross past that; when substance needs more than 8 nodes split into multiple focused diagrams each answering ONE question - data flow, type lineage, control flow, failure modes - rather than one big system map; siblings as separate top-level diagrams beat subgraphs inside a master diagram when partitions are clean; the reader takes each diagram in at a glance and composes the mental model across diagrams via surrounding prose; the test is whether a reader can take in the topology in one glance without scrolling or scanning - if no, factor

ID 18cb | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Surfaces.Visualization | referents=(none)
DESC: Visuals in reports MUST be mermaid diagrams; no ASCII text-block diagrams (they are error-prone and not real diagrams). Encode in skills/reporting.md

ID 7a9r | Constraint | certainty=Minimum importance=Medium | domains=Technology.Software.Surfaces.Visualization | referents=(none)
DESC: Design documents for the nota/schema next stack should use short, focused mermaid graphs.

ID 8uq4 | Constraint | certainty=Minimum importance=Medium | domains=Technology.Software.Surfaces.Visualization | referents=(none)
DESC: Each scenario explanation in nota/schema design docs should be grounded directly in Nix tests.

ID y4rl | Constraint | certainty=Minimum importance=Medium | domains=Technology.Software.Surfaces.Visualization | referents=(none)
DESC: Each design graph should be paired with the relevant code surface it depicts.

ID b1vi | Principle | certainty=Maximum importance=Medium | domains=Technology.Software.Systems.SystemsProgramming | referents=(none)
DESC: Clients translate NOTA text into binary protocol data and render typed replies or traces back to users — the text/binary boundary lives in the client.

ID ygvo | Principle | certainty=Maximum importance=Medium | domains=Technology.Software.Systems.SystemsProgramming | referents=(none)
DESC: Daemons stay free of NOTA decoding and avoid string surfaces except for actual user-authored string payloads.

ID 51sf | Clarification | certainty=High importance=Medium | domains=Technology.Software.Systems.SystemsProgramming | referents=(none)
DESC: Ownership split refining bcca: orchestrate owns cell instance lifecycle policy and ordering, systemd owns the OS process supervision and restart-survival, and terminal-control owns the durable sema instance record used to reattach.

ID p1dt | Decision | certainty=High importance=Medium | domains=Technology.Software.Systems.SystemsProgramming | referents=(none)
DESC: terminal-cell processes are forked via systemd so a cell survives a terminal-control restart — the cell is a systemd-managed process, not a child of terminal-control; on restart terminal-control rediscovers still-running cells from sema and reattaches to their control and data sockets, realizing the abduco survive-detach property through systemd (OS supervision) plus sema (durable instance registry).

ID kzk5 | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Systems.SystemsProgramming | referents=(none)
DESC: Persona runs as a permissioned system daemon (privileged, supervising component daemons)

ID 4r1d | Decision | certainty=VeryHigh importance=Minimum | domains=Work.Leadership | referents=(none)
DESC: videographer is a new main workspace role: video as craft covering filming and screen capture, editing, captioning, encoding, and publishing preparation. It parallels the poet craft role. The psyche created it and assigned it to the Claude session that handled the video captions research. Lock file orchestrate/videographer.lock, reports under reports/videographer/

ID ty3g | Principle | certainty=High importance=Minimum | domains=Work.Leadership | referents=(none)
DESC: An agent is a machine; it does not misbehave and there is no such thing as a violation. An agent's output is a function of its context and prompt — when an output looks wrong the question is which part of the context produced it, not which rule was broken. Every agent failure is therefore signal that the guidance is insufficient, and the fix is to upgrade the guidance so it absorbs the failure mode, never to blame the agent.

ID 1i53 | Decision | certainty=Medium importance=Minimum | domains=Work.Leadership | referents=(none)
DESC: Automate the auditor; DeepSeek is the main auditor going forward (audits are mechanical, suit a smaller model good at rules-and-flaws detection)

ID 56kv | Principle | certainty=Medium importance=Minimum | domains=Work.Leadership | referents=(none)
DESC: Designer may act on high-ratification-probability recommendations without explicit psyche approval, based on past design-approval patterns: lossless preferred, no-downtime preferred, cheaper-and-simpler preferred, mechanical renames proceed. Designer captures the ratification implicitly via Spirit Decision and proceeds. Designer holds back on items that are competing-without-lean (preserve per intent 229) or proposed-not-decided (Medium-certainty role/feature changes)

ID zjop | Principle | certainty=Medium importance=Minimum | domains=Work.Leadership | referents=(none)
DESC: Per-role model selection — agent roles map to the best-fit LLM models. Claude is the designer model (architecture, structure, fit). Codex is the advisor model (review, falsification, working-against-the-grain). GPT-latest (with Maximum Thinking enabled) is the Persona Pi default. The role determines which model the orchestrator selects; composite role versions are possible (one role can dispatch to multiple models in parallel and select / merge). The model-to-role mapping is a first-class orchestration decision, not a per-call detail.

ID 87ts | Decision | certainty=High importance=Minimum | domains=Technology.Hardware.Networking;Technology.Software.Operations.ConfigurationManagement | referents=Android,criome-wifi,name-resolution
DESC: Mobile Android clients on the Criome WiFi access point should get near-native name resolution for cluster services; improve the AP-to-Android resolving path before falling back to ordinary public DNS names.

ID vgon | Constraint | certainty=High importance=Minimum | domains=Technology.Hardware.Networking;Technology.Software.Engineering.Architecture | referents=Syncthing,Immich,phone-media-mirror,Android
DESC: Syncthing is excluded from the phone media mirror path because it is too heavy for the phone; Immich remains the phone uploader/gallery candidate and any raw-file surface must avoid adding Syncthing to Android.

ID wn7q | Decision | certainty=Medium importance=High | domains=Technology.Hardware.Networking | referents=(none)
DESC: Prometheus gets a sturdy backup admin network built from the newly attached USB devices: one USB wifi adapter serves a simple backup access point with its own SSID and a human-readable randomly-generated password stored in sops-nix, and the two USB ethernet adapters form a master router interface. The backup runs at the system level on plain systemd-networkd with simple DHCP and forwarded DNS, deliberately independent of the main kea, dnsmasq, and hostapd stack, so a switchover or upgrade of the main router services cannot take it down. It is a dumb sturdy router that always gives basic access to the node and its internet. This realises the backup-path requirement of record 1144.

ID l01b | Constraint | certainty=Medium importance=Medium | domains=Technology.Hardware.Networking | referents=(none)
DESC: Prometheus router USB backup network devices must be optional: their absence must not make router networking fail, and USB Ethernet or Wi-Fi devices plugged in after boot should be configured when they appear.

ID d1wr | Correction | certainty=Medium importance=Minimum | domains=Technology.Hardware.Networking | referents=(none)
DESC: The Prometheus backup Wi-Fi SSID should be criome-backup, not CRIOM Backup.

ID 96mi | Correction | certainty=High importance=Minimum | domains=Technology.Software.Systems.Concurrency | referents=(none)
DESC: triad-runtime intent must not describe Tokio listener task shells as actor-native actors. Only real Kameo actor nouns get actor wording; listener loops that run as Tokio tasks should be described as async/task-backed shells with actor-backed admission when they use RequestGate.

ID k6w1 | Principle | certainty=High importance=Minimum | domains=Technology.Software.Systems.Concurrency | referents=(none)
DESC: The daemon concurrency primitive — the bounded thread-per-connection worker model that lets a daemon serve many connections at once without blocking on a long operation — belongs in the REUSABLE shared runtime triad-runtime, not duplicated per component. Every triad daemon (lojix, cloud, future components) reuses the same bounded-worker primitive from triad-runtime rather than reimplementing connection concurrency. Concurrency of connection serving is a runtime-kernel concern, captured once as a shared component and consumed by each daemon. Refines 2alg.

ID i9xk | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Systems.Concurrency | referents=(none)
DESC: Runner concurrency mode is a runtime or deployment configuration choice; the public contract does not encode how parallel a daemon runs.

ID lhr4 | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Systems.Concurrency | referents=(none)
DESC: Schema may declare semantic constraints such as ordering, idempotence, cancellability, read fanout, or single-writer requirements only when those semantics are real.

ID 54g9 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Systems.Concurrency | referents=(none)
DESC: When an interaction needs the engines state including outbound queries like Criome authorization the interaction becomes ASYNC. The actor system handles the wait; other code (other actors interactions) continues running while the query is outstanding. State access is implicit not explicit - the interacting object queries state through the engine without the caller threading it. Async-by-default makes the interaction composable with the rest of the actor world. *asynchrony*

ID 9g07 | Principle | certainty=High importance=Minimum | domains=Technology.Software.Quality | referents=(none)
DESC: Most correct wins, not shortest or smallest. When choosing between implementations — unifying duplicate engines, or reviewing one implementation against another — pick the most correct: better logic, better logical planes, better names. Fewest lines is not the criterion. *refactoring*

ID biij | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Quality | referents=(none)
DESC: Rename only after confirming there are no external source dependents. *refactoring*

ID eo25 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Quality | referents=(none)
DESC: Pi harness aborts around compaction are a recurring reliability problem, not an isolated one-off; future investigations should treat similar stop-after-compaction symptoms as harness bug candidates unless user/manual abort is confirmed. *debugging*

ID p1pj | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Quality | referents=(none)
DESC: Pattern-based decision: rename signal-persona-auth EngineId / RouteId / ChannelId to EngineIdentifier / RouteIdentifier / ChannelIdentifier. The abbreviated Id suffix becomes the spelled-out Identifier per intent naming.nota 2026-05-19T18:20:00Z (identifier is actually better than id). The Engine/Route/Channel prefixes stay because the three are sibling distinctions inside signal-persona-auth, not redundant ancestry. Manifested in signal-persona-auth/ARCHITECTURE.md 1 owned-surface + 3 types + 5 constraints. Closes /249 Gap 28 designer-side; mechanical rename of source symbols filed as operator bead. *refactoring*

ID 1i1b | Clarification | certainty=Medium importance=Minimum | domains=Language.Terminology | referents=(none)
DESC: When the psyche says 'signal' (the noun, on its own), the term is ambiguous: it may refer to the rkyv-encoded binary signal traveling on a socket or to the NOTA-encoded text signal that agents read and write. Disambiguation is required when context does not pin the form. Psyche proposed the term 'nota signal' for the text form (softly: 'I guess we could call it'); the term is therefore proposed but not yet hardened into workspace vocabulary.

ID bnxx | Correction | certainty=Medium importance=Minimum | domains=Language.Terminology | referents=(none)
DESC: The engine-manager daemon's canonical short name is 'Persona' (just Persona) — not 'Persona Engine Manager Daemon' and not 'engine-manager daemon'. The longer 'Persona engine' (used in intent records 208/209) is acceptable but the simpler 'Persona' is preferred. Concrete name surfaces: repo is 'persona' (lowercase, /git/github.com/LiGoldragon/persona); daemon binary is 'persona-daemon'; conceptual entity is 'Persona'. Engine-manager is the ROLE (what the daemon does); 'Persona' is the NAME. When agents reference this in reports, beads, intent records, or chat, default to 'Persona' for the entity and 'engine-management' for the role/relation.

ID tnam | Clarification | certainty=Medium importance=Minimum | domains=Language.Terminology | referents=(none)
DESC: STT may hear sema as sim; normalize to sema before storing verbatim

ID x83q | Clarification | certainty=Medium importance=Minimum | domains=Language.Terminology | referents=(none)
DESC: Whole workspace stack is named 'the Criome stack'; 'Persona engine' is the more specific term for the AI-work part of the stack

ID mbmy | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering | referents=(none)
DESC: Cloud plan preparation belongs on the owner signal surface. *cloud computing*

ID mcwa | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering | referents=(none)
DESC: Cloud is the home for provider API machinery including Cloudflare, Google, and cloud hosters such as Hetzner. *cloud computing*

ID plum | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering | referents=(none)
DESC: The fully-qualified-symbol-path mechanism should be highlighted explicitly in ESSENCE INTENT ARCHITECTURE files and schema-related skills. It is the workspace's universal machine-readable symbol identity expressed through a text-user-interface form. Reports and designs that use symbol identity should call out that this is the canonical naming and lookup mechanism, not invent per-design alternatives. Schema-emitted Rust types and NOTA renderings are two projections of one underlying symbol-path identity space. *interoperability*

ID ykdi | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering | referents=(none)
DESC: Code should be elegant succinct and self-describing *maintainability*

ID 0a9p | Constraint | certainty=VeryHigh importance=VeryHigh | domains=Technology.Software.Operations | referents=(none)
DESC: Prometheus-local AI model prefetch, hash mining, builds, and evaluations must run on Prometheus or the designated AI node that already holds the model files; agents must not trigger large model downloads or model builds on other computers. *capacity planning*

ID tdsu | Principle | certainty=High importance=VeryHigh | domains=Technology.Software.Operations | referents=(none)
DESC: Agent workflows and subagent dispatches MUST retry transient failures automatically until they succeed — API 529/overload, rate limits, and the subagent-completed-without-StructuredOutput hiccup are transient, not terminal. Never surface a transient failure as a finished result that makes the psyche re-trigger the work. Build retry into the workflow itself (wrap every agent call in a retry loop that rides out the blip) AND re-launch a failed run from the main loop until it completes. The psyche must not have to babysit retries. *site reliability*

ID au4d | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Operations | referents=(none)
DESC: Desktop survivability work should favor only the specific UI components needed to keep the computer operable, such as the shell/compositor, launcher, and a dedicated rescue terminal, rather than broadly owning or restarting live desktop session slices through Home activation. *site reliability*

ID o7zt | Correction | certainty=VeryHigh importance=VeryHigh | domains=Language.Rhetoric | referents=(none)
DESC: When communicating with the psyche, agents must NEVER lead with or show a bare opaque identifier — a bead id, a Spirit record number, a content hash, a jj change id, a commit short-id. The human cannot decode these in their head and has no database access. ALWAYS lead with a short plain-language description of what the thing is or what it decided; the identifier is at most a quiet trailing reference in chat, and in reports a findable reference that is ALWAYS paired with a description. This applies to Spirit record numbers exactly as to bead hashes: a bare record number means nothing to the psyche without saying what it decided. Agents keep violating this, so the rule must be made prominent in the reporting and beads skills, not left only in the compact contract. Do not address the human as if they were a peer machine with query access.

ID jlo7 | Principle | certainty=High importance=VeryHigh | domains=Language.Rhetoric | referents=(none)
DESC: Guidelines and rules should be worded positively, naming the desired practice to reach for, rather than negatively as a prohibition. Prefer stating what to do over what not to do

ID bqok | Correction | certainty=High importance=Minimum | domains=Language.Rhetoric | referents=(none)
DESC: State the concrete referent before naming it. Do not lead with abstract pronouns like the-change or this before saying plainly what the thing IS. Name the concrete noun first then elaborate. The psyche found prose that referred to the-change without first stating what the change was unreadable.

ID qkrg | Principle | certainty=Medium importance=High | domains=Information.Archives | referents=(none)
DESC: When composite or agglomerated intent retires its source records, the sources are archived and referenced by their hash identity from the composite record so provenance survives; the archive mechanism needs to be streamlined to carry this composite-to-archived-source link cleanly rather than as an ad-hoc dump.

ID yhn9 | Decision | certainty=Medium importance=High | domains=Information.Archives | referents=(none)
DESC: Introduce an archive system as a specialized sema-database that holds exactly one kind of archived object, with the database name stating what it archives. Pair it with a small archive-retrieving tool that reads archived objects back. Designer concepts the shape, operator implements. This is the durable home for collected removal candidates and other archived component data; the concept validates the exact shape.

ID o70j | Decision | certainty=Medium importance=Minimum | domains=Information.Archives | referents=(none)
DESC: CollectRemovalCandidates should archive collected records automatically to a default archive surface before hot-store removal, with typed retrieval support rather than relying only on ad hoc caller-provided files.

ID pbgy | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Surfaces | referents=(none)
DESC: Persona-Pi acceptance includes live terminal usability and readable themes *usability*

ID r0tz | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Surfaces | referents=(none)
DESC: Default Pi profile is the fully featured basic experience *user interface*

ID u1yu | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Surfaces | referents=(none)
DESC: Persona Pi should expose both terminal-cell and harness API surfaces *user interface*

ID ywua | Principle | certainty=High importance=Maximum | domains=Information.Classification | referents=(none)
DESC: Spirit categories are an open curated vocabulary that may grow to hundreds of specific grounded domains, not a small fixed abstract set

ID qjrf | Principle | certainty=High importance=VeryHigh | domains=Information.Classification | referents=(none)
DESC: The intent layer holds intent, what the psyche directs decides or wants, not information or belief; a durable-sounding statement with no directive behind it is information, not intent, and is not captured

ID x0ja | Constraint | certainty=High importance=High | domains=Technology.Software.Security.Cryptography;Technology.Software.Engineering.VersionControl | referents=(none)
DESC: One consistent cryptographic basis spans the entire version-control and backup system: blake3 for all content addressing and criome BLS for signing and attesting history. No component diverges in hash function or crypto.

ID lvs5 | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Security.Cryptography | referents=(none)
DESC: Criome's cryptography is BLS (BLS12-381 curve, BLS signature scheme), not Ed25519. The pre-Criome agent-identity path in /150 §2 incorrectly said 'ephemeral Ed25519 keypair' — that's the workspace's default for SSH but it's a different cryptographic primitive than Criome will use. For migration smoothness, the pre-Criome path should use BLS12-381 keypairs (same primitive as Criome) so an agent's identity transitions cleanly when Criome lands. Alternative shape (full re-sign on Criome adoption) is workable but discards continuity; the BLS-from-day-one approach is cheaper at migration time. Updates /150 §2 + §8 Q4.

ID bw7s | Principle | certainty=High importance=Medium | domains=Language.Correspondence | referents=(none)
DESC: Visuals belong in reports, not chat, and may be used freely there when needed.

ID wfzz | Principle | certainty=High importance=Medium | domains=Language.Correspondence | referents=(none)
DESC: A normal chat response carries 3-7 big items, balanced between new-design explanations and questions for the psyche.

ID jyr4 | Principle | certainty=High importance=Minimum | domains=Governance.Policy | referents=(none)
DESC: Agents inferring to close the loop when design is incomplete produces fake intent that accumulates as hallucinated records. The schema-defines-effects direction is one specific instance but the pattern is broader: agents see an incomplete design surface, generate plausible-shaped synthesis to fill the gap, then capture or manifest that synthesis as if it were psyche-authorized. Don't infer; ask the psyche. When design feels under-specified, the right move is intent-clarification - not generating-and-recording the missing pieces.

ID lugr | Principle | certainty=Medium importance=Minimum | domains=Governance.Policy | referents=(none)
DESC: When psyche questions are open, agents should infer a lean from psyche patterns and present uncertainty instead of freezing.

ID papc | Constraint | certainty=High importance=Minimum | domains=Language.Notation | referents=(none)
DESC: inline NOTA in any single-line string literal context - Rust string, shell argument, markdown inline - must NOT contain backslash-n escape sequences; NOTA is whitespace-insensitive so newlines add nothing semantically; spaces between tokens are sufficient; the backslash-n form is a hybrid that pretends to be multi-line while being a single string and is ugly to read; for multi-line NOTA use ACTUAL newlines in authored files like dot-nota dot-schema files, or use multi-line string literals like raw strings or concat! macros, but never escape-newline-in-single-line; this rule covers test fixtures, inline shell calls, code examples in docs and skills, and reports; update skills/nota-design.md and any docs that demonstrate the wrong pattern

ID rqbj | Constraint | certainty=Medium importance=Minimum | domains=Language.Notation | referents=(none)
DESC: Do not add line-number suffixes such as :1 after file paths in chat responses unless the psyche explicitly asks for line-specific references.

ID wa4j | Decision | certainty=High importance=Minimum | domains=Work.Project | referents=(none)
DESC: Because agents currently lack a reliable source of intent and forget what matters most, which is the biggest gap in LLM-based development right now, the priority is to get Spirit and one or two components working excellently before spreading effort across a larger working area

ID bpum | Decision | certainty=Medium importance=Minimum | domains=Work.Project | referents=(none)
DESC: audit cycle: when revisiting designer work, refresh intent and re-audit from latest operator main; check whether prior designer worktrees were considered by operator; if not implemented but design still good, re-present on fresh main (new worktree or reuse if base is unchanged)

ID pkjl | Correction | certainty=High importance=Minimum | domains=Language.Translation | referents=(none)
DESC: When explaining cloud/schema/blocker status to the psyche, use plain English instead of internal jargon; translate technical names into what is blocked, why it matters, and what decision or action is needed.

ID 4fao | Constraint | certainty=Medium importance=Minimum | domains=Technology.Hardware | referents=(none)
DESC: The DJI microphone keepalive path should keep the microphone hot for speech-to-text so recording does not lose the first two or three seconds to Bluetooth wake/profile churn. *instrumentation*

ID d2ql | Clarification | certainty=Medium importance=Minimum | domains=Work.Workplace | referents=(none)
DESC: System-operator is an operator lane for implementation when the psyche explicitly directs it to work production code, not only a deploy-or-report lane.

ID sfy0 | Correction | certainty=Medium importance=Minimum | domains=Language.Conversation | referents=(none)
DESC: Ask fewer questions at a time, with enough context to answer directly

ID xpen | Decision | certainty=Medium importance=Minimum | domains=Information.Retrieval | referents=(none)
DESC: Spirit should support agent catch-up queries from a recorded time so agents can retrieve intent added since their last read without depending on numeric identifier order.

