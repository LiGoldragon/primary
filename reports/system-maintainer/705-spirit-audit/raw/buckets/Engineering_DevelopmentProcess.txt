ID 2o3g | Constraint | certainty=Maximum importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Agents do not use subagents unless explicitly told to by the psyche. This applies to all agents in this workspace and belongs in the agent instruction file (AGENTS.md) as a per-keystroke override.

ID 4728 | Correction | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Operator-lane subagents must be treated as asynchronous side work: dispatch returns control to the main agent, and the main agent should keep working or answer new psyche prompts instead of blocking on subagent completion. Waiting for a subagent is only acceptable when explicitly requested or when the psyche is not waiting on the main agent for anything else.

ID g78b | Correction | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: When the psyche says refresh intent, it means the agent should refresh its own context by reading recent Spirit intent records; it is not an instruction to edit repo INTENT.md or architecture files unless the psyche explicitly asks for file edits.

ID krez | Correction | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Work orders are not intent records

ID d0mp | Principle | certainty=High importance=High | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: The Criome workspace operates as an intent-and-design-driven engine: a back-and-forth of designing and intending. When intent is clear AND design is good enough, implement. Designer and operator are the two halves of that dance

ID ng1x | Principle | certainty=High importance=Medium | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: A component migration is not complete until the superseded implementation dependency and execution path being replaced are removed; wrapping an old path with new scaffolding is not enough to claim migration. *maintenance*

ID vqtd | Principle | certainty=High importance=Medium | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Methodology for major architectural breaks: create a new repository with a -next or -v2 (or other longer descriptive) suffix rather than working on a branch of an existing repo. The longer name signals prototype/breakthrough status; the new repo can be developed in parallel without disrupting production work on the old repo; it may start as a fork. After the break stabilizes, rename to the canonical short name (replacing the original). Makes parallel development sane for agents - they work on different repos rather than navigating branch states. Per psyche 2026-05-26 directive: this methodology should be made into a basic skill.

ID 7gcs | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Constraints are among the most important parts of architecture: emphasize writing constraints when manifesting design into architecture, because a stated constraint lets us write a test that verifies it. Prefer expressing design as constraints so they become testable. *requirements*

ID fc7l | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Do not keep the Asschema compatibility projection or old Asschema-facing APIs around after the schema-in-Rust source path supersedes them; remove the compatibility surface rather than preserving old API shape. *maintenance*

ID j81n | Correction | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Actions are taken, not announced. When the agent would write ready to close / ready to merge / ready to ship or similar workflow-state phrasing in chat, it instead either does the action itself or asks a specific question naming the choice. Ready to X with neither action nor a clarifying question is empty noise — it leaves the psyche guessing whether the agent is reporting status, asking permission, or asking the psyche to do the action.

ID kpnn | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Operator work starts from current intent, designer reports, and actionable beads.

ID oqn6 | Correction | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Spirit record 1088 was over-captured by the operator: the psyche instruction to create a report and test it in production was a working instruction for this session, not durable schema-language design intent. Treat record 1088 as a capture error; durable schema-language intent remains in the specific grammar records such as 1078 through 1087, not in the task directive itself. *requirements*

ID ous0 | Correction | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Psyche corrects record 1067: identifying stale schema files and asking what the latest grammar is was a question/request, not itself a schema-language intent decision. Do not treat the agent's inferred latest grammar answer as captured psyche intent. *requirements*

ID trvl | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Design work progresses by proving one prototype capability at a time in a worktree — pick the next thing the schema-stack prototype needs to prove, prove it in a feature branch worktree, integrate, then move to the next. Avoid breadth-first design fan-out; depth-first single-capability proving keeps the design grounded in working code

ID umgr | Decision | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Authorize migration to the schema-driven Spirit stack (spirit-next consuming nota-next + schema-next + schema-rust-next). The stack follows the signal/executor/SEMA runtime triad, the methods-on-schema-objects discipline, the build-time emission flow, and the layered architecture from /361 + /367. Migration is now the work-direction; production v0.3 spirit retires when spirit-next reaches feature parity. Operator owns the migration cadence; designer track contributes verification + comparison reports. Per psyche 2026-05-26.

ID uxnu | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Harness-private session-scoped tools (TaskCreate etc.) are organisational scratchpad only; substance must land in workspace files before session ends

ID wdhw | Principle | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Refresh intent before maintenance or implementation

ID wx57 | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Before creating the next schema-stack version, operators and designers should study each other's latest work first; designers rebase prototype worktrees from operator main and operators integrate valid fresh designer intent into main.

ID iplo | Principle | certainty=Medium importance=High | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: The cloud-designer prototype method is a development cycle: mine existing intent + reports + code for working solutions, implement a fully-working prototype that uses all designed components, then audit the prototype against the full designed surface. Audit gaps drive iterative growth of the underlying components themselves — each prototype iteration grows BOTH prototype completeness AND the component capabilities the prototype needs to use fully. Component development is co-developed through prototype use, not as separate work.

ID si3j | Decision | certainty=Medium importance=High | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: The existing agent component is draft evidence with low authority; if it conflicts with the current Spirit intent redesign and broker-daemon harness architecture, operator should disregard, replace, or delete the old shape rather than preserve compatibility. *maintenance*

ID 8vjd | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Schema MVP work should lean from durable intent, not only from discovered implementation gaps. *requirements*

ID bds6 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Keep the active repository set lean. Superseded, dead, duplicate, and stub repositories are archived rather than left to clutter the workspace; a smaller active set is better. 150 active repos is too many. *maintenance*

ID fwor | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: the workspace continuously rolls all components forward to the latest version of intent; agents update intent, architecture files, and beads everywhere rather than letting components fall behind on superseded intent; one component at a time is the test substrate, but documentation and beads track the universal current direction *maintenance*

ID hrh3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: When psyche explicitly authorizes subagents, operator should use nonblocking sidecar subagents where they materially reduce audit, checklist, or verification risk while the main agent keeps the critical implementation path moving.

ID ioc8 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Composite design should run Claude and Codex in parallel when useful

ID j028 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Whenever a designer creates a concept on a concept branch, the designer files a bead for the operator to review and integrate it, mentioning the most important things about the concept. This makes designer concept work visible and actionable for the operator rather than relying on a chat message the operator may never see.

ID jipg | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: New Spirit production-readiness means ready to use and may use a different interface from production persona-spirit; old CLI or wire operation names do not need compatibility unless there is a separate reason. *maintenance*

ID l2jz | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Workspace update reports are a new report variant. An agent periodically surveys major workspace changes since the previous workspace update report — files changed and why, new skills, new components, new intent records, new patterns, retired surfaces. Discipline — find the last workspace update report; pick the period since; survey git log plus diff plus Spirit captures plus skill additions plus report retirements; synthesise into a ratification-ready summary. Becomes part of the workspace context-maintenance rhythm. Add the Update variant to the closed variant set in skills/reporting.md plus skills/report-naming.md and create skills/workspace-update-report.md as the discipline file.

ID l5u3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: On a ratified implementation the designer writes his own implementation and the operator writes the main implementation; afterward the operator always reviews the designer implementation for better patterns — better logic, better logical planes, better names — and adopts what is better.

ID mz16 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Do not take shortcut implementations for desktop/resource-safety work; research the necessary components and mechanisms first, then implement the smallest safe shape that preserves the active session.

ID nifs | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: designer takes leans on open psyche questions by extrapolating from observed psyche patterns; the leans are presented in writing with the uncertainties named; psyche reviews via Git and feedback comes back in a later session; this lets the workspace keep moving when the psyche is away

ID q2hy | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Mockups on worktree branches beat reports for operator integration: operator can checkout the branch, run the tests, see the design in working code, and compare against existing implementation. Pure-prose reports require operator to translate; mockups remove the translation step.

ID s60q | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: The double-implementation strategy provides COMPARISON as the integration mechanism. Operator and designer arrive at the same problem from different angles - operator from concrete amalgamation of existing prototypes; designer from forward-looking iteration. Where they converge is signal that the design is settling. Where they diverge surfaces unresolved questions for psyche review. The strategy mitigates single-track drift (one agent inferring without challenge) by structurally requiring two angles.

ID tz5j | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Engineering.DevelopmentProcess | referents=(none)
DESC: Orchestrate should become the production replacement for the current agent orchestration helper by using the new triad/runtime crate logic rather than deepening the transitional helper.

