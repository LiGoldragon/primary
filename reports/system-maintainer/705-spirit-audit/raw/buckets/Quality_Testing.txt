ID aipc | Constraint | certainty=High importance=High | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Forbidden stand-ins for real implementation: stubs/placeholders dressed as done, circular golden tests that just dump and re-read the engine's own output, a per-file emitted reader standing in for the one shared codec, claimed-but-absent features, and text-round-trip macro black-boxes.

ID l50b | Constraint | certainty=High importance=High | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Implementation discipline: bring intents into constraints into tests. Every load-bearing intent statement should manifest as a constraint test that fails if the intent is not honored. Not principles-in-docs-only - verifiable tests that pin the rule structurally or behaviorally. Per psyche 2026-05-26 directive; the prototype demonstrates compliance through named constraint tests.

ID t7h0 | Constraint | certainty=High importance=High | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Full correctness means every pipeline step is genuinely implemented end-to-end and does its real job; green tests are not correctness. Design-implementation avoidance — the common bootstrapping failure mode — is forbidden.

ID vcin | Constraint | certainty=High importance=High | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Positive grep deployment checks are not allowed as proof of a live architecture or behavior. A build or deployment check must compile, execute, round-trip, or otherwise exercise the real path; grep may only be used as a narrow negative guard to prove a retired symbol or forbidden pattern is absent.

ID 7let | Clarification | certainty=High importance=Medium | domains=Technology.Software.Quality.Testing;Technology.Software.Operations.Deployment | referents=(none)
DESC: Clarifies and refines se72 — the full-microVM end-to-end decision stands. The lojix cutover e2e is harmless because lojix deploys a full OS INTO a throwaway KVM-hosted VM, so a broken deploy kills only the VM and never the host; it does NOT require reconfiguring the host to declaratively run a microVM. Prometheus is a fine host — bare-metal with AMD-V, a live /dev/kvm, 32 cores and 124 GiB free — running a transient qemu KVM VM via nix with no change to Prometheus production config or networking. The earlier vm-testing-host-module framing, reconfiguring the host generation with microvm.nix plus tap plus projected networking.hosts, which raised the 5hir5bnz host-risk, was designer over-engineering and not psyche intent. Use the host KVM infra, deploy into the VM, leave the host untouched. *e2e*

ID qem1 | Constraint | certainty=High importance=Medium | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Each mandatory pipeline step must be proven by real file-based tests: NOTA structural parse, the single shared codec in nota-next, schema read against the schema-of-schemas, macro/declaration assembly into macro-free Asschema data, the roots model, Rust emission of datatypes and derive-based trait impls over the shared codec, and the hand-written actor system running full constraints on spirit-next.

ID qnf8 | Constraint | certainty=High importance=Medium | domains=Technology.Software.Quality.Testing;Technology.Software.Operations.BuildSystem | referents=qemu,vm-testing
DESC: QEMU-backed VM checks run only on hosts explicitly designated or authorized for VM testing; agents verify the VM-testing host boundary before invoking qemu-backed NixOS VM checks or ask for the correct host, while ordinary non-VM-testing hosts remain compile and evaluation surfaces.

ID se72 | Decision | certainty=High importance=Medium | domains=Technology.Software.Quality.Testing;Technology.Software.Operations.Deployment | referents=(none)
DESC: For the lojix production-cutover validation specifically, the end-to-end deploy test runs against the full routed microVM target — a microVM with its own Criome domain and a reachable IP via the vm-testing module, microvm.nix plus a tap interface plus projected networking.hosts, currently designed and CI-tested on CriomOS next but deployed to no node. Go all the way: stand it up and prove the lojix daemon deploys a full OS to it and the deploy survives SSH disconnect. This sets the cutover-validation target for the lojix push and does not retire the general operator-nspawn e2e pattern btc0, which stays available for ordinary operator e2e checks. Honour constraint 5hir5bnz and do not break Prometheus networking while standing it up. *e2e*

ID g7yd | Decision | certainty=High importance=Minimum | domains=Technology.Software.Quality.Testing;Technology.Software.Operations | referents=VmHost,qemu
DESC: VM and nixosTests run on a node that carries the typed VM-testing service, the VmHost KVM-hosting capability, and the test runner is selected by that service capability rather than by whichever machine an agent happens to be running on. An agent never spawns QEMU or a nixosTest on an interactive workstation or any node lacking the VM-testing service. This places test-VM compute on the designated VmHost node, generalizing the heavy-work-placement rule 0a9p from AI model work to VM testing, and aligns with cncj and 77ic which home the test VM on a VmHost-carrying prometheus.

ID ttbt | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Quality.Testing | referents=nix,nix-local-stack
DESC: Nix workspace integration builds and flake input overrides must use pushed remote refs only; local path flake refs such as path:/git/... and git+file:// are forbidden in commands and Nix input code.

ID b2jg | Constraint | certainty=Medium importance=High | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Tests across the nota and schema and sema stack must NOT inline nota or schema or sema source as strings in Rust. The nota, schema, and sema code lives in its own separate file read from file as the real artifact: a .nota, .schema, or .sema file. A small shared helper library should provide this read-fixture-from-file pattern, built minimally or adopted from an existing one. This extends the real-.schema-fixtures direction to the whole stack and to the value-versus-code distinction: the artifact is a real separate file and Rust tests load it rather than embedding inline source strings.

ID dqg3 | Principle | certainty=Medium importance=High | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Anything that needs to be done to test something is done IN the test. When a test is blocked by missing infrastructure, the test mocks, stubs, hand-codes, or builds the minimal version of the missing piece to make the test run. Coming back with we cant test this because of a blocker is the wrong response - unblock the blocker IN THE TEST, exercise the design end-to-end, see what actually works, what does not, and how it works. Tests are where you get to do whatever you need to do; thats their point.

ID fvf1 | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Tests must prove, not pretend: audit existing tests for genuineness, treat in-process synthetic-state tests as lesser proof than real-binary-via-Nix tests, and replace identified pretend-tests with full-stack Nix integration tests. *integration testing*

ID hnoo | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Audits feed into bead filing focused on (a) bringing code into constraint (constraint tests, falsifiable specs per skills/architectural-truth-tests.md, type-system witnesses) and (b) end-to-end sandboxed engine testing (Nix-flake-based integration tests exercising full daemon-to-daemon paths). Audits are the natural input to constraint-tightening + integration-coverage filing

ID jkkq | Constraint | certainty=Medium importance=Medium | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Spirit-next production-candidate testing must include a production-copy handover test: seed a production-like SEMA database, copy it, start or exercise the candidate runtime against the copy, verify existing state is preserved, and verify candidate writes affect only the copy, not the original. *integration testing*

ID js3u | Constraint | certainty=Medium importance=Medium | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Tests must never bypass the trait surface, never use stringly-typed shortcut state, and never collapse typed events to encoded tokens — they are written in the same schema-type vocabulary the runtime uses. *integration testing*

ID k9kl | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Canonical integration-test shape: real .schema files in real project paths drive Nix code generation; the spirit CLI and daemon are built by Nix; tests launch the binaries, the CLI sends real rkyv signal frames over a real Unix socket, the Nexus executor (with its own typed input/output) processes and returns, SEMA holds durable state, and the reply comes back through the whole chain end-to-end — every component driven off the schema. *integration testing*

ID z2cr | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Tests must exercise the actual execution chain with per-plane typed values throughout: each engine step takes and returns its plane's schema-object type (signal-engine Signal→Signal, nexus-engine Nexus→Nexus plus Signal-to-Nexus/Nexus-to-SEMA translations, sema-engine SEMA→SEMA), invoking engines through their trait surfaces and asserting on typed schema-emitted outputs. *integration testing*

ID 0ofm | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Schema work must be tested end-to-end with actual .schema files and local relative imports *integration testing*

ID 4wdp | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Testing trace must prove runtime use of the schema-defined triad interfaces and runtime actors. A passing trace test should demonstrate that Signal, Nexus, and SEMA actor boundaries were entered through their engine/interface methods at runtime, not merely that trait names or event strings exist in source files. *integration testing*

ID 77ic | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing;Technology.Software.Operations.Deployment | referents=prometheus,horizon-rs,lojix
DESC: The CriomOS VM-testing node gains a durable on-demand mode for continual production-side OS testing — a persistent VM node defined in cluster data and homed on prometheus that is not booted by default but started on demand, so the production OS can be tested in a real VM on the production host as an ongoing development-and-validation target. The host-untouched boundary for this durable node is scoped: prometheus MAY carry the typed VmHost KVM-hosting capability declared in its generation, since that is the infrastructure that lets it host the VMs, while prometheus networking, the router, and production function stay untouched. The not-booted-by-default mode keeps the guest and its routed networking inactive until a test is started, and any activation on the live router uses BootOnce rather than a live switch-to-configuration. This extends the VM-testing node feature y1v5, cncj placement of the routed test VM on prometheus, and se72 cutover validation from one-off transient runs toward a durable continually-available target, and reconciles with 7let by permitting the KVM-host infrastructure touch while keeping everything else untouched. Its node-service shape follows qkvx as a real typed horizon-rs NodeService variant authored in datom.nota and projected typed rather than a string-keyed hack, superseding the garbage VmTesting work tracked by bead primary-wvey.

ID 8f8e | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: The workspace should be able to test UI and visual components end-to-end in a sandbox — launch real applications the component affects, run the component, and observe the actual visual effect (theme change, screen warmth, brightness) while watching for system freezes and errors — to verify a component actually works, not merely compiles. Sandboxes can run via systemd and spawn on a node such as Prometheus. chroma (theme/warmth/brightness) is the first target. *e2e*

ID btc0 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Operator e2e tests can use the designer nspawn pattern *e2e*

ID cpip | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing;Technology.Software.Operations.Deployment | referents=criome,spirit,prometheus,cloud
DESC: The criome cluster is validated by a fully-tested, actually-networked multi-node test substrate with the spirit gate authenticated end-to-end, reached through one easily-reusable interface for testing networked component clusters. One identical propagation test targets three substrates: a default hermetic host-untouched runNixOSTest VM cluster on prometheus (the 7let posture), an opt-in durable on-demand microvm node on prometheus (the 77ic tier), and opt-in DigitalOcean on-demand droplets for real cross-machine validation. The test drives real daemon processes across separate kernels and asserts the spirit gate accepts an authorized head and rejects an unauthorized one, fail-closed. It starts from the 1-of-1 local gate (xhwa) proven cross-kernel and builds toward multi-machine quorum (jk1w).

ID ddtj | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Schema files are NOTA documents: every .schema fixture used in tests must be NOTA-legal and parsable by nota-next before schema-specific lowering. Tests should exercise .schema files as real parseable NOTA artifacts, not non-NOTA sketches.

ID k9fs | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: agent test-claims must be verifiable against actual runnable test files; a report that claims a test ran or proved something must point at a concrete file path plus the test invocation plus the witness output that exercises the claimed behavior; the workspace periodically audits whether claims match reality

ID qga2 | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Horizon pure-schema work should be shown through a working end-to-end prototype that demonstrates imports, assembled schema, and generated data types, not only design prose. *e2e*

ID rxk9 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: when psyche asks for testing, designer must use the most real-world testing conditions; sandbox-only shortcuts that omit a load-bearing piece of the production topology are not real-world testing *integration testing*

ID xxgp | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: tests illustrate design when named as the invariant they prove. one concept per test. short inline fixtures. assertion exposing the key design point. PATTERN comment at top naming the invariant. derived from nota-next schema-next style. requested for pattern-a pilot tests on worktree report 401.

ID y1v5 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: CriomOS should gain a node feature for VM-based testing using the best available Linux virtual-machine technology, to properly test CriomOS itself and its components — including visual/UI/GPU cases such as chroma's gamma warmth and brightness that a headless sandbox cannot exercise (report 66 found headless wlroots has no wlr-gamma-control). The feature provides a real display/GPU/DRM-capable VM, ideally Nix-native and automatable for CI as well as interactively viewable. *e2e*

ID zze6 | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: End-to-end testing for the message-passing engine means a real agent sends a real message to another real agent, the receiving real agent responds, and that response comes back to the first agent; a routed daemon path ending at a fake terminal or acceptance socket is only a routing harness witness, not full e2e. *e2e*

ID cqit | Correction | certainty=Low importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: A local verification check that runs quietly for about 27 minutes should be treated as a failed or suspect verification unless the agent can show it is doing a known heavyweight build such as a Rust compiler or toolchain build. *test automation*

ID kxhv | Principle | certainty=Low importance=Minimum | domains=Technology.Software.Quality.Testing | referents=(none)
DESC: Persona engine testing should be pure Nix staged infrastructure *integration testing*

