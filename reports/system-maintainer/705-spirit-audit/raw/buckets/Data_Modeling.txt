ID hl1z | Correction | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema defines data types ONLY - no effects, no fan-out targets, no effect tables. Effects are runtime dispatch / logic and have NO place in schema. The EffectTable + FanOutTargets + StorageDescriptor Feature variants that landed via the schema-crystallization arc were agent-hallucinated drift; they were never psyche-authorized. Schema stays in its lane: it declares data types.

ID 34hu | Correction | certainty=High importance=High | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: A domain scope must be a typed nested prefix of the Domain enum written in the same nested paren form as a domain value, for example Technology containing Software containing Quality, not a flat vector of free string segments. The deployed DomainScope as a vector of strings is untyped and inconsistent with typed domain values; each segment should be a real enum variant so an invalid or misspelled segment is rejected at parse time

ID 42rh | Correction | certainty=High importance=High | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit domain vocabulary should be represented as grouped schema enums by area, for example Home carrying its Housing Maintenance and sibling domains, rather than as one flat enum

ID 61lk | Clarification | certainty=High importance=High | domains=Technology.Software.Data.Modeling;Technology.Software.Programming.Compilation | referents=(none)
DESC: All text-based program code is pseudocode: the compiler must infer meaning by parsing text, which opens a large correctness gap. Sema exists to close that gap and is to become the fully correct data system in which code and data are typed structure rather than text. NOTA and the schema language are the deliberate half-step from text toward full correctness: positional, strict, fully typed. The Rust runtime is the current center because it is the superior runtime available today.

ID oyli | Decision | certainty=High importance=High | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The intent unit is named domain, a specific grounded subject grouped under broad areas; the terms category and topic are retired

ID t4uq | Correction | certainty=High importance=High | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit must not have a separate Weight field: Weight was renamed to Importance, so entries carry Certainty and Importance, not Certainty plus Importance plus Weight.

ID u62s | Principle | certainty=High importance=High | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Certainty and importance are orthogonal axes and must never be conflated. A tentative idea can carry very low certainty yet high importance: it is not decided but it matters. Capture sets the two independently and neither substitutes for the other.

ID oxgh | Decision | certainty=High importance=Medium | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The assembled schema represents a struct as a key-value brace map where the field name is the key and the type is the value — the lowest-level form that struct sugar expands into.

ID tico | Decision | certainty=High importance=Medium | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: An assembled-schema type declaration is visibility-tagged as Public(name, value) or Private(name, value); Public declarations are exported for library use, Private and local (including inline) declarations are not.

ID woeb | Decision | certainty=High importance=Medium | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: An inline PascalCase type in a field is sugar: derive the field name from the type name and log the inline type as a Private local declaration, so a schema defines new types compactly inline; in emitted Rust this is a module-local non-exported type.

ID 6wwf | Correction | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The reactive schema and the base schema are PURELY POSITIONAL: the Input and Output enums are written BARE at their known positions and are NEVER labeled with the words Input or Output - the reactive-schema type already knows which position is which. Unifying rule: a NAME appears only where something is genuinely keyed - a namespace map entry by its key, or a sum variant by its tag; a POSITIONAL slot - a structs fields, or the input and output enum positions - is bare because its position already says what it is. This resolves the apparent tension between record 874 and 876: 874 forbids labeling the known Input and Output positions; 876 says namespace entries name themselves - they govern different things, positions versus map keys, so there is no conflict. Labeling the Input and Output positions is the record-874 denied pattern; it keeps resurfacing - an agent example just reintroduced it - and must stop.

ID 853n | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit record identifiers should be stable, non-reusable handles. Removing a record currently frees its identifier for reuse — a Principle capture took id 2572, the freed slot of a deleted test record — so an identifier is not a stable handle across removals, which breaks citations and links that treat the id as durable. The fix mechanism is undecided between a unique hash and a monotonic never-reused counter; the designer concepts both and recommends one.

ID a3l4 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit records gain a relations field - a vector of short record-identifier hashes sized just long enough for non-collision, pointing to other records. Not all kinds carry relations but some do, for example a Correction relates to the record it corrects; the relation meaning can come from the kind or field that holds it. The relations field is the only code change needed to support intent relation and agglomeration.

ID b05y | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The workspace's Rust subset has no tuples — tuples are treated as poorly-defined structs and excluded from the workspace's typed-record vocabulary. The schema specification language only handles this Rust subset: named-field structs, named-variant enums (bare, data-carrying, struct-variant, nested), newtype structs (single-unnamed-field), and the workspace's scalar primitives. No multi-field tuple structs; no anonymous tuples; no field-less anonymous types. The NOTA grammar at the user-facing layer already enforces this constraint (no positional-only records without a named-fields schema); the schema-specification-language constraint makes it explicit at the schema-source layer too.

ID bkzd | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Define the ASSEMBLED schema FIRST. It is the canonical, macro-free, FINAL data model that Rust is generated from. In the assembled schema there are NO macros and NO sugar: a vector, an optional, a map, an enum, a struct, a newtype are FINAL forms - they are variants of the assembled type-model sums. The type-reference is a sum with variants Plain, Vector, Map, Optional. The type-declaration is a sum with variants Struct, Enum, Newtype. Do NOT design surface sugar - macros, prefix or suffix sigils, invocation forms - before the assembled target is defined; sugar is a convenience over a known target, never invented first. Simplify: lock the assembled model first, then and only then decide surface syntax. Agents keep jumping to sugar before the target exists; this inverts that order for good.

ID bybe | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: A newtype in the assembled schema is represented as a single-element brace with just the wrapped type — (Public Topic { String }) — NOT (Public Topic { text String }) with a derived field name. The TypeValue enum carries a distinct Newtype(TypeReference) variant: a brace with ONE element lowers to Newtype(Type); a brace with alternating name-type pairs lowers to Struct(Vec<(Name, TypeReference)>). Rust emission: tuple-struct pub struct Topic(pub String) with NotaTransparent derive — NOT pub struct Topic { pub text: String }. This makes newtypes easier to use in Rust: Topic([foo].to_string()) and topic.0 instead of Topic { text: [foo] } and topic.text. Surface @-syntax — Topic@String (short bare form) and Topic@{ String } (explicit brace) both declare the newtype. Distinguishing rule at brace contents: 1 element = newtype; N alternating name-type pairs = multi-field struct. This refines record 1226 — multi-field structs ARE key-value maps, single-element newtypes are NOT — they are transparent wrappers. Per psyche 2026-05-30.

ID cw5t | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit should support composite intent records that reference older intent records as source material, so repeated or closely related intent can be agglomerated into a newer stronger record without losing provenance.

ID d5v6 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Universal Magnitude type — replace per-component Certainty enums (and any other small-vocabulary ordinal enum reaching for the same concept) with a single Magnitude type living in the workspace's shared-data-type-library (signal-core or whatever the workspace's shared-typed-record crate is). Magnitude has 7 variants: Minimum, VeryLow, Low, Medium, High, VeryHigh, Maximum. Binary cost is identical regardless of variant count because rkyv serialises to a fixed-byte discriminator; using 7 universally is free. Systems can choose to action only on the subset they care about (e.g., only Minimum/Medium/Maximum); the variant set is the schema, the consumption is per-component policy. The naming may use a wrapping form like SizeMagnitude or stay as Magnitude; psyche left the name slightly open. Supersedes the Maximum/Medium/Minimum-only Certainty enum currently in signal-persona-spirit (and likely elsewhere). Resolves the certainty-drift finding from /267 by widening the vocabulary rather than mapping High onto Maximum.

ID ddlv | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Every component repository should get a concept schema file starting at version 0.1.

ID dnvs | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit removal-candidate certainty should use a shared neutral Magnitude bottom rung named Zero, not Option None; Zero avoids Option confusion while remaining policy-neutral across components.

ID ezcy | Correction | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=spirit
DESC: Records 20jk and f0wm misread the psyche intent on record shape. The psyche was not proposing per-kind storage variants; the canonical stored Spirit record stays one uniform shape. What the psyche actually raised was a view of a record or a shorthand for creating one — a presentation and ergonomics layer over the uniform record, not the stored shape — and whether such a view or shorthand is useful is undecided.

ID gjr1 | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Separate a thing's categorical KIND (its one-of-N essence) from its additive CAPABILITY vector (the specific things it does). An additive vector only adds and has no natural way to express un-selection of a default, so the categorical essence cannot be modelled as pre-selected defaults overridden by features — it must be an explicit categorical choice with no implicit defaults, mutually-exclusive kinds validated at projection with a typed error. The general axis: additive vectors for things you turn on, categorical labels for the one-of-N essence that has no off.

ID glr2 | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: NOTA always corresponds to a specified object. The goal is that when NOTA-as-schema specifies the objects themselves, the correspondence chain runs all the way down — schema is data, schema specifies data types, schema-emitted Rust gives objects their shape, and operating on those objects through SEMA can itself edit the schema.

ID hfvl | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Typed-data-tree vocabulary: branches and leaves. The workspace's typed records (structs and enums in the sema database) form trees of branches and leaves. A LEAF is a fixed-size terminal — typically an enum of unit variants only (Bool as True/False; Magnitude's 7 unit variants; Kind's five-kind taxonomy). Final leaves have no variable size. A BRANCH carries variable-size or data-carrying content — vectors, data-carrying enum variants, structs containing variable-size members. Branches are themselves trees (recursive). A vector is a kind of dynamic-leaf in one reading (single field at one position) but practically a branch because it points into another tree or series of trees. A data-carrying enum variant is a branch because the variant's payload doesn't fit as a single tree node. Adopt this vocabulary going forward in design conversations and skill files.

ID i8wt | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: A schema declares its roots at one designated point; the roots are the named entry-point set, generalizing the fixed input and output into a roots collection. Everything else in the schema is a free datatype declaration, used by the roots at some place. This resolves the assembled-schema shape toward a roots section plus a free-datatype declarations section, matching the operator roots model rather than a fixed input-output pair. Roots reference the free datatypes.

ID jr65 | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: NOTA + schema together are MORE SPECIFIC as a data specification than the Rust code equivalent. Rust mixes logic with data declarations; NOTA + schema is PURE data specification. This is why NOTA was needed - to get an actual specification language for the data, similar to why CapnProto has its own specification language rather than declaring types in C++. The specification language exists because the general-purpose language is not specific enough.

ID kgvc | Clarification | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Importance, the axis previously framed as accumulated weight, can be declared directly by the psyche as how much an intent matters and is not only accumulated through reinforcement; a statement made once can carry high importance, and importance stays distinct from certainty, which is confidence that the statement is true

ID kr1v | Correction | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit record identity is a random hash, not a content-address fingerprint. Because records are mutable — ChangeCertainty and other edits — a content-address hash adds computation for very little value, so strike the content-fingerprint direction. The id is a random hash assigned once at creation, frozen, unique, never reused. Supersedes the content-fingerprint choice in record 2590.

ID mn66 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit record certainty is optional at the persona-spirit entry boundary rather than a None variant inside shared Magnitude vocabulary; the absence semantics stay local to Spirit records.

ID obye | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema source root headers should be able to list exported variant object names directly, with the variant payload shape resolved from the namespace when the name is defined there, instead of requiring every data variant to repeat its payload as a parenthesized pair in the header.

ID olwm | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema and assembled-schema notation must truthfully represent the underlying data type shape. A homogeneous container must not be used for heterogeneous fields, and semantically empty wrapper records must not be introduced just to make an encoding fit; the representation should use the real product, sum, map, scalar, or typed common element that the data actually has.

ID oxy1 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Settled: the removal-candidate floor is a new bottom VARIANT on signal-sema Magnitude named Zero - the neutral zero/absent rung - NOT None and NOT an optional magnitude, chosen explicitly to avoid the confusion an Option-flavored None would carry. Magnitude stays a bare always-present field; Zero is declared first so derived Ord places it below Minimum. The removal-candidate meaning is persona-spirit per-component interpretation of certainty equals Zero, layered on the shared payload-free vocabulary (persona-mind could read the same rung as forgettable). Resolves the open model question flagged in record 1192.

ID p02v | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Gap-fill from forwarded designer exchange: roots are declared only at the schema root position; nesting does not add more roots to the schema. Everything else is a free datatype declared in the namespace and used by roots at some position.

ID qkvx | Correction | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Cluster-data node-service features must be TYPED end-to-end, never string-keyed. The VmTesting work was garbage — it matched a string VmTesting via node-services has/payload with a defensive node.services or empty-list default, fed by a synthetic horizon fixture, instead of adding VmTesting as a real typed NodeService enum variant in the horizon-rs model (with NotaEncode/NotaDecode like NixBuilder), authoring the fact in goldragon datom.nota, projecting it typed, and consuming the typed value in CriomOS. String keys, or-empty-list defaults, and fixture-fed checks bypass the typed cluster-data pipeline and violate strings-only-at-the-edges. The correct order is typed-source-first — extend the horizon model, author the fact, project typed, consume typed — not consumption-first against a string.

ID tbff | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema inline declarations inside root input and output payloads should be promoted into the source type scope: later inline references such as Topic * should resolve to an earlier inline Topic String declaration, and library consumers should see those promoted types through the schema namespace/export surface rather than requiring duplicate trailing namespace entries.

ID uara | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit record topics should be broad atomic single-word concepts, not compound hyphenated phrases. If a record covers multiple concepts, use multiple topic words rather than a single compound. Example: prefer [intent logging] as two atomic topics over [intent-log] as one compound. The atomic broad shape makes records discoverable by either concept independently and avoids vocabulary explosion across nearly-synonymous compound forms. Reuse existing topic words when they cover the substance; only invent new topic words when no existing word fits.

ID utaa | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Assembled-schema scalar built-in types are named in full English: Integer, Boolean, String. Never abbreviations like Bool or U64. Text is set aside for now and may later mean a string-with-spaces variant, but String is the scalar we use now. Any further scalar built-ins follow the same full-English naming rule. This applies the workspace full-English identifier rule to the scalar floor and rejects the U64 and Bool spellings.

ID w54p | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit verbal depth scope vocabulary settled — Shallow Recent Deep VeryDeep with target counts 5 15 30 100 respectively; bare Recent adopts the explicit 15 count for consistency with the verbal-scope family; the three new variants append at end of RecordedTimeSelection per the record 1249 rkyv discriminant stability rule

ID wvpg | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema enum variants are themselves enums or data-carrying enums — mandatory two-layer enum-of-enums structure

ID x88n | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema field names and type names are unified — in a struct, each fields identity IS the type name. Field appears lowercase (camelCase derived from PascalCase) in code; type stays PascalCase. Example: in struct Entry, a Kind reference becomes field name kind (lowercase) with type Kind (PascalCase). Same identifier expressed once, lowered to two casings by context.

ID y1g8 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The root schema must itself be described by an explicit schema object. The .schema reader may know that it is reading the root Schema type, but the Schema type still needs a concrete schema description of its own fields and nested definitions.

ID lpk9 | Correction | certainty=Medium importance=High | domains=Technology.Software.Data.Modeling;Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema struct bodies must not accept the old field Type pair syntax. Struct bodies are positional lists of field types; an explicit field name different from the type uses the dot differentiator such as source.TypeReference. The schema reader should reject the old pair form rather than silently accepting it.

ID niv3 | Decision | certainty=Medium importance=High | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit category is a closed enum with a broad category-theory starting set, not a narrow free-text topic field.

ID vbx6 | Principle | certainty=Medium importance=High | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit records should distinguish certainty from weight: certainty is confidence in the statement, while weight is accumulated importance or reinforcement, especially for composite records derived from multiple source records.

ID c8lc | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Data.Modeling;Technology.Software.Engineering.Design | referents=(none)
DESC: Schema should grow nested type namespaces that mirror Rust module namespaces, so role-local names like Envelope.Destination or router::Envelope::Destination can exist without polluting one global type namespace. Serialized schema references may use a string-level delimiter macro such as colon to encode the namespace path.

ID 0be2 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema files create the data types for the libraries they serve and may live side by side with Rust source when that is the simplest working layout; the schema remains the source of data-type truth.

ID 0q0u | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema files can be split by contract or schema-file type so parsing is per file and positional over root NOTA objects rather than one overloaded monolithic file.

ID 1g6p | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Durable Spirit identity should not rely on reusable incremental numbers; unique hashes are a better identity direction, while recency tracking should use daemon-stamped time.

ID 2dzp | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The key-value-map collection type is named KeyValue - not KeyValueMap and not Map. Map alone risks being read as a verb, and the Map suffix is redundant once key and value are named. So the collection macros are Vec, Option, and KeyValue, each a parenthesised form with the keyword as head: KeyValue followed by key type then value type. Refines record 1034 which had proposed KeyValueMap.

ID 2szc | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Contract schema top-level is a vector of root-verb enums

ID 2wj2 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Assembled schema should carry fully qualified names

ID 5ew0 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: All components need schemas, with Spirit, Orchestrate, Mind, and Persona as preferred pilots.

ID 5ip8 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit next entries carry one clarified description and no verbatim field

ID 5mbd | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Assembled schema needs a built-in struct representation that uses a key-value brace map: each field key is the field name and each field value is the type reference. This is the long-form data object that authored struct sugar expands into.

ID 7118 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Plane payload types SHED their plane prefix: the per-plane envelope - Signal Nexus or Sema - carries the plane, so the payload is named just Input or Output. The envelope wraps a bare Input or Output rather than a plane-prefixed SemaInput or NexusOutput, naming the plane ONCE. Resolves the fork flagged in report 409 in favour of the DRY form. Consequence: rename the schema payload types SemaInput SemaOutput NexusInput NexusOutput to plane-module-scoped Input and Output so each plane has its own, aligning with the three-schema-languages-as-modules of record 982 and the no-ancestry naming rule. Refines records 1037 and 1038.

ID 75ea | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: A plain vector in NOTA and asschema denotes homogeneous elements. Therefore assembled root syntax must not show Input and Output as bare sibling vector elements if they are different types; the root representation must use a true common wrapper or a struct/field shape that preserves their different types.

ID 7se2 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Component schema defines regular signal owner signal and sema headers

ID 8l6x | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit accepted-record reply schema should avoid nested summary-shaped acknowledgements

ID 8rew | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit defines a small-record data type carrying the core load-bearing fields — identifier, topics, kind, description summary, magnitude, daemon-stamped date and time. The full record carries additional metadata. The small record is what variant-ladder short-form reads (Recent, Today, etc.) and CollectRemovalCandidates emit; what archiving and downstream tools consume. Reduces wire weight; matches the natural reading shape an agent or human wants.

ID 9ptu | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Assembled schema is an expanded schema that can be processed as ordinary data structures. The assembled form should be data-first and macro-free, not a hidden parser control path.

ID afa8 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema scalars include unit enums, booleans, integers, strings, vectors, and typed-string newtypes; strings are conceptually vectors of characters stored behind boxed string storage, and domain concepts should be typed beyond raw String.

ID aflu | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Confirm flat SymbolPath as the canonical typed-symbol identity: store a vector of Name segments and recover the symbol role from the schema position, closing the structured component-plane-variant-payload-field record alternative for now.

ID ba22 | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema top-level vectors are typed schema objects, not one flat mixed vector.

ID bw9v | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema files should not carry comments — descriptions belong in the code, not in the schema itself. Schema declarations are positional and self-explanatory by the names of the types they declare; explanatory prose goes in the Rust implementation files, not the schema source.

ID c782 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: CollectRemovalCandidates should report archive outcomes as typed variants such as archive-created or archive-appended or backup-archive-used, keeping human-readable explanation in type names rather than string messages.

ID cdcx | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Gap-fill from forwarded designer prompt: schema remains a strict ordered representation of data as stored. Structs are aesthetic and semantic vectors of fields; order is load-bearing and should remain visible in the authored and assembled forms.

ID cn5a | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema defines EnumIdentifier as a core constrained string type.

ID eb5l | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema structure is sectioned — outer vector contains multiple sub-vectors, each being a distinct section/category. First sub-vector is one type of objects (e.g. messaging/operations); second sub-vector is another standard part of a schema definition (e.g. type namespace for new object names). Designer 326 section 2 example was wrong because it was a single flat vector — that misses the section structure.

ID ewo6 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: NOTA should have a data-type declaration layer for composite type objects such as Vec and Optional and related Rust-like composite type constructors. Schema reads those NOTA-layer datatype objects instead of inventing vector and optional as schema-only sugar.

ID f743 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Nota does not own type declarations; schema owns declarations. Plain Nota or type-reference positions can name scalars or composites such as String and Vec without declaring a new type; type declarations use the positional struct, enum, and newtype forms.

ID fv2a | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Assembled schema is a typed Rust data model with a NOTA projection. Its Rust types include macro declarations, type declarations, and namespaces for reusable declarations; names must remain unique through module-qualified identity so assembled schema can refer to types from other modules, libraries, and crates. The future self-hosting path mirrors these Rust-defined asschema types back into schema shorthand, where schema checks and assembles its own types.

ID g8ln | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit weight uses the same Magnitude ladder (Zero through Maximum) on a second axis distinct from certainty, not an integer count, keeping the contract all-qualitative and queries uniform. Refines the weight-vs-certainty principle 6vsl.

ID gm3a | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The schema root should be represented faithfully as positional structs: an imports/exports struct for what the schema consumes and shares, and an input/output struct for the channel surface, using square brackets because structs are ordered field vectors.

ID h9xd | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema examples should include multi-variant headers, not only one-element vectors.

ID hnsn | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema field names are inferred from type names in field position

ID hp3r | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Production Spirit does not need an immediate weight-field migration; weight belongs in the future record-shape design until the mechanism is settled.

ID hrte | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Bare root schema headers should list exported object names, and namespace bindings define payload shape. Reports and documentation should stop presenting parenthesized root header signatures as the current shape.

ID hvta | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Clarifying record 1555: a schema root header is a bare list of PascalCase names for the Input and Output roots. Each bare name is both the operation or reply vocabulary and an exported top-level type that library consumers import. Exported object names means each bare header name resolves to a real named type, defined in the namespace or inline, that downstream crates can name and use.

ID i3p0 | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling;Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: A schema struct field written with dot syntax is invalid when the explicit field role is the same as the field name derived from the referenced type. Entry topic-dot-Topic must be rejected with guidance to use Entry Topic instead. Dot syntax is only for genuinely different field roles.

ID isvp | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: SignalCore is the workspace data table of universal basic types — the system-types part of the 64-bit namespace partition. Each persona cloud domain component contributes to its component-specific zone of the namespace; SignalCore holds the workspace-universal core types.

ID jwgm | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The Spirit observation mode named DescriptionOnly is misleading when the reply includes record identifier, topic, kind, description, and magnitude rather than only descriptions. The mode name or reply shape should be corrected so the name matches the data.

ID ktmd | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Namespaces use maps; other extensible schema shape favors enums and vectors

ID l1qe | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Maps are reserved for true name-value namespaces.

ID m76h | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Headers should be derivable from the actual assembled data type structure rather than authored in a special separate declaration place. The header represents the outermost shape used for dispatch triage.

ID mimk | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The schema used for the Signal Nexus and Sema planes should be called a REACTIVE SCHEMA - the extended form. There is a simpler BASE SCHEMA that is pure data specification: a root struct plus its namespace of type definitions. The reactive schema extends the base with the reactive surface - an Input and an Output - so that given an input message it produces an output message. Analogy: Nix has a built-in derivation as the primitive, and the reactive schema is the extended version layered on the base schema. The psyche is still working out the syntax details - whether the namespace comes first, how the base schema is rooted, how field names derive - so those remain OPEN; only the reactive-schema naming and the base-versus-extended distinction are settled here.

ID mkts | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema files should carry structure rather than explanatory comments

ID mr16 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema namespace keys and fields provide names

ID nh8y | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema namespace order should support linear loading.

ID nob0 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Header declarations come first and drive receive triage

ID pnt9 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit topics are user-creatable single strings

ID q4b3 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The schema import section is a key map whose values are import variants.

ID qtbd | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit random opaque record identities should use 96-bit CSPRNG values, rendered through lowercase base36 shortest-unique-prefix display codes with a minimum of three characters per record kind.

ID r1un | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema design creates base input and output enums for actor reaction types; execution logic matches these schema-created variants to choose the right reply and state action.

ID rmqo | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema has a standard three-part structure with the second and third parts being optional (possibly recursively optional). Part 1: SPECIFYING - imports and exports; what this schema consumes that is not implicit or standard (ImportAll or selective Import names), and the names this schema exports for object sharing. Name-sharing layer. Part 2: INPUT - what this thing receives (the channels incoming side; operations/queries accepted). Part 3: OUTPUT - what this thing gives back (the channels outgoing side; responses/replies emitted). Each part has a special two-part sub-structure where the first sub-part is the header; alternatively the header can be derived from the assembled schema.

ID rzsx | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema entry points should usually begin with an enum-like variant decision because the first structural match creates the namespace for routing, mirroring the binary signal header mechanism.

ID sdgj | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema struct fields per component (in order): optional metadata/imports section first, then three short-header definitions (regular signal header, owner signal header, sema operation header), then namespace map, possibly with a final extension vector of optional-fields. Each header is represented as a vector of variants. The schema struct itself is defined in the schema-library base schema.

ID tzp3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema imports should use explicit import variants rather than an opaque Path variant with optional selection.

ID ucqp | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema namespaces can import local type namespaces from other schemas

ID uqoi | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The header declaration is a vector of ordered enum variants.

ID vu2k | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: A pure-projection Horizon settled as the runtime shape by the rewrite charter needs the types-only-module schema shape. The report-39 finding is that a 4-position schema document forces a signal plane; a Horizon that is a pure projection library with no runtime triad therefore needs a types-only-module schema variant so the datatypes generate without forcing an Input or Output signal plane onto Horizon. This is the surviving mechanical constraint from the now-closed Horizon-runtime-shape trichotomy.

ID wipc | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema headers are enum namespace nodes, not one payload pair per verb.

ID wtaz | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Vector sugar is structurally a single-field container object

ID y0vr | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: There is no composite intent type in code - composite and agglomeration are a language and behavior concept for expressing an idea, not a record type or variant. This refines the earlier composite-record direction (Principle audg3): provenance and agglomeration are expressed through a relations field on records, not a dedicated Composite type. Refreshing several intents need not produce one merged record - it may produce two or three records of different kinds.

ID yntp | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Schema input/output inline declarations should stay shallow: use inline root payload bodies for the operation-level shape, but move deeper reusable or nested structure into named namespace types rather than burying full trees inside the root vectors.

ID u2s9 | Clarification | certainty=Low importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: Spirit entry weight is intended as a set record axis, not only a computed search score: agglomerating several related intents into one record should preserve their accumulated importance through a higher weight, because after compaction the source mentions are no longer visible as separate active records. This weight-field design is low-certainty and should be revisited before production implementation.

ID xzlv | Clarification | certainty=Low importance=Minimum | domains=Technology.Software.Data.Modeling | referents=(none)
DESC: The signal/nexus/sema-bearing schema should be called a REACTIVE SCHEMA. Analogy to Nix: derivation is the built-in primitive, and higher-level builders extend it; likewise the plain SCHEMA is the primitive that specifies data types (the shapes), and the REACTIVE SCHEMA is the extension that adds the reaction surface (signal in, nexus through, sema state, output back). Plain schema says what data IS; reactive schema says how data MOVES. Most schemas are plain; a component top-level schema is reactive. Naming proposed by the psyche; pending the schema-syntax design conversation settling.

