ID 7hrd | Correction | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Working orders - ephemeral task directives like create-a-report-and-test-it, dispatch-a-subagent-to-X, audit-Y-and-write-a-report, operator-integrate-these-branches - are NOT intent and must not be logged as Spirit records. The intent log holds only DURABLE psyche statements (Decision/Principle/Correction/Clarification/Constraint) that outlive any single task and guide future work. The test: would the record still be meaningful and guiding AFTER the task it refers to is done? If no, it is a working order and belongs in the session task list or beads, not Spirit. When a psyche prompt is a task directive, capture only the durable intent statements WITHIN it (the design decisions, principles, corrections), not the do-the-task part. Generalizes record 1090 (the 1088 over-capture by the operator). Per record 1091, a mis-logged working order is FLAGGED as a capture error, not deleted (append-only provenance). Manifest this test into skills/intent-log.md. *validation*

ID j6r4 | Principle | certainty=High importance=VeryHigh | domains=Technology.Software.Data | referents=(none)
DESC: Spirit certainty follows an honest 8-level magnitude rubric. Maximum only for foundational invariants and hard overrides that are repeatedly ratified and stable. VeryHigh for ratified decisions clearly in force. High for solid decisions with minor open edges. Medium for working decisions and design directions still settling, which is the honest home for most ticks agents used to reflexively mark Maximum. Low and VeryLow for leans, proposals, and explicitly-pending-information. Minimum for weak tentative signal. Zero for removal candidates. Certainty is assigned deliberately, never reflexively. *validation*

ID kasm | Principle | certainty=High importance=VeryHigh | domains=Technology.Software.Data | referents=(none)
DESC: The guardian decides an entry together with the new referents it introduces and the psyche statement that justifies them as one atomic accept or reject; a referent is never admitted for a refused entry, so no orphan referent can pollute the store *validation*

ID ut6z | Decision | certainty=High importance=VeryHigh | domains=Technology.Software.Data | referents=(none)
DESC: Every registration of a new referent is gated by the guardian, the same as records, not admitted by a self-service synonym check alone *validation*

ID ycwf | Decision | certainty=High importance=Medium | domains=Technology.Software.Data | referents=(none)
DESC: Vocabulary: the persistent typed-record store is the SEMA database. Stop calling it RAD or other nicknames. Sema is the workspace's pure binary domain-tree format (per repos/aski/spec/architecture.md from the aski-stack analog); the records persisted in redb are conceptually a sema database. Sema contains strings today (verbatim quotes, agent names, summaries); over time those reduce as sema grows large enough to contain natural-language as typed leaves. Adopt sema database going forward in all design conversations and reports. *database systems*

ID 2vp2 | Correction | certainty=High importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: A psyche statement that leans toward a choice while explicitly asking for more information is NOT a ratification. The phrase I would go with X but is there more context? is a lean pending information, not a ratified Decision. Spirit 1511 was over-captured as a Decision when the underlying psyche statement was a lean with explicit request for more context. Future captures must distinguish lean-pending-info from ratified-Decision; the conservative discipline understates when the psyche has explicitly flagged more-information-needed. *validation*

ID 3don | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Schema source lowering must reject duplicate declarations for the same type name when an inline declaration and another inline or trailing namespace declaration both introduce that name. Duplicate Topic declarations are an error, not an order-dependent override or merge. *validation*

ID mrkv | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Spirit capture is a blocking gate not an advisory check: the guardian must vet and admit a proposed record before capture succeeds, so duplicates, contradictions, compounds, and non-intent are resolved or refused at the door rather than admitted and cleaned up later *validation*

ID oj3i | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Zero certainty IS the removal-candidate state in Spirit: zero certainty means the record has no value, so it must not surface by default. The default Observe query excludes Zero-certainty records. Query gains a certainty selection with a floor whose default is Minimum (excludes Zero); querying by an explicit certainty floor is generally supported, and seeing Zero-certainty records requires an explicit certainty selection (e.g. floor Zero / Any). This unifies removal-candidate with zero certainty rather than an arbitrary ad-hoc query. *query processing*

ID 29ed | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Registered but undelegated domain names must have a typed no-records result *query processing*

ID 7783 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Public-only and private-only record query shortcut verbs are desirable because they make normal Spirit usage more ergonomic than requiring callers to spell the full privacy-filter query each time. *query processing*

ID 7i0k | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Production Spirit should support querying intent records by numeric identifier, including exact identifiers and identifier ranges. *query processing*

ID 7vre | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: The explicit privacy query should expose all privacy selectors as operations — at most inclusive (the main one), exact equal, and at least — not just at-most; build them all. They sit on the explicit privacy-query subtype that carries the privacy level, while the normal query stays Zero-only. *query processing*

ID 8won | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Spirit clients do not provide capture time *validation*

ID go8m | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Spirit intent-resolution operations are typed combined actions so the thinker resolves a guardian verdict in one atomic call; there should be several such actions, the simplest being adding a new record while deprecating a list of existing records referred to by identifier *transactions*

ID jn99 | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: When the psyche explicitly marks a new intent statement as low certainty, the agent should search existing Spirit records on that topic; if a higher-certainty record partly contains the low-certainty idea, split or correct the record so high-certainty and low-certainty parts are represented separately. *validation*

ID kvib | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Spirit topic observation should support multi-topic matching, including partial matches that return records matching one or more requested topics and full matches that require every requested topic. *query processing*

ID lxo3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: The Spirit record query should add a RECENCY filter as a first-class composable selector alongside the existing TopicSelection, optional Kind, and CertaintySelection. Today filter mixing already works for topic-kind-certainty but recency is missing - the only recency-adjacent path is Range on RecordIdentifiers which does not compose with the topic-and-certainty query. Recency should let a single query ask for example recent records on a certain topic at a certain certainty level, mixing all four filter dimensions. Likely shape on RecordQuery: a fifth field RecencySelection with Any for no filter, Newest N for the most recent N records matching the other filters, Since identifier or Since timestamp for a lower bound, or Window for a closed range. The daemon already stamps date-time so the storage side has what it needs. *query processing*

ID m3eg | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Cloud daemon starts almost-stateless; daemon caches last-known-state of queried Cloudflare resources; cache loss acceptable because Cloudflare is the source of truth *caching*

ID p83x | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: First cloud cache is runtime/volatile (in-memory); persistent storage deferred until state worth preserving emerges *caching*

ID qyny | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Internal database logic should use the same schema-defined message language as component signals: the daemon may keep the database engine internal for now, but a growing database component can split into its own daemon without changing the language pattern. *database systems*

ID zyab | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Spirit record observation should support filtering by certainty or magnitude, so review workflows can select low-confidence records such as removal candidates. *query processing*

ID 5grv | Principle | certainty=Low importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Spirit needs topic catalog queries *query processing*

ID 7iue | Principle | certainty=Low importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Spirit query ergonomics should let agents ask for qualitative depth such as deeper or very deep instead of making agents choose exact counts or absolute time windows for routine topic searches. *query processing*

ID g055 | Principle | certainty=Low importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Cloud first state may be lossy provider-backed cache *caching*

ID lvy9 | Decision | certainty=Low importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Real-time streaming support is a new signal capability (working name signal-real-time) *stream processing*

ID vz7c | Decision | certainty=Low importance=Minimum | domains=Technology.Software.Data | referents=(none)
DESC: Signal real-time is Signal, not Sema; storage format for real-time data is open (probably native real-time format, not Sema) *stream processing*

