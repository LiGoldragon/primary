ID umsv | Decision | certainty=High importance=High | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Foundational model, superseding the at-sigil for macro invocation from record 1087. EVERYTHING is a struct: a unit variant is a zero-field struct, a data-carrying variant is a one-field struct carrying its data struct, an enum is a single-field struct holding the active variant. A SCHEMA NODE is a tagged variant - a paren whose first object is the tag saying which variant or type this is, and whose second object is the carried data - read against the KNOWN type expected at its position: the tag picks the variant of that known type, so NO sigil is needed. A MACRO is a struct - a Rust struct defining its arguments; the macro-space is a tagged union of those macro structs; INVOKING a macro is just a tagged variant read against the known macro or reference type, with no at-sigil. Because every position's expected type is known - the assembled schema is read against the schema-of-schemas - a tagged paren is unambiguously a variant of that known type; the enum-versus-macro ambiguity dissolves through positional typing, not a sigil. Implementation focus: represent the assembled schema and define the macro structs in Rust first so we know how to compose a macro.

ID er9w | Principle | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The new paradigm: schema is the engine that turns NOTA - a delimiter-based language - into a powerful programming-interface specification language. Schema makes NOTA expressive as a specification surface for interaction between actors (major actors first, eventually all). The macro system lets us write specific domain languages for each interaction. Aesthetic: struct as a vector - a series of things of fixed number, each name describing what that part is. The framing the psyche named: a revival of ASCII (the original meaning) but more defined, more strict, perfectly strict; representing data as it is stored, in order.

ID khbv | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema language is self-describing — the meta-schema (what a schema IS) is itself defined using the schema language own format. There is a schema-library repo with a bare schema file that defines the foundational namespace (Schema, Channel, Operation, Reply, Event, Namespace, TypeName, TypeDeclaration, Path) everyone uses. The schema-library bootstraps itself using its own basic struct format.

ID lyo2 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: NOTA is a thin library for sense-of-structure - it exposes methods on parsed blocks (is_square_bracket / is_parenthesis / is_brace / holds_N_objects / qualifies_as_symbol / etc) that are macro primitives. People use NOTA to design any positional uncompromisingly-specific domain-specific configuration data language. NOTA itself does not interpret meaning; it delivers structure to the upper layer (macros / schema) which interpret.

ID mqlb | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Move the schema declaration out of Rust macros into a specialized NOTA-format schema language. The current signal_channel! macro AND the planned sema-side macro converge with the earlier-started NOTA schema language work (designer/263 + /279). The NOTA schema is the SOURCE; the codegen produces (a) Rust types and impls (the macro current role), (b) per-version namespace-slot-assignment tables, (c) version-diff that auto-marks types for upgrade-migration when the schema is recompiled across versions. The macro becomes a consumer of structured schema data, not a parser of Rust syntax. The NOTA schema is human-readable enough to keep portions of it editable; compilation handles slot-number assignment + size optimization automatically.

ID mx58 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The schema language is a reusable macro-lowered structured declaration bedrock, not only a Spirit contract format.

ID qeej | Principle | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema is a way to interpret the delimiter-based language NOTA into standard typed interfaces. An interface is the input and output for a type of relationship. NOTA carries delimiters; schema interprets them as structured I/O for some interaction relationship. The same NOTA text means different things in different contexts via different schemas applied to it.

ID rl4j | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: the schema namespace is dependency-ordered; most basic definitions come first, derived definitions later; consumers reference only earlier-declared names

ID tace | Principle | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema is a SUPERSET of CapnProto-style specification languages. It adds three things on top of pure data-type declaration: (1) its own module system; (2) a macro system; (3) shape-driven node-type matching - structure + number of members + type of members inside a node determine what type it resolves as. The three additions together make schema expressive enough to be a real specification language without becoming a general-purpose programming language.

ID uujd | Principle | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema names carry meaning from where they are defined; references can be forward (later in the schema). Names are not free symbols invented at use sites — they refer to typed declarations elsewhere in the schema OR imported from another schema.

ID w1ru | Decision | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The schema language should be specified by its own schema file.

ID wqi2 | Correction | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Root struct positional fields are bare — no Name@ prefix. The universal Name@Delimiter rule (record 1216) applies to DECLARATIONS where the user invents a new type name, not to positional values at the root struct's known fields. A .schema file's root is a struct with fixed positional fields (imports / input / output / namespace per the schema-of-schemas); the value at each field is bare — empty-brace imports, [Record@Entry Observe@Query ...] input enum variants, [RecordAccepted@... ...] output variants, brace-namespace declarations. Same treatment bare {} for imports and bare { ... } for the namespace already had; Input/Output need the same treatment because they are the same kind of thing — values at positional root-struct slots, not user declarations. Additional plane roots (NexusInput, NexusOutput, SemaInput, SemaOutput) declared INSIDE the namespace stay as Name@[...] because they ARE user declarations. Corrects the 1216 carve-out that named Input, Output, and plane roots together as needing Name@: top-level Input/Output are positional values, not declarations. Per psyche 2026-05-30 against report 429 §5a.

ID y11g | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Imported schema names must not collide in one context

ID ospz | Clarification | certainty=Medium importance=High | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: CORRECTION to record 656's STT artifact: the original psyche quote contained 'in Aski in my previous works' which was speech-to-text-corrupted to 'in ASCII'. The actual referent is the psyche's Aski language work - aski-core (rkyv contract types for askic-veric-semac triad: compiler, verifier, semantics), vscode-aski editor tooling, the core parse-tree primitives (body, domain, expression, module, pattern, primitive, program, statement, trait, type). The structural patterns that crystallized in the schema substrate emerged FIRST in Aski - deep typed trees, closed variants, recursive composition, the separation of syntax/verification/semantics. The schema engine is the SECOND iteration of structural insights established in Aski. Reports requiring correction: /341 §1 Phase 1 (the foundational trajectory paragraph); /344 Slide 2 (the trajectory slide); any reference to 'ASCII sketches in early designer reports' as the pre-substrate phase. The designer should have known Aski - failure to grep before assuming STT corruption.

ID wt7s | Decision | certainty=Medium importance=Medium | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: the schema language is ONE recursive shape: a root-layer struct with macro-expanded fields all the way down to scalar leaves; the top-level schema document IS a known struct named at the dialect level (the basic schema definition / Schema) with positional fields of known types; if a field is a vector it carries a variable number of macro-call instances; macros ARE structural expansion based on position; inner struct or enum declarations are themselves macros that fire at their positions; the objects inside a macro call become the MATCH against the macro pattern; enum bodies get re-parsed because the enum name carries forward; the recursion terminates at FINAL built-in macros expanding leaf structs or enums whose fields are only SCALAR types: vectors of integers, vectors of unit-variant enums, booleans, integers, strings; strings at the language level are typed newtypes around character sequences for proper identity not bare strings; this gives a uniform structural model from the root schema document down to every scalar position - position determines type, macros do structural expansion, leaves are scalars

ID 0qda | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Gap-fill from forwarded designer prompt: the schema macro system is the basis for domain-specific interaction languages across components and actors, turning delimiter-shaped NOTA into typed input-output interfaces.

ID 23y5 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schemas need namespace imports, with local Spirit definitions enough for the MVP

ID 2v9u | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: each of the three schema types (Signal, Nexus, SEMA) is its own LANGUAGE with the SAME 4-position structural shape - Imports Input Output Namespace - and all three share the SAME import/export mechanism for type reuse via the single-colon namespace path that mirrors Rusts module structure but with ONE colon instead of two; the three schemas differ in what fits inside each position (signal-specific nexus-specific sema-specific types and macros) but the structural skeleton and the import/export protocol are uniform across them; the namespace position is BOTH the export surface (what this schema exposes to other schemas) AND the definitions surface (what this schema declares for itself); imports name the foreign types via colon-path eg signal-frame:Frame or schema:spirit:Entry; operators report 219 (schema fullstack prototype) does not yet carry this clarification - it should be reflected in designer reports and per-repo INTENT.md files so all three schema types are documented as having symmetric input output import export shape

ID 3c0k | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema imports and exports are part of the root schema structure: schemas consume names from other schemas and export names for assembly, with conflict checking across imported exports.

ID 859k | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The schema layer is the programmable composability layer on top of NOTA: it uses typed schema nodes and macros to express complex data-driven interfaces and behavior trees, including Signal, Nexus, SEMA, and internal engine sub-behavior, while preserving unambiguous quote-free NOTA encoding.

ID amjn | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Gap-fill from forwarded designer prompt: NOTA is a delimiter-based embedded language whose meaning is derived from schema context. Its bracket-string discipline lets complete NOTA expressions live inline inside shell or code double quotes without internal quote collisions.

ID b0v3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema files use dot-schema extension

ID givl | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Cloud signal language starts small and typed

ID gyfg | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The recursive parser + dispatch model that powers the schema language is the foundation for a custom-language library — anyone can build new custom languages over NOTA using the same dispatch + lowering pattern. The schema language is the first application of this pattern; the underlying library is reusable for any DSL the workspace wants to express in NOTA. Inspired by Shen-style mid-compilation assembly.

ID hh9p | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Enum variants cannot be declared without an enum type name. A parenthesized list such as ((Parse Expression) (Render Expression)) has no enum type name for the outer list, so it is not a valid enum declaration surface. Any enum declaration must name the enum type; variant tags such as Parse and Render only make sense inside that named enum declaration.

ID hz4m | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema imports need selective object import as well as load-all.

ID igbb | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Inside a vector, everything is qualified-symbol. Vectors of PascalCase represent ordered struct types (each field name = its type). Vectors of vectors-of-PascalCase represent inner structs. These resolve through macros at the schema layer; the schema-side is where the qualified-symbol becomes a real type.

ID kevh | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema source root headers should support inline declarations that create exported namespace objects, so a root variant can define its payload shape inline and that definition is entered into the namespace for later reference.

ID km3w | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Two paired moves on signal-schema documentation. (1) NOTA signal schemas must be documented as a class — what the wire vocabulary looks like in its NOTA form, separately from the binary signal layer. (2) Psyche is proposing a deeper design direction: define signal schemas IN NOTA itself — a NOTA schema language that serves both as the documentation and as the source of truth from which the Rust code is generated, the way a Rust macro generates types except expressed in NOTA. The proposal builds on prior workspace explorations the psyche has done. Direction is firm at Medium certainty; the exact schema-language shape requires back-and-forth design and testing.

ID n1px | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Assembled schema must remain legal NOTA data; authored schema declarations use the positional struct and enum forms — the pipe delimiters are generics and traits, not declaration forms. Inline PascalCase declarations may introduce local reusable types when they appear before later references, preserving left-to-right declaration order. Lowercase camelCase names remain field names rather than exported reusable type declarations.

ID nt8a | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema data should not rely on comments for section structure.

ID nz5v | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The schema system needs explicit schemas/specifications for AssembledSchema data types and for the minimal built-in schema object, with a bootstrap layer before schema can fully describe itself.

ID r08u | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema names must resolve from defined namespaces or imports, with Spirit allowed to define all types locally for the MVP.

ID rjml | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Operator 219 is missing a first-class Nexus language and SEMA language. Nexus and SEMA each have input and output surfaces plus import and export surfaces for type reuse. The import/export shape is shared across languages and mirrors Rust module namespaces with a single colon instead of double colon.

ID st16 | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Operator report 195 should not imply that a delimiter shape globally means struct or newtype. A shape like a square-bracket field vector becomes a struct or newtype only when it appears in the schema macro position whose lowerer gives it that meaning.

ID uhz9 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema is the composability and programmability layer on top of NOTA: it turns NOTA into a system for describing composable data types and data-driven behavior, programmable by the user to create new interfaces for specialized engine behavior expressed as a node-graph data type. Schemas nest: an engine may contain sub-engines, each with its own schema and interface, so signal, nexus, and sema are the three top schemas but internal sub-behaviors get their own. The namespace mirrors Rust modules: one file with fully-qualified colon-separated names, or split into files like Rust modules, importable and re-exportable. A schema is a key-value struct at the top whose entries declare the module public symbols. Imports are a key-value map of module name to a data-carrying variant naming the import KIND such as import-all, from-git, from-path, adjacent-directory, same-directory, or from-repository-root, so the import system is modular across scenarios.

ID vvua | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Spirit schema files have no outer root parentheses, declare header/root first, encode enum-like surfaces as ordered variant vectors, defer namespace imports beyond MVP, and cover ordinary signal, owner signal, and Sema headers.

ID ymq8 | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: In authored schema, only the root object name can be omitted because the reader already knows the schema root type. A nested object being specified must still name itself. A parenthesis object with two inner objects, where the first is a qualified symbol name and the second is a parenthesis object of qualified symbol names, is the shape of an enum definition. The corresponding square-bracket second object is the shape of a struct definition; if that square-bracket object contains one symbol, it is a newtype struct.

ID z1yz | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Schema must have a schema-authored self-description even while the bootstrap Rust reader remains hand-written. The schema file that defines how to read schema files is required as a tested artifact: it does not have to be load-bearing for the reader yet, but it must lower through the current engine and expose the root Schema shape and nested declaration types clearly.

ID zt9i | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The aski-core (and its successor askic / askicc) pattern is an analog past experience, not the foundation to replicate. The earlier design intent there was a more beautiful syntax to represent Rust; the persona-component schema work is an entirely different goal. The new direction is a dedicated schema specification language akin to Cap'n Proto schema files but more explicit, scoped only to the workspace's Rust subset (no tuples, since the workspace treats tuples as poorly-defined structs). Reading aski-core and askicc as foundational substrate for design was the wrong frame; treat them as analogues that inform the discipline without dictating shape.

ID b0s4 | Clarification | certainty=Low importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: The enum-versus-macro-invocation distinction CANNOT be resolved by delimiter-shape or position alone: a collection macro such as Vec applied to one element type, and a two-variant enum, are structurally identical paren objects and can appear at the same node position - the head symbol is an operator in one case and a variant name in the other, with nothing marking which. So an explicit DISCRIMINATOR on the paren head is required. This corrects the earlier framing that position alone resolves it. The struct bracket and the map brace do NOT have this problem - only the paren is doubly loaded as both enum and head-applied macro. Three candidate discriminators: a reserved operator-word set (implicit), a sigil marking operator heads (explicit, structural), or moving collections off the paren entirely onto suffix sigils and the brace - vector as a star suffix, option as a question suffix, map as a brace - so the paren stays pure enum. Open decision: which discriminator, and whether enum stays the bare paren default.

ID uuu9 | Principle | certainty=Low importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Assembled schema should make names globally unambiguous by context

ID ychx | Decision | certainty=Low importance=Minimum | domains=Technology.Software.Programming.DomainSpecificLanguages | referents=(none)
DESC: Bracket semantics swap: () is for enum/variants and [] is for struct/fields. Structural justification: a struct IS a vector of fields (all present positionally - matches [] vector semantics); an enum IS one-of from alternatives (matches () grouping/variant semantics). This REVERSES /326-v13s convention where [...] was sequence/enum and (...) was record/struct. Touches every existing .schema file + operator/181+/182+/190 implementations + intent 614 field-naming form. Reading: Entry [Topic Kind Summary] is a struct (6 fields); Kind (Decision Principle Correction) is an enum (one-of). Container expressions: (Vec T) stays () since Vec is a single-tagged-variant form; or [Vec T] under stricter reading - needs clarification.

