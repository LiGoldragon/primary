ID 29pb | Constraint | certainty=High importance=High | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Component Sema databases, the daemon durable state, must be backed up to a server atomically, and state loss is unacceptable. Pursue native version-controlled component databases rather than treating the store as an opaque binary blob. Mechanism is under design and Dolt-informed, with the strict-typed hard-migration-per-schema-change shape as the core constraint to solve.

ID 5hjv | Constraint | certainty=High importance=High | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: All Spirit Signal, Nexus, and SEMA operations must be declared in the schema files first; generated schema types are the source of every operation data type, and handwritten Rust may only implement behavior on those generated nouns.

ID 1aam | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema is the macro-language source of truth for component data, wire, storage, and upgrade behavior.

ID c9fv | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema migration is a real workspace-wide concern for persona daemons. When an agent changes a component signal contract or storage schema, the running daemon binary AND its redb database need to migrate coherently to the new shape. The pattern for that migration does not exist yet and needs designing. Applies to every persona triad, not just spirit.

ID gvgu | Correction | certainty=High importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: The claim in reports/designer/261 that NOTA-no-tail-omission removes Cap'n-Proto-style field-id discipline from the workspace is wrong. NOTA's strict positional rule governs the user-facing text format only; the schema-migration concern is at the rkyv storage layer, which has natural headroom (e.g., a bool occupies a byte, a small-variant enum's discriminator fits in a byte). Within that headroom, additions are wire-compatible without migration — adding a fourth unit variant to a three-variant enum is zero-cost so long as the byte still holds it and variant order is preserved. The Cap'n Proto-style structural-compatibility discipline IS available at the rkyv layer; it just has to be reasoned about there, not at the NOTA layer.

ID k12x | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Spirit needs an explicit capability to remove intent entries, beyond the current supersession or negation-by-new-record procedure.

ID mtrx | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Spirit should support removal-candidate review as a first-class observation flow, using record certainty or magnitude so low-confidence records can be found before removal rather than hard-deleted ad hoc.

ID nm97 | Decision | certainty=High importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Components have TWO schema categories: EXTERNAL and INTERNAL. EXTERNAL schemas describe surfaces external to daemon runtime - wire contracts to other components (the signal-vocabulary that survives process boundaries via socket) AND database/storage contracts (state that survives process exit; external to runtime because it lives in files/redb/disk). INTERNAL schemas describe surfaces within daemon runtime - internal actor message vocabularies, internal channel contracts between actors that share process memory. The two-languages principle from record 659 (wire vs effect) is REFINED: it's a wider taxonomy. Multiple schemas per component possible, each schema describes ONE channel - wire-channel or storage-channel or internal-message-channel. Internal schemas matter because changing them does NOT break the database (storage external) and does NOT break the wire format (signal external); they make engine internal changes visible without breaking external surfaces. Major actors get a schema for their message vocabulary.

ID py4h | Principle | certainty=High importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Upgrade mechanisms are live SEMA operations: a protocol or database-format change is represented as a typed operation or message, applied as database work, and used as the source for derived datatype and upgrade code.

ID mn3k | Decision | certainty=Medium importance=VeryHigh | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Domain relations live as first-class highly visible schema-declared vocabulary in a dedicated taxonomy schema surface, a new schema interface kind alongside the plane schemas, not an ad-hoc data file. The relation and expansion mechanism is meant to be reusable across components such as the mind

ID uuh7 | Principle | certainty=Medium importance=High | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Components treat recompiling to change an enum set as trivial; zero-downtime upgrade is a design goal, so schema and enum changes are not a cost to design around

ID j487 | Constraint | certainty=Medium importance=Medium | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: The version-controlled server-backed atomic durability mechanism for component Sema databases must be built once as a reusable library of generic types and traits, not reimplemented per component. Every component is to have the option to version-control its state through this one shared mechanism. The reusable-library direction is firm; the exact generic mechanism is undesigned and to be settled by prior-art research and implementation experiments. Extends 29pb.

ID 1352 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Version migration is per type

ID 1wn5 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Upgrade mechanism is realized as SEMA operations on the Asschema. The same library that handles record-level SEMA operations also handles schema-changing operations — they are the same kind of operation acting on different objects.

ID 4wlk | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Production Spirit should implement an explicit collect-removal-candidates operation that archives or emits reviewed Zero-certainty records before retracting them from the hot store.

ID 5cyn | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema diffs should drive upgrade trait requirements: unchanged types emit no upgrade code, changed types require hand-written upgrade and accept behavior on the schema object, database load runs needed upgrades, and old-version incoming messages can be upgraded, accepted, and logged for introspection or routing.

ID 74in | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema-derived upgrade code is compile-time optional per main-next pair

ID 8cs3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: schema diff operations have three families — Add, Remove, Modify — and Modify subdivides into ContainerEmbed, EnumWrap, Reorder, and KeyChange sub-cases

ID 9pil | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema changes that affect stored data should be accepted through an upgrade testing pipeline: derive the new code, start a newly compiled daemon, test against a minimal specified database first, then test against a disposable copy of the live database before accepting the schema change.

ID 9yxh | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema namespace additions should eventually follow append only discipline, adding new names at the end of the available namespace so future enum-like compilation and upgrade logic can remain compatible.

ID apgi | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Enum slot planning should minimize database rewrites across compatible upgrades.

ID b1c4 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Upgrade and downgrade are one compatibility projection relation

ID bpry | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema upgrade knowledge belongs to the next version, which assembles against the previous schema.

ID h2oa | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Spirit releases need schema-aware version numbers

ID lyms | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: A component schema has at least ordinary, owner, and Sema headers

ID ngk0 | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Vocabulary + development-model discipline: authors ALWAYS WRITE FROM THE POINT OF VIEW OF NEXT. Whatever is being written IS the next version. MAIN is imported as the current published baseline (comparison point). PREVIOUS or LAST refers to the past iteration of any given thing (previous day, last message format, previous version, etc.). The vocabulary: NEXT = the in-progress version being authored; MAIN = the current published canonical version; PREVIOUS / LAST = the prior version. From the author's point of view they are always on next; main is just a label for the published baseline. Development model: if main == next at the same revision, nothing has changed since main and NO UPGRADE PATH is needed - potentially smaller binary (no version-projection From-chain machinery compiled in). If main != next, the upgrade machinery generates the From-chain from main to next (the version-projection emission per /338 §8). This formalizes the historical/current-shape pattern from skills/spirit-cli.md sema-upgrade migration discipline - 'historical' as a name should be replaced or supplemented with 'previous' or 'last' since previous/last is the canonical vocabulary the psyche uses. Applies workspace-wide: every reference to the prior iteration of a contract / schema / message format / data shape should use PREVIOUS or LAST; every reference to in-flight authoring should use NEXT; MAIN is the imported published baseline.

ID o10d | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Deployed Spirit versions should be tagged before schema migration planning

ID o7lx | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Record-shape changes in the schema-derived spirit pilot are clean-break prototypes: change the shape, start a fresh database and bump the on-disk schema version so stale pilot databases are rejected, with no in-pilot migration. Migrating real production records is operator work at the point production Spirit cuts over to the schema-derived stack. Designer prototypes the shape; operator owns production migration.

ID qkdk | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: The upgrade path goes through next-version-as-cargo-dependency: each schema crate declares the next versions schema crate as a Cargo dependency, so the macro has compile-time visibility into both schemas and can emit the VersionProjection at the current versions compile time.

ID rq3p | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: When an old-version message is upgraded and accepted, the runtime should emit an observable event so introspection, routers, or agents can notify the source to upgrade its client schema.

ID thi1 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema and upgrade work should carry explicit provenance.

ID ux4w | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: rkyv's natural storage layout uses a larger namespace than the data strictly needs (a bool occupies a byte; a small-variant enum's discriminator fits within a byte even when there are only 2-3 active variants). This headroom enables a class of zero-cost schema changes that require no data migration whatsoever: adding a fourth unit variant to a three-variant enum that still fits in the byte. Constraint: variant order must be preserved (existing payload bytes must still decode under the new schema). The schema-layout schema must expose this headroom (e.g., declared discriminator width) so the migration-derivation step can identify zero-cost changes and skip the migration step. Other zero-cost-class candidates: adding fields at the end of a struct under append-only encoding; widening fixed-width integer fields where rkyv reserves space.

ID veqq | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: The update mechanism (sema-upgrade) is a structural prerequisite for any deployed persona component. The deploy → restart → update flow means a daemon coming up against an existing redb must either find no schema drift (no migration needed) or have a migration path through sema-upgrade. Without that path, any contract edit after first deploy breaks the next restart. The implication for getting mind + orchestrate deployed: their schema must be stable enough OR sema-upgrade must cover their migration before deployment, or the operator must be willing to drop-and-rebuild on every contract edit until sema-upgrade catches them.

ID w1ss | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Persona daemon schema migration uses in-process versioned reads. Every stored record carries a schema-version tag; the daemon dispatches read-side on the tag, migrating older records on read into the current shape (optionally rewriting upgraded). No external migrator binary; zero downtime per schema bump. Trade-offs accepted: the daemon links every prior schema version's types, and read paths fork by version (squashable later through explicit version-collapse work). Applies to every persona triad, not just spirit. Per-record-version (not per-table or per-database) is implied by the selection wording "stores a schema-version tag on every record".

ID wrjl | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Design direction (refining intent record 12 schema-in-NOTA): each persona contract carries an explicit schema-layout schema written in a NOTA-based language. The schema-layout description is fully content-addressable — its hash is its identity. Any edit to the schema changes the address, which is the version. The version-checking pipeline (built into component handling) detects schema-address mismatches between current code and stored data, walks the diff between addresses, and derives the migration operations (zero-cost variant extensions, append-only struct fields, structural migrations) from the diff. The migratory process is baked into how the types are written rather than written separately. More sophisticated than Cap'n Proto. Warrants deep testing and investigation before committing to a concrete shape.

ID wsgh | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: MigrationIndex is a runtime decoder lookup

ID wv7w | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema diffs should be isolated as typed upgrade operations.

ID x2yz | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Per-repo data-type discipline (signal-X repos with versioned schema libraries) is what enables cross-version decoding and recovery

ID xbu8 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: The schema daemon IS the editor of the schema. It receives upgrade messages, applies them to its Asschema, derives both the new data type code and the upgrade migration code. The system becomes self-editing through this flow.

ID z550 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Schema and Spirit should move toward a runtime-upgradable schema system: schema changes are represented as typed upgrade objects, and Rust generation emits upgrade and compatibility code from those objects so new generated components can accept previous schema/data/message shapes through explicit typed upgrade paths.

ID z8gj | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: The schema component exposes a schema type library — types that other crates import to reference schema metadata. The library is the build-time substrate consumed by macros (the signal-side macro plus the symmetric sema-side macro per spirit 390). Three faces of the schema component: (1) Runtime daemon — serves short-header to schema lookups; (2) Library crate — exposes the schema types other crates import; (3) Macro substrate — the library is what the signal/sema macros consume when generating code from a NOTA schema input (per spirit 391). The library is the build-time face; the daemon is the runtime face; both stem from the same NOTA schema source.

ID 00zx | Decision | certainty=Low importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Per-type Migration trait includes a self-check / peer-coordination message that knows whether to consult the next-version daemon

ID 0wr7 | Principle | certainty=Low importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Version handover should be explicit daemon protocol

ID v1ya | Clarification | certainty=Low importance=Minimum | domains=Technology.Software.Data.SchemaEvolution | referents=(none)
DESC: Gap-fill from forwarded designer prompt: the schema daemon or schema agent should resolve schemas, namespaces, standard core definitions, and macro libraries, caching assembled schema material in memory for consumers.

