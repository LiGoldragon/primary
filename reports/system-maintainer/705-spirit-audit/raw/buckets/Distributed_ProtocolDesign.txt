ID 2t89 | Principle | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: A well-specialized, clean-context, well-instructed guardian can outperform the submitting agent at judging a capture even on a nominally weaker model, because the agent is buried in noise from messy interaction and code while the guardian judges one case at full attention with extensive on-point instructions. The lever for a smarter guardian is specialization, clean context, and training, not raw model strength; a larger-context model like DeepSeek Pro helps only when paired with proper training. *admission control*

ID 4jgt | Constraint | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The guardian must always return a well-formed NOTA response; a malformed guardian response must never happen. Its instructions must specify the exact response format unambiguously, with the verdict grammar and the closed rejection-reason set generated into the prompt, plus a parse-and-retry and a shape test, so an out-of-format reply is impossible in practice. *admission control*

ID 4oev | Principle | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: There should be no NOTA between components. Daemons and components exchange binary protocol data; the CLI is the translation/debugging surface that can wrap a normal call in a debugging request with options such as where logs should be displayed or stored. *IPC*

ID 7xnx | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The guardian judges a submission across several dimensions read from its justification: whether the justification justifies the intent, whether the domain is right, whether the certainty fits the justification, and whether the importance fits the justification. A justification may reference existing records and propose a cross-record operation, for example replacing several existing records with one or more new records at stated importances; the guardian judges that whole proposed operation as part of its single yes or no. *admission control*

ID 88mw | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The guardian needs proper per-operation prompts: each operation type gets detailed guidance with worked examples of what passes as an accept and what is rejected and why. Over-train the guardian first with rich, detailed instructions and many examples, then scale back once it works, because the guardian calls are currently undertrained. *admission control*

ID ans1 | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The upgraded justification is a strongly-typed structured argued case: typed verbatim testimony with optional antecedents, plus a reasoning that argues why the record should be admitted and why its certainty, domain, kind, and importance are right. The claim is the operation own Entry, never a justification sub-field. It is never a stringly-typed blob. Redundant citation-signal tags are excluded; a cited record relationship is carried by the operation or by the reasoning prose. *admission control*

ID bwqe | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Ship the lean core of the intent-justification upgrade first: typed verbatim testimony plus a check that the quoted words actually support the claimed certainty, with a binary guardian verdict. Defer the elaborate court structure, the typed warrant sub-sections, a separate stakes-basis slot, and cross-record motions-as-precedent, to a later phase, adding each only where the lean core demonstrably falls short. *admission control*

ID gad7 | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Supersede must support replacing a retired set with multiple new intents in one atomic operation, not just a single replacement. The reason is correctness, not convenience: when the content of the retired records splits into several new records, replacing them with a single record loses meaning and the guardian should refuse it, so superseding with several replacements must be one operation the guardian judges as a whole, asking whether the new set together preserves the kernel of the retired set. *admission control*

ID ll41 | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: An intent submission carries a justification that argues the case for the intent: the submitting agent sells why this should become intent, embedding the psyche verbatim words with the surrounding context of what was discussed and proposed. A justification can quote several verbatim fragments and can reference previously recorded intents. Verbatim spans in a justification are surrounded by asterisks. *admission control*

ID so3b | Principle | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Certainty is the burden of proof the verbatim testimony must clear. The submitting agent proposes a burden by claiming a certainty level, and the psyche verbatim quotes must clear that level on their modal strength. An over-claimed certainty on hedged words fails its burden and is rejected; the guardian never silently lowers the level. This makes honest understatement the rational strategy because a lower claim faces an easier bar. *admission control*

ID woku | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Intent capture must preserve the psyche modality and never upgrade tentative wording into confident assertion: when the psyche hedges with I think, I feel like, could, or maybe, the recorded certainty must stay low and the wording must not be rewritten from could into should or from a feeling into a fact. The guardian needs visibility into how the psyche actually worded the statement and rejects over-stated capture. The guardian stays a binary yes-or-no gate and never silently lowers certainty itself, because a rejection sends the thinking back to the submitting agent, the highest-thinking-power model, who reconsiders and rewords; keeping the gate binary keeps the thinking where it belongs. *admission control*

ID z3ka | Decision | certainty=High importance=High | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The intent-capture protocol is modeled as a court of law. The submitting agent is an advocate making a case and addressing a jury; the justification is the argued case; the psyche verbatim quotes are the testimony and evidence; the guardian is the judge or jury rendering a binary verdict that justifies admitting or refusing the intent. The design draws on legal argumentation, rules of evidence, standards of proof, and dialectics. *admission control*

ID 3chp | Decision | certainty=High importance=Medium | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The policy and control socket in triad components should be called the meta socket, not the owner socket. *IPC*

ID bwxn | Constraint | certainty=High importance=Medium | domains=Technology.Software.Distributed.ProtocolDesign | referents=spirit
DESC: Spirit should not call the referent guardian when a referent registration request names an already registered referent; existing registry state settles that request without LLM judgment. *admission control*

ID 7l7l | Decision | certainty=High importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Sema classification vocabulary is forbidden on the public contract wire. The six Sema words — Assert, Mutate, Retract, Match, Subscribe, Validate — must NOT appear as request-root tags in any signal-component or meta-signal-component contract; a contract must NOT mirror them as an AuthorizedSignalVerb enum; and a contract event must NOT carry the payloadless SemaObservation classification label. These words belong to sema-engine execution and observation only, never the public contract spine — contract operation roots are domain verbs and the Sema class is something the daemon derives internally, not something a peer sends or names on the wire. This hardens the prior soft guidance that most contracts should-not into a firm prohibition and mandates a cleanup pass now across the six legacy contracts carrying the pattern: signal-mind, signal-router, and signal-criome with its AuthorizedSignalVerb enum, plus owner-signal-persona, signal-persona-spirit, and signal-orchestrate whose EffectEmitted SemaObservation event gets a non-Sema label. Distinct from and milder than the engine-trait and plane leak of record 2593, but forbidden on the same wire-only principle.

ID rj9y | Decision | certainty=High importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign;Technology.Software.Engineering.VersionControl | referents=(none)
DESC: Cross-host component transport for the version-control mirror is a tailnet-bound TCP listener in triad-runtime, reusing the length-prefixed frame codec, with peer identity as a typed closed sum distinguishing kernel-vouched Unix-socket peers from tailnet TCP peers. Ssh-forwarded sockets are rejected as the transport shape.

ID 1gho | Principle | certainty=Medium importance=Medium | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Short headers should encode enough top-level variant shape for cheap routing.

ID xf25 | Correction | certainty=Medium importance=Medium | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Spirit without a configured guardian fails closed rather than accepting ungated writes. *admission control*

ID 2qia | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Root signal shapes should generally begin with an enum that differentiates input and output spaces. Explicit variant numbering over an 8-bit root enum can reserve numeric ranges for those spaces while keeping dispatch cheap.

ID 3gki | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Keep domain-criome contract vocabulary provider-neutral and record-only

ID 7816 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Multi-version dispatch protocol: CLI sends to the next-version daemon; next-version daemon coordinates back with main; missing-next returns error logged-as-failed

ID ajcg | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The Spirit full-schema branch must keep public and permissioned sockets as separate signal schemas, equivalent to the current ordinary and owner contracts, but generated from schema files rather than the legacy signal macro surface.

ID bodd | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The Signal contract repository question is workspace-wide, not cloud-local: every contract repo must be audited for whether it incorrectly carries internal Nexus or SEMA engine surfaces instead of only the messaging vocabulary that crosses the component boundary.

ID bzde | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Header declarations contain an enum identifier plus ordered endpoint nodes.

ID c2ar | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Upgrade orders (triggering the smart handover) come through the OWNER socket of the target component *IPC*

ID c59v | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Per-operation request replies should not repeat operation kind

ID cqxg | Correction | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The 64-bit signal root-verb namespace is PER-COMPONENT not workspace-wide; provenance (which channel/socket the log came from) keys the decoder; universal data variants (U8 U16) stay shared but live in BYTES 1-7 not byte 0; byte 0 is purely each channels own root verb enum (up to 256 verbs)

ID dcqz | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The wire-header pattern is extensible: current 8-byte (64-bit) header is the base; longer headers (256-byte / 512-byte / variable) hold most of the message frame structure. The shorter 8-byte header lives INSIDE the longer one as a prefix - always parseable by code expecting only 8 bytes. Vectors of data come AFTER the header as memory arrays; the header carries length + type-of-vector indexes pointing at them. Most of the message structure fits in the schema-defined header positions; the post-header memory holds the bulk payload (variable-length vectors, blob data). Schema declares which fields fit in 8 bytes vs which need the extended header positions. Header growth is by extension not replacement.

ID duis | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Sema gets its own short header symmetric with signal-side. The sema-engine operations (executor, lowering, command dispatch, database reads/writes) ALL carry the same 8-enum 64-bit short-header structure, with sema-specific root + sub-enum vocabulary. Tap-anywhere observability extends to sema-side operations identically to signal-side. Two parallel headers — signal-header and sema-header — using the same macro machinery but routing to different paths. This is the symmetric counterpart to spirit 388: short headers are universal across both the wire (signal) and the engine (sema) surfaces.

ID dx10 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Agent short identifiers — when an orchestrator launches a new role or unique agent, the agent receives a short ID derived from the first three bytes of its Criome master-identity public key. The short ID is exposed via a shortest_id() method on the full ID; the method checks for collision against currently-active agents and returns the shortest non-colliding prefix (3 bytes by default; can widen up to the NOTA namespace width if collisions force it). Because identities are binary in the signal layer, the collision check is a direct byte comparison on the public-key prefix. *IPC*

ID gq9p | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: PeerCheck retired; signal-version-handover is the single discovery mechanism for next-version daemons

ID jk1w | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign;Technology.Software.Engineering.Architecture | referents=criome
DESC: A running multiple-machine criome cluster is a near-term goal, wanted soon - not deferred behind the single-host propagation loop; multi-machine criome is elevated from eventual to near-term priority. The single-host causal loop is the prerequisite foundation, and the cross-machine enablers follow as near-term work.

ID l96d | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: parallel daemon versions need versioned sockets *IPC*

ID lw73 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Spirit should keep separate ordinary and owner sockets for now; permissions are enforced by filesystem socket access while development still runs locally under trusted user setup. A future general socket with explicit permission prefix is possible but not the current implementation target. *IPC*

ID ooxy | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Full request pipeline: Signal triages wire input → Nexus computes → SEMA performs durable op → Nexus receives SEMA reply → Nexus decides next steps → Nexus returns reply to Signal → Signal frames reply to wire client. Asynchronous throughout; rolling origin identifiers thread through the whole pipeline so each layer routes responses back to the correct waiter, including partial SEMA responses for multi-op work. *IPC*

ID oxur | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Every message prefixed with the 64-bit Tier 1 micro type marker; receivers know type + size + structure of the root object from the prefix; rkyv zero-copy compat (read directly from memory after network-fingerprint check); tap-anywhere sync emission default-on with build-time disable for faster execution; the prefix-of-every-message pattern doubles as observation channel

ID piig | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Extended (512-byte / 64-byte) version of signal types accommodates payloads needing public keys, identities, or larger structured data. Sits between the 64-bit micro variant (Tier 1) and the full rkyv record (Tier 3). Example use: Criome authorization payload carrying quorum public keys plus a signature; component identity references; short string descriptions

ID r2jx | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Signal-frame protocol (framing + short-header dispatch + route enum derivation + codec object) belongs declaratively in a root-level signal-frame.schema file imported by component schemas. Component schemas (spirit.schema, etc) IMPORT signal-frame.schema and INHERIT frame methods on their Input/Output surfaces - eliminating the hand-rolled transport.rs route table + frame encoder/decoder gap per /208 P1. This is schema-as-protocol-substrate: the signal-frame is itself schema-declared, not hand-authored Rust.

ID t312 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The 64-bit signal namespace is partitioned into a system-types zone (SignalCore) and a component-specific-types zone, with pre-allocated zone sizes. Repartitioning the allocation between zones is a major version event with backward-compatibility implications.

ID z9d6 | Principle | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign;Technology.Software.Data.Modeling | referents=criome,spirit,router,mirror
DESC: Criome authorization contracts should be content-addressed composable objects: each component can decide acceptance based on another accepted object or contract, and those dependencies should refer to each other's content-addressed contract objects rather than ad hoc mutable state. Threshold, majority, or time-window acceptance policies are criome contract logic layered on those composable authorization objects.

ID zbuy | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Guardian model performs semantic judgment for every guarded write; deterministic code gathers context, validates structure, and applies typed verdict consequences. *admission control*

ID zrrv | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: A daemon may expose more than one signal protocol or signal interface. Configuration can be modeled as another typed signal surface, differentiated inside the root enumerator that contains the daemon's accepted signal surfaces.

ID 0j2x | Decision | certainty=Low importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Byte-0 namespace splits by golden ratio (~0.39 / 0.61) into two sections; owner contract takes one section, public/ordinary contract takes the other; per-component choice with compile-time macro enforcement (both contracts MUST agree on split or compilation error); default is owner=small public=big; opens potential for single-socket-per-component design where the byte-0 section encodes which namespace the message belongs to

ID ahuv | Constraint | certainty=Low importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: Pi operator-safety must not create repeated confirmation burden during normal work *admission control*

ID amab | Clarification | certainty=Low importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: The input-output split can be represented in the root enum namespace by reserving variant number ranges, so one first-byte header can distinguish request-like input variants from response-like output variants for the same actor mechanism.

ID qbz4 | Decision | certainty=Low importance=Minimum | domains=Technology.Software.Distributed.ProtocolDesign | referents=(none)
DESC: New components: persona-listen (audio input) + persona-speak (audio output) with real-time stream exchange relationship *IPC*

