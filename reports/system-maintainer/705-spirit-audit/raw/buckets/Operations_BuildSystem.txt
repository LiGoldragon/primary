ID 6x2k | Principle | certainty=VeryHigh importance=High | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Use the github remote url form for flake and repository references, both in committed flake inputs and in nix build or nix run commands. Commit and push changes first, then build from the remote, so the build is reproducible, machine-independent, and free of silent uncommitted state. Never build or deploy from a local path checkout or a git-file reference: it is non-reproducible bad form. If a build needs a not-yet-pushed change, push it rather than pointing the build at the dirty local checkout. A local path or override-input is only for throwaway local iteration, never for anything that deploys or lands.

ID ejec | Principle | certainty=VeryHigh importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: avoid invoking nix store hash paths directly; use nix run, nix shell, or nix build --out-link with temporary symlinks

ID 1sx5 | Constraint | certainty=High importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: all tests must run through nix *CI*

ID 4d8f | Principle | certainty=High importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Development tooling for schema-derived crates should regenerate emitted Rust automatically or through one central command whenever schema files change, then rerun the relevant build or tests so agents edit schema first and consume regenerated types.

ID 5fck | Decision | certainty=High importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Trace code must be optional at compile time so spirit-next can build without it, and Nix should expose differentiated packages for normal lean builds versus trace-enabled testing builds.

ID 9xwr | Principle | certainty=High importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: for nix-managed emacs configurations the .eln native-compiled artefacts must be produced at nix build time as part of the derivation, never JIT-compiled at runtime — runtime native-compilation invalidates on every nix rebuild and is a regression

ID gopu | Decision | certainty=Medium importance=High | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Forge is the workspace's emerging build-system component family, intended as the eventual Nix replacement. First version: forge-nix-builder, which wraps Nix straight up — Forge is the persona-system layer above Nix initially, eventually replacing it. Forge-core is the standardization point: the shared contract that sub-component forges adopt as the build ecosystem matures. Each sema-upgrade-shaped component may have its own forge that eventually standardises on forge-core; forge subcomponents have their own language until standardisation. Forge's design discipline is to identify what is eternal in Nix's logic (content-addressing, derivation graphs, hermetic builds) versus what does not belong in the build layer (authentication moves to Criome; binary signing moves to a workspace-owned content-addressed store). Phase Nix out as forge matures.

ID 12mi | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Cloud component implementation should use the existing old NOTA / old signal macro stack, matching the production Spirit-era approach, rather than waiting for the new schema stack.

ID 2hi3 | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Build configuration is itself a NOTA struct with fields — the same single-NOTA-argument rule that governs daemon and CLI input also governs build configuration. Testing-build is a configurable field on that struct; how the build switches between production and testing modes is the same NOTA-shaped declaration as the rest of the build config.

ID 6wz8 | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Synthetic bare-metal builds must avoid unrelated unfree firmware.

ID fyuz | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Synthetic firmware gating work must stay generic and verified with constrained Nix checks.

ID hfbr | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Existing forge daemon stays as the Criome-stack executor; forge-nix-builder extracts as library underneath rather than replacing forge

ID ntsg | Decision | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Each Rust crate that participates in schema generation should carry a standard schema folder with lib.schema as the entry point and additional module schemas such as signal.schema loaded through that root, following Rust module style.

ID pc8t | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Schema boilerplate and Spirit database update validation must run through Nix tests. *CI*

ID uylt | Clarification | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Schema imports should resolve by crate name from the imported crate's standard schema folder, so schema and source travel together as one compilation and namespace unit.

ID zdie | Constraint | certainty=Medium importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: CriomOS must build Spirit's binary startup archive during Nix build/evaluation so the user service starts instantly instead of generating the archive at service startup or activation.

ID z5aw | Principle | certainty=Low importance=Minimum | domains=Technology.Software.Operations.BuildSystem | referents=(none)
DESC: Future forge build logic may let generated Rust code become content-addressed crates directly, reducing the need for separate generated-code repositories once forge can own that build path outside Nix.

