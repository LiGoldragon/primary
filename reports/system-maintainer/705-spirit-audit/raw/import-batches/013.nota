(Import [(4itr ([(Technology (Software (Programming Macros)))] Constraint [|Everything is a serializable data object, the macro most of all: a macro is data with name, position, pattern, and template, where pattern and template are themselves data trees (not text with sigils). Macros pre-assemble, serialize, and load as data; reading a schema loads pre-assembled macro data and interprets schema-data against it. The only code is one tiny generic interpreter; no per-macro bespoke parsing. Test: everything round-trips through NOTA text and rkyv bytes, macros included.|] Maximum VeryHigh Zero [nota rkyv macros-as-data schema])) (xai7 ([(Technology (Software (Programming Macros)))] Principle [|NOTA extension is structural matching over nodes. The structural macro node is a NOTA enum decoded by shape: decode is type-directed, the codec structurally matches each variant in declaration order (most-specific first, first wins), recursively; encode emits a matching NOTA block (bidirectional). Realized as #[derive(StructuralMacroNode)] with per-variant shape attributes, not a runtime registry or string-name dispatch. The raw parser preserves delimiter structure, counts, sigils, and nesting; macros are NOTA-layer typed records (name, structural-match pattern, capture specs, position) dispatched as an ordered list with conflict detection at registry construction. The pattern language is a typed enum of primitives (arity, atom-case, atom-sigil, delimiter-type, atom literal) so macro data stays serializable.|] High VeryHigh Zero [nota structural-macro-node macro-registry type-reference])) (av1q ([(Technology (Software (Programming Macros)))] Decision [|Define assembled schema (.asschema) before sugar: it is macro-free final data — Vec, Optional, Enum, Struct, Newtype, Plain, Carries are final assembled-schema variants, not macro invocations. Sugar belongs only in authored schema before lowering. Schema macros re-emit schema structure toward AssembledSchema (conceptually re-emitting NOTA, intermediate may stay binary) and must expand into a fully one-to-one assembled tree describing all generated types, endpoint variant names, and carried input structs before Rust is emitted. Application is iterative to a fixed point: each pass applies lowerers at macro positions, macros may introduce new macros, until the namespace is pure typed enums, structs, newtypes.|] High Medium Zero [asschema assembled-schema nota schema-macro])) (wx5c ([(Technology (Software (Programming Macros)))] Decision [|Built-in core macros live in the basic schema library and are ALWAYS imported when a schema imports the schema module - no opt-in. They define how to assemble data structures (enum / struct / newtype / etc). User-defined macros are lazy-loaded by name from explicit imports. The split: core macros are non-negotiable substrate; user macros are extensions that compose with the core.|] High Minimum Zero [core-macros schema-library user-macros])) (h6fh ([(Technology (Software (Programming Macros)))] Constraint [|the Macro declaration form is the locked NOTA syntax for declaring user-defined macros inside a namespace: positional record of the form (Macro Input+ Output) - Macro tag plus one or more input shapes plus exactly one output shape. This sits inside the namespace alongside ordinary types per record 843; the macro lives as a registry entry resolvable by qualified name. Per Q2 of designer-assistant 372 the explicit Macro tag wins over the contextual shape-driven matching alternative for introspectability.|] High Minimum Zero [macro nota namespace])) (v0n6 ([(Technology (Software (Programming Macros))) (Technology (Software (Programming Compilation)))] Clarification [|Everything reading NOTA-shaped structure above the raw parser goes through typed structural macro nodes; surviving hand-parsing sites (e.g. the schema-next macro library) are violations to fix, and a shape structural macro nodes cannot express is surfaced to the psyche, not worked around. NOTA itself should have a schema, its generated Rust schema-derived rather than a hand-maintained parser island. Dispatch precedence among competing reference forms (built-in heads, then declared macros, then the generic catch-all) is reified as an explicit typed NOTA value, so every reader and the resolver consult one canonical declared precedence.|] Medium Minimum Zero [nota structural-macro-node schema-next type-reference dispatch-precedence])) (m91k ([(Technology (Software (Programming Macros)))] Decision [|The signal/sema macro derives the typed wire layer from one schema source: signal_channel! embeds the 64-bit Tier-1 micro header by default (LogVariant + frame_micro projection), standardizes the two-enum-namespace shape (ordinary + owner contracts sharing byte-0 namespace via golden-ratio split), derives the small object directly (no hand-impl for Tier 1), gives every enum a Help variant documenting its namespace (command docs hook into Help), emits NOTA encode/decode from the same source, and emits a VersionProjection between current and next schema versions as the upgrade path.|] Medium Medium Zero [signal sema signal-channel-macro nota version-projection])) (xprx ([(Technology (Software (Programming Macros)))] Principle [When you see repetition, extract it into a library or the schema macro; prefer pushing logic back into the schema macro. The more the schema generates the better, as long as it stays good — schema is clear, readable, and shows the logic as object shapes, which is cognitively sensible.] Medium Medium Zero [schema-macro])) (5p9s ([(Technology (Software (Programming Macros)))] Principle [A NOTA formatter can be derived from the macro definitions, since each macro's pattern tells the formatter where line breaks, indentation, and spacing belong.] Medium Low Zero [nota-formatter macro])) (dqmc ([(Technology (Software (Programming Macros)))] Decision [|At a struct field or enum variant position, an @-sigil with a type name and no preceding name derives the binding name from the type: @Topics ≡ topics@Topics (field = camelCase of type); @SomethingHoldingSomething ≡ a data variant named after the held type; @(Vec Topic) derives from the composite. Bare PascalCase at an enum position stays a unit variant; explicit name@Type still works. The EnumVariants macro also supports compact self-tagged variants where tag equals payload type: a one-element parenthesised form expands to tag-and-type; a parenthesised name with a brace body registers a named struct, with a bracket body a named enum. An inline enum-body list may declare names without marking unit vs data-carrying; the codec resolves against the type table (a type ⇒ data-carrying, else unit). Assembled model unchanged — Vec<(Name, TypeReference)> for fields, named Variant for enum variants; derivation at lowering.|] High Medium Zero [nota schema-next type-reference enum-variants same-name-shorthand])) (506w ([(Technology (Software (Programming Macros)))] Decision [|Macros are sugar in the schema layer: the brace-enum form {Variant Payload ...} expands to the canonical paren-list ((Variant Payload) ...) — both lower to the same AssembledSchema, where variants are already (VariantName payload-option) records in a homogeneous vector. The macro engine dispatches on multiple criteria — delimiter, internal shape, root-object count, symbol qualification, and combinations — choosing the most specific. Enum-body vectors honor homogeneity: each element is one variant declaration — a bare PascalCase symbol (unit) or a parenthesised (Name Type) record (data); the older interleaved at-suffix VariantName@ PayloadType is retired as dishonest. The macro shape itself is two-position: pattern it matches, then template for how it expands.|] Medium Medium Zero [nota schema-macro enum-variants assembled-schema vector-homogeneity])) (5mxn ([(Technology (Software (Programming Macros)))] Clarification [|The schema-derived stack (nota-next, schema-next, schema-rust-next, signal-frame.schema, spirit.schema) does not reference Nexus, a separate NOTA-using vocabulary track (nexus + nexus-cli). Reserved sigils ~@!?*= are reserved at the NOTA syntax layer but the schema stack does not consume them; schema macros are plain NOTA records dispatched by position+shape in the schema-next MacroRegistry, not sigils.|] Medium Minimum Zero [schema-next nota-next nexus macro-registry])) (d6if ([(Technology (Software (Programming Macros)))] Principle [Micro-macros are small simple composable macro units that can be named in specific positions within larger macros. Examples: enum-short-syntax-applier, struct-short-syntax-applier, structure-match dispatcher. Larger macros compose them by calling them at attachment points - a macro library pattern where macros are reusable building blocks rather than monolithic transformations.] Medium Minimum Zero [micro-macro macro-library])) (pul9 ([(Technology (Software (Programming Macros)))] Principle [|Macro dispatch is two-phase: a structure-match macro first asks what shape the NotaValue is (enum short-syntax? struct? newtype?), then the matching per-shape transformation macro applies its lowering — they compose because the same syntactic placement can carry different shapes. Schema variants are ordinary vectors or macros, and each macro carries its own schema-reading logic, customizing interpretation by structural shape (e.g. curly-bracket with a single identifier; curly-bracket with even item count where all key positions resolve to a consistent type) rather than positional tokens. This is the schema language's extension mechanism; new shapes arrive as new macros enter the precompiled core or per-schema imports.|] Medium Minimum Zero [schema-macro nota structure-match macro-dispatch])) (sd7x ([(Technology (Software (Programming Macros)))] Clarification [|The schema lowering engine producing AssembledSchema is built from reusable data-carrying macro-variant lowerers at node-definition points: each macro-schema variant carries a struct defining its input type, and at a node-definition the builtin engine dispatches by shape to the matching variant, which consumes its input struct to produce lowered output. The engine is extensible by adding new variants plus their input structs, not by hard-coding shape rules. SchemaMacro in authored macro-library source is a typed/tagged variant carrying the macro-definition payload (not an untyped sentinel string), collected by the macro-library container before projection to serialized macro-library data.|] Medium Minimum Zero [assembled-schema macro-variant schema-macro macro-library])) (sanf ([(Technology (Software (Programming Macros)))] Decision [|The bootstrap core schema (the schema-schema) is implemented in ordinary Rust first and exposed as the macro interface for writing schema macros: it names macro inputs by type (a macro's input type IS its name in the macro), so macros inspect Nota block structure and emit assembled schema data, and people build their own macros against this core. Every schema file is loaded with the schema-schema attached implicitly; it defines the structural shape expected for schema files (root struct + sections) and is the lowest-level macro primitive everything builds on. Built-in macros bootstrap expansion before the user-declared macro space (the schema's creator space for typed macros) expands.|] Medium Minimum Zero [schema-schema macro-interface bootstrap-core-schema schema-macro])) (ydpa ([(Technology (Software (Programming Macros)))] Principle [|Macro loading uses a first indexing pass that collects all macro names from the schema and its imports before any macro is invoked; later passes resolve references by name from the index, enabling forward and out-of-order references across imports, with lazy loading (a macro loads only when referenced during dispatch). Expansion needs indexed reading order with lazy resolution, and passes may hand generic NOTA subobjects to later passes. Macro handlers receive NotaBody streams with outer wrapper delimiters stripped at match time, not the wrapper.|] Medium Minimum Zero [macro-loading macro-index nota-body lazy-resolution])) (rfg9 ([(Technology (Software (Programming Macros)))] Clarification [|Schema macro resolution maps nodes onto a closed macro space whose variants are the built-in type forms (Enum, Struct, Vec, Option, KeyValue, Newtype) via dual dispatch: look at the head keyword first, and if it is a reserved built-in (Vec, Option, KeyValue) dispatch directly and skip structural mapping; otherwise the node is a user type declaration resolved by structural shape — parenthesis holding an inner parenthesis is an enum, a bracket is a struct or newtype. In a strict brace key-value context a key-value pair is one semantic macro-node preserving the NOTA brace: qualified key plus brace value matches the struct macro, plus bracket value the enum macro, plus atom/reference value the newtype macro. Adds a keyword fast-path over the structure-and-position MacroRegistry.|] Low Minimum Zero [macro-space macro-registry dual-dispatch schema-next])) (e8iu ([(Technology (Software (Programming Macros)))] Clarification [|Open macro-node design frontiers, represented as macro-node configuration/data rather than parser magic: macro bodies need an explicit binding-and-reference mechanism for assigned symbols (a sigil such as dollar-sign is a candidate, the firm constraint being references stay visible in the schema language); and single-object sigil sugar where a leading sigil makes one NOTA object stand for a larger pair or field expression when the sigil makes the intended macro case obvious.|] Low Minimum Zero [macro-node nota sigil macro-binding])) (n9ta ([(Technology (Software (Programming Metaprogramming)))] Decision [|The assembled schema is a live serializable artifact — NotaDecode, NotaEncode, and rkyv on Asschema and all substructure — with the in-memory typed Rust value as bootstrap only; the Rust emitter consumes the serialized assembled form (checked-in or build-time-emitted .asschema NOTA files), not the in-memory value directly.|] High High Zero [asschema nota rkyv schema-emitter])) (g2xr ([(Technology (Software (Programming Metaprogramming)))] Principle [Schema is self-hosting all-the-way-down: NOTA's own grammar and types are described by the foundational schema, which generates the Rust that interprets NOTA; the authoring language is itself a macro expanding to assembled-schema data, so the schema-of-schemas generates its own assembled and short-syntax forms. Schema is the load-bearing crystallization of earlier ASCII/component work — self-describing code, wire format, and docs from one .schema file.] High Medium Zero [schema nota schema-engine self-hosting])) (6jdv ([(Technology (Software (Programming Metaprogramming)))] Principle [|Schema files form namespaces (prefix initially the file name) mirroring Rust-style module decomposition: qualified colon-separated names or names split across module files, resolved via typed import variants (all, from path, from repo root, adjacent directory, git; namespace imports with optional selection) and re-exports, never flat-global. Entries can reference other entries, enabling recursive types and meta-schema self-bootstrap.|] Medium Minimum Zero [schema schema-namespace type-reference meta-schema])) (o2xk ([(Technology (Software (Programming Metaprogramming)))] Constraint [A CLI-with-NOTA structurally forces the daemon protocol to be binary: the CLI translates NOTA into the binary protocol calls the daemon receives.] High Medium Zero [nota cli daemon binary-protocol])) (qr9g ([(Technology (Software (Programming Metaprogramming)))] Principle [|engine logic at the high level is tree-vs-tree matching; canonically enum-against-enum, or enum-against-a-mix-of-enums-and-method-calls; when two enums meet under match, the cross-product of their variants is a common-language relationship node point that should be made explicit (often via a trait) rather than scattered across ad-hoc match arms|] Medium Minimum Zero [schema-engine enum trait])) (qe84 ([(Technology (Software (Programming Metaprogramming)))] Principle [Schema macros compose from reusable micro-macros] Medium Minimum Zero [schema macro])) (z9kv ([(Technology (Software (Programming Metaprogramming)))] Clarification [|The schema type — which of the three planes (Sema, Nexus, Signal/Data) a schema declares — is a Kind enum schema::Kind, each variant carrying that plane's schema body (the four-position content), so a schema document's root is a Kind value. This gives the three typed engines their dispatch point and mirrors the Sema runtime envelope at the message level. Kind-versus-Plane naming still open.|] Low Minimum Zero [schema-kind sema nexus signal])) (3742 ([(Technology (Software (Programming TypeSystems))) (Technology (Software (Programming Grammars)))] Decision [|A type's kind is announced at its declaration by the delimiter wrapping its value: brace body = struct, square-bracket body = enum, never inferred from syntactic-slot position. Generics use the reserved pipe-parenthesis delimiter, Name (| [params] body |) — params the binder list, body a nested struct/enum. nota-next already parses PipeParenthesis/PipeBrace, so this is schema-level work; use sites resolve by name like Vector/Map/Optional.|] High Minimum Zero [schema-next nota-next type-reference generics])) (wqdi ([(Technology (Software (Programming TypeSystems)))] Principle [Multi-argument type references and applications use NOTA's flat positional form — head then arguments inline, like Map then K then V — not a grouped form that wraps the arguments in their own delimiters. The head already names the construct, so a trained human or LLM reads the positional arguments correctly without extra grouping, and the flat form is fewer characters while staying equally legible. This settles the Map type-reference shape as the flat head-K-V form and generalizes to multi-argument type-reference applications.] Medium Minimum Zero [nota-next schema-next type-reference map])) (5jac ([(Technology (Software (Programming TypeSystems)))] Decision [|In schema, camelCase names are fields (instances); PascalCase declares or references a TYPE. Field names DERIVE by lowercasing the type name — an entry is just a list of type names (Entry (Topic Kind Summary ...)). A PascalCase type may be declared inline at first use, reused by name; resolution is declare-before-use, single-pass. Top-level namespace entry makes a type export-visible. Divergent field names use a newtype (Certainty (Magnitude)); the bare name-type form declares a distinct newtype, aliases lose it.|] High Minimum Zero [schema-next nota field-name type-reference newtype pascal-case camel-case])) (2cuo ([(Technology (Software (Programming TypeSystems)))] Principle [|The schema type system reduces to one canonical kind plus the unit case: everything is a STRUCT (data-carrying) or a UNIT VARIANT. Preferred shape is enum or vector-of-enum-variants; a Vector is structurally an enum (choice of all its members), a Vec a single-field struct. Maps are reserved for namespaces. Enum data-carrying variants ARE structs; variants are unit (bare PascalCase) or data-carrying (parenthesized), positional, in order. Bool's True/False is a two-unit-variant enum — why NOTA capitalizes booleans.|] Medium Minimum Zero [schema nota enum struct boolean vector map])) (5myr ([(Technology (Software (Programming TypeSystems)))] Clarification [|The NOTA schema namespace section is a key-value MAP {key value ...} of user-defined types — keys are type names, values are definitions — not a flat sequence of separate declarations. Inline enum: EnumName (Variant1 Variant2 ...), parens holding variant names, variants themselves possibly nested enums. Structs are a different syntactic shape. Universal Unknown injection stays a behind-the-scenes macro mechanism; no user-authored Features section.|] Medium Medium Zero [nota schema namespace enum])) (ba6d ([(Technology (Software (Programming TypeSystems))) (Technology (Software (Programming Grammars)))] Decision [|Pipe-brace traits and impls are a data catalog referencing impls/methods already on the Rust side, so schema can name them and a method-calling syntax invokes them from schema bodies. An impl fuses onto its target type: a type declaration carries a trailing pipe-brace impl block, target implicit. Each entry is a bare trait atom (marker), a trait atom plus bracketed method signatures (body-bearing impl), or a bare signature (inherent method); a signature is name + brace parameter struct + return type, naming an existing impl. Calls resolve from receiver shape plus this catalog.|] High Minimum Zero [nota nota-next schema-next schema-cc impl trait pipe-brace])) (iypq ([(Technology (Software (Programming TypeSystems)))] Decision [|The entire built-in type-name vocabulary lives in Schema, not NOTA: scalars String, Integer, Boolean, Path and composites Vec, Optional, Map, Set, Box are Schema's type-reference vocabulary, alongside struct/enum declarations. NOTA stays pure structure (its delimiters) plus the codec and value literals None/(Some x). The (Vec X) tagged FORM and value-vs-type distinction hold ([X] is a vector value, (Vec X) the type) but live at the Schema layer; the codec stays in nota-next as serialization. Refines record 1137.|] High Minimum Zero [schema nota nota-next type-reference codec])) (rnrg ([(Technology (Software (Programming TypeSystems)))] Clarification [NOTA is at heart a hack on the text user interface — using common sense, good patterns, beauty, delimiters, and typed structure. NOTA is a typed language; everything is read as a known type in data-type-theory terms. Structure macro nodes can have structurally different shapes at certain positions because they extend the type-declaration vocabulary, but the macro extension is itself typed, so the node still resolves to a known type. A well-formatted valid NOTA expression decodes reliably to its declared types. This is the deeper rationale for the workspace's NOTA-everywhere direction — typed-text reliability through a beautiful user-facing surface.] High Minimum Zero [nota])) (3sq4 ([(Technology (Software (Programming TypeSystems)))] Clarification [|At a slot whose type is already known to be an enum, the authored value may omit only that enum type name and provide the enum body directly. Thus ((Parse Expression) (Render Expression)) is valid when the surrounding slot supplies the outer enum type; the fully named equivalent would be (SomeEnum ((Parse Expression) (Render Expression))). Only the known outer type name is omitted; variant tags remain named inside the body.|] Medium Minimum Zero [nota schema enum])) (3itj ([(Technology (Software (Programming TypeSystems)))] Principle [|Schema-defined type trees are normally 6-7 layers deep: enum-of-struct-of-enum-of-struct-of-enum-of-struct etc. The same types are referenced in multiple places throughout the tree via a TYPE INDEX (a flat index of named types that the tree refers into). Deep nesting is the normal shape, not an exception. The schema engine must traverse and emit through arbitrary depth via canonical recursive walks; no ad-hoc per-layer handling. Cross-reference resolution goes through the type index so the same type appearing in many places resolves to one canonical declaration.|] Medium Minimum Zero [schema type-index])) (ugig ([(Technology (Software (Programming TypeSystems)))] Decision [|The schema root plane surface is a data-carrying enum named Plane with Signal, Nexus, and Sema variants carrying the actual plane messages, so runtime code matches directly on the plane rather than pairing a thin kind tag with a separate envelope. This supersedes the earlier thin per-plane envelope-type approach while keeping its goal: engine trait signatures type-check on plane membership (a signal-plane message cannot pass where a nexus/sema message is expected), so the trait chain is impossible to mis-wire on order and plane axes.|] Medium Minimum Zero [schema plane signal nexus sema type-checking])) (aipc ([(Technology (Software (Quality Testing)))] Constraint [Tests prove real behavior: every pipeline step is genuinely implemented end-to-end and does its real job; green tests alone are not correctness. Audit tests for genuineness, prefer real-binary-via-Nix proof over in-process synthetic state, and when blocked build the minimal real version of the missing piece IN the test. Forbid stubs-dressed-as-done, circular golden tests re-reading the engine's own output, per-file readers standing in for the shared codec, and text-round-trip black-boxes.] High High Zero [nix integration-testing genuine-tests])) (l50b ([(Technology (Software (Quality Testing)))] Constraint [|Every load-bearing intent statement manifests as a constraint test (falsifiable spec or type-system witness per skills/architectural-truth-tests.md) that fails if the intent is not honored — structural/behavioral pins, not principles-in-docs. Audits feed bead filing toward constraint-tightening and end-to-end sandboxed daemon-to-daemon integration coverage.|] High High Zero [constraint-test spirit integration-testing])) (vcin ([(Technology (Software (Quality Testing)))] Constraint [|Proof of build/deploy/test claims must compile, execute, or round-trip the real path; grep is allowed only as a narrow negative guard proving a retired symbol or forbidden pattern is absent, never as positive proof. Test-claims cite a concrete file path, the invocation, and the witness output; the workspace audits whether claims match reality. A quiet ~27-minute local check is suspect unless it is a known heavyweight build (e.g. Rust toolchain).|] High Medium Zero [grep test-claim verification]))])