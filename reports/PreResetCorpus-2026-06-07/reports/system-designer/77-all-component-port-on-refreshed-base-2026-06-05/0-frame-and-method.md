# 77.0 — Frame & method: port ALL components onto the refreshed foundation

Role: system-designer. Date: 2026-06-05. Session unit: this directory (GC'd whole).
Ultracode session (xhigh + workflow orchestration).

## The psyche prompt

After choosing **Option A** for message (build the existence-log, realize `l3k4` —
captured as Spirit `alom`), the psyche said: *"A — and refresh intent, more work landed
on basic crates, and keep your porting to all other components — signal has moved a lot
so check it out first."*

Three working orders (none is durable intent — only the Option-A Decision was, captured
as `alom`): (1) refresh intent; (2) extend the port design from the
message/router/orchestrate three to **all other components**; (3) re-baseline the moved
foundation crates, **signal first**, before porting.

## The foundation moved a lot — confirmed (the reason for a re-baseline)

Report 75 designed the ports against a foundation snapshot that has since shifted
materially. Verified in source/log this session:

- **`signal-core` → `signal-frame`.** `signal-core` now carries a deprecation banner
  pointing at `signal-frame` + `signal-sema` (three-layer alignment). Ports build on
  `signal-frame` (frames + the schema-crystallized streaming surface), not the
  deprecated `signal-core` transport report 75/3 named.
- **`schema-rust-next` tokenization landing.** Recent commits tokenize engine-trait,
  nexus-runner, trace, and streaming emission via `quote!`; runtime string emission is
  explicitly "marked as debt." Realizes `de8i`/`4np2`/`o7a3`/`e6v5`; Asschema removed
  (`vez8`, Maximum).
- **`sema-engine` grew identity surface** (`add identified record families`, `add
  identified mutation`) — the `ox7e`/`fosp` surface — and is now the **exclusive** DB
  boundary; raw redb in a component (even the spirit pilot) is a violation (`fosp`).
- **`triad-runtime` streaming committed.** `add streaming subscription runtime` +
  `register pre-minted subscription tokens` landed (report 9 had it uncommitted).
  `MultiListenerDaemon` + `MultiListenerRuntime` shipped and exported (`rcn3` corrects
  `latq` — nothing new to build; lojix is first consumer).

## Method — two sequenced workflows (stay in the loop between them)

Per ultracode multi-phase guidance, this is **two workflows**, not one, so the
load-bearing recipe is verified before 13 component plans inherit it:

1. **Re-baseline** (`foundation-rebaseline-for-port`): four parallel deep reads
   (wire/frame kernel, schema emitter, sema-engine storage, triad-runtime) → one
   adversarial synthesizer that produces the refreshed recipe and enumerates exactly
   what is stale in report 75/3. Output → `1-refreshed-port-recipe.md` (this dir).
2. **All-component sweep** (launched after the recipe is reviewed): a pipeline over every
   remaining component (mind, introspect, system, harness, terminal + terminal-cell,
   persona, repository-ledger, upgrade, cloud, domain-criome, criome, agent), each read
   against the refreshed recipe, each producing a port plan; then a synthesis with the
   cross-component roadmap. Outputs → numbered per-component files + `N-overview.md`.

## The component set (from `protocols/active-repositories.md`)

Already designed (report 75): **message, router, orchestrate**. Pilot: **spirit**.
Active first-driver of the runtime primitives: **lojix**. Cloud port already
intent-decided (`vnnx`/`ey5p`). To plan here ("all other components"): **mind,
introspect, system, harness, terminal, terminal-cell, persona, repository-ledger,
upgrade, cloud, domain-criome, criome, agent** — with special cases noted (persona is
the engine-manager/supervisor; agent's daemon does not exist yet, only `signal-agent`;
criome is a BLS-specific daemon; terminal-cell is a low-level primitive under terminal).

## Discipline

Read-only on `/git` code repos (design session). The Option-A decision was captured to
Spirit (`alom`) BEFORE this work, per capture-intent-first. No further intent in the
prompt (the rest are working orders). Raw per-agent output lives in the workflow
transcripts; this directory carries the synthesis.
