# 191 - Intent-first context maintenance frame

Kind: Meta-report frame  
Topic: second-operator context maintenance, intent audit, constraint table  
Lane: second-operator  
Date: 2026-05-25  
Intent cut: Spirit record 655

## Frame

Psyche asked for an intent-first maintenance pass: refresh the latest intent
before touching old reports, agglomerate the second-operator report lane into
a small current picture, audit repository intent files and related architecture
surfaces, and keep a constraint table that can be checked against new intent.

This directory is the one meta-report for the pass because a sub-agent was
explicitly authorized for report agglomeration.

## Current hot intent

The pass treats these Spirit records as the active window:

| Records | Topic | Active meaning |
|---|---|---|
| 628-631 | schema | Parentheses are enum/variant syntax; square brackets are struct/field-vector syntax. |
| 635-636 | schema/workspace | Design corrections apply globally unless explicitly scoped. |
| 637-643 | schema | Schema-derived Rust emission is a fresh top-down composer named `emit_schema!`; it must not route through legacy `signal_channel!`. |
| 644-651 | intent/reports/architecture/code | Refresh intent first; track intent cut positions and hot topic windows; maintain constraint tables; treat repo `INTENT.md` as core; reopen library/interface design when implementation feels wrong; keep code elegant and self-describing; agglomerate report lanes after intent refresh. |
| 652-655 | interaction | Model workspace logic as interactions between typed domains: `Interact`, match matrices, typed unavailable/error outcomes, and async engine-state queries through the actor system. |

## Method

1. Captured the new psyche intent through Spirit before editing reports.
2. Queried current Spirit records for schema, workspace, intent, reports,
   workflow, architecture, code, and orchestrate.
3. Read fresh designer, operator, and second-designer reports around schema
   engine, delimiter reversal, and `emit_schema!`.
4. Audited repo intent/architecture surfaces for `schema`, `nota-codec`, and
   `signal-frame`.
5. Dispatched one sub-agent to agglomerate old second-operator reports into
   `1-report-agglomeration.md`.
6. Synthesized the current state, constraint table, gaps, and retirement plan
   in the overview.
