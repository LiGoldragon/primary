# 52 - handover 2026-05-14

*Designer-assistant context-maintenance checkpoint. This is a short
rollover for the current Signal/Sema work, not a full archive. The
substance lives in the linked reports, skill, protocol, and repository
architecture files.*

## What landed

- `reports/designer-assistant/50-signal-core-base-verb-shape.md` —
  independent DA research supporting seven `signal-core` roots:
  `Assert`, `Mutate`, `Retract`, `Match`, `Subscribe`, `Atomic`,
  `Validate`; the other five names are read-plan vocabulary.
- `reports/designer/162-signal-verb-roots-synthesis.md` — designer's
  canonical synthesis adopting the seven-root shape, with status-aware
  implementation handoff.
- `reports/designer-assistant/51-review-designer-162-signal-verb-roots.md`
  — DA review trail plus the current maintenance note.
- `skills/contract-repo.md` — now states the seven `SignalVerb` roots
  and says `Constrain`, `Project`, `Aggregate`, `Infer`, and `Recurse`
  live in `sema_engine::ReadPlan`, not as frame roots.
- `protocols/orchestration.md` — report lanes now explicitly have
  implied locks; agents should not claim reports in their own lane.

Observed live repo state during this maintenance pass:

- `signal-core` has `SignalVerb` with seven variants,
  `RequestPayload::signal_verb()`, payload-first construction, and
  `unchecked_operation` as the named escape hatch.
- `sema-engine` owns `ReadPlan` / query-algebra vocabulary and states
  that schema/catalog operations are catalog data under the seven roots,
  not an eighth `Structure` root.

## What's open

- **Contract sweep.** Some direct `signal-core` consumers may still need
  `SemaVerb`/`sema_verb()` to `SignalVerb`/`signal_verb()` migration.
  `/162` is the current source of truth for which crates are done vs.
  pending.
- **Designer 157 cleanup.** `/162` still marks
  `reports/designer/157-sema-db-full-engine-direction.md` §4 as pending:
  the old twelve-row verb-storage table should become seven root
  behaviors plus five read-plan algebra behaviors.
- **Operator integration.** Operators are actively migrating
  `signal-persona`, `signal-persona-mind`, `persona-mind`, and related
  direct consumers. Review compile/test status after that work lands.

## Decisions to preserve

- `Atomic` and `Validate` keep their current implementation names.
  `Bind` and `Assay` are language-design notes only.
- `Structure` is not a root now. Schema/catalog changes default to
  `Assert`, `Mutate`, `Retract`, and `Atomic` over catalog records.
- Runtime user-defined types do not automatically force `Structure`;
  they force `Structure` only if they require new Signal boundary
  semantics that catalog writes cannot honestly model.
- `Handshake` remains frame-control, not an operational Signal verb,
  unless the user later decides transport setup must itself become a
  root operation.

## Side notes

- The user corrected report-claim behavior: reports are per-lane, so a
  role's report subdirectory is already its implied lock.
- There was unrelated working-copy dirt during this pass:
  `primary.code-workspace`, `system-assistant.lock`, and deletion of
  `.claude/scheduled_tasks.lock`. This maintenance pass did not touch
  those.

## Next-session targets

1. Read current `/162` before acting; it has the freshest status table.
2. Check whether the direct contract sweep is complete across all
   `signal-*` crates.
3. If implementation is green, retire or update older DA reports that
   still describe the twelve-root model as active architecture.
4. Keep future Signal root additions behind concrete traffic. Do not
   add `Structure` speculatively.
