# 01 - handover 2026-05-14

*Poet-assistant context-maintenance checkpoint for the Book of Sol /
library publishing lane.*

## What landed

- The Book of Sol publishing protocol now lives in
  `/git/github.com/LiGoldragon/TheBookOfSol/skills.md`: publishable
  articles use YAML front matter with `kind: article`, `publish:
  true|false`, and `banner_image`; published articles embed the
  banner image in the Markdown body as well as the Substack manifest.
- "In Praise of Agni" is published at Substack as post `197113361`,
  slug `in-praise-of-agni`. Its banner image is
  `/git/github.com/LiGoldragon/TheBookOfSol/generated-images/in-praise-of-agni-banner.png`.
- "Refinement" was published, then explicitly unpublished at the
  user's request. `substack post delete 197122040` succeeded; its
  source has `publish: false`, and its `.substack-posts.json` entry no
  longer carries a `post_id` or `slug`.
- Arthur M. Young's *Science & Astrology: The Relationship Between
  the Measure Formulae and the Zodiac* was acquired through `annas`
  into the library:
  `/git/github.com/LiGoldragon/library/en/arthur-young/science-and-astrology.pdf`.
- The Young PDF matches the bibliography hash exactly:
  `2a6d9827e1359b7b9401bb73820d7b5f`.
- A readable local OCR extract was generated at
  `/git/github.com/LiGoldragon/library/en/arthur-young/science-and-astrology.ocr.txt`.
  It is 2,397 lines / 115,232 bytes and is searchable enough for
  ordinary reading and quotation-finding.
- Library commit `774d56db` was pushed to `main`. It records the
  exact filename in `bibliography.md` and adds `**/*.ocr.txt` to
  `.gitignore`.

## What's open

- The Young PDF and OCR extract are intentionally ignored by the
  library repository, following the library convention: binaries live
  locally and are recovered by bibliography MD5 through Annas.
- The library working copy still has unrelated local GC / hooks /
  runtime state on top of `main`, including `.beads/*`,
  `.claude/skills/core.gc-*`, `.codex/hooks.json`, `.gc/settings.json`,
  `.runtime/session_id`, and `library/.codex/hooks.json`. Those were
  not part of the Arthur Young acquisition.
- The primary workspace is dirty from other roles. Current observed
  files include `AGENTS.md`, `skills/reporting.md`,
  `reports/designer-assistant/53-signal-core-cli-verb-implementation-audit.md`,
  `primary.code-workspace`, `system-assistant.lock`, and deletion of
  `.claude/scheduled_tasks.lock`. Do not fold those into a
  poet-assistant commit.
- Poet currently has an active lock on
  `/home/li/primary/systematic-Rudhyar-astrology-extraction-into-BookOfSol-Rudhyar-subsection`.
  Coordinate before touching that lane.

## Side notes

- The title the user asked about as "Science and the Zodiac" resolves
  to Young's *Science & Astrology*. The subtitle is the zodiac piece.
- The PDF is a scan with an OCR text layer. `pdftotext` extraction is
  readable, though front matter includes library/catalogue noise.
- For Substack CLI work, keep using lore's `substack/basic-usage.md`
  plus TheBookOfSol's `skills.md`; avoid `--publish-linked-files`
  unless recursive link publishing is specifically intended.

## Next-session targets

1. Use
   `/git/github.com/LiGoldragon/library/en/arthur-young/science-and-astrology.ocr.txt`
   as the readable local source for Young.
2. If another machine lacks the PDF, recover it through Annas with MD5
   `2a6d9827e1359b7b9401bb73820d7b5f` and filename
   `science-and-astrology.pdf`.
3. Before any Book of Sol publication, check `publish:` front matter,
   `.substack-posts.json`, and the in-body banner image link together.
4. Before any library commit, run `jj st` and separate unrelated GC /
   hooks / runtime state from bibliography or source-acquisition work.
