# Pi 0.78 update — 2026-06-01

## Result

The earlier update was to Pi `0.77.0`. After the follow-up correction that `0.78.0` is available, I updated the Home package to Pi `0.78.0` and activated it locally.

## Commits

- CriomOS-home `804547afc43a` — `home: update Pi to 0.78`.
- CriomOS `d4bff3cd2eb3` — `system: repin CriomOS-home for Pi 0.78`.

Both commits are pushed to `origin/main`.

## Validation

- Built `github:LiGoldragon/CriomOS-home#pi` from the pushed ref.
- Built the pushed-ref Pi Home checks:
  - `pi-harness-profile`
  - `pi-criomos-extension-load`
- Activated Home on `ouranos` from pushed `github:LiGoldragon/CriomOS-home/main`.
- Verified the live profile reports `pi --version` as `0.78.0`.
- Verified a local Prometheus-backed Pi smoke prompt using `criomos-local/gemma-4-26b-a4b-ud-q4-k-xl`; it returned `OK`.
- Evaluated pushed `github:LiGoldragon/CriomOS/main` for `FullOs goldragon ouranos` through `lojix-cli` after repinning CriomOS-home.

## Notes

The currently running Pi conversation was launched before the profile activation. New Pi processes will use `0.78.0`; this existing process should be restarted when convenient if the runtime itself must be from `0.78.0`.
