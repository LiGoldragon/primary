# Psyche-vision context handover — the Protos engine effort (2026-07-21)

Retired vocabulary (psyche ruling 2026-07-21): "mouth" -> textual interface; "organs" -> the two trees (nametree, structuretree); "spine" -> core invariant / core pathway; "door" -> entry point; "currency" -> value type. Historical text below is unreworded; read it through this table.

## HEALTH WARNING
This carries ONLY the psyche's vision: verbatims, rulings, hedges, open questions, durable pointers. No agent chronology. Verify every factual claim at the named artifact before acting. Quotes marked as his are his; derivations are flagged and are NOT rulings. Codex-claimed facts are evidence, not fact, until witnessed.

## 0. Binding conduct (carried laws, plus this session's)
- NEVER use the Fable model for subagents; Opus at most. Workflows forbidden — subagents only.
- All-green truthful denominators; no bypasses seated as design; fabrication barred, disclosure never; the psyche is the human.
- Field-name TOTAL ban in the Protos family; positional disambiguation; deterministic ordinal Rust names.
- Codex delta channel: every new ruling reaches the running Codex ONLY as a paste-ready numbered delta the psyche pastes; never a document edit. Ledger reached line 40 this session.
- Consult Spirit before raising joints; questions derivable from accepted intent must not reach the psyche.
- NEW — "one single question, simply explained, at a time." And no manufactured permission gates: "so you dont have a real question" — proceed on settled rulings; only genuine joints reach him.
- NEW — paraphrase hazard: never compress his words into standalone slogans ("that doesnt even make sense" — his rebuke of a mangled fragment). Spirit records come from his dictation, never from chat-fragment compression.
- Communication: explanations from basics; plain ASCII visuals, multiple layers, no Mermaid; no unexplained agent jargon ("section 4 of what!?" — his rebuke); no first person in Spirit records.

## 1. Vision settled this session (his words)
- Privacy re-founded: "A private spirit should just be a spirit demon running in a certain environment. Privacy would then be enforced on the Criom side with authorized access... we'll control privacy through access... the engine is many demons." The daemon is privacy-naive; which daemon a caller can reach IS the privacy decision. Criome-side environments (cluster identity, quorum-guarded replica tables): direction only, "I don't wanna go too deep into the Criom."
- Certainty removed: "the concept of everything that's in Spirit is essentially certain. It's all high certainty. So we only leave the importance."
- Referents dissolved: "I dont see a good reason to keep referents. if we have two string fields in each record, what is the point?" — v14 store is ONE family (IntentRecord); subjects are inline canonical atoms resolved at admission by the admitting agent; v13 referent rows are consumed by the fold. And on referent routing under isolation: "I dont understand the problem about privacy if spirit's privacy is done at the cluster-level?" — correct; the routing question was an artifact of the dead family.
- Near roadmap: "near roadmap is getting everything running on the new protos engine and testing the hell ouf of it." Cross-criome propagation (Cluster Authorization Slice Design §4) stays PARKED; working sockets "are not public facing, until we design a component specifically for that."
- Generation-context pin: "it would involve all 3 engines, since some logos will be 'standard logos' (ostensibly the only pre-written logos) which can change the output, and nomos can obviously change the output as well." Envelope pin = one entry per engine (schema, nomos, standard logos), content id + nametable id each.
- Pipeline corrections (his): "the nomos transformation happens after files are read into the encodedform, both in schema and nomos." "There isnt really a logos source; logos is all generated from a nomos transform on schema (encodedform); we can get the logos textualform to inspect the result. or we might hand-write some logo to test stuff in logos." Manifest lists schema + macro sources only; logos text is inspection projection plus a hand-written test door.
- Provenance granularity: "go with m2" — pin identities only, fetch schema slice BY NAMETABLE IDENTITY; no document-revision wrappers in the contract; storage packaging (DocumentKey+Version) never crosses it.
- Escape set sealed: $x realize + $@xs splice; splice = typed vector concatenation, vector element positions only, one level, may contribute zero elements; records and enum payloads refuse it statically. His verbatim was "agreed" to a manager-worded package (flagged so on tracker). Optionality needs no new syntax: disjoint shapes / option positions / empty vectors (standing law).
- SEMA universals (his questions drove them; shapes are manager derivations he confirmed): retention "A" (prune only after digest-verified projection to a cold SEMA store); collection = mark (atomic lifecycle flip) + sweep; migration = sealed layout version + one-way typed fold, reject-not-coerce, frozen reader per window, HAND-WRITTEN disposable crate; per-row history = the versioned commit log; FAMILY LAW: an application family holds only application data — shared concerns are engine features declared in schema. "I like the two-family storage issue... Let's see how much of this can be abstracted into re-usable logic/macros." (Two families later became one when referents dissolved.)
- Actor split "looks great"; lanes ruled option (c): per-component schema-declared closed lane set, standard minimum Working + Meta; backpressure = Close (typed closure on full queue, never loss). Head-digest privacy: "I dont know" — ObserveHead stays meta by default.
- Delegation: "go with your leans and give codex a handover with references to implement." All lean-sealed rulings are flagged manager-lean on the tracker with his verbatims distinguished.

## 2. Executed and standing (verify at pointers)
- Delta ledger 26–40 issued in chat this session. 26–28 pasted by him; 29–39 evidenced pasted (Codex answered 38 and accepted 39). **Delta 40 (referents dissolved) paste UNCONFIRMED — if unpasted, Codex builds the dead ReferentRecord family.**
- Codex accepted the authoritative order (his reply, delta 39): S1/S2 fix -> merge slicing -> complete Core*->Encoded* rename train immediately (explicit boundary) -> delta-38 identity/core producers -> contracts -> SEMA identity index -> engines -> signals -> four-process witness.
- S1/S2 at last witness: UNFIXED, branch tips unmoved (protos c7510d35, core-schema 827995c3). S2 = five .expect() sites in name-table non-test code (table.rs:69,:81,:181,:209; transaction.rs:53); S1 = README says Encoded* over Core* code.
- Escape ruling captured: tracker notes on epic + .2/.42, RULED stamp on macro-escape-taxonomy-v1.md, commit bb445a22. Delegated-rulings batch captured as notes on epic + .13/.39/.48/.46 (beads store is outside jj; no commit exists for that batch — the notes are the record).
- Privacy/certainty strip (deltas 35/36): Entry -> { Domains Kind Description Importance Referents }; ops Records (merged), TextSearch, redefined Intent; ChangeCertainty dead; SubscribeIntent filter loses two axes; judge/guardian prompt repos EXCLUDED from the strip, awaiting the burden-model pass. Fold: routing by the existing public predicate into two environments; certainty dropped; Certainty::Zero sentinel -> lifecycle Archived (delta 37).
- Provenance ruling (delta 38) accepted by Codex; his implementation consequences are Codex-claimed evidence awaiting the next normal cross-examination: identity->slice storage index; new nomos generation-content identity (current MacroPackage hash omits output-affecting selection); typed Standard Logos semantic object with own content identity; typed missing/mismatch/wrong-namespace/provenance-free failures. His rejection of a cross-layer document-revision locator is a correct application of 38 ("stored occurrence is not language identity").
- Audit-verified this session (firsthand worker evidence): rename-twins share content hash (witness test exists); predecessor HashFetch is first-match (sema-storage lib.rs:381) and absent from the current stack; composed borrow is branch-only; sema-engine already carries commit log, checkpoint/import digest verification, staged advance, layout-fold reject-not-coerce.

## 3. OPEN on his desk
1. Two Spirit records his to dictate: drastic engineering (higher abstraction than the rejected draft, ruling-psyche voice, no first person); privacy-by-isolation (his dictation only — the manager compression was rejected).
2. Judge burden model after the strip: admission bar reduces to genuine directive intent, clarity, non-duplication, honest Importance — needs his direction or approval of a design pass. Blocks the judge-prompt repos' strip.
3. cloud + meta-signal-cloud still consume the retired signal-domain-criome contract — migrate or retire (suggested: scout their liveness first).
4. Orchestrate daemon 0.14.1 deploy (no git/ssh in PATH, no reap levers, phantom admin commands; one deploy + PATH fix clears all) and CriomOS criomos-home flake.nix/lock mismatch — ownership unassigned; suggested: one OS-front dispatch.
5. Delta 40 paste confirmation (see §2).
6. Carried review-pile, untouched: .9 syntax slate, .38 wire short-header layout, .12 lost bootstrap question (only HE can restate it), .14 reports/logos hygiene; clarification-skill discussion; public-repo verbatims question; CommitSequence item-envelope rendering (.43).
7. Family law + this session's SEMA/privacy rulings have no doctrinal home yet (tracker notes + deltas only); the accepted SEMA redesign artifact is NOT amended — its corrections live in deltas 30/35/36/40. Manifestation pass pending.

## 4. Derivations pending nothing / flagged
- Universal collection/migration shapes, lane option (c), backpressure Close, fold-by-public-predicate: sealed under his delegation, flagged manager-lean on tracker. Explanations delivered in chat this session (the presentation he asked for); no explanation debt is carried.
- Codex delta-38 consequences (§2) and branch test-greenness figures (96+25): Codex-claimed, unwitnessed.

## 5. Durable pointers (verify here)
- Tracker epic primary-56d1; new ruling notes on epic and .2 .13 .39 .42 .46 .48; .50–.54 delete-on-merge conditions unchanged (meetable after cascade re-pin, delta 26).
- Predecessor handover: reports/logos/protos-engine-psyche-handover-2026-07-20.md (its carried laws remain in force).
- Design authorities: macro-escape-taxonomy-v1.md (RULED stamp, bb445a22); id-namespace-slicing-design-v1.md; protos-engine-next-layer-design-v1.md; textual-form-vision-design-v2.md; sema-engine ARCHITECTURE.md (the existing engine substrate); agent-outputs/ProtosEngine/SpiritSemaRedesign/spirit-sema-daemon-redesign.md (ACCEPTED WITH AMENDMENTS — read only with deltas 30/35/36/40); core-schema branch ProtosSourceFormsProposal 398d92e SOURCE_SURFACE_DECISION.md (accepted, witness-gated); agent-outputs/PersistentSpiritMirror/ClusterAuthorizationSliceDesign.md (§4 = the parked propagation slice; spirit/src cites its section numbers without naming it — fix instructed in delta 34).
- Producer branches: protos + core-schema identifier-slicing-contracts on HOLD until S1/S2; SpiritV14Implementation branches (protos a591162, core-schema f3167f4) and ProtosSourceFormsProposal are Codex's live work, un-audited internally.
- The running Codex has ONLY what was pasted. The delta ledger (11–25 prior session, 26–40 this session) exists only in chat plus this file's summaries.
