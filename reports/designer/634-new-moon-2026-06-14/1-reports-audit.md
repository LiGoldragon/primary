# New-Moon Meta-Report: Audit of Designer Reports 624-633 (2026-06-13/14)

## 624 — Audit of report 623's self-hosting-macros series
**Established.** A 21-agent claim-by-claim audit of the retracted `623` series against real source: 3 outright fabrications (`node.lower()`, an invented `Arity` schema type, a "we shipped `Application(ApplicationHead, …)`" claim), 1 won't-compile bootstrap (the `StructuralMacroNode` derive is enums-only), plus misleading premises — chiefly that macros-as-data is *already built and test-proven* in schema-next (`MacroLibrary` + `DeclarativeSchemaMacro`), not a future capability. It re-states the real names and the genuine remaining frontier (self-host the macro-table type itself).

**Current accuracy — STILL TRUE.** This is the factual bedrock the rest of the arc builds on; nothing since has contradicted it. Its "real frontier #1 — self-host the macro-table type" is exactly what 625 specs and what 632 still lists as in-flight. One forward-looking caveat it raised (the "schema generics proven across 5 branches" framing from 618-622 "may be overstated; re-audit before relying on it") was effectively answered by 630/631: the generics work was real and got reconciled and integrated. Accurate and load-bearing.

## 625 — Operator spec: schema-emit the macro-table noun family (self-host POC stage 1)
**Established.** A code-grounded operator handoff (bead `primary-bojw`, the PascalCase-clean schema-emitted macro-table noun family) to generate the macro-pattern noun family from `core.schema` and swap out the hand-written copies in `declarative.rs`, with the `MacroTemplate` structural-vs-typed-kind fork explicitly fenced off as designer/psyche work and proof required as Nix witnesses.

**Current accuracy — STILL TRUE as a spec; partly OVERTAKEN conceptually.** The slice remains the named, in-flight self-host stage (632 still lists it "in progress"). Two later softenings to note: (a) the `MacroTemplate` design fork it gates on was *dissolved conceptually* by Spirit `2zed` / kinds-as-macros (626) — "pattern and template are ordinary typed fields, not special kinds" — though that is a deeper redesign than this slice, so the spec's stop-line still holds operationally; (b) its own header already retracts the "macro = runtime reaction frame" framing, which 626 formalizes. The spec's mechanics (file:line inventory, the `MacroPatternObjects` wrapper concern, Nix-witness proof) are still valid.

## 626 — Vision: a language is data (structural macros all the way down)
**Established.** The north-star synthesis: a programming language *is* a set of structural macros, and the differentiator is keeping that macro set as typed data rather than frozen in a compiler — anchored to Spirit `7c71` (Principle, VeryHigh) and a ladder of supporting records (`2zed`, `t85k`, `j9du`, decision `wfdt`). Includes the formal retraction of the "a macro is a reaction" category error.

**Current accuracy — STILL TRUE.** Pure vision/intent grounded in recorded Spirit records; nothing in the day's integration touches it. Its brick table still reads correctly (macro definitions done; macro-table nouns in flight via 625; macro-node shapes = report 103 next). The retraction it carries is consistent with 625's header and with 627's "correct schema↔engine bridge is a typing relation" note. Durable.

## 627 — Structural Forms: the concept (flagship naming doc)
**Established.** Names and frames the paradigm — "the grammar *is* a type, matched by shape, recursively, and because node-types are data the language is open" — recommends **Structural Forms** (with Shape Grammar as the punchier alias), tells the "aski was right, the method was the trap" story, and honestly separates what's real today from what's still designed.

**Current accuracy — STILL TRUE.** Concept/naming doc; unaffected by integration mechanics. Its "real vs designed" section correctly points at the operator slice (`primary-bojw`) and report 103 as the live bricks. The naming recommendation is still an open ask to the psyche (no evidence a final name was chosen). 632 adopts "Structural Forms" as the working term throughout, so the recommendation has at least informal traction.

## 628 — Reality-check: the Structural Forms epic against intent + implementation
**Established.** Grounds the psyche's epic brain-dump against intent + code: the destination is sound, but **three current-state premises are stale** — (1) "move the signal schema out of Spirit" is already done; (2) the "keep a generated-Rust reference copy / co-host for the orphan rule" model is backwards (per-owner generate-and-commit); (3) "the engine is a set of actors" is the recorded *target* (`zk6y`), not the current one-actor synchronous build. Plus a 33-worktree audit.

**Current accuracy — STILL TRUE, and its flagged drift is now RESOLVED.** The one item that was live drift when first written — meta-signal being wrongly grouped daemon-local — carries an in-report RESOLVED note, and I verified it against spirit main: the local `meta-signal.schema`/module is gone, and `src/lib.rs` does `pub use meta_signal_spirit::schema::meta_signal::*` (commits `4ec746b` + `adeba15`). So Claim 1's correction is now fully reflected in built state. Claims 2 and 3 remain accurate (per-owner generate-and-commit unchanged; "set of actors" is still target, not built). The worktree audit is the most perishable section — branch lists churn daily and several of its "review/remove" worktrees were since consumed by the 631 reconciliation and the integration branch, so treat that section as a 2026-06-13/14 snapshot, not current.

## 629 — Actor counts and granularity (grounded research)
**Established.** Answers "am I overreaching for actors?" with surveyed evidence (BEAM, Akka, Orleans, Pony, CAF, kameo/tokio): low hundreds of actors is conservative by 2-4 orders of magnitude; the real axis is per-boundary (isolation / single-writer / supervision / per-entity lifecycle), not count. Corrects the recalled "50 million actors/GB" to 50M *messages*/sec.

**Current accuracy — STILL TRUE.** External research grounded in dated sources (kameo v0.20.0, April 2026); independent of the day's integration. Its conclusion reinforces 628's Claim 3 and 632's Layer 4 (the three engines *should be* kameo actors; the current single-`EngineActor` is sanctioned-as-drift). No drift. Standalone-durable reference.

## 630 — Cross-lane review (operator main vs designer branches)
**Established.** A 9-agent adversarial review across the seven epic repos producing an 8-target action split. Three headlines: meta-signal drift already fixed by operator (CONSISTENT); the reaction frame consistent across three lanes (clean NEEDS_INTEGRATION); and **`TypeReference` (#2/#3) is the one real conflict — a blocking designer decision**. Proposes the reconciliation recipe (keep `schema-generics` lineage, fold in `typeref`'s three improvements; heads `Vector`/`Optional`/`ScopeOf`; flat `(Map K V)`).

**Current accuracy — targets #2/#3 SUPERSEDED by 631; rest STILL TRUE.** As 631 explicitly states, the "designer reconciles `TypeReference` first" action (#2/#3) is **done** — superseded by the 631 outcome; what remained was operator integration. The meta-signal CONSISTENT verdict (#7) is confirmed on spirit main. The other NEEDS_INTEGRATION targets (#1 reaction frame, #5 store-decomposition, #6 sema-engine, #8 nota-next leaves) were subsequently acted on by operator on the integration branch (633). The open psyche question it surfaced (flat vs nested Map) was answered — 631 records flat `(Map K V)` per Spirit `wqdi`. So 630 is correct *as the snapshot that triggered 631*, with its central conflict now closed.

## 631 — TypeReference reconciliation — outcome
**Established.** Reports the `TypeReference` conflict reconciled into one designer branch set (nota-next `next/combined-leaf-shapes` `70a246b6`; schema-next `next/typeref-structural-generics` `17b4ebc7`), building/testing green (nota-next 69, schema-next 159). Folds `schema-generics`' generics lineage + three `typeref` improvements (derive-single-source, thiserror, HeadedAtom for `(Bytes N)`); applies full-English heads + flat `(Map K V)` (`wqdi`); notes `TypeReference` self-host is still *partial* (top-level decode keeps a hand-written delegating impl — a named edge of the "tiny seed").

**Current accuracy — TRUE as the designer outcome; its handoff was only PARTLY honored.** The reconciled branch `17b4ebc7` is real and present (verified in schema-next's jj refs). But its operator handoff — "integrate the reconciled `next/typeref-structural-generics`, not raw `schema-generics`" — was **not** followed: I confirmed `17b4ebc` (and its sibling `b336f4e`) are **not ancestors** of `origin/structural-forms-integration`, and the integration branch still carries the hand-written machine codec with no thiserror. So 631's "three judgment calls" and "partial self-host" findings remain accurate, but its three folded-in improvements were lost in integration — which is precisely what 633 catches. 631 stands; its handoff is the thread 633 picks up.

## 632 — The architecture, and the agent's friction
**Established.** Two halves: a top-down, diagrammed architecture walk-through (substrate → structural forms → schema → schema-Rust → component triads → intent layer → self-host seed), and a candid account of the agent's friction with context, visuals, and code — arguing the architecture *is* the answer to that friction (intent layer = external memory; schema-derived code = less fabrication surface; visuals-from-schema = the shape the agent can't hold).

**Current accuracy — STILL TRUE.** Architecture-level by design (no file:line to rot). Its self-host-closure paragraph correctly cites 631's partial-`TypeReference` finding and the `primary-bojw` slice as still-in-flight. The friction half is reflective and timeless. One forward reference — "the coming auditor role" — matches the AGENTS.md roadmap (auditor still shape-decided, not built), so still accurate. The architecture digest it proposes as a load-on-start surface is essentially what this report half *is*. No drift.

## 633 — Operator integration review (8 repos)
**Established.** Designer review of operator's `structural-forms-integration` branch (8 independent re-running reviewers): corrects the report's *own* wrong premise up front (reviewers flagged "not on main," but branch-not-main is the *intended* state per the psyche's directive); 6 of 8 repos faithful; **two real regressions** operator's verification missed — schema-next reverted 631's three fold-ins (hand codec back, thiserror gone, HeadedAtom seam dropped), and spirit's `testing-trace` broke under `--all-features` (the `Arc<SemaDatabase>` guarded direct write bypassed the `SemaEngine::apply` hook so `SemaWriteApplied` stopped firing).

**Current accuracy — premise correction STILL TRUE; one regression since FIXED, one still OPEN.** I verified both: (1) both repos' `structural-forms-integration` branches are confirmed **not on main** — 633's "intended branch state" correction is right (and is the cleaner statement of the prompt's "operator integrated the branch": integration landed on the branch, not main). (2) **spirit regression #2 is now FIXED** — operator advanced the branch past the reviewed `d2cf86f` to `bb6bff6 "spirit: trace guarded direct sema writes"`, which adds `#[cfg(feature = "testing-trace")] self.trace_direct_sema_write()` on the guarded direct-write paths, restoring the trace 633 flagged. (3) **schema-next regression #1 is still OPEN** — the integration branch HEAD is still `2bb0228`, the hand-written `NotaDecode for TypeReference` machine codec and `from_fixed_bytes_width` are still present, thiserror is still not a dependency, and the 631 reconciliation commits are still not ancestors. So 633's action item #1 (carry forward `17b4ebc7`'s fold-ins) remains outstanding as of this audit; item #2 is resolved; item #3 (remerge-to-main + drop branch pins) remains the pending directive step. The "review loop worked" conclusion holds.

## The report arc — how they chain

The ten reports form one tight new-moon cycle: **distrust → audit → vision → concept → reality-check → research → review → reconcile → synthesize → re-review**, the designer-operator loop closing on itself.

- **624 is the ground-clearing.** The psyche's distrust of `623` triggers a forensic audit that not only kills the fabrications but reframes the whole problem: macros-as-data already exists; the real frontier is self-hosting the macro-table type. Everything downstream stands on this verified floor.
- **625 acts on 624's frontier #1** — turning "self-host the macro-table type" into a concrete operator slice (`primary-bojw`), code-grounded against the source 624 verified.
- **626 → 627 lift the same insight to vision and then to a named paradigm.** 626 anchors "a language is data" in the Spirit corpus; 627 gives it the public name (Structural Forms) and the "aski was right, the method was the trap" story. Both carry forward 624/625's retraction of "macro = reaction."
- **628 is the honesty checkpoint** — does the epic match built reality? It finds the vision sound but three premises stale, and surfaces the one live drift (meta-signal), which then gets fixed mid-thread.
- **629 is the lateral research spur** — answering the "am I overreaching for actors?" worry 628's Claim 3 raised, with surveyed evidence; it backs the "engines should be actors" target.
- **630 is the cross-lane reconciliation pass** that turns vision into an integration plan, isolating `TypeReference` as the one blocking designer decision.
- **631 resolves that decision** (superseding 630's #2/#3), producing the reconciled branch — and honestly flags that `TypeReference` self-host is still partial.
- **632 synthesizes the whole architecture top-down** and turns the lens on the agent itself, arguing the architecture is the answer to the agent's own limits — and names the next reductions (intent digest, schema-driven diagram emitter, auditor).
- **633 closes the loop** — designer reviews operator's integration of all that branch work, catches two regressions the operator's own verification missed (one since fixed, one still open), and validates that the Designer-Operator review protocol did its job.

The through-line: **ground in source before asserting (624), make the truth data (625/626/627), check it against reality (628/629), reconcile rival designs before integration (630/631), then re-verify the integration adversarially (633)** — the same "make the truth data, keep it legible, derive what you can, and always verify" discipline 632 names as the system's core. The single still-open thread at cycle's end is 633's schema-next action item #1: the integration branch was built from raw `schema-generics`, not 631's reconciled `17b4ebc7`, so the three code-quality fold-ins are still missing on `structural-forms-integration`, and neither integration branch has reached main.