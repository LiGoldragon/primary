# System re-situation — frame and method

The psyche asked to step all the way back and be re-grounded in the system we're
touching, then the system within it, then the system within that — about three
layers — top-down, in plain language, checking understanding at each step,
because the implementation detail (the `Absent`-marker tradeoff) had lost them
and they could no longer engage with the design. Then save it all to meta-reports
for a context restart.

Trigger (psyche, verbatim): *"when you start losing me like this, I don't feel
like I understand the design anymore… I can't really respond to you so I need to
understand the system better… step back and explain to me everything… explain the
system right now that we're touching. And if I don't understand we'll step back
another step… maybe three layers."* (Spirit `obo5`.)

## The three layers

1. **The reaction frame** — the system we're directly touching: the universal
   arrive→decide loop every component shares, why fourteen hand-copies is the
   problem, and the one-generic-frame fix. (`1-layer-1-the-reaction-frame.md`)
2. **The component + schema→Rust pipeline** — the system within: what a daemon /
   signal / sema / nexus is, and how a component's types are *generated* from a
   schema rather than hand-written. (`2-layer-2-...` — written when we reach it.)
3. **NOTA + the intent→Spirit→schema foundation** — the substrate: the notation
   everything is written in, the structural-macro decode model, and how the
   psyche's recorded intent drives the whole pipeline. (`3-layer-3-...`)

## Method

Explain layer 1, check it lands, go inward only as far as the fog requires —
the psyche drives the depth. Saved incrementally so context can restart
mid-re-grounding. The earlier dump (report 620) is the comparable precedent.

This re-grounding is upstream of the schema-generics work (reports 618–622),
which is design-proven and handed to operator as beads — that work is paused at a
clean point while we re-situate.
