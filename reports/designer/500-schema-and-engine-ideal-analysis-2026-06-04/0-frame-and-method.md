---
title: 500 - Schema and engine ideal analysis - frame and method
role: designer
variant: Synthesis
date: 2026-06-04
topics: [schema, schema-in-action, ideal-vs-current, reusable-pattern, triad-engine, design-analysis]
description: |
  Orchestrator frame for a four-agent analysis: for each thing in the
  persona engine, what is the ideal — the most correct, future-oriented,
  reusable pattern — versus what we have now. And the centerpiece the psyche
  most wants: schema in action, concretely, this-code-creates-this-code,
  backward and forward, inside and outside. This is the start of a sustained
  schema focus, not a one-shot.
---

# 500 - Schema and engine ideal analysis

## The directive

The psyche, 2026-06-04: do an in-depth analysis of all these things — for
each, what should it be ideally, the best idea, the most correct,
future-oriented, reusable pattern; and what do we have now. And: start
focusing more and more on schema. [I want to see how it works, in action,
backward and forward, up and down, inside and outside] — keep giving what
goes into the schema, what comes out, what the schema creates, the
implementation: [this code creates this code, over and over again, in
reports]. This applies to operator and designer; the operator acts on
implementing the rest.

Captured: record 2549 (Principle, High) — schema is the growing focus; both
roles keep producing the in/out/creates schema-in-action reports as an
ongoing discipline. Record 2550 (Principle, Medium) — design analysis
targets the ideal pattern, contrasted explicitly with current state.

## Method

Four research agents; each writes its numbered entry here and returns
structured findings. The orchestrator synthesises the schema-in-action
report and the ideal-vs-current analysis. Schema is the weighted focus.

| Entry | Slice |
|---|---|
| 1 | **Schema in action** — the concrete this-code-creates-this-code walkthrough of the real spirit schema, multi-angle (forward, backward, inside, outside, the rkyv layer). The centerpiece. |
| 2 | Schema stack ideal-vs-current — per repo (nota-next, schema-next, schema-rust-next, triad-runtime): the ideal reusable pattern vs what exists. |
| 3 | Triad engine + components + cutover ideal-vs-current — the broader engine. |
| 4 | Open design questions ideal patterns — SymbolPath, the runner, the RustItem token model, help namespace, NOTA config-by-convention. |

The synthesis establishes the recurring schema-in-action report shape the
psyche wants repeated; the operator acts on the implementation.
