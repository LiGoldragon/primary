# 682 — overview again + context maintenance: frame and method

The psyche asked for a refreshed system overview and a context-maintenance sweep,
with the note that system-operator is landing the **struct schema syntax fix** (which
resolves the 681 validation caveat — the positional dot-differentiator form moving
onto schema-next main, and the `structural-forms` skill becoming consistent with it).

The session since the last overview (675/677) produced a lot: the criome→Telos level
correction (criome is the agreement machine *within* Telos), the integrated PoC (680),
both propagation forks resolved (Fork A subscribe + `l2ha`; the contract-scheduled
heartbeat in `m0p2`), and the `signal-standard` shared-vocabulary design (681, `eeeo`).
So both an updated overview and an intent/report hygiene pass are due.

## Method — 2 grounding/audit perspectives + 1 synthesis

| File | Perspective | Grounding |
|---|---|---|
| `1-intent-audit.md` | intent-layer coherence sweep — the session's criome/Telos records verified for duplicates / contradictions / staleness; maintenance flagged (not applied) | the Spirit store |
| `2-state-and-staleness.md` | current state (landed vs designed vs open) + the SO struct-syntax fix + report/skill staleness | criome / signal-criome / schema-next main, recent reports + skills |
| `3-overview.md` | **the refreshed overview + the context-maintenance findings** | all of the above + 675/677/680/681 |

The audit *recommends* intent-layer fixes; the orchestrator applies any (guardian-gated)
supersede/clarify/remove itself. The synthesis is the refreshed overview the psyche
asked for, plus a maintenance ledger (verified-coherent / fixed / flagged-for-action).
