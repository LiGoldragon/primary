# 560-archive — removed Spirit records (scratchpad + borderline)

designer, 2026-06-08. Full NOTA text of every record removed from the live Spirit intent store in the 560 cleanup (psyche-authorised: "clean all the way to borderline, inclusive"). Preserved here so the intent layer is clean while nothing is lost — any genuine durable kernel can be re-recorded cleanly from this archive. Classification (verdict / pollution type / reason) is in 560.

## Scratchpad (184)

- `03ok` [scratchpad/too-specific-ephemeral]
  ```
  ([03ok] [persona-pi] Clarification [Third-designer report 20 is relevant Pi harness input] Medium Zero)
  ```
- `03s3` [scratchpad/working-instruction]
  ```
  ([03s3] [criomos llm gemma testing] Clarification [When testing Gemma 4 on Prometheus, use Pi headless calls as the client-side probe and root SSH to inspect Prometheus llama-router service logs and resource behavior.] Medium Zero)
  ```
- `0am6` [scratchpad/too-specific-ephemeral]
  ```
  ([0am6] [testing] Clarification [Designer Spirit nspawn worktree path] High Zero)
  ```
- `0yad` [scratchpad/status-snapshot]
  ```
  ([0yad] [spirit] Decision [start using spirit v0.1.1 after database update] High Zero)
  ```
- `1mko` [scratchpad/status-snapshot]
  ```
  ([1mko] [audio dji mic keep alive broken] Correction [The existing ad hoc DJI keep-alive service no longer works because it does not show up in the volume control panel, so the user cannot route the DJI mic to it to keep the microphone hot.] High Zero)
  ```
- `1ndi` [scratchpad/working-instruction]
  ```
  ([1ndi] [desktop rescue terminal shortcut] Clarification [Use Ctrl+Mod+Enter as the candidate custom shortcut for the dedicated rescue terminal, pending keybinding conflict check before final implementation.] Medium Zero)
  ```
- `1vbj` [scratchpad/production-framing]
  ```
  ([1vbj] [schema signal contracts] Constraint [signal_channel! is deprecated; published signal and meta-signal contract crates should migrate to schema-next/schema-rust-next derived contracts instead of retaining the old macro surface.] VeryHigh Zero)
  ```
- `1zmv` [scratchpad/too-specific-ephemeral]
  ```
  ([1zmv] [workspace] Decision [lane formerly named second-designer-assistant is renamed to third-designer (Structural / parallel-main authority)] High Zero)
  ```
- `27xa` [scratchpad/working-instruction]
  ```
  ([27xa] [schema-macros make-it-more-real designer-assistant-375] Principle [every claim in 375 needs empirical proof; make-it-more-real implies running code with shell transcripts proving v0.3 capability through schema-emitted route plus SignalCodec; if a part proves infeasible document precisely and commit partial state and flag in open shape questions.] Maximum Zero)
  ```
- `2i3s` [scratchpad/working-instruction]
  ```
  ([2i3s] [workspace] Clarification [operator should read latest designer synthesis and surface biggest intent questions] High Zero)
  ```
- `2i87` [scratchpad/working-instruction]
  ```
  ([2i87] [spirit-next schema-stack e2e-tests schema-emitted-rust prototype] Constraint [Restart the schema-derived prototype from the latest main stack and make end-to-end tests center schema-emitted Rust and schema-type traits so generated schema objects drive the system and tests exercise the thing being built.] High Zero)
  ```
- `3b26` [scratchpad/working-instruction]
  ```
  ([3b26] [beads] Decision [Sweep stale beads, especially items referencing roles that no longer exist] High Zero)
  ```
- `3t40` [scratchpad/working-instruction]
  ```
  ([3t40] [audit-quote-check] Decision [verify no quotation marks appear in NOTA output] Maximum Zero)
  ```
- `3vdg` [scratchpad/working-instruction]
  ```
  ([3vdg] [deployment cloud] Decision [Deploy the local model service token to the prometheus llama-server through sops-nix in the production criome configuration, then deploy the new inference node carrying the Gemma 4 model.] High Zero)
  ```
- `4eo5` [scratchpad/working-instruction]
  ```
  ([4eo5] [workspace] Constraint [Claim primary-kbmi.2.1 scopes as second-system-assistant before editing and release when done] High Zero)
  ```
- `4lcv` [scratchpad/status-snapshot]
  ```
  ([4lcv] [upgrade upgrade-from accept-previous version-projection manual-upgrade schema-rust-next deferred] Clarification [The schema-rust-next emitted UpgradeFrom and AcceptPrevious traits are stub code, not yet a real upgrade mechanism — that work has not been reached. Upgrades are performed manually for now. The upgrade-component port should not build on the UpgradeFrom or AcceptPrevious dispatch yet; the schema-emitted upgrade surface is deferred future work. Answers system-designer report 77 decision 2.] High Zero)
  ```
- `4msi` [scratchpad/working-instruction]
  ```
  ([4msi] [persona] Constraint [Operator should continue persona engine handover work until no clear work remains, ideally reaching readiness to deploy persona-daemon for full component no-downtime upgrades.] High Zero)
  ```
- `561o` [scratchpad/working-instruction]
  ```
  ([561o] [workflow designer operator mutual-study convergence] Decision [designer rebases their work onto the newest main of operator and operator works on main again to include any fresh intent the designer has surfaced as valid in the designer prototypes; each role studies the OTHER role's work FIRST before creating the next version - designer studies operator main current state including the recent schema-next restructure and operator reports while operator studies designer prototypes and recent designer reports for valid surfaced intent; only after mutual study do both create the next version implementing the psyche directives in their respective worktrees; include the result in docs intent and architecture as the canonical next-version representation] High Zero)
  ```
- `5dg6` [scratchpad/too-specific-ephemeral]
  ```
  ([5dg6] [domain-criome] Constraint [Limit primary-kbmi.2.1 assistant writes to three domain-criome runtime worktrees] High Zero)
  ```
- `5nic` [scratchpad/working-instruction]
  ```
  ([5nic] [component-shape] Decision [Delete the persona-sema repo. It came from an older design phase when there was thought to be a separate database crate per component. Verify current contents first, absorb anything useful into other repos (likely sema-engine, signal-sema, or persona daemon code), then delete the repository from GitHub] High Zero)
  ```
- `5qjh` [scratchpad/working-instruction]
  ```
  ([5qjh] [workspace] Decision [File research-and-trial beads for /292 section 3.5 library finds: unitbus (systemd D-Bus SDK), kameo 0.16 Scheduler, rkyv 0.7-to-0.8 audit, winnow 1.0.0 stable close-out for primary-36iq] High Zero)
  ```
- `5zd6` [scratchpad/status-snapshot]
  ```
  ([5zd6] [workspace] Clarification [MVP development is the active workspace phase — all design, audit, and operator work prioritizes MVP migration urgency. Counter-ego work folds critique with active advancement and concrete fix execution.] High Zero)
  ```
- `63vm` [scratchpad/working-instruction]
  ```
  ([63vm] [workspace] Decision [Trigger a deep context refresh sweep across the accumulated workspace material - reports, intent records, skill files, architecture documents - viewed through the schema-warrants-per-channel reframing (records 668-670). Goal stated by psyche: 'wake up like I had a fresh night's sleep and you just woke up all fresh and not a bunch of staling, rotting context reports'. Subagent dispatched to roll through context and surface (a) what survives unchanged under the new framing, (b) what is superseded or stale, (c) what needs new framing under the schema-per-channel-contract lens, (d) what is freshly load-bearing. The synthesis report is the designer's job AFTER the subagent returns - the subagent assists context refresh; designer writes the final synthesis. One subagent (per psyche directive 'just one I guess').] High Zero)
  ```
- `65lk` [scratchpad/working-instruction]
  ```
  ([65lk] [component-shape] Decision [To execute the sema-upgrade canonical architecture, designer must specify concrete shape: Migration trait library crate location, trait method signatures, peer-check message shape, dispatch implementation locations (CLI / daemon / signal-X libs), persona-introspect recovery decode helpers] High Zero)
  ```
- `6ant` [scratchpad/working-instruction]
  ```
  ([6ant] [spirit] Decision [extend the spirit-nspawn upgrade test to send messages to the socket during the v0.1.0 to v0.1.1 transition; verify whether a caching actor replays them after the new daemon takes over] High Zero)
  ```
- `6d0b` [scratchpad/status-snapshot]
  ```
  ([6d0b] [spirit] Decision [Begin using the Spirit CLI as the substrate for intent logging immediately, to work out the kinks of the deployed pilot while the legacy intent/*.nota append flow is in the process of being retired.] High Zero)
  ```
- `6fwc` [scratchpad/working-instruction]
  ```
  ([6fwc] [spirit repository] Decision [Rename spirit-next to spirit and delete the stale spirit repository rather than treating spirit-next as a permanent second Spirit repository.] Medium Zero)
  ```
- `6gb0` [scratchpad/agent-conclusion]
  ```
  ([6gb0] [schema schema-next nota-next sigils tokenization] Clarification [Correcting my own record 1095 with the implemented reality: NO nota-next pin bump is required for the sigil grammar. nota-next parse-atom breaks atoms only on whitespace semicolon and delimiters at every pin, so atVec and ParseStar are single atom tokens regardless of how that pin classifies at and star as symbol-vs-text characters. schema-next reads the sigil from the raw atom text via demote-to-string (MacroInvocation strips the at prefix; VariantTag strips the star suffix) rather than through the symbol-filtering schema-name, so the grammar works whether or not nota-next treats at and star as symbol characters. The sigil belongs to schema-next grammar not nota-next lexis. Implemented and proven green: schema-next built and tested against nota-next 5e063042 locally and against fff3dd19 transitively in schema-rust-next.] High Zero)
  ```
- `6k8k` [scratchpad/status-snapshot]
  ```
  ([6k8k] [cloud] Constraint [Cloud component production slice uses old Rust signal macro path] High Zero)
  ```
- `6mz4` [scratchpad/working-instruction]
  ```
  ([6mz4] [reporting] Constraint [Schema macro work needs an all-encompassing operator report explaining current understanding and implementation state.] High Zero)
  ```
- `71yj` [scratchpad/working-instruction]
  ```
  ([71yj] [workflow subagents] Constraint [For this Nota/schema prototype and vision task, use subagents in parallel and report their findings before synthesizing the final implementation vision.] High Zero)
  ```
- `71zi` [scratchpad/working-instruction]
  ```
  ([71zi] [workspace] Decision [Intent manifestation + unimplemented-intent audit subagents in parallel; output is small-component-shape operator beads that other subagents can share] High Zero)
  ```
- `7es5` [scratchpad/working-instruction]
  ```
  ([7es5] [spirit] Correction [Spirit component remote/package naming should be checked against the intended persona-spirit to spirit rename] Medium Zero)
  ```
- `7het` [scratchpad/working-instruction]
  ```
  ([7het] [horizon schema pipeline prototype handoff] Constraint [System operator should inspect the system-designer Horizon schema pipeline prototype frame before deciding how the working schema-rust prototype should converge with it.] High Zero)
  ```
- `7i7i` [scratchpad/working-instruction]
  ```
  ([7i7i] [beads] Decision [Find implementable or preparable beads before choosing next work.] High Zero)
  ```
- `7o63` [scratchpad/status-snapshot]
  ```
  ([7o63] [schema nota] Clarification [Latest schema grammar: schema files are read against a known root type, root Input and Output labels disappear, base schema is just namespace, reactive schema root is positional input enum body then output enum body then namespace, same-name data-carrying variant sugar uses suffix star such as Record* lowering to (Record Record), macro invocations use at-sign heads such as (@Vec (Topic)) (@Option (Topic)) (@KeyValue (Topic Entry)), unsigiled Vec Option and KeyValue forms are stale, and brace namespace is key/value style rather than a parenthesized declaration list.] VeryHigh Zero)
  ```
- `7s0j` [scratchpad/too-specific-ephemeral]
  ```
  ([7s0j] [stt-interpreter psyche transcription-drift vocabulary-mapping] Clarification [STT may transcribe psyche as SIDEQ — close-sound mishearing. Stt-interpreter lookup table should map SIDEQ to psyche for future STT contexts. The psyche flagged this directly in chat.] Medium Zero)
  ```
- `7ync` [scratchpad/working-instruction]
  ```
  ([7ync] [workspace] Decision [Persona-pi triad (reports/designer/266) is not ready for direct implementation. The next workflow step: the prime designer writes a referencing piece that bundles the persona-pi design report, the pi-mono API research, and the cited intent records into a single brief for the operator. The operator then writes an implementation proposal based on this brief; the designer reviews the proposal. This is the designer-as-bridge workflow at finer grain — the bridge writes an operator-input piece, not just a report-numbered design.] Maximum Zero)
  ```
- `7zhf` [scratchpad/too-specific-ephemeral]
  ```
  ([7zhf] [stt] Correction [Prior chain-function transcript was speech-to-text corruption, not psyche instruction] High Zero)
  ```
- `8675` [scratchpad/status-snapshot]
  ```
  ([8675] [scope-deferral backpressure runtime-control inner-engine actor-prioritization future-deeper-runtime not-touching-for-a-while] Decision [Workspace explicitly defers backpressure handling runtime control layer inner Nexus engine actor scheduling/prioritization and related deeper-runtime work. These are future-deeper-runtime that won't be touched for a while. Spirit 1465 (inner Nexus engine) + designer 478 (recursive runtime control) + operator 289 (NexusControlCommand backpressure) all carry the deferred-future label going forward. Production-orientation focuses on the substrate (NexusWork/NexusAction + effects per component + Stash) and component interaction (spirit-next + introspect first pair). Runtime control extensions land later if/when overload evidence appears in real production load.] High Zero)
  ```
- `8gmx` [scratchpad/too-specific-ephemeral]
  ```
  ([8gmx] [deploy] Clarification [Prometheus node has existing nspawn-based sandbox testing — pointer for the audit start] Minimum Zero)
  ```
- `8jjl` [scratchpad/too-specific-ephemeral]
  ```
  ([8jjl] [open-question-leans] Decision [Confirmed leans from /172 mockup overview §3: (a) imported types render UIDs under source schemas component not importing schemas (intent §3.1 lean b); (b) DurableIdentity types eventually migrate to signal-persona-origin alongside UnixUserIdentifier (intent §3.4 lean b); (c) feature-branch worktrees at ~/wt per-feature path shape] High Zero)
  ```
- `8p9o` [scratchpad/working-instruction]
  ```
  ([8p9o] [naming] Decision [Bundle primary-fka1.1 (crate rename signal-persona-auth -> signal-persona-origin) and primary-7ru6 (EngineId/RouteId/ChannelId rename in same crate) into one operator pass] High Zero)
  ```
- `92a3` [scratchpad/working-instruction]
  ```
  ([92a3] [deployment cloud] Decision [Push the deployment through on the criome main branch now rather than coordinating onto the horizon reconcile. Extend the llama module for the sops api-key and an hf-model gemma source, make the lojix-cli change to carry the new sops secret, land the fetch-hf-model utility to the lib main branch, and deploy to prometheus.] High Zero)
  ```
- `94n4` [scratchpad/working-instruction]
  ```
  ([94n4] [schema signal] Decision [Concept build authorized: explore signal-frame schema-derivation in a design-repo (per skills/double-implementation-strategy.md design- prefix). Permanent home for signal-frame.schema decision (existing signal repo if its current schema is retirable, OR signal-frame repo, OR new dedicated repo) follows concept verification. Per psyche 2026-05-26 directive.] High Zero)
  ```
- `950e` [scratchpad/working-instruction]
  ```
  ([950e] [spirit] Decision [defer the spirit v0.1.0 to v0.1.1 cutover until the schema-macro full vision is remedied across receive-dispatch, version-projection, upgrade-migration, and nspawn private-socket handover] High Zero)
  ```
- `9p7y` [scratchpad/working-instruction]
  ```
  ([9p7y] [cloud audit component-triad] Constraint [After implementation, audit the prototype with the critique standard that all designed components must be used fully.] High Zero)
  ```
- `9rxq` [scratchpad/status-snapshot]
  ```
  ([9rxq] [spirit-next sema durable redb sema-file config] Clarification [spirit-next realizes records 1007/1008 durable SEMA: the Store is a redb database written to a .sema file with a records table (identifier to rkyv-archived Entry) and a ledger table persisting next-identifier and commit-sequence counters so a store reopened from the same .sema path resumes its records AND its commit sequence (next write after restart continues the count not restart at 1); StateDigest is now a real content-addressed blake3 hash over the committed records reduced to the schemas Integer width with an empty store digesting to zero replacing the wrapping_mul(31) toy fold; the daemons single NOTA argument became a positional (socket database) record so the .sema path fills an existing config position with no flag (single-argument rule holds) and a bare socket-path argument still works defaulting the .sema beside the socket; this closes bead primary-q2au; the pilot uses redb directly behind a mutex rather than the sema-engine/kameo single-writer substrate to keep the proof self-contained] High Zero)
  ```
- `a5u7` [scratchpad/working-instruction]
  ```
  (a5u7 [deploy] Decision [Adopt /156 Gap 2 Path A: Spirit v0.1.0 protocol-aware maintenance build. Rebuild persona-spirit v0.1.0 from main (with handover protocol code, same DB schema), re-tag as v0.1.0, redeploy as production. Protect the no-downtime precedent established by intent 203. Path B (staged with sema-upgrade-handover-temporary offline copy) is rejected as compromising the no-downtime story] High Zero)
  ```
- `ae2s` [scratchpad/working-instruction]
  ```
  (ae2s [browser-use largeai cloud] Decision [Psyche asks this lane to try the browser-use browser-driving path against the deployed local largeAI model endpoint, rather than only describe the design.] Medium Zero)
  ```
- `ae85` [scratchpad/working-instruction]
  ```
  (ae85 [workspace] Constraint [Subagents are still calling jj in ways that open the editor despite the AGENTS.md hard override. Reinforce the signal at a lower level: provide an easy out-of-the-box quick-reference for common jj invocations so the inline-only discipline is impossible to miss. The hard override is right at the principle level but agents apparently need concrete command cheat-sheet form. Action: augment skills/jj.md (or create if missing) with a quick-reference table of common commands + their inline-required forms.] High Zero)
  ```
- `aeek` [scratchpad/too-specific-ephemeral]
  ```
  (aeek [workspace] Decision [Gap-closure-vs-migration framing: Design C hybrid - keep gap tracking but relax /249 primary-focus framing; migration work IS gap-closure-relevant; transition to explicit rescission once persona engine has a working slice] High Zero)
  ```
- `aet6` [scratchpad/working-instruction]
  ```
  (aet6 [fully-schema-and-nota-mvp] Decision [Build all-encompassing understanding report + MVP example with whatever worktrees needed to implement all blockers: nota + schema + the nota-macro system with builtin macros (where builtin macros are themselves macros for READING macros - the meta-circular substrate). Sub-agent has permission to fork any repo and unblock all blockers per intent 546. The goal is a runnable end-to-end demonstration of the multi-pass macro system reading a real .schema file through shape-logic dispatch.] High Zero)
  ```
- `ayfl` [scratchpad/working-instruction]
  ```
  (ayfl [spirit] Decision [Disable Spirit v0.1.1 until migration test] High Zero)
  ```
- `ayhd` [scratchpad/status-snapshot]
  ```
  (ayhd [running-spirit-concept designer-track] Decision [Designer-assistant beginning end-to-end-runnable spirit concept on new architecture per psyche 2026-05-26 record 845: minimum-viable-daemon proving NOTA-at-boundary plus rkyv-on-wire plus actor-engine handling for ONE operation Record end-to-end. Designer feature branches designer-running-concept-2026-05-26 on signal-spirit and spirit. Schema-rust-next at f76d6483 already emits rkyv derives plus NOTA codec impls so emission gaps may be minimal. Will document precisely what is hand-rolled vs emitted in report 368.] High Zero)
  ```
- `b2mk` [scratchpad/too-specific-ephemeral]
  ```
  (b2mk [cloud cloudflare deployment] Clarification [Psyche asks what is needed to use the cloud component to deploy new DNS name records on Cloudflare.] Medium Zero)
  ```
- `b2xk` [scratchpad/status-snapshot]
  ```
  (b2xk [repository schema] Clarification [New clean repositories for the Nota/schema stack are now an agreed direction to consider for implementation, while old-repo feature branches remain acceptable when they are the faithful place to prototype quickly.] High Zero)
  ```
- `b559` [scratchpad/agent-conclusion]
  ```
  (b559 [schema plane-message origin-route runtime] Clarification [The data-carrying Plane enum (record 1054) folds the auto-created origin route (records 1038/1039) into each variant as the leading element of a carried tuple: Plane Signal carries OriginRoute then Input, Plane Nexus carries OriginRoute then the Nexus message, Plane Sema carries OriginRoute then the Sema message. This keeps Plane a single data-carrying enum matched directly (1054) with the origin route carried INSIDE the variant rather than as a separate kind tag beside an envelope (1052 forbids the latter), and satisfies 1039 that origin route is a field on every root plane message object — here the field is the variants leading tuple element, minted at ingress and echoed back on the reply Plane. The schema emitter generates Plane per module from the planes the schema declares. Proposed by the system-designer horizon-next finish subagent; pending psyche confirmation of the tuple-leading-element placement versus a named struct field.] Medium Zero)
  ```
- `b7mp` [scratchpad/status-snapshot]
  ```
  (b7mp [engine-trait-architecture spirit-next live-landing sema-split d29dc6c] Clarification [The engine-trait architecture (Spirit 1326-1336 + 1327 broad-triad) is LIVE in spirit-next. SignalEngine triage + reply form at src/schema/lib.rs:1431; NexusEngine execute at :1436; SemaEngine apply + observe SPLIT landed at commit d29dc6c (2026-06-01 15:17:26), closing Spirit 1332 parallel-reads decision in code. Spirit-next ARCHITECTURE.md + INTENT.md updated by operator. Workspace-level discipline for the broad triad pattern lives in skills/component-triad.md §Runtime triad engine traits.] High Zero)
  ```
- `bqcq` [scratchpad/working-instruction]
  ```
  (bqcq [orchestrate-resume-after-spirit-refresh] Clarification [Resume orchestrate parity after Spirit refresh] High Zero)
  ```
- `bwky` [scratchpad/working-instruction]
  ```
  (bwky [schema] Constraint [Implementation starts with the emit_schema and legacy_signal_channel macro split.] High Zero)
  ```
- `cbm6` [scratchpad/working-instruction]
  ```
  (cbm6 [spirit deployment preserves intent data] Constraint [Production Spirit deployment must not lose any intent data; preserve and verify existing records across the cleaner observation-surface deployment.] High Zero)
  ```
- `cpsv` [scratchpad/working-instruction]
  ```
  (cpsv [spirit cleaner production observation surface] Decision [Implement a better production Spirit observation surface: use truthful projection names, avoid single-field vector wrapper output in plural observation replies, and make the human-facing NOTA shape match the concept rather than preserving misleading old names as canonical.] High Zero)
  ```
- `cqwm` [scratchpad/working-instruction]
  ```
  (cqwm [spirit privacy constraints production next] Constraint [Production Spirit and next Spirit design surfaces must carry the privacy access-classification constraint before implementation proceeds.] High Zero)
  ```
- `cta3` [scratchpad/working-instruction]
  ```
  (cta3 [schema prototype implementation audit] Decision [Mine recent intent reports and prototypes for working solutions, then implement a fully working prototype rather than another design only pass.] High Zero)
  ```
- `cupj` [scratchpad/working-instruction]
  ```
  (cupj [schema asschema implementation] Decision [Proceed with the operator's leaned schema gap-closure order from report 254: check in reviewable .asschema text artifacts first, then RustModule data emission, typed MacroLibrary, schema-core support nouns, SchemaDiff/UpgradePlan, crate split after artifacts/imports stabilize, and first self-hosting through Asschema.] High Zero)
  ```
- `cy2p` [scratchpad/status-snapshot]
  ```
  (cy2p [workspace] Decision [Designer pivots to /249 gap closure next sessions; new-component design tasks park] High Zero)
  ```
- `cyir` [scratchpad/working-instruction]
  ```
  (cyir [[spirit deployment]] Decision [Spirit next should implement multi-topic records and be run on the spirit-next deployment lane, with a hard database migration acceptable for the schema change.] High Zero)
  ```
- `d4p3` [scratchpad/status-snapshot]
  ```
  (d4p3 [spirit generated schema] Constraint [spirit-next src schema lib.rs is generated and should not be edited manually. Handwritten edits should avoid changes that require regenerated schema output until the main operator repins schema-next and schema-rust-next.] High Zero)
  ```
- `dash` [scratchpad/working-instruction]
  ```
  (dash [beads] Decision [Second-operator may pick up any currently free ready bead and work it.] High Zero)
  ```
- `dfxz` [scratchpad/working-instruction]
  ```
  (dfxz [spirit identifiers migration legacy] Decision [Spirit should migrate existing legacy numeric record identifiers to random opaque hash identities now, accepting that old numeric references may break while preserving a temporary mapping from former numeric identifiers to new hashes for transition lookup.] VeryHigh Zero)
  ```
- `dkzq` [scratchpad/working-instruction]
  ```
  (dkzq [desktop ui-priority implementation-staging] Decision [Implement the desktop UI priority policy in code so it is ready for safe deployment, but do not deploy or activate it in the current active session. The implementation should protect specific UI components rather than broad live session slices.] High Zero)
  ```
- `dn37` [scratchpad/working-instruction]
  ```
  (dn37 [schema signal nexus sema] Decision [The next proof should implement schema-emitted Signal Nexus and SEMA projections before schema-core extraction when the projection slice can stay small.] High Zero)
  ```
- `dska` [scratchpad/working-instruction]
  ```
  (dska [nix deploy repository-management] Correction [For CriomOS-home update work, push to main first and use github: flake references for Nix calls rather than evaluating local checkout state.] High Zero)
  ```
- `dvxd` [scratchpad/working-instruction]
  ```
  (dvxd [orchestrate schema-derived migration] Correction [The orchestrate component migration target is the current schema-derived stack: schema-rust-next generates source-visible schema modules from authored schema, schema-next is the semantic schema proof/input layer, and hand-written signal-channel or old schema dependency surfaces are transitional rather than correct final dependencies.] High Zero)
  ```
- `dx1r` [scratchpad/working-instruction]
  ```
  (dx1r [pi-operator reporting] Constraint [For the refreshed pi-operator read, psyche wants an updated report that re-reads current intent and reports, includes many visuals and code examples, and explicitly marks what the agent likes and dislikes.] Medium Zero)
  ```
- `dxyi` [scratchpad/working-instruction]
  ```
  (dxyi [workspace] Decision [designer dispatched mockup-2: implement .schema parser + AssembledSchema lowering POC] Minimum Zero)
  ```
- `e0nq` [scratchpad/working-instruction]
  ```
  (e0nq [workspace] Constraint [Use feature worktree when locked] Medium Zero)
  ```
- `e1lg` [scratchpad/status-snapshot]
  ```
  (e1lg [schema schema-language grammar] Clarification [Psyche identifies schema/nexus.concept.schema and likely other files as carrying stale schema grammar, and asks for the current latest grammar so stale files can be judged against it.] Medium Zero)
  ```
- `e2fs` [scratchpad/too-specific-ephemeral]
  ```
  (e2fs [versioning] Clarification [Spirit next development version is either 0.1.1 or 0.2 depending on breakage] Medium Zero)
  ```
- `e6v5` [scratchpad/status-snapshot]
  ```
  (e6v5 [schema-rust-next codegen rust-native lowering engine string-emission migration] Correction [The schema-rust-next Rust generation is moving at HIGH certainty to the newer rust-native approach (its exact shape still being settled with the operator), away from the build-time string-emission codegen — the format!/self.line surface in lib.rs. Characterizing the remaining string-emission runtime surface as the KEPT design, the ad-hoc string codegen the psyche objected to in 4np2, is WRONG: that surface is transitional and mid-migration, not the intended end state. The designer over-claimed a settled-direction migration (designer report 535) as an open debt needing clarification; the direction is decided. Build the engine stack on the rust-native approach properly.] High Zero)
  ```
- `ej6j` [scratchpad/working-instruction]
  ```
  (ej6j [signal] Decision [Adopt /156 Gap 5 lean: execute the ComponentName rename now — rename signal_persona_auth::ComponentName to ComponentPrincipal (closed enum, identity surfaces) and signal_persona::ComponentName to ComponentInstanceName (open String newtype, spawn-envelope surfaces). Bundle with the Axis 2 rename completion on the persona daemon side (primary-wvdl Track B item 8)] High Zero)
  ```
- `ejf4` [scratchpad/too-specific-ephemeral]
  ```
  (ejf4 [architecture] Decision [/287 substance distributes to relevant per-repo ARCHITECTURE.md files; contradictory ARCH parts get rewritten] High Zero)
  ```
- `ez8w` [scratchpad/status-snapshot]
  ```
  (ez8w [privacy access gate] Clarification [Current privacy/access-gate work is preparatory tooling and protocol work; the psyche does not believe sensitive private content has been said yet in this thread.] Medium Zero)
  ```
- `f063` [scratchpad/working-instruction]
  ```
  (f063 [signal] Decision [Focus next design work on the namespacing for the 64-bit small-object signal type (Tier 1) per spirit records 244 + 251 + 271 + 272 + 273; deep design on root-verb + 7 sub-variants structure] High Zero)
  ```
- `f2pq` [scratchpad/status-snapshot]
  ```
  (f2pq [schema-language] Correction [The active v4 schema-language designer surface is reports/designer/326-v4-spirit-complete-schema-vision.md.] High Zero)
  ```
- `f8wl` [scratchpad/working-instruction]
  ```
  (f8wl [workspace] Principle [integrate designer downstream work] High Zero)
  ```
- `fe02` [scratchpad/status-snapshot]
  ```
  (fe02 [persona] Decision [Second-operator pivots from orchestrate executor migration to reviewing operator's smart-handover work on Persona for possible parallel slices; orchestrate executor migration considered done from second-operator side] High Zero)
  ```
- `fkgs` [scratchpad/working-instruction]
  ```
  (fkgs [naming] Decision [File thorough operator beads for the full naming-rule application pass across active workspace repos - Identifier sweep, crate rename, abbreviation sweep] High Zero)
  ```
- `fkpe` [scratchpad/too-specific-ephemeral]
  ```
  (fkpe [intent-log] Clarification [Gemma 4 is released as of 2026-05-22. The agent's training-knowledge hedge in /4 research (saying Gemma 4's audio-input capability is uncertain) is overruled — Gemma 4 exists; whether it has the audio-input multimodal capability the recording system needs is the substantive question the research's system-specialist verification step answers. Refresh /4's framing to remove the existence-uncertainty hedge while preserving the audio-capability verification.] High Zero)
  ```
- `fnbv` [scratchpad/working-instruction]
  ```
  (fnbv [schema implementation designer] Constraint [Before implementing the schema-derived stack, operator should review the latest designer critique material and let those critiques shape the implementation.] High Zero)
  ```
- `fnic` [scratchpad/too-specific-ephemeral]
  ```
  (fnic [workspace] Decision [Decompose primary-a5hu (persona engine epic) into 5 sub-beads with concrete per-axis DoD] High Zero)
  ```
- `fteq` [scratchpad/working-instruction]
  ```
  (fteq [cloud component core-crate context refresh] Clarification [Refresh the cloud component intent and context against the latest core-crate development, so cloud-component work stays aligned with current shared runtime and schema/crate direction.] High Zero)
  ```
- `g57s` [scratchpad/working-instruction]
  ```
  (g57s [criomos-home pi local-model-selection] Decision [After adding new large-AI model variants to the NixOS model inventory, CriomOS-home should be redeployed so Pi can select the new local model identifiers from its generated models.json/settings surface.] High Zero)
  ```
- `ganh` [scratchpad/status-snapshot]
  ```
  (ganh [criomos-home wifi recovery] Clarification [The local operator machine did not automatically return to the goldragon.criome Wi-Fi after Prometheus came back; NetworkManager priority should prefer goldragon.criome when available while retaining the apartment Wi-Fi as fallback.] Medium Zero)
  ```
- `gb4n` [scratchpad/too-specific-ephemeral]
  ```
  (gb4n [orchestration] Correction [Operator workers should use an assistant lane lock for this R10 task instead of the parent operator lock.] High Zero)
  ```
- `gjou` [scratchpad/working-instruction]
  ```
  (gjou [workspace] Decision [Context-maintenance sweep: dispatch subagent per skills/context-maintenance.md to filter older reports across role subdirs, migrate substance to permanent homes, delete superseded entries; keep working report lane fresh for moving deeper into the signal/sema macro work] High Zero)
  ```
- `guxk` [scratchpad/working-instruction]
  ```
  (guxk [workspace] Decision [Workspace-wide CONVERSION OF HERESY sweep: dispatch subagents to scan architecture documents, intent files, code comments, and code itself for anything that contradicts the schema-crystallized architecture principles. Mark every contradictory item for rewriting or significant change. This is a mass coordinated sweep across all repos and all workspace files - not surgical cherry-picking. Psyche language is 'vast conversion of heresy' - preserve the framing in the sweep's own working artifacts. Scope is broader than the prior schema-rust-rename sweep: any framing that contradicts (a) schema as crystallized architecture and self-describing format, (b) extensible header pattern, (c) deep tree with type index, (d) two languages internal effect plus external wire, (e) interact-trait plus interaction-actor mediation, (f) effect-table match-driven dispatch, (g) actor fan-out execution. Anything endorsing alternative dispatch patterns, alternative mediation patterns, alternative emission paths, or alternative wire-encoding patterns is heresy and gets flagged for rewrite.] High Zero)
  ```
- `h3cy` [scratchpad/impl-detail-belongs-in-report]
  ```
  (h3cy [schema-rust-next plane-type plane-projection noun-owned runtime-emitter token-lowering] Decision [The runtime-half token migration's PlaneType blocker is resolved by promoting the implicit plane axis (three bools in RuntimePlaneSet + the per-plane Runtime target variants + inline conditionals) into TWO first-class nouns. Plane is the NODE — Signal/Nexus/Sema — owning plane-intrinsic naming as pure self constants: module_name, wrapper_path, envelope_name, trace_enum_name, trace_prefix, engine_trait_name, alias_names, canonical_source_type_names. PlaneProjection is the EDGE — the directed cross-plane transforms (NexusWork to Action, NexusAction to SemaWrite/SemaRead/SignalOutput, SemaOutput to NexusWork) — owning edge identity + the two wrapper paths, while per-variant mapping stays on the projection noun. Each runtime construct noun holds a Plane or a PlaneProjection and composes it with its schema data in ToTokens; the RustWriter god-struct dissolves. Three decision classes stay OFF the plane nouns because putting them there only relocates the smear: target-set questions (emits-all, is-plane-emitted) stay on RuntimePlaneSet/RustEmissionTarget; the SignalRuntime-vs-ComponentRuntime concreteness special-case stays on the target; plane-plus-construct-presence questions (role-traits by position, actor-variant labels by roots, route-type membership) take the schema roots as a parameter. RuntimePlaneSet is re-expressed as a set of Plane. The wire-contract no-role-impls orchestrator gate is preserved at the call site. Reference family for the prototype is emit_plane_namespaces becoming PlaneNamespaceTokens. Full design in designer report 537; to be validated by the reference prototype and operator integration.] High Zero)
  ```
- `h906` [scratchpad/too-specific-ephemeral]
  ```
  (h906 [local-ai prometheus downloads] Constraint [Large local-AI model pre-downloads for this toolkit exploration should run on the Prometheus node, not on the local node, and should be left alive there as durable background work while the psyche is away.] High Zero)
  ```
- `h95r` [scratchpad/working-instruction]
  ```
  (h95r [workspace] Constraint [Subagents are authorized for the schema-design task.] High Zero)
  ```
- `h9iw` [scratchpad/working-instruction]
  ```
  (h9iw [component-shape] Decision [Sema-upgrade implementation sequence — migration logic comes first, then daemon/socket/thin CLI. A temporary CLI is used to test the migration logic directly against the sema database before the formal triad daemon wraps it. Applies to sema-upgrade's first concrete migration (persona-spirit 0.1.0 → 0.1.1, the Magnitude consumption path) and as the general development pattern: validate transformation correctness through a temp CLI on the real database before promoting to the formal component triad. Also: the migration is actually run (not just coded). This implicitly sequences operator/153 Q1 toward Shape A (universal sema-upgrade triad first) AND adds a temp-CLI testing step ahead of daemon assembly.] High Zero)
  ```
- `hex1` [scratchpad/working-instruction]
  ```
  (hex1 [testing] Clarification [Operator may implement e2e in CriomOS-test-cluster main] High Zero)
  ```
- `hfn8` [scratchpad/agent-conclusion]
  ```
  (hfn8 [signal-sema naming sema-engine contract] Clarification [The signal-sema crate needs role and naming reconsideration: some of its functionality may still be useful, but the name is misleading if the crate is not actually a Signal contract for talking to a SEMA engine.] Medium Zero)
  ```
- `hgvm` [scratchpad/working-instruction]
  ```
  (hgvm [implementation directive spirit-next schema-next daemon upgradable-runtime-schema rust-code-generation upgrade-objects compatibility-code best-of-latest-designs] Decision [Implement the best of latest designs from current main HEADs across spirit-next and schema-next. spirit-next applies NexusWork/NexusAction vocabulary (Spirit 1438 + operator 287) + inline enum payload pattern (Spirit 1467/1468) + contract-repo split (Spirit 1422) + Stash effect pilot (operator 287 lean) + typed trace integration (operator's TraceObject hierarchy already landed). schema-next becomes a daemon with upgradable runtime schema — schema definitions stored in SEMA upgradable via Signal operations per designer 447 upgrade-as-SEMA. schema-rust-next code generation produces upgraded Rust types and emits upgrade/compatibility code from upgrade objects. Worktree-based designer implementation pilots (per Spirit 1352); operator integrates main per AGENTS.md lane discipline.] High Zero)
  ```
- `hiue` [scratchpad/working-instruction]
  ```
  (hiue [nota] Decision [Legacy intent slash star.nota files migrate via a separate programmatic extractor that preserves the original psyche timestamps; this work is separate from the legacy-quote-removal heresy sweep] High Zero)
  ```
- `hjxx` [scratchpad/working-instruction]
  ```
  (hjxx [schema-spirit-upgrade] Decision [Second-operator should work toward a fully working schema-driven Spirit upgrade] High Zero)
  ```
- `hkl4` [scratchpad/status-snapshot]
  ```
  (hkl4 [criomos-home pi update] Clarification [The running local Pi profile still reports update 0.77.0 available, confirming the Pi update has not yet been activated in the local Home profile.] Medium Zero)
  ```
- `hlxu` [scratchpad/working-instruction]
  ```
  (hlxu [verification explanation-discipline] Clarification [psyche directive: bottom-up tour of the schema/NOTA stack one layer per report; purpose is to verify that what was built matches psyche intent; checkpoint between layers so psyche can engage or redirect before next layer] High Zero)
  ```
- `i7cj` [scratchpad/status-snapshot]
  ```
  (i7cj [[spirit deployment]] Decision [Spirit production can cut over from v0.2.0 to the multi-topic next version now because agents are not using Spirit during the migration window.] High Zero)
  ```
- `iijx` [scratchpad/working-instruction]
  ```
  (iijx [spirit] Decision [Spirit gets upgraded to the latest version with bracket-string NOTA support and no echo-back on capture; the cutover is a hard switchover during a quiet window with a database copy first to verify the new daemon takes over correctly] High Zero)
  ```
- `ishx` [scratchpad/working-instruction]
  ```
  (ishx [testing] Constraint [Upgrade mechanism implementation should emphasize constrained tests that actually exercise the designed behavior.] High Zero)
  ```
- `itfh` [scratchpad/too-specific-ephemeral]
  ```
  (itfh [cloud orchestrate] Clarification [For this token and Gemma 4 deployment the psyche directed cloud-designer to carry the work end to end, including the operator-lane steps of landing changes and deploying to the prometheus host. Explicit psyche direction overrides the designer-stays-on-feature-branches default for this task.] Medium Zero)
  ```
- `iw9t` [scratchpad/status-snapshot]
  ```
  (iw9t [domain-criome] Constraint [domain-criome runtime feature work uses a separate worktree bookmark] High Zero)
  ```
- `j2zo` [scratchpad/working-instruction]
  ```
  (j2zo [naming] Decision [Bundle the signal-persona-auth crate rename with the Identifier rename in that same surface.] Medium Zero)
  ```
- `j42m` [scratchpad/working-instruction]
  ```
  (j42m [signal] Decision [Migrate the new horizon and lojix stacks to the latest low-level library versions in the nota / signal / sema family. The lean-stack consumers (horizon-rs, lojix, signal-lojix, related Nix consumers) come into line with the post-2026-05-21 nota-codec, signal-frame, signal-executor, signal-sema, signal-persona-spirit shapes that have been landing rapidly. Scope includes any consumer impact from the role-merge cluster-data shape (reports/system-designer/29) plus the new map syntax, three-case NotaRecord, ToSemaOutcome trait, BatchErrorClassification, and Spirit substrate work.] High Zero)
  ```
- `j9ba` [scratchpad/working-instruction]
  ```
  (j9ba [schema-next nota-next logics lojix horizon actors] Decision [New lojix-horizon logics work is a schema-deep rewrite on nota-next plus schema-next: deep actor system with actors whose interfaces AND data shapes are schema-defined, method-only Rust per existing hard override, framed by psyche as almost a full rewrite. Psyche explicitly authorizes modifying schema-next itself if reaching the required depth demands it. Sequence: reload state, self-train, write vision meta-report, dispatch subagent on worktree branch with concept running including tests passing and at least one sandbox OS booted on the new logics.] High Zero)
  ```
- `jc3g` [scratchpad/working-instruction]
  ```
  (jc3g [spirit-next tests report visuals pattern-a lifecycle hooks] Decision [Improve the real spirit-next Pattern A tests first, then produce an operator report that explains the implementation with focused visuals and code excerpts taken from those real tests.] High Zero)
  ```
- `jq0q` [scratchpad/working-instruction]
  ```
  (jq0q [domain-criome] Constraint [Exercise production Store and Signal paths for domain-criome authority delegation tests] High Zero)
  ```
- `juus` [scratchpad/working-instruction]
  ```
  (juus [upgrade-component] Decision [Merge sema-upgrade + version-handover into one upgrade component triad: upgrade (daemon) + signal-upgrade (working contract) + owner-signal-upgrade (owner contract). version-projection stays as a separate library since every contract crate depends on the VersionProjection trait. PrototypeHandover state machine deletes (production code in persona-spirit superseded it). Two MigrationIndex types collapse to one. AttemptHandover/AttemptUpgrade verb confusion resolves to one AttemptUpgrade verb. Persona narrows to engine supervision; upgrade daemon owns orchestration.] High Zero)
  ```
- `k1h9` [scratchpad/too-specific-ephemeral]
  ```
  (k1h9 [deployment cloud] Decision [The gemma 4 deploy ships with full multimodal now: fetch the mmproj file alongside the weights and wire the mmproj flag, rather than deploying text-only and adding vision later.] High Zero)
  ```
- `k1kr` [scratchpad/working-instruction]
  ```
  (k1kr [testing] Constraint [Upgrade tests should include transition-window messages that differ across versions.] High Zero)
  ```
- `k2eg` [scratchpad/status-snapshot]
  ```
  (k2eg [schema-language] Correction [Schema file has no outer struct wrapper] High Zero)
  ```
- `k4gj` [scratchpad/working-instruction]
  ```
  (k4gj [pi-continue pi nix profile package-management] Correction [The stateful npm install of pi-continue was wrong and must be removed in favor of Nix-profile installation.] Maximum Zero)
  ```
- `k8v3` [scratchpad/working-instruction]
  ```
  (k8v3 [schema-macros designer-assistant-375] Decision [psyche directed a deeper pass on the schema-derived spirit stack: finish the schema macro engine in schema-next so it is a real fixed-point expansion engine with import resolution and macro registry; then USE the finished engine to produce a working spirit CLI plus daemon that materialises Route plus SignalCodec from imported signal-frame.schema macros rather than hand-rolling them.] High Zero)
  ```
- `ka59` [scratchpad/working-instruction]
  ```
  (ka59 [nix component-boilerplate library] Clarification [The repeated Nix package pattern for component main, trace/debug, and last-version builds should be considered for extraction into a shared Nix library once the spirit-next shape proves out.] High Zero)
  ```
- `kbwb` [scratchpad/working-instruction]
  ```
  (kbwb [spirit-next signal nexus lifecycle hooks implementation] Decision [Implement Pattern A in the pilot project with hookable lifecycle events, including an on_sent-style event at the Signal to Nexus handoff, so the object-flow discipline is visible in code and tests.] High Zero)
  ```
- `kmma` [scratchpad/working-instruction]
  ```
  (kmma [audio dji mic keep alive nix] Decision [Investigate and solve the DJI mic keep-alive problem by moving the ad hoc service into Nix so that, when a DJI mic is plugged in, the system provides the virtual audio keep-alive path needed to keep a stream active and prevent the mic from taking several seconds to wake for speech-to-text.] High Zero)
  ```
- `lhxv` [scratchpad/working-instruction]
  ```
  (lhxv [upgrade-mechanism-design-report] Decision [Psyche directs full design report on the upgrade mechanism with visuals showing how the psyche intends it: new daemon talks to old daemon during cutover. Report becomes the basis for the upcoming discussion and what gets implemented. Reframes Q3 multi-endpoint question into the broader upgrade-mechanism design context. Second-designer to research what has been tested re new-spirit-talks-to-old-spirit and deliver the design.] High Zero)
  ```
- `ll5r` [scratchpad/too-specific-ephemeral]
  ```
  (ll5r [component-shape] Decision [Engine-manager Axis 2 rename lands now as step 0 of /257 contract migration sweep (bead primary-u8vo)] High Zero)
  ```
- `luqx` [scratchpad/working-instruction]
  ```
  (luqx [spirit cli range query documentation] Decision [The Spirit CLI skill documentation should show how to query intent records by numeric identifier range, including SummaryOnly and WithProvenance examples.] High Zero)
  ```
- `m028` [scratchpad/working-instruction]
  ```
  (m028 [orchestrate-schema-upgrade] Constraint [Second-operator should port orchestrate in a worktree while operator keeps main] High Zero)
  ```
- `m0p4` [scratchpad/working-instruction]
  ```
  (m0p4 [component-shape] Decision [Decompose primary-uq04 (cross-component CLI migration sweep) into per-component sub-beads for parallel operator pickup] High Zero)
  ```
- `mvfm` [scratchpad/status-snapshot]
  ```
  (mvfm [workspace] Decision [the test substrate is now real enough that subsequent MVP test passes use the real nota-and-macro-parsed-schema pipeline end-to-end; no more vendored wire-types bypass, no more hand-built AssembledFragments, no more ad-hoc string-strip parsers; the test exercises the actual production schema engine + brilliant-macro emission + signal-version-handover + persona-daemon supervisor] High Zero)
  ```
- `n18u` [scratchpad/status-snapshot]
  ```
  (n18u [role-lanes reporting cloud-operator] Decision [The current agent session is now the cloud-operator role; its reports should live under reports/cloud-operator rather than reports/pi-operator.] High Zero)
  ```
- `ni3s` [scratchpad/too-specific-ephemeral]
  ```
  (ni3s [operator] Constraint [Operator subagent dispatch for /318 must restate jj inline-message discipline.] High Zero)
  ```
- `o0pp` [scratchpad/status-snapshot]
  ```
  (o0pp [schema schema-rust-next cross-repo-integration nota-next-pin] Clarification [Building schema-rust-next collections against schema-next collections surfaced that the two repos pin DIFFERENT nota-next commits in their Cargo.lock files - schema-next at 5e063042 which has StructureHeader, schema-rust-next at 1c11876b which lacks it. When schema-rust-next is patched to depend on a local schema-next, cargo unifies on schema-rust-next's older nota-next pin and schema-next fails to build. The fix for a coupled designer feature branch is to align schema-rust-next's nota-next pin to the commit schema-next builds against, AND patch schema-rust-next's schema-next dependency to the designer feature branch (not a local path, which Nix source-filtering cannot reach) so nix flake check vendors the matching schema-next. Operator reconciles both pins to main on integration. This is integration coupling, not collections work.] Medium Zero)
  ```
- `o4sf` [scratchpad/working-instruction]
  ```
  (o4sf [component-shape] Decision [Designer parallel implementation slice: make a running spirit CLI + daemon concept work on latest main of operator s new repos (nota-next + schema-next + schema-rust-next + spirit + signal-spirit + core-signal-spirit). Concept does not need full v0.3 capability parity - it needs end-to-end communication: CLI takes NOTA arg; CLI encodes to rkyv binary; daemon receives via socket; daemon decodes; daemon processes via actor; daemon emits reply via rkyv; CLI decodes + prints NOTA. Per /366 §10 next-slice path. Empirical proof of the chain. Per psyche 2026-05-26.] High Zero)
  ```
- `o772` [scratchpad/too-specific-ephemeral]
  ```
  (o772 [orchestrate] Decision [During MVP development phase, second-designer has cross-lane authority to direct operator agents to edit code (or reports/skills) that should be fixed right now for the MVP migration. Fast-track scope for MVP-blocking fixes — bracket-string sweeps, mechanical text fixes, small obvious code changes.] High Zero)
  ```
- `oacd` [scratchpad/too-specific-ephemeral]
  ```
  (oacd [mermaid reports designer-report] Constraint [Psyche authorizes a one-task lock override to edit reports/designer/407-schema-stack-detailed-presentation-nota-to-daemon.md and try the Mermaid box-size/readability fix there.] Low Zero)
  ```
- `ocu7` [scratchpad/status-snapshot]
  ```
  (ocu7 [triad-engine runtime migration component-port persona] Decision [triad_main (the schema-emitted daemon module/runner, designer report 542) is now implemented. The core components — mind, message, orchestrate, router, terminal-control, spirit, persona — migrate onto the new schema-derived triad runtime (triad_main + the Signal/Nexus/SEMA engine traits). spirit is the existing pilot; the others port to the same shape. triad_main is audited and obvious flaws fixed as the migration proceeds. Per psyche 2026-06-06.] High Zero)
  ```
- `ofo3` [scratchpad/working-instruction]
  ```
  (ofo3 [schema] Constraint [Build an MVP that makes the schema macro system real across NOTA parsing and schema assembly.] High Zero)
  ```
- `ojuh` [scratchpad/working-instruction]
  ```
  (ojuh [persona] Decision [The priority destinations are (a) persona-mind running as the beads replacement and (b) persona-orchestrate running as the tools/orchestrate shell-helper replacement. The intent-to-mind (Spirit→Mind owner contract) is NOT a current blocker — psyche is not worried about it; defer. Get both daemons usable enough to substitute for their current ad-hoc/shell substitutes.] High Zero)
  ```
- `ouub` [scratchpad/working-instruction]
  ```
  (ouub [spirit] Decision [old spirit should redirect after safe dual write] Medium Zero)
  ```
- `ovft` [scratchpad/status-snapshot]
  ```
  (ovft [workspace] Constraint [Use the unversioned spirit binary for intent capture; it routes to the current production target (now v0.3.0). The versioned binaries (spirit-v0.2.0, spirit-v0.1.0, etc.) write to retired sandbox databases that will be lost when the side-by-side wrappers retire.] High Zero)
  ```
- `pce9` [scratchpad/status-snapshot]
  ```
  (pce9 [agent-contract] Constraint [Do not edit a persona-agent daemon for this rename because that daemon does not exist.] High Zero)
  ```
- `pe2d` [scratchpad/status-snapshot]
  ```
  (pe2d [design-nota-from-schema recursion-floor verdict] Decision [Designer parallel-implementation track complete. design-nota-from-schema repo created at github.com/LiGoldragon/design-nota-from-schema with kernel (726 LOC hand-authored recursion floor), emit (519 LOC reader+emitter), nota-emitted (185 LOC AUTOGENERATED from schemas/nota.schema), integration (kernel-to-emitted lifter). 35 tests pass, nix flake check green. Verdict on the narrower recursion-floor cut from record 746: PARTIALLY FEASIBLE. Type declarations emit cleanly (31 schema-derived types). Byte-recognition policy (lexer/walker) cannot emit without a separate sub-language for declaring lexer states + disambiguation rules. Recommendation: hybrid position keep operators wider cut for byte recognition narrow the cut for type declarations. Full report at reports/designer/363.] High Zero)
  ```
- `phrx` [scratchpad/working-instruction]
  ```
  (phrx [concept worktree subagent-dispatch designer-protocol] Decision [Concept implementation methodology for the schema-deep new-logics rewrite: vision lands in a meta-report under reports/system-designer; then dispatch a background subagent (run-in-background per existing hard override) on a worktree branch following the designer worktree pattern under tilde-wt; concept must demonstrate new logics running with all tests passing and at least one sandbox operating system booted on the new logics with new nota plus new schema powering everything. Reinforces designer-protocol parallel-subagent default and operator-rebase boundary.] High Zero)
  ```
- `plr7` [scratchpad/working-instruction]
  ```
  (plr7 [persona] Decision [Adopt /156 Gap 3 lean: build selector-flip-aware routing into primary-ezzp (Persona FD-handoff infrastructure) from day one rather than file as follow-on bead. Per-handoff read of active-version snapshot is microsecond-cost, simpler than caching + invalidation on flip. The selector flip then becomes the same path as steady state — no special-case routing code] High Zero)
  ```
- `psd3` [scratchpad/too-specific-ephemeral]
  ```
  (psd3 [schema] Clarification [Forwarded operator prompt is audit context only.] Medium Zero)
  ```
- `px6d` [scratchpad/status-snapshot]
  ```
  (px6d [deploy] Clarification [Spirit migration is not final until default switch] High Zero)
  ```
- `q1np` [scratchpad/too-specific-ephemeral]
  ```
  (q1np [criomos prometheus llm] Decision [Prometheus should include the new Gemma 4 model set alongside its existing AI models.] High Zero)
  ```
- `qtj6` [scratchpad/status-snapshot]
  ```
  (qtj6 [upgrade] Decision [U1 upgrade triad work is scaffold only] Medium Zero)
  ```
- `r33n` [scratchpad/status-snapshot]
  ```
  (r33n [running-spirit-concept designer-track] Decision [Designer-assistant ships running spirit concept on new architecture. Both feature branches green (signal-spirit at 710b628e and spirit at 643978cf). End-to-end chain works on real Unix socket: NOTA argv via shell to CLI to rkyv on socket to daemon to Recorder actor to rkyv reply to CLI to NOTA stdout. Five of /366 truth claims now empirically verified (CLI is NOTA plus binary rkyv between components plus engine matches Input to Action plus SEMA reaction object plus Signal IN-OUT rkyv on wire). Report at /368. Schema-rust-next emission already covers types plus rkyv derives plus NOTA codec impls plus short headers — only the wire framing plus actor plus daemon substrate hand-rolled.] High Zero)
  ```
- `r9qy` [scratchpad/working-instruction]
  ```
  (r9qy [component-triad naming meta-signal owner-signal fleet-rename policy-contract] Decision [The owner-signal to meta-signal rename is active work not tentative. The workspace policy-contract naming standard is meta-signal then component uniformly; the owner-signal prefix is stale. Run a deep rename pass that audits and updates the workspace guidance and the affected contract repositories. As a fleet operation all existing owner-signal contract repos rename to meta-signal — the 13 affected repos: owner-signal-agent owner-signal-cloud owner-signal-domain-criome owner-signal-mind owner-signal-orchestrate owner-signal-persona owner-signal-persona-spirit owner-signal-repository-ledger owner-signal-router owner-signal-sema-upgrade owner-signal-terminal owner-signal-upgrade owner-signal-version-handover. Future components are born meta-signal from inception. For the lojix rewrite specifically: the policy contract must be born meta-signal-lojix never owner-signal-lojix.] High Zero)
  ```
- `rcn3` [scratchpad/agent-conclusion]
  ```
  (rcn3 [triad-runtime daemon multi-listener dual-listener correction] Correction [Correction to the two-socket daemon decision latq: triad-runtime ALREADY ships the generic multi-socket primitive — MultiListenerDaemon plus the MultiListenerRuntime trait in daemon.rs and a role module for listener authority tags, all exported from lib.rs. The earlier refresh report that triad-runtime had only a single-listener daemon was wrong; the agents missed MultiListenerDaemon. So NO new DualListenerDaemon needs building in triad-runtime. The lojix daemon honors the two-socket runner-wired intent by IMPLEMENTING MultiListenerRuntime: bind the ordinary and owner sockets as two ListenerSockets, tag each arrival by role into the SignalInput union, and route through the generated NexusEngine execute recursive runner that already exists in generated code. lojix is the first consumer of MultiListenerDaemon; the primitive is complete and was unused until now. This removes the load-bearing blocker and the platform-surgery framing.] High Zero)
  ```
- `rp8q` [scratchpad/working-instruction]
  ```
  (rp8q [component-shape] Decision [commit_sequence is built in sema-engine before the first Spirit cutover; gives the handover protocol true zero-downtime semantics] High Zero)
  ```
- `sl18` [scratchpad/working-instruction]
  ```
  (sl18 [signal] Decision [Deploy operation moves from signal-forge to signal-lojix] High Zero)
  ```
- `sroc` [scratchpad/working-instruction]
  ```
  (sroc [workspace] Decision [Encode the 3-7 chat-items + design-question-balance policy as the default for ALL agents in AGENTS.md] High Zero)
  ```
- `strt` [scratchpad/too-specific-ephemeral]
  ```
  (strt [spirit] Decision [Real handover testing requires both v0.1.0 and v0.1.1 daemons coexisting] High Zero)
  ```
- `svyv` [scratchpad/status-snapshot]
  ```
  (svyv [workspace] Decision [designer pursues remedy paths in parallel via subagent feature branches; operator pursues complementary paths] High Zero)
  ```
- `sybk` [scratchpad/agent-conclusion]
  ```
  (sybk [workspace design-nota-from-schema recursion-floor] Decision [I am the designer-assistant subagent for the parallel-implementation track per record 823. Creating design-nota-from-schema repo to empirically test the narrower recursion-floor cut from record 746 actually emit nota-codec from nota.schema. Either feasibility outcome is load-bearing.] High Zero)
  ```
- `t0xm` [scratchpad/agent-conclusion]
  ```
  (t0xm [spirit-next origin route emitter divergence] Clarification [The spirit-next 408 prototype implements record 1029 origin route as a hand-written RUNTIME type minted on entry (statistically-unique salt+counter), distinct from message identity, never a schema field, threaded signal-nexus-sema and tagged on a runtime RoutedOutput wrapper. The operator-track schema-rust-next emitter (HEAD 15a6483) instead EMITS OriginRoute(message_id) as a schema-emitted field on MessageSent/NexusMail/MessageProcessed where the route equals the message identifier. Both serve reply-to-query correlation but differ: runtime-minted-separate vs emitter-id-derived. The prototype pins the emitter at the no-OriginRoute revision (68559f86) to stay self-consistent. Reconciling the two designs is a psyche/operator decision, carried not resolved.] High Zero)
  ```
- `ttm7` [scratchpad/too-specific-ephemeral]
  ```
  (ttm7 [signal] Constraint [Cloud signal foundation report lane is read-only] High Zero)
  ```
- `u8p6` [scratchpad/working-instruction]
  ```
  (u8p6 [orchestrate] Correction [Operator worker should use an assistant lane lock for this repo scaffold] Medium Zero)
  ```
- `u9au` [scratchpad/working-instruction]
  ```
  (u9au [reports] Decision [track which reports were read] High Zero)
  ```
- `ultb` [scratchpad/working-instruction]
  ```
  (ultb [component-shape] Decision [Psyche approved /301 elegant signal_cli! design; file operator beads to land foundation (~200 LOC one PR) + persona-spirit migration first proof + cross-component sweep after] High Zero)
  ```
- `v6c5` [scratchpad/working-instruction]
  ```
  (v6c5 [spirit] Decision [unversioned spirit should dual-write during migration] High Zero)
  ```
- `vl3c` [scratchpad/status-snapshot]
  ```
  (vl3c [spirit intent-maintenance] Correction [Production Spirit already supports explicit record removal; record 1093 is satisfied as an implementation target, and remaining removal work is only deeper policy or audit surface if desired.] High Zero)
  ```
- `w6py` [scratchpad/working-instruction]
  ```
  (w6py [upgrade-mechanism-soup-to-nuts] Decision [Psyche wants the full upgrade mechanism documented end-to-end: every part involved, every method call, every code reference, where the header is and what it does, how upgrade paths are derived from the schema, how the implementation deviates from the ideal. Goal: prove the agents understand what the psyche wants. Followed by sub-agent that actually tests the mechanism end-to-end and brings back real findings.] High Zero)
  ```
- `whjq` [scratchpad/status-snapshot]
  ```
  (whjq [workspace] Correction [The dispatching designer agent is reassigned to the second-designer lane (a second main-designer authority alongside the prime designer), NOT the second-designer-assistant lane as recorded in workspace intent ID 58. The second-designer lane operates at designer authority — structural decisions, design reports, skill edits — not at assistant scope. Matches the /264 composite-designer flow where Claude (this agent) runs in parallel with Codex; both arms operate at designer level. Existing skills/role-lanes.md anticipates designer / designer-assistant / second-designer-assistant only; the second-designer lane is new and requires lane registration (reports/second-designer/ subdirectory, orchestrate/roles.list entry, AGENTS.md role-table entry).] High Zero)
  ```
- `wmdp` [scratchpad/status-snapshot]
  ```
  (wmdp [criomos llm gemma] Correction [Gemma 4 models are present in the Prometheus model inventory but not currently usable because the deployed llama.cpp build reports unknown model architecture gemma4 when loading gemma-4-26b-a4b.] High Zero)
  ```
- `wpvz` [scratchpad/working-instruction]
  ```
  (wpvz [component-migration] Decision [Port stale components to current executor/signal/nota foundations.] High Zero)
  ```
- `wrbj` [scratchpad/too-specific-ephemeral]
  ```
  (wrbj [role-lanes reporting cloud-operator] Constraint [The cloud-operator has explicit psyche override to rename its report directory without taking a lock for this handoff.] High Zero)
  ```
- `x8iv` [scratchpad/status-snapshot]
  ```
  (x8iv [horizon-rs component-shape] Clarification [horizon-rs is currently a hack and does not have the regular component-triad shape; it predates the schema-derived component convention the rewrite is moving toward.] Low Zero)
  ```
- `xbwf` [scratchpad/too-specific-ephemeral]
  ```
  (xbwf [browser cloud] Clarification [The 26-billion mixture-of-experts model is the browser actor for initial browser testing because its four-billion active params keep it fast. The 31-billion dense model is installed only because it fits and the psyche may want to use it later, not as the primary browser actor.] Medium Zero)
  ```
- `xdw4` [scratchpad/working-instruction]
  ```
  (xdw4 [spirit] Correction [Intent capture should use Spirit during this session] Medium Zero)
  ```
- `xqyp` [scratchpad/working-instruction]
  ```
  (xqyp [persona] Decision [Create the persona-pi repository now.] High Zero)
  ```
- `xwxu` [scratchpad/agent-conclusion]
  ```
  (xwxu [schema schema-language collections] Clarification [|Psyche questions the explicit collection field syntax ClusterProposal [(nodes (KeyValueMap NodeName NodeProposal))]: nodes appears to be a field name rather than a type, and the nested field-pair plus collection-constructor wrapping looks suspicious and needs clarification against schema field/type intent.|] Medium Zero)
  ```
- `y35z` [scratchpad/status-snapshot]
  ```
  (y35z [deploy] Decision [Spirit v0.1.1 cutover uses Path A - stop/freeze/migrate/start NOW; documented as last pre-commit_sequence migration] High Zero)
  ```
- `ybq2` [scratchpad/too-specific-ephemeral]
  ```
  (ybq2 [workspace] Constraint [Avoid unrelated repository edits during U6] High Zero)
  ```
- `yvtm` [scratchpad/working-instruction]
  ```
  (yvtm [system-designer collaboration] Decision [Use the system-designer reports as the coordination context for production-to-next-stack CriomOS reconciliation.] High Zero)
  ```
- `zcga` [scratchpad/working-instruction]
  ```
  (zcga [deploy] Decision [Spirit v0.1.0 retrofit confirmed: modify persona-spirit source to add the handover protocol code, re-tag v0.1.0 at the new commit (same database schema), redeploy v0.1.0 in production with the protocol BEFORE the v0.1.1 cutover] High Zero)
  ```
- `zla9` [scratchpad/status-snapshot]
  ```
  (zla9 [workspace] Decision [The dispatching designer-assistant agent is reassigned to the second-designer-assistant lane (reports/second-designer-assistant/) and operates as a parallel designer alongside the prime designer.] High Zero)
  ```

## Borderline (174)

- `08az` [borderline/working-instruction]
  ```
  ([08az] [criomos-home webcam linux cameractrls] Decision [Install cameractrls in the medium Home profile so Linux webcam focus controls are part of the normal Bird/Zeus user environment.] High Zero)
  ```
- `0amp` [borderline/status-snapshot]
  ```
  ([0amp] [schema signal] Decision [Home for signal-frame.schema is the existing LiGoldragon/signal-frame repository. Per psyche 2026-05-26 confirming option b from /372 recommendation. The existing signal-frame repo s old macro infrastructure (signal_channel! etc) retires; the repo s new contents are the schema-derived signal-frame.schema declaring Frame + Route + SignalCodec + FrameError primitives + scalar/Vec plumbing. Decision locks; operator picks up the migration slice.] High Zero)
  ```
- `0bw0` [borderline/status-snapshot]
  ```
  ([0bw0] [schema-rust-next rust-native-lowering codegen] Correction [The remaining schema-rust-next runtime emitter string-based codegen is not an accepted design; the stack is moving with High certainty toward Rust-native typed/token lowering, and the runtime emitter string/format path is debt to replace.] High Zero)
  ```
- `0huc` [borderline/working-instruction]
  ```
  ([0huc] [spirit production parity persona-spirit migration] Decision [The new schema-derived spirit must reach FEATURE PARITY with production persona-spirit — bring ALL production Spirit features into the new one: 4-field Record shorthand, full Observe/Topics vocabulary with certainty and recency selection, the privacy access-control tier, recorded-time provenance, base36 random friendly identity with long-id compatibility, the CollectRemovalCandidates exact-Zero safety guard, and owner/upgrade lifecycle channels. Production parity wins over the pilot simplifications.] High Zero)
  ```
- `0kir` [borderline/too-specific-ephemeral]
  ```
  ([0kir] [persona-pi] Decision [Pi operator workflow should support YOLO mode] High Zero)
  ```
- `0ktv` [borderline/working-instruction]
  ```
  ([0ktv] [cloud cloudflare implementation] Decision [Create a working cloud component tool invoked through the cloud CLI for Cloudflare DNS management.] High Zero)
  ```
- `0uap` [borderline/too-specific-ephemeral]
  ```
  ([0uap] [testing] Correction [Full Spirit upgrade tests must consider CriomOS-test-cluster] High Zero)
  ```
- `0xcq` [borderline/working-instruction]
  ```
  ([0xcq] [intent-manifestation repo-intent architecture per-repo discipline] Decision [intent must be manifested into PER-REPO files - INTENT.md and ARCHITECTURE.md - of the repos worked with at all time, not just in the workspace-level INTENT.md and Spirit record log; per-repo manifestation makes intent visible to agents working IN that repo without forcing them to read the workspace-wide log first; the discipline propagates whenever new captured intent affects how to design implement or test in a specific repo - agents reflect into those files alongside or after capturing through Spirit; the basic agent skill governing this lives in skills/repo-intent.md and is also surfaced in the AGENTS.md required-reading section so that agents working inside a repo know to consult AND maintain the repos INTENT.md and ARCHITECTURE.md; immediately apply this discipline by manifesting the recent session intent (records 894 through 941) into the relevant per-repo files across nota-next, schema-next, schema-rust-next, signal-frame, spirit-next, spirit, and other active repos] High Zero)
  ```
- `0yk3` [borderline/none]
  ```
  ([0yk3] [daemon configuration bootstrap virgin-daemon component-triad] Clarification [EXPLORATORY (psyche floated 2026-06-07, invited pushback, pending designer analysis): a virgin daemon — one that has never started, holds no state, and is unconfigured — boots into a semi-started state and waits for a configuration signal message (a meta-signal Configure) rather than taking a configuration-file argument at startup. Configuration becomes a runtime reaction, not a startup arg. Not yet ratified.] Minimum Zero)
  ```
- `0zah` [borderline/working-instruction]
  ```
  ([0zah] [browser automation playwright cli browser-use chrome] Decision [CriomOS-home should configure Playwright CLI as a browser-automation tool, while browser-use integration should be treated as a separate agent/browser layer rather than assuming it can drive the Playwright CLI directly.] Medium Zero)
  ```
- `1f76` [borderline/working-instruction]
  ```
  ([1f76] [persona-pi] Correction [Pi operator-safety dirty-repository prompt must disappear from live Pi] High Zero)
  ```
- `1g8y` [borderline/working-instruction]
  ```
  ([1g8y] [component-migration terminal terminal-control triad-runtime] Clarification [In the triad-runtime component-migration list, terminal-control maps to the EXISTING terminal component repo: migrate terminal. There is no separate terminal-control component repo to create; the migration target is terminal. Per psyche 2026-06-06.] High Zero)
  ```
- `1ljn` [borderline/working-instruction]
  ```
  ([1ljn] [schema header trace extended-header] Clarification [The header system needs better specification because headers identify object behavior. Trace may need an extended header, potentially wider than the normal short header, that can include second-row enum information when the payload object is itself an enum.] Medium Zero)
  ```
- `1xjh` [borderline/impl-detail-belongs-in-report]
  ```
  ([1xjh] [schema rust-emission src-schema in-tree] Decision [|schema-derived Rust code emits to src/schema/lib.rs and src/schema/[module].rs in the crate source tree NOT to OUT_DIR/schema; this matches the literal wording of record 902 and is the load-bearing choice for visibility and grep-ability - the schema-derived Rust lives alongside hand-written Rust in the same crate src directory and can be read by humans and tools without rebuilding; operators emission choice in schema-rust-next commit 5ca1c964 to OUT_DIR is corrected by this decision; spirit-next build.rs and any other consumer of schema-rust-next emits to src/schema/ instead; src/schema/ is generated content that gets re-emitted on schema-file edits in development; may be committed or gitignored depending on workspace policy but the path is fixed|] High Zero)
  ```
- `1znt` [borderline/agent-conclusion]
  ```
  ([1znt] [component-shape] Decision [Migration split affirmed] High Zero)
  ```
- `2dku` [borderline/working-instruction]
  ```
  ([2dku] [sema database-work file-extension redb-rename] Clarification [SEMA means database work specifically WRITING to the durable state file; the file extension is currently .redb but should be renamed to .sema to make the substrate name consistent across runtime layer + file extension; the runtime SEMA engine does database work into the .sema file; this makes the naming chain coherent - SEMA schema describes the data types, SEMA engine processes input/output, SEMA file holds the durable state; the rename lands when durable storage is implemented per bead primary-q2au which currently uses Mutex Vec in-memory; the rename also disambiguates from any other .redb files that might appear in the workspace - .sema clearly signals this is the canonical SEMA-database file of this component] High Zero)
  ```
- `2g1c` [borderline/too-specific-ephemeral]
  ```
  ([2g1c] [signal] Decision [Adopt /155 Part 1 design leans for first prototype: three-tier signal type sizing — Tier 1 LogVariant (autogen via signal_channel! macro, packs root variant tag plus packed sub-discriminators into u64), Tier 2 LogSummary (hand-impl with const-generic 64-byte bound check), Tier 3 current rkyv record; variant always at root for signal_channel!-generated types (Operation/Reply/Effect/SemaObservation); internal data records (structs) emit a constant discriminator plus packed fields rather than reshape into top-level enums] High Zero)
  ```
- `2ikm` [borderline/impl-detail-belongs-in-report]
  ```
  ([2ikm] [engine-report tooling language-server] Decision [Engine situation reports should investigate and use code-introspection tools, including language-server-compatible tools when available, to identify important interfaces, signatures, locations, and code structure.] High Zero)
  ```
- `2otq` [borderline/too-specific-ephemeral]
  ```
  ([2otq] [spirit] Decision [Spirit records may carry a vec of topics rather than a single topic - proposed; current shape is single topic] Medium Zero)
  ```
- `2we4` [borderline/too-specific-ephemeral]
  ```
  ([2we4] [spirit] Decision [Spirit skill records tool version] High Zero)
  ```
- `2xb8` [borderline/agent-conclusion]
  ```
  ([2xb8] [schema] Correction [The current schema parser is not the intended shape; schema parsing should be re-centered on passes over NOTA-delimited object forms whose goal is producing the AssembledSchema.] High Zero)
  ```
- `3jed` [borderline/status-snapshot]
  ```
  ([3jed] [repository nota schema] Decision [The raw NOTA replacement repository is nota-next. It is the new NOTA implementation, not a branch-only temporary surface.] High Zero)
  ```
- `3n4c` [borderline/impl-detail-belongs-in-report]
  ```
  ([3n4c] [context-maintenance topic-recency cross-lane meta-report] Decision [context maintenance method update - for each topic find all reports across all lanes and recency-rank them, identify what is stale vs newer design and favor newer; cross-lane context maintenance produces ONE meta-report directory in the DISPATCHERS lane with per-lane sub-reports inside, structured as standard meta-directory: 0-frame-and-method.md, then one numbered sub-report per lane covered, then N-overview.md as orchestrator synthesis; each per-lane sub-report is a HANDOFF the agent in that role reads when they do their own context maintenance; this discipline applies to both kept lanes (verify nothing is stale even though active) and retired lanes (amalgamate interesting content into 2-3 topic-grouped summary reports per lane); update skills/context-maintenance.md with the topic-recency method and per-lane sub-report cadence] High Zero)
  ```
- `3uoc` [borderline/too-specific-ephemeral]
  ```
  ([3uoc] [signal-short-header] Constraint [MVP scope: short-header starts simple — 1 root + 7 sub-enums, each sub-enum gets one full byte (256 variants). No sub-byte packing for MVP. Spirit 389 packing optimization (bool 1-bit, 16-variant 4-bit, multi-byte sub-enums, freed-bits deeper nesting) is post-MVP work. The upgrade-handover mechanism (spirit pilot delivering primary-x3ci) means the bit-packing can land later as a no-downtime live upgrade. The minimum viable version is the even-byte 7-sub-enum split; the unevenly-divided-namespace optimization comes when concrete need surfaces and the upgrade machinery can carry the cutover.] High Zero)
  ```
- `4f8b` [borderline/status-snapshot]
  ```
  ([4f8b] [schema-macro] Decision [MVP Spirit should run on schema-derived signal code] High Zero)
  ```
- `4ta9` [borderline/working-instruction]
  ```
  ([4ta9] [schema syntax] Clarification [Do not assume schema sugar syntax has really been implemented; audit and prove the sugar syntax before depending on it.] Medium Zero)
  ```
- `4xi6` [borderline/working-instruction]
  ```
  ([4xi6] [nota] Decision [Authorize removal of legacy double-quoted-string acceptance from nota-codec lexer; migrate all emitters (CriomOS-home Nix modules, lojix-cli, examples, downstream consumers) to canonical bracket form; EXCLUDE legacy intent slash star.nota files which need a separate programmatic extractor that preserves timestamps] High Zero)
  ```
- `4y1h` [borderline/too-specific-ephemeral]
  ```
  ([4y1h] [schema] Decision [Open clarification carry-uncertainty: field ordering inside the schema root struct. Options: A) imports/exports first (let-statement style - define before use), B) input/output first (function-signature style - what-it-takes before what-it-defines). Psyche named both options as candidates; decision not yet locked. Carry both possibilities until psyche picks; design files use option A pending decision since the existing canonical signal-persona-spirit/spirit.schema starts with imports.] Medium Zero)
  ```
- `510s` [borderline/working-instruction]
  ```
  ([510s] [trace enablement per-crate documentation skills component-triad] Decision [Per-crate trace enablement rule should be documented explicitly in skills/component-triad.md — currently implicit in the testing-trace Cargo feature usage. Psyche ratification of designer 487 sub-agent A Decision A.2.] High Zero)
  ```
- `5h76` [borderline/status-snapshot]
  ```
  ([5h76] [nota] Clarification [Square-bracket string delimiters are under active consideration for shell-friendly NOTA] Medium Zero)
  ```
- `5jrg` [borderline/working-instruction]
  ```
  ([5jrg] [component-shape] Decision [Migration trait placement must be decided before implementation] High Zero)
  ```
- `60an` [borderline/working-instruction]
  ```
  ([60an] [signal] Decision [Create owner-signal-version-handover contract now with force-flip / rollback / quarantine authority] High Zero)
  ```
- `65bo` [borderline/agent-conclusion]
  ```
  ([65bo] [deployment cloud] Correction [Re-running the live switch deploy to prometheus with backup internet in place still broke connectivity the same way. The activation runs inside the ssh session, and our ssh to prometheus rides the router services hostapd and dnsmasq that the activation restarts, so stopping them kills the ssh and the half-finished activation, leaving the services down and prometheus unreachable. A backup wan for prometheus does not change our administrative path into it, so it does not save a live switch. The router node must be deployed with boot-mode then a reboot, with no live restart during the deploy, or with the activation run detached on the host so it survives the ssh drop. Backup internet is the recovery net for a bad boot, not a switch-survival mechanism.] High Zero)
  ```
- `6apw` [borderline/none]
  ```
  ([6apw] [spirit] Clarification [Multi-topic Spirit support is worth auditing as a way to bring the implementation closer to intent, and another hard database migration is acceptable to consider for that feature.] Medium Zero)
  ```
- `6px0` [borderline/duplicate-or-superseded]
  ```
  ([6px0] [deploy] Correction [Spirit cutover Path A (stop/freeze) is superseded - use the smart socket handover (full version-projection + signal-version-handover mechanism per /285) for the first cutover] High Zero)
  ```
- `76jt` [borderline/impl-detail-belongs-in-report]
  ```
  ([76jt] [spirit certainty magnitude rkyv intent-removal] Correction [Record 1215 carried a flawed implementation detail. The Zero variant on shared Magnitude must be declared physically LAST in the enum source order, not first - declaring Zero first shifts every pre-existing rkyv discriminant by one byte and silently reinterprets every archived Spirit record. Persisted rkyv discriminants for Minimum=0 through Maximum=6 must stay stable. Semantic-bottom for Zero is restored via a manual Ord impl using an order_rank function where Zero ranks 0 and Minimum..Maximum rank 1..7, not via Rust derived Ord on declaration order. The operator deployed signal-sema with Zero=7 plus manual Ord and was correct to deviate from the agent-authored 1215 wording. The substance of 1215 - Zero variant on shared Magnitude, persona-spirit per-component meaning - stands; only the declaration-order detail is corrected. This is agent transcription drift, not psyche supersession.] High Zero)
  ```
- `7ca4` [borderline/working-instruction]
  ```
  ([7ca4] [triad runtime runner] Decision [Extract the generic triad runtime runner now: every schema-derived daemon should plug component logic into shared Signal, Nexus, and SEMA runner objects instead of hand-writing repetitive daemon boilerplate.] High Zero)
  ```
- `7fq9` [borderline/none]
  ```
  ([7fq9] [orchestrate] Decision [agent checkout should drive commits] Medium Zero)
  ```
- `7jd9` [borderline/too-specific-ephemeral]
  ```
  ([7jd9] [schema-prep-sandbox-upgrade] Constraint [Schema prep starts repo schema files at version 0.1 and tests Spirit database upgrades only against a sandbox copy] High Zero)
  ```
- `7mgq` [borderline/impl-detail-belongs-in-report]
  ```
  ([7mgq] [schema macro] Principle [Schema macro recognition should be expressible as recursive shape predicates over object blocks, such as holding two root objects where the second object is a square-bracket block or qualifies as a symbol.] High Zero)
  ```
- `7u3j` [borderline/working-instruction]
  ```
  ([7u3j] [reports] Correction [Long answers with diagrams / tables / headings / walk-throughs MUST go in reports, not chat. Strengthen the AGENTS.md rule with explicit shape triggers] High Zero)
  ```
- `7wq1` [borderline/too-specific-ephemeral]
  ```
  ([7wq1] [pi compaction pi-continue] Decision [Use the pi-continue package as the Pi continuation workaround for compaction stopping during long work.] High Zero)
  ```
- `81b2` [borderline/impl-detail-belongs-in-report]
  ```
  ([81b2] [database-copy-not-share] Clarification [The new daemon during upgrade handover COPIES the old database to its own location and runs migration, rather than opening the shared redb. The shared-DB picture in /175 §6 and §7.2 was imprecise. Copy+migrate is required for cross-version upgrades (different schemas) and is also safer for same-version handovers (allows rollback if new daemon fails post-cutover). Implication for Mirror: the operation transfers state changes that happened AFTER the copy point but BEFORE the cutover instant - sync-write components like Spirit need no mirror delta; in-memory-state components like orchestrate do.] High Zero)
  ```
- `84hx` [borderline/status-snapshot]
  ```
  ([84hx] [spirit] Principle [Spirit readiness needs live daemon tests] High Zero)
  ```
- `8fh4` [borderline/working-instruction]
  ```
  ([8fh4] [operator] Principle [Operator should implement clear designer-downstream work in parallel] High Zero)
  ```
- `8mdt` [borderline/impl-detail-belongs-in-report]
  ```
  ([8mdt] [rkyv schema-emission closed-sum-enum sub-division] Decision [Schema-rust-next emitter should sub-divide closed-sum enums past approximately ten variants — when an authored closed-sum exceeds the rule-of-thumb ten-variant threshold, the emitter splits into sub-enums grouped by semantic kinship rather than ship one flat enum. Audit Suggestion 3 from designer 452 surfaces this from rkyv match-table size and authoring-ergonomics rationale in the eight-pilot audit] High Zero)
  ```
- `8sii` [borderline/impl-detail-belongs-in-report]
  ```
  ([8sii] [schema schema-language collections nota-schema-language] Clarification [Proposed syntax for the collection capability gap named in the 40 port-feasibility report as the decisive gate. Collection types at type-reference positions are positional macro forms with the collection name first: a vector is Vec followed by its element type, and a key-value map is KeyValueMap followed by key type then value type. This fits the existing schema macro model per record 880 - dispatch by head symbol and shape - and is the concrete shape of letting TypeReference carry type arguments. Proposed by the psyche as a question pending confirmation; Option and the exact map naming still open.] Low Zero)
  ```
- `8w1v` [borderline/status-snapshot]
  ```
  ([8w1v] [spirit] Decision [spirit upgrade testing requires a full sandbox upgrade run inside systemd-nspawn on Prometheus; this is the new acceptance gate for either lanes implementation] High Zero)
  ```
- `9aur` [borderline/working-instruction]
  ```
  ([9aur] [domain-criome] Decision [Create domain-criome as the Criome-domain component: a name-server/root-registry shape that also speaks the richer Signal resolution protocol.] Medium Zero)
  ```
- `9l1z` [borderline/too-specific-ephemeral]
  ```
  ([9l1z] [cloud] Principle [cloud-designer scope on the browser-control work: design + prototype + write report + create beads for cloud-operator to implement. cloud-designer does not implement. Reports must carry two open-question sections — one for the psyche (questions awaiting human direction before implementation can proceed) and one for the implementing cloud-operator (clarifications they should resolve as they implement). Passing forward open questions is part of the lane handoff.] High Zero)
  ```
- `9ylx` [borderline/impl-detail-belongs-in-report]
  ```
  ([9ylx] [upgrade schema-edit examples] Clarification [Concrete examples of schema-editing upgrade operations: add a new field to a named object with a given type; change a single string field to a vector field with migration moving the previous single value into the vector as first member; change a single symbol-qualified string to a vector of symbol-qualified strings with the same wrap-singleton migration shape.] High Zero)
  ```
- `a3p9` [borderline/production-framing]
  ```
  (a3p9 [orchestrate replacement] Correction [The real orchestrate replacement does not need legacy argv compatibility. The replacement surface is the real component CLI using one NOTA argument against orchestrate-daemon; old tools/orchestrate argv compatibility is not the target.] VeryHigh Zero)
  ```
- `abjw` [borderline/status-snapshot]
  ```
  (abjw [schema] Decision [The schema repository continues at its current name; no rename needed because it is already free of the persona prefix. The new schema-derived stack continues to build there. Per psyche: Schema is already new, right? Yeah, get started on that.] High Zero)
  ```
- `ak2p` [borderline/too-specific-ephemeral]
  ```
  (ak2p [cloud] Constraint [API key via env-var-populated-by-password-manager pattern (existing FEMOS utility pattern); investigate safer auth alternatives during design] Medium Zero)
  ```
- `awn3` [borderline/too-specific-ephemeral]
  ```
  (awn3 [criomos llm gemma] Decision [Fixing Gemma 4 on Prometheus requires the serving stack to actually support the gemma4 GGUF architecture, not merely listing Gemma 4 in the model inventory.] High Zero)
  ```
- `b1lv` [borderline/too-specific-ephemeral]
  ```
  (b1lv [component-shape] Decision [ItemPriority in signal-persona-mind collapses onto signal_sema::Magnitude] High Zero)
  ```
- `bbb6` [borderline/working-instruction]
  ```
  (bbb6 [spirit certainty production] Decision [Production Spirit should support live certainty changes on existing records, including using the shared Magnitude Zero floor for removal-candidate nomination, so the removal-candidate workflow is implemented rather than only documented or query-shaped.] High Zero)
  ```
- `bn6g` [borderline/working-instruction]
  ```
  (bn6g [engine-report skill] Decision [Add an engine report skill so the workspace can repeatedly produce standardized situation reports over engines and components.] High Zero)
  ```
- `bo17` [borderline/too-specific-ephemeral]
  ```
  (bo17 [deploy] Decision [Spirit cutover uses dual write] High Zero)
  ```
- `bvuo` [borderline/too-specific-ephemeral]
  ```
  (bvuo [persona-pi] Decision [Persona-Pi should expose GPT-5.5 and GPT-5.4-mini model choices] High Zero)
  ```
- `c0uv` [borderline/working-instruction]
  ```
  (c0uv [workspace] Decision [Vocabulary sweep + skill: one-shot designer-led sweep across ARCH + reports + code; canonical vocabulary encoded in new skills/workspace-vocabulary.md] High Zero)
  ```
- `cmn7` [borderline/too-specific-ephemeral]
  ```
  (cmn7 [prometheus networking router] Decision [Prometheus should use the newly connected USB WiFi adapter and two USB Ethernet adapters as backup connection points.] VeryHigh Zero)
  ```
- `cub9` [borderline/impl-detail-belongs-in-report]
  ```
  (cub9 [spirit documentation agent-errors] Constraint [Spirit documentation must make the production Observe query surface unambiguous for agents by showing the exact accepted wrapper forms and warning that Search is not a live request head unless implemented.] High Zero)
  ```
- `def2` [borderline/impl-detail-belongs-in-report]
  ```
  (def2 [schema-component] Principle [Possible new schema component — a dedicated triad that keeps track of all schemas across the engine and serves schema lookups keyed by the short header. Given a 64-bit short header from a wire message or sema operation, the schema component returns the typed schema (signal or sema) that produced it. This is the runtime registry side of the NOTA schema language work (spirit 391); it complements the upgrade triad (spirit 369) which handles cutover orchestration. Open question: is this a separate triad (schema) or absorbed into upgrade-daemon? Medium certainty — the idea is named and concrete but the daemon-shape decision (separate vs absorbed) is open. Forward direction: depends on whether second-designer-/319 schema-language design names a runtime registry surface; if so the schema component is the persistent home for that registry.] Medium Zero)
  ```
- `dppt` [borderline/duplicate-or-superseded]
  ```
  (dppt [spirit removal-candidates] Decision [Spirit removal candidates are represented by record certainty being absent: None means confidence withdrawn and nominated for removal; Some Minimum remains weak but real intent and must not be collapsed with removal nomination.] High Zero)
  ```
- `e78u` [borderline/none]
  ```
  (e78u [orchestrate-port] Decision [Port the orchestrate component to schema-based engine + no-downtime upgrade in parallel with operators Spirit MVP work. Operator stays on main; second-designer port runs on a worktree. This is both a concrete porting task and a precedent for how to scale schema-based migration to multiple components concurrently without blocking each other.] High Zero)
  ```
- `ey5p` [borderline/none]
  ```
  (ey5p [cloud schema triad] Decision [The cloud component upgrade should move onto the new schema-interface and triad-engine approach, with next carrying the breaking design path and main carrying the operator integration path.] Maximum Zero)
  ```
- `fip0` [borderline/working-instruction]
  ```
  (fip0 [tests schema-data-types nota-as-test-format no-string-encoding] Correction [tests are NOT REAL if they use string-encoded state instead of schema-emitted data types - observer assertions like vec of flow-sent-1 flow-processed-1 hide the typification in strings, the observer state is stringly-typed which bypasses the very type system the design relies on; tests must hold TYPED schema-emitted data (e.g. Vec of MailLedgerEvent) and assert against typed values; NOTA is the canonical data format for assertions where serializable representation is desired - the system already converts to NOTA at the wire boundary, so tests can use NOTA round-trip for assertions or compare typed values directly; same anti-pattern as skills rust methods Dont hide typification in strings but at the test layer; this discipline applies broadly - audit existing tests including the on_sent_hookable_pilot test file at spirit-next branch designer-on-sent-hookable-pilot, fix them to use typed observer state OR NOTA round-trip, and add the discipline to skills/testing.md so future tests follow it] High Zero)
  ```
- `fizd` [borderline/agent-conclusion]
  ```
  (fizd [testing-build runtime-witness layer-2 proof-of-usage] Principle [The testing-build logging socket is the workspace canonical Layer 2 runtime witness for engine-trait usage. When testing build is active and logs flow to the CLI, the agent observes proof that Signal/Nexus/SEMA engine traits are actually called by the runtime — not just present in source. This realizes the proof-of-usage ladder from designer 459 as a deployable feature.] High Zero)
  ```
- `fmcz` [borderline/too-specific-ephemeral]
  ```
  (fmcz [sema-upgrade] Decision [temporary migration CLI is acceptable for real database upgrade testing] High Zero)
  ```
- `fxyk` [borderline/too-specific-ephemeral]
  ```
  (fxyk [orchestrate] Decision [Second-designer dispatches subagents during MVP development phase to look at different design angles, finding clarifications between psyche-enunciated design and current implementation. Counter-ego role extends beyond doubt/critique into active advancement of design.] High Zero)
  ```
- `fz9n` [borderline/agent-conclusion]
  ```
  (fz9n [schema rust rkyv asschema emission] Decision [The schema pipeline report 524 and operator feedback 316 are accepted as the implementation direction: implement schema-in-Rust as the rkyv-serializable typed schema value, use canonical round-trip rather than byte-identical source preservation, avoid a public SchemaResolution IR, and have Rust emission lower from high-level schema datatype methods.] High Zero)
  ```
- `g1wb` [borderline/too-specific-ephemeral]
  ```
  (g1wb [schema rust composer] Decision [The Rust emission repository for the schema-derived stack is schema-rust-next. Rust emission is a separate step from Rust macros: the stack generates Rust code first, and macros are a later or separate consumption surface.] High Zero)
  ```
- `g3ax` [borderline/working-instruction]
  ```
  (g3ax [triad-main daemon emitter peer-credential connection-context origin] Decision [The emitted daemon spine (triad_main) must thread per-connection context — peer credentials from SO_PEERCRED (std UnixStream::peer_cred) — into the handle_working_input hook, so components that mint origin tags from peer credentials (message forwarding to router; router; persona) keep that capability. The current emitted spine discards the UnixStream and passes only the decoded Input, which regressed message origin minting to a constant Owner tag. Enhance triad-runtime (a ConnectionContext type) + the schema-rust-next emitter so the emitted ComponentDaemon hook gains a connection-context parameter and the spine captures and passes it. Per psyche 2026-06-06.] High Zero)
  ```
- `gb95` [borderline/working-instruction]
  ```
  (gb95 [schema-rust-next signal-frame plane-gating runtime-emitter] Decision [Signal-frame support must NOT be emitted for every target. It is currently plane-ungated — emit_signal_frame_support is called unconditionally at render time — so nexus-only, sema-only, and wire-contract targets wrongly receive signal-frame code. Gate it by emits_signal() like every other signal construct: only signal-emitting targets get signal-frame support. This resolves the latent inconsistency flagged in designer report 537 — it was a bug, not intentional always-on wire support.] High Zero)
  ```
- `gh29` [borderline/impl-detail-belongs-in-report]
  ```
  (gh29 [rkyv schema-emission recursive-variants box] Decision [Schema-rust-next emitter should automatically wrap recursive enum variants in Box — when a variant payload type is the same as or recursive on the enclosing type, emit Box<T> rather than T to avoid the unsized-type compile failure every schema author would hit by hand. Audit Suggestion 1 from designer 452 verified across eight pilot tests on the audit-rkyv-enum-wrapping-presumption schema-next branch] High Zero)
  ```
- `gpkq` [borderline/too-specific-ephemeral]
  ```
  (gpkq [workflow operator designer worktree prototype] Clarification [For this workstream, operator integration remains on main while designer work for anything new proceeds in worktrees rebased on main, reusing past work where appropriate, and continuing one design/prototype at a time around what the prototype is meant to prove.] High Zero)
  ```
- `gwiv` [borderline/working-instruction]
  ```
  (gwiv [component-shape] Decision [CLI writing should be one macro invocation that emits the full binary AND auto-injects the caller pid into the frame envelope; design the elegant shape] High Zero)
  ```
- `h16n` [borderline/too-specific-ephemeral]
  ```
  (h16n [criomos vm-testing deploy prometheus router networking constraint] Constraint [The CriomOS VM-testing node feature is authorized to land and deploy to Prometheus, but ONLY if it will not break networking — Prometheus is the cluster router, so its routing and networking must remain fully intact. The feature's networking (VM bridges, TAP devices, NAT/firewall rules, Criome-domain routing) must be proven non-conflicting with Prometheus's router role before any deploy; if non-breakage cannot be established, do not deploy to Prometheus. Extends records 2630 and 2631.] High Zero)
  ```
- `h2rv` [borderline/working-instruction]
  ```
  (h2rv [nota-design] Decision [NOTA name-value notation is the curly-brace map form and must be implemented and tested.] High Zero)
  ```
- `h4zw` [borderline/status-snapshot]
  ```
  (h4zw [version-control] Decision [Optional third 'stable' branch alongside main and next is deferred until the architecture port from Spirit (the pilot) reaches all other components AND each component bootstraps from its own architecture discovery. Until then main + next is sufficient.] Medium Zero)
  ```
- `h7ql` [borderline/impl-detail-belongs-in-report]
  ```
  (h7ql [mermaid-failure-patterns] Decision [Document the recurring patterns that break mermaid rendering when agents generate diagrams: semicolons in sequenceDiagram + stateDiagram labels, arrow forms without proper spacing such as -x or --x, semicolons or commas in Note text confusing parser, long descriptions that overlap visually in narrow nodes. The skills/mermaid.md skill should explicitly call these out as anti-patterns so agents avoid them on first try rather than after a render-error round trip with the psyche.] High Zero)
  ```
- `hehp` [borderline/working-instruction]
  ```
  (hehp [signal-executor nexus execution-engine component-dependency drift migration] Correction [No component daemon depends on signal-executor or the executor layer — execution runs through nexus. The current orchestrate, repository-ledger, persona-spirit, and upgrade dependencies on signal-executor are drift to remove by migrating their request-execution to nexus. signal-executor is archived; consumers resolve it read-only until migrated.] High Zero)
  ```
- `hp7q` [borderline/working-instruction]
  ```
  (hp7q [cloud cloudflare cli] Clarification [Investigate Cloudflare command-line tooling as the possible first adapter because the psyche expects it may be the most stable way to reach Cloudflare, while preserving the option to use the API directly if the CLI is not suitable.] High Zero)
  ```
- `hwsk` [borderline/working-instruction]
  ```
  (hwsk [schema spirit signal sema] Decision [Build a full feature-equivalent branch for Spirit using the full-schema approach: ordinary signal, owner signal, and signal-to-sema languages are all defined as schema files that expand through schema macros into AssembledSchema and then into Rust.] High Zero)
  ```
- `i1bd` [borderline/impl-detail-belongs-in-report]
  ```
  (i1bd [spirit-next nexus mail-keeper typestate] Clarification [spirit-next realizes record 970s Nexus-holds-the-mail as a data-bearing TYPESTATE: in-flight mail is Mail<BeingProcessed> carrying the lowered SemaInput and reached mail is Mail<Processed> carrying the Signal Output; the two phases hold DIFFERENT data so they are structurally distinct values not one struct wearing a PhantomData marker; the only constructor of Mail<Processed> is the private run_sema which consumes a Mail<BeingProcessed> by value and threads it through the durable store so Nexus-holds-the-mail-is-being-processed becomes a compile-time fact rather than a log entry; the Nexus object owns the SEMA store handle plus the mail ledger and the Engine is a thin composer that never calls the store directly] High Zero)
  ```
- `ic7c` [borderline/too-specific-ephemeral]
  ```
  (ic7c [ouranos swap deploy root-ssh] Constraint [Ouranos swapfile creation and activation must use root SSH rather than local sudo.] VeryHigh Zero)
  ```
- `iqkj` [borderline/too-specific-ephemeral]
  ```
  (iqkj [cloud] Decision [Prefer Cloudflare CLI over direct HTTP API for first integration if easier] Medium Zero)
  ```
- `ite1` [borderline/impl-detail-belongs-in-report]
  ```
  (ite1 [schema nota parser] Principle [Schema parsing needs an object-block pass that breaks NOTA text into delimiter-bounded blocks with source locations before schema macro lowering. Blocks should expose methods such as delimiter kind, source span, root object count, and recursive child-shape predicates.] High Zero)
  ```
- `iu7g` [borderline/too-specific-ephemeral]
  ```
  (iu7g [sema] Principle [Legacy intent files can be modeled as an earlier Sema database version for migration tests] High Zero)
  ```
- `j7u7` [borderline/working-instruction]
  ```
  (j7u7 [spirit-next interface] Constraint [The spirit-next implementation should be based on the best current designer and operator prototype ideas, with elegant explicit interfaces in code rather than ad hoc wiring.] High Zero)
  ```
- `jxi9` [borderline/too-specific-ephemeral]
  ```
  (jxi9 [lojix new-stack bring-online build-evaluate milestone prometheus-vm end-to-end-test cutover-sequencing] Decision [Before full production cutover there is an intermediate milestone for the new lojix stack: bring the whole new stack online to where it can actually BUILD node closures and be EVALUATED with the new design and components — buildable-and-evaluable, not yet production-durable. In-memory runtime state is acceptable for this milestone; durable persistence and full Stack A parity are the later production-cutover bar per fe2j and tvbn. End-to-end validation aspires to a real build and, if Prometheus VM capability allows, a real deploy to the Prometheus virtual machine as the concrete sandbox-OS-booted-on-the-new-logics test from j9ba. The immediate goal is to build things with the new stack and evaluate, not to switch production nodes yet.] High Zero)
  ```
- `k1i1` [borderline/status-snapshot]
  ```
  (k1i1 [persona naming deprecation migration component-triad workspace] Constraint [The persona-* prefixed names are DEPRECATED. The persona-mind/persona-router/persona-harness/persona-system/persona-orchestrate component repos and the signal-persona-{mind,message,system,harness,router,orchestrate,introspect} contract names are RETIRED and DELETED; the canonical live names are the bare component names (mind, router, harness, system, orchestrate, message, introspect) plus signal-<component> / meta-signal-<component> contracts. KEEP (still live): persona (the meta-repo), persona-spirit and signal-persona-spirit, signal-persona-origin. Any other persona-* reference — in docs, flake.nix/Cargo build files, or code — is drift to migrate. Notably persona own flake.nix references ~176 deleted persona-* repo URLs and its Cargo.toml depends on deleted signal-persona-* contracts, so persona is badly un-migrated and likely does not build. Per psyche 2026-06-07.] High Zero)
  ```
- `k217` [borderline/impl-detail-belongs-in-report]
  ```
  (k217 [spirit] Principle [Spirit schema migration design must account for live writes during migration] High Zero)
  ```
- `k8in` [borderline/impl-detail-belongs-in-report]
  ```
  (k8in [upgrade compilation binary-spawn] Constraint [New Rust data types require recompiling the daemon binary. The upgrade flow culminates in compiling a new daemon binary from the derived types, then spawning the new daemon alongside the old one to test the upgrade against either a throwaway copy of live data or a minimal test database.] High Zero)
  ```
- `ketd` [borderline/too-specific-ephemeral]
  ```
  (ketd [workspace] Decision [Possible persona-operator role for persona-stack maintenance and renaming work] Medium Zero)
  ```
- `kfw0` [borderline/too-specific-ephemeral]
  ```
  (kfw0 [schema rust interface] Constraint [The implementation presentation should especially show typical schema components and Rust code emission components as first-class interfaces.] High Zero)
  ```
- `kgqr` [borderline/working-instruction]
  ```
  (kgqr [reports] Decision [Aggressive consolidation: move old reports into re-contextualized new reports leaving out stale context in the deleted old versions. Supersedes conservative defaults of context-maintenance sweep when psyche explicitly directs consolidation. Pairs with skills/contract-repo.md as a primary lens for re-contextualization (verb form, reply naming, namespace, closed enums)] High Zero)
  ```
- `kuw2` [borderline/impl-detail-belongs-in-report]
  ```
  (kuw2 [schema rust emission] Decision [Ratify a small Rust item, Rust impl block, and Rust match token model for schema-rust-next emission, with the writer owning indentation depth instead of emitting most implementation code through hand-counted formatted strings.] Medium Zero)
  ```
- `l0uh` [borderline/status-snapshot]
  ```
  (l0uh [system] Clarification [Prometheus has full systemd-nspawn sandbox capability for testing persona system components with full OS permissions] High Zero)
  ```
- `l910` [borderline/impl-detail-belongs-in-report]
  ```
  (l910 [design-mockup-method] Decision [Each sub-agent dispatched for mockup work lands: a worktree feature branch, a working implementation with tests, a per-agent report in the meta-directory, and a bead pointing operator at the worktree. Orchestrator writes the overview report tying all sub-agent worktrees together so operator can find every branch from one location.] High Zero)
  ```
- `latq` [borderline/working-instruction]
  ```
  (latq [triad-runtime daemon two-socket dual-listener runner-wired component-triad] Decision [The two-socket runner-wired triad daemon is built once in triad-runtime as a DualListenerDaemon, not worked around per component. It binds the ordinary peer-callable socket and the owner meta socket, tags each arrival into the shared SignalInput union the nexus schema declares, and routes through the generated NexusEngine execute recursive runner. Every triad component needs this; lojix is the first driver and cloud should adopt it too, replacing its legacy inline two-listener bypass. Modifying triad-runtime for this is authorized.] High Zero)
  ```
- `lkp7` [borderline/working-instruction]
  ```
  (lkp7 [schema prototype compatibility strict-syntax e2e spirit-intent] Decision [Because this stack is still a prototype, remove compatibility paths instead of preserving old schema syntax. The implementation should follow the current specs directly: positional roots, strict brace key-value declarations, homogeneous enum vectors, no labeled root wrappers, no old keyed declaration sigils, no pipe declaration compatibility. End-to-end tests should prove the current path through schema, asschema, emitted Rust, and spirit runtime.] High Zero)
  ```
- `lnhj` [borderline/impl-detail-belongs-in-report]
  ```
  (lnhj [triad-main component-triad schema-rust-next daemon code-emission streaming-emission daemon-configuration] Decision [triad_main! is implemented as an EMITTED source-visible daemon module — schema-rust-next emits src/schema/daemon.rs — NOT a literal macro, consistent with the source-visible-emission discipline (triad-runtime neither owns nor emits it; live anchor is 1488; cited 1419/1486 are dead refs). The emitter generates the uniform daemon skeleton (fn main, DaemonCommand argv-to-config, Single/MultiListenerDaemon bind/serve, the decode-execute-encode handle_stream spine, per-tier dispatch). The component hand-writes ONLY an impl ComponentDaemon declaring its escape hatches (build_runtime for required Store/Engine construction; Configuration/Engine/Error/PROCESS_NAME) plus a schema-side NexusDaemonShape declaration (process name, listener tiers, socket modes). Decided forks: generate the runtime spine (not a required trait impl); explicit NexusDaemonShape; a triad-runtime DaemonConfiguration trait the component config implements; a thin component-agnostic run_to_exit_code helper in triad-runtime; spirit pilots single-listener before cloud multi and domain-criome. STREAMING IS OPTION B (psyche 2026-06-06): the emitter generates the daemon-side publish/subscribe wiring FROM the schema declared Schema streams, shrinking the component escape hatch from hand-writing the subscription hub to merely DECLARING the stream; spirit hand-written SubscriptionHub becomes emitted streaming plumbing. Full design: designer report 542.] High Zero)
  ```
- `lzgd` [borderline/agent-conclusion]
  ```
  (lzgd [architecture] Principle [Design problems reopen library and architecture questions] Medium Zero)
  ```
- `m5po` [borderline/working-instruction]
  ```
  (m5po [schema macro spirit] Decision [The schema macro design must be finished enough to be used in the Spirit schema path. Schema-next should move beyond hard-coded lowering into a real macro registry / macro-dispatch design, and spirit-next should consume that path through its existing schema build pipeline so the macro system is exercised by the working CLI/daemon target.] High Zero)
  ```
- `mdmr` [borderline/too-specific-ephemeral]
  ```
  (mdmr [workflow worktree next-branch rotation deferred] Clarification [A standard worktree rotation for next branches is a possible future convention but is explicitly deferred and not decided right now; for now designers use the existing worktree location for next-branch work.] VeryLow Zero)
  ```
- `mnl1` [borderline/working-instruction]
  ```
  (mnl1 [schema-next nota asschema syntax] Correction [Obsolete ASSchema vector-record syntax must be erased from active code examples and operator reports, not merely marked obsolete. Current examples must use the new raw-NOTA-first schema direction: .schema files are legal NOTA, core schema roots are known from filenames, and datatype maps are native brace key-value maps of raw NOTA datatype objects before lowering.] High Zero)
  ```
- `mxmn` [borderline/too-specific-ephemeral]
  ```
  (mxmn [persona] Decision [Pattern-based decision: each engine has its own persona-spirit daemon, scoped under /var/lib/persona/<engine-id>/spirit.redb. Per persona ARCH 1.5 engine-id-scoped resources, each engine owns its own component federation; spirit is part of that federation. Manifested in persona/ARCHITECTURE.md 1.5 as the Spirit-per-engine subsection. Closes /249 Gap 19. Pattern is the engine-id-scoped resources table that already exists for mind/router/harness/terminal/message; spirit follows the same shape.] High Zero)
  ```
- `n3k6` [borderline/working-instruction]
  ```
  (n3k6 [schema nota prototype] Principle [The prototype should demonstrate the intended direction: NOTA object parsing is delimiter-first, schema interpretation is a separate lowering layer, and Rust reading code is emitted from assembled schema rather than from legacy signal macro text.] High Zero)
  ```
- `naet` [borderline/status-snapshot]
  ```
  (naet [criomos prometheus networking] Decision [Production CriomOS is now the target surface for the Prometheus USB network backup work.] High Zero)
  ```
- `nf5k` [borderline/too-specific-ephemeral]
  ```
  (nf5k [spirit] Correction [RecordAccepted schema should not produce nested echo noise] High Zero)
  ```
- `nlv8` [borderline/too-specific-ephemeral]
  ```
  (nlv8 [nexus-engine signal-message-embedding brainstorm] Clarification [Psyche brainstorm (not firm): a Nexus input message may include the original Signal message verbatim in most cases so Nexus has access to the raw input it is deciding on. Pattern not yet ratified; operator audit during implementation will surface whether this is universal or per-variant.] Medium Zero)
  ```
- `np01` [borderline/too-specific-ephemeral]
  ```
  (np01 [schema testing spirit] Constraint [The full-schema Spirit branch must prove the generated path works and must match the current production Spirit 0.3 capability surface.] High Zero)
  ```
- `nt00` [borderline/working-instruction]
  ```
  (nt00 [prototype schema-at-heart end-to-end-tests cycle-2 restart] Decision [the next prototype iteration runs with SCHEMA AT THE HEART - schema-emitted Rust types are the canonical truth source for every type that appears in the system; everything else is methods written by agents on those nouns or trait impls that schema-emitted types implement; no hand-written enum stubs at system boundaries, no hand-written observer state, no hand-written ValidationError or DatabaseMarker shims; the tests test THE THING WE ARE BUILDING - every test constructs schema-emitted values invokes the engine through its schema-emitted trait surfaces observes schema-emitted events and asserts on schema-emitted typed outputs; end-to-end tests prove the schema drives the system; this iteration RESTARTS the prototype on a fresh designer feature branch rebased off the latest operator main stack rather than incrementing on the existing designer-on-sent-hookable-pilot branch; the audit step at the end of the cycle critiques whether every component is fully schema-driven and surfaces the remaining shims as next-cycle development tasks] High Zero)
  ```
- `o1sl` [borderline/working-instruction]
  ```
  (o1sl [spirit identifier migration hash mapping-dump transition] Correction [Migrate ALL existing Spirit records to random-hash ids now rather than grandfather the integers. Existing integer references may break, which is acceptable. Produce a transitional dump file mapping former integer ids to the new hashes, kept around for a little while so agents can still resolve old references during the transition, then retired. Yields a uniform hash id space with a temporary bridge. Supersedes the grandfather decision in record 2591.] High Zero)
  ```
- `o7a3` [borderline/working-instruction]
  ```
  (o7a3 [schema-rust-next rust-emission quote tokens] Constraint [schema-rust-next runtime emission should move to quote typed-token lowering now; hand-formatted runtime emission is not an acceptable target shape.] High Zero)
  ```
- `ocrf` [borderline/working-instruction]
  ```
  (ocrf [schema-implementation design-review logic-on-objects] Constraint [Before implementing schema stack changes, the operator should inspect current designer work for better design patterns and prefer elegant logic-on-objects: behavior lives on schema-created data types with NOTA and binary signal representations, not on free functions or legacy macro helper surfaces.] High Zero)
  ```
- `ojiy` [borderline/agent-conclusion]
  ```
  (ojiy [reports] Clarification [context maintenance repackages older relevance into fresh context] High Zero)
  ```
- `ot9e` [borderline/too-specific-ephemeral]
  ```
  (ot9e [criomos testing nix] Constraint [Full operating-system build tests for this reconciliation must run on remote builders with max-jobs zero, not as local laptop build fanout.] High Zero)
  ```
- `p4dj` [borderline/production-framing]
  ```
  (p4dj [workspace component-shape] Decision [Double-implementation strategy for major architectural breaks: operator and designer run two parallel implementation tracks. Operator creates new repositories (suffix -next when upgrading a previous concept; new name when introducing a new concept), then works directly on the main branch of those new repos - the main branch IS the amalgamation of best ideas from previous prototypes integrated into one substrate. Designer iterates a parallel design on worktree branches off operator's main as base, OR on dedicated design-prefixed repositories. The two tracks are COMPARED periodically; convergence drives integration. Per psyche 2026-05-26.] High Zero)
  ```
- `pijx` [borderline/working-instruction]
  ```
  (pijx [spirit skills documentation] Decision [Live production Spirit multi-topic record search syntax belongs in the Spirit CLI skill documentation so agents use the deployed interface rather than stale query shapes.] High Zero)
  ```
- `prwb` [borderline/too-specific-ephemeral]
  ```
  (prwb [component-shape] Decision [The sema database holds its schema-version hash as the canonical version identity. The hash is derived from a deterministic content-addressable version schema — these phrases are the same idea expressed two ways: deterministic because the schema's content fully determines its hash; content-addressable because the hash IS the address. The hash supersedes (or is layered above) Cargo semver as the version-identity machinery. Connects /276 critical point 10a (schema-address vs semver) with a Maximum-certainty answer: schema-version-hash IS the version. Today's design focus: design this further — the NOTA schema language that emits the hashable schema, how the hash is computed, how the database stores it, how sema-upgrade-daemon dispatches on it.] High Zero)
  ```
- `pzqw` [borderline/agent-conclusion]
  ```
  (pzqw [redb-copy-during-writes-unsafe] Correction [Intent 550 was premature. Sub-agent A proved the OS does not lock the redb file against fs::copy when another process holds it open (true on Linux - no mandatory locks by default). Sub-agent A did NOT prove the resulting copy is transaction-coherent under concurrent writes. fs::copy is NOT atomic with respect to writes happening mid-copy: torn reads, partial pages, inconsistent B-tree state, header-points-to-nonexistent-pages are all possible failure modes. The correct design needs either: (a) freeze writes at AskHandoverMarker BEFORE copy (matches /331 brief-outage MVP), or (b) coherent copy mechanism like redb backup API or filesystem CoW snapshot. Lean (a) for MVP, (b) long-term.] High Zero)
  ```
- `q427` [borderline/too-specific-ephemeral]
  ```
  (q427 [reports] Clarification [current situation means post-maintenance work state] High Zero)
  ```
- `qfgx` [borderline/working-instruction]
  ```
  (qfgx [schema prototype component completeness] Principle [The prototype must be audited against whether it uses all designed components fully: NOTA structure, schema macro lowering, assembled schema, Rust emission, generated signal, Nexus mail keeper, SEMA state handling, and Spirit runtime behavior.] High Zero)
  ```
- `qluk` [borderline/too-specific-ephemeral]
  ```
  (qluk [cloud cloudflare dns] Decision [First useful cloud actions are reading DNS/name records and setting new DNS/name records through Cloudflare.] High Zero)
  ```
- `qov9` [borderline/status-snapshot]
  ```
  (qov9 [orchestration] Principle [Designer-to-operator cascade is the emerging development flow] Medium Zero)
  ```
- `qu12` [borderline/agent-conclusion]
  ```
  (qu12 [rkyv enum-wrapping structural-macros sized-format] Clarification [Psyche hypothesis: when structural macros produce outputs of different shape, wrapping with a closed-sum enum carrying each shape as a distinct data-bearing variant is the honest representation in rkyv-archived schema-emitted code, because rkyv archive is sized-exact portable binary — unlike JSON, CBOR, or MessagePack, every value must have known archive size. Designer audit pending to verify or refute through actual implementation testing with a pilot component.] High Zero)
  ```
- `qzxe` [borderline/working-instruction]
  ```
  (qzxe [schema spirit component-shape] Decision [The Signal / Executor / SEMA runtime-triad intent must be spread into implementation surfaces, not remain report-only. Spirit-next is the concrete target: a full working Spirit CLI and daemon should embody the schema-derived Signal plane, explicit Executor lowering, and single-writer SEMA state path as the migration target from production Spirit.] High Zero)
  ```
- `r2cv` [borderline/working-instruction]
  ```
  (r2cv [mermaid reporting skills] Constraint [Psyche wants the Mermaid skill updated with graph-readability limits and the reporting skill to warn, with reference, that unreadable Mermaid graphs violate report quality.] Medium Zero)
  ```
- `r310` [borderline/working-instruction]
  ```
  (r310 [actor-native migration component locking testing] Constraint [Actor-native engine migrations should proceed component by component: lock one repository at a time, test that component thoroughly before moving to the next, and revise the approach when implementation evidence shows a better design.] VeryHigh Zero)
  ```
- `rd7v` [borderline/working-instruction]
  ```
  (rd7v [lojix deploy branch-refs main] Correction [Operator-facing lojix deploy requests should use main branch refs rather than exact revision identifiers.] High Zero)
  ```
- `rnys` [borderline/working-instruction]
  ```
  (rnys [workspace] Constraint [Use jj and run full checks for every changed domain-criome runtime branch] High Zero)
  ```
- `rs0v` [borderline/working-instruction]
  ```
  (rs0v [spirit] Constraint [Upgrade preflight should test backup read responsiveness and mark the target database] Medium Zero)
  ```
- `rsfq` [borderline/too-specific-ephemeral]
  ```
  (rsfq [workflow] Decision [Operator and designer role skills are explicit targets for the rolling design-implementation loop] High Zero)
  ```
- `s8q2` [borderline/impl-detail-belongs-in-report]
  ```
  (s8q2 [macro-table-as-data rust-module-as-data schema-core schema-diff schema-upgrade self-hosting bootstrap-completion four-gaps vision] Decision [Vision for closing the four remaining gaps named in operator 252 (after Stage 3 of report 434 closed). Gap A — macro-table-as-data: macros become typed records in a serializable MacroTable, each with name plus positions plus pattern plus expansion template; bootstrap macros pre-loaded, user macros loaded from .asschema files; unblocks Stage 5 self-hosting and per-domain schema vocabularies. Gap B — RustModule-as-data emitter: emitter produces typed RustModule (Vec<RustItem>) and a renderer turns it into source; tests assert on data, not strings; unblocks semantic snapshot tests and speculative multi-target emission. Gap C — shared schema-core support nouns: extract plane envelopes, routing, frame primitives, mail nouns into a shared schema-core crate (or set); emitter emits use schema_core::Signal instead of inline definitions; deduplicates ~500 lines per emitted component. Gap D — schema diff plus upgrade: Asschema::diff produces typed AsschemaDiff, AsschemaUpgrade artifact codifies migrations, emitter generates From v1 to v2 impls per diff op; sema persistence upgrades at daemon boot when needed. A plus B close the Stage 5 self-hosting loop; C deduplicates emission; D unblocks production schema evolution. Recommended ordering: C → A → B → D absent specific deploy or developer-extensibility pressure. Full vision in report reports/designer/435-vision-for-the-four-remaining-gaps.md. Per psyche 2026-05-30.] High Zero)
  ```
- `shw7` [borderline/working-instruction]
  ```
  (shw7 [schema] Decision [The schema and NotaCore restack should be audited across recent prototypes, with the best pieces and known bad paths distilled into one implementation-target design for operators. Newer prototypes should be weighted as carrying more recent intent, while older branches remain useful evidence for ideas to reuse or avoid.] High Zero)
  ```
- `sjmz` [borderline/status-snapshot]
  ```
  (sjmz [cloud] Clarification [The large AI node is prometheus.goldragon.criome — the production reality. The atlas hostname found in CriomOS-test-cluster/fixtures/horizon/atlas.json is a TEST FIXTURE for the LargeAiRouter node type, not the production node. Beads and design references to atlas as the production target should be updated to prometheus.] High Zero)
  ```
- `tdit` [borderline/working-instruction]
  ```
  (tdit [skills rust skill-discipline fortification] Correction [Agents skip the rust skill when writing rust code and the resulting code is shitty. The skill mechanism itself must be fortified so it cannot be skipped at the moment of rust authorship: structural prevention (hooks, AGENTS.md hard-override pointer, or skill-index restructure that forces a read) rather than a hortative reminder inside the skill file. Verify on disk against skills/rust.md and any skills/rest.md before picking the fortification surface; speech-to-text artifact likely makes rest equal rust.] Maximum Zero)
  ```
- `tvy2` [borderline/working-instruction]
  ```
  (tvy2 [schema implementation interface] Decision [The next schema-derived stack work should implement concrete interfaces and present them clearly, rather than staying at report-only design.] High Zero)
  ```
- `u6z3` [borderline/working-instruction]
  ```
  (u6z3 [component-shape] Decision [Create three new GitHub repositories for the persona-prefix-retirement: spirit, signal-spirit, core-signal-spirit. Side-by-side with the existing persona-spirit / signal-persona-spirit / owner-signal-persona-spirit repos which retire to maintenance status when the new stack reaches feature parity. New repos are CONTRACT-focused at this stage - schema-driven typed surfaces; daemon implementation comes after the contract surface settles. Per psyche 2026-05-26 explicit authorization following the carry-uncertainty in record 779.] High Zero)
  ```
- `ubgg` [borderline/working-instruction]
  ```
  (ubgg [spirit streaming agents intent] Decision [The first Spirit streaming pilot should explore an agent-facing subscription: an agent calls the Spirit CLI, subscribes to new incoming intent records, and can filter by topic or similar criteria so agents stay up to date with what other agents record in Spirit.] Medium Zero)
  ```
- `ug6i` [borderline/status-snapshot]
  ```
  (ug6i [schema-rust-next regeneration generated-artifacts stale schema-canonical no-ceremony freshness-gate contract] Principle [The generated Rust of a contract must ALWAYS be regenerated from its schema. The schema is the single source of truth and the only step is regenerating the Rust from it — there is no ceremony beyond regeneration. Stale checked-in generated artifacts are a failure: the meta-signal-cloud and meta-signal-lojix artifacts went stale after the schema-rust-next emitter changed and were not regenerated, which masked a real emitter bug and made the cloud and lojix daemons depend on stale generated code rather than current schema output. The per-crate write_or_check freshness gate that asserts checked-in artifacts are byte-identical to generator output must actually run fleet-wide so staleness cannot persist; regenerate from schema on every emitter change.] High Zero)
  ```
- `ujwl` [borderline/impl-detail-belongs-in-report]
  ```
  (ujwl [schema signal-output busy enum-payload] Correction [Busy output should try the compact enum-payload shape where a data-carrying variant carries a reason enum directly, for example Busy carrying a BusyReason enum such as DatabaseOverloaded ResourceDisconnected or OtherBusyReason, instead of immediately wrapping the reason and retry guidance in a larger BusyReport struct.] High Zero)
  ```
- `urpr` [borderline/too-specific-ephemeral]
  ```
  (urpr [cloud] Decision [cloud-designer creates a branched-worktree prototype of the cloud component in parallel to cloud-operator implementation, using mockup-on-worktree method per intent 502-504. Prototype uses old NOTA stack/old signal-channel macro (the approach Spirit runs today), shells out to Cloudflare CLI for DNS get/set name records and (stretch) redirect configuration, invoked through the cloud CLI binary. Parallel-implementation pair with cloud-operator's working-tool effort.] High Zero)
  ```
- `urte` [borderline/working-instruction]
  ```
  (urte [signal] Clarification [The schema-in-NOTA proposal builds on prior workspace work in the now-archived aski-core repository, which was itself evolving into a successor whose name the psyche does not currently recall. Locating aski-core is a prerequisite for grounded design on the schema-in-NOTA thread; the design should not start from a blank slate.] High Zero)
  ```
- `uw10` [borderline/too-specific-ephemeral]
  ```
  (uw10 [spirit] Constraint [Manual downtime handover is acceptable for 0.1 runtime switches] Medium Zero)
  ```
- `uyon` [borderline/too-specific-ephemeral]
  ```
  (uyon [workspace component-shape] Decision [Designer parallel implementation track begins per double-implementation strategy. Focus: the narrower recursion-floor cut from record 746 (all-the-way-back) - actually emit parts of nota-codec from nota.schema instead of accepting the wider hand-authored cut /361 4 settled on. Rationale: this is the part designer is most critical of operator on (operator track defaults to wider cut; record 746 stays aspirational in operator work) and the part designer understands best (schema-emission discipline plus the layered architecture). Design substrate: a design-nota-from-schema repository per skills/double-implementation-strategy.md naming convention. Deliverable: empirical proof of whether the narrower cut is feasible. If yes: changes the foundation. If no: empirically demonstrates the wider cut is necessary - psyche learns either way.] High Zero)
  ```
- `v0xn` [borderline/impl-detail-belongs-in-report]
  ```
  (v0xn [component-shape] Decision [Add Unknown variant to signal_sema::Magnitude; Health/Readiness collapse onto Magnitude using Unknown for indeterminate state] High Zero)
  ```
- `v1ne` [borderline/working-instruction]
  ```
  (v1ne [schema runtime cleanup] Decision [Implement the operator-recommended runtime cleanup direction: keep schema responsible for component meaning, move reusable byte and process-edge mechanics into triad-runtime, and apply the cleanup in the sequence of shared length-prefixed framing, typed component arguments, then thiserror-based runtime errors.] High Zero)
  ```
- `v8ke` [borderline/impl-detail-belongs-in-report]
  ```
  (v8ke [nota] Clarification [If compact bracket strings land, the indented multiline string form likely changes away from triple quotes; the brainstormed delimiter is `[|` to open and `|\]` to close.] Minimum Zero)
  ```
- `va72` [borderline/working-instruction]
  ```
  (va72 [schema nota prototype] Constraint [Implement a prototype schema-driven NOTA parser stack that reads a schema-like declaration, lowers it into a typed assembled form, emits Rust reader code from that assembled form, and proves the generated reader path with tests.] High Zero)
  ```
- `vq9b` [borderline/impl-detail-belongs-in-report]
  ```
  (vq9b [spirit collect-removal-candidates archive default-location backup feedback] Decision [CollectRemovalCandidates archives collected records to a default location derived from where the daemon runs, creating a file there. If it cannot write the default it tries a backup location, and if that also fails it errors. The outcome is reported through properly named result enums — archive created with items, items appended to an existing archive, fell back to backup, failed — never through long string messages.] High Zero)
  ```
- `vuy5` [borderline/too-specific-ephemeral]
  ```
  (vuy5 [schema schema-next nota] Clarification [Draft uncertain idea: schema likely needs a default built-in core schema before the full schema self-description. The core schema is not the full communication schema; it defines the minimal built-in macro positions and shapes needed to read richer schemas. Test this as a concrete artifact: if a node has macro shape, lower it through the built-in macro path; if it already has assembled-schema shape, treat it as assembled schema. This is exploratory, not final.] Medium Zero)
  ```
- `w1t7` [borderline/working-instruction]
  ```
  (w1t7 [persona] Decision [Build a Rust RPC wrapper for headless Pi as a useful baseline even if other Pi integration surfaces are added] Medium Zero)
  ```
- `w3za` [borderline/too-specific-ephemeral]
  ```
  (w3za [component-shape] Clarification [Cloud capability cfg-gating via Cargo features is deferred for v1. Cloud builds with all providers compiled in by default (Cloudflare first, then Hetzner). Intent 283 (provider integrations may be build-time opt-ins) stays as a long-term direction but is not blocking; build-time opt-in does not gate v1 work. Bead primary-kbmi.1.1 (cloud build-time provider opt-in) is therefore parked.] Medium Zero)
  ```
- `wgni` [borderline/impl-detail-belongs-in-report]
  ```
  (wgni [spirit output-target archive-location print-style client-render collect-removal-candidates] Decision [The output target for extraction operations is the location of the archive database, the backup archive sema-database, with the default-then-backup-then-error fallback accepted. It also gains a Print style — Print Stdout and Print Stderr — where the daemon returns the typed NOTA-decodable archive record type and the client converts and prints it. Choosing Print means no archive is written; the agent just receives the records and decides what to do. So the operation either archives to a database location or just prints to a stream.] High Zero)
  ```
- `wm4r` [borderline/working-instruction]
  ```
  (wm4r [spirit production identifiers] Decision [Production Spirit should be fixed now by integrating and deploying the random stable identifier work rather than waiting for the next schema-derived Spirit stack.] Maximum Zero)
  ```
- `wskg` [borderline/too-specific-ephemeral]
  ```
  (wskg [component-shape] Clarification [Meta-signal is the preferred candidate name over owner-signal, but this is a tentative rename direction rather than a completed vocabulary change.] Minimum Zero)
  ```
- `wuvc` [borderline/impl-detail-belongs-in-report]
  ```
  (wuvc [signal-actor validation push-to-nexus on-sent-hookable pilot] Decision [Pattern A refinement: the Signal actor VALIDATES the signal message (and possibly takes pre-processing actions like a check) before PUSHING it to the Nexus engine; PUSHING means sending the typed object through to its next logical destination - the Signal actor pushes to Nexus, Nexus may push to SEMA, SEMA pushes a reply back; the on_sent hookable lifecycle event fires at the Signal-to-Nexus push moment, allowing observers to attach methods on the typed mail event and react (UI, introspection, subscription); implementation target is spirit-next pilot which already has MailLedgerEvent::Sent and ::Processed shapes from operator commit 6afe7280 - extend to full multi-observer fanout subscription (bead primary-lrf8) and add the Signal actor validation step that gates the push; demonstrate the working flow end-to-end in the pilot] High Zero)
  ```
- `xm5s` [borderline/working-instruction]
  ```
  (xm5s [self-editing design-now] Decision [The self-editing system design starts now, not later. Operator implements as far as they can understand it; designer researches how it should look and presents back; operator then presents the implementation showing NOTA-to-object correspondence in detail — proving the design understands how NOTA always corresponds to a specified object all the way down to NOTA-as-schema specifying the objects themselves.] High Zero)
  ```
- `xp0b` [borderline/impl-detail-belongs-in-report]
  ```
  (xp0b [reporting pattern-a] Decision [walkthrough reports use multiple short focused mermaid diagrams of 4 to 7 nodes per record 912 each answering ONE question. real test-code excerpts anchor each diagram. paragraph explains what the diagram and code show. structure walks pattern-a invariants: validate-then-push flow. rejection path. multi-observer fanout. RAII subscription drop. separate channels for on-sent vs on-processed. type-level non-bypassability of SignalAccepted witness.] High Zero)
  ```
- `y0nj` [borderline/too-specific-ephemeral]
  ```
  (y0nj [schema syntax two-parts namespace root struct] Clarification [The plain schema is fundamentally simple - two parts: the NAMESPACE (the type vocabulary, a map of name to definition) and the ROOT struct (the entry-point type; a struct is a vector of fields). The psyche leans namespace-first but is explicitly undecided on ordering. Open design questions the psyche is working out via back-and-forth (NOT yet settled, do not freeze): which delimiter is the struct vs the vector, how field names are derived (positional name-from-type vs explicit name-type pairs), and namespace-first vs root-first. The goal is the SHORTEST unambiguous syntax for data specification, with delimiters carrying structural meaning so no keywords are needed.] Medium Zero)
  ```
- `y6ek` [borderline/status-snapshot]
  ```
  (y6ek [spirit cutover new-spirit production forward-target schema-derived] Decision [The new schema-derived spirit is the forward target and is nearly ready to move to production; new functionality targets it rather than the hand-written production daemon. A heavy formal cutover plan is not wanted — we are in very early territory; move to the new spirit when it is close enough. The designer should assess how close the new spirit is to production use.] High Zero)
  ```
- `yhkm` [borderline/working-instruction]
  ```
  (yhkm [sema-upgrade] Decision [prototype sema-upgrade should first target persona-spirit next-version upgrades] High Zero)
  ```
- `ym3i` [borderline/too-specific-ephemeral]
  ```
  (ym3i [orchestrate] Decision [retired lane identifiers can disappear for now] Medium Zero)
  ```
- `yni3` [borderline/status-snapshot]
  ```
  (yni3 [gas-city criopolis orchestrator cascade bead-orchestration abandoned archival] Decision [Gas City — the bead-based agent-orchestration experiment (Criopolis, the cascade dispatcher, gc sling, mayor) — is abandoned. Everything related to it is archived: the orchestrator daemon, the test-city sandbox, the gascity flake inputs and wiring, and any other Gas-City-dependent repo.] High Zero)
  ```
- `yuzv` [borderline/working-instruction]
  ```
  (yuzv [signal] Decision [Persona signal repositories should be rearranged by socket authority.] High Zero)
  ```
- `yzy4` [borderline/impl-detail-belongs-in-report]
  ```
  (yzy4 [spirit-next signal nexus push lifecycle hooks] Clarification [Pattern A push means sending a typed object to its next logical place in the runtime flow. After the Signal actor validates an incoming signal message and performs any Signal-layer checks, it pushes the object onward to Nexus for processing.] High Zero)
  ```
- `z67n` [borderline/working-instruction]
  ```
  (z67n [cloud prototype implementation] Decision [Implement a fully working prototype, not a paper-only design or partial scaffold.] High Zero)
  ```
- `zj6n` [borderline/working-instruction]
  ```
  (zj6n [workspace component-shape] Decision [Proposed (Medium certainty): create new repositories with the renamed targets (spirit, signal-spirit, core-signal-spirit, possibly others under the persona-prefix-retirement direction of record 765+767) as the home for the new schema-derived stack. Use them in parallel with the existing persona-* repos so production v0.3 stays undisturbed while the new stack matures. Mitigates branch-name complexity (designer-schema-derived-nota-2026-05-26 etc.) which has become unwieldy. Trade-offs to weigh: Cargo dependency rewiring across consumers, CriomOS-home flake input rewiring, history-split (mitigated by cross-reference from new repos to old as history pointer). Authority: system-specialist (repo creation + CI/CD + flake wiring) + operator (Cargo coordination) - psyche authorizes; carry-uncertainty until decision lands.] Medium Zero)
  ```
- `zne3` [borderline/too-specific-ephemeral]
  ```
  (zne3 [component-shape] Clarification [Sema-upgrade dogfoods the version-projection mechanism for its own self-upgrade; sema-upgrade tags v0.1.0 + v0.1.1 will land at commits implementing the full update mechanism] High Zero)
  ```
- `zr26` [borderline/too-specific-ephemeral]
  ```
  (zr26 [persona] Decision [Persona engine lands BEFORE Spirit cutover; persona engine orchestrates component upgrades from day one (bead primary-2y5 becomes blocking for primary-x3ci)] High Zero)
  ```
