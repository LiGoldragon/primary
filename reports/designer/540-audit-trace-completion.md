---
title: 540 — Audit of the operator's trace-path Plane completion + doc fix
role: designer
variant: Audit
date: 2026-06-06
topics: [audit, schema-rust-next, spirit, plane-type, trace, architecture-doc, verification, closure]
description: |
  Adversarial audit of operator report 326 (schema-rust-next 9ca8754 + spirit
  bc28feb), closing designer audit 539. Verdict: FULLY CLOSED and clean. All
  three 539 findings closed, generated output byte-identical (re-proven, not
  trusted), three-tier discipline intact, builds/tests reproduced, docs now
  code-accurate. No new findings. The Plane migration arc (namespaces →
  engine-traits → trace) is complete and verified.
---

# 540 — Audit of the trace-path completion

## Verdict: fully closed, clean

The operator (report 326) completed the trace object-name path onto `Plane`,
fixed both `ARCHITECTURE.md` defects, and removed the tracked zero-byte
`spirit/src/main.rs`. Audited across three dimensions with independent
build/test re-runs and a forced byte-identical re-proof. **Every concrete
finding from audit 539 is genuinely closed; no new findings.**

## Closures verified

- **539 #1 (partial trace migration) — CLOSED.** `TraceObjectNameEnumTokens`
  now carries `plane: Plane` (the `enum_name`/`rendered_prefix` string fields
  are gone; grep-zero); it derives the enum name from `Plane::trace_enum_name()`
  and the actor prefix from `Plane::wrapper_name()`. `TraceSupportTokens`
  carries `Vec<Plane>` and routes its `ObjectName` variants + match arms
  through `Plane`; `emit_trace_support` passes `Plane` to `emit_object_name_enum`,
  not string pairs. The trace path now single-sources from `Plane`.
- **539 #2 (doc defects) — CLOSED.** `ARCHITECTURE.md:112-115` now lists
  signal-frame encode/decode among what `WireContract` does **not** emit
  (the false clause is gone), which is code-accurate (`emit_signal_frame_support`
  is gated on `emits_signal()`, false for `WireContract`). And with the trace
  path actually migrated, the "Plane owns … trace enum names" line is now
  **genuinely true** (`Plane::trace_enum_name()` returns those names).
- **539 #3 (tracked zero-byte main.rs) — CLOSED.** `spirit/src/main.rs` is
  absent from worktree and index; both trees clean.

## Verification

- **Build/test reality (PASS).** Both repos at the exact commits, clean trees.
  `schema-rust-next` `cargo test` → 54, `clippy -D` clean (forced reclippy);
  `spirit` forced recompile → `cargo test --all-features` 71 passed / 9
  nix-ignored, `clippy -D` clean.
- **Byte-identical, re-proven (not trusted).** Commit `9ca8754` touched only
  `ARCHITECTURE.md` + `src/lib.rs` — zero golden fixtures. The auditor forced a
  Spirit regen (`SPIRIT_UPDATE_SCHEMA_ARTIFACTS=1`, cleared build cache) → zero
  diff in `src/schema/`; `spirit` `bc28feb` is lock-files-only. The refactor
  changed no generated byte.
- **Three-tier intact.** `impl Plane` is pure `&self` naming constants;
  grep-zero for `emits_`/`runtime_planes`/`RustEmissionTarget`/`declarations`/
  `root_enums`.
- **Audit hygiene (mine).** After last round's stray scratch test, these
  auditors were instructed to leave the trees untouched and confirm
  cleanliness — both repos verified clean, no artifacts left.

## Notes (not defects)

- The operator's *factoring* differs from the designer branch (`77affc0b`) and
  is equally valid: he reused `Plane::wrapper_name()` for the actor prefix
  (designer used a dedicated `trace_prefix`), and inlined the dispatcher
  variant generation (designer used a `object_name_variant` helper). Same
  output, same single-sourcing.
- One pre-existing minor phrasing imprecision at `ARCHITECTURE.md:192`
  ("Generated signal roots emit … signal-frame") correctly attributes
  signal-frame to *signal roots*, not `WireContract` — accurate, just terse.
  Optional polish, not a defect.

## The arc is complete; the real next work is production

The `Plane` migration the designer prototyped (namespaces → engine-traits →
trace) is now fully integrated, byte-identical, and discipline-clean on main.
The remaining blockers are the production-readiness ones from 324/325/326,
**unchanged and now unobstructed**:

- full `meta-signal-spirit` contract/listener path;
- a Nix-built subscription-specific streaming witness;
- deployed `persona-spirit` → schema-derived Spirit cutover / migration proof.

Per psyche 2026-06-06 ("look at this and come back to me with all this").
