# 558.8 — system — early (effort: large)

# system — migration assessment

*Role: Persona's portable OS / window-manager / focus-observation boundary. Today a **paused**, hand-written socket-daemon skeleton that comes up as a supervised first-stack component, answers supervision status/readiness, and returns typed `SystemRequestUnimplemented` for every domain request. `system` (daemon) and `signal-system` (contract) exist; **`meta-signal-system` does not exist** (confirmed gap).*

## Headline

`system` is one of the **least-migrated** components on the new-architecture rubric, but for an unusual reason: it has nothing of the OLD executor stack to remove (no `signal-executor`, no `Lowering`/`CommandExecutor`/`ObserverSet` anywhere), yet it also has **none** of the NEW emitted machinery. The daemon is a bespoke blocking `UnixListener` spine with no `build.rs`, the contract is still `signal_channel!`, the third triad leg is missing, and triad-runtime is pulled in only for argument parsing. It is "clean but un-generated," not "migrated."

## Axis-by-axis

| Axis | Status | Evidence | Work |
|---|---|---|---|
| daemon-shell-actor | not-started | No `build.rs`; `daemon.rs:165` `for stream in self.listener.incoming()` blocking loop, `runtime.block_on` per connection (`daemon.rs:115`); triad-runtime used only at `daemon_command.rs:4`. | Lower schema, add GenerationDriver/NexusDaemonShape build.rs, emit `src/schema/daemon.rs`, move onto ActorMultiListenerDaemon, reduce bin to `run_to_exit_code()`. |
| execution-nexus | not-started | Hand-written `match` in `daemon.rs:391-409` (`SystemRequestHandler::reply_for_request`); no NexusWork/NexusAction/Runner anywhere. | Define ops in schema so schema-rust-next emits the nexus runner; route via `Runner::drive`. (No old executor to remove — system isn't one of the four.) |
| contracts-schema-next | not-started | `signal-system/src/lib.rs:34` `use signal_frame::signal_channel;`, `lib.rs:416` `signal_channel! { channel System {...} }`; no schema-next dep. `.concept.schema` files are bare unused stubs. | Author a real `system.schema`, lower+emit, add schema-(rust-)next dep, delete `signal_channel!`. |
| contract-shape-two | partial | `signal-system` present; `meta-signal-system` → "No such file or directory". signal-engine-management (`Cargo.toml:29`) is the shared supervision contract, not a system-specific third leg. | Create `meta-signal-system` (focus/privileged-action policy) for the exact two-contract shape. |
| naming-bare | done | Bare `system` repo/crate/bins; `flake.nix` has no persona-* inputs (only the prose "for Persona."); no persona-* Cargo deps. | None. |
| storage-sema | n-a | `ARCHITECTURE.md:9-12` "System has no durable store today"; no sema-engine/redb in deps or src. | None now; open `system.sema` via sema-engine only if subscription/backend cursors become durable on unpause. |
| kameo-discipline | hybrid | Real ask-based actors `SystemSupervisor` (`daemon.rs:282`), `SupervisionPhase` (`supervision.rs:90`), `FocusTracker` (`niri_focus.rs:87`). But no `RestartPolicy` (grep empty); supervision listener leaks a thread (`supervision.rs:80` `std::thread::spawn` → `_thread: JoinHandle`); actors driven under per-connection `block_on`. | Declare RestartPolicy; replace the leaked thread with a supervised task; drive on a shared async runtime. (No redb, so the inline-redb hazard is moot.) |
| nota-binary-boundary | done | `daemon_command.rs:36-45` accepts only `SignalFile`, rejects `InlineNota`/`NotaFile`; decodes rkyv at `daemon_command.rs:72`. INTENT.md:18-21 matches. | Optionally drop the legacy `from_socket`/`PERSONA_SOCKET_MODE` env path; the primary boundary already passes. |
| intent-arch-docs | hybrid | Docs honest about the paused skeleton, no Kameo/persona-* overclaims. But `ARCHITECTURE.md:94-95` cites `signal_channel!` at `lib.rs:303-330` — actual location is `lib.rs:416-441` (stale). | Fix the line ref; add schema-next + meta-signal-system + actor-native-substrate migration notes. |

## Migration path for system

Because `system` is paused (INTENT.md:13-14, awaiting a real focus consumer), the sequencing is not "rip out the old executor" — there is none — it is "stand up the new generation pipeline once." Order: (1) write a real `system.schema` and lower it through schema-next; (2) add `schema-(rust-)next` to `signal-system`, emit `src/schema/`, and delete the `signal_channel!` block, which simultaneously satisfies contracts-schema-next and unlocks the emitted nexus runner; (3) add a `build.rs` with GenerationDriver + NexusDaemonShape to `system`, emit `src/schema/daemon.rs`, and replace the hand-written `SystemDaemon`/`BoundSystemDaemon`/`SystemRequestHandler` trio with the emitted actor-native shell on triad-runtime's `ActorMultiListenerDaemon` (collapsing daemon-shell-actor and execution-nexus together); (4) create the missing `meta-signal-system` contract to complete the two-contract shape; (5) clean up kameo discipline (RestartPolicy, the leaked supervision thread, per-connection `block_on`) as part of moving onto the shared async substrate; (6) refresh the docs' stale `signal_channel!` reference and migration notes. The binary-NOTA boundary (done) and bare naming (done) carry forward unchanged. Effort is **large** but low-risk — nothing in production depends on system's internals, and the paused domain surface means the work is mostly scaffolding, not behavior preservation.
