# Handover — fresh session: capture intent first-degree, clear the designer pile

You are the **designer lane** on the `primary` coordination repo. This is
the prompt a fresh agent receives: an information map of what accumulated,
where the evidence lives, and what is still open. Keep your own judgment —
where the psyche gave an exact instruction it is quoted as instruction, not
turned into inference.

## The goal (psyche's words, verbatim)

A designer-lane context-maintenance that the psyche re-grounded into a
first-degree intent exercise:

> "the idea should either become intent, or a work assignment, or
> abandoned ... so we go through each idea and decide"

> "reports are generated by agents, and the source of intent should be
> first-degree, with an intent-alignment to establish certainty"

> "I want to clear out all the reports now, and have a very clean, lean,
> relevant small quantity at any time"

Working purpose (an agent's one-sentence framing the psyche affirmed *"good
for now"* — NOT captured intent, do not treat as durable): *an
agent-operated system where typed schemas generate the runtime, an intent
layer (Spirit) governs what agents build, and authenticated components
(criome / mentci / router / mirror) carry it across machines.*

## The correction that shaped this session

A prior pass mined the 129 agent-written reports for "intent candidates"
and asked the psyche to ratify them. That is backwards: `skills/intent-log.md`
records **only psyche statements, never agent-written files**. Intent is
first-degree. The reports are a *thread-map*, not an intent source. Do not
repeat the laundering.

## What is DONE — the work fate is executed

129 reports → 312 raw ideas → 195 distinct → 70 genuine work items, deduped
against all 166 live beads, adversarially re-checked (3 dropped as dups, 4
narrowed to deltas), woven into **9 epics + 67 beads**, all tagged
`designer-weave`. Reversible as one unit by that label.

- View: `bd list --labels designer-weave` · `bd epic status` · `bd dep tree <epic>`
- Epics: schema `primary-o7j2` · criome `primary-om4g` · mentci
  `primary-iy51` · intent-spirit `primary-c4dz` · guardian `primary-nlx5`
  · orchestrate `primary-fzwd` · signal-router `primary-2wj8` · trace-e2e
  `primary-4ddb` · mirror `primary-9ppu`. Standalone: `primary-o2lr`
  (visuals-as-data), `primary-pl60` (unify actor runtime), `primary-8jzu`
  (cloud cost-cap).
- `bd dep cycles` is clean. The weave relate-links into the existing
  backlog (34 links).

## What is OPEN — your work

1. **Capture two psyche principles first-degree.** Both stated this
   session, both passed the Spirit gate, neither yet recorded:
   - *"the source of intent should be first-degree, with an
     intent-alignment to establish certainty"* (Correction/Principle).
   - *"a very clean, lean, relevant small quantity [of reports] at any
     time"* (Principle/Decision on report hygiene; candidate to fold into
     `skills/context-maintenance.md`).
   Before writing: reload `skills/intent-log.md` + `skills/spirit-cli.md`,
   then **query the domain first** — records like [intent is primordial]
   and [inferring intent is forbidden] likely already exist at high
   magnitude, so these may be a `Clarify`/refinement of an existing record
   rather than new `Record`s. Use affirmative framing (the guardian remands
   `NegativeGuideline`). Manifest into the affected `INTENT.md`.

2. **Clear the report pile to zero** — psyche instruction: *"clear out all
   the reports now."* The work is preserved in the weave, so
   `reports/designer/` (129 entries, numbered 528–729, plus this `730-`
   dir once its job is done) can be `rm`'d; git preserves history. Keep
   nothing by default — durable substance lives in `INTENT.md` / Spirit /
   skills / beads.

3. **The first-degree intent interview (the psyche's call).** The psyche
   wanted intent re-established first-degree via intent-alignment from
   basic principles — one plain-prose question per turn, never the
   structured UI (`skills/intent-alignment.md`). The working purpose is
   affirmed "for now." Decide with the psyche whether to run the full
   interview or whether the purpose + the two principles suffice before
   clearing. The 11 threads (interview scaffold) and opening question spine
   are in this dir's history; the live thread-map is the ledger.

## Current state / locators

- **Working copy: UNCOMMITTED.** New files under
  `reports/designer/730-idea-disposition-2026-06-24/` (`0-frame-and-method.md`,
  `1-idea-ledger.md`, this file). The weave lives in the `bd` store (its own
  substrate), not the working copy.
- Reports to clear: `ls reports/designer/` (129 + this `730-` dir).
- Disposition ledger (thread-map / work checklist — reference only, NOT an
  intent source): `reports/designer/730-idea-disposition-2026-06-24/1-idea-ledger.md`.
- Method frame: `0-frame-and-method.md` (same dir).
- Beads: `bd list --labels designer-weave` (the weave); `bd list` (ready
  slice); 166 active total. Related epic `primary-ptvb` carries an in-flight
  intent to rename the beads system to **Mind/memory/weave** vocabulary.
- Skills: `intent-log.md`, `spirit-cli.md`, `human-interaction.md`,
  `intent-alignment.md`, `context-maintenance.md`, `reporting.md`.

## Forks to settle with the psyche (don't pre-decide)

- **Clear now, or interview first?** Psyche said "now"; a reasonable order
  is: capture the two principles → clear the pile → then offer the
  interview. The psyche chooses.
- **Commit/push to main?** Primary commits straight to main, but the psyche
  has not authorized committing the new `730-` dir or the mass report
  deletion. Ask before committing — especially the deletion.
- **Capture the two principles standalone now, or as the interview's first
  act?** Either works; ask if the psyche wants the interview at all first.
