# Recent Spirit intent for pi-operator

## Source and cut

Queried the unversioned production `spirit` CLI with `Observe Records` and provenance. The log currently runs through record 811 (`2026-05-26 12:36:39`). This report is Spirit-intent only; sibling reports in this meta-report directory should cover operator/designer/nota-designer reports and branch state.

Focus window: records 662-811, especially the 2026-05-26 schema/NOTA/Spirit restack records 715-811.

## Latest durable intent

| Records | Topics | Certainty | Summary | Implication for pi-operator |
|---|---|---|---|---|
| 727, 728; also 700, 701, 691, 673-678 | `spirit`, `workspace`, `nota` | Maximum | Normal Spirit capture/observation uses the unsuffixed `spirit` production binary. Inline NOTA CLI arguments are wrapped in shell double quotes; NOTA strings themselves use bracket forms. Spirit records are dense clarified descriptions, daemon-stamped, multi-topic, with terse acknowledgements. | Use `spirit`, not versioned wrappers, for intent work. Do not revive legacy files. Keep report summaries dense and do not copy huge verbatim prompt blocks into intent-like summaries. |
| 690, 698, 703-705 | `nota` | Maximum | NOTA strings come exclusively from bracket forms: `[text]` and `[|text|]`; quotation marks are not NOTA string syntax and legacy quoted-string acceptance is authorized for removal after emitters migrate. Bracket-only NOTA embeds cleanly inside double-quoted host strings. | Any schema, signal, CLI, test, or example authored by pi-operator should use bracket strings and shell-level double quotes only around the whole NOTA object. Audit generated examples for legacy quotes. |
| 715-724, 737-738, 751, 753, 759-764 | `schema`, `nota`, `signal`, `component-shape` | Mostly Maximum; 718/726/760/763/764 High | Schema parsing is NOTA-first and pass-based: generic delimiter/object parsing, then schema macro/lowering passes to a fully resolved `AssembledSchema`. Macro recognition is structural over delimiter, arity, identifier case, and position. End state is a NOTA-representable assembled schema with no macros left. Headers derive from assembled data structure, not special authored header declarations. | Implement from the structural pipeline, not from ad-hoc text parsing or old macro bodies. Tests should prove object-block parsing, macro lowering, and assembled-schema output, not just string snapshots. |
| 637-643 | `schema` | Maximum | Schema-derived Rust emission is a fresh top-down composer from fully typed `AssembledSchema`; it must not delegate to or invoke the legacy `signal_channel!` macro. The proc macro name is `emit_schema!`; old text-body signal macro material is fenced as `legacy_signal_channel!` during migration. | If implementing schema Rust emission, start from `emit_schema!` and direct Rust composition. Treat reuse of `signal_channel!` as architectural drift to remove, not a shortcut. |
| 770-776, 783-803 | `nota`, `schema`, `parser`, `macro`, `symbol` | Mostly Maximum; 771/773/775/788 High | NOTA is the thin structural library: object blocks with delimiter kind, source spans, object counts, recursive child shape, and `qualifies_as_*` predicates. NOTA does not decide final semantics such as PascalCase validity; schema/macro context does. Prefer qualified-symbol classification first, then demote to string when type context requires. Blocks reassemble sequentially. | Start NotaCore/nota-next work at the object-block API. Names like `is_symbol` are suspect; prefer `qualifies_as_symbol`. Preserve source spans for diagnostics. Keep final casing/type legality in schema/macro layers. |
| 785, 790-807 | `schema`, `schema language`, `schema core macro` | Maximum except 806 Medium | A `.schema` file is read under an implicit schema-schema: root `Schema` struct is implied by the file extension, not explicitly wrapped. The root is positional; current leaning is imports/exports namespace first, then input/output struct(s). The schema-schema is core Rust bootstrap code and macro interface. Record 806 keeps root field ordering as an open clarification, with imports-first used pending decision. | Do not hard-code a final root-field-order decision beyond the current imports-first lean. Make the bootstrap/core schema boundary explicit and testable. |
| 668-670, 692-696, 714, 720, 725, 739 | `schema`, `component-shape`, `signal`, `sema`, `spirit` | Maximum | One channel/contract gets one schema. Components can have multiple schemas: external wire schemas in `signal-*`, storage schemas in the daemon repo, and internal actor-message schemas in the daemon repo. Vocabulary: schema = specification, signal = movement/transport, sema = body/state at rest. rkyv is the single binary representation for both wire and database; NOTA is the text projection. Signal-frame itself is schema-described. Current implementation keeps ordinary/owner sockets separate; permission variants may later live inside signal data. | When working Spirit/schema, do not assume one schema per repo. Keep wire/storage/internal contracts separate. For current Spirit compatibility, keep separate ordinary and owner sockets unless a later implementation task explicitly moves permission into data. |
| 666, 667, 730-736, 757, 758 | `component-shape`, `schema`, `workspace` | Maximum | Important corrections: the special `InteractTrait`/`InteractionActor` abstraction is retracted; ordinary actor methods are interactions. Schema defines data types only, not effects, fan-out targets, effect tables, or user-visible `Features`. ASKI, not ASCII, is the prior language lineage. Agents must not convert agent-authored inference into psyche-authorized intent; suspected hallucinated/duplicate records are flagged for psyche review, not deleted unilaterally. | Avoid reviving the retracted interact-trait or schema-effects designs. Keep runtime logic in Rust methods/actors, not schema. Treat old reports through these supersessions before implementing from them. |
| 765-769, 780-782, 809-811; also 778-779 | `spirit`, `naming`, `component-shape`, `repository`, `workflow`, `schema`, `nota` | 780/782/811 Maximum; 765-769/781/809-810 High; 779 Medium | Persona-prefix retirement: create new repos `spirit`, `signal-spirit`, and `core-signal-spirit`; old persona-named repos retire to maintenance. The resolved assembled schema artifact is `Asschema` / AS-schema. Nota work goes on a `nota-next` branch in the existing nota repo. The schema repo keeps its current name. For major architectural breaks, a separate `-next`/`-v2` prototype repo is an accepted method, and recent schema/NotaCore prototypes should be audited with newer prototypes weighted more heavily. | Do not start new schema-derived Spirit work in the old persona-prefixed repos unless explicitly maintaining production. For NOTA, expect `nota-next` branch work, not a new repo. Before implementing a final restack, read the recent prototypes and distill best paths/bad paths. |
| 729, 777, 798, 808 | `workspace`, `operator workflow` | 729/777/808 Maximum; 798 High | Rust functions are methods/associated functions except `fn main` and `#[cfg(test)]` test modules. Load-bearing intent should become constraint tests. Large schema implementation/synthesis tasks may use subagents when explicitly authorized, with the main operator responsible for integration, constraints, tests, and final report. Agents need concrete `jj` inline-command discipline because editor-opening `jj` remains a failure mode. | For this task, no subagents because the prompt forbids them. For later implementation, write tests that pin intent, put functions on impls, and never run a description-taking `jj` command without `-m`/inline message. |

## Open uncertainty and risk

- Record 806 is the main open schema-language uncertainty: root schema field order is not settled. Imports/exports first is the current lean, not final psyche lock.
- Records 720/725 and 739 should be read together: separate ordinary/owner sockets are the current implementation target; signal-level permission variants are a possible future generalization.
- Records 666 and 730 supersede large pieces of prior schema-crystallized architecture. Any report before those corrections needs review before implementation.
- Record 810 says the schema/NotaCore restack should be audited across recent prototypes before turning into an operator implementation target.

## Bottom line for pi-operator

The current durable direction is: implement the schema/NOTA restack from a delimiter-object NOTA core, lower through schema macros to `AssembledSchema`, emit structural Rust from schema while keeping handwritten Rust to engine logic, and keep Spirit/Signal/Sema contracts split by channel. Treat old interact-trait, schema-effects, old signal macro reuse, quoted NOTA strings, and persona-prefixed new-development repos as stale or wrong unless a maintenance task explicitly says otherwise.
