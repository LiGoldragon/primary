# Spirit over-capture removal tombstone

Date: 2026-06-07
Role: pi-operator

## Removed record

Identifier: `6xfz`

Observed before removal:

```nota
(RecordProvenancesObserved [(([6xfz] [lojix example component] Correction [Do not frame lojix as an example component or as an exemplar component stack for cloud. Lojix is not currently one of the example components, and cloud context should not inherit example-component framing from it.] High Zero) 2026-06-07 09:32:12)])
```

## Reason

The psyche identified this as an over-captured information statement, not durable intent. The statement that a component is not something right now is current information, not by itself a design intent.

## Removal

Removed with:

```nota
(Remove [6xfz])
```

Spirit replied:

```nota
(RecordRemoved [6xfz])
```
