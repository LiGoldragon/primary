# 123 — Handover, 2026-05-15

*System-specialist + operator session, evening pass. Closing
handover before context compaction. Substance lives in the
appropriate permanent homes (reports 119, 121, the commits and
branches named below); this file just names the pickup points.*

## What landed in this session

**Production sops-nix Wi-Fi secret deployment (CriomOS main).**
Phased dual-radio approach. Phase 1 spun up an independent
hostapd-sops-test.service on the secondary USB Wi-Fi adapter with
the password sourced from /run/secrets/, end-to-end verified
(joined SSID, DHCP, NAT, internet); primary `criome` was kept
untouched on a build-time-embedded password file during the test.
Phase 2 swapped primary to the same sops-loaded path, removed the
test radio module, made the new generation persistent
(`switch-to-configuration boot` on the live Test-active state, no
service restarts in that step). CriomOS main fast-forwarded.
Detail in `reports/system-specialist/121-sops-nix-wifi-secret-integration.md`
§"Production Deployment, 2026-05-15".

**Refactor branch sops infrastructure (CriomOS horizon-re-engineering).**
WIP commit pushed bringing the production sops shape to the
refactor branch: sops-nix flake input, the empty `secrets` stub,
modules/nixos/secrets.nix, criomos.nix import, and the
nordvpn.nix consumer wiring (replaces the shadow operator-seeded
`/etc/nordvpn/privateKey` path with `sops.secrets.<name>` from
the NordvpnProfile.credentials SecretReference). **Not yet
`nix flake check`-tested.** Worktree at
`/home/li/wt/github.com/LiGoldragon/CriomOS/horizon-re-engineering`,
top commit subject "modules/nixos: port production sops
infrastructure to refactor branch + wire NordvpnProfile.credentials".

**Reports updated.**
- `reports/system-specialist/121` — production sops deployment,
  the two operator gotchas (broken `Requires=sops-nix.service`,
  asymmetric-routing verification trap and the
  `nmcli +ipv4.routes "<dst>/32"` workaround), kea stale-socket
  side-note, refactor implications.
- `reports/system-specialist/119` — refactor schema-step status
  as of today (done: 1, 4, 6, 6b, 7a, 7b, 8, 11, 14; open: 3, 5,
  9, 10, 12, 13, 15), and the unblock note for the
  SecretReference throws.

## What's open

**Refactor CriomOS sops verification.** The pushed WIP commit
needs an end-to-end check before it can be considered safe to
build for prometheus from the refactor branch. Plan:

- Run `nix flake check` on
  `~/wt/github.com/LiGoldragon/CriomOS-test-cluster/horizon-re-engineering`
  with the criomos input bumped to the new tip. The fieldlab
  fixture has empty `vpn_profiles` so the nordvpn module is
  inert there — the check should pass without a sops file.
- For a goldragon-prometheus build via lojix-cli, a
  `nordvpn-credentials` sops file (or whatever name the
  NordvpnProfile.credentials SecretReference holds) must exist
  in `goldragon/secrets/` — it does not today, only the wifi
  one does. Production goldragon does not currently exercise
  nordvpn so this gap doesn't bite production.

**pi-models.nix `AiProvider.api_key` throw.** The CriomOS-home
side still throws when a provider declares an api_key. The
chataigne pi config takes the apiKey as a STRING, not a path —
sops-style file integration needs either a chataigne change to
accept apiKeyFile, or a `sops.templates.<name>` mechanism to
render the chataigne config with the key inlined at activation.
Today's local providers all have api_key=None so the throw
doesn't fire. Defer to when a cloud provider is actually wired.

**llm.nix already uses an apiKeyFile**. Looking at the refactor
branch's llm.nix, it consults a fixed `${runtimeHome}/api-key`
path; if the file is empty the api-key flag is omitted. That's
fine as-is — when llama.cpp's router-side credential matters,
swap that path for `config.sops.secrets.<name>.path`.

**Remaining horizon-re-engineering schema steps.** Per report
119 §"Refactor Status, 2026-05-15": 3 (criomeDomainName /
cluster-domain), 5 (Wi-Fi typed records — note the choice
between `cluster.wifi` vs the production-main shape that puts
the password as SecretReference on RouterInterfaces), 9
(NodePlacement), 10 (Machine data-bearing variants), 12
(ProjectedNodeView typed shape), 13 (Ghost-as-Publication), 15
(source-constraint tests).

**kea-dhcp4-server stale-socket bug.** Pre-existing, surfaced
during sops debugging. Across activations where kea's unit file
doesn't change, the running daemon stays bound to its
unicast UDP socket on the bridge IP only. Fresh DHCPDISCOVER
broadcasts are silently ignored; renewals (DHCPREQUEST unicast
to server IP) keep working. Restart fixes it. Independent of
the schema work. Suggested fix: add the kea config file to the
unit's `restartTriggers` so any change forces a restart.

**lojix daemon implementation.** Per the 24 constraints in
`lojix/horizon-re-engineering/ARCHITECTURE.md`. Cutover from
lojix-cli to the lojix daemon is constraint C24; current
production uses lojix-cli (see commits and request shape in
`lojix-cli/src/request.rs`).

## Side notes

- **No-hashes preference.** The user wants commit hashes, store
  paths, and content-addressed identifiers kept out of chat
  output. Reference by branch + commit subject when needed.
- **Wired connection on this workstation is load-bearing.**
  Don't bring it down without permission — other agents on it
  need it. The `nmcli +ipv4.routes "<dst>/32 <wifi-gw>"` +
  `nmcli device reapply <iface>` recipe is the polkit-allowed
  per-destination route override that lets you test wifi-only
  paths without disturbing wired.
- **systemd-run for prometheus activations.** When activating
  on prometheus over an SSH path that depends on Wi-Fi (e.g.
  if hostapd will restart),
  `systemd-run --collect --unit=<name> --no-block <profile>/bin/switch-to-configuration <action>`
  detaches the activation from the SSH session so it survives
  the disconnect. Then poll `systemctl status <name>` after
  reconnect.

## Next-session targets

In rough order of unblocking power:

1. `nix flake check` the WIP refactor CriomOS sops commit
   against the test cluster. If it passes, the refactor branch
   is safe to build/deploy from once the nordvpn-credentials
   sops file is authored (only needed if a node opts into
   nordvpn).
2. Step 5 (Wi-Fi typed records) on the refactor branch. The
   simpler shape — putting the password as a SecretReference
   on RouterInterfaces, leaving SSID/country/regulatory as
   typed fields on the same record or on a `cluster.wifi`
   record — already exists in production main; mirror it on
   the refactor side.
3. Step 3 (criomeDomainName / cluster-domain policy).
4. Steps 9, 10, 12, 13, 15 in some order.
5. kea restartTriggers fix as a separate small commit on
   CriomOS main.
6. Lojix daemon implementation (separate arc).
