## Recovery diagnosis

1. **Failed command/root error**
   - `cd /tmp/spirit-luna-witness && cargo build`
   - Cargo resolved `nota` at `7dc0ab7e…` and `schema-language` at `6aae825d…`, then failed compiling `schema-language`:
     - unresolved import `nota::AtomClassification`
     - missing `Delimiter::PipeBrace` / `PipeParenthesis`
     - missing `Block::is_pipe_brace`
     - final: `error: could not compile schema-language (lib) due to 49 previous errors`
     - `Command exited with code 101`
   - Earlier attempt `cargo build --locked` also failed because the temporary witness had no lockfile.

2. **Phase reached**
   - Temporary Luna component-witness dependency resolution and compilation.
   - Failure occurred before executing the Luna witness.

3. **Completed/not completed**
   - Backup: **completed** — size `987136`, SHA-256 metadata recorded.
   - Logical marker: **completed** — `(974, 7784350440604474991)`.
   - Auth status: **completed earlier** via non-secret `codex login status`; no secret material inspected.
   - Luna call: **not reached**.
   - Deployment/activation: **not reached**.
   - Terra call: **not reached**.
   - No Spirit records were inspected or created.

4. **Current status**
   - `spirit-judge.service`: **inactive/dead**
   - `spirit-daemon.service`: **inactive/dead**
   - Lojix query shows latest persisted Current generation **81**, `/nix/store/7x834m8kdi6zm287a8fwkwaj8cg23z56-home-manager-generation`, pinned to CriomOS revision `43d9234f…`; approved cutover generation was never activated.

5. **Repository/pin changes**
   - No evidence of repository or approved pin changes after source approval.
   - The temporary witness generated/fetched dependency state under `/tmp`; approved `spirit-judge` lock assertions still matched the approved pinned revisions.
   - This diagnosis made no changes.

6. **Safest next action**
   - **Source remediation required, not transient retry.**
   - Align the temporary Luna witness dependency graph with the approved `nota`/`schema-language`/consumer revisions and produce a reproducible lockfile before any retry or activation. Do not retry the same build as a transient fetch failure.