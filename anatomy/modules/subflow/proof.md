Kind: proof. Build the smallest thing that proves the contract works, with
real fixture files. The expected value comes from outside the code under
test. See the new test fail once before trusting it.
