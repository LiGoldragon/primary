# Stream as fourth kind; synthetic naming later; MVP first

> I think we make stream a forest kind and we could even... Yeah.
> Yeah. Eventually, I mean, not now, we could potentially write a
> transformer that also creates the required input objects to initiate
> and end the stream, although it's not necessary for now. And it
> would also mean that we have transformers that can name things,
> obviously synthetically create names, so that if the stream is
> called observer, then it would create an object called observer
> stream initiation, and then another object called observer stream
> termination, or something like that. But yeah, for now we could just
> create, write it all by hand and wire it up in the implementation.
> I'm more interested in getting the syntax right, getting the
> concepts right, and getting to minimum viable product.

— psyche, 2026-08-06T18:01:48Z (Designer session 5abf3be8; entry
captured 2026-08-08 from the session transcript during the
rulings-audit backfill; "forest kind" is dictation for "fourth kind")

Context, kept apart from the quote: origin of the stream-as-fourth-
kind ruling that streamSection.md (2026-08-07) later refines to a
section inside the object. Transformers that synthetically create and
name stream initiation/termination objects are envisioned but
deferred; hand-authored for now. Priority order stated: syntax,
concepts, minimum viable product.
