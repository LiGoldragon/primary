# All matches all — ScopeOf root target semantics — 2026-07-31

Context: in the management session, codex asked whether the root target
`All` in authored `ScopeOf` should match only its explicit node
(identity-exact — codex's stated lean, requiring recursive `Invoke` to
perform traversal), or act as a wildcard matching the entire rooted type
tree.

Agent text answered: codex's question, as characterized by the psyche in the
management session; this session did not locate codex's original written
text, only the psyche's paraphrase of it.

Psyche ruling (verbatim): "All should, as the name implies, match all."

Reading: `All` is a whole-tree wildcard — it matches every node in the
rooted type tree, not only its own explicit node. This is a genuine psyche
ruling, not a lean; grade: psyche-ruled.

Disposition of prior readings:

- Rejects codex's identity-exact lean for `All`.
- Reverses the related overnight lean from `reports/NomosTrainAddendum-2026-07-30.md`,
  Decision 1 ("All-variant matching only itself"), which the 2026-07-30
  audit had flagged as a behavioral change from legacy and marked for
  psyche review.
- Restores legacy semantics: `All` matches the whole tree, as before.

Accompanying direction (context, not yet a ruling): the psyche also directed
that `ScopeOf`'s domain logic is to be the worked subject for designing the
authoring stack — first look at the trait-based Rust the transformer must
generate, then the minimal-information Ethos syntax, then design the Nomos
transformer between them.

This is forward-only provenance: it does not edit
`reports/NomosTrainAddendum-2026-07-30.md`, which stands as a record of what
was leaned overnight. This entry records the psyche's ruling that supersedes
Decision 1's All-variant reading, given 2026-07-31 in the management session.
