# -next Residue Sweep — complete per-repo ledger (authoritative, honest)

Session: RenamePropagator. Role: General Code Implementer (Claude Opus 4.8, 1M).
Date: 2026-07-04. Scope: the AUTHORITATIVE whole-graph `-next`→canonical residue
sweep across the 86-repo run config, and de-`next`ing repos with live residue.
**GATE HELD: no migration `main` touched; all edits on `drop-next` only; every
edited repo's `main@origin` verified unchanged.**

Producer canonical tips observed live this session: `nota@main bea7e284` (no
drop-next); `schema@drop-next a393c8c8`; `schema-rust@drop-next` **advanced
`7f746c02 → 982fe099` mid-session** (concurrent producer movement — see §Root
cause). `nota` derive crate at bea7e284 publishes `nota-derive` (NOT
`nota-next-derive`).

## CRITICAL METHOD CORRECTION — local refs are unreliable; only `ls-remote` is authoritative

The first pass grepped `refs/remotes/origin/drop-next` in each canonical
checkout. **Those local remote-tracking refs are STALE/DIVERGED for many repos.**
`git ls-remote` proved the true pushed state differs: e.g. local said persona
drop-next `8587b75`, agent `f51c8cb`, triad-runtime `2afd7f3`; the actual GitHub
tips are persona `f2149c0`, agent `55d96af`, triad-runtime `f9a6a7c`. Several
workers made LOCAL-ONLY rename re-runs (author `rename@criome.net`, e.g. persona
`8587b752`) that were never pushed, and jj's `@origin` tracking diverged. **Every
residue judgment below is against the `git ls-remote` tip** (recorded in
`scratchpad/authoritative-tips.txt`, all 86 objects present locally). This is the
same stale-read trap that produced the earlier false "persona f2149c0 still pins
nota-next" claim (see §persona). A "0 residue" claim read from a local jj bookmark
is worthless here.

## LIVE CONCURRENT CASCADE — the graph moved during the sweep

A synchronizer staged-cascade is running concurrently. During the session
`meta-signal-repository-ledger` origin/drop-next advanced `12bc96b → 6ffe7720`
("synchronizer: cascade dependency bumps", `rename@criome.net`, 20:04) and
`schema-rust` drop-next advanced `7f746c02 → 982fe099`; `system` drop-next
advanced `f112d19a → 647f21e9`. The sweep is therefore a snapshot of a MOVING
target. Key finding: **the cascade's incremental `cargo update -p <producer>`
bumps do NOT clear the stale `nota-next-derive` lock entry** — `6ffe7720` still
carried it, and the LATER `system` cascade tip `647f21e9` (a fresh "synchronizer:
cascade dependency bumps" pushed mid-session) STILL carries both
`nota-next-derive` AND the stale `@generated by schema-rust-next` artifact. Only a
full `cargo generate-lockfile` clears the lock (proven below); only a build.rs
regen clears the artifact. This is definitive: the running cascade cannot converge
these repos.

## Complete per-repo residue ledger (86 repos = 3 producers + 83 swept)

| category | count | meaning |
|---|---|---|
| CLEAN (0 family tokens) | 37 | no `nota-next`/`schema-next`/`schema-rust-next` anywhere |
| AUTHORIZED-LITERAL-ONLY | 40 | family tokens present but ALL authorized (0 build-critical toml/lock/flake/use-path) |
| FIXED THIS SWEEP | 3 | live lock residue cleared + pushed drop-next |
| LIVE-RESIDUE, CASCADE-BLOCKED | 2 | live lock residue that a single-repo fix cannot clear |
| STALE-ARTIFACT-ONLY | 2 | `@generated by schema-rust-next` header only (regen item, not dep/URL/lock residue) |
| KNOWN-PENDING (excluded) | 2 | `mind`, `spirit` — live residue, not touched |

Per-repo classification file: `scratchpad/final-class.txt`. The non-clean/
non-authorized repos:

| repo | authoritative drop-next | disposition |
|---|---|---|
| signal-repository-ledger | `d63228c → edabdd4a` | **FIXED** (nota-only re-lock) |
| meta-signal-repository-ledger | `6ffe7720 → d6e472df` | **FIXED** (full lock regen on top of live cascade tip) |
| repository-ledger | `502d77e → 54311fdc` | **FIXED** (repin 5 producers→drop-next + regen lock + regen artifact + store.rs assertion) |
| system | `f112d19a` | **LIVE RESIDUE, BLOCKED** — lock `nota-next-derive` + stale artifact |
| meta-signal-spirit | `b05464fd` | **LIVE RESIDUE, BLOCKED** — lock `nota-next.git`/`schema-next.git` (transitive) |
| persona | `f2149c0` | STALE-ARTIFACT-ONLY (+ transitive flake.lock); Cargo.toml/lock ARE canonical |
| signal-terminal | `4f75a6c` | STALE-ARTIFACT-ONLY; lock/deps clean |
| mind | `292b11c` (no drop-next) | KNOWN-PENDING — 8 toml deps, 27 lock, live `use nota_next` across src |
| spirit | `a082c5e` (no drop-next) | KNOWN-PENDING — 3 toml deps, 5 lock |

## persona — the flagged "3rd silently-skipped repo" is a FALSE ALARM

The dispatch premise (from `Tier2-Regen-SchemaDrift-Evidence`: "persona drop-next
`f2149c0` still pins `nota-next` + `schema-rust-next@main` cad9ec27") is FALSE —
it was a stale-ref misread (same trap as above). persona's ACTUAL `f2149c0` tree:

- `Cargo.toml`: fully migrated — `nota@main`, `schema@drop-next`,
  `schema-rust@drop-next`, all `signal-*` at `drop-next`. **0 family residue.**
- `Cargo.lock`: canonical — `nota.git@main#bea7e284`,
  `schema.git@drop-next#a393c8c8`, `schema-rust.git@drop-next#7f746c02`. **0
  `nota-next`.**

persona's only residue: (1) `src/schema/daemon.rs` header `@generated by
schema-rust-next` — a STALE artifact (the current emitter writes `@generated by
schema-rust`, verified on domain-criome/cloud), regenerable against
`schema-rust@drop-next`; (2) `flake.lock` nodes `nota-next-source` /
`schema-next-source` / `schema-rust-next-source` — **TRANSITIVE** (persona's own
`flake.nix` has zero `-next`; the nodes are inherited from a dependency flake).
persona needs no dep re-lock. `main@origin ac629103` unchanged (untouched).

## FIXED repos — evidence

All three: fresh change on the authoritative origin `drop-next` tip, verify 0
build-critical residue by grep, `jj bookmark set drop-next -r @`,
`jj git push --bookmark drop-next`; `main@origin` verified unchanged.

- **signal-repository-ledger** `d63228c → edabdd4a`: repin `signal-frame`
  `main→drop-next` (44d22a07, de-nexted), keep `nota@main`; full
  `cargo generate-lockfile`. Lock: `nota-derive` (was `nota-next-derive`),
  `signal-frame@drop-next#44d22a07`, `nota@main#bea7e284`. 0 residue. `main@origin
  f3a5150e` unchanged.
- **meta-signal-repository-ledger** `6ffe7720 → d6e472df`: built ON TOP of the
  live cascade tip `6ffe7720` (which repinned the manifest correctly but left
  `nota-next-derive` in the lock). Full `cargo generate-lockfile` cleared it →
  `nota-derive`. Only `Cargo.lock` changed. 0 residue. `main@origin c209de61`
  unchanged.
- **repository-ledger** `502d77e → 54311fdc`: `502d77e` was a bare rename with
  ALL producers at `branch=main` (fully un-cascaded — the Tier1 fix `97386393`
  had been clobbered off origin). Repinned 5 producers `main→drop-next`
  (meta-signal-repository-ledger, signal-repository-ledger, signal-frame,
  triad-runtime, schema-rust; nota stays main); full `cargo generate-lockfile`
  resolved cleanly (LOCK CLEAN); regenerated `src/schema/daemon.rs`
  (`REPOSITORY_LEDGER_UPDATE_SCHEMA_ARTIFACTS=1 cargo build --locked` → **exit 0,
  compiled fully green in 25.5s** through schema-rust@982fe099) so the header
  flipped `schema-rust-next → schema-rust`; updated the stale `tests/store.rs:537`
  freshness assertion. Only remaining `-next` is the authorized `src/daemon.rs:6`
  `//!` doc comment. `main@origin ca23437` unchanged.

## LIVE-RESIDUE, CASCADE-BLOCKED — cannot be fixed by a single-repo re-lock

Both carry live lock residue whose clean re-lock is blocked by an INCOMPLETE
cascade in a DIFFERENT repo (a producer still pinned at `branch=main`). A full
`cargo generate-lockfile` was attempted on each and fails / cannot converge:

- **system** `f112d19a` — lock `nota-next-derive` + stale artifact. Full regen
  fails: `system → meta-signal-system@drop-next → signal-system@main` (97ca223e),
  and `signal-system@main` still declares the RETIRED dependency key `nota-next`
  → `error: no matching package named 'nota-next' found` (the renamed `nota.git`
  publishes `nota`, not `nota-next`). Root fix: `meta-signal-system` must repin
  `signal-system main→drop-next` (its `Cargo.toml:21` currently pins
  `signal-system@main`). Blocked one level up.
- **meta-signal-spirit** `b05464fd` — lock has `nota-next.git@main` +
  `schema-next.git@main` + `schema-cc@schema-next.git`. Its own `Cargo.toml` is
  correctly cascaded (signal-frame/signal-spirit/schema-rust all `drop-next`), but
  `signal-spirit@drop-next` (bbd9b32) pins `schema`/`schema-rust` at `branch=main`;
  `schema@main 9af2c546` declares `nota` via `nota-next.git`, reintroducing the
  old URL as a distinct cargo source. Root fix: `signal-spirit@drop-next` must
  repin `schema`/`schema-rust` `main→drop-next`. Blocked one level up.

## STALE-ARTIFACT-ONLY (regen-convergence items, NOT dep/URL/lock/use-path residue)

Complete `@generated by schema-rust-next` set at authoritative tips = persona,
repository-ledger (FIXED), signal-terminal, system (blocked, above). Standalone:

- **persona** — see §persona. Regenerate `src/schema/daemon.rs` against
  `schema-rust@drop-next`. Its Cargo deps are already canonical.
- **signal-terminal** `4f75a6c` — `src/schema/lib.rs` header stale; Cargo.toml/
  lock CLEAN. Working copy is peer-dirty (leftover regen state) — NOT touched.

These belong to the stale-generated-artifact regen convergence (the Tier2-Regen
bead's class), not the dep-residue sweep. The current `schema-rust` emitter writes
`@generated by schema-rust`, so a regen pass clears them.

## AUTHORIZED LITERALS (40 repos, left exactly as-is)

Spot-verified (signal, nexus, nota, horizon-rs, signal-frame, upgrade, schema):
0 build-critical toml/lock/flake family lines. The family tokens in these repos
are exclusively:

- **dependency_boundary NEGATIVE guards** — `for forbidden in ["nota-next",
  "nota-codec", "signal-core"] { !tree.contains(...) }` (signal-lojix,
  meta-signal-lojix, signal-upgrade, meta-signal-upgrade, upgrade). LEFT.
- **doc comments** (`//!`, `///`, `//`) referencing the old crate/generator names
  (harness, introspect, orchestrate, system, terminal-cell, criome, message,
  mentci-lib, nexus, signal, signal-frame, signal-terminal, triad-runtime,
  clavifaber, horizon-rs, …). "docs/historical strings." LEFT.
- **error-message strings** (`#[error("nota-next: {0}")]` in nexus). LEFT.
- **nota/schema in-repo test-data + NOTA fixtures** (nota `tests/codec.rs`
  `schema-next:core` strings; mentci `schema/*.schema` `;;` comments). LEFT.
- **.md docs** (ARCHITECTURE.md, README.md, DESIGN.md, skills.md). LEFT.

Two flagged NON-blocking, out-of-strict-scope literals (reported, not fixed):
(a) **stale POSITIVE guard-test assertions** `tree.contains("nota-next")` at
signal-lojix / meta-signal-lojix / upgrade `dependency_boundary.rs:39-40` — these
would FAIL post-rename (the tree contains `nota`, not `nota-next`); they are
build-breaking test literals, not dep/URL/lock/use-path residue, and are tracked
guard-test items (bead zy24 class); (b) orchestrate `tests/ledger.rs:636-637`
hardcoded paths `/home/li/wt/.../schema-next/schema-help`.

## No live `use *_next` module paths in any in-scope repo

Every underscore-form hit (`nota_next::`, `schema_rust_next::`, `use *_next`) in
the 84 in-scope repos is in `.md`/`docs`/`.schema` comment text. The ONLY repo
with live `use nota_next` / `extern crate nota_next` / `derive(nota_next::…)`
across `src/` is **mind** (excluded, KNOWN-PENDING).

## ROOT CAUSE (systemic) — why residue persists on origin/drop-next

The graph's `drop-next` branches are in a PARTIALLY-cascaded state. A bare
rename-propagator re-stage was pushed to several repos' `origin/drop-next`
(token-renaming manifests but keeping `branch=main` producer pins and NOT
regenerating locks), which (a) left stale `nota-next-derive`/old-URL locks, and
(b) **clobbered prior careful fixes** — repository-ledger's Tier1 `97386393` and
signal-repository-ledger's `2bc09662` are no longer the origin tips. The
concurrent synchronizer cascade advances tips but its incremental
`cargo update -p` bumps do NOT clear `nota-next-derive`, and it cannot converge
repos whose producers are pinned at `branch=main` where the main still uses the
retired `nota-next` key/URL. **The durable fix is a completed whole-graph
staged-cascade that repins every producer to `drop-next` AND does a full lock
regeneration** — not incremental bumps, and not hand-patching (which the re-stage
re-clobbers). system + meta-signal-spirit will clear once their one-level-up
producers (meta-signal-system, signal-spirit) complete their cascade.

## Safety / boundaries

- No migration `main` moved. Fixed tips are fast-forward `drop-next` advances;
  each repo's `main@origin` re-checked after push (f3a5150e / c209de61 / ca23437
  — all unchanged). Producer mains untouched (`nota bea7e284`, `schema 9af2c546`).
- Each edited repo claimed via Orchestrate lane `rename-propagator`, released
  after. Roster showed only one live peer claim all session (`cloud-maintainer`
  Codex on `cloud` — not touched; also confirmed stale by Cloud-Stage evidence).
- jj inline messages only; no editor; no stray workspaces; work done in the
  canonical checkouts on fresh changes over the authoritative origin tip.
- Zero project/private data on any public surface.

## Honesty caveats — what I did NOT fully verify

- The graph is a MOVING target (live cascade). My fixed tips were the origin head
  at push time and verified to stand at push; a later cascade re-push could
  supersede them (the same mechanism that clobbered Tier1). Not re-polled after.
- `mind`, `spirit` residue reported by count only (excluded, not swept in detail).
- I verified FIXED repos by residue grep (task bar) + one full green build
  (repository-ledger). I did NOT run prometheus composed builds of the other two.
- `system` + `meta-signal-spirit` are reported BLOCKED, not fixed — I did not
  chase their multi-repo cascade chains (meta-signal-system, signal-spirit, and
  possibly deeper) to avoid pushing partial/re-clobberable state into a live
  convergence.
