# cloud — de-`next` stage + verify evidence (bead `primary-fv4l`, cloud slice)

Session: RenamePropagator. Role: General Code Implementer (Claude Opus 4.8, 1M,
high reasoning). Date: 2026-07-04. Scope: bring the held-out `cloud` repo
(github.com/LiGoldragon/cloud) to the same staged state as the rest of the
`-next`→canonical graph, on its own `drop-next` branch. Producers resolved
against: `schema-rust@drop-next 7f746c02`, `schema@drop-next a393c8c8`,
`nota@main bea7e284`.

**GATE HELD: cloud `main@origin` is UNCHANGED. All work is on `drop-next` only.**

- `cloud` `drop-next` tip (pushed): **`a1921eb8ab7b928a4d379b5eb7b3e2701125a2d2`**
- `cloud` `main@origin`: **`704213b0`** — unchanged by this work (see §6).

## 1. Claim state (honest) — proceeded on a stale peer claim, psyche-confirmed free

`(Observe Roles)` still shows a live `cloud-maintainer` (Codex) claim on
`(Path /git/github.com/LiGoldragon/cloud)` reason `[fix DigitalOcean gopass
credential path]`. My `(Claim (rename-propagator …))` was **rejected**
(`ClaimRejection`) because that stale claim persists in the roster. The psyche
confirmed (relayed by dispatch) that nobody is working on `cloud` now, and the
canonical checkout is genuinely idle: working copy clean (`jj status` = no
changes), last real commit 2026-06-19, no dirty peer WIP. I proceeded on
`drop-next` in the canonical checkout (psyche-authorized, no checkout sharing
risk, no stray workspace), never touching `main`. **Coordination note for the
orchestrator: the `cloud-maintainer` Codex claim should be released — it is
stale.**

## 2. Silent-skip bug not triggered — no `main*` divergence

The rename-propagator silent-skip precondition (a local jj `main*` divergence)
does NOT hold for `cloud`: at entry `main`, `main@git`, `main@origin` were all
`efae1776`. I staged the rename by hand (explicit edits, not a blind tool run)
and confirmed the edit landed by re-reading the pushed tree (§4, §5). No silent
skip.

## 3. Rename staged onto `drop-next`

Base: cloud `main`. (Remote `main` fast-forwarded `efae1776 → 704213b0`,
one concurrent li@ docs commit "remove INTENT.md", mid-session; I rebased
`drop-next` onto the current `main` `704213b0` so it does not reintroduce
`INTENT.md`.)

- **Cargo.toml**: `nota-next → nota`, `schema-rust-next → schema-rust` (dep
  keys + git URLs). Family deps repinned to `branch = "drop-next"` (6 deps:
  schema-rust, meta-signal-cloud, signal-cloud, signal-domain-criome,
  signal-frame, triad-runtime); `nota` stays `branch = "main"` (nota has no
  drop-next). Repin pattern matches the peer GREEN consumer `signal-cloud`.
- **Non-generated source use-paths** (`nota_next → nota`, `schema_rust_next →
  schema_rust`): `src/client.rs:8`, `build.rs:3`, `src/lib.rs:72`,
  `tests/runtime.rs:24`.
- **Generated files** `src/schema/{daemon,sema,nexus}.rs`: regenerated (§5), so
  their `nota_next` derives and `@generated by schema-rust-next` headers become
  `nota` / `@generated by schema-rust`.
- **Prose fix**: `src/schema_daemon.rs:7` doc comment `schema-rust-next → schema-rust`.

## 4. Schema-language drift — FIXED (`cloud` carried it too)

`cloud`'s `schema/sema.schema` used the retired named struct-field pair syntax
that `schema@a393c8c8` rejects (`RetiredStructFieldSyntax`). Applied the
established retired→positional transform to the 4 struct bodies:

| retired | current | rule |
|---|---|---|
| `RejectionReport { reason RejectionReason marker StateMarker }` | `{ reason.RejectionReason marker.StateMarker }` | field-name ≠ type-name → dotted role |
| `StateMarker { commit_sequence CommitSequence state_digest StateDigest }` | `{ CommitSequence StateDigest }` | field-name == type-name, non-scalar → bare |
| `AccountBinding { provider Provider provider_account ProviderAccount credential_handle CredentialHandle }` | `{ Provider ProviderAccount CredentialHandle }` | all equal → bare |
| `StoredPlan { plan Plan approved Boolean marker StateMarker }` | `{ Plan approved.Boolean marker.StateMarker }` | `Plan` bare; `Boolean` reserved scalar → `approved.Boolean`; `marker`≠`state_marker` → `marker.StateMarker` |

`schema/nexus.schema` has NO struct bodies (only import map + `Name Reference` /
enum decl pairs) → no drift, untouched. The transform rules were validated
byte-for-byte against the peer `signal-cloud@drop-next` applied example
(`provider Provider`→`Provider`, `host_name DomainName`→`host_name.DomainName`,
`records (Vector X)`→`records.(Vector X)`, `reason UnsupportedReason`→
`reason.UnsupportedReason`) and the reserved-scalar dotted form
(`recovered.Boolean`, `major.Integer`) seen across the converged graph.

## 5. Generated artifacts — REGENERATED (load-bearing, not cosmetic)

`cloud`'s checked-in `src/schema/*.rs` were genuinely stale against the current
`schema-rust@drop-next 7f746c02` generator (not merely `nota`-renamed):
regeneration changed `nexus.rs` by +144 and `sema.rs` by +184 lines (daemon.rs
header-only). Regenerated via `cloud`'s `build.rs`
(`GenerationDriver…write_or_check("CLOUD_UPDATE_SCHEMA_ARTIFACTS")`) driven by
`CLOUD_UPDATE_SCHEMA_ARTIFACTS=1`. Post-regen: **0 `nota_next`** in all
generated files; headers `// @generated by schema-rust`.

**cloud's own freshness gate PASSES**: a `cargo clean -p cloud` + rebuild with
`CLOUD_UPDATE_SCHEMA_ARTIFACTS` UNSET (build.rs in freshness-CHECK mode) against
the committed artifacts finished green (2.64s, no `StaleGeneratedArtifact`
panic). cloud's generation is correct and fresh.

## 6. Residue scan (final committed tree) — 0 live `-next`

- **0 live `-next` Cargo deps** in `Cargo.toml` (git URLs / bare family keys).
- **0 `-next` sources** in `Cargo.lock`.
- **0 live `use *_next` / `*_next::`** across ALL rust source (incl. generated).
- Cargo.lock repinned, no `-next`, exact drop-next tips: `nota@main bea7e284`,
  `schema-rust@drop-next 7f746c02`, `schema@drop-next a393c8c8`,
  `signal-cloud@988c769d`, `meta-signal-cloud@4b95bc7e`,
  `signal-domain-criome@00f43ba8`, `signal-frame@44d22a07`,
  `triad-runtime@f9a6a7c8`.
- Only residual `-next` substring after the one prose fix: none live; all
  authorized (the `branch = "drop-next"` repins). Full-tree scan clean.

## 7. Main-integrity gate — HELD

- Remote `main` = `704213b0` both before my push and after (verified by
  `git ls-remote`). My `jj git push --bookmark drop-next` reported only
  `Add bookmark drop-next to a1921eb8`; it did not push or move `main`.
- The `efae1776 → 704213b0` advance was a concurrent li@ fast-forward docs
  commit ("remove INTENT.md"), NOT authored by this work.
- No `jj` command targeted `main`; `drop-next` is a fresh bookmark based on
  `main`.

## 8. Build verification

**cloud's OWN correctness — VERIFIED GREEN.** With every relevant
`*_UPDATE_SCHEMA_ARTIFACTS` set (so the whole tree, including the not-yet-
regenerated upstream deps, regenerates in the local cargo cache), `cargo check
--locked` compiled **`cloud v0.2.0` green** (`Finished dev in 9.82s`,
exit 0). So `cloud`'s rename + schema fix + regenerated artifacts + repinned
lock are internally correct and coherent.

**Standalone composed build — BLOCKED UPSTREAM (convergence-cascade, not a
cloud defect).** A plain `cargo check --locked` (and the authoritative
`nix build github:LiGoldragon/cloud/<rev>#…default` on the prometheus remote
builder — see below) fails at **`signal-domain-criome@drop-next 00f43ba8`**,
whose `build.rs:31` panics `StaleGeneratedArtifact { path: ".../src/schema/
lib.rs", update_environment_variable: "SIGNAL_DOMAIN_CRIOME_UPDATE_SCHEMA_
ARTIFACTS" }`. That dep's checked-in artifacts are stale against
`schema-rust@drop-next` and its regeneration pass has NOT run (its `drop-next`
tip `00f43ba8` is byte-identical to the Tier2 evidence's "PAST-SCHEMA /
StaleGeneratedArtifact" state). The producers (`schema-rust`, `schema`, `nota`)
and `signal-frame`/`triad-runtime` build fine; the block is purely the upstream
stale-artifact regen that the whole-graph convergence owns.

Conclusion: **cloud is correctly staged and self-verified; composed green will
follow at the convergence cascade** once `signal-domain-criome` (and the sibling
`signal-cloud` / `meta-signal-cloud`) have their generated artifacts regenerated
against `schema-rust@drop-next`. This is exactly the "verify standalone; composed
re-verify at convergence" case the brief anticipated.

### Authoritative Nix build (durable evidence)

`nix build github:LiGoldragon/cloud/a1921eb8#packages.x86_64-linux.default -L`
run locally with prometheus as the remote builder (`NIX_EXIT=1`):

- **Vendor coherence PROVEN.** `vendor-registry.drv` + `vendor-cargo-deps.drv`
  built **locally** (this credentialed machine) and copied to prometheus. crane
  vendored the ENTIRE dep set from the repinned `Cargo.lock` with no `-next`
  fetch and no `--locked` source-map conflict — i.e. the rename + repin + lock
  are coherent (the un-rewritten-producer-main class of failure from the earlier
  StageAndVerify does NOT occur for cloud). The remote-builder flow (local FOD
  fetch → prometheus compile) worked; no prometheus credential gap hit.
- **Composed block is the crane `cloud-deps-0.2.0.drv` (buildDepsOnly) step on
  prometheus**, failing at BOTH `signal-domain-criome@drop-next 00f43ba8` and
  `signal-cloud@drop-next 988c769d` `build.rs:31` `StaleGeneratedArtifact`. It
  fails while building cloud's DEPENDENCIES — before cloud's own crate — so it is
  unambiguously an upstream regen gap, not cloud code. Expected (unrealized)
  output would be `/nix/store/ph60dbzad7sa8gqmiqgzabznr08wr6h6-cloud-0.2.0`.

## 9. Status set

`primary-fv4l` cloud slice: **cloud staged on `drop-next` + self-verified
green; composed-blocked upstream on `signal-domain-criome` regen (convergence
item).** The `mind` and `spirit` slices of `primary-fv4l` are untouched by this
work.

## 10. Checks run

- `(Observe Roles)`, `(Claim …)` → ClaimRejection (stale peer claim).
- `git ls-remote` for producer/dep drop-next tips + main integrity.
- `cargo generate-lockfile` (repin) — exit 0, lock residue-free.
- `cargo check --locked` with all `*_UPDATE_SCHEMA_ARTIFACTS` → **cloud GREEN**.
- `cargo clean -p cloud` + `cargo check` (CLOUD_UPDATE unset) → cloud freshness
  gate **passes**.
- `cargo check --locked` (no update vars) → composed-blocked at
  `signal-domain-criome` StaleGeneratedArtifact.
- `nix build github:LiGoldragon/cloud/a1921eb8#…default` (remote builder) →
  authoritative durable result recorded above.
- `jj git push --bookmark drop-next` → drop-next `a1921eb8`; main unchanged.
</content>
</invoke>
