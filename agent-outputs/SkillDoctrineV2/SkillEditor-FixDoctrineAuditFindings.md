# Fix Doctrine Audit Findings

## Task And Scope

Task: FixDoctrineAuditFindings for SkillDoctrineV2.

Scope was limited to skill-system source modules, role source, manifests, generated primary outputs, and verification scans for the audit findings:

- stale generated-role `skills/*.md` references from `bead-weaver` and `architectural-truth-tests`;
- auditor/repo-operator role packets inheriting implementation edit authority from `code-implementation-core`;
- stale V1 wording in intent-translator role source and manifest comments;
- generated-output reconciliation and focused stale-reference scans.

Concurrent changes were already present in both `/git/github.com/LiGoldragon/skills` and `/home/li/primary`. I did not revert or normalize unrelated work.

## Source Changes

Changed source files:

- `/git/github.com/LiGoldragon/skills/modules/bead-weaver/full.md`: replaced `skills/*.md` reads and See also entries with generated harness skill names and dispatch-envelope wording.
- `/git/github.com/LiGoldragon/skills/modules/architectural-truth-tests/full.md`: replaced See also `skills/*.md` references with module/skill names.
- `/git/github.com/LiGoldragon/skills/manifests/active-outputs.nota`: changed the manifest comment from V1 to SkillDoctrineV2; removed `code-implementation-core` from `rust-auditor`, `nix-auditor`, and `repo-operator` role bundle assignments.
- `/git/github.com/LiGoldragon/skills/manifests/module-dependencies.nota`: changed the dependency-index comment from V1 to SkillDoctrineV2.
- `/git/github.com/LiGoldragon/skills/roles/intent-translator/full.md`: removed stale V1 wording from the lead/orchestrator boundary.

No new source module was added. The authority fix uses bundle assignment: auditors/operators keep their role prose and specialized review/operation modules, but no longer receive the implementation core that says a worker owns code edits.

## Generated Outputs

Command run from `/git/github.com/LiGoldragon/skills`:

```sh
SKILLS_SOURCE_ROOT=$PWD SKILLS_WORKSPACE_ROOT=/home/li/primary cargo run -- skills-generate.nota
```

Result: pass, exit 0. The generator emitted all active skill and role outputs, including:

- `/home/li/primary/.agents/skills/architectural-truth-tests/SKILL.md`
- `/home/li/primary/.agents/skills/bead-weaver/SKILL.md`
- `/home/li/primary/.claude/skills/architectural-truth-tests/SKILL.md`
- `/home/li/primary/.claude/skills/bead-weaver/SKILL.md`
- all active role packets under `/home/li/primary/.claude/agents/`, `/home/li/primary/.codex/agents/`, and `/home/li/primary/.pi/agents/`
- `/home/li/primary/skills/generated-role-outputs.nota`

The generator rewrote the full active role surface, so primary shows role-packet drift beyond the three role bundles touched here. That drift reflects generating from the current source tree, including pre-existing pending source changes.

## Verification

Command run from `/git/github.com/LiGoldragon/skills`:

```sh
SKILLS_SOURCE_ROOT=$PWD SKILLS_WORKSPACE_ROOT=/home/li/primary cargo run -- skills-check.nota
```

Result: pass, exit 0.

Focused scans:

- `rg -n "skills/[A-Za-z0-9_-]+\\.md" /home/li/primary/.claude/agents /home/li/primary/.codex/agents /home/li/primary/.pi/agents /home/li/primary/skills/generated-role-outputs.nota`
  - Result: no matches, exit 1.
- `rg -n "Implementation Core Purpose|worker owns local understanding|code-implementation-core" <auditor/operator generated packets> /home/li/primary/skills/generated-role-outputs.nota`
  - Result: no matches, exit 1.
- `rg -n "V1" manifests/active-outputs.nota manifests/module-dependencies.nota roles/intent-translator/full.md /home/li/primary/.claude/agents/intent-translator.md /home/li/primary/.codex/agents/intent-translator.toml /home/li/primary/.pi/agents/intent-translator.md`
  - Result: no matches, exit 1.
- `rg -n "skills\\.nota" <generated role packets, generated role index, generated skill outputs, manifests, modules, roles>`
  - Result: no matches, exit 1.
- `rg -n "runtime[^\n]{0,80}discover|discover[^\n]{0,80}runtime|skill[^\n]{0,80}discover|discover[^\n]{0,80}skill|skill[- ]index|runtime[- ]discovery" <generated role packets, generated role index, manifests, modules, roles>`
  - Result: no matches, exit 1.
- `rg -n "(?i)(generated by|generated file|auto-generated|autogenerated|do not edit this file|this file is generated)" <generated role packets and touched generated skill outputs>`
  - Result: no matches, exit 1.
- `awk 'match($0, /(modules|roles)\/[^ )]+/) { path=substr($0, RSTART, RLENGTH); if (system("test -e " path) != 0) print path }' manifests/module-dependencies.nota`
  - Result: no output, exit 0; every dependency-index source path exists.
- Per-file heading uniqueness check on touched source files:
  - Result: no output, exit 0; no duplicate headings within each touched source file.

Source pre-scan after edits:

- `rg -n "V1|skills/[A-Za-z0-9_-]+\\.md|code-implementation-core" manifests/active-outputs.nota manifests/module-dependencies.nota roles/intent-translator/full.md modules/bead-weaver/full.md modules/architectural-truth-tests/full.md`
  - Result: only intentional `code-implementation-core` entries for implementer/scaffolder roles and the dependency-index module record remained.

## Remaining Tradeoffs

I did not create a separate review/verification core. The narrower bundle fix fully removes the edit-authority leak from auditor/operator generated packets while avoiding new doctrine surface area. If later packets need shared non-editing review procedure beyond the existing role prose, a dedicated review core would be the next source-owned place for it.

No commit or push was performed.
